# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

## Findings

- `cpp/` 重构实现当前已存在于工作树：`cpp/init.go`、`cpp/state.go` 新增，`cpp/cpp.go` 中初始化与共享状态已拆分出去；`./maze --build` 也通过，说明本地未提交代码在构建层面成立。
- 但从分支交付视角看，`git diff --name-only origin/master...HEAD` 仍未包含任何 `cpp/` 或 `common/utils.go` 相关改动，说明本次 issue 的核心实现尚未进入分支提交范围，当前 HEAD 仍不能作为可交付实现通过验收。
- 关键回归样例 `cpp/20260225-cpp-globals-and-weak`、`cpp/20260225-cpp-vtable-types`、`cpp/20260225-cpp-containers`、`cpp/20260225-cpp-smart-ptr`、`cpp/20260304-stack-multithread` 全部通过，说明就本地工作树而言，`cpp/` 拆分暂未引入明显 C++ 主路径回归。
- `python/20260129-complex-types-311` 通过，说明 AC6 中 Python 3.x 兼容性抽样未见退化。
- `nodejs/20260225-maze-vs-heapsnapshot` 仍失败；本次 `summary.total_objects=61802`、`summary.total_size=31246832` 已为有效值，说明此前 summary 为空的问题已修复，但 `Object / RegExp / ArrowFunction / URL` 等类型与 heapsnapshot 的数量差异仍显著超阈值，Node.js 准确性验收仍未闭环。

## 详细结果

### TC-001: 全局符号与 weak malloc 路径回归
- **结果**: PASS
- **实际行为**: `cpp/20260225-cpp-globals-and-weak` 用例通过，`Record`、`Point3D`、`Node` 等对象统计正常。
- **关键指标**: `Record=5000`，known/topN 比例约 `46.0%`，总结果项 `28`
- **备注**: 存在符号/ctypes 的 S3 下载失败日志，但未影响结果通过。

### TC-002: vtable 对象识别回归
- **结果**: PASS
- **实际行为**: `cpp/20260225-cpp-vtable-types` 用例通过，`Dog`、`Cat`、`GoldFish` 均按预期识别，vtable 分类主路径可用。
- **关键指标**: `Dog=10000`，`Cat=5000`，`GoldFish=3000`，Distinct animal types=`5`
- **备注**: 未见对象漏报或类型名退化。

### TC-003: STL 容器分类主路径回归
- **结果**: PASS
- **实际行为**: `cpp/20260225-cpp-containers` 用例通过，`Widget`、`Session`、`TaskQueue` 均按预期识别。
- **关键指标**: `Widget=5000`，`Session=2000`，`TaskQueue=1000`
- **备注**: 容器展开与对象区分未见明显退化。

### TC-005: 智能指针场景回归（抽样 smart-ptr）
- **结果**: PASS
- **实际行为**: `cpp/20260225-cpp-smart-ptr` 用例通过，`Player`、`Bullet`、`Effect` 统计符合基线。
- **关键指标**: `Player=3001`，`Bullet=5001`，`Effect=4001`
- **备注**: 本次仅执行 test-cases 中抽样的 `smart-ptr` 子用例，未扩展到 `multi-inherit` / `string`。

### TC-006: 栈局部变量收集路径回归
- **结果**: PASS
- **实际行为**: `cpp/20260304-stack-multithread` 用例通过，成功识别 `ThreadObject`。
- **关键指标**: `ThreadObject=30`，总结果项 `12`
- **备注**: 未见 panic / traceback。

### TC-008: 准确性对比非回归（Maze vs Heapsnapshot）
- **结果**: FAIL
- **实际行为**: 用例已能正常执行 `validate.py`，且 JSON `summary.total_objects` / `summary.total_size` 为有效值；但 `Object / RegExp / ArrowFunction / URL` 等类型与 heapsnapshot 的数量差异仍超阈值，整体校验失败。
- **关键指标**:
  - `Object: maze=501, heap=5610`
  - `RegExp: maze=550, heap=3`
  - `ArrowFunction: maze=1502, heap=1`
  - `URL: maze=500, heap=4`
  - `Matched mappings: 16 / 20`
  - `summary.total_objects=61802`, `summary.total_size=31246832`
- **备注**: Node.js 20.20.0 结构加载成功；失败已从“summary 无效”收敛为“真实准确性偏差”。

### TC-009: Python 复杂对象场景兼容性非回归
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 顺序执行后通过，复杂对象统计正常。
- **关键指标**: `pymempool_objects=68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`
- **备注**: 说明当前本地工作树未破坏 Python 3.11 主路径。

## 失败的 Test Case

### TC-008: 准确性对比非回归（Maze vs Heapsnapshot）
- **结果**: FAIL
- **预期行为**: `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 应通过 Maze 与 heapsnapshot 的数量/体积对比校验，且 summary 字段有效。
- **实际行为**: summary 字段已有效，但 `Object / RegExp / ArrowFunction / URL` 等映射类型计数差异超阈值，最终校验失败。
- **复现步骤**:
  1. 在仓库根目录执行 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
  2. 等待 Maze 完成 coredump 分析
  3. 观察验证阶段输出的映射差异与失败结论
- **可能原因**:
  - Node.js 类型聚合/命名策略与 heapsnapshot 映射仍不一致
  - 某些 `formatResultType` 结果仍未正确归并到预期对象类型
  - `RegExp` / `ArrowFunction` / `URL` 等类型可能存在误归类或过度聚合
- **建议修复**: 优先检查 Node.js 类型格式化、对象归类和 heapsnapshot 映射规则，重点排查 `Object / RegExp / ArrowFunction / URL` 的统计来源。

## 代码与实现检查

- **计划符合性检查**: `dev-flow/plan.md` 要求“保持功能不变重构 cpp package”。当前工作树中确实有对应实现：`cpp/init.go`、`cpp/state.go` 新增，`cpp/cpp.go` 中初始化/共享状态已拆分。
- **交付完整性检查**: `git diff --name-only origin/master...HEAD` 未包含任何 `cpp/` 或 `common/utils.go` 文件；说明这些实现仍停留在未提交工作树，尚未成为分支交付物。
- **实现逻辑抽查**:
  - `cpp/init.go` 中存在 `InitCpp`、`WaitCppInited`、collector 注册、fake symbol 初始化、忽略类型过滤与初始化流程。
  - `cpp/state.go` 中集中管理 `TYPE_CODE_*` 常量、共享状态变量与并发字段。
  - `common/utils.go` 已为 JSON 输出补充 `summary.total_objects`、`summary.total_size`，并在结果汇总阶段累计。

## 测试结果汇总

- 已执行用例数: 7
- 通过: 6
- 失败: 1

已执行命令：

```bash
./maze --build
python3 testdata/run_test.py cpp/20260225-cpp-globals-and-weak
python3 testdata/run_test.py cpp/20260225-cpp-vtable-types
python3 testdata/run_test.py cpp/20260225-cpp-containers
python3 testdata/run_test.py cpp/20260225-cpp-smart-ptr
python3 testdata/run_test.py cpp/20260304-stack-multithread
python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot
python3 testdata/run_test.py python/20260129-complex-types-311
```

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不通过验收**。原因有二：
  1. `nodejs/20260225-maze-vs-heapsnapshot` 仍存在真实准确性失败，AC6 未闭环；
  2. `cpp/` 重构与 `common/utils.go` 修复虽然已出现在工作树并通过本地回归，但尚未进入 `origin/master...HEAD` 的分支提交范围，当前分支交付物仍不完整。
