# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。`./maze --build`、关键 Go 回归测试、Python 2/3 脚本语法检查以及代表性 `testdata` 动态用例均通过；主链路的 `db/` 输出也已通过静态和动态方式验证生效。

但当前实现仍存在一项未闭合问题：`qa/issue110/prepare_migration_test.go:156` 继续显式读取 `filepath.Join(legacyRoot, "postman-db")`。这与计划文档 AC5“迁移测试覆盖旧目录输入兼容 + 新目录输出生效”、以及当前需求上下文中“当前回归链路应收口到 `db/` 语义”的目标不一致，因此总体结论为 FAIL。

另有一项低成本检查发现：`dev-flow/test-cases.md` 声明新增测试文件为 `qa/issue153/db_path_migration_test.go`，但仓库中实际不存在该文件，当前对应覆盖落在 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go`。这不单独构成实现失败，但说明测试报告与实际测试文件清单未完全同步。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 常量已切换为 `db`；`prepare/args.go` 中 `BuildOutputDir()` 默认输出目录为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 常量值 `db`；默认目录模板 `./db/demo-tester-42`
- **备注**: `prepare/db_path_behavior_test.go` 中 `TestBuildOutputDirDefaultsToDB`、`TestCurrentPrepareDBRoot` 与源码一致。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅优先查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切换到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线输出。
- **关键指标**: 失败位置 `qa/issue110/prepare_migration_test.go:156`
- **备注**: 业务主链路基本收口到 `db/`，但迁移回归测试仍保留旧目录静态引用，导致 AC5 未闭合。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中结果回收逻辑使用 `filepath.Join("db", "*", "maze.log")` 与 `filepath.Join("db", "*", "maze.py.log")` 复制日志；`main_db_layout_test.go` 已覆盖从 `db/` 目录读取日志的行为。
- **关键指标**: `main.go:868-869` 使用 `db/*/maze.log` 与 `db/*/maze.py.log`
- **备注**: 与 AC3 一致。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py` 已使用 `POSTMANDB = "./db"`、`POSTMANDB_ELF = "./db/elf"`；相关脚本语法检查通过。
- **关键指标**: Python 3 `py_compile` 通过；Python 2 `py_compile` 通过
- **备注**: 仅完成语法级兼容验证，尚未覆盖 Python 2.7 下的完整动态执行链路。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md` 中中间文件示例已切换为 `db/<project>-<user>-<profileId>/`；`doc/support.md` 中未见新的 `postman-db` 路径示例。
- **关键指标**: `.gitignore:51` 为 `db/`
- **备注**: `doc/maze_py_analysis.md` 仍是历史 `.maze.py` 分析文档，但本轮抽查未发现新增 `postman-db` 路径示例。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 实际执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，通过 `maze --tar ... --text --json-output --rmlog --limit 500` 完成分析与校验。
- **关键指标**: `1 passed, 0 failed`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，可证明代表性场景在新目录规则下仍可完成结果读取。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 在 Python 3 与 Python 2 下均可通过 `py_compile`。
- **关键指标**: Python 2/3 `py_compile` 全部通过
- **备注**: 这是基础兼容性检查，不等价于 Python 2.7 运行时主流程完全通过。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前迁移回归链路应与实现一起收口到 `db/` 语义，迁移测试应正确表达“新输出落到 `db/` 且所需兼容语义明确”，不应继续以 `postman-db` 作为当前基线读取目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 查看 `compareLegacyAndPrepareSnapshot()`
  3. 定位到第 156 行
  4. 可见基线目录仍硬编码为 `postman-db`
- **可能原因**: 本轮目录迁移主要覆盖了业务主链路、日志回收、脚本与文档，但回归测试文件未同步完成收口，导致测试资产本身仍保留旧目录语义。
- **建议修复**: 由开发侧统一修正 `qa/issue110/prepare_migration_test.go` 的基线目录约定，并同步核对 `dev-flow/test-cases.md` 中测试文件清单，确保测试设计文档与实际测试实现一致。

## 其他发现

- `dev-flow/test-cases.md` 第 121 行声明测试文件为 `qa/issue153/db_path_migration_test.go`，但仓库中不存在该文件；当前实际覆盖由 `prepare/db_path_behavior_test.go` 和 `main_db_layout_test.go` 承担。建议同步更新测试报告，避免后续 QA/开发误判测试覆盖位置。

## 已执行的检查项

- 阅读最新开发报告：`dev-flow/20260316181454-dev-report.md`
- 阅读 testcase 报告：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 静态核对关键实现文件：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、`cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`.gitignore`、正式文档等
- 执行构建验证：`./maze --build`
- 执行关键 Go 回归：`go test -count=1 ./prepare ./qa/issue110 .`
- 执行 Python 2/3 语法兼容检查：`python3 -m py_compile ... && python2 -m py_compile ...`
- 执行代表性动态用例：`python3 testdata/run_test.py python/20260129-complex-types-311`

## 未执行项

- Python 2.7 下完整动态运行链路验证
  - **未执行原因**: 本轮优先完成低成本静态核对、构建、关键 Go 回归与至少一个代表性动态用例；现有失败已足以判定总体 FAIL，继续做更高成本动态链路验证收益有限。
  - **影响范围**: AC6
- 不同 glibc 版本验证
  - **未执行原因**: 当前环境单一，且本轮已有明确失败点。
  - **影响范围**: AC2、AC6
- 不同 Node.js 版本验证
  - **未执行原因**: 本需求主链路已可由当前失败点与代表性 Python 用例覆盖核心结论，继续扩展 Node.js 版本回归不改变总体结论。
  - **影响范围**: AC2、AC8
- 大内存性能场景验证
  - **未执行原因**: 本轮目标为功能验收，且当前已有功能性未闭合项。
  - **影响范围**: 性能附加场景，不影响本轮 FAIL 结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果摘要

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile ...`：通过
- `python2 -m py_compile ...`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不通过发布验收**。请先修正 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的静态引用，并同步更新 testcase 报告中的测试文件清单后再复验。
