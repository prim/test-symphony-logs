## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings
- 最新 human review 明确要求“不要兼容 `./postman-db/`”，但当前回归链路仍未完全收口：`qa/issue110/prepare_migration_test.go:156` 依然从 `filepath.Join(legacyRoot, "postman-db")` 读取历史基线目录。
- 主运行链路迁移基本完成：Go 常量、prepare/process、主程序日志回收、Python/GDB 工具、`.gitignore` 与正式文档均已切到 `db/` 语义。
- 构建、关键 Go 测试、Python 2/3 语法检查以及至少一个代表性 testdata 动态用例均通过，但由于 AC5/TC-002 的回归链路仍含旧目录依赖，整体不能判定为 PASS。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `db`；`prepare/args.go` 通过 `currentPrepareDBRoot(".")` 生成默认输出目录，行为测试期望为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 未发现默认输出仍指向 `postman-db/`。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线目录也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍显式从 `filepath.Join(legacyRoot, "postman-db")` 读取基线结果。
- **关键指标**: `go test -count=1 ./qa/issue110` 虽通过，但测试通过前提仍包含旧目录夹具依赖。
- **备注**: 该项与最新 human review“不要兼容 `./postman-db/`”直接冲突，因此判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `prepareTarPostmanLayout()` 已在 tar 模式下创建 `db/` 结构；`copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志。
- **关键指标**: 根包测试通过；新增 `main_db_layout_test.go` 覆盖日志复制与目录结构。
- **备注**: `main_db_layout_test.go` 同时验证未创建 `postman-db` 输出目录。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 均改为使用 `db/` 路径。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 静态检查未见运行期 Python/GDB 工具继续读取 `./postman-db`。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；抽检 `doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 均已使用 `db/` 示例路径。
- **关键指标**: 静态检查通过。
- **备注**: 历史 dev-log 中保留旧词汇属于历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 成功；测试执行与结果读取正常，未见对 `postman-db/` 的依赖报错。
- **关键指标**: 1 个代表性 testdata 用例通过；`pymempool_objects = 68626`，PersonClass=1000，GameEntity=300，TreeNode=600，SimpleClass=500。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本在 Python 2.7 与 Python 3.x 下均可通过语法编译检查。
- **关键指标**: `python2 -m py_compile ...` 与 `python3 -m py_compile ...` 均通过。
- **备注**: 本轮完成的是基础兼容性检查，不代表已完成 Python 2.7 下完整动态链路验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review，回归链路也应全面收口到 `db/`，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 可见第 156 行仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 运行 `go test -count=1 ./qa/issue110`，测试虽通过，但其断言链路仍建立在旧目录夹具之上。
- **可能原因**: 本轮实现已收口主流程与 snapshot-only 代码路径，但没有同步收口该回归测试夹具路径，导致实现语义与验收语义不一致。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的基线路径与相应夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- 未追加执行 Node.js 与 C++ 其他代表性 testdata 用例；原因是阶段 A/B 已足够定位当前阻塞点，继续执行高成本动态测试对 AC5 的结论没有增益。
- 未执行 Python 2.7 下完整动态 testdata 链路；本轮仅完成语法兼容性检查。
- 以上未执行项可能影响 AC6、AC8 的更广覆盖结论，但不影响当前因 AC5 失败而整体 FAIL 的判定。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
