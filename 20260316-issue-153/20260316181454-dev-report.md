# 开发报告

## 完成的任务
- [x] Task 1: 再次阅读最新 QA 报告、code review 报告与计划文档，确认唯一未闭合项仍是 `qa/issue110/prepare_migration_test.go` 中对旧目录 `postman-db` 的静态引用。
- [x] Task 2: 复核当前源码、正式文档与新增测试覆盖，确认业务主链路、日志回收、Python/GDB 工具链与 `main_db_layout_test.go` 仍统一使用 `db/`。
- [x] Task 3: 重新执行构建、Go 回归、Python 2/3 语法检查与代表性动态用例验证，并将本轮结论追加到正式 dev-log。

## 修改的文件
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 追加 fix iteration #17 复核记录，整理已完成项、未完成项、未验证项与 BLOCKED 结论。

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`
- 测试结果: 通过
- 测试命令: `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`
- 测试结果: 通过
- 测试命令: `go test -count=1 ./prepare ./qa/issue110 .`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，`1 passed, 0 failed`

## 验收核对
- AC1: PASS，运行期数据库根目录仍统一为 `db/`。
- AC2: PASS，Python/GDB 工具链与 ELF 路径常量仍指向 `db/elf`。
- AC3: PASS，`main.go` 日志回收逻辑保持从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果。
- AC4: PASS，prepare/process 与 tar 布局相关路径均保持在 `db/` 体系下。
- AC5: FAIL，`qa/issue110/prepare_migration_test.go` 仍静态引用 `filepath.Join(legacyRoot, "postman-db")`。
- AC6: PARTIAL，Python 2.7/3.x 语法检查通过，但 Python 2.7 运行时动态链路未补充验证。
- AC7: PASS，`./maze --build` 通过。
- AC8: PASS，代表性用例 `python/20260129-complex-types-311` 通过，结果文件可从 `db/` 规则下正确读取。
- AC9: PASS，`.gitignore` 已忽略 `db/`，本轮复核未发现回退。

## 剩余工作
- 未完成项: `qa/issue110/prepare_migration_test.go` 中旧目录静态引用尚未迁移到 `db/` 语义。
- 未验证项: Python 2.7 完整运行时链路、不同 glibc / Node.js 版本场景与大内存性能场景未在本轮执行。

## Self-Review 发现与修复
- 发现的问题: 本轮未发现新的源码层 `postman-db` 回退依赖；阻塞点仍集中在测试文件静态引用。
- 修复说明: 无新增源码修复，本轮仅补充正式开发记录并重新验证当前实现状态。

## 注意事项
- 当前系统约束禁止修改测试文件，因此无法直接修复 QA 指向的 `qa/issue110/prepare_migration_test.go`。
- 依据最新 QA 与 code review，当前状态应判定为 `BLOCKED`，不能宣称全部完成。
