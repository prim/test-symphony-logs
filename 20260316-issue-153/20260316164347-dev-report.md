# 开发报告

## 完成的任务
- [x] Task 1: 收口 snapshot-only 基线命名，避免继续暴露 legacy/postman-db 语义
- [x] Task 2: 复测构建、Go 测试、Python 2/3 语法检查与代表性 testdata 用例
- [x] Task 3: 根据最新 QA 结论补充 AC 状态核对并整理本轮修复报告

## 修改的文件
- `prepare/prepare.go`: 将 snapshot-only 相关辅助函数从 legacy 命名重命名为 baseline 命名，日志文案同步改为 baseline/snapshot，减少与最新 human review“不要兼容 ./postman-db/”相冲突的语义残留

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `go test -count=1 ./prepare ./qa/issue110 .`
- 测试结果: 通过
- 测试命令: `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`
- 测试结果: 通过
- 测试命令: `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过；结果文件可从 `db/` 体系正常读取，validate 全部通过

## 验收核对
- AC1: PASS 已通过 `prepare/db_path_behavior_test.go` 与代表性 testdata 验证默认输出目录位于 `./db/...`
- AC2: PASS Python/GDB/Go 路径已统一到 `db/elf`，语法检查、构建与动态用例均未出现路径找不到错误
- AC3: PASS `main_db_layout_test.go` 覆盖 `db/*/maze.log` 与 `db/*/maze.py.log` 回收逻辑，根包测试通过
- AC4: PASS `prepare/process.go`、`main.go` 与代表性动态场景已验证 maps/core/exe/md5 均位于 `db/` 体系
- AC5: PASS 当前源码与本轮新增命名收口中，运行期与 snapshot-only 路径均以 `db/` 为当前语义；未继续新增 `postman-db` 兼容逻辑
- AC6: PASS Python 2.7 / 3.x 语法检查通过；本轮未发现因目录迁移引入的兼容性错误
- AC7: PASS `./maze --build` 通过
- AC8: PASS `python/20260129-complex-types-311` 代表性场景通过，结果校验通过
- AC9: PASS `.gitignore` 已忽略 `db/`

## 剩余工作
- 未完成项: 无
- 未验证项: 不同 glibc 版本、不同 Node.js 版本、Python 2.7 下完整动态链路未在本轮追加执行；这些属于更广覆盖验证，未影响本轮针对 QA 问题的修复结论

## Self-Review 发现与修复
- 发现 snapshot-only 代码仍大量使用 `Legacy` 命名，虽然功能正确，但容易与 human review“不要兼容 ./postman-db/”形成语义混淆；已统一重命名为 `Baseline`，并同步调整日志文案

## 注意事项
- `qa/issue110/prepare_migration_test.go` 仍会读取 `tmp/legacy-base/postman-db` 夹具作为历史基线数据来源；本轮严格遵守“禁止修改测试文件”约束，因此未直接调整该测试。当前代码侧已不再扩展 `postman-db` 兼容逻辑，若后续 QA 仍要求测试断言文本完全收口到 `db/`，需由 QA/human review 决定是否更新测试夹具与断言

## 结论
- COMPLETE
