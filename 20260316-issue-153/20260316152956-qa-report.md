## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `"db"`；`prepare/args.go` 通过 `currentPrepareDBRoot(".")` 生成默认输出目录，默认路径为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `go test -count=1 ./prepare` 通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 已优先查找 `./db/*-%s.maps.gdb` 与 `db/elf/*`，同时保留 `postman-db/elf/*` 回退；`prepare/prepare.go` 与 `qa/issue110/prepare_migration_test.go` 继续保留 `tmp/legacy-base/postman-db` 历史兼容语义。
- **关键指标**: `go test -count=1 ./prepare`、`go test -count=1 ./qa/issue110` 均通过。
- **备注**: 满足 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 tar 模式布局已写入 `db/`，`copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志。
- **关键指标**: `go test -count=1 .` 通过，新增 `main_db_layout_test.go` 覆盖日志复制行为。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 已切换为 `db/` 语义；未发现新的主链路 `./postman-db` 硬编码残留。
- **关键指标**: `python2 -m py_compile ...` 与 `python3 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换为 `db/`。
- **关键指标**: 静态检视通过；残留 `postman-db` 仅见于历史说明、测试兼容夹具与历史输出样例，不属于本次正式入口文档缺陷。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已改为清理与读取 `db/` 目录；代表性用例 `python/20260129-complex-types-311` 运行通过，结果文件与日志可在新目录规则下正常处理。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；校验输出包含 `68626` 个 `pymempool_objects`，多个类型计数与预期一致。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 工具脚本在 Python 2.7 与 Python 3.x 下均可通过语法编译检查，未因路径常量迁移引入兼容性错误。
- **关键指标**: `python2 --version` 显示 `Python 2.7.18`；对应 `py_compile` 通过；Python 3 编译检查也通过。
- **备注**: 本轮完成的是脚本级兼容性基础验证，不等同于跨版本全链路运行验证。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）
- 未执行不同 glibc 版本动态验证；当前环境仅覆盖单一 Linux/glibc 运行环境，可能影响 AC6 的跨环境结论外推范围。
- 未执行不同 Node.js 版本代表性动态回归；本轮已完成代表性 Python testdata 用例，未覆盖 Node.js 版本矩阵，可能影响 AC6/兼容性扩展结论。
- 未执行大内存进程（>1GB）性能验证；本需求核心为目录迁移，当前未观察到需要继续做高成本性能回归的阻塞修复点，但 AC 中未要求性能门槛，本项仅作补充说明。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以通过本次 QA 验收。`postman-db -> db` 迁移的主链路、legacy 输入兼容、日志回收、测试执行器与正式文档已完成闭环，且已满足“至少一个代表性 testdata 用例通过”的硬性要求。

## Findings
- 已按需求完成低成本静态核对、关键 Go 回归测试、构建验证与至少一个代表性 testdata 动态验收；未发现阻断发布的问题。
- human review 指出的“不要额外增加 cmd/maze_entry.py”问题已被消除；当前分支未包含该额外入口文件。

## Test Results
- `./maze --build`：通过
- `go test -count=1 ./prepare`：通过
- `go test -count=1 .`：通过
- `go test -count=1 ./qa/issue110`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
