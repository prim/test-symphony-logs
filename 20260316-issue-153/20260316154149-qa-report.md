# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `"db"`；`prepare/args.go` 通过 `currentPrepareDBRoot(".")` 生成默认输出目录，默认路径为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `go test -count=1 ./prepare` 通过；`TestBuildOutputDirDefaultsToDB`、`TestCurrentPrepareDBRoot` 所在测试集通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 已优先查找 `./db/*-%s.maps.gdb` 与 `db/elf/*`，同时保留 `postman-db/elf/*` 回退；`prepare/prepare.go` 与 `qa/issue110/prepare_migration_test.go` 继续保留 `tmp/legacy-base/postman-db` 历史兼容语义。
- **关键指标**: `go test -count=1 ./prepare && go test -count=1 ./qa/issue110` 通过。
- **备注**: 满足 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 tar 模式布局已写入 `db/`，`copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果文件。
- **关键指标**: `go test -count=1 .` 通过；`main_db_layout_test.go` 覆盖 `prepareTarPostmanLayout()` 与日志回收行为。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 已切换为 `db/` 语义；未发现新的主链路 `./postman-db` 硬编码残留。
- **关键指标**: `python2 -m py_compile ...` 与 `python3 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换到 `db/`。
- **关键指标**: 静态检查通过；分支 diff 中相关文档与忽略规则已更新。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；输出日志明确显示 `output dir: ./db/coredump-default-2636550`，结果文件读取成功。
- **关键指标**: `1 passed, 0 failed`；`All validations passed!`。
- **备注**: 满足 AC8，并实际完成至少一个 testdata 用例验收。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在、非空，且在 Python 2.7 / Python 3 下均可通过语法编译检查；本轮 code review 指出的 Python 3 bytes/str 兼容问题已修复。
- **关键指标**: `python2 -m py_compile ...` 通过；`python3 -m py_compile ...` 通过。
- **备注**: 覆盖 AC6 的基础兼容性验证。

## 失败的 Test Case（如有）

无。

## 已执行的检查项
- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告 `dev-flow/20260316153802-dev-report.md`
- 审查分支变更与关键文件：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、Python/GDB 工具链、`.gitignore`、正式文档
- 核对 human review 约束：仓库内不存在 `cmd/maze_entry.py`
- 执行构建：`./maze --build`
- 执行 Go 回归：`go test -count=1 .`、`go test -count=1 ./prepare`、`go test -count=1 ./qa/issue110`
- 执行 Python 2/3 语法检查：`py_compile` 覆盖关键脚本
- 执行动态验收：`python3 testdata/run_test.py python/20260129-complex-types-311`
- 检查动态日志：`tmp/python-20260129-complex-types-311.maze-output.log`、`maze.log`

## 未执行项（如有）
- 不同 glibc 版本的跨环境动态验证未执行；原因：当前工作树环境单一，且本轮回归目标聚焦 human review 后的路径迁移与 Python 3 兼容修复。
- 不同 Node.js 版本的跨环境动态验证未执行；原因同上。
- 这些未执行项主要影响 AC6 的扩展兼容性外延，不影响本轮主链路验收结论。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 未发现阻断本需求验收的功能性问题。
- human review 指出的“不要额外增加 `cmd/maze_entry.py`”已满足：当前分支无该文件。
- 代表性动态用例验证显示新输出目录实际落在 `db/`，且 `maze.log` 中 ELF 解析路径也已落在解包临时目录下的 `db/elf/`。

## Test Results
- 构建：通过
- Go 回归：通过
- Python 2/3 关键脚本语法检查：通过
- 代表性 testdata：`python/20260129-complex-types-311` 通过

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以通过本次 QA 验收。`postman-db -> db` 迁移主链路、legacy 输入兼容、日志回收、Python 2/3 基础兼容性与至少一个代表性动态用例均已验证通过。
