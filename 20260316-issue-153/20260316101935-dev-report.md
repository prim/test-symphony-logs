## Summary
本轮完成了运行期数据库目录从 `postman-db` 到 `db` 的主路径迁移：Go 常量、prepare/process 路径、tar 模式结果回收、Python/GDB 工具链、测试执行器、正式文档与 `.gitignore` 已统一切换到 `db/`，同时保留 `prepare/process` 对历史 `postman-db/elf` 与 `tmp/legacy-base/postman-db` 的兼容读取语义。

结论：`PARTIAL`。Issue #153 的核心迁移与代表性验证已完成，但仍存在未完成/未验证项：`qa/issue110` 现有基线测试依赖缺失的 `tmp/legacy-base` 夹具且同时暴露仓库中 `maze` 入口仍为 Python wrapper 的既有问题，导致该兼容回归链路无法在本轮仓库状态下完整验证；另外计划文档中的 AC10（额外创建 GitHub issue）未执行，因为当前需求本身已基于现有 issue #153 开发。

## Changes
- `.gitignore`: 将运行产物忽略目录从 `postman-db/` 切换为 `db/`
- `common/const.go`: 将 `POSTMANDB` 常量值切换为 `db`
- `prepare/args.go`: 默认输出目录改为 `./db/<project>-<user>-<profileId>`
- `prepare/process.go`: `maps.gdb`、ELF/md5 候选路径优先切换到 `db/`，同时保留 `postman-db/elf` 历史回退
- `prepare/prepare.go`: 新增当前输出根目录辅助函数，并保留 `tmp/legacy-base/postman-db` 历史输入语义
- `main.go`: tar 模式临时布局改为 `tempDir/db/...`，日志回收从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制
- `cmd/postman_prelude.py`: 预处理脚本改为写入 `./db` 与 `./db/elf`
- `cmd/maze-gdb-coredump.py`: coredump 调试脚本改为使用 `./db`，并补上 `print_function` 以保持 Python 2/3 可解析
- `cmd/postman_lz4.py`: LZ4 结果路径改为 `./db`
- `cmd/diff-maps.py`: 示例路径切换为 `db/...`
- `gdb/gdb-scripts/gdb_tool.py`: GDB 调试入口的 md5/exe/core/elf 读取路径切换为 `./db`
- `gdb/gdb-scripts/az_fake_maps.py`: 文档示例路径切换为 `./db/...`
- `testdata/run_test.py`: 测试执行器清理、日志探测和结果读取改为 `db/` 语义
- `qa/issue110/prepare_migration_test.go`: 保留 legacy `postman-db` 输入检查，同时显式清理新 `db/` 输出目录
- `doc/debugging.md`: 调试示例路径切换到 `db/<project>-<user>-<profileId>/` 与 `db/*/pyo`
- `doc/maze_py_analysis.md`: 运行期分析数据目录说明更新为 `db/`
- `doc/support.md`: 支持案例中的 ELF 缓存示例路径更新为 `/db/elf/`
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 勾选本轮已完成的迁移任务

## Testing
- `./maze --build`：通过
- `go test ./qa/issue153`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，结果文件可从 `db/` 体系下生成并读取
- `python3 -m py_compile cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py testdata/run_test.py`：通过
- `python2.7 -m py_compile cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `go test ./qa/issue110`：失败（阻塞项）
  - 失败 1：仓库当前 `maze` 入口仍是 Python wrapper，触发 `TestMazeEntryIsNoLongerPythonWrapper` 失败
  - 失败 2：缺少 `tmp/legacy-base` 基线夹具，触发 `TestPrepareRunMatchesLegacyJSONBaseline` 失败

## 验收核对
- AC1: PASS — `POSTMANDB` 与 `prepare/args.go` 默认输出已切换到 `./db/<project>-<user>-<profileId>`
- AC2: PASS — Go/Python/GDB 的 ELF/md5 读取主路径改为 `db/elf/`，并保留旧目录回退
- AC3: PASS — `main.go` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志
- AC4: PASS — prepare/process 的 maps/core/exe/md5 相关路径已统一到 `db/` 体系
- AC5: PARTIAL — 代码与测试接线已保留“旧目录输入兼容 + 新目录输出生效”语义，但 `qa/issue110` 动态回归因缺少 legacy 夹具与既有入口问题未跑通
- AC6: PASS — 关键 Python 脚本已通过 Python 2.7 / 3.x 语法编译检查
- AC7: PASS — `./maze --build` 成功
- AC8: PASS — `python/20260129-complex-types-311` 代表性场景通过，结果正确读取
- AC9: PASS — `.gitignore` 已忽略 `db/`
- AC10: FAIL — 计划文档要求额外创建 GitHub issue 并打 `symphony:todo` label，但当前工作已基于既有 issue #153 实施，本轮未重复建单

## 已完成项
- 已完成 `postman-db -> db` 的主目录常量迁移
- 已完成 Go prepare/process/main 路径切换
- 已完成 Python/GDB 工具链路径切换
- 已完成测试执行器 `testdata/run_test.py` 的 `db/` 语义迁移
- 已完成正式文档与忽略规则更新
- 已完成构建、静态测试、脚本兼容性检查和代表性动态场景验证

## 未完成项
- 未处理 `maze` 入口仍为 Python wrapper 的既有问题（该问题导致 `qa/issue110` 失败，但超出本 issue 的目录迁移最小改动范围）
- 未执行计划文档中的 AC10 额外建单动作

## 未验证项
- `qa/issue110` 依赖 legacy 基线夹具的完整动态迁移回归尚未验证通过
- 未对更多语言场景（如 Node.js/C++）做本轮动态复测

## Self-Review
- 发现 `cmd/maze-gdb-coredump.py` 原本仍使用 Python 2 风格 `print cmd`，会导致 Python 3 语法检查失败；已补充 `from __future__ import print_function` 并改为 `print(cmd)`
- 检查了 `prepare/process.go` 的 ELF 候选路径，确保新 `db/elf` 优先，同时保留 `postman-db/elf` 作为历史输入兼容回退，避免直接破坏 legacy 数据读取

## Notes
- `prepare/prepare.go` 中 `tmp/legacy-base/postman-db` 为历史输入夹具语义，按需求保留，未机械替换
- `go test ./qa/issue110` 当前失败主要由仓库预存问题/夹具缺失引起，不是本轮 `db/` 迁移引入的新编译或语法错误
