# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

原因：虽然构建、关键静态单测、`qa/issue110` 回归测试以及代表性 `testdata` 动态用例均通过，但代码库仍保留 `qa/issue110/prepare_migration_test.go:156` 对 `tmp/legacy-base/postman-db` 的当前回归输入引用，违反最新人审要求“不要兼容 `./postman-db/`”，因此 AC5 未闭合。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 已将 `POSTMANDB` 常量切换为 `db`；`prepare/args.go` 默认输出目录通过 `currentPrepareDBRoot(".")` 生成 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `go test ./prepare -run 'TestBuildOutputDirDefaultsToDB|TestBuildOutputDirPrefersLogdir|TestCurrentPrepareDBRoot'` 通过。
- **备注**: 满足 AC1。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 与 `prepare/prepare.go` 已切换为 `db/` 体系；但 `qa/issue110/prepare_migration_test.go:156` 仍通过 `filepath.Join(legacyRoot, "postman-db")` 读取旧目录作为当前回归输入。
- **关键指标**: `go test ./qa/issue110` 虽然通过，但静态检查确认仍存在旧目录硬编码。
- **备注**: 这不是运行时崩溃问题，而是需求与回归链路不一致；违反最新 human review“不要兼容 ./postman-db/”。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 搜索并复制日志；`prepareTarPostmanLayout()` 也已在临时目录下创建 `db/` 布局。
- **关键指标**: `go test . -run 'TestPrepareTarPostmanLayoutWritesDBStructure|TestCopyResultFilesReadsLogsFromDBDirectory'` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`。
- **关键指标**: `python2 -m py_compile ... && python3 -m py_compile ...` 通过。
- **备注**: 满足 AC2、AC6 的语法层验证。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已更新为 `db/`。
- **关键指标**: 静态检查通过。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理并查找 `db/` 下的产物与日志；执行代表性用例后，结果成功生成并读取。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过（1 passed, 0 failed）。
- **备注**: 满足 AC8；同时验证 `maze.log` 中 ELF 路径已落到 `tmp/.../db/elf/...`。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 工具脚本在 Python 2.7 与 Python 3.x 下均可完成语法编译。
- **关键指标**: `python2 -m py_compile ...` 与 `python3 -m py_compile ...` 均通过。
- **备注**: 仅完成语法级兼容验证，未覆盖 Python 2.7 完整运行时链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新人审要求，当前实现与回归链路都应收口到 `db/` 语义，不应继续以 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 查看 `compareLegacyAndPrepareSnapshot()`
  3. 定位第 156 行，可见当前回归链路仍从 `tmp/legacy-base/postman-db` 读取基线结果
- **可能原因**: 修复主要集中在业务主链路与文档收口，但测试/基线夹具的路径语义未同步完全迁移。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应基线夹具收口到 `tmp/legacy-base/db`，避免继续把 `postman-db` 作为当前回归输入语义。

## 已执行的检查项
- 阅读计划文档、test case 报告、最新 dev report。
- 审查 `origin/master...HEAD` 的代码差异。
- 静态核对 `common/const.go`、`prepare/args.go`、`prepare/prepare.go`、`prepare/process.go`、`main.go`、Python/GDB 脚本、`.gitignore`、正式文档、`testdata/run_test.py`。
- 执行 `./maze --build`。
- 执行 `go test ./prepare -run 'TestBuildOutputDirDefaultsToDB|TestBuildOutputDirPrefersLogdir|TestCurrentPrepareDBRoot|TestProcessManagerUsesDBPathsOnly|TestCacheProcMapsPrefersDBGeneratedMapsGDB'`。
- 执行 `go test . -run 'TestPrepareTarPostmanLayoutWritesDBStructure|TestCopyResultFilesReadsLogsFromDBDirectory'`。
- 执行 `go test ./qa/issue110`。
- 执行 `python2 -m py_compile ...` 与 `python3 -m py_compile ...`。
- 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`。

## 未执行项
- Python 2.7 完整运行时动态分析链路未执行，仅完成语法级检查。
- 不同 glibc 版本测试未执行。
- 不同 Node.js 版本测试未执行。
- 大内存进程性能测试未执行。

上述未执行项不影响本次 FAIL 结论；当前失败已由静态需求违例直接确定，继续扩展动态验证不会改变 AC5 未闭合这一事实。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`：通过
- `go test ./prepare -run 'TestBuildOutputDirDefaultsToDB|TestBuildOutputDirPrefersLogdir|TestCurrentPrepareDBRoot|TestProcessManagerUsesDBPathsOnly|TestCacheProcMapsPrefersDBGeneratedMapsGDB'`：通过
- `go test . -run 'TestPrepareTarPostmanLayoutWritesDBStructure|TestCopyResultFilesReadsLogsFromDBDirectory'`：通过
- `go test ./qa/issue110`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（1 passed, 0 failed）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 暂不通过验收。请先修复 `qa/issue110/prepare_migration_test.go` 与对应基线/夹具中的旧目录语义，再重新验收。
