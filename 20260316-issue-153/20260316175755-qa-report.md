# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。`./maze --build`、关键 Go 回归测试、Python 2/3 脚本语法检查以及代表性 `testdata` 动态用例均通过；主链路的 `db/` 迁移也基本成立。但根据最新 human review“**不要兼容 `./postman-db/`**”的强约束，当前回归链路仍未完全收口：`qa/issue110/prepare_migration_test.go:156` 仍显式使用 `filepath.Join(legacyRoot, "postman-db")`。因此 AC5 未满足，总体结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 将 `POSTMANDB` 常量切换为 `db`；`prepare/args.go` 中默认输出目录由 `currentPrepareDBRoot(".")` 统一生成 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `common/const.go:5` 为 `POSTMANDB string = "db"`；`prepare/args.go:42` 输出目录模板为 `./db/%s-%s-%s`。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找 legacy 输出目录。
- **关键指标**: `prepare/process.go:47-49` 仅保留 `./db/*-%s.maps.gdb`；`prepare/prepare.go:281` 与 `373` 使用 `tmp/legacy-base/db`；`qa/issue110/prepare_migration_test.go:156` 仍硬编码 `postman-db`。
- **备注**: 与最新 human review“不要兼容 `./postman-db/`”冲突，AC5 未通过。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 tar 解包布局写入 `db/`；日志回收逻辑从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果。
- **关键指标**: `main.go:774-776` 使用 `filepath.Join(tempDir, "db")`；`main.go:868-869` 读取 `db/*/maze.log` 与 `db/*/maze.py.log`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 等默认从 `./db` 和 `./db/elf` 读写。
- **关键指标**: `cmd/postman_prelude.py:9-10`、`gdb/gdb-scripts/gdb_tool.py:350-360` 已切换到 `db/` 体系。
- **备注**: Python 2/3 语法检查通过，满足 AC2、AC6 的基础兼容要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换为 `db/`。
- **关键指标**: 差异中可见 `.gitignore` 由 `postman-db/` 改为 `db/`。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，结果文件可在新目录规则下正确生成并读取，验证通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`；测试总结为 `1 passed, 0 failed`。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `python3 -m py_compile ... && python2 -m py_compile ...` 执行成功，关键脚本至少在语法层面兼容 Python 2.7/3.x。
- **关键指标**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 均完成 py_compile。
- **备注**: 完成语法级验证，但未覆盖 Python 2.7 完整运行时动态链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review 要求，当前实现与当前回归链路都应完全收口到 `db/` 语义，不应继续把 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍使用 `legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 定位到第 156 行
  3. 可见 legacy 输出目录查找逻辑仍硬编码 `postman-db`
- **可能原因**: 本轮代码主链路已迁移到 `db/`，但迁移测试文件自身未同步收口，导致回归链路仍保留旧目录语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- Python 2.7 完整运行时动态验证未执行；本轮仅完成语法级检查。
- 不同 glibc 版本测试未执行；当前环境仅覆盖单一 Linux 环境。
- 不同 Node.js 版本测试未执行；本轮需求以目录迁移为主，且已有阻塞点，不继续扩展高成本动态验证。
- 大内存性能场景未执行；当前已有明确 FAIL，继续执行性能测试对定位本次阻塞帮助有限。

这些未执行项主要影响 AC6 的完整运行时兼容性外延验证，以及性能/跨版本扩展信心；但不影响本轮 FAIL 的核心结论，因为 AC5 已被静态检查直接否决。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 执行记录

- 构建：`./maze --build` ✅
- 关键 Go 回归：`go test -count=1 ./prepare ./qa/issue110 .` ✅
- Python 语法兼容：`python3 -m py_compile ... && python2 -m py_compile ...` ✅
- 代表性动态用例：`python3 testdata/run_test.py python/20260129-complex-types-311` ✅

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的旧目录引用，使回归链路与 `db/` 新规范一致后再重新验收。
