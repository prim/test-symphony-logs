# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。`./maze --build`、关键 Go 回归测试、Python 2/3 脚本语法检查以及代表性 `testdata` 动态用例均通过；主链路的 `db/` 输出也已静态和动态验证生效。但当前回归链路仍未完全收口到 `db/`：`qa/issue110/prepare_migration_test.go:156` 仍显式使用 `filepath.Join(legacyRoot, "postman-db")`。这与计划文档、最新开发说明及本轮需求中“当前实现与当前回归链路统一收口到 `db/`，不继续把 `postman-db` 作为当前输入语义”的要求冲突，因此总体结论为 FAIL。

## Findings

- 发现 1：业务主链路已迁移到 `db/`，但回归测试文件 `qa/issue110/prepare_migration_test.go` 仍硬编码旧目录 `postman-db`，导致 AC5 未满足。
- 发现 2：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`main.go`、Python/GDB 工具链与正式文档已基本统一到 `db/`。
- 发现 3：代表性动态用例 `python/20260129-complex-types-311` 已通过，说明主流程在新目录规则下可运行并正确读取结果。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go:5` 已定义 `POSTMANDB string = "db"`；`prepare/args.go:40-47` 默认输出目录通过 `currentPrepareDBRoot(".")` 生成 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 默认目录常量与拼接逻辑均指向 `db/`。
- **备注**: 满足 AC1、AC4 的静态要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go:46-53` 仅查找 `./db/*-%s.maps.gdb`；`prepare/prepare.go:281`、`373` 的 snapshot baseline 目录也已改为 `tmp/legacy-base/db`。但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找旧目录。
- **关键指标**: 业务代码已收口到 `db/`，回归测试仍保留 1 处旧目录硬编码。
- **备注**: 当前失败点位于回归链路本身，而非主功能路径。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:868-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志结果。
- **关键指标**: 日志回收通配路径已切换到 `db/`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 中示例与路径常量均已切换到 `db/`。
- **关键指标**: Python 2/3 `py_compile` 均通过。
- **备注**: 满足 AC2、AC6 的基础兼容性检查。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore:51` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切到 `db/`。
- **关键指标**: 差异中 `.gitignore` 已由 `postman-db/` 改为 `db/`。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，结果为 `1 passed, 0 failed`。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`，其余断言均通过。
- **备注**: 说明主流程在新目录规则下可分析并读取结果，满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **关键指标**: 关键脚本全部可解析，无路径迁移引入的语法错误。
- **备注**: 仅覆盖语法级兼容，不等同于完整双版本运行时回归。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前实现与当前回归链路都应完全收口到 `db/` 语义，不应继续把 `postman-db` 作为当前输入或当前回归断言路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍使用 `legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 定位 `compareLegacyAndPrepareSnapshot` 函数
  3. 查看第 156 行，可见 legacy 输出目录查找逻辑仍硬编码 `postman-db`
  4. 对照 `prepare/prepare.go:281`、`373` 可见业务基线路径已切换为 `tmp/legacy-base/db`
- **可能原因**: 本次迁移已覆盖主业务代码与文档，但回归测试文件未同步收口到 `db/`，导致测试语义与当前实现不一致。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- 不同 glibc 版本的动态验证未执行；原因：当前环境仅提供单一运行环境，且已有明确局部阻塞点。可能影响 AC2、AC6。
- 不同 Node.js 版本的动态验证未执行；原因：本轮优先验证目录迁移主链路与代表性 Python 场景。可能影响 AC2、AC8。
- 大内存进程性能验证未执行；原因：当前需求主目标为目录迁移正确性，且 AC5 已失败，不继续扩展高成本性能测试。可能影响性能维度结论，但不影响本轮 FAIL 判定。
- Python 2.7/3.x 完整运行时双版本动态验证未执行；本轮仅完成语法级检查。可能影响 AC6 的运行时兼容性充分性。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的旧目录引用，使回归链路与 `db/` 新规范一致后再重新验收。
