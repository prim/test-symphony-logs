# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 human review 退回后的修订版进行复验，重点核查两项历史问题：
- 已移除不应新增的 `cmd/maze_entry.py`
- `currentPrepareDBRoot()` 已被 `prepare/args.go` 实际复用，默认输出目录抽象已落地

低成本静态检查、构建、兼容性语法检查、迁移回归测试以及至少一个代表性 testdata 动态用例均已执行并通过，当前未发现阻塞发布的问题。

## Findings

- `maze` 入口已收回构建与执行逻辑，仓库内不存在 `cmd/maze_entry.py`，符合人工评审“不要额外增加入口脚本”的约束。
- `prepare/args.go` 已通过 `currentPrepareDBRoot(".")` 生成默认输出根目录，默认输出语义已统一为 `./db/<project>-<user>-<profileId>`。
- `prepare/process.go`、`main.go`、Python/GDB 工具链、`.gitignore`、正式文档、`testdata/run_test.py` 已整体切换到 `db/` 体系，同时保留 `postman-db/elf` 与 `tmp/legacy-base/postman-db` 的历史兼容输入语义。
- 代表性动态测试 `python/20260129-complex-types-311` 通过，结果文件与日志可在 `db/` 体系下正确生成与读取。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `"db"`；`prepare/args.go` 默认输出目录通过 `currentPrepareDBRoot(".")` 生成，已不再默认写入 `./postman-db/...`。
- **关键指标**: `go test -count=1 ./qa/issue153` 通过。
- **备注**: 同时满足本轮人工评审要求，路径抽象不再是未使用代码。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 已优先使用 `db/*-%s.maps.gdb`、`db/elf/*`，并保留 `postman-db/elf/*` 历史回退；`prepare/prepare.go` 与 `qa/issue110/prepare_migration_test.go` 继续保留 `tmp/legacy-base/postman-db` 兼容语义。
- **关键指标**: `go test -count=1 ./qa/issue110` 通过，耗时约 16.9 秒。
- **备注**: 兼容性回归链路已恢复。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` tar 模式布局与日志回收逻辑已从 `db/*/maze.log`、`db/*/maze.py.log` 读取，不再依赖 `postman-db/*`。
- **关键指标**: `go test -count=1 ./qa/issue153` 通过。
- **备注**: 与代表性动态用例的日志产物读取结果一致。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 等关键脚本均已切换到 `./db` / `./db/elf`。
- **关键指标**: `go test -count=1 ./qa/issue153` 通过；Python 2.7/3.x `py_compile` 均通过。
- **备注**: 未发现因路径迁移引入的语法兼容问题。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 示例已切换到 `db/`。
- **关键指标**: `go test -count=1 ./qa/issue153` 通过。
- **备注**: 历史 dev-log 中保留 `postman-db` 描述属于历史记录，不构成本次失败。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理并读取 `db/` 产物；执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 后，Maze 成功生成并读取结果。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`；测试总结为 `1 passed, 0 failed`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本均存在且可解析；`maze` 入口现为 shell+Python 兼容包装，`cmd/maze-gdb-coredump.py` 已补充 `from __future__ import print_function`，Python 2.7 与 Python 3.x 语法检查均通过。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2.7 -m py_compile ...` 通过。
- **备注**: 本轮完成的是脚本级兼容性基础校验，未覆盖多解释器下完整端到端运行差异。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./qa/issue153`：通过
- `go test -count=1 ./qa/issue110`：通过
- `python3 -m py_compile maze cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py testdata/run_test.py`：通过
- `python2.7 -m py_compile maze cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行更多高成本扩展动态回归（如不同 glibc 版本、不同 Node.js 版本、>1GB 大内存场景）。
- 原因：本轮需求聚焦目录迁移与人工评审残留问题修复；关键静态检查、构建、兼容性基础检查与代表性主链路动态验证均已通过，继续扩展高成本矩阵的收益较低。
- 可能影响范围：AC6 的跨版本完整运行时覆盖、以及性能相关的非核心补充验证项。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以通过本次 QA 验收；就本需求范围看，`postman-db -> db` 迁移及 human review 指出的残留问题已完成修复。
