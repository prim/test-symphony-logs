# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #153 执行了静态核对、构建验证、Go 回归、Python 语法/基础运行验证，以及至少一个代表性 testdata 动态验收。此前多轮 QA 反复指出的两类问题已闭合：
- `qa/issue110/prepare_migration_test.go` 已收口到 `tmp/legacy-base/db`
- `dev-flow/test-cases.md` 已修正为引用真实存在的测试文件与 `db/` 语义

当前仓库实现满足计划文档中的主要求：默认输出目录、prepare/process、Python/GDB 工具链、日志回收、`.gitignore` 与代表性测试链路均已切换到 `db/`，且未发现新的 `postman-db` 主路径回退。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 为 `db`；`prepare/args.go` 的 `BuildOutputDir()` 默认生成 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go` 中 `TestBuildOutputDirDefaultsToDB`、`TestCurrentPrepareDBRoot` 通过；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 中 `cacheProcMaps()` 仅优先查找 `./db/*-<pid>.maps.gdb`；`prepare/prepare.go` 的 snapshot baseline 路径为 `tmp/legacy-base/db`；`qa/issue110/prepare_migration_test.go` 已从 `filepath.Join(legacyRoot, "db")` 读取历史基线。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，`maze/qa/issue110` 包测试耗时约 7.657s。
- **备注**: 满足 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 读取日志；`prepareTarPostmanLayout()` 在 tar 模式下创建 `db/` 布局，不再创建 `postman-db/`。
- **关键指标**: `main_db_layout_test.go` 中 `TestCopyResultFilesReadsLogsFromDBDirectory` 与 `TestPrepareTarPostmanLayoutWritesDBStructure` 存在且随根包 Go 测试通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`；相关文件未发现新的 `./postman-db` 主路径硬编码。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 满足 AC2 的静态/语法侧要求；未发现因本次路径迁移导致的 Python 2/3 语法回归。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md` 中调试目录示例已切换为 `db/<project>-<user>-<profileId>/` 与 `cat db/*/pyo`；`doc/support.md` 中故障示例路径已为 `db/elf/...`。
- **关键指标**: `.gitignore:51` 为 `db/`。
- **备注**: `doc/maze_py_analysis.md` 为历史分析文档，主体仍描述 `.maze.py` 历史架构，但本轮未发现其对当前验收标准形成阻塞。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理并扫描 `db/` 目录，日志快照与结果定位均基于 `db/*`；代表性用例可正常运行并完成校验。
- **关键指标**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；输出显示 `Maze log: /home/hcjn0770/temp/worktrees/issue-153/maze.log`，验证结果全部通过。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本均存在且非空；`py_compile` 在 Python 2/3 下均通过；`cmd/postman_prelude.py --help` 在 Python 2/3 下均能正常启动。
- **关键指标**:
  - `python2 -m py_compile ...`：通过
  - `python3 -m py_compile ...`：通过
  - `python2 cmd/postman_prelude.py --help`：通过
  - `python3 cmd/postman_prelude.py --help`：通过
- **备注**: `python3 cmd/postman_lz4.py` 运行时因环境缺少 `lz4` 依赖而退出，但错误与 `db/postman-db` 路径迁移无关，未构成对本需求的失败判定。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行多 glibc 版本动态回归；当前工作树仅完成静态与代表性动态验证。
- 未执行多 Node.js 版本动态回归；本轮重点聚焦此前反复失败的 `db/` 路径迁移问题与其代表性主链路。
- 未执行 Python 2.7/3.x 全量脚本运行时矩阵；已完成语法检查与关键入口基础运行检查。

这些未执行项不会推翻本轮关于 issue #153 主需求已闭合的结论，但会影响更广泛兼容性置信度。

## 兼容性验证
- [x] Python 2.7 测试通过（语法检查 + `postman_prelude.py --help`）
- [x] Python 3.x 测试通过（语法检查 + `postman_prelude.py --help`）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果摘要

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## Findings
- 未发现新的 `postman-db` 作为主输出目录的实现回退。
- 之前阻塞 AC5 的 `qa/issue110/prepare_migration_test.go` 现已切换为 `tmp/legacy-base/db`。
- 之前阻塞交付完整性的 `dev-flow/test-cases.md` 文件清单与说明已修正，不再引用不存在的测试文件。
- `cmd/db_file_ops.py` 本轮还补充了 tar 成员类型白名单，进一步减少 tar 解包时的异常成员风险；这是附加安全收益，不影响本次 PASS 结论。

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 本 issue 从 QA 角度可判定通过；如需进一步提升信心，可在后续补充多 glibc / 多 Node.js 版本兼容回归，但这不阻塞当前需求验收。
