# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态验证”执行验收。`./maze --build`、关键 Go 回归测试、Python 2.7/3.x 脚本语法检查以及代表性 `testdata` 动态用例均通过，但仍发现一处与最新 human review 强约束 **“不要兼容 ./postman-db/”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取当前回归输入。该问题说明 AC5 未闭合，因此总体结论为 FAIL。

## Findings

- `common/const.go`、`prepare/args.go`、`prepare/process.go`、`main.go`、`cmd/*.py`、`gdb/gdb-scripts/gdb_tool.py` 已基本完成 `db/` 路径迁移，主链路未见回退到 `postman-db/`。
- `prepare/prepare.go` 的 snapshot-only 基线路径已从 `tmp/legacy-base/postman-db` 切换到 `tmp/legacy-base/db`。
- 但 `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，与最新人审“不要兼容 ./postman-db/”冲突。
- 代表性动态用例 `python/20260129-complex-types-311` 通过，且 `maze.log` 中可观察到 ELF 实际从 `tmp/.../db/elf/...` 读取，说明运行期主链路已可使用 `db/`。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 将 `POSTMANDB` 设为 `db`；`prepare/args.go` 默认输出目录为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在且 `go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC1 的主路径切换要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍使用 `tmp/legacy-base/postman-db` 作为当前回归输入。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，但测试通过的前提仍包含旧目录基线引用。
- **备注**: 与最新 human review 冲突，AC5 未闭合。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:868-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志；新增 `main_db_layout_test.go` 覆盖 tar 解包布局与日志复制。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过；静态检查未见 `postman-db/*/maze.log` 回收逻辑。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`。
- **关键指标**: `python2 -m py_compile ... && python3 -m py_compile ...` 通过。
- **备注**: 语法兼容性通过，运行期主路径未见 `postman-db` 依赖。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已切换示例到 `db/`。
- **关键指标**: `git diff origin/master...HEAD` 中上述文件已更新。
- **备注**: 历史 dev-log 中保留旧术语视为历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，结果校验全部通过；`maze.log` 中 ELF 加载路径显示为 `tmp/.../db/elf/...`。
- **关键指标**: 1 passed, 0 failed。
- **备注**: 满足“至少一个代表性场景通过”的 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本在 Python 2.7 与 Python 3.x 下均可完成语法编译检查。
- **关键指标**: `py_compile` 双版本通过。
- **备注**: 本轮仅完成语法级验证，未完成 Python 2.7 全链路动态运行验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新人审要求，当前实现与回归链路都应收口到 `db/` 语义，不应继续以 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 查看 `compareLegacyAndPrepareSnapshot()`
  3. 定位第 156 行
  4. 可见当前回归链路仍从 `tmp/legacy-base/postman-db` 读取基线目录
- **可能原因**: 本次迁移已覆盖主业务链路，但测试夹具/回归链路未同步收口到 `db/`，遗留了旧目录静态引用。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `tmp/legacy-base/db` 语义，避免继续把 `postman-db` 作为当前回归输入。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（1 passed, 0 failed）

## 未执行项

- 不同 glibc 版本迁移回归：未执行，本轮无多版本系统环境。
- 不同 Node.js 版本迁移回归：未执行，本轮优先验证代表性 Python 主链路。
- Python 2.7 全链路动态运行验证：未执行，仅完成语法检查。
- 大内存性能场景：未执行，本轮需求核心为路径迁移正确性，且当前已有明确 FAIL，无继续执行高成本性能测试的必要。

上述未执行项主要影响 AC6 的“运行时兼容性充分性”与潜在性能补充结论，但**不影响**本轮 FAIL 的主因判定；主因已由 AC5 的静态残留明确成立。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **当前不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 当前输入依赖，未满足最新 human review 的强约束，需修复后再复验。
