# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。主链路构建、关键 Go 回归、Python 语法兼容检查与代表性 testdata 动态用例均通过，但仍发现测试链路未完全收口到 `db/` 语义：`qa/issue110/prepare_migration_test.go:156` 仍显式引用 `filepath.Join(legacyRoot, "postman-db")`。结合最新 human review“**不要兼容 `./postman-db/`**”的强约束，该残留不满足 AC5，因此总体结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `"db"`；`prepare/args.go` 的默认输出目录通过 `currentPrepareDBRoot(".")` 生成 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 静态检查通过
- **备注**: 对应 AC1、AC4 满足。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: 业务源码主链路已统一写入 `db/`，但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找 legacy 输出目录。
- **关键指标**: 1 处旧目录静态引用残留
- **备注**: 该残留与最新 human review“不要兼容 `./postman-db/`”冲突，AC5 不满足。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:868-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志文件。
- **关键指标**: 静态检查通过
- **备注**: 对应 AC3 满足。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`。
- **关键指标**: Python 2/3 `py_compile` 通过
- **备注**: 对应 AC2、AC6 满足。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换到 `db/`。
- **关键指标**: 静态检查通过
- **备注**: 对应 AC9 满足。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，结果文件可正常读取，测试摘要为 `1 passed, 0 failed`。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`
- **备注**: 对应 AC8 满足。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `python2 -m py_compile` 与 `python3 -m py_compile` 均通过，未因目录常量迁移出现语法错误。
- **关键指标**: 5 个关键脚本完成双版本语法检查
- **备注**: 仅完成语法级兼容验证，未完成 Python 2.7 全链路动态验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前实现与回归链路应完全收口到 `db/` 语义，不再把 `postman-db` 作为当前输入路径；迁移测试也应与最新目录规范一致。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍使用 `legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 定位到第 156 行
  3. 观察 legacy 输出目录查找逻辑仍硬编码 `postman-db`
- **可能原因**: 测试夹具/迁移测试在本轮迁移中未同步收口到 `db/` 语义，导致回归链路与业务主链路存在规范不一致。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的当前回归输入/断言统一迁移到 `db/` 语义，并重新核对对应 fixture 与断言是否仍依赖旧目录命名。

## Findings

- `qa/issue110/prepare_migration_test.go:156` 仍保留 `postman-db` 旧目录静态引用，违反本轮 human review“不要兼容 `./postman-db/`”的收口要求。
- 其余已检查的业务主链路、日志回收、Python/GDB 工具链、正式文档与 `.gitignore` 均已迁移至 `db/`。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 未执行项

- Python 2.7 全链路动态运行验证：本轮仅完成语法级检查，未执行实际分析流程。
- 不同 glibc 版本验证：未执行。
- 不同 Node.js 版本验证：未执行。
- 大内存性能场景验证：未执行。

未执行原因：当前已有明确需求不满足项（AC5/TC-002），继续扩展高成本兼容性与性能验证不能改变本轮总体 FAIL 结论，因此按早停策略停止阶段 C 的扩展验证。

受影响范围：主要影响 AC5；兼容性/性能未执行项分别影响 AC6 的运行时层面补充信心，以及性能类扩展结论完整性，但不影响本轮已确认的核心失败点。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的旧目录引用，使回归链路与 `db/` 新规范一致后再重新验收。
