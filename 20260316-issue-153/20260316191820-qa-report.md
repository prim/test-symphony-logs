# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #153“将 postman-db 目录迁移至 db”执行了静态核对、构建校验、Go 回归、Python 2/3 语法兼容检查与代表性动态用例验证。此前反复阻塞的 `qa/issue110/prepare_migration_test.go` 已收口到 `tmp/legacy-base/db`，本轮未再发现业务主链路对 `postman-db` 的运行时依赖。

## Findings

- 需求主链路已切换到 `db/`：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、Python/GDB 工具链均已按新目录语义工作。
- 历史基线路径兼容已按当前实现收口到 `tmp/legacy-base/db`，未保留新的 `postman-db` 运行时兼容分支。
- 仓库存在大量与 issue #153 无直接关系的改动，增加了审查噪音；但本轮针对 AC 的关键路径检查未发现因此引入的 db 迁移回退。
- `dev-flow/test-cases.md` 中“Test File(s)”仍写为 `qa/issue153/db_path_migration_test.go`，与仓库当前实际覆盖文件 `prepare/db_path_behavior_test.go`、`main_db_layout_test.go` 不一致。这是测试文档准确性问题，不影响本轮实现验收结论。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `db`；`prepare/args.go` 通过 `currentPrepareDBRoot(".")` 生成默认目录 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 默认输出目录为 `./db/...`
- **备注**: 对应 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 读取当前 `./db/*-<pid>.maps.gdb`；`prepare/prepare.go` 的 snapshot-only 基线目录为 `tmp/legacy-base/db`；`qa/issue110/prepare_migration_test.go` 已从 `db` 基线读取。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过
- **备注**: 当前实现未保留 `postman-db` 运行时兼容分支，符合本轮收口方向。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 `copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 搜索并复制结果日志。
- **关键指标**: 日志复制通配模式为 `db/*/maze.log`、`db/*/maze.py.log`
- **备注**: 对应 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 均已切换到 `./db`/`./db/elf`；文件操作已收口到 `cmd/db_file_ops.py`。
- **关键指标**: `python3 -m py_compile ... && python2 -m py_compile ...` 通过
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已使用 `db/` 语义。
- **关键指标**: `.gitignore` 包含 `db/`
- **备注**: 对应 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 实际执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，结果文件读取链路正常。
- **关键指标**: `1 passed, 0 failed`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，对应 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 2.7 与 Python 3.x 下均通过 `py_compile`，未因路径迁移产生语法问题。
- **关键指标**: Python 2/3 `py_compile` 全部通过
- **备注**: 这是基础兼容性验证，未覆盖不同 glibc / Node.js 版本的实际动态回归。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python2 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## 未执行项

- 未执行更多高成本动态回归（如 Node.js、不同 glibc 版本、不同 Node.js 版本、>1GB 大内存性能测试）。
- **原因**: 本轮关键 smoke test、构建、Go 回归与代表性 testdata 已通过，且需求核心是目录迁移正确性；继续扩展动态验证收益较低。
- **可能影响范围**: AC6 的跨版本运行时兼容、性能项与更广覆盖场景仍需后续专项回归补充。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 就 issue #153 的验收标准而言，本轮可以判定通过；如要发布，建议后续补充跨 glibc / Node.js / 性能专项回归，并修正文档中的测试文件清单描述。
