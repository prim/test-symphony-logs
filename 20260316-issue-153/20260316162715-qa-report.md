# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本静态检查 → 构建与关键 Go 测试 → 至少一个代表性 testdata 动态用例”执行。构建、根包测试、prepare 包测试、Python 2/3 语法检查以及代表性动态用例均通过；但仍发现与最新 human review 强约束 **“不要兼容 `./postman-db/`”** 不一致的残留：`qa/issue110/prepare_migration_test.go` 仍显式从 `tmp/legacy-base/postman-db` 读取历史基线。该问题说明验收/回归链路尚未完全收口到 `db/` 语义，因此本轮结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB = "db"`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go:10-17`、`prepare/args.go:40-46` 与 `common/const.go:3-6` 一致。
- **备注**: 未发现默认输出仍指向 `postman-db/`。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线读取也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍显式从 `filepath.Join(legacyRoot, "postman-db")` 读取历史基线目录。
- **关键指标**: `go test -count=1 ./qa/issue110` 通过，但通过前提仍是测试代码继续消费 `postman-db` 目录夹具。
- **备注**: 按最新 human review，这已不应再作为当前回归链路的一部分。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `prepareTarPostmanLayout()` 在 tar 场景创建 `db/`、`db/elf/`、`db/<profile>/`；`copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果。
- **关键指标**: `main.go:773-870`、`main_db_layout_test.go:9-82`；`go test -count=1 .` 通过。
- **备注**: `main_db_layout_test.go:47-48` 还验证了不会创建 `postman-db` 目录。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过；静态检查未见运行期 Python/GDB 工具继续读取 `./postman-db`。
- **备注**: 当前残留 `postman-db` 主要位于历史报告与测试文件，不在主运行路径中。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；正式文档中本轮抽检 `doc/support.md` 使用的是 `db/elf` 示例路径，未发现正式文档仍要求使用 `postman-db/` 作为当前规则。
- **关键指标**: `.gitignore:51` 为 `db/`。
- **备注**: 历史 dev-log 中仍出现 `postman-db`，属历史记录，不作为本条失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理 `db` 缓存、从 `db/*/maze.log` / `db/*/maze.py.log` 发现日志，并执行代表性用例成功。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；校验输出包含 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500` 等预期值。
- **备注**: 满足“每次验收至少实际执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 2.7 与 Python 3.x 下均可通过语法编译。
- **关键指标**: `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze` 通过；`python3 -m py_compile ...` 通过。
- **备注**: 本轮完成的是语法级兼容核对，未覆盖双版本完整运行时行为。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review“不要兼容 `./postman-db/`”，回归链路也应全面收口到 `db/`，不应继续使用 `postman-db` 作为基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 可见第 156 行仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 运行 `go test -count=1 ./qa/issue110`，测试虽通过，但其成功建立在旧目录夹具仍被使用之上。
- **可能原因**: 本轮修复仅收口了运行时代码 `prepare/prepare.go`，没有同步收口与 issue #153 迁移相关的回归测试夹具和断言。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的基线路径与夹具语义同步迁移到 `tmp/legacy-base/db`，避免回归测试继续强化已被 human review 否定的旧目录兼容行为。

## 未执行项
- 未执行更多高成本动态用例（如 Node.js / C++ 代表场景、不同 glibc 版本、不同 Node.js 版本、大内存性能场景）。
- 未执行原因: 当前已发现明确违反最新 human review 强约束的范围问题；继续扩大动态验证不能消除该静态缺陷，仅会增加重复成本。
- 可能影响范围: AC5、与迁移验收相关的测试收口完整性；兼容性/性能条目仍缺少本轮实测证据。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

说明：本轮对 Python 2.7 只完成了脚本语法编译检查，未执行完整 testdata 动态链路，因此不记为“测试通过”。

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 已执行的命令

```bash
./maze --build
go test -count=1 ./prepare
go test -count=1 ./qa/issue110
go test -count=1 .
python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze
python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze
python3 testdata/run_test.py python/20260129-complex-types-311
```

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。主运行链路的 `db/` 迁移基本完成，构建与代表性动态用例也通过，但回归测试仍保留 `postman-db` 基线依赖，未完全满足最新 human review“不要兼容 `./postman-db/`”的强约束。需先清理该测试残留后再复验。
