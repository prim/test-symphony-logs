## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings

- 本轮重点复核了上一轮被指出的两处 legacy 兼容缺口：`prepare/process.go` 已恢复对历史 `./postman-db/*.maps.gdb` 的回退识别，`gdb/gdb-scripts/gdb_tool.py` 已恢复对历史 `postman-db` profile/elf/core 产物的读取兼容。
- 人工 review 明确指出“不需要额外增加 `cmd/maze_entry.py`”，本轮仓库中已不存在该文件，入口仍由根目录 `maze` 脚本承载。
- 代表性动态用例 `python/20260129-complex-types-311` 通过；运行输出明确显示 `output dir: ./db/coredump-default-2636550`，说明新目录语义已实际生效。
- `.gitignore` 已忽略 `db/`；正式文档中的运行期目录示例已切换到 `db/` 体系。历史问题记录文档 `doc/support.md` 中仍保留旧案例/绝对路径样例，但该文档属于历史支持记录，不构成当前规范入口缺陷。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB = "db"`；`prepare/args.go` 通过 `currentPrepareDBRoot(".")` 生成默认输出目录，默认路径为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `go test -count=1 ./prepare` 通过；`TestBuildOutputDirDefaultsToDB`、`TestCurrentPrepareDBRoot` 通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 中 `maps.gdb` 查找顺序为 `./db/*-pid.maps.gdb` 优先、`./postman-db/*-pid.maps.gdb` 回退；`md5Candidates()` 与 `ResolveExistingPath()` 同时保留 `postman-db/elf` 兼容输入；`prepare/prepare.go` 继续保留 `tmp/legacy-base/postman-db` 语义。
- **关键指标**: `go test -count=1 ./prepare`、`go test -count=1 ./qa/issue110` 均通过。
- **备注**: 满足 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`prepareTarPostmanLayout()` 在 tar 模式下创建 `db/` 与 `db/elf/` 结构，不再创建 `postman-db/`。
- **关键指标**: `go test -count=1 .` 通过；`TestPrepareTarPostmanLayoutWritesDBStructure`、`TestCopyResultFilesReadsLogsFromDBDirectory` 通过。
- **备注**: 满足 AC2、AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`cmd/postman_lz4.py` 默认根目录均为 `./db`；`gdb/gdb-scripts/gdb_tool.py` 已改为 `db/` 优先、`postman-db/` 回退的候选路径解析。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 通过。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md` 的正式目录示例已切换到 `db/`。
- **关键指标**: 静态检查通过。
- **备注**: `doc/support.md` 中存在历史故障样例路径 `.../db/elf/...`，与新规范一致，不构成失败。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理和扫描 `db/` 目录；动态执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，输出中出现 `output dir: ./db/coredump-default-2636550`，结果文件成功读取并完成校验。
- **关键指标**: 1/1 用例通过；`pymempool_objects = 68626`；关键对象数量校验全部通过。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本在 Python 2.7 / Python 3.11 下均可通过语法编译；`python2 cmd/postman_prelude.py --help` 可正常输出帮助。
- **关键指标**: `python2 --version` = 2.7.18，`python3 --version` = 3.11.2。
- **备注**: `python3 cmd/postman_lz4.py --help` 因环境缺少 `lz4` 依赖而退出，但这不是本次路径迁移引入的问题，也不影响已执行的路径兼容性结论。

## 失败的 Test Case（如有）

- 无。

## 未执行项（如有）

- 未执行不同 glibc 版本的真实动态验证；当前仅完成现环境静态/动态核验。
- 未执行不同 Node.js 版本的动态回归；本轮代表性动态用例选择了默认推荐的 Python 场景。
- 未执行 Python 2.7 下的完整 `run_test.py` 动态链路；已完成脚本级 `py_compile` 与帮助入口验证。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare`：通过
- `go test -count=1 .`：通过
- `go test -count=1 ./qa/issue110`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 兼容性验证

- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以通过本次 QA 验收。就 issue #153 范围看，`postman-db -> db` 迁移主链路、legacy 输入兼容、日志回收、Python 2/3 基础兼容性以及至少一个代表性动态用例均已验证通过。
