# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## QA Result: FAIL

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态用例”执行验收。`./maze --build`、关键 Go 测试、Python 2.7/3.x 语法检查以及代表性 `testdata` 动态用例均通过，但仍发现一处与最新 human review 强约束 **“不要兼容 `./postman-db/`”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线目录。该问题说明回归链路尚未完全收口到 `db/` 语义，因此总体结论必须为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 将 `POSTMANDB` 常量切换为 `db`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出目录为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 对应逻辑已存在；静态 diff 未见默认输出仍指向 `postman-db`。
- **备注**: 满足 AC1。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只查找 `./db/*-<pid>.maps.gdb` 且 `db/elf` 路径已切换完成；`prepare/prepare.go` 的 snapshot-only 基线目录也已改为 `tmp/legacy-base/db`。但是 `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，继续把 `postman-db` 作为当前回归输入目录。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 虽通过，但通过前提仍是测试代码继续消费 `postman-db` 基线夹具。
- **备注**: 与最新 human review“不要兼容 `./postman-db/`”直接冲突，因此判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`prepareTarPostmanLayout()` 也改为在临时目录下创建 `db/` 结构。
- **关键指标**: `main_db_layout_test.go` 覆盖日志复制与 db 目录布局；静态检查显示不再读取 `postman-db/*/maze.log`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 均已切换到 `./db` / `./db/elf`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过；静态检查未见运行期 Python/GDB 工具继续读取 `./postman-db`。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；抽检 `doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 均已改为 `db/` 规则。
- **关键指标**: `git diff origin/master...HEAD` 显示上述文件已更新；静态搜索未发现正式入口文档继续把 `postman-db/` 作为当前规范。
- **备注**: 历史 dev-log 中仍出现 `postman-db` 属历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，Maze 运行、结果读取与 `validate.py` 校验均通过，未见对 `postman-db/` 的依赖报错。
- **关键指标**: `1 passed, 0 failed`；`pymempool_objects = 68626`，并成功识别 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500` 等关键对象统计。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强制要求，同时覆盖 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 3 与 Python 2.7 下均可通过 `py_compile` 语法检查，未因路径常量迁移引入基础兼容性错误。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2 -m py_compile ...` 通过。
- **备注**: 仅完成基础语法级兼容验证，尚未完成 Python 2.7 动态全链路验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review，回归链路也应完全收口到 `db/`，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 定位 `compareLegacyAndPrepareSnapshot()`。
  3. 查看第 156 行，可见仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 运行 `go test -count=1 ./prepare ./qa/issue110 .`，测试会通过，但其成功前提仍包含旧目录夹具依赖。
- **可能原因**: 本次实现已把主运行链路与 snapshot-only 基线路径迁到 `db/`，但迁移回归测试文件未同步收口，导致测试语义仍保留旧目录输入。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的基线路径与对应夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- 多 glibc 版本兼容性验证未执行：当前工作树与本地环境仅完成单机静态/动态验证，未覆盖不同系统 libc 版本；可能影响 AC2、AC6。
- 不同 Node.js 版本兼容性验证未执行：本轮未运行 Node.js testdata；可能影响 AC2、AC8。
- 大内存进程性能验证未执行：未构造 >1GB 目标进程场景；可能影响性能项，但不影响本轮已发现的主阻塞结论。

## 已执行的检查项
- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新 `dev-flow/20260316164917-dev-report.md`
- 审查 `git diff origin/master...HEAD`
- 静态核对 `common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、Python/GDB 工具链、`.gitignore` 与正式文档
- 执行 `./maze --build`
- 执行 `go test -count=1 ./prepare ./qa/issue110 .`
- 执行 Python 2.7/3.x `py_compile` 兼容性检查
- 执行代表性动态用例 `python3 testdata/run_test.py python/20260129-complex-types-311`

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
