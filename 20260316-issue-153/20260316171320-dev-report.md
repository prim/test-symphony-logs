## Summary

本轮未新增业务源码修复点，重点是根据最新 QA 报告与 human review 再次核对 issue #153 的真实收口状态，并补充正式开发记录。确认当前源码主链路已经统一使用 `db/`，未继续保留 `postman-db` 运行时兼容；当前唯一未闭合项仍是测试文件 `qa/issue110/prepare_migration_test.go` 中对旧目录的静态引用，但受系统约束限制，本轮不能修改测试文件。

## Changes

- `dev-log/2026-03-16-move-postman-db-to-db.md`: 补充 fix iteration #7 记录，明确最新阻塞点、三类结论与本轮状态为 BLOCKED。

## Testing

- `./maze --build`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py maze`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py maze`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## Acceptance Check

- AC1: PASS，默认输出目录仍为 `./db/<project>-<user>-<profileId>`。
- AC2: PASS，Python/GDB/Go 主链路均使用 `db/elf`。
- AC3: PASS，日志回收逻辑从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果。
- AC4: PASS，prepare/process 路径统一位于 `db/`。
- AC5: FAIL，`qa/issue110/prepare_migration_test.go` 仍引用 `filepath.Join(legacyRoot, "postman-db")`。
- AC6: PASS，Python 2/3 语法检查通过。
- AC7: PASS，`./maze --build` 通过。
- AC8: PASS，代表性 `testdata` 动态用例通过且结果可正常读取。
- AC9: PASS，`.gitignore` 已忽略 `db/`。

## Remaining Work

- 未完成项：将 `qa/issue110/prepare_migration_test.go` 及对应 baseline/夹具迁移到 `db/` 语义。
- 未验证项：Python 2.7 完整运行时链路、不同 glibc 版本、不同 Node.js 版本的动态回归未在本轮执行。

## Conclusion

BLOCKED：当前阻塞点是测试文件仍保留 `postman-db` 路径引用，而本轮受系统约束禁止修改测试文件，无法在当前轮次内彻底关闭 AC5。
