# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `db`；`prepare/args.go` 默认输出目录已切换到 `./db/<project>-<user>-<profileId>`；`prepare/db_path_behavior_test.go` 对默认输出目录与根目录生成逻辑有覆盖。
- **关键指标**: 静态检查通过；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 的 maps.gdb、elf 等路径均切换到 `db/`；`prepare/prepare.go` 中 snapshot baseline 目录读取为 `tmp/legacy-base/db`；`qa/issue110/prepare_migration_test.go` 第 156 行也已读取 `filepath.Join(legacyRoot, "db")`。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 与此前多轮 QA 报告不同，仓库当前测试文件已不再引用 `postman-db`。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 使用 `filepath.Join("db", "*", "maze.log")` 与 `filepath.Join("db", "*", "maze.py.log")` 回收日志；`main_db_layout_test.go` 覆盖了日志复制路径。
- **关键指标**: `main.go:868-869` 命中 `db/*/maze.log` 与 `db/*/maze.py.log`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`；相关脚本已复用 `cmd/db_file_ops.py` 处理文件移动和 tar 解包。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；正式文档与调试示例已更新为 `db/` 规范。
- **关键指标**: `.gitignore` 静态检查通过。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理并读取 `db` 目录；执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，输出中显示 `output dir: ./db/coredump-default-2636550`。
- **关键指标**: 1/1 用例通过；`pymempool_objects = 68626`；`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500` 均符合预期。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本均存在且能通过 Python 2 / Python 3 语法编译检查。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2 -m py_compile ...` 通过。
- **备注**: 仅完成语法级兼容验证，未覆盖 Python 2.7 全链路动态执行。

### TC-008: 测试用例报告与仓库实际测试资产一致性
- **结果**: FAIL
- **实际行为**: `dev-flow/test-cases.md` 第 121 行声明新增测试文件为 `qa/issue153/db_path_migration_test.go`，但仓库中不存在该文件；实际存在的是 `main_db_layout_test.go` 与 `prepare/db_path_behavior_test.go`。另外，第 125 行备注仍写明“`prepare/prepare.go` 中 `tmp/legacy-base/postman-db` 语义必须保留”，与当前实现及计划收口到 `tmp/legacy-base/db` 的状态不一致。
- **关键指标**: `glob qa/**/db_path_migration_test.go` 无结果；`glob main_db_layout_test.go` 与 `glob prepare/db_path_behavior_test.go` 有结果。
- **备注**: 这是文档/测试资产接线不一致问题，会误导后续 QA 与开发判断覆盖范围。

## 失败的 Test Case

### TC-008: 测试用例报告与仓库实际测试资产一致性
- **结果**: FAIL
- **预期行为**: `dev-flow/test-cases.md` 应准确列出实际存在的测试文件，并与当前迁移语义保持一致。
- **实际行为**: 报告引用了不存在的 `qa/issue153/db_path_migration_test.go`，且备注仍保留过时的 `postman-db` 说明。
- **复现步骤**:
  1. 打开 `dev-flow/test-cases.md`。
  2. 查看 `## Test File(s)` 与 `## Notes for Developer` 段落。
  3. 在仓库中搜索 `qa/issue153/db_path_migration_test.go`。
- **可能原因**: 多轮修复后实际测试文件发生调整，但 test case 报告未同步更新。
- **建议修复**: 更新 `dev-flow/test-cases.md`，改为引用仓库中真实存在的测试文件，并删除/修正过时的 `postman-db` 说明。

## 未执行项
- Python 2.7 全链路动态运行未执行；当前仅完成语法编译检查，可能影响 AC6 的运行时层面覆盖深度。
- 不同 glibc 版本、不同 Node.js 版本兼容性未执行；可能影响兼容性扩展结论，但不影响本轮对主迁移链路的核心判断。
- 大内存进程性能验证未执行；可能影响性能相关信心，但本需求主目标为目录迁移。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果摘要
- `./maze --build`: 通过
- `python3 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`: 通过
- `python2 -m py_compile cmd/db_file_ops.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`: 通过
- `go test -count=1 ./prepare ./qa/issue110 .`: 通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`: 通过

## 总结
- 通过: 7 / 8
- 失败: 1 / 8
- 建议: 功能主链路与代表性动态验收已通过，`db/` 迁移实现整体可用；但由于 `dev-flow/test-cases.md` 仍存在测试资产接线与说明不一致问题，本轮 QA 结论维持 FAIL，建议先修正文档与测试清单后再重新验收。
