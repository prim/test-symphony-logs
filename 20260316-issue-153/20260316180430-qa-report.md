# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。`./maze --build`、关键 Go 回归测试、Python 2/3 脚本语法检查以及代表性 `testdata` 动态用例均通过；主链路的 `db/` 输出也已实际生效。但当前回归链路仍未完全收口到 `db/`：`qa/issue110/prepare_migration_test.go:156` 仍显式使用 `filepath.Join(legacyRoot, "postman-db")`。这与本需求最新要求“当前实现与当前回归链路统一收口到 db，不继续把 postman-db 作为当前输入语义”冲突，因此总体结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go:5` 已将 `POSTMANDB` 设为 `db`；`prepare/args.go:40-47` 默认输出目录已切换为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/args.go:42` 使用 `currentPrepareDBRoot(".")` 生成 `./db/...`。
- **备注**: 满足 AC1 的静态实现要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go:46-53` 仅查找 `./db/*-%s.maps.gdb`，`prepare/prepare.go:281` 与 `373` 的 snapshot baseline 目录也已改为 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找旧目录。
- **关键指标**: 全仓库针对该静态模式的检索仅剩 1 处命中：`qa/issue110/prepare_migration_test.go:156`。
- **备注**: 当前回归链路未完全统一到 `db` 语义，AC5 未满足。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:868-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`prepareTarPostmanLayout` 也已创建 `tempDir/db/...` 布局。
- **关键指标**: `main_db_layout_test.go` 已覆盖 `prepareTarPostmanLayout` 与 `copyResultFiles` 的 `db` 行为；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一改为 `./db` / `./db/elf`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 语法级兼容验证通过，满足 AC2 与 AC6 的基础要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换到 `db/`。
- **关键指标**: 差异中 `.gitignore` 已由 `postman-db/` 改为 `db/`。
- **备注**: 满足 AC9 的静态要求。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；测试输出明确显示 `output dir: ./db/coredump-default-2636550`，结果文件读取成功。
- **关键指标**: 动态验收结果 `1 passed, 0 failed`；maze 输出日志第 15 行显示新目录为 `./db/coredump-default-2636550`。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 2.7 与 Python 3.x 下均可通过 `py_compile` 语法检查。
- **关键指标**: `python2 -m py_compile ...`、`python3 -m py_compile ...` 均成功退出。
- **备注**: 本轮未执行 Python 2.7 完整运行时动态链路，仅完成基础兼容性检查。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前实现与当前回归链路都应完全收口到 `db/` 语义，不应继续把 `postman-db` 作为当前输入或当前回归断言路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍使用 `legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 定位到 `compareLegacyAndPrepareSnapshot` 函数
  3. 查看第 156 行，可见 legacy 输出目录查找逻辑仍硬编码 `postman-db`
- **可能原因**: 业务主链路已迁移到 `db/`，但回归测试夹具/断言未同步收口，导致实现与验收链路语义不一致。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 已执行的检查项
- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告 `dev-flow/20260316180113-dev-report.md`
- 审查 `git diff origin/master...HEAD`
- 静态核对 `common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、Python/GDB 脚本与正式文档
- 执行 `./maze --build`
- 执行 `go test -count=1 ./prepare ./qa/issue110 .`
- 执行 Python 2/3 语法检查
- 执行代表性动态用例 `python3 testdata/run_test.py python/20260129-complex-types-311`

## 未执行项
- Python 2.7 完整运行时动态验收
  - **未执行原因**: 当前已有脚本语法级验证，且本轮阻塞点已明确位于回归测试链路残留旧路径，继续扩展高成本动态验证对定位主问题收益有限。
  - **可能影响 AC**: AC6
- 不同 glibc 版本、不同 Node.js 版本兼容回归
  - **未执行原因**: 当前仓库未提供多版本环境矩阵；本轮主阻塞已能通过静态检查明确复现。
  - **可能影响 AC**: AC2、AC6、AC8
- 大内存性能场景验证
  - **未执行原因**: 本需求核心阻塞为目录迁移一致性问题，性能验证不影响当前 FAIL 判定。
  - **可能影响 AC**: 无直接 AC，但影响扩展验收置信度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的旧目录引用，使回归链路与 `db/` 新规范一致后再重新验收。
