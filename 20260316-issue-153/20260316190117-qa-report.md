# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本静态检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。

结论：`db/` 迁移主链路基本已经生效，构建、关键 Go 回归、Python 2/3 语法检查以及至少一个代表性 `testdata` 动态用例均通过；但回归测试链路仍存在旧目录残留，未完全满足 AC5，且 `dev-flow/test-cases.md` 中测试文件清单与仓库实际情况不一致。因此总体结果判定为 **FAIL**。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 常量已切换为 `"db"`；`prepare/args.go` 中 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `common/const.go:5`、`prepare/args.go:40-46`
- **备注**: 满足 AC1 的静态实现要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 与 `prepare/prepare.go` 已收口到 `db/`，例如 `cacheProcMaps()` 仅查找 `./db/*-<pid>.maps.gdb`，snapshot baseline 目录也改为 `tmp/legacy-base/db`。但 `qa/issue110/prepare_migration_test.go:156` 仍显式从 `filepath.Join(legacyRoot, "postman-db")` 读取历史输出目录，回归测试链路未完全迁移到 `db/` 语义。
- **关键指标**: `prepare/process.go:46-53`、`prepare/prepare.go:281`、`prepare/prepare.go:373`、`qa/issue110/prepare_migration_test.go:156`
- **备注**: 这是当前最主要阻塞点，对应 AC5 未闭合。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `copyResultFiles()` 已从 `db/*/maze.log` 和 `db/*/maze.py.log` 复制日志，不再依赖 `postman-db/*`。
- **关键指标**: `main.go:868-869`
- **备注**: 满足 AC3 的静态实现要求。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`。
- **关键指标**: `cmd/maze-gdb-coredump.py` 中 `POSTMANDB = "./db"`、`POSTMANDB_ELF = "./db/elf"`；`gdb/gdb-scripts/gdb_tool.py:342-347`
- **备注**: 静态检查通过；后续动态覆盖仍可继续扩展。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；本轮 diff 中 `doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已更新到 `db/` 语义。
- **关键指标**: `.gitignore:51`
- **备注**: 满足 AC9 的主要要求。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已使用 `db` 目录清理缓存、定位日志并读取结果；执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，汇总结果为 `1 passed, 0 failed`。
- **关键指标**: `testdata/run_test.py:77`、`122-123`、`156`、`236`；动态用例输出 `Total: 1 passed, 0 failed`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强约束，也支撑 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 执行 `python3 -m py_compile ... && python2 -m py_compile ...` 通过，未出现因路径常量迁移导致的语法错误。
- **关键指标**: Python 2/3 语法检查均通过
- **备注**: 仅完成语法级验证，尚未完成 Python 2.7 运行时动态链路验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 迁移测试应覆盖“新目录输出生效”且回归链路不应继续把 `postman-db` 作为当前输入语义，至少应与计划文档中的 `db/` 收口目标保持一致。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍通过 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))` 读取旧目录。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 定位到第 156 行附近，可见仍引用 `filepath.Join(legacyRoot, "postman-db")`。
- **可能原因**: 测试/夹具迁移未完全收口，业务代码已迁移到 `db/`，但回归测试仍保留旧目录路径。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 与相关夹具路径统一迁移到 `db/` 语义，并重新核对 AC5 的测试描述与实现是否一致。

## 额外发现

- `dev-flow/test-cases.md` 第 121 行声明测试文件为 `qa/issue153/db_path_migration_test.go`，但仓库实际未找到该文件；当前实际覆盖更接近 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go`。这会影响测试文档与实际实现的一致性。

## 已执行的检查项

- 阅读最新开发报告：`dev-flow/20260316185804-dev-report.md`
- 阅读测试用例：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 查看分支改动：`git diff origin/master...HEAD`
- 静态检查关键实现与路径：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、`cmd/*.py`、`gdb/gdb-scripts/gdb_tool.py`、`.gitignore`、`testdata/run_test.py`
- 执行构建：`./maze --build`
- 执行关键 Go 回归：`go test -count=1 ./prepare ./qa/issue110 .`
- 执行 Python 2/3 语法兼容检查：`python3 -m py_compile ... && python2 -m py_compile ...`
- 执行代表性动态用例：`python3 testdata/run_test.py python/20260129-complex-types-311`

## 未执行项

- Python 2.7 完整运行时动态验收未执行，仅完成语法级检查。
  - **未执行原因**: 当前已有代表性主链路通过，但 AC5 已因测试链路残留而失败，继续扩展高成本动态验证收益较低。
  - **可能影响**: AC6 的运行时层面仍存在覆盖不足。
- 不同 glibc 版本测试未执行。
  - **未执行原因**: 当前环境单一，且本轮以迁移主链路验证为主。
  - **可能影响**: 兼容性项未完全闭合。
- 不同 Node.js 版本测试未执行。
  - **未执行原因**: 当前关键 smoke 已覆盖代表性 Python 主链路，Node.js 扩展回归不属于本轮阻塞定位所必需。
  - **可能影响**: AC2/AC8 在 Node.js 场景下仍需后续补充覆盖。
- 大内存性能场景未执行。
  - **未执行原因**: 当前需求核心是目录迁移正确性，且已有明确失败点，不适合继续投入高成本性能验证。
  - **可能影响**: 性能项暂无结论。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## Findings

- 发现 1：`qa/issue110/prepare_migration_test.go:156` 仍引用 `postman-db`，导致 AC5 未完全满足。
- 发现 2：`dev-flow/test-cases.md` 的测试文件清单与仓库实际覆盖位置不一致，文档准确性不足。

## 总结

- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不发布**。请先修正迁移回归测试链路中的旧目录残留，并同步修正文档中的测试文件清单；之后再重新执行 QA。
