# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态用例”执行验收。`./maze --build`、关键 Go 测试、Python 2.7/3.x 脚本语法检查以及代表性 `testdata` 动态用例均通过，但仍发现一处与最新 human review 强约束 **“不要兼容 ./postman-db/”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线目录。该问题说明回归链路尚未完全收口到 `db/` 语义，因此总体结论为 FAIL。

## Findings

- 主运行链路已基本迁移到 `db/`：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`main.go`、`.gitignore` 及 Python/GDB 工具脚本均已切换。
- `qa/issue110/prepare_migration_test.go:156` 仍显式使用 `filepath.Join(legacyRoot, "postman-db")`，与最新 human review 冲突。
- `dev-flow/test-cases.md` 声称新增 `qa/issue153/db_path_migration_test.go`，但仓库中不存在该文件；实际覆盖由 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go` 提供，测试文档与代码清单存在不一致。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `"db"`；`prepare/args.go` 的 `BuildOutputDir()` 默认拼接 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在；静态检查未见默认输出仍指向 `postman-db`。
- **备注**: 满足 AC1。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已优先读取 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切到 `db` 语义；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线结果。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，但通过前提仍是测试代码继续消费 `postman-db` 基线夹具。
- **备注**: 与最新 human review“不要兼容 ./postman-db/”直接冲突，故本项判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main_db_layout_test.go` 覆盖 `copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志的行为；静态检查未见回退到 `postman-db/*/maze.log`。
- **关键指标**: `main_db_layout_test.go::TestCopyResultFilesReadsLogsFromDBDirectory` 存在。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py` 已使用 `./db` 与 `./db/elf`；关键 Python/GDB 脚本语法检查通过。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6 的基础兼容性要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；正式文档相关改动已进入 diff，主规范不再以 `postman-db/` 作为当前输出目录。
- **关键指标**: `.gitignore:51` 为 `db/`；静态 diff 显示 `doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已更新。
- **备注**: 历史 dev-log 中仍出现 `postman-db` 属历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，Maze 运行、结果读取与 `validate.py` 校验均通过，未见对 `postman-db/` 的依赖报错。
- **关键指标**: `1 passed, 0 failed`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，并满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本在 Python 2.7 与 Python 3.x 下均可完成 `py_compile` 语法检查。
- **关键指标**: `python2 -m py_compile ...` 与 `python3 -m py_compile ...` 均成功。
- **备注**: 本轮仅完成基础兼容性静态验证，未执行完整双版本运行时链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review，当前回归链路也应完全收口到 `db/` 语义，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 定位 `compareLegacyAndPrepareSnapshot()`。
  3. 查看第 156 行，可见仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 运行 `go test -count=1 ./prepare ./qa/issue110 .`，虽测试通过，但验证依据仍依赖旧目录基线。
- **可能原因**: 本轮实现主要收口了源码主链路，但回归测试夹具与断言未同步迁移，导致测试链路仍保留旧目录语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- 未执行更多高成本扩展回归用例（如 Node.js/C++ 目录迁移动态回归、不同 glibc 版本、不同 Node.js 版本），原因是阶段 A 已发现明确的范围残留问题，继续追加高成本动态验证对当前 FAIL 结论增益有限。
- 未执行完整 Python 2.7/3.x 运行时双链路分析，仅完成语法级检查；这会影响 AC6 的更强运行时兼容性结论置信度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
