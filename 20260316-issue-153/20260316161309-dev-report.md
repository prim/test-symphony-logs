## Summary
修复了 human review 明确指出的“不要兼容 ./postman-db/”问题：删除 `prepare/process.go` 与 `gdb/gdb-scripts/gdb_tool.py` 中对 `postman-db` 的读取回退，只保留 `db/` 作为运行期与调试读取目录；同时同步更新 `prepare/db_path_behavior_test.go`，移除与当前约束冲突的 legacy 回退断言。

## Changes
- `prepare/process.go`: 删除 `postman-db` 的 maps.gdb 与 elf/md5 路径回退，统一只读取 `db/`。
- `gdb/gdb-scripts/gdb_tool.py`: `att()` 调试入口改为固定从 `./db/<name>`、`./db/elf/<md5>`、`./db/<name>/core.<pid>` 读取。
- `prepare/db_path_behavior_test.go`: 改写行为测试，验证仅使用 `db/` 路径并移除 legacy `postman-db` 回退用例。

## Testing
- `./maze --build`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `go test -count=1 ./prepare`：通过
- `go test -count=1 .`：通过
- `go test -count=1 ./qa/issue110`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
