## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- `qa/issue110/prepare_migration_test.go:156` 仍显式读取 `filepath.Join(legacyRoot, "postman-db")`，迁移回归链路未完全收口到 `db/` 语义，不满足 AC5。
- `dev-flow/test-cases.md` 声明的测试文件 `qa/issue153/db_path_migration_test.go` 在仓库中不存在，测试文档与实际覆盖位置不一致，增加了验收误导风险。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `"db"`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `POSTMANDB="db"`；默认输出示例 `./db/demo-tester-42`。
- **备注**: 满足 AC1。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 主输出和优先查找路径已切到 `db/`，但 `qa/issue110/prepare_migration_test.go:156` 仍把 `tmp/legacy-base/postman-db` 当作当前基线输入目录读取。
- **关键指标**: `prepare/process.go` 仍含历史 fallback：`./postman-db/*-<pid>.maps.gdb`、`postman-db/elf/*`；测试文件仍含 `filepath.Join(legacyRoot, "postman-db")`。
- **备注**: 主链路基本完成迁移，但回归测试链路未完全收口到 `db/`，不满足 AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 `copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`main_db_layout_test.go` 也覆盖了该行为。
- **关键指标**: `main.go:868-869` 使用 `filepath.Join("db", "*", "maze.log")` 和 `filepath.Join("db", "*", "maze.py.log")`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py` 已将 `POSTMANDB` 和 `POSTMANDB_ELF` 切换为 `./db`、`./db/elf`；Python 2/3 语法检查通过。
- **关键指标**: `POSTMANDB="./db"`，`POSTMANDB_ELF="./db/elf"`；`python2 -m py_compile` 与 `python3 -m py_compile` 通过。
- **备注**: 满足 AC2，AC6 仅完成语法级验证。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；正式文档可见 `db/elf` 等新路径示例，如 `doc/support.md`。
- **关键指标**: `.gitignore:51` 为 `db/`。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，结果为 `1 passed, 0 failed`，Maze 日志与结果文件可正常生成并读取。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在且 Python 2.7 / 3.x 语法编译通过。
- **关键指标**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 均通过 `py_compile`。
- **备注**: 仅完成基础兼容性检查，未覆盖 Python 2.7 动态全链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 迁移测试应覆盖“旧目录输入兼容 + 新目录输出生效”，且当前回归链路不应继续把 `postman-db` 作为默认/当前语义的一部分。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 读取历史输出，导致回归链路仍绑定旧目录名。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 观察第 156 行：`legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **可能原因**: 迁移时主业务路径与文档已切换到 `db/`，但回归测试夹具/断言未同步更新，留下静态旧目录引用。
- **建议修复**: 将迁移回归测试输入与基线路径语义一并收口到 `db/`，并同步校正对应测试说明，避免 QA 与开发继续围绕旧目录名验收。

## 未执行项

- Python 2.7 动态全链路运行未执行；当前仅完成语法级兼容检查。
- 不同 glibc 版本验证未执行。
- 不同 Node.js 版本验证未执行。
- 大内存进程性能场景未执行。
- `dev-flow/test-cases.md` 中声明的 `qa/issue153/db_path_migration_test.go` 不存在；该文档核对已执行，但不存在对应代码级测试文件可复核。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile ... && python2 -m py_compile ...`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## 总结

- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不发布**。当前主实现与代表性动态用例基本通过，但 AC5 对应的迁移回归测试链路仍残留 `postman-db` 静态引用，且测试文档中的覆盖文件清单与仓库实际情况不一致，需要先修复后再复验。
