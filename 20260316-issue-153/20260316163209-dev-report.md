## Summary

本轮按最新 QA 与 human review 继续收口 `postman-db -> db` 迁移。
由于仓库约束明确禁止修改测试文件，本轮未改动 `qa/issue110/prepare_migration_test.go`；仅补齐了源码与正式开发文档中的残余 `postman-db` 表述，并完成构建、关键 Go 测试、Python 2/3 语法检查和代表性 testdata 验证。

结论：**BLOCKED**。
阻塞点是最新 QA 指向的剩余问题位于测试文件 `qa/issue110/prepare_migration_test.go`，而当前执行约束禁止修改任何测试文件，因此无法在不违反约束的前提下彻底消除该失败项。

## Changes

- `python/results.go`: 将残留注释中的示例路径从 `./postman-db/...` 更新为 `./db/...`，避免误导后续维护者。
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 按最新 human review“不要兼容 ./postman-db/”收口说明，明确当前实现与回归链路都应以 `db/` 为准，不再把 `postman-db` 描述为当前兼容输入路径。

## Testing

- `./maze --build`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `go test -count=1 ./prepare`：通过
- `go test -count=1 ./qa/issue110`：通过
- `go test -count=1 .`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 完成的任务

- [x] 清理源码中的残余 `postman-db` 注释引用
- [x] 收口正式开发文档中的路径语义说明
- [x] 完成构建、自测与代表性动态验证

## 修改的文件

- `python/results.go`: 更新残余注释路径为 `db/`
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 收口路径语义说明，移除“当前实现兼容 postman-db 输入”的表述

## 验收核对

- AC1: PASS - 默认输出目录与主工作目录已落到 `./db/<project>-<user>-<profileId>/`，现有测试通过。
- AC2: PASS - Python/GDB/Go 主链路已使用 `db/elf/`，构建与代表性验证通过。
- AC3: PASS - `main.go` 的日志回收测试通过，日志从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制成功。
- AC4: PASS - prepare/process 与 tar 场景相关路径测试通过，产物位于 `db/` 体系。
- AC5: FAIL - 最新 QA 指出的剩余问题仍在 `qa/issue110/prepare_migration_test.go`，但该文件属于测试文件，按约束禁止修改，当前无法完成收口。
- AC6: PASS - Python 2.7 / 3.x 语法编译检查通过，未见因路径迁移导致的兼容性语法问题。
- AC7: PASS - `./maze --build` 成功。
- AC8: PASS - `python/20260129-complex-types-311` 代表性场景通过，结果可正常读取。
- AC9: PASS - `.gitignore` 已忽略 `db/`。

## 已完成项

- 已清理源码残余注释中的旧目录示例
- 已收口正式开发说明中的目录语义
- 已完成构建、单测、语法检查与代表性动态测试

## 未完成项

- `qa/issue110/prepare_migration_test.go` 中仍存在 `postman-db` 基线路径引用；由于测试文件禁止修改，本轮未完成

## 未验证项

- Python 2.7 下完整动态 testdata 链路尚未执行，仅完成语法编译检查
- Node.js / C++ 更多代表性场景未在本轮追加复测

## Self-Review 发现与修复

- 发现 `python/results.go` 仍保留旧目录注释，虽不影响运行，但会误导后续维护，已修正。
- 发现现有 dev-log 仍延续早期“兼容 postman-db 输入”的描述，与最新 human review 冲突，已更新为 `db/` 收口表述。
- 未修复项：QA 明确指出的测试文件残留路径，因“禁止修改测试文件”约束而保留，并在此标记为阻塞。

## 注意事项

- 最新 human review 明确要求“不要兼容 ./postman-db/”，本轮所有新增说明均以该约束为最高优先级。
- 当前阻塞不是代码实现不可行，而是修复点位于测试文件；若要彻底消除 QA 失败项，需要人工确认是否放宽“禁止修改测试文件”约束，或由 QA 调整对应测试夹具与断言。
