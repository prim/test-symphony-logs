# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态验证”执行验收。`./maze --build`、关键 Go 回归测试、Python 2.7/3.x 脚本语法检查以及代表性 `testdata` 动态用例均通过；主链路的 `db/` 迁移也基本成立。但仍发现一处与最新 human review 强约束 **“不要兼容 ./postman-db/”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明回归链路尚未完全收口到 `db/` 语义，因此总体结论为 FAIL。

## Findings
- `common/const.go`、`prepare/args.go`、`prepare/process.go`、`main.go`、`cmd/*.py`、`gdb/gdb-scripts/gdb_tool.py` 已完成主路径到 `db/` 的迁移，运行期主链路未见新的 `postman-db` 依赖。
- `prepare/prepare.go` 的 snapshot-only 基线路径已切换到 `tmp/legacy-base/db`，代表性动态用例日志与输出也显示实际产物落在 `db/` 下。
- 但 `qa/issue110/prepare_migration_test.go:156` 仍将 `tmp/legacy-base/postman-db` 作为当前回归输入路径，违反最新人审“不要兼容 ./postman-db/”要求，导致 AC5 未闭合。
- 另有 `main_db_layout_test.go:47` 对“不创建 legacy 输出目录 postman-db”的断言存在旧目录文本，但其语义是负向校验，不构成运行期兼容残留；本轮失败主因仍是 `qa/issue110` 回归输入路径未收口。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `db`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在；静态检查未见默认输出仍指向 `postman-db`。
- **备注**: 符合 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍使用 `tmp/legacy-base/postman-db` 作为当前回归输入。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，但静态检查确认第 156 行仍存在旧目录引用。
- **备注**: 与最新人审“不要兼容 ./postman-db/”冲突，AC5 未通过。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 通过 `copyFirstMatch(filepath.Join("db", "*", "maze.log"), ...)` 与 `copyFirstMatch(filepath.Join("db", "*", "maze.py.log"), ...)` 回收日志。
- **关键指标**: 静态检查未见 `postman-db/*/maze.log` 回收逻辑。
- **备注**: 符合 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`。
- **关键指标**: `python2 -m py_compile ... && python3 -m py_compile ...` 通过。
- **备注**: 语法兼容性通过，运行期主路径未见 `postman-db` 依赖。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已改为 `db/` 示例。
- **关键指标**: 静态检查未见正式文档仍要求使用 `postman-db/` 作为当前规范。
- **备注**: 符合 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；输出日志显示 `output dir: ./db/coredump-default-2636550`。
- **关键指标**: 1 passed, 0 failed；`maze.log` 中 ELF 路径为 `tmp/.../db/elf/...`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，符合 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在且可被 Python 2.7、Python 3.x 成功 `py_compile`。
- **关键指标**: `python2 -m py_compile` 与 `python3 -m py_compile` 均返回成功。
- **备注**: 本轮仅完成语法级兼容性检查，未覆盖 Python 2.7 完整运行时动态链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新人审要求，当前实现与回归链路都应收口到 `db/` 语义，不应继续以 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 定位第 156 行。
  4. 可见当前回归链路仍从 `tmp/legacy-base/postman-db` 读取基线目录。
- **可能原因**: 之前实现先迁移了业务主链路与 snapshot-only 基线路径，但 `qa/issue110` 回归夹具/断言未同步收口，遗留旧目录输入语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `tmp/legacy-base/db` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 已执行的检查项
- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告 `dev-flow/20260316173047-dev-report.md`。
- 审查 `git diff origin/master...HEAD` 与关键源码文件，核对 `db/` 主链路迁移覆盖面。
- 执行 `./maze --build`。
- 执行 `go test -count=1 ./prepare ./qa/issue110 .`。
- 执行 Python 2.7/3.x 语法检查。
- 执行代表性 testdata：`python3 testdata/run_test.py python/20260129-complex-types-311`。
- 检查 `maze.log` 与动态输出日志，确认运行期产物位于 `db/` 体系。

## 未执行项
- 未继续执行更多高成本动态回归（如更多 Node.js / C++ 用例、不同 glibc / Node.js 版本场景、大内存性能场景）。
- **原因**: 阶段 A 静态检查已确认 AC5 存在明确范围缺失；继续扩展动态验证对当前 FAIL 结论帮助有限，故按早停策略停止阶段 C 扩展回归。
- **影响范围**: 主要影响 AC5、以及与“回归链路是否完全收口到 `db/`”相关的 TC-002；不影响本轮已验证通过的 AC1/2/3/4/7/8/9 的主链路结论。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（1 passed, 0 failed）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **当前不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但 `qa/issue110/prepare_migration_test.go` 仍残留 `postman-db` 当前输入依赖，未满足最新 human review 的强约束，需修复后再复验。
