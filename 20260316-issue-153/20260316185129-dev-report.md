## Summary
修复了 QA 指出的两处源码残留：移除 `prepare/process.go` 中对 `postman-db` 的 maps.gdb / elf 运行时回退，以及移除 `gdb/gdb-scripts/gdb_tool.py` 中对 `./postman-db` / `./postman-db/elf` 的候选路径回退。

## Changes
- `prepare/process.go`: 删除 `./postman-db/*-<pid>.maps.gdb` 与 `postman-db/elf/*` 的运行时回退，仅保留 `db/` 路径。
- `gdb/gdb-scripts/gdb_tool.py`: 删除 profile 目录与 ELF 目录的 `postman-db` 候选路径，仅保留 `./db` / `./db/elf`。
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 追加 fix iteration #22 记录，说明本轮完成项、未完成项、未验证项与 PARTIAL 结论。

## Testing
- `./maze --build`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python2 -m py_compile ...`：通过
- `go test -count=1 ./prepare .`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## 验收核对
- AC1: PASS - 默认输出目录仍为 `./db/<project>-<user>-<profileId>/`。
- AC2: PASS - ELF 路径仅读取 `db/elf`，源码侧已移除 `postman-db/elf` 回退。
- AC3: PASS - 日志回收逻辑未回退，仍从 `db/*/maze.log` 与 `db/*/maze.py.log` 读取。
- AC4: PASS - prepare/process 路径继续收口到 `db/`，本轮移除了残余 legacy 运行时回退。
- AC5: PARTIAL - 测试文件 `qa/issue110/prepare_migration_test.go` 仍引用 `postman-db`，但本轮受限于禁止修改测试文件。
- AC6: PASS - Python 2/3 语法检查通过；动态 Python 2.7 全链路仍未执行。
- AC7: PASS - `./maze --build` 成功。
- AC8: PASS - 代表性 testdata 用例通过，结果可正常读取。
- AC9: PASS - `.gitignore` 已忽略 `db/`，本轮未回退。

## Remaining Work
- `qa/issue110/prepare_migration_test.go` 的旧目录静态引用仍待迁移到 `db/` 语义。
- `dev-flow/test-cases.md` 仍声明不存在的 `qa/issue153/db_path_migration_test.go`，与实际覆盖位置不一致。
