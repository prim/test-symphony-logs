## Summary

修复了 code review 指出的两处 legacy 兼容缺失：`prepare/process.go` 现在会在优先读取 `db/*.maps.gdb` 的同时回退识别 `postman-db/*.maps.gdb`；`gdb/gdb-scripts/gdb_tool.py` 现在可同时解析 `db/` 与历史 `postman-db/` 中的 profile、ELF 和 core 产物，避免手工调试链路丢失旧目录输入兼容能力。

结论：PARTIAL。本轮仅修复最新 code review 问题并完成定向验证；未重新执行代表性 `testdata` 动态用例，因此不宣称全部范围再次完整验证。

## Changes

- `prepare/process.go`: 为 `maps.gdb` 增加 `db/` 优先、`postman-db/` 回退的候选列表，恢复 legacy 输入兼容。
- `gdb/gdb-scripts/gdb_tool.py`: 为 `att()` 增加统一的候选路径解析，兼容历史 `postman-db` profile/elf/core 产物读取。
- `prepare/db_path_behavior_test.go`: 新增 legacy `postman-db/*.maps.gdb` 回退测试，保留并验证 `db` 优先策略。

## Testing

- `./maze --build`
- `go test -count=1 ./prepare`
- `go test -count=1 .`
- `go test -count=1 ./qa/issue110`
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py`
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py`

以上命令本轮均通过。

## 验收核对

- AC1: PARTIAL - 本轮未重新执行动态目录创建验证，但相关实现未回退，已有历史 QA 通过记录。
- AC2: PASS - GDB 调试入口已补齐对 legacy `postman-db/elf` 的回退读取。
- AC3: PARTIAL - 本轮未重跑日志回收动态验证；代码未触及该路径，已有历史 QA 通过记录。
- AC4: PASS - `prepare/process.go` 现满足新输出路径优先且保留旧输入兼容的设计要求。
- AC5: PASS - `prepare` 与 `qa/issue110` 定向回归通过，且新增了 legacy `maps.gdb` 回退测试。
- AC6: PASS - 关键 Python 脚本在 Python 2.7 与 Python 3 下 `py_compile` 通过。
- AC7: PASS - `./maze --build` 通过。
- AC8: PARTIAL - 本轮未重新执行代表性 `testdata` 动态场景。
- AC9: PARTIAL - 本轮未再检查 `.gitignore`，但该文件未被此次修复改动。

## 剩余工作

- 未完成项: 无新增代码修复项。
- 未验证项: 代表性 `testdata` 动态场景、日志回收动态链路、本轮修复后的手工 GDB 调试实际打开旧产物链路。

## Self-Review 发现与修复

- 发现 `cacheProcMaps()` 之前只认 `./db/*.maps.gdb`，会丢失历史 prepare/GDB 产物读取；已修复为新路径优先、旧路径回退。
- 发现 `gdb_tool.py` 的 `att()` 之前固定读取 `./db/...`，无法复用已有 `postman-db` 手工调试产物；已加入统一候选路径解析。

## 注意事项

- 保持了“新输出写入 `db/`、旧输入允许从 `postman-db/` 读取”的分层语义，未把 legacy 路径重新引入写入链路。
- 本轮遵循 human review 约束，未增加 `cmd/maze_entry.py`。
