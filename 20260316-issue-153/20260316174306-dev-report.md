## Summary

本轮未新增业务源码修复点；按最新 QA 报告与 human review 再次核对后，确认唯一未闭合项仍是 `qa/issue110/prepare_migration_test.go:156` 对 `filepath.Join(legacyRoot, "postman-db")` 的静态引用。由于当前系统级约束禁止修改测试文件，本轮只能补充正式开发记录、复跑必要验证，并将结论维持为 `BLOCKED`。

## Changes

- `dev-log/2026-03-16-move-postman-db-to-db.md`: 补充 fix iteration #12 记录，明确已完成项、未完成项、未验证项以及阻塞结论。

## Testing

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 开发报告

# 开发报告

## 完成的任务
- [x] Task 1: 复核最新 QA 报告、code review、原始计划与 human review，确认当前唯一未闭合项仍为测试文件中的旧目录静态引用
- [x] Task 2: 补充正式 dev-log，记录 fix iteration #12 的阻塞事实与验收状态
- [x] Task 3: 重新执行构建、Go 回归、Python 语法检查与代表性动态用例验证

## 修改的文件
- `dev-log/2026-03-16-move-postman-db-to-db.md`: 新增 fix iteration #12 补充说明，更新已完成项、未完成项、未验证项与 BLOCKED 结论

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `go test -count=1 ./prepare ./qa/issue110 .`
- 测试结果: 通过
- 测试命令: `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，输出 `1 passed, 0 failed`

## 验收核对
- AC1: PASS 已通过既有实现与测试确认默认输出目录为 `./db/<project>-<user>-<profileId>/`
- AC2: PASS 已通过脚本路径复核与 Python 语法检查确认 `db/elf/` 读取链路正常
- AC3: PASS `main.go` 日志回收已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制结果
- AC4: PASS prepare/process 产物路径已统一到 `db/` 体系
- AC5: FAIL `qa/issue110/prepare_migration_test.go:156` 仍引用 `filepath.Join(legacyRoot, "postman-db")`，且本轮禁止修改测试文件
- AC6: PASS Python 2.7/3.x 语法级兼容检查通过
- AC7: PASS `./maze --build` 通过
- AC8: PASS 代表性用例 `python/20260129-complex-types-311` 通过，结果可正常读取
- AC9: PASS `.gitignore` 已忽略 `db/`

## 剩余工作
- 未完成项: `qa/issue110/prepare_migration_test.go` 仍需迁移到 `db/` 语义
- 未验证项: Python 2.7 全链路动态运行、不同 glibc / Node.js 版本、大内存性能场景未在本轮扩展验证

## Self-Review 发现与修复
- 无新增源码缺陷；本轮确认阻塞点仅位于测试文件中的旧路径静态引用

## 注意事项
- 最新 human review 明确要求“不要兼容 `./postman-db/`”，因此不能通过恢复运行时兼容分支来规避 QA 失败
- 当前流程禁止修改测试文件，导致 AC5 无法在本轮闭合

## 结论

`BLOCKED`: 当前唯一阻塞点为 `qa/issue110/prepare_migration_test.go` 仍保留 `postman-db` 旧目录语义，而本轮流程禁止修改测试文件。
