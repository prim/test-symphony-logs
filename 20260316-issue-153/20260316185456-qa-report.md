# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”完成了代码静态核对、构建、Go 回归、Python 2/3 语法检查以及 1 个代表性 testdata 动态用例。主链路的 `db/` 迁移基本生效，但仍存在 1 个会影响 AC5 判定的残留：`qa/issue110/prepare_migration_test.go` 仍从 `tmp/legacy-base/postman-db` 读取历史基线目录，导致迁移测试没有完全收口到 `db/` 语义。此外，`dev-flow/test-cases.md` 中声明的测试文件 `qa/issue153/db_path_migration_test.go` 与仓库实际测试文件不一致，存在测试文档与实现脱节问题。

## Findings
- FAIL: `qa/issue110/prepare_migration_test.go:156` 仍使用 `filepath.Join(legacyRoot, "postman-db")`，与当前迁移目标“输出与当前回归语义统一到 db”不一致，AC5 不能判定通过。
- FAIL: `dev-flow/test-cases.md` 的 `## Test File(s)` 声明 `qa/issue153/db_path_migration_test.go`，但仓库实际新增测试位于 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go`，测试用例文档与实现覆盖位置不一致。
- PASS: Go、Python/GDB、日志回收、`.gitignore` 和代表性动态链路均已落到 `db/`，未发现新的业务运行时 `postman-db` 回退。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB = "db"`；`prepare/args.go` 的 `BuildOutputDir()` 通过 `currentPrepareDBRoot(".")` 生成默认输出目录 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `POSTMANDB="db"`；默认输出模板为 `./db/demo-tester-42`。
- **备注**: 与 AC1、AC4 一致。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot baseline 根目录也已改为 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找历史输出。
- **关键指标**: `prepare/process.go:48` 为 `./db/*-%s.maps.gdb`；`prepare/prepare.go:281`、`373` 为 `tmp/legacy-base/db`；`qa/issue110/prepare_migration_test.go:156` 仍是 `postman-db`。
- **备注**: 当前源码主链路已迁移，但迁移测试本身未完全收口，AC5 未满足。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `prepareTarPostmanLayout()` 在临时目录创建 `db/`、`db/elf/` 与 profile 子目录；`copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志。
- **关键指标**: `main.go:868-869` 使用 `filepath.Join("db", "*", "maze.log")` 与 `filepath.Join("db", "*", "maze.py.log")`。
- **备注**: `main_db_layout_test.go` 已覆盖该行为。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`，未见新的 `./postman-db` 候选路径回退。
- **关键指标**: `POSTMANDB="./db"`；`POSTMANDB_ELF="./db/elf"`；`gdb_tool.py` 候选目录仅返回 `./db/<name>` 与 `./db/elf`。
- **备注**: Python 2 与 Python 3 语法检查均通过。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md` 中中间文件示例改为 `db/<project>-<user>-<profileId>/` 与 `cat db/*/pyo`；`doc/support.md` 中错误示例路径也已切换到 `db/elf`。
- **关键指标**: `.gitignore:51` 为 `db/`。
- **备注**: 正式文档入口未见新的 `postman-db` 误导示例。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 会清理 `db` 缓存并从 `db/*/maze.log`、`db/*/maze.py.log` 识别日志；执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，maze 控制台输出显示 `output dir: ./db/coredump-default-2636550`。
- **关键指标**: 动态验收结果 `1 passed, 0 failed`；`tmp/python-20260129-complex-types-311.maze-output.log:15` 为 `output dir: ./db/coredump-default-2636550`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过，路径常量迁移未引入明显语法兼容问题。
- **关键指标**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 的 Python 2/3 语法检查通过。
- **备注**: 仍缺少真实 Python 2.7 全链路动态运行验证，因此 AC6 只能判定为基础兼容通过、非完整环境覆盖。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前输出路径与迁移测试语义应统一收口到 `db/`；如果保留历史兼容，也应在明确限定范围内，不应让当前迁移测试继续把 `postman-db` 作为基线目录入口。
- **实际行为**: 业务代码已迁移到 `db/`，但 `qa/issue110/prepare_migration_test.go` 仍从 `tmp/legacy-base/postman-db` 读取历史结果目录。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 观察第 156 行：`legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **可能原因**: 迁移实现已更新主流程与 snapshot baseline 根目录，但回归测试文件未同步完成 `postman-db -> db` 收口。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的当前基线目录语义更新为 `db`，并重新核对该测试是否仍准确覆盖“旧目录输入兼容 + 新目录输出生效”的需求边界。

## 未执行项
- 未执行更多高成本动态用例（如 Node.js、C++、大内存/性能场景、不同 glibc / Node.js 版本矩阵）。
- **未执行原因**: 本轮阶段 A/B 已足以确认主链路可运行，但同时已发现 AC5 的明确静态阻塞；继续扩展动态回归无法改变总体 FAIL 结论，故按早停策略停止阶段 C。
- **影响范围**: 主要影响 AC6 的完整环境覆盖、AC8 的更广泛代表性验证，以及性能/兼容性扩展结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果汇总
- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **当前不建议判定为可发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中仍引用 `postman-db` 的迁移测试残留，并同步修正文档中的测试文件清单，再重新进行 QA 验收。
