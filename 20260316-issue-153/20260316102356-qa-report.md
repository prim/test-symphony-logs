## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings
- `postman-db -> db` 主路径迁移的核心实现已落地：Go 常量、prepare/process、main 日志回收、Python/GDB 工具链、`testdata/run_test.py`、正式文档与 `.gitignore` 已统一切换到 `db/`。
- 代表性动态场景 `python/20260129-complex-types-311` 已通过，证明新目录规则下结果可以从 `db/` 正确生成并读取。
- 仍存在关键兼容回归未闭环：`go test ./qa/issue110` 失败，导致 AC5“旧目录输入兼容 + 新目录输出生效”的自动化回归链路未通过。
- `qa/issue110` 失败包含两类问题：其一，`TestMazeEntryIsNoLongerPythonWrapper` 断言当前仓库 `maze` 入口仍是 Python wrapper；其二，`TestPrepareRunMatchesLegacyJSONBaseline` 依赖的 `tmp/legacy-base` 基线夹具缺失。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `db`；`prepare/args.go` 默认输出目录为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 静态检查通过；`go test ./qa/issue153` 通过。
- **备注**: 满足 AC1。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: 代码层面已保留 `postman-db/elf` 与 `tmp/legacy-base/postman-db` 的兼容路径；但 `go test ./qa/issue110` 未通过，自动化兼容回归链路失败。
- **关键指标**: `go test ./qa/issue110` 失败 2 项。
- **备注**: 影响 AC5；AC4 的静态路径切换本身已完成，但“旧目录输入兼容”未被动态回归验证证明。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 tar 布局已使用 `tempDir/db/...`；日志回收匹配已切换为 `db/*/maze.log` 与 `db/*/maze.py.log`。
- **关键指标**: 静态检查通过；`go test ./qa/issue153` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 等关键脚本默认路径已切换到 `./db` / `./db/elf`。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2.7 -m py_compile ...` 通过。
- **备注**: 满足 AC2；AC6 获得语法级基础验证，但未覆盖完整双版本运行时链路。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已更新为 `db/` 示例。
- **关键指标**: 静态检查通过；`go test ./qa/issue153` 通过。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已按 `db/` 语义清理缓存、探测日志并读取结果；代表性用例实际运行成功。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键脚本在 Python 2.7 与 Python 3 下均可通过 `py_compile`。
- **关键指标**: Python 2.7/3.x 语法编译均通过。
- **备注**: 属于基础兼容性检查，通过但不等同于完整运行时兼容验收。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前输出统一落到 `db/`，并且针对历史 `postman-db` 输入的迁移兼容回归测试可执行且通过。
- **实际行为**: `go test ./qa/issue110` 失败：
  1. `TestMazeEntryIsNoLongerPythonWrapper` 报错“maze 入口仍然是 Python wrapper，而不是统一的 Go 入口”；
  2. `TestPrepareRunMatchesLegacyJSONBaseline` 报错缺少 `tmp/legacy-base`：`stat /home/hcjn0770/temp/worktrees/issue-153/tmp/legacy-base: no such file or directory`。
- **复现步骤**:
  1. 在仓库根目录执行 `go test ./qa/issue110`
  2. 观察上述两个失败点
- **可能原因**:
  1. 兼容回归测试对仓库入口形态有额外假设，而当前仓库仍保留 Python wrapper 入口；
  2. 旧目录输入兼容验证依赖的 legacy 基线夹具未提供，导致测试链路不完整。
- **建议修复**:
  1. 明确 `qa/issue110` 对入口形态的预期是否仍为本需求强约束，若是则需补齐对应实现；
  2. 补齐或恢复 `tmp/legacy-base` 回归夹具，确保“旧目录输入兼容”可以被自动化验证。

## 未执行项
- Node.js 与 C++ 代表性动态用例未在本轮复测。
  - 原因: 已完成 1 个代表性主链路用例与关键 smoke，但本轮主要阻塞集中在 `qa/issue110` 兼容回归失败。
  - 可能影响: AC8 的跨语言覆盖深度不足，不能证明所有语言场景都已在 `db/` 规则下回归通过。
- 不同 glibc 版本、不同 Node.js 版本的兼容性动态验证未执行。
  - 原因: 当前环境单一，且计划优先级放在主路径迁移与关键兼容回归。
  - 可能影响: AC6 的环境兼容性结论仍不完整。
- 性能验证未执行。
  - 原因: 本需求核心为路径迁移，且存在兼容回归失败，继续高成本性能测试收益较低。
  - 可能影响: 无法确认路径迁移是否引入额外性能回退。

## 兼容性验证
- [x] Python 2.7 测试通过（语法编译级）
- [x] Python 3.x 测试通过（语法编译级，且代表性动态用例通过）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`: 通过
- `go test ./qa/issue153`: 通过
- `python3 -m py_compile cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py testdata/run_test.py`: 通过
- `python2.7 -m py_compile cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`: 通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`: 通过
- `go test ./qa/issue110`: 失败

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议以 PASS 验收通过。** `db/` 主路径迁移本身基本完成，但旧目录输入兼容回归链路未通过，需先修复/补齐 `qa/issue110` 的阻塞项后再复验。
