## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- 主运行链路已基本完成 `postman-db -> db` 迁移：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、`cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 均已切到 `db/` 体系。
- `./maze --build`、`go test -count=1 ./prepare ./qa/issue110 .`、Python 2.7/3.x 脚本语法检查以及代表性动态用例 `python/20260129-complex-types-311` 本轮均通过。
- 但 `qa/issue110/prepare_migration_test.go:156` 仍显式使用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，与最新人审强约束 **“不要兼容 ./postman-db/”** 直接冲突，因此 AC5 未闭合，整体验收失败。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `db`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在且 `go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC1、AC4 的主路径切换要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍将 `tmp/legacy-base/postman-db` 作为当前回归输入路径。
- **关键指标**: `grep` 明确命中 `qa/issue110/prepare_migration_test.go:156`；未再发现业务主链路读取 `./postman-db/*-<pid>.maps.gdb`。
- **备注**: 与最新人审“不要兼容 ./postman-db/”冲突，AC5 未通过。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:868-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志。
- **关键指标**: `main_db_layout_test.go::TestCopyResultFilesReadsLogsFromDBDirectory` 存在；静态检查未见 `postman-db/*/maze.log` 回收逻辑。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py` 中 `POSTMANDB`/`POSTMANDB_ELF` 已切到 `./db`/`./db/elf`；GDB 脚本示例也改为 `db/`。
- **关键指标**: Python 2.7/3.x `py_compile` 检查通过。
- **备注**: 满足 AC2、AC6 的基础静态与语法兼容要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md` 中运行产物示例已切到 `db/`；正式文档未发现要求继续使用 `postman-db/` 作为当前规范的入口说明。
- **关键指标**: `.gitignore:51` 为 `db/`；`doc/debugging.md:73,76` 使用 `db/` 示例。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，结果通过，`maze.log` 正常输出，结果文件读取链路可用。
- **关键指标**: `1 passed, 0 failed`；校验项全部通过，包括 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，并支撑 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `python2 -m py_compile` 与 `python3 -m py_compile` 均通过，未因路径常量迁移引入语法错误。
- **关键指标**: 检查文件包括 `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py`。
- **备注**: 本轮完成语法级兼容验证；未覆盖真实双版本运行时行为差异。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新人审要求，当前实现与当前回归链路都应完全收口到 `db/` 语义，不应继续以 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 定位到第 156 行，可见当前回归链路仍从 `tmp/legacy-base/postman-db` 读取基线目录。
- **可能原因**: 业务源码迁移已完成，但测试/回归夹具未同步收口，保留了旧目录名作为当前验证输入。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `tmp/legacy-base/db` 语义，避免继续把 `postman-db` 当作当前回归输入。

## 未执行项

- 未执行更多高成本扩展动态回归（如更多语言/分配器 testdata、性能场景、不同 glibc/Node.js 版本矩阵）。
- 原因: 阶段 A 已发现与人审强约束直接冲突的明确范围问题，继续扩大动态覆盖无法改变 AC5 失败结论。
- 可能影响 AC: AC2、AC6、AC8 的更广泛兼容性与覆盖深度；但不影响本轮对 AC5 失败的判定。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（1 passed, 0 failed）

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **当前不建议发布**。`db/` 主链路迁移、构建、脚本语法检查与代表性动态用例均已通过，但 `qa/issue110/prepare_migration_test.go` 仍残留 `postman-db` 当前输入依赖，未满足最新 human review 的强约束，需修复后再复验。
