## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings

- 已复测此前阻塞项：`qa/issue110` 当前已通过，说明 `maze` 入口兼容性问题与 `tmp/legacy-base` 缺失问题已被修复。
- `postman-db -> db` 主路径迁移在 Go 主流程、Python/GDB 工具链、日志回收、文档与忽略规则上已闭环，同时保留了 `postman-db/elf` 与 `tmp/legacy-base/postman-db` 的历史输入兼容语义。
- 代表性动态用例 `python/20260129-complex-types-311` 在新目录规则下通过，输出日志显示工作目录落在 `./db/...`，满足本需求核心验收目标。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已为 `db`；`prepare/args.go` 默认输出目录为 `./db/%s-%s-%s`。
- **关键指标**: 静态检查通过；`go test -count=1 ./qa/issue153` 通过。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 优先使用 `db/*-%s.maps.gdb`、`db/elf/*`，同时保留 `postman-db/elf/*` 回退；`prepare/prepare.go` 与 `qa/issue110/prepare_migration_test.go` 继续保留 `tmp/legacy-base/postman-db` 历史兼容语义。
- **关键指标**: `go test -count=1 ./qa/issue153`、`go test -count=1 ./qa/issue110` 均通过。
- **备注**: 满足 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 tar 模式回收逻辑从 `db/*/maze.log` 与 `db/*/maze.py.log` 收集日志，不再依赖 `postman-db/*`。
- **关键指标**: `go test -count=1 ./qa/issue153` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` / `./db/elf`。
- **关键指标**: 静态检查通过；`python3 -m py_compile ...` 与 `python2.7 -m py_compile ...` 通过。
- **备注**: 满足 AC2、AC6。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；正式文档示例已切换到 `db/` 规范。
- **关键指标**: 静态检查通过；`go test -count=1 ./qa/issue153` 通过。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理并读取 `db` 产物；执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功。
- **关键指标**: `1 passed, 0 failed`；日志中出现 `output dir: ./db/coredump-default-2636550`。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在、非空，且在 Python 2.7 / 3.x 下均可通过语法编译检查。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2.7 -m py_compile ...` 通过。
- **备注**: 满足 AC6。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./qa/issue153`：通过
- `go test -count=1 ./qa/issue110`：通过
- `python3 -m py_compile maze cmd/maze_entry.py cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py testdata/run_test.py`：通过
- `python2.7 -m py_compile maze cmd/maze_entry.py cmd/postman_prelude.py cmd/maze-gdb-coredump.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未扩展复测 Node.js / C++ 额外代表性动态场景。
- 未执行不同 glibc 版本、不同 Node.js 版本的跨环境动态验证。
- 原因：本轮以 issue #153 的核心迁移回归为主，关键 smoke test 与兼容回归链路均已通过，未再追加高成本跨环境扩展测试。
- 可能影响 AC：主要影响 AC6 的更强兼容性外延验证，但不影响本轮已完成的 Python 2.7 / 3.x 语法兼容与核心主链路验收结论。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以进入后续发布/合并流程；若要进一步提高发布置信度，建议补充 Node.js 与 C++ 代表性动态场景，以及多 glibc / 多 Node.js 版本环境回归。
