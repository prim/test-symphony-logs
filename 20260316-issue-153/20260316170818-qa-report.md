# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态用例”执行验收。`./maze --build`、关键 Go 回归测试、Python 2.7/3.x 脚本语法检查以及代表性 `testdata` 动态用例均通过，但仍发现一处与最新 human review 强约束 **“不要兼容 ./postman-db/”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线目录。该问题说明回归链路尚未完全收口到 `db/` 语义，因此总体结论为 FAIL。

## Findings

- `prepare/args.go:40-46` 已将默认输出目录切换到 `./db/<project>-<user>-<profileId>`，满足新主路径要求。
- `main.go:773-869` 已将 tar 解包布局与日志回收路径切换到 `db/`，未再从 `postman-db/*/maze.log` 读取。
- `cmd/maze-gdb-coredump.py:42-43`、`gdb/gdb-scripts/gdb_tool.py:350-360` 已统一切换到 `./db` / `./db/elf`。
- `qa/issue110/prepare_migration_test.go:156` 仍显式使用 `filepath.Join(legacyRoot, "postman-db")`，与最新 human review 冲突。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: 静态检查确认 `common/const.go` 中 `POSTMANDB` 为 `"db"`，`prepare/args.go` 中 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在；静态 diff 未见默认输出仍指向 `postman-db`。
- **备注**: 满足 AC1、AC4 的主路径切换要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切换到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线结果。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，但通过前提仍是测试代码继续消费 `postman-db` 基线夹具。
- **备注**: 与最新 human review“不要兼容 ./postman-db/”直接冲突，因此本项判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`prepareTarPostmanLayout()` 也改为在 `db/` 下组织解包产物。
- **关键指标**: `main_db_layout_test.go` 覆盖 `copyResultFiles()` 与 tar 布局写入 `db/` 的行为。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`，静态检查未见运行期脚本继续硬编码 `./postman-db`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6 的基础要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 已改用 `db/` 作为当前规范示例。
- **关键指标**: 静态 diff 显示正式文档与忽略规则已同步更新。
- **备注**: 历史 dev-log 中仍出现 `postman-db` 属历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，Maze 运行、结果读取与 `validate.py` 校验均通过，未见对 `postman-db/` 的依赖报错。
- **关键指标**: `1 passed, 0 failed`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，并支撑 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本均存在且在 Python 2 / Python 3 下通过语法编译检查。
- **关键指标**: `python2 -m py_compile ...`、`python3 -m py_compile ...` 均返回成功。
- **备注**: 本轮完成的是语法级兼容性检查，不等同于完整双版本运行时链路验证。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`1 passed, 0 failed`

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review，当前回归链路也应完全收口到 `db/` 语义，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 定位 `compareLegacyAndPrepareSnapshot()`。
  3. 查看第 156 行，可见仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 结合最新 human review “不要兼容 ./postman-db/” 对照即可确认冲突。
- **可能原因**: 代码主链路已迁移到 `db/`，但回归测试夹具路径与基线读取逻辑未同步收口，导致测试层仍保留旧目录语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项

- 不同 glibc 版本的动态迁移回归未执行；原因是当前环境仅完成单一 Linux 环境验证；可能影响 AC2、AC4、AC8 的跨环境置信度。
- 不同 Node.js 版本的动态迁移回归未执行；原因是本轮以 issue #153 路径迁移为主，且当前阻塞已足以判定 FAIL；可能影响 AC2、AC8 在 Node.js 场景下的覆盖度。
- Python 2.7 / Python 3.x 的完整运行时双版本动态链路未执行；本轮完成的是脚本语法级检查；可能影响 AC6 的运行时兼容性结论强度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
