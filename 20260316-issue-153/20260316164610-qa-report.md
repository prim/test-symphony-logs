# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本静态检查尽量完整 + 高成本验证分阶段执行”完成验收。构建、关键 Go 测试、Python 2.7/3.x 语法检查以及代表性 `testdata` 动态用例均通过，但仍存在一处与最新 human review 强约束 **“不要兼容 `./postman-db/`”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线目录。该问题说明回归链路尚未完全收口到 `db/` 语义，因此总体结论必须为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 已将 `POSTMANDB` 常量切换为 `db`；`prepare/args.go` 的 `BuildOutputDir()` 默认输出为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go:10-39` 覆盖 `BuildOutputDir()` 与 `currentPrepareDBRoot()`，`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 未发现默认输出仍指向 `postman-db/`。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只查找 `./db/*-<pid>.maps.gdb` 且 `db/elf` 路径已切换完成；`prepare/prepare.go` 的 snapshot-only 基线目录也已改为 `tmp/legacy-base/db`。但是 `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，继续把 `postman-db` 作为当前回归输入目录。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 虽通过，但通过前提仍是测试代码继续消费 `postman-db` 基线夹具。
- **备注**: 该项与最新 human review“不要兼容 `./postman-db/`”直接冲突，因此判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志；新增 `main_db_layout_test.go` 验证 tar 布局产物与日志复制逻辑。
- **关键指标**: `main_db_layout_test.go:52-82` 通过；`go test -count=1 ./prepare ./qa/issue110 .` 中根包测试通过。
- **备注**: `main_db_layout_test.go:47-48` 同时验证不会创建 `postman-db` 目录。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py` 均已切换到 `./db` / `./db/elf`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过；静态检查未见运行期 Python/GDB 工具继续读取 `./postman-db`。
- **备注**: `cmd/maze-gdb-coredump.py` 同时补充了 Python 2/3 文本处理兼容写法。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；抽检 `doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md`，正式文档示例已切换到 `db/` 语义。
- **关键指标**: `git diff origin/master...HEAD` 显示上述文件已更新；静态搜索中未发现正式入口文档继续把 `postman-db/` 作为当前规范。
- **备注**: 历史 dev-log 中仍出现 `postman-db` 属历史记录，不作为失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，Maze 运行、结果读取与 `validate.py` 校验均通过，未见对 `postman-db/` 的依赖报错。
- **关键指标**: `1 passed, 0 failed`；`pymempool_objects = 68626`；多个 Python 类型数量断言均通过。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 3 与 Python 2.7 下均可通过语法检查，未因路径常量迁移出现语法错误。
- **关键指标**: `python3 -m py_compile ...` 通过；`python2 -m py_compile ...` 通过。
- **备注**: 本轮完成的是语法级兼容检查，不等于 Python 2.7 下完整动态链路已验证通过。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review，回归链路也应完全收口到 `db/`，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 定位 `compareLegacyAndPrepareSnapshot()`。
  3. 查看第 156 行，可见仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 运行 `go test -count=1 ./qa/issue110`，测试虽通过，但通过的前提仍是旧目录夹具存在。
- **可能原因**: 本轮修复已收口主运行链路与 snapshot-only 读取逻辑，但回归测试文件未同步迁移，导致实现层与验收链路存在语义不一致。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的基线路径与对应夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 已执行的检查项
- 阅读最新开发报告：`dev-flow/20260316164347-dev-report.md`
- 阅读 test case 报告：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 审查代码变更：`git diff origin/master...HEAD`
- 静态搜索残余路径引用：检索 `postman-db`
- 构建验证：`./maze --build`
- 关键 Go 测试：`go test -count=1 ./prepare ./qa/issue110 .`
- Python 3 语法检查：`python3 -m py_compile ...`
- Python 2.7 语法检查：`python2 -m py_compile ...`
- 代表性动态用例：`python3 testdata/run_test.py python/20260129-complex-types-311`

## 未执行项
- 不同 glibc 版本动态验证：本轮未执行，原因是当前仓库环境单一，且已有明确 FAIL 阻塞点，不继续扩大高成本动态覆盖。
- 不同 Node.js 版本动态验证：本轮未执行，原因同上。
- Python 2.7 下完整动态链路验证：本轮仅完成语法检查，未追加完整运行链路验证。

上述未执行项主要影响 AC6 的“更广覆盖证明”与潜在兼容性外延验证，但**不影响**本轮 FAIL 结论，因为当前失败已由 AC5 / TC-002 的明确残留问题单独成立。

## 兼容性验证
- [x] Python 2.7 语法检查通过
- [x] Python 3.x 语法检查通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试命令与结果摘要
- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
