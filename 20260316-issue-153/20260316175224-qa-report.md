# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。`./maze --build`、关键 Go 回归、Python 2/3 语法检查以及代表性 `testdata` 动态用例均通过，但仍发现回归链路没有完全收口到 `db/`：`qa/issue110/prepare_migration_test.go:156` 仍显式引用 `filepath.Join(legacyRoot, "postman-db")`。结合最新 human review“**不要兼容 `./postman-db/`**”的强约束，AC5 未满足，因此总体结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 已将 `POSTMANDB` 切换为 `db`；`prepare/args.go` 中 `BuildOutputDir()` 默认输出为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 静态检查命中 `common/const.go:5`、`prepare/args.go:40-42`。
- **备注**: 满足 AC1、AC4 的默认输出目录要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的当前根目录也已统一为 `db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `filepath.Join(legacyRoot, "postman-db")` 查找 legacy 输出目录。
- **关键指标**: `grep` 命中 `qa/issue110/prepare_migration_test.go:156`；`prepare/process.go:46-53` 仅包含 `./db/*-%s.maps.gdb`。
- **备注**: 与最新人审“不要兼容 `./postman-db/`”冲突，AC5 未通过。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志；新增 `main_db_layout_test.go` 覆盖 tar 解压布局与日志复制逻辑。
- **关键指标**: `main.go:868-869` 使用 `db` 通配路径；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`。
- **关键指标**: Python 2.7/3.x `py_compile` 均通过；`gdb_tool.py:350-360` 使用 `./db`。
- **备注**: 语法兼容性满足 AC2、AC6 的基础要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式说明已切换到 `db/`。
- **关键指标**: 差异中可见 `.gitignore` 由 `postman-db/` 改为 `db/`。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，结果通过，Maze 结果文件可正常读取。
- **关键指标**: `1 passed, 0 failed`；`pymempool_objects = 68626`；多个对象类型数量校验通过。
- **备注**: 满足 AC8；本轮已满足“至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在且 `python2 -m py_compile`、`python3 -m py_compile` 均通过。
- **关键指标**: `cmd/maze-gdb-coredump.py` 新增 `from __future__ import print_function` 与文本解码兼容处理后，双版本语法检查通过。
- **备注**: 本轮仅完成语法级兼容检查，未做完整 Python 2.7 运行时动态链路验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新人审要求，当前实现与当前回归链路都应完全收口到 `db/` 语义，不应继续把 `postman-db` 作为当前输入或兼容路径。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍使用 `legacyOutputDir := findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 定位到第 156 行附近
  3. 观察 legacy 输出目录查找逻辑仍硬编码 `postman-db`
- **可能原因**: 本轮迁移已覆盖主业务链路与大部分测试辅助逻辑，但 issue110 的迁移回归测试未同步收口到 `db/` 目录语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 未执行项
- Python 2.7 完整运行时动态链路未执行；本轮仅完成语法级检查，可能影响 AC6 的运行时层面结论。
- 不同 glibc 版本测试未执行；可能影响兼容性扩展结论，但不影响本轮已确认的 AC5 失败。
- 不同 Node.js 版本测试未执行；可能影响兼容性扩展结论，但不影响本轮已确认的 AC5 失败。
- 大内存性能场景未执行；可能影响性能验收项，但不影响本轮主结论。

## Findings
- `qa/issue110/prepare_migration_test.go:156` 仍保留 `postman-db` 旧目录静态引用，违反本轮 human review“不要兼容 `./postman-db/`”的收口要求。
- 主运行链路、日志回收、Python/GDB 工具链和正式文档已基本完成 `postman-db -> db` 迁移。
- 当前 FAIL 属于测试/回归链路未完全收口，不是构建失败或主流程 fatal。

## Test Results
- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不建议发布**。请先修复 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的旧目录引用，使回归链路与 `db/` 新规范一致后再重新验收。
