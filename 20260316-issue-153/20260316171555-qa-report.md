# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“阶段 A 低成本静态检查尽量完整 + 阶段 B 关键 smoke test + 阶段 C 代表性动态用例”执行验收。`./maze --build`、关键 Go 回归测试、Python 2.7/3.x 脚本语法检查以及代表性 `testdata` 动态用例均通过，但仍发现一处与最新 human review 强约束 **“不要兼容 ./postman-db/”** 直接冲突的残留：`qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线目录。该问题说明回归链路尚未完全收口到 `db/` 语义，因此总体结论为 FAIL。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `db`；`prepare/args.go` 中 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `prepare/db_path_behavior_test.go::TestBuildOutputDirDefaultsToDB` 存在；静态 diff 未见默认输出仍指向 `postman-db`。
- **备注**: 满足 AC1、AC4。

### TC-002: prepare/process 新路径生效且回归链路应收口到 db
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只查找 `./db/*-<pid>.maps.gdb`，`prepare/prepare.go` 的 snapshot-only 基线路径也已切换到 `tmp/legacy-base/db`；但 `qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线结果。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过，但通过前提仍是测试代码继续消费 `postman-db` 基线夹具。
- **备注**: 与最新 human review“不要兼容 ./postman-db/”直接冲突，因此本项判 FAIL。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go:865-869` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 回收日志；`prepareTarPostmanLayout()` 也改为在临时目录下创建 `db/` 布局。
- **关键指标**: `main_db_layout_test.go` 覆盖 `copyResultFiles()` 与 tar 解包布局；静态检查未见回退到 `postman-db/*/maze.log`。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 已切换到 `./db` / `./db/elf`，静态检查未见运行期脚本继续硬编码 `./postman-db`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过。
- **备注**: 满足 AC2、AC6 的静态与语法层要求。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例均已切换到 `db/`。
- **关键指标**: `git diff origin/master...HEAD` 显示上述文件已更新；正式入口文档未继续把 `postman-db/` 作为当前规范。
- **备注**: 历史 dev-log 中仍出现 `postman-db` 属历史记录，不作为失败依据；满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`，Maze 运行、结果读取与 `validate.py` 校验均通过，未见对 `postman-db/` 的依赖报错。
- **关键指标**: `1 passed, 0 failed`；`testdata/run_test.py` 的日志发现与清理逻辑已使用 `db/*`。
- **备注**: 满足 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 2.7 与 Python 3.x 下均完成语法编译检查，无因目录常量迁移导致的语法错误。
- **关键指标**: `python2 -m py_compile ...`、`python3 -m py_compile ...` 均返回成功。
- **备注**: 仅完成语法级验证，未覆盖 Python 2.7 完整运行时动态链路。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且回归链路应收口到 db
- **结果**: FAIL
- **预期行为**: 按最新 human review，当前回归链路也应完全收口到 `db/` 语义，不应继续使用 `postman-db` 作为当前基线输入目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`，说明测试仍依赖旧目录基线。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`。
  2. 查看 `compareLegacyAndPrepareSnapshot()`。
  3. 检查第 156 行，可见仍从 `tmp/legacy-base/postman-db` 读取基线目录。
  4. 对照最新 human review “不要兼容 ./postman-db/” 即可确认冲突。
- **可能原因**: 业务主链路已迁移到 `db/`，但 issue110 回归测试夹具与断言未同步迁移，导致测试仍保留旧目录语义。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 及其对应 baseline/夹具统一迁移到 `db/` 语义，避免继续把 `postman-db` 作为当前回归输入。

## 已执行的检查项
- 阅读最新开发报告：`dev-flow/20260316171320-dev-report.md`
- 阅读测试用例报告：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 审查分支改动：`git diff origin/master...HEAD`
- 静态核对关键文件：`common/const.go`、`prepare/args.go`、`prepare/process.go`、`prepare/prepare.go`、`main.go`、Python/GDB 工具链、文档、`.gitignore`
- 构建验证：`./maze --build`
- 关键 Go 回归：`go test -count=1 ./prepare ./qa/issue110 .`
- Python 2/3 语法兼容性检查：`python2 -m py_compile ...`、`python3 -m py_compile ...`
- 代表性动态用例：`python3 testdata/run_test.py python/20260129-complex-types-311`

## 未执行项
- Python 2.7 完整运行时动态链路验证未执行，仅完成语法级检查。
- 不同 glibc 版本测试未执行。
- 不同 Node.js 版本测试未执行。
- 大内存进程性能测试未执行。

未执行原因：当前 issue #153 的核心验收点是目录迁移正确性与回归链路收口；在已发现一处明确违反最新 human review 的残留后，继续扩大高成本兼容性/性能动态验证收益较低，因此按早停策略停止扩展回归。

可能影响范围：上述未执行项主要影响 AC6 的完整动态兼容结论，以及性能/跨版本环境的补充置信度；不改变本轮因 TC-002 失败而得到的总体 FAIL 结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前**不建议发布**。`db/` 主链路迁移、构建与代表性动态用例均已通过，但回归测试仍残留 `postman-db` 基线依赖，未满足最新 human review 的强约束，需修复后再复验。
