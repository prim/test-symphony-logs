# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本静态检查 + 关键构建/测试 + 至少一个 testdata 动态用例”执行。虽然构建、Go 测试、Python 语法检查和代表性动态用例均通过，但仍发现与最新 human review 约束不一致的遗留兼容逻辑：代码与回归测试仍显式依赖 `postman-db` 作为 legacy 输入夹具路径，未完全满足“不要兼容 `./postman-db/`”这一最新强约束。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `"db"`；`prepare/args.go` 默认输出目录为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `BuildOutputDir()` 行为测试通过；未发现默认输出仍指向 `./postman-db/...`。
- **备注**: 满足新输出目录主链路要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已只使用 `db` 路径，符合最新 human review；但 `prepare/prepare.go` 的 `snapshotOnlyLegacyOutputPath()` 与 `loadSnapshotOnlyLegacyJSON()` 仍固定读取 `tmp/legacy-base/postman-db`，`qa/issue110/prepare_migration_test.go` 也仍从 `tmp/legacy-base/postman-db` 读取历史基线。
- **关键指标**: `go test -count=1 ./qa/issue110` 虽通过，但通过的前提仍是 legacy `postman-db` 夹具存在。
- **备注**: 这说明“不要兼容 `./postman-db/`”尚未彻底收口，兼容逻辑只是从主流程收缩到了 snapshot-only / 回归基线路径。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 `prepareTarPostmanLayout()` 在 tar 场景创建 `db/` 与 `db/elf/` 结构；`copyResultFiles()` 从 `db/*/maze.log`、`db/*/maze.py.log` 回收日志。
- **关键指标**: `go test -count=1 .` 通过；`main_db_layout_test.go` 覆盖未创建 `postman-db` 输出目录。
- **备注**: AC3 主链路通过。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py` 默认均已改为 `db/`。
- **关键指标**: `python3 -m py_compile ...` 与 `python2 -m py_compile ...` 均通过；静态检查未见运行期 `./postman-db` 读写回退。
- **备注**: 与本轮最新修复目标一致。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换到 `db/`。
- **关键指标**: 静态检查通过。
- **备注**: 历史 dev-log 中残留 `postman-db` 说明属于历史记录，不作为本条失败依据。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，Maze 主流程生成并读取 `db/` 语义下的结果，验证通过。
- **关键指标**: 1/1 动态用例通过；`pymempool_objects = 68626`；多个 Python 类型计数校验全部通过。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本在 Python 2.7 与 Python 3.x 下均可通过语法编译。
- **关键指标**: `python2 -m py_compile ...` 通过；`python3 -m py_compile ...` 通过。
- **备注**: 完成了基础兼容性冒烟检查，但未覆盖多版本运行时全链路动态验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 按最新 human review 约束，不应继续兼容 `./postman-db/`；实现与测试链路都应彻底收口到 `db/`，仅保留必要的历史说明，而不是继续在代码路径中读取 `postman-db`。
- **实际行为**: `prepare/prepare.go:281`、`prepare/prepare.go:373` 仍通过 `tmp/legacy-base/postman-db` 读取 legacy snapshot；`qa/issue110/prepare_migration_test.go:156` 仍将 `postman-db` 作为历史输出基线路径。
- **复现步骤**:
  1. 阅读 `prepare/prepare.go` 中 `snapshotOnlyLegacyOutputPath()` 与 `loadSnapshotOnlyLegacyJSON()`。
  2. 阅读 `qa/issue110/prepare_migration_test.go` 中 `compareLegacyAndPrepareSnapshot()`。
  3. 搜索仓库运行期代码与测试中的 `postman-db` 残留引用，可见上述路径仍参与当前回归链路。
- **可能原因**: 本轮修复主要删除了主流程与 GDB 工具的 `postman-db` 回退，但没有同步移除 snapshot-only 基线复用逻辑及其对应测试夹具依赖，导致实现层与最新 human review 约束不完全一致。
- **建议修复**:
  - 移除 `prepare/prepare.go` 中 snapshot-only 对 `tmp/legacy-base/postman-db` 的读取依赖，改为新的基线组织方式或新的 `db` 基线夹具。
  - 同步更新 `qa/issue110/prepare_migration_test.go`，避免继续以 `postman-db` 目录作为回归基线来源。
  - 修订 `dev-flow/test-cases.md` 中仍要求“保留 legacy 输入兼容”的描述，确保测试规范与最新 human review 一致。

## 已执行检查项
- 阅读最新开发报告：`dev-flow/20260316161309-dev-report.md`
- 阅读 test case：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 检查代码改动：`git diff origin/master...HEAD`
- 构建验证：`./maze --build`
- Go 测试：`go test -count=1 ./prepare`、`go test -count=1 .`、`go test -count=1 ./qa/issue110`
- Python 兼容性语法检查：`python3 -m py_compile ...`、`python2 -m py_compile ...`
- 动态验收：`python3 testdata/run_test.py python/20260129-complex-types-311`
- 静态残留搜索：检查运行期代码与测试中 `postman-db` 的剩余引用

## 未执行项
- 未执行更多高成本 testdata 回归（Node.js、C++、大内存/性能场景）
  - **原因**: 阶段 A 静态检查已发现与最新 human review 强约束冲突的范围问题；继续扩展动态回归无法改变该结论。
  - **可能影响范围**: AC8 的更广覆盖面、兼容性与性能相关 TC。
- 未执行不同 glibc 版本、不同 Node.js 版本的真实动态验证
  - **原因**: 当前轮次优先聚焦 issue #153 的路径迁移与最新 review 反馈复核。
  - **可能影响范围**: AC6 的完整运行时兼容结论。

## 兼容性验证
- [x] Python 2.7 测试通过（语法编译）
- [x] Python 3.x 测试通过（语法编译）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果摘要
- `./maze --build`: 通过
- `go test -count=1 ./prepare`: 通过
- `go test -count=1 .`: 通过
- `go test -count=1 ./qa/issue110`: 通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`: 通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`: 通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`: 通过

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: 当前不建议按 PASS 结论放行。主链路迁移本身已基本完成，但仍需清理与最新 human review 冲突的 `postman-db` snapshot-only / 回归基线兼容逻辑，并同步更新测试规范后再复验。
