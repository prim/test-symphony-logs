# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按“低成本静态检查 + 关键构建/测试 + 至少一个代表性 testdata 动态验收”的策略执行。已确认 `postman-db -> db` 主链路迁移在当前分支上可用：默认输出目录、ELF 缓存、日志回收、prepare/process 路径、Python/GDB 工具链、`.gitignore` 与代表性动态用例均工作正常。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB` 已切换为 `"db"`；`prepare/args.go` 中 `BuildOutputDir()` 默认输出 `./db/<project>-<user>-<profileId>`。
- **关键指标**: `POSTMANDB = "db"`；`BuildOutputDir()` 返回格式为 `./db/demo-tester-42`（见 `prepare/db_path_behavior_test.go`）。
- **备注**: 与 AC1、AC4 一致。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: PASS
- **实际行为**: `prepare/process.go` 中 `cacheProcMaps()` 优先查找 `./db/*-<pid>.maps.gdb`；`prepare/prepare.go` 的 snapshot-only 基线路径为 `tmp/legacy-base/db`；同时 `qa/issue110/prepare_migration_test.go` 仍显式从 `tmp/legacy-base/postman-db` 读取历史基线输出，覆盖 legacy 输入兼容场景。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 通过；`maze/qa/issue110` 用例执行通过。
- **备注**: 当前实现体现了“新输出使用 `db/`，旧基线输入仍可验证”的双场景覆盖，符合 AC4、AC5。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 的 `copyResultFiles()` 已从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志文件。
- **关键指标**: `main.go:868-869` 使用 `filepath.Join("db", "*", "maze.log")` 与 `filepath.Join("db", "*", "maze.py.log")`。
- **备注**: 未发现对 `postman-db/*/maze.log` 的依赖。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`gdb/gdb-scripts/gdb_tool.py` 已统一使用 `./db` 与 `./db/elf`。
- **关键指标**: `POSTMANDB = "./db"`、`POSTMANDB_ELF = "./db/elf"`；GDB 工具 `profile_dir` 与 `elf` 候选路径均指向 `./db`。
- **备注**: Python 2/3 语法编译检查通过，未发现因路径迁移引入的脚本语法问题。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md` 的中间文件示例已切换为 `db/<project>-<user>-<profileId>/` 与 `db/*/pyo`；`doc/support.md` 中示例路径为 `maze/db/elf/...`。
- **关键指标**: `.gitignore:51` 为 `db/`。
- **备注**: `doc/maze_py_analysis.md` 为历史分析文档，未作为新的运行路径入口文档使用；本轮未发现其引入错误的 `postman-db` 示例。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: 成功执行 `python3 testdata/run_test.py python/20260129-complex-types-311`；Maze 输出日志明确显示 `output dir: ./db/coredump-default-2636550`；测试验证全部通过。
- **关键指标**: `1 passed, 0 failed`；动态日志中输出目录为 `./db/coredump-default-2636550`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，并直接覆盖 AC8。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: `cmd/maze-gdb-coredump.py`、`cmd/postman_prelude.py`、`cmd/postman_lz4.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 均通过 Python 3 与 Python 2 `py_compile`。
- **关键指标**: `python3 -m py_compile ... && python2 -m py_compile ...` 通过。
- **备注**: 这是基础兼容性检查；不等同于完整双版本运行时回归。

## 失败的 Test Case（如有）
- 无。

## 覆盖性与附加发现
- `dev-flow/test-cases.md` 的 “Test File(s)” 声称新增文件为 `qa/issue153/db_path_migration_test.go`，但仓库中未找到该文件。
- 实际覆盖已分散落在 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go`，再配合 `qa/issue110/prepare_migration_test.go` 完成关键验收。
- 该问题属于测试报告清单与仓库实际位置不一致，**不影响当前功能验收通过结论**，但建议后续同步测试文档，避免误导回归排查。

## 未执行项（如有）
- 不同 glibc 版本的实际动态回归未执行。
- 不同 Node.js 版本的实际动态回归未执行。
- Python 2.7 / Python 3.x 的完整运行时双版本回归未执行，仅执行了脚本级 `py_compile`。
- 性能验证（>1GB 大内存进程扫描时间、分析进程内存占用比例）未执行。

### 未执行原因
- 本轮需求聚焦目录迁移正确性；在构建、Go 回归、Python 语法兼容检查与代表性 testdata 动态验收已通过的前提下，未进一步扩展到高成本跨版本/性能回归。

### 可能影响范围
- 可能影响 AC6 的“完整运行时兼容性”外延结论。
- 可能影响性能类非功能指标，但不影响本轮对目录迁移主链路的通过判定。

## 兼容性验证
- [ ] Python 2.7 测试通过（完整运行时未执行；仅 `py_compile` 通过）
- [x] Python 3.x 测试通过（代表性 testdata 动态用例通过）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 执行记录
- `./maze --build`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 当前分支可判定为**通过本轮目录迁移验收**；如要关闭所有非功能风险，建议后续补做跨 glibc / Node.js / Python 运行时兼容与性能回归。
