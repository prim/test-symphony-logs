# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按计划文档、最新开发报告、test case 列表与 `origin/master...HEAD` 变更完成了阶段 A 静态检查、阶段 B 关键 smoke/构建验证，以及至少一个代表性 `testdata` 动态用例执行。

主业务实现大部分迁移已收口到 `db/`：
- Go 常量、默认输出目录、prepare/process 路径、日志回收、Python/GDB 工具链、`.gitignore` 与正式文档已切换到 `db/`
- `./maze --build`、关键 Go 测试、Python 2/3 语法检查、代表性 `testdata` 用例均通过

但仍存在一个与验收标准 AC5 直接冲突的未闭合问题：
- `qa/issue110/prepare_migration_test.go:156` 仍显式从 `tmp/legacy-base/postman-db` 读取基线输出：`findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`

这说明当前迁移回归链路尚未完全收口到 `db/` 语义，因此总体结论必须为 **FAIL**。

另有一项流程一致性问题：
- `dev-flow/test-cases.md` 声明测试文件为 `qa/issue153/db_path_migration_test.go`，但仓库实际新增覆盖文件为 `prepare/db_path_behavior_test.go` 与 `main_db_layout_test.go`，文档与实际覆盖位置不一致。

## 详细结果

### TC-001: Go 常量与默认输出目录切换到 db
- **结果**: PASS
- **实际行为**: `common/const.go` 中 `POSTMANDB = "db"`；`prepare/args.go` 中 `BuildOutputDir()` 默认拼接为 `./db/<project>-<user>-<profileId>`。
- **关键指标**: 静态检查通过；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC1 的主路径切换要求。

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **实际行为**: `prepare/process.go` 已仅优先查找 `./db/*-<pid>.maps.gdb`；`prepare/prepare.go` 的 snapshot-only 基线路径也已切换为 `tmp/legacy-base/db`。但 `qa/issue110/prepare_migration_test.go:156` 仍从 `tmp/legacy-base/postman-db` 读取基线输出。
- **关键指标**: `go test -count=1 ./prepare ./qa/issue110 .` 虽通过，但静态检查显示迁移测试本身仍依赖旧目录语义。
- **备注**: 与 AC5“迁移测试覆盖并收口到新目录语义”的目标不一致。

### TC-003: main.go 日志回收逻辑从 db 复制结果
- **结果**: PASS
- **实际行为**: `main.go` 中 `copyResultFiles()` 从 `db/*/maze.log` 与 `db/*/maze.py.log` 复制日志；`prepareTarPostmanLayout()` 将 tar 模式产物整理到 `db/` 目录结构。
- **关键指标**: `main_db_layout_test.go` 覆盖 `copyResultFiles()` 与 tar 布局；`go test -count=1 ./prepare ./qa/issue110 .` 通过。
- **备注**: 满足 AC3。

### TC-004: Python/GDB 工具链路径常量切换到 db
- **结果**: PASS
- **实际行为**: `cmd/postman_prelude.py`、`cmd/maze-gdb-coredump.py`、`gdb/gdb-scripts/gdb_tool.py`、`gdb/gdb-scripts/az_fake_maps.py` 已使用 `./db` / `./db/elf`。
- **关键指标**: `python3 -m py_compile ... && python2 -m py_compile ...` 通过。
- **备注**: 完成了 AC2、AC6 所要求的静态与语法层切换。

### TC-005: .gitignore 与正式文档统一新目录规范
- **结果**: PASS
- **实际行为**: `.gitignore` 已忽略 `db/`；`doc/debugging.md`、`doc/maze_py_analysis.md`、`doc/support.md` 的正式示例已切换到 `db/` 语义。
- **关键指标**: 静态检查通过。
- **备注**: 满足 AC9。

### TC-006: 代表性测试执行器能够切换到 db 产物语义
- **结果**: PASS
- **实际行为**: `testdata/run_test.py` 已清理 `db/` 缓存并从 `db/*/maze.log` / `db/*/maze.py.log` 捕获日志；代表性用例成功运行并读取结果。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，结果为 `1 passed, 0 failed`。
- **备注**: 满足 AC8 的最小动态验收要求。

### TC-007: Python 工具脚本兼容性基础检查
- **结果**: PASS
- **实际行为**: 关键 Python 脚本存在且 Python 2 / Python 3 `py_compile` 均通过。
- **关键指标**: `python3 -m py_compile ... && python2 -m py_compile ...` 通过。
- **备注**: 仅证明语法兼容；尚未完成 Python 2.7 全链路动态验证。

## 失败的 Test Case

### TC-002: prepare/process 新路径生效且保留 legacy 输入兼容
- **结果**: FAIL
- **预期行为**: 当前迁移回归链路应与实现一起收口到 `db/` 语义；迁移测试应表达“新输出落到 `db/` 且必要基线读取规则一致”，不应继续把 `postman-db` 作为当前基线目录。
- **实际行为**: `qa/issue110/prepare_migration_test.go:156` 仍调用 `findLegacyOutputDir(t, filepath.Join(legacyRoot, "postman-db"))`。
- **复现步骤**:
  1. 打开 `qa/issue110/prepare_migration_test.go`
  2. 查看 `compareLegacyAndPrepareSnapshot()`
  3. 可见第 156 行仍硬编码 `filepath.Join(legacyRoot, "postman-db")`
  4. 对照 `prepare/prepare.go` 中 `snapshotOnlyBaselineOutputPath()` 与 `loadSnapshotOnlyBaselineJSON()`，其基线路径已改为 `tmp/legacy-base/db`
- **可能原因**: 迁移时主业务实现与快照基线路径已切换，但 issue110 回归测试夹具/断言未同步完成收口。
- **建议修复**: 将 `qa/issue110/prepare_migration_test.go` 的当前基线读取语义与实现保持一致，移除对 `postman-db` 的当前路径依赖；同时复核测试夹具说明，避免继续把旧目录当作当前回归输入。

## Findings

- 发现 1：`qa/issue110/prepare_migration_test.go:156` 仍静态引用 `postman-db`，导致 AC5 未闭合，整体验收不能通过。
- 发现 2：`dev-flow/test-cases.md` 的 “Test File(s)” 指向不存在的 `qa/issue153/db_path_migration_test.go`，与当前实际测试覆盖文件不一致，存在文档误导风险。

## Test Results

- `./maze --build`：通过
- `go test -count=1 ./prepare ./qa/issue110 .`：通过
- `python3 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py && python2 -m py_compile cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py gdb/gdb-scripts/gdb_tool.py gdb/gdb-scripts/az_fake_maps.py`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过（`1 passed, 0 failed`）

## 未执行项

- Python 2.7 全链路动态用例未执行；当前仅完成语法级兼容检查，可能影响 AC6 的运行时兼容结论完整性。
- 不同 glibc 版本测试未执行；可能影响 AC2、AC4、AC6 的跨环境兼容性判断。
- 不同 Node.js 版本测试未执行；本轮未扩展 Node.js 迁移场景验证，可能影响 AC2、AC8 的 Node 侧结论完整性。
- 大内存性能场景未执行；可能影响性能相关扩展验证结论，但不影响本轮基于 AC5 的 FAIL 判定。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不通过发布验收**。请先修正 `qa/issue110/prepare_migration_test.go` 中对 `postman-db` 的当前基线引用，并同步修正 `dev-flow/test-cases.md` 的测试文件清单后再复验。
