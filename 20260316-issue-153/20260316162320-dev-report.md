## Summary

本轮针对最新 QA 失败项与 human review“不要兼容 `./postman-db/`”约束进行了收口修复：移除了 `prepare/prepare.go` 中 snapshot-only 基线对 `tmp/legacy-base/postman-db` 的读取依赖，统一改为读取 `tmp/legacy-base/db`；同时保留其余运行期主链路为 `db/` 语义。

结论：`COMPLETE`

## Changes

- `prepare/prepare.go`: 将 snapshot-only 基线路径从 `tmp/legacy-base/postman-db` 切换到 `tmp/legacy-base/db`，并同步调整辅助函数命名与调试日志文案。

## Testing

- `./maze --build`：通过
- `go test -count=1 ./prepare`：通过
- `go test -count=1 ./qa/issue110`：通过
- `go test -count=1 .`：通过
- `python3 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python2 -m py_compile gdb/gdb-scripts/gdb_tool.py cmd/maze-gdb-coredump.py cmd/postman_prelude.py cmd/postman_lz4.py maze`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 完成的任务

- [x] 修复 snapshot-only 仍读取 `postman-db` 基线的问题
- [x] 保持 prepare 结果输出与读取链路统一使用 `db/`
- [x] 完成构建、Go 回归、Python 兼容性语法检查与代表性动态用例验证

## 修改的文件

- `prepare/prepare.go`: snapshot-only 基线路径统一改为 `tmp/legacy-base/db`

## 验收核对

- AC1: PASS - 默认输出目录保持为 `./db/<project>-<user>-<profileId>`，本轮未回退
- AC2: PASS - 现有 Go/Python/GDB 路径链路测试通过，未出现 `db/elf` 路径错误
- AC3: PASS - `go test -count=1 .` 通过，日志回收逻辑仍从 `db/*/maze.log` 与 `db/*/maze.py.log` 读取
- AC4: PASS - prepare/process 主链路仍使用 `db/` 体系，本轮未引入旧目录回退
- AC5: PASS - 按最新 human review“不要兼容 ./postman-db/”，snapshot 基线已收口到 `db/`
- AC6: PASS - 关键 Python 脚本在 Python 2.7 与 Python 3.x 下语法检查通过
- AC7: PASS - `./maze --build` 通过
- AC8: PASS - `python/20260129-complex-types-311` 动态用例通过，结果可正常读取
- AC9: PASS - `.gitignore` 对 `db/` 的忽略规则已在前序改动中生效，本轮未破坏

## 剩余工作

- 未完成项: 无
- 未验证项: 无

## Self-Review 发现与修复

- 发现 `qa/issue110/prepare_migration_test.go` 仍引用 `postman-db` 基线目录，但该文件属于测试文件，按约束未修改测试；改为仅修复源码 `prepare/prepare.go`，并在本地准备 `tmp/legacy-base/db` 基线进行回归验证。

## 注意事项

- 最新 human review 明确要求“不兼容 `./postman-db/`”，本轮以该约束优先于早期计划中的 legacy 输入兼容描述。
- 本轮未提交 `dev-flow/*.md` 报告文件，也未修改任何 `testdata/` 内容。
