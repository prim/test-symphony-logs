# 开发报告

## 完成的任务
- [x] Task 1: 定位 `common.ResultItem` 的 canonical 定义，并整理字段含义与辅助函数。
- [x] Task 2: 梳理 `GetResultItem` / `AddToResultItems` / `AddToResultItems1` / `MergeResultItems` / `AddResult` 的主数据流。
- [x] Task 3: 覆盖 Python、txos、mallocer、nodejs、postman 等主要上层使用场景。
- [x] Task 4: 说明 `SortByValue`、`PrintSortedResults`、`PrintTextModeResults` 等输出链路。
- [x] Task 5: 区分 `common/utils.go` 局部 DTO 与 `sub-repos/lua-bak/` 历史遗留实现，完成新的 dev log。

## 修改的文件
- `dev-log/2026-03-07-resultitem-analysis.md`: 新增 ResultItem 分析文档，说明 canonical 定义、聚合流程、模块调用、输出链路与同名结构辨析。
- `dev-log/dev-report.md`: 记录本次文档型任务的完成情况、自测说明与注意事项。

## 自测结果
- 测试命令: 无自动化测试命令；按 `dev-log/test-cases.md` 要求执行人工代码审阅与文档核对。
- 测试结果: 已对照 `common/result_items.go`、`common/webui.go`、`common/utils.go`、`python/`、`txos/`、`mallocer/`、`nodejs/`、`postman/`、`sub-repos/lua-bak/` 完成核对；本 issue 为纯文档任务，未修改源码逻辑，未新增/修改测试文件。

## Self-Review 发现与修复
- 初稿中容易把 `common/utils.go` 的局部 `ResultItem` 误写成主统计结构；已在正式文档中明确标注其仅为 text/json 输出 DTO。
- 初稿中上层使用若只写 `common.ResultItem` 容易忽略 dot-import 造成的未限定名调用；已在文档中补充这一阅读特征与搜索建议。

## 注意事项
- `ResultItem` 的主链路核心在 `common/result_items.go`，但展示层和消费层分散在 `common/webui.go`、`common/utils.go`、`postman/`，阅读时不要只盯定义文件。
- `postman/` 主要消费已汇总结果，不是主要结果生产者。
- `sub-repos/lua-bak/` 中存在历史遗留的另一套 `ResultItem`，不能误归入当前主仓库统计链路。
