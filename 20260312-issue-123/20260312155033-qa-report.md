## QA Result: PASS

## 验收范围

本轮验收针对 **prim/maze#123: 回归 jemalloc testdata 并修复已知兼容性/边界问题**。

根据用户指令，以下两个不存在的边界场景目录已确认忽略：
- `cpp/20260306-jemalloc-empty-tcache`（目录不存在）
- `cpp/20260306-jemalloc-extreme-sizes`（目录不存在）

## Findings

### 代码修改审查

本次修改涉及 4 个功能代码文件 + 1 个测试文件 + 1 个 dev-log 文档，修改范围合理：

1. **`cpp/cpp.go`** (+15 行): 过滤 `TYPE_CODE_VOID` 类型的 C++ 节点，避免与 `ClassifyMemoryPiece` 兜底统计重复
2. **`mallocer/pieces.go`** (+22/-18 行): 重构 `ClassifyMemoryPiece()`，仅为 `TypeIdx != 0` 的弱引用节点创建分类条目，`TypeIdx == 0` 时不再追加额外统计
3. **`mallocer/jemalloc/walk_v4.go`** (+38/-7 行): 增加 chunk/piece 级去重保护 (`pieceSeen`)，修正 4.x large run header 导致的伪尺寸问题
4. **`mallocer/jemalloc/guess.go`** (+4 行): 补充 4.5.0 old jemalloc `tsd.tcache` 指针有效性检查
5. **`tests/test_issue_123_jemalloc_regression.py`** (+152 行): 新增回归测试脚本
6. **`dev-log/2026-03-12-jemalloc-regression-results.md`** (+221 行): 正式回归结果文档，包含失败根因、修复方案、复测结果

修改逻辑清晰，注释详尽，无遗留调试代码或遗留文件。

### 静态检查结果

- ✅ `./maze --build` 构建成功
- ✅ 5 个必需 testdata 目录均含 tarball 和 `validate.py`
- ✅ dev-log 文件完整，包含"失败根因"、"修复方案"、"复测结果"三个关键章节
- ✅ 无遗留文件、无错误接线

## Test Results

### 动态回归（全部 5 个用例逐一执行通过）

| 用例 | 结果 | 关键指标 | 备注 |
|------|------|----------|------|
| `cpp/20260210-jemalloc-5-3-0` | ✅ PASS | malloc(1MB)=100, malloc(2MB)=100, malloc(3MB)=100, 小块各 10000~20000 | 单线程主链路正常 |
| `cpp/20260211-jemalloc-5-3-0-multithread` | ✅ PASS | 所有 size bucket ratio 100%~100.23%，10/10 checks passed | 多线程 200% 假通过已修复 |
| `cpp/20260211-jemalloc-4-5-0-multithread` | ✅ PASS | 1MB=100%（之前 200%），所有 bucket 100%~104%，9/9 checks passed | 老版本兼容路径修复 |
| `cpp/20260210-jemalloc-5-0-0` | ✅ PASS | 所有 bucket 计数精确，total chunks=100302 | 老版本兼容通过 |
| `cpp/20260210-jemalloc-5-0-1` | ✅ PASS | 所有 bucket 计数精确，total chunks=100302 | 老版本兼容通过 |

### 关键验证点

- **无 panic / fatal / Traceback**: 所有 5 个用例输出中均无异常错误
- **无假通过**: 5.3.0 多线程场景之前 200% 的假通过已修复至 100%；4.5.0 多线程 1MB 从 200% 修复至 100%
- **真实执行确认**: 所有用例输出均包含 `Running Maze Analysis` 和 `✅ Test PASSED`

### 跳过项（用户确认忽略）

| 用例 | 原因 |
|------|------|
| `cpp/20260306-jemalloc-empty-tcache` | 目录不存在，用户已确认忽略 |
| `cpp/20260306-jemalloc-extreme-sizes` | 目录不存在，用户已确认忽略 |

## 验收标准核对

- **AC1（现存 jemalloc testdata 可以按计划完成顺序回归）**: ✅ PASS — 5 个现存用例全部按顺序回归通过
- **AC2（关键代表版本与边界场景通过）**: ✅ PASS — 关键代表版本 5.3.0 单/多线程、4.5.0 多线程、5.0.0、5.0.1 全部通过；边界场景用户确认忽略
- **AC3（无明显 panic / fatal / 假通过）**: ✅ PASS — 无 panic/fatal/Traceback；多线程 200% 假通过已修复
- **AC4（修复结果有正式 dev-log 记录）**: ✅ PASS — `dev-log/2026-03-12-jemalloc-regression-results.md` 完整记录失败根因、修复方案和复测结果

## 已知限制（不影响本轮验收）

- 5.1.0/5.2.0/5.2.1 多线程场景中大块（1MB+）仍有轻微重复（~199%），根因为 `ClassifyVtablePiece` 与 `InitAllMemoryPieceSize` 的架构性双重统计。dev-log 已记录此问题，需更深层的架构调整，超出当前 issue 范围。

## 总结

- 通过: 5 / 5
- 失败: 0 / 5
- 跳过: 2（用户确认忽略）
- 建议: **可以发布**。所有现存 jemalloc testdata 回归通过，关键多线程假通过问题已修复，正式 dev-log 已记录。
