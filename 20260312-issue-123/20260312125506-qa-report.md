# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮验收未通过。阶段 A 静态检查确认：需求要求的两个边界 jemalloc testdata 目录 `cpp/20260306-jemalloc-empty-tcache` 与 `cpp/20260306-jemalloc-extreme-sizes` 在当前仓库中不存在，导致资产完整性校验失败，且关键 smoke 链路无法按计划完整执行。尽管已存在的关键版本与兼容版本动态回归通过，但 AC2/AC3 仍未闭环，整体结论必须为 FAIL。

## Findings

- 缺少 `testdata/cpp/20260306-jemalloc-empty-tcache` 与 `testdata/cpp/20260306-jemalloc-extreme-sizes`，直接违反计划与测试用例中的边界场景覆盖要求。
- `tests/test_issue_123_jemalloc_regression.py` 中的资产检查可稳定复现该问题，失败信息明确指向上述两个目录缺失。
- 已新增 `dev-log/2026-03-12-jemalloc-regression-results.md`，且包含“失败根因 / 修复方案 / 复测结果”章节，TC-005 可通过。
- 已存在的 jemalloc 关键版本与兼容版本动态回归通过，当前未观察到明显 `panic` / `fatal` / `Traceback`，但无法据此替代缺失的边界场景验收。

## 详细结果

### TC-001: 回归计划文档覆盖关键 smoke 与兼容路径
- **结果**: PASS
- **实际行为**: `dev-log/2026-03-12-jemalloc-testdata-regression-plan.md` 包含 smoke、兼容路径与扩展回归阶段，顺序正确。
- **关键指标**: `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_plan_covers_required_cases` 通过。
- **备注**: 静态计划覆盖满足要求。

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **实际行为**: 自动化检查报告缺少 `cpp/20260306-jemalloc-empty-tcache` 与 `cpp/20260306-jemalloc-extreme-sizes` 目录。
- **关键指标**: `test_required_jemalloc_testdata_assets_exist` 失败 2 项子用例。
- **备注**: 这会阻塞后续边界 smoke 与扩展回归。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **实际行为**: 已按顺序执行前两个 smoke 用例并通过：`cpp/20260210-jemalloc-5-3-0`、`cpp/20260211-jemalloc-5-3-0-multithread`；第三个 smoke 用例 `cpp/20260306-jemalloc-empty-tcache` 因目录缺失无法执行。
- **关键指标**: 前两个用例输出均包含 `Running Maze Analysis` 与 `✅ Test PASSED`；第三个用例因缺少测试资产未执行。
- **备注**: 关键 smoke 链路未完整闭环，按验收标准判定失败。

### TC-004: 老版本兼容路径回归通过
- **结果**: PASS
- **实际行为**: `cpp/20260211-jemalloc-4-5-0-multithread`、`cpp/20260210-jemalloc-5-0-0`、`cpp/20260210-jemalloc-5-0-1` 顺序执行均通过。
- **关键指标**: 三个 `python3 testdata/run_test.py <case>` 均返回 0，输出中未见明显 `panic` / `fatal` / `Traceback`。
- **备注**: 已覆盖需求中指定的老版本兼容路径。

### TC-005: 修复结果有正式回归 dev-log 记录
- **结果**: PASS
- **实际行为**: `dev-log/2026-03-12-jemalloc-regression-results.md` 存在，且包含“失败根因 / 修复方案 / 复测结果”章节。
- **关键指标**: `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_result_devlog_exists_with_required_sections` 通过。
- **备注**: 文档约束满足。

## 失败的 Test Case

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **预期行为**: 所有计划回归目录均存在，且每个目录都包含 tarball 与 `validate.py`。
- **实际行为**: `cpp/20260306-jemalloc-empty-tcache` 与 `cpp/20260306-jemalloc-extreme-sizes` 目录不存在。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist
     ```
  2. 观察失败输出。
- **可能原因**: 需求/测试计划引用了边界场景 testdata，但当前仓库未包含对应测试资产。
- **建议修复**: 补齐上述两个测试目录及其 tarball、`validate.py`，再重新执行完整回归。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **预期行为**: 三个 smoke 用例 `5.3.0` 单线程、`5.3.0` 多线程、`empty-tcache` 应全部可执行并通过。
- **实际行为**: 前两个 smoke 用例通过；`empty-tcache` 因测试目录缺失无法启动。
- **复现步骤**:
  1. 先执行前两个已存在用例：
     ```bash
     python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0
     python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread
     ```
  2. 检查 `testdata/cpp/20260306-jemalloc-empty-tcache` 是否存在。
  3. 再执行资产检查：
     ```bash
     python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist
     ```
- **可能原因**: smoke 计划包含了仓库中尚未提供的边界场景 testdata。
- **建议修复**: 先补齐 `empty-tcache` 测试资产，再复跑整个 smoke 阶段，确认主链路在边界场景也能通过。

## Test Results

- `./maze --build`：通过
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_plan_covers_required_cases`：通过
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`：失败（缺少两个边界测试目录）
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_result_devlog_exists_with_required_sections`：通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`：通过
- `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`：通过
- `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`：通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`：通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-1`：通过

## 未执行项

- `python3 testdata/run_test.py cpp/20260306-jemalloc-empty-tcache`
  - **原因**: 测试目录不存在。
  - **影响范围**: TC-003，AC2，AC3。
- `python3 testdata/run_test.py cpp/20260306-jemalloc-extreme-sizes`
  - **原因**: 测试目录不存在。
  - **影响范围**: 边界场景稳定性、扩展回归、AC2，AC3。
- 进一步边界场景下的 size bucket 重复计数/漏计核对
  - **原因**: 缺少对应 testdata，动态验证失去意义。
  - **影响范围**: 需求第 4 条中的边界与稳定性问题验证。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 3 / 5
- 失败: 2 / 5
- 建议: 当前**不建议发布**。请先补齐 `empty-tcache` 与 `extreme-sizes` 两个边界 jemalloc testdata 资产，并在补齐后重新执行 smoke 与边界回归，再进行最终验收。
