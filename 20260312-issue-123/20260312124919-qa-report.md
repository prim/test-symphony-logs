## QA Result: FAIL

## Findings
- 阻塞性问题：`testdata/cpp/20260306-jemalloc-empty-tcache` 与 `testdata/cpp/20260306-jemalloc-extreme-sizes` 两个计划内边界用例目录在当前仓库中不存在，导致 AC1/AC2 相关静态校验与 smoke 链路无法完整通过。
- 已验证的现有 jemalloc 主链路与兼容路径可通过：`cpp/20260210-jemalloc-5-3-0`、`cpp/20260211-jemalloc-5-3-0-multithread`、`cpp/20260211-jemalloc-4-5-0-multithread`、`cpp/20260210-jemalloc-5-0-0`、`cpp/20260210-jemalloc-5-0-1` 均通过，输出中未见明显 `panic` / `fatal` / `Traceback`。
- 本次新增的正式结果文档 `dev-log/2026-03-12-jemalloc-regression-results.md` 存在，且包含“失败根因 / 修复方案 / 复测结果”必需章节；但文档内容将问题定性为“BLOCKED”，与本轮 QA 结论一致：需求尚未完成，不能验收通过。
- `tests/test_issue_123_jemalloc_regression.py` 的测试设计能够防止历史结果假通过（检查 `Running Maze Analysis` 与显式通过标记），这一点符合需求；但由于边界资产缺失，`test_smoke_cases_pass_via_run_test` 仍失败。

## Test Results

### 阶段 A：低成本/静态检查
- `./maze --build`：通过。
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_plan_covers_required_cases`：通过。
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`：失败。
  - 失败明细：
    - 缺少测试目录 `cpp/20260306-jemalloc-empty-tcache`
    - 缺少测试目录 `cpp/20260306-jemalloc-extreme-sizes`
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_result_devlog_exists_with_required_sections`：通过。

### 阶段 B：关键 smoke test
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`：通过。
- `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`：通过。
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_smoke_cases_pass_via_run_test`：失败。
  - 失败原因：第三个 smoke 用例 `cpp/20260306-jemalloc-empty-tcache` 目录不存在，无法执行。

### 阶段 C：扩展回归
- `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`：通过。
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`：通过。
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-1`：通过。
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_compatibility_cases_pass_via_run_test`：通过。

### 结果判定
- TC-001 回归计划文档覆盖关键 smoke 与兼容路径：PASS
- TC-002 必需 jemalloc testdata 资产齐备：FAIL
- TC-003 关键 smoke 用例顺序回归通过：FAIL
- TC-004 老版本兼容路径回归通过：PASS
- TC-005 修复结果有正式回归 dev-log 记录：PASS

### 失败用例详情

#### TC-002: 必需 jemalloc testdata 资产齐备
- 预期行为：所有计划回归目录均存在，且包含 tarball 与 `validate.py`。
- 实际行为：缺少 `cpp/20260306-jemalloc-empty-tcache`、`cpp/20260306-jemalloc-extreme-sizes`。
- 复现步骤：
  1. 执行 `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`
  2. 观察断言失败，提示缺少上述目录。
- 初步分析：这是测试资产缺失，不是当前已执行主链路中的运行时崩溃问题，但它直接阻断需求要求的边界场景验收。

#### TC-003: 关键 smoke 用例顺序回归通过
- 预期行为：`5.3.0` 单线程、`5.3.0` 多线程、`empty-tcache` 三个 smoke 用例均可按顺序通过。
- 实际行为：前两个通过，第三个因目录缺失无法执行，导致 smoke 链路整体失败。
- 复现步骤：
  1. 执行 `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_smoke_cases_pass_via_run_test`
  2. 观察 `case='cpp/20260306-jemalloc-empty-tcache'` 断言失败。
- 初步分析：需求明确将 empty tcache 列为关键 smoke；当前实现交付未满足这一验收前提。

### 未执行项
- `python3 testdata/run_test.py cpp/20260306-jemalloc-empty-tcache`
  - 原因：测试目录缺失。
  - 影响范围：AC1、AC2、AC3；对应 TC-002、TC-003。
- `python3 testdata/run_test.py cpp/20260306-jemalloc-extreme-sizes`
  - 原因：测试目录缺失。
  - 影响范围：AC2、AC3；对应 TC-002 及边界稳定性要求。

### 兼容性/正确性补充观察
- 已执行兼容版本回归中，`5.0.0`、`5.0.1` 输出结果只看到 `malloc(...)` 汇总，没有 `weak` / `C++ void` 的成对分类；不过当前对应 `validate.py` 仍判定通过，且未见崩溃。此现象建议后续开发进一步确认是否符合预期统计模型，但不构成本轮新增失败项。
- 多线程用例验证统计比例约为 200%，说明 validate 逻辑当前接受“双份命中”统计模型；从 QA 角度看，至少说明“多线程 size bucket 重复计数或漏计”这一已知风险尚未被更严格测试消除，尤其在缺失边界用例时无法宣称全部完成。

## Conclusion
本轮验收结论为 **FAIL**。虽然已有 jemalloc 关键版本与兼容路径可通过，且新增 dev-log 满足文档要求，但 issue #123 明确要求的两个边界 testdata 资产缺失，导致关键 smoke 与边界场景无法完成验证，因此不能通过验收。
