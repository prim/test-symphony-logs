## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- 边界场景 `testdata/cpp/20260306-jemalloc-empty-tcache` 与 `testdata/cpp/20260306-jemalloc-extreme-sizes` 在当前 `testdata` 子模块提交 `ce1877e10d34862e914628d66b4d6fefba04e612` 中不存在，导致 AC2 所需边界回归无法执行，且静态资产校验失败。
- `cpp/20260211-jemalloc-4-5-0-multithread` 虽然 `run_test.py` 显示通过，但实际验证输出仍显示 `1MB` bucket 为 `200 / 100 = 200%`，说明“无明显假通过”这一验收标准未满足。
- 已验证 `./maze --build` 通过，且至少一个关键 smoke 用例 `cpp/20260210-jemalloc-5-3-0` 可以通过主链路执行；因此失败并非构建阻塞，而是需求闭环与结果正确性未达标。

## 详细结果

### TC-001: 回归计划文档覆盖关键 smoke 与兼容路径
- **结果**: PASS
- **实际行为**: `dev-log/2026-03-12-jemalloc-testdata-regression-plan.md` 已包含关键 smoke、兼容路径与阶段顺序，阶段 A/B/C 顺序正确。
- **关键指标**: smoke 用例 3 个、兼容路径 3 个、边界场景 2 个均已在计划中列出。
- **备注**: 满足计划文档覆盖要求。

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **实际行为**: 单元测试 `test_required_jemalloc_testdata_assets_exist` 失败，缺少 `cpp/20260306-jemalloc-empty-tcache` 与 `cpp/20260306-jemalloc-extreme-sizes` 目录。
- **关键指标**: 2 个必需边界用例目录缺失。
- **备注**: 直接影响 smoke 链路完整性与边界场景 AC。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **实际行为**: 已实际执行 `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`，主链路通过且无 `panic` / `fatal` / `Traceback`；但第三个必需 smoke 用例 `cpp/20260306-jemalloc-empty-tcache` 目录不存在，因此无法完成完整 smoke 顺序回归。
- **关键指标**: 已执行 smoke 1/3 通过；缺失 smoke 1/3；其余 smoke 未在本轮继续执行。
- **备注**: 根据分阶段早停策略，确认资产缺失后不再继续无意义的完整 smoke 链路验证。

### TC-004: 老版本兼容路径回归通过
- **结果**: FAIL
- **实际行为**: `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread` 返回通过，但验证输出中 `1MB` bucket 为 `200.00%`；这与需求中“无明显假通过”“多线程 size bucket 不重复计数或漏计”相冲突。
- **关键指标**: `1MB` bucket `Expected=100`，`Found=200`，`Ratio=200.00%`。
- **备注**: 该问题说明当前修复未完全消除老版本多线程路径的重复计数/误判通过。

### TC-005: 修复结果有正式回归 dev-log 记录
- **结果**: PASS
- **实际行为**: 存在 `dev-log/2026-03-12-jemalloc-regression-results.md`，且包含“失败根因”“修复方案”“复测结果”章节。
- **关键指标**: 必需章节 3/3 齐备。
- **备注**: 文档记录完整，但文档中的 `PARTIAL` 结论也佐证当前未达到全部 AC。

## 失败的 Test Case

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **预期行为**: 所有计划中的关键 smoke、兼容版本与边界场景目录均存在，且每个目录包含 tarball 与 `validate.py`。
- **实际行为**: `cpp/20260306-jemalloc-empty-tcache`、`cpp/20260306-jemalloc-extreme-sizes` 目录不存在。
- **复现步骤**:
  1. 执行 `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`
  2. 观察失败输出，提示缺少上述两个目录。
- **可能原因**: 计划文档与测试代码要求的边界 testdata 未进入当前 `testdata` 子模块提交。
- **建议修复**: 先补齐或同步缺失的边界 testdata 资产，再继续完成 issue #123 的边界回归。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **预期行为**: `cpp/20260210-jemalloc-5-3-0`、`cpp/20260211-jemalloc-5-3-0-multithread`、`cpp/20260306-jemalloc-empty-tcache` 应可按顺序全部通过。
- **实际行为**: 已确认 `cpp/20260210-jemalloc-5-3-0` 通过，但 `cpp/20260306-jemalloc-empty-tcache` 目录缺失，完整 smoke 链路无法成立。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`
  2. 再执行 `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`
  3. 观察 empty-tcache 资产缺失导致 smoke 无法完整执行。
- **可能原因**: 关键 smoke 所依赖的边界 testdata 缺失。
- **建议修复**: 补齐 empty-tcache 测试数据，并重新顺序执行完整 smoke 回归。

### TC-004: 老版本兼容路径回归通过
- **结果**: FAIL
- **预期行为**: 老版本兼容路径通过，且无明显重复计数、漏计或假通过。
- **实际行为**: `cpp/20260211-jemalloc-4-5-0-multithread` 输出中 `1MB` bucket 仍为 `200.00%`，但测试脚本因仅检查最低阈值而判定通过。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`
  2. 查看验证结果中的 `[Per-Size Validation]`
  3. 观察 `1MB` 行为 `Expected 100 / Found 200 / Ratio 200.00%`。
- **可能原因**: old jemalloc 4.5.0 多线程路径仍存在 1MB bucket 重复计数；同时 `validate.py` 判定条件过宽，未拦截超额统计。
- **建议修复**: 继续定位 4.5.0 多线程 1MB bucket 重复来源，并收紧验证逻辑，避免“Found 远超 Expected”仍被判定为通过。

## 未执行项

- `TC-003` 中 `cpp/20260211-jemalloc-5-3-0-multithread`、`cpp/20260306-jemalloc-empty-tcache` 未在本轮按完整 smoke 序列继续执行。
  - **原因**: 已通过静态检查确认 empty-tcache 资产缺失，继续完整 smoke 序列无法形成闭环。
  - **影响范围**: 影响 AC2、TC-003。
- `cpp/20260306-jemalloc-extreme-sizes` 动态回归未执行。
  - **原因**: 测试目录缺失。
  - **影响范围**: 影响 AC2、边界稳定性验证。
- 其余 5.x 扩展回归未继续执行。
  - **原因**: 当前已发现明确阻塞（边界 testdata 缺失）与局部正确性问题（4.5.0 多线程 1MB 200% 假通过），继续高成本扩展回归收益有限。
  - **影响范围**: 影响 AC1/AC3 的完整覆盖声明。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`：通过
- `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`：命令返回通过，但验证输出存在 `1MB` bucket `200.00%` 的明显假通过信号
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`：失败，缺少 `empty-tcache` 与 `extreme-sizes`

## 总结

- 通过: 2 / 5
- 失败: 3 / 5
- 建议: **当前不建议发布。** 需先补齐缺失的边界 testdata 资产，并继续修复/收紧 `4.5.0` 多线程 1MB bucket 的重复计数与假通过问题，之后再重新完成 smoke 与兼容性回归。
