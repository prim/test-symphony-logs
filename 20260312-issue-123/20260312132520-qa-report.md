# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮阶段 A 静态检查与阶段 B/C 动态回归显示：已实现的部分修复并未完全满足需求。`5.3.0` 主链路与部分兼容版本可通过，但仍存在两个阻塞性问题：

1. 计划要求的边界 testdata 资产缺失，导致 `empty-tcache` / `extreme-sizes` 无法验收。
2. `cpp/20260211-jemalloc-4-5-0-multithread` 仍存在 **结果误判通过**：`validate.py` 判定 PASS，但实际 1MB bucket 仍为 **200%**，不符合“无明显假通过”的验收标准。

## Findings

- 发现 1：`testdata/cpp/20260306-jemalloc-empty-tcache` 与 `testdata/cpp/20260306-jemalloc-extreme-sizes` 目录在当前仓库中不存在，静态资产检查失败，且关键 smoke 链路无法完整执行。
- 发现 2：`cpp/20260211-jemalloc-4-5-0-multithread` 的动态结果与开发报告不一致；实际输出里 1MB bucket 为 `200 / 100 = 200%`，属于明显假通过。
- 发现 3：`cpp/20260211-jemalloc-4-5-0-multithread/validate.py` 对 large block 使用 ±15% 容差聚合统计，会把 `1048576` 与 `1052672` 一并计入 1MB bucket，掩盖真实问题。

## 详细结果

### TC-001: 回归计划文档覆盖关键 smoke 与兼容路径
- **结果**: PASS
- **实际行为**: `dev-log/2026-03-12-jemalloc-testdata-regression-plan.md` 中包含 smoke、兼容路径、边界场景以及阶段顺序。
- **关键指标**: 静态单测 `test_regression_plan_covers_required_cases` 通过。
- **备注**: 阶段顺序满足测试用例要求。

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **实际行为**: 静态单测报告缺少 `cpp/20260306-jemalloc-empty-tcache` 与 `cpp/20260306-jemalloc-extreme-sizes` 目录。
- **关键指标**: `test_required_jemalloc_testdata_assets_exist` 失败 2 项。
- **备注**: 这同时阻塞 TC-003 中第三个 smoke 用例以及边界场景 AC。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **实际行为**: 已实际执行 `cpp/20260210-jemalloc-5-3-0` 与 `cpp/20260211-jemalloc-5-3-0-multithread`，均通过；但第三个 smoke 用例 `cpp/20260306-jemalloc-empty-tcache` 因目录缺失无法执行。
- **关键指标**:
  - `cpp/20260210-jemalloc-5-3-0`: `✅ Test PASSED`
  - `cpp/20260211-jemalloc-5-3-0-multithread`: 各主要 bucket 约 `100%`
  - `cpp/20260306-jemalloc-empty-tcache`: 未执行
- **备注**: 因 smoke 链路不完整，TC-003 判定失败。

### TC-004: 老版本兼容路径回归通过
- **结果**: FAIL
- **实际行为**: `cpp/20260210-jemalloc-5-0-0` 与 `cpp/20260210-jemalloc-5-0-1` 通过；`cpp/20260211-jemalloc-4-5-0-multithread` 虽返回 `✅ Test PASSED`，但实际验证输出显示 1MB bucket 为 `200.00%`，仍属于结果误判通过。
- **关键指标**:
  - `cpp/20260211-jemalloc-4-5-0-multithread`: `1MB Expected=100 Found=200 Ratio=200.00%`
  - `cpp/20260210-jemalloc-5-0-0`: 通过
  - `cpp/20260210-jemalloc-5-0-1`: 通过
- **备注**: 不满足 AC3“无明显假通过”。

### TC-005: 修复结果有正式回归 dev-log 记录
- **结果**: PASS
- **实际行为**: `dev-log/2026-03-12-jemalloc-regression-results.md` 存在，且包含“失败根因 / 修复方案 / 复测结果”章节。
- **关键指标**: 静态单测 `test_regression_result_devlog_exists_with_required_sections` 通过。
- **备注**: 文档存在，但其中关于 4.5.0 多线程“已修复”的结论与本次实际回归结果不一致。

## 失败的 Test Case

### TC-002: 必需 jemalloc testdata 资产齐备
- **结果**: FAIL
- **预期行为**: 所有计划回归目录存在，且每个目录都有 tarball 与 `validate.py`。
- **实际行为**: 缺少 `testdata/cpp/20260306-jemalloc-empty-tcache` 与 `testdata/cpp/20260306-jemalloc-extreme-sizes`。
- **复现步骤**:
  1. 执行 `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist`
  2. 观察失败信息中的缺失目录。
- **可能原因**: 计划文档与测试代码引用了不存在的 testdata 资产。
- **建议修复**: 补齐缺失的边界 testdata 目录、tarball 与 `validate.py`，并重新执行 smoke/边界回归。

### TC-003: 关键 smoke 用例顺序回归通过
- **结果**: FAIL
- **预期行为**: `5.3.0` 单线程、`5.3.0` 多线程、`empty-tcache` 三个 smoke 用例都可按顺序执行并通过。
- **实际行为**: 前两个通过，第三个因 testdata 缺失无法执行。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`
  2. 执行 `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
  3. 检查 `testdata/cpp/20260306-jemalloc-empty-tcache`，目录不存在。
- **可能原因**: 关键 smoke 清单与仓库现有测试资产不一致。
- **建议修复**: 先补齐 `empty-tcache` 测试资产，再完成完整 smoke 链路验证。

### TC-004: 老版本兼容路径回归通过
- **结果**: FAIL
- **预期行为**: `4.5.0` 多线程、`5.0.0`、`5.0.1` 均真实回归通过，且无明显假通过。
- **实际行为**: `4.5.0` 多线程命令退出码为 0，但验证输出中 `1MB` bucket 为 `200.00%`；这说明统计或验证仍有问题。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`
  2. 查看验证输出中的 `Per-Size Validation`
  3. 观察 `1MB Expected=100 Found=200 Ratio=200.00%`，但脚本仍输出 `VALIDATION PASSED`
- **可能原因**:
  - `old jemalloc` large run 统计修复尚未真正消除重复计数；或
  - `validate.py` 的 15% 容差聚合把 `1048576` 与 `1052672` 混算到同一 bucket，继续掩盖问题。
- **建议修复**: 重新核查 `mallocer/jemalloc/walk_v4.go` 的 large run 用户块统计，并收紧/重构 `4.5.0` 多线程验证逻辑，避免 200% 结果仍被判 PASS。

## 已执行的检查项

- 阅读最新开发报告：`dev-flow/20260312131905-dev-report.md`
- 阅读测试用例：`dev-flow/test-cases.md`
- 阅读需求文档：`dev-flow/plan.md`
- 审查分支改动：`git diff origin/master...HEAD`
- 构建检查：`./maze --build`
- 静态单测：
  - `test_regression_plan_covers_required_cases`
  - `test_required_jemalloc_testdata_assets_exist`
  - `test_regression_result_devlog_exists_with_required_sections`
  - `test_compatibility_cases_pass_via_run_test`
- 动态回归：
  - `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`
  - `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
  - `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`
  - `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`
  - `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-1`

## 未执行项

- `TC-003` 中 `cpp/20260306-jemalloc-empty-tcache`
- `TC-005` 之外的边界动态验证：`cpp/20260306-jemalloc-extreme-sizes`
- `tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_smoke_cases_pass_via_run_test` 的完整执行

**未执行原因**:
- 对应 testdata 目录缺失，后续动态验证已失去意义，继续执行只会重复报同类缺失错误。

**可能影响的 AC / TC 范围**:
- 直接影响 AC1、AC2、AC3
- 直接影响 TC-002、TC-003，以及边界稳定性相关的扩展回归结论

## Test Results

- `./maze --build`：通过
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_plan_covers_required_cases tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_required_jemalloc_testdata_assets_exist tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_regression_result_devlog_exists_with_required_sections`：失败（2 个缺失 testdata）
- `python3 -m unittest tests.test_issue_123_jemalloc_regression.TestIssue123JemallocRegression.test_compatibility_cases_pass_via_run_test`：通过，但需结合实际输出判定 `4.5.0` 多线程存在假通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-3-0`：通过
- `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`：通过
- `python3 testdata/run_test.py cpp/20260211-jemalloc-4-5-0-multithread`：命令通过，但结果存在 1MB 200% 假通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`：通过
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-1`：通过

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 2 / 5
- 失败: 3 / 5
- 建议: **暂不发布**。需要先补齐边界 testdata 资产，并修复 `4.5.0` 多线程的假通过问题，再重新完成 smoke 与兼容性回归。
