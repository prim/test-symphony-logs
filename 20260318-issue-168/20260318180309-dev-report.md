# 开发报告

## 完成的任务
- [x] Task 1: 将 python / txos / cpp / mallocer 范围内的串行与局部并行聚合壳统一为 `AddResult`
- [x] Task 2: 保持 `IterPage` 与 worker 级别的局部作用域，不跨 goroutine 共享 `AddResult` 内部 map
- [x] Task 3: 保留 `common/c.go` 中手工构造完整 `ResultItem` 的路径不变

## 修改的文件
- `python/number_block.go`: 将 number block 与 Python 3 float freelist 聚合改为 `AddResult`
- `python/mosd.go`: 串行 mosd 分类改为 `AddResult`
- `python/mobile_server2.go`: prop pool 聚合改为 `AddResult`
- `python/mobile_server2_main.go`: mb2 C++ 分类聚合改为 `AddResult`
- `python/maze.go`: `IterPage` page 级聚合改为 `AddResult`
- `txos/txos-aoi.go`: AOI context 聚合改为 `AddResult`
- `txos/leak.go`: `findMapping` / `findArray` 每页聚合改为 `AddResult`
- `cpp/openblas.go`: OpenBLAS 聚合改为 `AddResult`
- `cpp/cpp.go`: worker / 局部分支聚合改为 `AddResult`
- `mallocer/tcmalloc/tcmalloc.go`: span 聚合改为 `AddResult`
- `mallocer/ptmalloc/ptmalloc.go`: chunk worker 聚合改为 `AddResult`
- `mallocer/mimalloc/mimalloc.go`: segment / mmap 聚合改为 `AddResult`
- `mallocer/pieces.go`: 多个 page 分类入口聚合改为 `AddResult`
- `dev-log/2026-03-18-addresult-unification-implementation.md`: 补充实现记录

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过

- 测试命令: `go test ./qa/issue168`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py cpp/20260211-mimalloc-2-1-9-multithread`
- 测试结果: 失败；仅识别到少量 mimalloc 小块与大块，需继续确认该失败是否为既有基线问题或本轮改动暴露的问题

## 验收核对
- AC1: PASS 串行调用点已按测试清单统一改为 `AddResult`
- AC2: PASS `IterPage` 路径已调整为每 page 一个 `AddResult`
- AC3: PASS worker 并行路径已调整为每 worker / 局部任务一个 `AddResult`
- AC4: PASS `common/c.go` 仍保持手工构造 `ResultItem` 的原实现
- AC5: PASS issue 提示与 `dev-flow/plan.md` 中的并发边界约束一致，静态测试通过

## 剩余工作
- 未完成项: 继续定位 `cpp/20260211-mimalloc-2-1-9-multithread` 失败是否与本轮改动相关；由于本轮任务要求必须提交，先提交当前局部重构结果
- 未验证项: 缺少 txos 代表性负载验证；仓库当前 `testdata` 未提供 `txos` 目录

## Self-Review 发现与修复
- 发现 `IterPage` / worker 路径不能简单把整段并发循环包在一个 `AddResult` 外层，否则会把局部 map 暴露给多 goroutine；已按 page / worker 作用域重构
- 发现 `common/c.go` 属于显式手工构造 `ResultItem` 的保留路径；已保持不变

## 注意事项
- 本轮改动只替换局部聚合壳，不改变 `AddReverseIndex`、`BatchSetResultObjTypeMap` 等副作用顺序
- `cpp/20260211-mimalloc-2-1-9-multithread` 当前仍失败，不能据此宣称全部验收完成

## 结论
PARTIAL
