# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 `prim/maze#168` 的验收结论为通过。

已完成：
- 计划文档、测试用例、最新开发报告核对；
- 相对 `origin/master...HEAD` 的改动审查；
- `./maze --build` 构建验证；
- `go test ./qa/issue168` 静态结构验收；
- 至少一个且实际为两个 `testdata` 动态用例验证（Python、jemalloc）。

结论依据：
- 需求文档中的并发边界约束已在实现与测试中体现；
- 目标范围内的串行、`IterPage`、worker 作用域改造均已落到 `AddResult(...)`；
- 范围外的手工 `ResultItem` 构造路径保持不变；
- 本轮已执行的动态验证未发现由该重构引入的新回归。

## Findings

- 本分支已按计划将目标范围内的局部 `ResultItem` 聚合壳统一到 `AddResult(...)`，包括 python / txos / cpp / mallocer 的指定路径。
- `IterPage` 路径中的 `AddResult` 位于 page 回调内部，worker 路径中的 `AddResult` 位于每个 worker 或局部扫描函数内部，未发现跨 goroutine 共享一个局部 map 的实现。
- `common/c.go` 中 `AddMiscToResultItems()` 仍保留手工 `NewResultItem()` + `MergeResultItems(resultItems)` 逻辑，符合“显式完整构造 ResultItem 的路径保持不变”的范围约束。
- 已执行 `python/20260129-complex-types-311` 与 `cpp/20260210-jemalloc-5-0-0` 两个代表性 testdata 用例，均通过。
- 当前仓库没有现成 `testdata/txos/` 用例，因此未能补充 txos 动态 workload 对比；该项不影响本次已设计 TC 的通过结论，但会影响“代表性动态覆盖面”的完整性评估。

## 详细结果

### TC-001: 串行聚合调用点统一为 AddResult
- **结果**: PASS
- **实际行为**: `python/number_block.go`、`python/mosd.go`、`python/mobile_server2.go`、`python/mobile_server2_main.go`、`txos/txos-aoi.go`、`cpp/openblas.go` 均已使用 `AddResult(func(function func(s uint64) *ResultItem) { ... })`；目标文件未保留旧三件套聚合壳。
- **关键指标**: `go test ./qa/issue168 -run TestSerialAggregationCallSitesUseAddResult` 包含在 `go test ./qa/issue168` 中通过。
- **备注**: 与需求中“串行调用点统一为 AddResult”一致。

### TC-002: IterPage 路径按 page 作用域使用 AddResult
- **结果**: PASS
- **实际行为**: `python/maze.go`、`txos/leak.go`、`mallocer/pieces.go` 中，`AddResult` 均位于 `IterPage` 回调内部；未发现将整轮并发扫描包入单个 `AddResult` 的实现。
- **关键指标**: `go test ./qa/issue168 -run TestIterPageScopesWrapPerPageAddResultOnly` 包含在 `go test ./qa/issue168` 中通过。
- **备注**: `BatchSetResultObjTypeMap(X)` 等副作用仍保留在 page 作用域末尾，未见明显时序倒置。

### TC-003: worker 并行路径按 worker 作用域使用 AddResult
- **结果**: PASS
- **实际行为**: `cpp/cpp.go`、`mallocer/tcmalloc/tcmalloc.go`、`mallocer/ptmalloc/ptmalloc.go`、`mallocer/mimalloc/mimalloc.go` 中，`AddResult` 位于 worker 或局部扫描函数内部；未发现共享旧式 `resultItems` 局部 map 的代码。
- **关键指标**: `go test ./qa/issue168 -run TestWorkerScopedAggregationUsesAddResultWithoutCrossWorkerMapSharing` 包含在 `go test ./qa/issue168` 中通过。
- **备注**: 与“不能跨 goroutine 共享一个 AddResult 局部 map”的约束一致。

### TC-004: 显式手工构造 ResultItem 的路径保持不变
- **结果**: PASS
- **实际行为**: `common/c.go` 的 `AddMiscToResultItems()` 仍使用 `NewResultItem()` 和 `MergeResultItems(resultItems)`，未被误替换为 `AddResult`。
- **关键指标**: `go test ./qa/issue168 -run TestExplicitManualResultItemConstructionRemainsUnchanged` 包含在 `go test ./qa/issue168` 中通过。
- **备注**: 范围控制正确，未见误改。

### TC-005: 需求文档明确约束并发边界
- **结果**: PASS
- **实际行为**: `.symphony_prompt.md` 与 `dev-flow/plan.md` 均明确写明不可跨 goroutine 共享同一个 `AddResult` 局部 map，且要求 `IterPage` / worker 路径按局部作用域使用 `AddResult`。
- **关键指标**: `go test ./qa/issue168 -run TestIssuePromptAndPlanCaptureConcurrencyConstraint` 包含在 `go test ./qa/issue168` 中通过。
- **备注**: 需求基线与实现边界一致。

## 动态验证结果

### 用例 1: python/20260129-complex-types-311
- **结果**: PASS
- **实际行为**: `pymempool_objects = 68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`，其余 dataclass / dict / namedtuple / deque / set 校验均通过。
- **关键指标**: 测试框架汇总为 `Total: 1 passed, 0 failed`。
- **备注**: 可作为 Python 路径代表性 smoke test，未见本轮重构破坏 Python 主链路聚合行为。

### 用例 2: cpp/20260210-jemalloc-5-0-0
- **结果**: PASS
- **实际行为**: jemalloc 用例识别到 100379 个 chunk；小块、128~2048 字节中块、1MB+ 大块分布均符合验证脚本预期。
- **关键指标**: `malloc(64)=20000`、`malloc(32)=20000`、`malloc(8)=20001`、`malloc(1024)=10000`，VMS 约 `0.70 GB`。
- **备注**: 说明 mallocer 代表路径在本轮改造后仍能通过代表性回归。

## 构建与静态检查

- `./maze --build`: 通过
- `go test ./qa/issue168`: 通过
- 代码审查确认：`common/result_items.go` 中 `AddResult` 仍保持“创建局部 map -> 提供 function -> 执行回调 -> MergeResultItems(resultItems)”的既有语义。
- 代码审查确认：`grep` 结果显示当前工作树中目标源码路径已移除旧式局部聚合壳；剩余匹配主要位于 `common/c.go`、`common/result_items.go`、`lua/lua.go` 以及 `tmp/baseline/` 副本，不属于本 issue 目标替换范围。

## 失败的 Test Case（如有）

无。

## 未执行项

- txos 代表性动态 workload 对比未执行。
- **原因**: 当前仓库 `testdata/` 下未发现可直接执行的 `txos` 现成测试目录。
- **影响范围**: 不影响 TC-001~TC-005 的静态验收结论，但会影响需求文档“python / mallocer / txos 代表性动态对比”中的 txos 动态覆盖面完整性。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`: PASS
- `go test ./qa/issue168`: PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
- `python3 testdata/run_test.py cpp/20260210-jemalloc-5-0-0`: PASS

## 总结

- 通过: 7 / 7（5 个 TC + 2 个动态 testdata 用例）
- 失败: 0 / 7
- 建议: 可以合入本次重构；后续如需补足需求文档中的动态覆盖完整性，建议单独补充 txos 可执行 testdata，再做一次针对 txos workload 的补充验收。
