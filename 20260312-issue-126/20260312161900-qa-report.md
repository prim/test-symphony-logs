## QA Result: PASS

## 概述

验收 prim/maze#126：修复 standalone 模式下 HttpSetup 缺失 base URL 导致 panic。

本次为 QA retry #5。根据 human review 反馈（"不应该这样！看看最近的对 ./maze 文件的提交改动，分析一下为什么没有默认配置"），developer 已将修复策略从"跳过 /new 注册"改为"在 `FillCommonVars()` 中恢复遗漏的默认 HttpUrl 配置"。本次验收重点验证该根因修复是否正确。

## Findings

### 代码变更范围（静态检查）

**变更文件**：
- `prepare/fill.go`：在 `FillCommonVars()` 末尾添加 `S.Common.HttpUrl` 和 `S.Http` 的默认值设置（`defaultHttpUrl = "http://175.178.73.182:9090"`），恢复旧版 Python 入口脚本中遗漏的硬编码默认值
- `http.go`：新增 `resolveHttpTarget()`、`hasValidHttpBaseURL()`、`httpRegisterNewSession()` 辅助函数作为防御性校验层；重构 `HttpJsonRequest()` 将 panic helper `C(err, ...)` 替换为 error return；`HttpSetup()` 和 `HttpLoop()` 增加 base URL 校验分支
- `tests/test_issue_126_standalone_http_setup_base_url.py`：5 个 TDD 回归测试
- `dev-log/2026-03-12-fix-standalone-httpsetup-missing-base-url.md`：开发日志更新

### 根因分析验证

| 层级 | 位置 | 分析结果 |
|------|------|----------|
| 根因修复 | `prepare/fill.go:183-189` | ✅ 正确恢复默认值。`FillCommonVars()` 在 `S.Common.HttpUrl == ""` 时设置默认值 `"http://175.178.73.182:9090"`，与旧版 Python 入口一致 |
| 一致性 | `prepare/prepare.go:257-258` | ✅ `buildLegacyStorageSnapshot()` 中也有相同的 httpUrl 默认值设置（用于快照文件） |
| 一致性 | `main.go:124` | ✅ `mazeHost` 常量也是 `"http://175.178.73.182:9090"`，各处保持一致 |
| 防御层 | `http.go:50-74` | ✅ `resolveHttpTarget()` 使用 `net/url.Parse` 校验 scheme/host；`hasValidHttpBaseURL()` 快速判断 |
| 防御层 | `http.go:119-125` | ✅ `HttpSetup()` 在 base URL 意外无效时跳过 /new（防御性，正常流程不触发） |
| 防御层 | `http.go:944-951` | ✅ `HttpLoop()` 在 base URL 意外无效时进入 local-only 模式（防御性，正常流程不触发） |
| 错误处理 | `http.go:76-101` | ✅ `HttpJsonRequest()` 中所有 `C(err, ...)` 已替换为 `return nil, fmt.Errorf(...)`，不再 panic |

### 与 human review 反馈的对应关系

| human review 反馈 | 实际修复 | 状态 |
|-------------------|----------|------|
| "不应该这样"（之前跳过 /new 注册） | 现在正常执行 /new 注册（默认值已恢复） | ✅ 已修正 |
| "看看最近的对 ./maze 文件的提交改动" | 追溯到 #110 迁移时遗漏了 Python 入口的 `host = "http://175.178.73.182:9090"` | ✅ 已分析 |
| "分析一下为什么没有默认配置" | 在 `FillCommonVars()` 中恢复默认配置 | ✅ 已恢复 |

### 注意事项（非阻塞）

- `HttpPushByReqId()` 调用 `HttpJsonRequest("push", args)` 时忽略了返回的 error。在正常流程下这不会造成 panic（因为 `C(err, ...)` 已被移除），但建议后续优化。
- `handleL2WithReqId` 和 `apiL2Data` 中仍直接使用 `S.Common.HttpUrl` 构建 upload URL（非通过 `resolveHttpTarget`），但因为默认值已被正确设置，不会出问题。

## Test Results

### TC-001: standalone 未配置远端地址时跳过 `/new` 注册
- **结果**: PASS
- **验证方式**: 静态代码审查 + 单元测试 + live 进程动态验证
- **实际行为**: 默认值已恢复，`HttpSetup()` 正常走 /new 注册流程（防御性跳过分支仅在异常情况触发）

### TC-002: HTTP 请求前必须校验并构造绝对地址
- **结果**: PASS
- **验证方式**: 静态代码审查 + 单元测试
- **实际行为**: `resolveHttpTarget()` 使用 `net/url.Parse` 校验 scheme/host 后拼接绝对 URL

### TC-003: 缺失或非法 base URL 走可诊断错误而非 panic
- **结果**: PASS
- **验证方式**: 静态代码审查 + 单元测试
- **实际行为**: `HttpJsonRequest()` 中所有 `C(err, ...)` 已移除，替换为 `return nil, fmt.Errorf(...)`

### TC-004: tar `--http` 回归路径仍可进入 HTTP 主链路
- **结果**: PASS
- **验证方式**: 静态代码审查 + testdata 动态验证
- **实际行为**: `mainTarMode()` → `mainStandalone()` → `mazehttp()` → `HttpLoop()` 路径完整

### TC-005: HTTP 初始化重构后项目仍可构建
- **结果**: PASS
- **验证方式**: `./maze --build` 成功 + 单元测试 5/5 通过
- **实际行为**: 构建成功，退出码 0

### 动态测试详情

#### 关键测试：Live 进程 + `--build --run --pid`（human review 要求）

| 步骤 | 结果 |
|------|------|
| 启动 `python3 testdata/python/20260128-basic/basic.py`（PID 1759415）| ✅ 进程正常运行 |
| 执行 `./maze --build --run --pid 1759415`（timeout 120s）| ✅ 分析完成，无 panic |
| 输出包含 `please goto http://175.178.73.182:9090/maze/#/...` | ✅ 默认 URL 正确 |
| 输出包含 `WebUI V2: http://175.178.73.182:9090/v2/#/...` | ✅ WebUI 链接正常 |
| 输出包含内存分析摘要（18910 objects, 3.7M total）| ✅ 分析结果正常 |
| maze.log 中 `HttpJsonRequest begin new` → `status=200` | ✅ /new 注册成功 |
| maze.log 无 panic / unsupported protocol scheme / skip 日志 | ✅ 未发现异常 |
| 退出码 124（timeout 命令杀掉 HttpLoop 中的轮询）| ✅ 预期行为 |

**关键对比**：之前的 QA 报告中看到 "skip remote /new" 日志，但**本次测试不再出现该日志**——因为默认值已正确恢复，`hasValidHttpBaseURL()` 返回 true，`HttpSetup` 正常走远端 `/new` 注册流程。这正是 human review 期望的行为。

#### 单元测试

| 测试 | 结果 |
|------|------|
| `python3 -m unittest tests/test_issue_126_standalone_http_setup_base_url.py -v` | ✅ 5/5 通过 |

#### testdata 回归测试

| 测试用例 | 结果 |
|----------|------|
| `python3 testdata/run_test.py python/20260128-basic` | ✅ PASSED |
| `python3 testdata/run_test.py python/20260129-complex-types-311` | ✅ PASSED |

## AC 验证汇总

| AC | 描述 | 状态 | 验证方式 |
|----|------|------|----------|
| AC1 | standalone 不再 panic | ✅ PASS | live 进程 `--build --run --pid` 实测：无 panic，/new 注册成功 |
| AC2 | basic.py 进程可进入本地 WebUI 流程 | ✅ PASS | live 进程实测：输出 WebUI URL，进入 HttpLoop 远端轮询 |
| AC3 | HttpSetup 跳过 /new 并日志说明 | ✅ PASS | 防御性分支存在（代码审查确认），正常流程走 /new 注册 |
| AC4 | 合法 URL 使用绝对地址 | ✅ PASS | `resolveHttpTarget()` 使用 `net/url.Parse` + maze.log 确认 /new status=200 |
| AC5 | 非法 URL 返回可诊断错误 | ✅ PASS | 代码审查：`resolveHttpTarget` 返回描述性错误，不 panic |
| AC6 | tar --http 回归无异常 | ✅ PASS | 两个 testdata 回归测试通过 |

## 总结

- 通过: 5 / 5 TC
- 失败: 0 / 5 TC
- AC 全部满足: 6 / 6
- 建议: **可以发布**。本次修复正确恢复了遗漏的默认 HTTP base URL 配置，与 human review 反馈完全一致。Live 进程端到端测试确认 /new 注册成功（status=200），不再出现 panic 或 skip 行为。回归测试全部通过。
