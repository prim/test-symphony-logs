## Summary
Verified the existing fix for standalone mode HttpSetup panic when base URL is missing. The code changes in commit a053706d correctly handle the case where `S.Common.HttpUrl` is empty by gracefully skipping remote `/new` session registration and entering local-only interactive mode.

Human review requested live testing with `basic.py` + `./maze --build --run --pid <PID>`, which has been completed successfully.

## Changes
- No additional source code changes needed — the existing fix in `http.go` is correct and complete.

## Testing
All tests verified:

1. **Live process test**: Started `testdata/python/20260128-basic/basic.py` (PID 1530505), ran `./maze --build --run --pid 1530505` — analysis completed successfully, entered local-only interactive mode without panic.
2. **Testdata regression**: `python3 testdata/run_test.py python/20260128-basic` → ✅ PASSED
3. **Testdata regression**: `python3 testdata/run_test.py python/20260129-complex-types-311` → ✅ PASSED
4. **Unit tests**: `python3 -m unittest tests/test_issue_126_standalone_http_setup_base_url.py -v` → 5/5 OK

## AC Verification
- AC1 (no panic): PASS — verified via live `--build --run --pid` execution
- AC2 (basic.py WebUI flow): PASS — analysis + local-only HttpLoop confirmed
- AC3 (skip /new with log): PASS — log output: `HttpSetup: skip remote /new — no valid HTTP base URL configured, using local-only mode`
- AC4 (absolute URL for valid base): PASS — code uses `resolveHttpTarget()` with `net/url.Parse` validation
- AC5 (diagnostic error for invalid URL): PASS — all `C(err, ...)` replaced with `fmt.Errorf`
- AC6 (tar --http regression): PASS — both testdata cases pass
