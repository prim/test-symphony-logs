# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 验收范围与依据

- 验证目标：`prim/maze#59`
- 本轮重点：
  - 复验 common result item / type id 重构是否已彻底收敛到 `core/result` 新设计
  - 复验旧 `common` 侧结果状态入口是否已删除，不再保留旧方案
  - 复验 Node.js 最终输出不再泄漏 `formatResultType <id>`
  - 按 prompt 要求执行多组 `testdata` 串行回归
- 已阅读：
  - `.symphony_prompt.md`
  - `dev-flow/20260310122721-dev-report.md`
  - `core/result/store.go`
  - `core/result/typeid.go`
  - `common/result_items.go`
  - `mallocer/pieces.go`
  - `main.go`
  - `http.go`
  - `python/results.go`
  - `python/maze.go`
  - `txos/results.go`
  - `nodejs/maze.go`
- 说明：当前工作树中仍不存在 `dev-flow/plan.md` 与 `dev-flow/test-cases.md`，因此无法依据文档逐项映射 AC / TC；本轮依据 `.symphony_prompt.md`、最新开发报告、实际代码与真实回归结果执行验收。

## 详细结果

### TC-001: 结果状态所有权统一收敛到 `core/result`
- **结果**: PASS
- **实际行为**: `core/result/store.go` 已集中维护 `Items`、`ItemPool`、`CurObjSize`；主代码路径直接访问 `result.Items` / `result.ItemPool` / `result.CurObjSize`。
- **关键指标**:
  - `core/result/store.go:5-13` 定义 `Items`、`ItemPool`、`CurObjSize`
  - `mallocer/pieces.go:58` 初始化时直接重置 `result.Items`
  - `main.go:169` 使用 `SortByValue(result.Items, SortBySize)`
  - `http.go:631-632` 直接遍历 `result.Items`
- **备注**: 结果状态所有权已不在 `common` 保留。

### TC-002: 不再保留旧 `common` 侧结果状态入口
- **结果**: PASS
- **实际行为**: `common/result_types.go` 已不存在；代码搜索未发现主代码路径继续保留 `ResultItems` / `ResultItemPool` 兼容变量入口，`CurObjSize` 使用均为 `result.CurObjSize` 新设计访问。
- **关键指标**:
  - `common/result_types.go`: 不存在
  - 全仓搜索仅剩：`result.CurObjSize` 新设计访问、注释、以及 `sub-repos/lua-bak` 历史代码
  - `python/results.go:176` 直接读取 `result.Items[s]`
  - `python/maze.go:270` 直接读取 `result.Items[s]`
  - `txos/results.go:47,366` 使用 `result.CurObjSize = result.Items[...]`
- **备注**: 满足“两个都不保留，直接改写成新的设计”的要求。

### TC-003: 仅保留类型别名兼容签名，不再保留旧状态方案
- **结果**: PASS
- **实际行为**: `common/result_items.go` 仅保留 `type ResultItem = result.ResultItem` 与 `type PriorityQueue = result.PriorityQueue` 类型别名；未再定义旧全局状态变量。
- **关键指标**:
  - `common/result_items.go:18-19` 仅保留类型别名
  - 文件内不存在 `ResultItems` / `ResultItemPool` 兼容变量定义
- **备注**: 兼容范围已缩小到类型签名层，不再是旧设计入口。

### TC-004: Node.js 最终输出不再泄漏 `formatResultType <id>`
- **结果**: PASS
- **实际行为**: 代码检查与准确性回归均确认，未知 Node.js 类型输出为结构化 `unknown{...}`，未再出现旧内部 fallback 文本。
- **关键指标**:
  - `nodejs/maze.go:1264-1279` 解析 synthetic Node.js TypeID
  - `nodejs/maze.go:1309` 最终 fallback 为 `unknown{type_id=0x%x}`
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
  - 验证脚本明确输出：`No leaked 'formatResultType <id>' fallback found`
- **备注**: 历史问题点复验通过。

### TC-005: 多组 testdata 串行回归执行
- **结果**: PASS
- **实际行为**: 已严格串行执行 Python / C++ / Node.js 多组 testcase，满足 prompt 中“qa 需要多执行几个 testdata 回归”的要求。
- **关键指标**:
  - `./maze --build`: PASS
  - `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
  - `python3 testdata/run_test.py python/20260201-py-merge`: PASS
  - `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS
  - `python3 testdata/run_test.py nodejs/20260201-basic-objects`: PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
- **备注**: 回归覆盖 Python / C++ / Node.js 三类测试资产。

### TC-006: 日志检查无阻断性异常
- **结果**: PASS
- **实际行为**: 已检查最近 Node.js 回归生成的 `maze.log` 与 `maze.py.log`；未见导致分析中断的 panic、traceback 或致命错误，分析流程完成并产出结果。
- **关键指标**:
  - `postman-db/coredump-default-3110770/maze.log`：存在正常初始化、扫描与结果输出日志
  - `postman-db/coredump-default-3110770/maze.py.log`：存在 corefile / maps / 进程初始化日志，无阻断性异常
- **备注**: 测试过程中仍可见符号下载失败等环境类警告，但未影响 testcase 最终通过。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 执行过的命令

```bash
git diff --stat origin/master...HEAD
./maze --build
python3 testdata/run_test.py python/20260129-complex-types-311
python3 testdata/run_test.py python/20260201-py-merge
python3 testdata/run_test.py cpp/20260201-basic-malloc
python3 testdata/run_test.py nodejs/20260201-basic-objects
python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot
```

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: **本轮可记为通过**。

本轮结论：
- `core/result` 已成为结果状态与 TypeID 的统一归属；
- 旧 `common` 侧结果状态入口已删除，未再保留旧方案；
- Node.js 最终输出已不再泄漏 `formatResultType <id>`；
- 多组 Python / C++ / Node.js 回归已实际串行执行并通过；
- 日志检查未发现阻断性异常。

因此，本轮 QA 结果为 **PASS**。
