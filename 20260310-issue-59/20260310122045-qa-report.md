# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 验收范围与依据

- 验证目标：`prim/maze#59`
- 本轮重点：
  - 复验 common result item / type id 重构是否已彻底收敛到 `core/result` 新设计
  - 复验是否已删除旧 `common` 侧结果存储兼容入口
  - 复验 Node.js 最终输出不再泄漏 `formatResultType <id>`
  - 按 prompt 要求执行多组 `testdata` 回归
- 已阅读：
  - `.symphony_prompt.md`
  - `dev-flow/20260310121624-dev-report.md`
  - `core/result/store.go`
  - `core/result/item.go`
  - `common/result_items.go`
  - `common/classifier.go`
  - `common/callback.go`
  - `common/webui.go`
  - `common/c.go`
  - `main.go`
  - `http.go`
  - `mallocer/pieces.go`
  - `python/results.go`
  - `python/maze.go`
  - `txos/results.go`
  - `nodejs/maze.go`
- 说明：当前工作树中仍不存在 `dev-flow/plan.md` 与 `dev-flow/test-cases.md`，因此无法按要求文档逐项映射 AC / TC；本轮依据 prompt、最新开发报告、实际代码与真实回归结果执行验收。

## 详细结果

### TC-001: 结果状态所有权统一收敛到 `core/result`
- **结果**: PASS
- **实际行为**: `core/result/store.go` 已承载全局 `Items`、`ItemPool`、`CurObjSize`；主流程代码直接访问 `result.Items` / `result.ItemPool` / `result.CurObjSize`，未再通过 `common` 暴露旧状态变量。
- **关键指标**:
  - `core/result/store.go:5-13` 定义 `Items`、`ItemPool`、`CurObjSize`
  - `common/result_items.go:24` 使用 `result.ItemPool.Get()`
  - `common/result_items.go:103` 合并结果时写入 `result.Items`
  - `mallocer/pieces.go:58` 初始化时直接重置 `result.Items`
- **备注**: 状态所有权迁移已完成。

### TC-002: 不再保留旧 `common` 侧结果设计入口
- **结果**: PASS
- **实际行为**: 当前工作树中 `common/result_types.go` 已删除；代码搜索未发现主代码路径继续使用 `ResultItems` / `ResultItemPool` 旧兼容变量名，剩余命中仅为注释、`result.CurObjSize` 的新设计访问，或 `sub-repos/lua-bak` 历史代码。
- **关键指标**:
  - `common/result_types.go`: 不存在
  - `main.go:169` 使用 `SortByValue(result.Items, SortBySize)`
  - `http.go:631-632` 直接遍历 `result.Items`
  - `python/results.go:176` 直接读取 `result.Items[s]`
  - `txos/results.go:47,366` 使用 `result.CurObjSize = result.Items[...]`
- **备注**: 当前实现已满足“两个都不保留，直接改写成新的设计”的要求。

### TC-003: `ResultItem` / `PriorityQueue` 类型签名兼容但不再保留旧状态变量
- **结果**: PASS
- **实际行为**: `common/result_items.go` 仅保留 `type ResultItem = result.ResultItem`、`type PriorityQueue = result.PriorityQueue` 类型别名，以兼容现有函数签名；未再保留旧全局变量入口。
- **关键指标**:
  - `common/result_items.go:18-19` 仅保留类型别名
  - 文件中未定义 `ResultItems` / `ResultItemPool` / `CurObjSize` 兼容变量
- **备注**: 兼容范围已缩小到类型名层面，符合开发报告说明。

### TC-004: Node.js 最终输出不再泄漏 `formatResultType <id>`
- **结果**: PASS
- **实际行为**: 代码检查与真实回归均确认，未知 Node.js 类型输出为结构化 `unknown{...}`，未再出现旧内部 fallback 文本。
- **关键指标**:
  - `nodejs/maze.go:1264-1279` 使用 synthetic Node.js TypeID 解析
  - `nodejs/maze.go:1309` 最终 fallback 为 `unknown{type_id=0x%x}`
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
- **备注**: 历史问题点本轮复验通过。

### TC-005: 多组 testdata 串行回归执行
- **结果**: PASS
- **实际行为**: 已严格串行执行 Python / C++ / Node.js 多组 testcase，满足 prompt 中“qa 需要多执行几个 testdata 回归”的要求。
- **关键指标**:
  - `./maze --build`: PASS
  - `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
  - `python3 testdata/run_test.py python/20260201-py-merge`: PASS
  - `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS
  - `python3 testdata/run_test.py nodejs/20260201-basic-objects`: PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
- **备注**: 日志中存在若干 `s3 download failed ... NoSuchKey` 和 `⚠ Ptmalloc heap size not available` 警告，但均未阻断执行，相关 testcase 最终全部通过。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 执行过的命令

```bash
git status --short --branch && git diff origin/master...HEAD
./maze --build
python3 testdata/run_test.py python/20260129-complex-types-311
python3 testdata/run_test.py python/20260201-py-merge
python3 testdata/run_test.py cpp/20260201-basic-malloc
python3 testdata/run_test.py nodejs/20260201-basic-objects
python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot
```

## 总结
- 通过: 5 / 5
- 失败: 0 / 5
- 建议: **本轮可记为通过**。

本轮结论：
- `common/result_types.go` 兼容入口已删除，旧 `common` 侧结果状态入口未再保留；
- 主代码路径已统一直接访问 `core/result` 的状态与类型；
- Node.js 最终输出已不再泄漏 `formatResultType <id>`；
- 多组 Python / C++ / Node.js 回归已实际串行执行并通过。

因此，本轮 QA 结果为 **PASS**。
