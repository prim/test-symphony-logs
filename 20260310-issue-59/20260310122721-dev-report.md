# 开发报告

## 完成的任务
- [x] Task 1: 将 common result item / type id 相关实现收敛到 `core/result` 新设计，主流程统一直接访问 `result.Items` / `result.ItemPool` / `result.CurObjSize`。
- [x] Task 2: 保留 `common/result_items.go` 中的类型别名兼容现有签名，同时删除旧状态入口思路，不再通过 `common` 保留结果状态所有权。
- [x] Task 3: 重新执行构建，并串行完成 Python / C++ / Node.js 多组回归验证。

## 修改的文件
- `core/result/store.go`: 集中维护 `Items`、`ItemPool`、`CurObjSize` 全局结果状态。
- `core/result/item.go`: 提供 `ResultItem` / `PriorityQueue` 实现，供新结果存储设计直接使用。
- `core/result/typeid.go`: 提供新的 `TypeID` 模块化编码与 Node.js/Piece/Vtable/BigSize 基值。
- `common/result_items.go`: 改为直接操作 `core/result` 的结果状态，仅保留类型别名以兼容函数签名。
- `common/classifier.go`: 分类类型范围判断改为基于 `core/result.TypeID` 模块常量。
- `common/c.go`: misc 结果项创建改为直接使用 `result.NewResultItem()`。
- `common/callback.go`: 结果读取改为直接访问 `result.Items`。
- `common/utils.go`: 文本输出、随机 dot、show-stringify 等逻辑改为直接访问 `result.Items`。
- `common/webui.go`: Web UI 排序、TopN 裁剪与对象读取改为直接使用 `result.Items`。
- `main.go`: 文本模式排序输出改为直接使用 `result.Items`。
- `http.go`: L4 接口汇总内存时改为直接遍历 `result.Items`。
- `mallocer/pieces.go`: 初始化结果映射与分类回调签名改为直接使用 `core/result`。
- `python/maze.go`: Python 聚合回调与 debug 数据读取改为直接使用 `core/result` 类型/状态。
- `python/results.go`: Python Postman 结果读取改为直接访问 `result.Items`。
- `txos/results.go`: 优先队列与对象大小映射改为直接使用 `result.PriorityQueue` / `result.CurObjSize`。
- `nodejs/maze.go`: Node.js synthetic type id 与未知类型 fallback 改为使用 `core/result` 新 TypeID 设计。

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过（输出 `begin go build`，后续回归测试均可正常执行）。

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过。

- 测试命令: `python3 testdata/run_test.py python/20260201-py-merge`
- 测试结果: 通过。

- 测试命令: `python3 testdata/run_test.py cpp/20260201-basic-malloc`
- 测试结果: 通过。

- 测试命令: `python3 testdata/run_test.py nodejs/20260201-basic-objects`
- 测试结果: 通过。

- 测试命令: `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
- 测试结果: 通过；验证脚本确认未泄漏 `formatResultType <id>`，结构化未知类型输出正常。

## Self-Review 发现与修复
- 检查结果状态所有权：确认主代码路径已直接使用 `core/result`，未再保留 `ResultItems` / `ResultItemPool` / `CurObjSize` 旧状态变量入口。
- 检查类型兼容性：保留 `common/result_items.go` 中 `ResultItem` / `PriorityQueue` 类型别名，避免跨包函数签名大范围扰动。
- 检查 Node.js fallback：确认未知类型输出为结构化 `unknown{...}`，未再回退到旧的 `formatResultType <id>` 文本。

## 注意事项
- 当前工作树中仍不存在 `dev-flow/plan.md`，本轮依据 `.symphony_prompt.md`、最新 QA 报告与现有代码上下文完成修复与验证。
- 本轮严格未修改任何 `testdata/` 下测试文件。
- 按 Symphony 协议，本报告写入 `dev-flow/` 作为交付物，但不会纳入本次源码提交。
