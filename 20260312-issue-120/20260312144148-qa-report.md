## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- 已阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md` 与既有 QA 报告，确认本 issue 的核心验收标准是：`testdata/python` 下全部可运行用例顺序执行后全部通过，且至少覆盖 `py27`、`py311`、`py312`。
- `dev-flow/` 目录下仍未发现 `*-dev-report.md`，因此“读取最新开发报告”步骤无法执行；本次验收基于计划文档、测试用例文档、代码差异检查与实际测试结果完成。
- `./maze --build` 构建通过，说明当前工作区至少具备执行动态验收的基础条件。
- 但在执行已知历史失败用例 `python/20260129-complex-types-35` 时，虽然 `pymempool_objects` 与 `PersonClass / GameEntity / TreeNode / SimpleClass` 已恢复，仍出现 `Player dataclass not found`，因此“7 个已知失败回归全部转绿”与“Python testdata 全量串行回归通过”均不能判定达成。
- 先前 `dev-flow/20260312134131-qa-report.md` 给出 PASS 结论，但该报告明确未跑完整动态回归；本轮实际复测已发现阻断性失败，因此最终结论应为 FAIL。

## 详细结果

### TC-001: 已知失败用例资产完整性检查
- **结果**: PASS
- **实际行为**: 已核对 `dev-flow/test-cases.md` 中列出的 7 个历史失败用例，测试计划要求统一通过 `python3 testdata/run_test.py <case>` 执行；当前仓库中的测试组织方式与该要求一致。
- **关键指标**: 计划文档与测试用例文档一致指向 7 个历史失败用例与 16 个全量 Python 用例集合。
- **备注**: 本项属于低成本静态核对，未发现测试入口被绕开。

### TC-002: 代表 Python 版本回归通过
- **结果**: PASS（基于已完成代表性复测记录）
- **实际行为**: 之前已完成代表版本复测，`py27`、`py311`、`py312` 均通过，满足代表版本覆盖要求。
- **关键指标**:
  - `python/20260129-complex-types-27`: PASS
  - `python/20260129-complex-types-311`: PASS
  - `python/20260129-complex-types-312`: PASS
- **备注**: 代表版本通过不等于全量回归通过，仍需结合 TC-003 / TC-004 判定最终结果。

### TC-003: 7 个已知失败回归全部转绿
- **结果**: FAIL
- **实际行为**: 本轮实际执行 `python3 testdata/run_test.py python/20260129-complex-types-35`，用例在 dataclass 校验阶段失败。
- **关键指标**:
  - `pymempool_objects = 55970`，已满足 `> 50000`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
  - 失败点：`Assertion Error: Player dataclass not found`
- **备注**: 说明该分支虽然修复了部分旧问题，但 `Python 3.5` 兼容路径仍存在 dataclass/类型识别缺陷。

### TC-004: Python testdata 全量串行回归通过
- **结果**: FAIL
- **实际行为**: 根据验收标准，只要任一可运行 Python 用例失败，就不能判定全量通过。本轮复测已发现 `python/20260129-complex-types-35` 失败，因此本项直接不成立。
- **关键指标**: 至少 1 个可运行 Python 用例失败。
- **备注**: 在已发现阻断性失败后，继续执行剩余高成本动态回归对“是否可验收”结论不会产生正向改变，因此按早停策略停止阶段 C 的剩余执行。

## 失败的 Test Case

### TC-003: 7 个已知失败回归全部转绿
- **结果**: FAIL
- **预期行为**: `python/20260129-complex-types-35` 应通过全部断言，包括 `Player` dataclass 与 `Config` dataclass 的识别。
- **实际行为**: 用例执行到 dataclass 校验时失败，报错 `Assertion Error: Player dataclass not found`。
- **复现步骤**:
  1. 在仓库根目录执行 `./maze --build`
  2. 执行 `python3 testdata/run_test.py python/20260129-complex-types-35`
  3. 观察验证阶段输出，失败于 `Validating dataclass instances`
- **可能原因**: Python 3.5 路径上的类型名恢复、对象分类或 dataclass 相关实例聚合逻辑仍不完整，导致 `Player` 未出现在结果中。
- **建议修复**: 请开发侧重点排查 Python 3.5 下 dataclass / class name 恢复链路，确认 `Player` 与 `Config` 是否被错误归类、被截断、或在 top 结果聚合时丢失。

## Test Results

- `./maze --build`: PASS
- `python3 testdata/run_test.py python/20260129-complex-types-35`: FAIL

## 未执行项

- 未执行其余历史失败用例的本轮复测（如 `python/20260129-complex-types`、`-36`、`-37`）
  - **原因**: `python/20260129-complex-types-35` 已证明“历史失败用例全部转绿”这一关键 AC 不成立，继续执行剩余高成本动态回归不会改变总体 FAIL 结论。
  - **可能影响 AC**: AC1、AC3、AC4。
- 未执行完整 16 个 Python testdata 串行动态回归
  - **原因**: 已出现阻断性失败，不满足“全量全部通过”的必要前提，按早停策略停止后续高成本动态验证。
  - **可能影响 AC**: AC1、AC2、AC3、AC4。

## 兼容性验证

- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过（部分版本）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 2 / 4
- 失败: 2 / 4
- 建议: 当前分支**不能发布**。请先修复 `python/20260129-complex-types-35` 的 `Player dataclass not found` 问题，并在修复后重新执行历史失败集合与全量 Python testdata 串行回归。
