## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- 阶段 A 静态检查已完成：需求文档、测试用例文档、关键代码改动、测试文件与测试资产均已核对。
- `dev-flow/` 下未发现任何 `*-dev-report.md`，因此“失败根因与修复结果有 dev-log / dev report 记录”的可追溯性证据不完整；本次仅读到 `dev-log/2026-03-12-issue-120-python-regression-test-design.md`。
- 关键 smoke test 在代表版本 `py27` 上即失败：`python/20260129-complex-types-27` 的 `pymempool_objects` 仍为 `24348`，未达到计划要求的 `> 30000`，说明旧版本兼容主链路尚未修复。
- 由于 TC-002 的首个代表用例已证明 AC1/AC2 不成立，继续执行其余高成本动态回归大概率只会重复暴露同一兼容路径问题，因此按早停策略停止阶段 B/C 的后续动态测试。

## 详细结果

### TC-001: 已知失败用例资产完整性检查
- **结果**: PASS
- **实际行为**: 已确认 `testdata/run_test.py` 存在；7 个已知失败用例目录存在；对应目录均包含 `validate.py` 与 `coredump-*.tar.gz`。
- **关键指标**: 7 / 7 用例资产完整。
- **备注**: 此项通过，说明回归覆盖面未被意外删减。

### TC-002: 代表 Python 版本回归通过
- **结果**: FAIL
- **实际行为**: 执行首个代表版本用例 `python/20260129-complex-types-27` 失败，校验阶段输出 `pymempool_objects = 24348`，低于阈值 `> 30000`。
- **关键指标**: `pymempool_objects = 24348`，阈值要求 `> 30000`。
- **备注**: 该失败直接否定了旧版本兼容路径已修复的结论；因此 `py311`、`py312` 未继续执行。

### TC-003: 7 个已知失败回归全部转绿
- **结果**: FAIL
- **实际行为**: 未逐个执行全部 7 个失败用例；但由于代表版本中的 `python/20260129-complex-types-27` 仍失败，已足以证明“全部转绿”不成立。
- **关键指标**: 已验证失败用例 1 / 7，其中失败 1 / 1。
- **备注**: 属于被关键 smoke test 阻断后的结论性失败。

### TC-004: Python testdata 全量串行回归通过
- **结果**: FAIL
- **实际行为**: 未启动全量 16 个用例串行回归；在关键 smoke test 阶段已确认代表版本用例失败，AC1 无法满足。
- **关键指标**: 全量动态回归未执行。
- **备注**: 按早停策略停止，避免重复消耗时间在已知失效主链路上。

## 失败的 Test Case

### TC-002: 代表 Python 版本回归通过
- **结果**: FAIL
- **预期行为**: `python/20260129-complex-types-27`、`python/20260129-complex-types-311`、`python/20260129-complex-types-312` 三个代表版本用例均应通过 `python3 testdata/run_test.py <case>` 验收。
- **实际行为**: `python/20260129-complex-types-27` 运行失败，`validate.py` 报告 `Expected > 30000 pymempool objects, got 24348`。
- **复现步骤**:
  1. 在仓库根目录执行 `./maze --build`
  2. 执行 `python3 testdata/run_test.py python/20260129-complex-types-27`
  3. 观察验证输出中的 `pymempool_objects = 24348`
  4. 查看测试总结，可见该用例返回失败
- **可能原因**:
  - 本分支虽修改了 Python 版本识别、dict 大小计算与 deque 遍历兼容逻辑，但未覆盖 `py27` 下 `pymalloc` 对象统计偏低的核心链路；
  - 从当前改动范围看，没有看到直接针对 `pymempool_objects` 计数链路的专门修复，因此计划中“复查 py27/35/36/37 对象识别与 pymempool_objects 统计链路”的工作尚未完成。
- **建议修复**:
  - 继续针对 Python 2.7 的 `pymalloc / traverse / classify` 统计路径做专项排查；
  - 修复后先复测 `python/20260129-complex-types-27`，再继续 `py311`、`py312` 与全量回归。

## 未执行项

- `TC-002` 中的 `python/20260129-complex-types-311`、`python/20260129-complex-types-312` 未执行
  - 原因：代表版本首个关键用例 `py27` 已失败，旧版本兼容主链路不成立
  - 可能影响 AC：AC2、AC3
- `TC-003` 剩余 6 个已知失败用例未执行
  - 原因：关键 smoke test 已证明“7 个失败用例全部转绿”不成立，继续执行只会扩大重复性失败
  - 可能影响 AC：AC1、AC3、AC4
- `TC-004` 的 16 个 Python 用例全量串行回归未执行
  - 原因：前置代表版本回归失败，主线全绿目标当前已不满足
  - 可能影响 AC：AC1、AC2、AC3、AC4

## 兼容性验证
- [x] Python 2.7 测试已执行且失败（代表版本 `python/20260129-complex-types-27`）
- [ ] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- 已执行静态检查：需求文档、测试用例、关键代码差异、测试资产完整性
- 已执行动态测试：
  - `./maze --build` ✅
  - `python3 testdata/run_test.py python/20260129-complex-types-27` ❌
- 早停结论：关键代表用例失败，当前分支不满足验收标准

## 总结
- 通过: 1 / 4
- 失败: 3 / 4
- 建议: **不能发布**。需先修复 Python 2.7 兼容路径上的 `pymempool_objects` 统计问题，并补充开发报告/修复记录后，再重新执行代表版本与全量 Python testdata 回归。
