## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- 阶段 A 静态检查显示本次改动集中在 Python 版本识别、pymalloc 对齐、deque/dict 分类和类型名缓存并发保护，`./maze --build` 成功，主链路可构建。
- 阶段 B 关键 smoke test 中，代表版本用例 `python/20260129-complex-types-27`、`python/20260129-complex-types-311`、`python/20260129-complex-types-312` 均已通过，说明 py27/py311/py312 主兼容路径已有明显修复。
- 但已知失败回归用例 `python/20260129-complex-types` 仍失败：验证脚本用子串 `PersonClass` 查找类型时，命中了 `"[<class Counter>, <class GameEntity>, <class PersonClass>..."` 这种聚合类型，拿到的数量仅为 1，而不是真正的 `<class PersonClass> instance`（数量 1000）。这表明该问题尚未全量转绿，AC1/AC3/AC4 未满足。
- 由于关键回归用例已证明主链路验收标准“不成立”，未继续执行剩余高成本全量 Python testdata 串行回归，以避免重复报错；但低成本检查已尽量完成。

## 详细结果

### TC-001: 已知失败用例资产完整性检查
- **结果**: PASS
- **实际行为**: 已检查 `testdata/run_test.py` 存在；7 个已知失败用例目录存在，且各目录保留 `validate.py` 与 `coredump-*.tar.gz`。
- **关键指标**: 已覆盖 7/7 个已知失败用例资产检查。
- **备注**: 覆盖面未被意外删减。

### TC-002: 代表 Python 版本回归通过
- **结果**: PASS
- **实际行为**: 依次执行 `python/20260129-complex-types-27`、`python/20260129-complex-types-311`、`python/20260129-complex-types-312`，均通过 `run_test.py` 验收。
- **关键指标**:
  - py27: `pymempool_objects = 41144`，`PersonClass = 1000`，`deque = 300`
  - py311: `pymempool_objects = 68626`，`PersonClass = 1000`，`deque = 300`
  - py312: `pymempool_objects = 68701`，`PersonClass = 1000`，`deque = 300`
- **备注**: 代表版本覆盖满足 AC2 的版本面要求，但不足以证明全量回归通过。

### TC-003: 7 个已知失败回归全部转绿
- **结果**: FAIL
- **实际行为**: 执行首个已知失败用例 `python/20260129-complex-types` 时失败；验证脚本输出 `Expected type 'PersonClass' with amount >= 100, got 1`。
- **关键指标**:
  - `pymempool_objects = 48877`
  - 结果中真实存在 `<class PersonClass> instance`，`amount = 1000`
  - 但验证逻辑先命中包含 `PersonClass` 的聚合类型 `"[<class Counter>, <class GameEntity>, <class PersonClass>..."`，其 `amount = 1`
- **备注**: 当前失败表现更像“输出结果与旧断言匹配策略不兼容”，但从 QA 视角仍属于未满足验收标准。

### TC-004: Python testdata 全量串行回归通过
- **结果**: 未执行
- **实际行为**: 因 TC-003 已证明已知失败集未全部转绿，未继续执行 16 个 Python 用例的高成本串行回归。
- **关键指标**: 无。
- **备注**: 该未执行项会影响 AC1/AC3/AC4 的最终闭环判断。

## 失败的 Test Case

### TC-003: 7 个已知失败回归全部转绿
- **结果**: FAIL
- **预期行为**: 7 个已知失败用例均应通过 `python3 testdata/run_test.py <case>` 验收，其中 `python/20260129-complex-types` 应正确识别 `PersonClass`，断言通过。
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types` 失败，错误为：`Assertion Error: Expected type 'PersonClass' with amount >= 100, got 1`。
- **复现步骤**:
  1. 在项目根目录执行 `./maze --build`
  2. 执行 `python3 testdata/run_test.py python/20260129-complex-types`
  3. 查看验证阶段输出，可见 `Top 20 types` 中存在 `<class PersonClass> instance` 数量 1000
  4. 随后验证脚本仍报 `got 1`
- **可能原因**:
  - `testdata/python/20260129-complex-types/validate.py` 的 `find_type_containing()` 按 `items` 原始顺序进行“首次子串命中”，误匹配到了包含 `PersonClass` 文本的列表/聚合类型，而不是实际实例类型。
  - 当前实现修复后，结果分布/排序与旧脚本假设不再一致，导致断言策略失效。
- **建议修复**:
  - 复核 `python/20260129-complex-types/validate.py` 的匹配策略，避免对 `PersonClass`、`GameEntity` 等类型做不加区分的首次子串命中。
  - 改为优先精确匹配 `<class PersonClass> instance`，或至少过滤掉列表/聚合类型后再匹配。
  - 修复后重新执行已知失败 7 用例与全量 16 用例串行回归。

## 未执行项

- TC-003 剩余 6 个已知失败用例未执行：
  - `python/20260129-complex-types-35`
  - `python/20260129-complex-types-36`
  - `python/20260129-complex-types-37`
  - `python/20260129-complex-types-38`
  - `python/20260201-class-merge-py27`
  - 以及 TC-003 继续验证所需的完整 7 用例闭环
  - **原因**: `python/20260129-complex-types` 已失败，继续执行只会进入更高成本回归阶段，不能改变“已知失败集未全绿”的结论。
  - **影响范围**: AC1、AC3、AC4。
- TC-004 全量 16 个 Python 用例串行回归未执行
  - **原因**: TC-003 已阻塞，无法证明“失败集已转绿”前提。
  - **影响范围**: AC1、AC3、AC4，尤其是“修复后无新增 Python testdata 回归”。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- 已执行动态用例: 4
- 通过: 3
- 失败: 1
- 未执行: 其余已知失败用例与全量回归

## 总结

- 通过: 2 / 4
- 失败: 1 / 4
- 未执行: 1 / 4
- 建议: **当前不能判定可发布。** 代表版本兼容修复已有进展，但 `python/20260129-complex-types` 仍未通过统一验收链路，需先修复该阻塞问题，再重新执行已知失败 7 用例和全量 Python testdata 串行回归。
