# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 `prim/maze#147` 的 VM 门面化重构进行第 4 次复验，重点核对此前发现的 build tags 双开冲突保护问题，并补充静态结构检查、专项 Go 测试、构建验证以及实际 `testdata` 主链路回归。当前已验证的范围内未发现新的阻塞问题。

## Findings

- 未复现此前的双 build tags 冲突保护缺失问题；`vm_lru` 与 `vm_full` 同时启用时，`common/vm/select_conflict.go` 能稳定触发编译失败。
- `common/vm` 门面、`v1/v2` 版本实现、`shared` 公共层以及 `common/vm.go` 兼容转发层均已落地，结构与测试用例约束一致。
- 至少两个真实 `testdata` 用例通过：Python 主链路 `python/20260129-complex-types-311`、C++ 主链路 `cpp/20260201-basic-malloc`。
- 兼容性矩阵与性能基准本轮未完整执行，因此这些内容仅记录为未覆盖项，不影响当前已执行范围内的 PASS 结论。

## 详细结果

### TC-001: 门面目录骨架与版本目录存在
- **结果**: PASS
- **实际行为**: `common/vm/`、`common/vm/v1/`、`common/vm/v2/`、`common/vm/shared/` 均存在；`facade.go`、`types.go`、`select_default.go`、`select_lru.go`、`select_full.go`、`select_conflict.go`、`shared/helpers.go` 均存在。
- **关键指标**: `go test ./qa/issue147 -run TestVMFacadePackageScaffoldExists` 已包含在 `go test ./qa/issue147` 中通过。
- **备注**: 目录结构符合门面化拆分目标。

### TC-002: 编译期 build tags 选择规则正确
- **结果**: PASS
- **实际行为**: 默认构建绑定 `bindV2()`；`vm_lru` 绑定 `bindV1()`；`vm_full` 绑定 `bindV2()`；双 tag 同开时编译失败，报错 `invalid array length 0 - 1`。
- **关键指标**: `go test -tags vm_lru ./common/vm/...` 通过；`go test -tags vm_full ./common/vm/...` 通过；`go test -tags "vm_lru vm_full" ./common/vm/...` 按预期失败。
- **备注**: 本轮重点复验项，结果符合预期。

### TC-003: v1/v2 导出 API 与门面绑定保持一致
- **结果**: PASS
- **实际行为**: `facade.go` 暴露 `GetMemory*`、`GetMemoryRange`、`VMStat`、`Init`、`CurrentVMType`；`qa/issue147` AST 校验通过，v1/v2 导出函数集合和签名一致。
- **关键指标**: `go test ./qa/issue147` 通过。
- **备注**: 门面可无差别绑定 v1/v2。

### TC-004: common/vm.go 收敛为兼容转发层
- **结果**: PASS
- **实际行为**: `common/vm.go` 已改为兼容层，主要负责 hooks 注入、初始化调度和 `SetVMType` 转发；旧的 ELF 解析、共享库加载、静态内存读取等主实现细节已迁出。
- **关键指标**: `go test ./qa/issue147` 通过；`./maze --build` 通过。
- **备注**: 另有 `common/vm_helpers.go` 保留若干通用辅助函数，不违反测试用例对 `common/vm.go` 的收敛要求。

### TC-005: 业务层不直接依赖 VM 版本包
- **结果**: PASS
- **实际行为**: 扫描结果显示 `maze/common/vm/v1`、`maze/common/vm/v2`、`maze/common/vm/shared` 仅在 `common/vm` 内部和 `qa/issue147` 中被引用，业务层未直接导入。
- **关键指标**: `go test ./qa/issue147` 通过；附加 grep 扫描未发现越界依赖。
- **备注**: 满足“业务层只依赖稳定门面”的目标。

### TC-006: 运行时切换兼容入口前移到门面层
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 提供 `SwitchToLRU` 与 `SwitchToFull`；`common/vm.go` 中 `SetVMType` 已转发到门面层，不再直接调用旧版 `LRU_initVirtualMemory` / `FULL_initVirtualMemory`。
- **关键指标**: `go test ./qa/issue147` 通过。
- **备注**: 运行时切换能力保留，职责位置符合设计目标。

### Smoke-001: 项目构建验证
- **结果**: PASS
- **实际行为**: `./maze --build` 成功完成。
- **关键指标**: 主程序可编译。
- **备注**: 说明门面化改造未破坏主工程构建链路。

### Smoke-002: Python 主链路 testdata 回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，输出中 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500` 等关键校验全部成功。
- **关键指标**: `pymempool_objects = 68626`，全部验证项通过。
- **备注**: 说明 VM 门面化后 Python 主链路分析结果仍正常。

### Smoke-003: C++ 主链路 testdata 回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260201-basic-malloc` 通过，识别到 `(<32) C++ A`，对象数约 `80000`，达到预期。
- **关键指标**: `amount = 80000`，`avg_size = 24`。
- **备注**: 验证 C++/ptmalloc 主链路未被本次重构破坏。

## 失败的 Test Case（如有）

无。

## Test Results

- `./maze --build`：通过
- `go test ./qa/issue147`：通过
- `go test ./common/vm/...`：通过
- `go test -tags vm_lru ./common/vm/...`：通过
- `go test -tags vm_full ./common/vm/...`：通过
- `go test -tags "vm_lru vm_full" ./common/vm/...`：按预期编译失败
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`：通过

## 未执行项

- `nodejs` 相关 testdata 未在本轮执行。
- Python 2.7 兼容性未在本轮执行。
- 不同 glibc 版本、不同 Node.js 版本兼容性未在本轮执行。
- 大 corefile 性能基准与内存占用对比未在本轮执行。

**未执行原因**:
- 本轮按“低成本检查尽量完整 + 高成本验证分阶段早停”策略执行；在静态检查、专项测试、构建、Python/C++ smoke 均通过后，已足以完成当前重构正确性复验。
- 剩余项属于扩展兼容性与性能验证，成本较高，且不属于本次已提供 test case 的核心阻塞项。

**可能影响范围**:
- 主要影响兼容性与性能相关结论，不直接影响本轮已验证的 AC1/AC4/AC5 主体实现正确性判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 日志检查

- `maze.log` 已检查，未发现与 VM 门面化重构直接相关的 panic / fatal。
- 日志中可见若干历史性或环境相关的 `GDB.Execute error` / `Cpp error` 调试输出，但对应 Python 与 C++ 用例最终均通过验证，未构成当前验收失败依据。

## 总结
- 通过: 9 / 9
- 失败: 0 / 9
- 建议: 当前已验证范围可以判定通过；如要关闭整个重构工单，后续仍建议补充 Node.js、跨版本兼容性以及性能基准回归。
