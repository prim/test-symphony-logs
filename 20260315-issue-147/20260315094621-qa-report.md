# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 retry #7 的重点问题“编译期 build tags 选择未真正隔离实现”进行了复验。静态结构检查、构建检查、门面包分标签编译检查、依赖图检查，以及至少一个 testdata 主链路动态验收均已执行；当前分支满足 `dev-flow/plan.md` 中 AC1~AC5 的要求，且未复现此前已指出的问题。

## 详细结果

### TC-001: 门面目录骨架与版本目录存在
- **结果**: PASS
- **实际行为**: `common/vm/`、`common/vm/shared/helpers.go`、`common/vm/v1`、`common/vm/v2`、选择文件均存在，目录结构与 testcase 约束一致。
- **关键指标**: `go test ./qa/issue147 -run TestVMFacadePackageScaffoldExists` 隐含通过（整组 `go test ./qa/issue147` 通过）。
- **备注**: 骨架文件中额外存在 `select_dual.go` 与 `select_dual_conflict.go`，用于显式双实现构建，不影响 AC1~AC5。

### TC-002: 编译期 build tags 选择规则正确
- **结果**: PASS
- **实际行为**: 默认构建仅依赖 `v2`；`vm_lru` 构建仅依赖 `v1`；`vm_full` 构建仅依赖 `v2`；`vm_dual` 构建同时依赖 `v1`/`v2`；`select_conflict.go` 保留 `vm_lru && vm_full` 冲突保护。
- **关键指标**:
  - `go test ./common/vm/...` 通过
  - `go test -tags vm_lru ./common/vm/...` 通过
  - `go test -tags vm_full ./common/vm/...` 通过
  - `go test -tags vm_dual ./common/vm/...` 通过
  - `go list -deps ./common/vm` 仅出现 `maze/common/vm/v2`
  - `go list -tags vm_lru -deps ./common/vm` 仅出现 `maze/common/vm/v1`
  - `go list -tags vm_full -deps ./common/vm` 仅出现 `maze/common/vm/v2`
  - `go list -tags vm_dual -deps ./common/vm` 同时出现 `maze/common/vm/v1` 与 `maze/common/vm/v2`
- **备注**: 已覆盖此前 QA / code review 反复指出的“未选中实现仍被编译”问题，本轮未复现。

### TC-003: v1/v2 导出 API 与门面绑定保持一致
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 暴露 `GetMemory*`、`GetMemoryRange`、`VMStat`、`Init`、`CurrentVMType`，`v1` 与 `v2` 导出函数集合及签名一致，门面可统一绑定。
- **关键指标**: `go test ./qa/issue147` 通过，覆盖 `TestVMFacadeExportsUniformGlobalFunctions`。
- **备注**: `facade.go` 通过注册表绑定实现，未再常驻导入具体版本包。

### TC-004: common/vm.go 收敛为兼容转发层
- **结果**: PASS
- **实际行为**: `common/vm.go` 已转为兼容入口，导入 `maze/common/vm`，负责 hooks 配置、兼容类型转发、初始化选择与回退；旧的 ELF 解析、共享库加载、静态内存读取等实现已迁出到 `common/vm/shared/helpers.go`。
- **关键指标**:
  - `go test ./qa/issue147` 通过，覆盖 `TestCommonVMBecomesThinCompatLayer`
  - `./maze --build` 通过
- **备注**: 与原 issue 目标一致，兼容层不再承载主实现细节。

### TC-005: 业务层不直接依赖 VM 版本包
- **结果**: PASS
- **实际行为**: 仓库中业务包未发现直接导入 `maze/common/vm/v1`、`maze/common/vm/v2`、`maze/common/vm/shared` 的情况；版本依赖集中在门面层与 QA 测试。
- **关键指标**: `go test ./qa/issue147` 通过，覆盖 `TestBusinessPackagesAvoidDirectVMVersionImports`。
- **备注**: 额外全文检索也未发现业务层绕过门面的直接依赖。

### TC-006: 运行时切换兼容入口前移到门面层
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 提供 `SwitchToLRU` / `SwitchToFull`；`common/vm.go` 的 `SetVMType` 已转发到门面层，并在切换前检查实现是否已编译；初始化阶段增加未编译实现的保守回退逻辑。
- **关键指标**: `go test ./qa/issue147` 通过，覆盖 `TestVMRuntimeSwitchCompatibilityMovesToFacade`。
- **备注**: 当前运行时切换能力通过 `vm_dual` 构建模式显式保留；单实现构建下不会误切换到未编译版本。

### TC-007: Python 主链路 smoke test
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 用例成功跑通，Maze 能正常分析 tarball 并输出 JSON 结果，校验脚本全部通过。
- **关键指标**:
  - 验收命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的硬性要求。

### TC-008: C++ 主链路回归 testdata
- **结果**: PASS
- **实际行为**: `cpp/20260201-basic-malloc` 用例成功跑通，C++ 对象统计与预期一致，基础 malloc 主链路未受 VM 门面化重构影响。
- **关键指标**:
  - 验收命令: `python3 testdata/run_test.py cpp/20260201-basic-malloc`
  - `(<32) C++ A: amount=80000, avg_size=24`
  - `Total chunks counted: 80003`
- **备注**: 验证了非 Python 路径的基础回归。

## 失败的 Test Case（如有）

无。

## 未执行项

- Node.js testdata 主链路回归未在本轮执行。
  - **原因**: 本轮重点是 retry #7 已知问题复验，优先验证 build tags 隔离、门面绑定与最关键 smoke test；在主链路 smoke 已通过后，未继续做更高成本扩展回归。
  - **可能影响 AC**: 主要影响 AC1/AC5 在 Node.js 使用场景下的额外回归信心，但不影响当前对结构拆分与编译隔离的验收结论。
- Python 2.7 / 不同 glibc / 不同 Node.js 版本兼容性未执行。
  - **原因**: 当前环境单一，未提供多版本矩阵。
  - **可能影响 AC**: 属于扩展兼容性覆盖，不直接阻断本次 issue 的结构性验收。
- 性能基准与大 corefile 场景未执行。
  - **原因**: 本轮无现成 >1GB / >20GB 性能数据集，且前序目标是验证此前失败点是否修复。
  - **可能影响 AC**: 影响对 LRU/FULL 模式性能收益与回退策略的量化结论，但不影响本次代码结构和构建隔离验收。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：通过
- `go test ./qa/issue147`：通过
- `go test ./common/vm/...`：通过
- `go test -tags vm_lru ./common/vm/...`：通过
- `go test -tags vm_full ./common/vm/...`：通过
- `go test -tags vm_dual ./common/vm/...`：通过
- `go list -deps ./common/vm`：默认仅依赖 `v2`
- `go list -tags vm_lru -deps ./common/vm`：仅依赖 `v1`
- `go list -tags vm_full -deps ./common/vm`：仅依赖 `v2`
- `go list -tags vm_dual -deps ./common/vm`：同时依赖 `v1` / `v2`
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`：通过

## Findings
- 本轮 retry #7 关注的核心问题“编译期 build tags 选择未真正隔离实现”已修复，默认 / `vm_lru` / `vm_full` / `vm_dual` 的依赖图与实现选择符合预期。
- `common/vm.go` 已收敛为兼容层，旧实现细节已迁移到 `common/vm/shared` 与 `common/vm/v1|v2`，满足 issue 预期的 facade + 版本实现结构。
- 运行时切换能力仍保留，但被限制在显式双实现构建或已编译实现集合内，降低了切换到未编译实现的风险。

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本次 issue 的 QA 验收；后续如要提升发布信心，建议补充 Node.js 回归、多版本兼容性与大 corefile 性能验证。
