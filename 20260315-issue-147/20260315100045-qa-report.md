# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 prim/maze#147 的第 10 次 QA 重试，重点复核此前发现的 VM 生命周期与资源释放问题。静态结构检查、门面化约束测试、VM 包级回归、`vm_dual` 构建模式回归，以及至少一个真实 `testdata` 主链路用例均已通过；未发现新的阻断性问题。

## 详细结果

### TC-001: 门面目录骨架与版本目录存在
- **结果**: PASS
- **实际行为**: `common/vm/`、`common/vm/shared/helpers.go`、`common/vm/v1`、`common/vm/v2` 等门面化骨架均存在，目录结构与测试用例约束一致。
- **关键指标**: `go test ./qa/issue147 -run TestVMFacadePackageScaffoldExists` 已包含在 `go test ./qa/issue147` 中并通过。
- **备注**: 额外确认存在 `select_dual.go` 与 `select_dual_conflict.go`，用于显式双实现构建模式。

### TC-002: 编译期 build tags 选择规则正确
- **结果**: PASS
- **实际行为**: 默认构建绑定 `v2`，`vm_lru` 绑定 `v1`，`vm_full` 绑定 `v2`，双 tag 冲突保护文件存在；同时新增 `vm_dual` 显式双实现模式。
- **关键指标**:
  - `go list -deps ./common/vm | rg "maze/common/vm/(v1|v2)"` → 仅 `v2`
  - `go list -tags vm_lru -deps ./common/vm | rg "maze/common/vm/(v1|v2)"` → 仅 `v1`
  - `go list -tags vm_full -deps ./common/vm | rg "maze/common/vm/(v1|v2)"` → 仅 `v2`
  - `go list -tags vm_dual -deps ./common/vm | rg "maze/common/vm/(v1|v2)"` → 同时包含 `v1`、`v2`
- **备注**: `vm_dual` 不在最初 testcase 文档中，但与“保留运行时切换能力”目标一致。

### TC-003: v1/v2 导出 API 与门面绑定保持一致
- **结果**: PASS
- **实际行为**: `facade.go` 暴露统一函数入口，`v1` 与 `v2` 导出函数集合及签名保持一致，门面可无差别绑定。
- **关键指标**: `go test ./qa/issue147` 通过；人工复核 `common/vm/v1/vm.go` 与 `common/vm/v2/vm.go`，均导出 `Init/Close/GetMemory/GetMemorySafe/GetMemory4/GetMemory4Safe/GetMemoryRange/VMStat`。
- **备注**: 本轮新增 `Close` 生命周期入口，补齐此前资源释放缺口。

### TC-004: common/vm.go 收敛为兼容转发层
- **结果**: PASS
- **实际行为**: `common/vm.go` 已不再承载 ELF 解析、共享库加载、静态内存读取等主实现；这些逻辑已迁移到 `common/vm/shared/helpers.go`，兼容辅助函数则拆分到 `common/vm_helpers.go`。
- **关键指标**: `go test ./qa/issue147` 通过；`git diff origin/master...HEAD` 显示 `common/vm.go` 大幅收敛，`common/vm_lru.go` / `common/vm_full.go` 已删除。
- **备注**: `configureVMFacade()` 负责向门面注入 hooks，`initVirtualMemory()` 负责选择实现并调用门面初始化。

### TC-005: 业务层不直接依赖 VM 版本包
- **结果**: PASS
- **实际行为**: 业务层未发现直接导入 `maze/common/vm/v1`、`maze/common/vm/v2`、`maze/common/vm/shared` 的情况。
- **关键指标**: `go test ./qa/issue147` 通过。
- **备注**: 当前版本实现依赖被限制在 `common/vm` 门面内部与 QA 测试中。

### TC-006: 运行时切换兼容入口前移到门面层
- **结果**: PASS
- **实际行为**: `SetVMType()` 已转发到 `vm.SwitchToLRU()` / `vm.SwitchToFull()`；门面新增 `CloseCurrent()` 与切换前关闭旧实现的逻辑，修复了此前 LRU shard fd 与 FULL mmap 资源泄漏问题。
- **关键指标**:
  - `go test ./common/vm/...`：PASS
  - `go test -tags vm_dual ./common/vm/...`：PASS
  - `common/vm/facade_lifecycle_test.go`、`common/vm/implementation_lifecycle_test.go` 已覆盖“切换前关闭旧实现”“重复 Init 释放旧资源”两类回归场景。
- **备注**: 运行时切换能力当前依赖 `vm_dual` 构建模式；单实现构建只保留已编译实现，符合当前设计说明。

### Smoke Test: Python 主链路回归
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 成功完成 Maze 分析与 `validate.py` 校验，典型 Python 对象统计结果正常。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行 Node.js、C++、txos 等更多动态主链路回归；原因是本轮按“低成本检查尽量完整 + 高成本验证分阶段早停”策略，关键静态检查与一个代表性 smoke test 已通过，且当前无阻断迹象。
- 未执行 Python 2.7 / 不同 glibc / 不同 Node.js 版本的兼容性矩阵；原因是当前环境单一，且需求文件与 testcase 文档未提供对应强制验收命令。
- 未执行大 corefile 性能对比与内存占用量化验证；原因是当前轮次聚焦此前已知资源释放问题复核，且仓库内未附带对应 >1GB 标准性能数据集与基线脚本。
- **可能影响范围**: 主要影响扩展兼容性与性能结论，不影响本轮已执行的 TC-001 ~ TC-006 结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 未发现此前 QA / code review 指出的 VM 生命周期资源泄漏问题回归；新增 `Close` 生命周期与双实现切换测试已有效覆盖该风险点。
- `common/vm` 门面化目标已落地，`common/vm.go` 已收敛为兼容层，业务层仍保持对稳定门面的依赖。
- 当前运行时切换明确依赖 `vm_dual` 构建模式，这一行为与设计文档一致，建议后续文档继续明确该约束，避免误用单实现构建产物进行运行时切换预期验证。

## Test Results
- `./maze --build`：PASS
- `go test ./qa/issue147`：PASS
- `go test ./common/vm/...`：PASS
- `go test -tags vm_dual ./common/vm/...`：PASS
- `go list -deps ./common/vm` / `-tags vm_lru` / `-tags vm_full` / `-tags vm_dual`：依赖图符合预期
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: **可以合并当前修复**。若要关闭更大范围的回归与性能风险，建议后续补充 Node.js/C++ 动态回归以及大 corefile 性能基准，但这些未构成本轮 issue 147 门面化与资源泄漏修复的阻断项。
