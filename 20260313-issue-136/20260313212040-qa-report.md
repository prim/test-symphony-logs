# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: Context 重复结果字段删除约束
- **结果**: PASS
- **实际行为**: 检查 `prepare/context.go`，`GDBManager`、`SymbolsManager`、`CppAnalyzer`、`PythonLangManager`、`TcmallocManager`、`JemallocManager`、`MimallocManager`、`NodejsLangManager`、`AsiocoreManager`、`MosdManager`、`TxosManager` 中，本轮计划要求收敛到 `common.S` / `common.Cpp` 的镜像结果字段均已删除。
- **关键指标**: `go test ./qa/issue136 -run TestContextStructRemovesConvergedResultFields` 隐含通过（整组 `go test ./qa/issue136` 通过）。
- **备注**: `prepare/context.go` 中仅保留运行态字段与少量暂不收敛字段，符合 AC1、AC2。

### TC-002: FillCommonVars 去同步化约束
- **结果**: PASS
- **实际行为**: `prepare/fill.go` 已删除 `ctx.* -> S/Cpp` 的等价字段同步赋值，只保留 `Process/Corefile/Pthread/VMS` 等非等价转换及 `S.Mosd.PyGameObjectType3 = ctx.GameObjType3`。
- **关键指标**: `go test ./qa/issue136 -run TestFillCommonVarsNoLongerCopiesConvergedFields` 隐含通过（整组 `go test ./qa/issue136` 通过）。
- **备注**: 避免了此前 map/slice 浅赋值同步点风险，符合 AC3、AC7。

### TC-003: 已删除 Context 字段无残留引用
- **结果**: PASS
- **实际行为**: 额外使用脚本扫描仓库 `.go` 文件（排除 `testdata/`、`.git/`、`tmp/`、`qa/issue136/`），未发现对本轮已删除 `ctx.GDB.Sizeof`、`ctx.Symbols.Symbols`、`ctx.Cpp.*` 等结果字段的残留引用；下游读取均已改为 `S` / `Cpp`。
- **关键指标**: `go test ./qa/issue136` 输出 `ok   maze/qa/issue136`。
- **备注**: 满足 AC4、AC6。

### TC-004: C++ 快照导出单一来源约束
- **结果**: PASS
- **实际行为**: `prepare/prepare.go` 中 `buildLegacyCppSnapshot()` 已移除 `currentPrepareContext.GDB.ToJson*` 回退组装逻辑，直接 `return structToMap(Cpp)`。
- **关键指标**: 静态检查通过；`go test ./qa/issue136 -run TestCppSnapshotExportUsesCommonCppInsteadOfGdbMirror` 隐含通过（整组 `go test ./qa/issue136` 通过）。
- **备注**: `cpp.json` 导出来源已统一，符合 AC5、AC6。

### TC-005: 构建回归检查
- **结果**: PASS
- **实际行为**: 执行 `./maze --build` 成功返回，无编译错误、无明显 fatal。
- **关键指标**: 构建命令退出码为 0。
- **备注**: 说明字段收敛后未引入编译层残留引用。

### TC-006: 动态 smoke test（新建 testdata）
- **结果**: PASS
- **实际行为**: 由于仓库 `testdata` 子模块提交不可获取，无法使用官方样例；我按 QA 约束自行创建了最小 Python smoke 用例 `testdata/python/20260313-qa-smoke-prepare-single-source/`，并通过统一入口 `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source` 实际运行。Maze 主链路完成 prepare、分析、导出，`maze-result.json` 生成成功，验证脚本通过。
- **关键指标**: 
  - 命令：`python3 cmd/maze-gen-coredump.py -o testdata/python/20260313-qa-smoke-prepare-single-source/ "python3 testdata/python/20260313-qa-smoke-prepare-single-source/qa_smoke_prepare_single_source.py"`
  - 命令：`python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source`
  - 结果：输出 `Running Maze Analysis` 与 `✅ Test PASSED`
  - 观测：`maze-result.json` 中存在 `items`，且 `Payload` 类型条目被识别为 512 个对象
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强约束，也证明本次重构未破坏主分析链路。

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- 官方 `testdata` 子模块中的既有用例未执行。
- **原因**: `git submodule update --init --recursive testdata` 失败，错误为“子模块引用提交 `35c7e708ce9a4a99e8b150a88adb07a6091d6019` 不存在，直接获取失败”。当前工作树中的 `testdata` 目录初始为空，无法直接运行计划外的历史官方样例。
- **影响范围**: 不影响本工单 AC1-AC6 的静态结构验收结论，但会减少基于历史 coredump 样本的广覆盖回归信心，尤其影响跨语言/跨分配器的额外兼容性验证范围。
- **缓解措施**: 已自行创建并执行 1 个新的 Python smoke testdata 用例，完成最低动态验收要求。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 就本工单“prepare 重复结果字段收敛到 `common.S` / `common.Cpp`”的需求范围来看，可以通过验收；建议后续在修复 `testdata` 子模块引用后，再补跑官方历史样例做更广覆盖回归。
