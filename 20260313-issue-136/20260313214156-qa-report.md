# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #136 的“prepare 结果字段收敛到 `common.S` / `common.Cpp`”进行了静态核对、关键 smoke 与至少一个 `testdata` 动态用例验收，未发现新的阻塞问题。

## 详细结果

### TC-001: Context 重复结果字段删除约束
- **结果**: PASS
- **实际行为**: `prepare/context.go` 中 `SymbolsManager`、`GDBManager`、`CppAnalyzer`、`PythonLangManager`、`TcmallocManager`、`JemallocManager`、`MimallocManager`、`NodejsLangManager`、`AsiocoreManager`、`MosdManager`、`TxosManager` 已删除计划内重复结果字段，仅保留运行态字段。
- **关键指标**: `go test ./qa/issue136 -run TestContextStructRemovesConvergedResultFields` 已包含该约束并通过。
- **备注**: 与 AC1、AC2 一致。

### TC-002: FillCommonVars 去同步化约束
- **结果**: PASS
- **实际行为**: `prepare/fill.go` 已不再复制本轮收敛字段，仅保留非等价字段转换，例如 `S.Mosd.PyGameObjectType3 = ctx.GameObjType3` 以及进程/统计等非等价映射。
- **关键指标**: `go test ./qa/issue136 -run TestFillCommonVarsNoLongerCopiesConvergedFields` 已通过。
- **备注**: 满足 AC3、AC7。

### TC-003: 已删除 Context 字段无残留引用
- **结果**: PASS
- **实际行为**: 静态核对显示 prepare 相关写入点已直接改写到 `S` / `Cpp`；仓库内未发现计划内已删除字段的业务残留引用，检索命中仅来自 `qa/issue136/prepare_single_source_fields_test.go` 的断言字符串。
- **关键指标**: `go test ./qa/issue136 -run TestPreparePackageHasNoResidualReferencesToRemovedContextResultFields` 已通过。
- **备注**: 满足 AC4、AC6。

### TC-004: C++ 快照导出单一来源约束
- **结果**: PASS
- **实际行为**: `prepare/prepare.go` 中 `buildLegacyCppSnapshot()` 在 snapshot-only 兼容路径之外，直接 `return structToMap(Cpp)`；已移除通过 `currentPrepareContext.GDB.ToJson*` 组装 `cpp.json` 的旧回退路径。
- **关键指标**: `go test ./qa/issue136 -run TestCppSnapshotExportUsesCommonCppInsteadOfGdbMirror` 已通过。
- **备注**: 满足 AC5、AC6。

### TC-005: 构建 smoke
- **结果**: PASS
- **实际行为**: 执行 `./maze --build` 成功完成，未出现编译错误。
- **关键指标**: 构建命令退出成功。
- **备注**: 说明本轮字段删除与写入迁移未破坏主构建链路。

### TC-006: Python prepare 单一数据源动态冒烟
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source` 成功，Maze 完成 prepare、分析与结果导出，最终输出 `✅ Test PASSED`。
- **关键指标**: `prepare.Run took 5.346201332s`；生成 `maze-result.json`、`maze-result.txt`、`maze.log`；Top Objects 与内存汇总输出正常。
- **备注**: 证明至少在 Python 3.11 smoke 场景下，字段收敛后主链路仍然成立。

## 失败的 Test Case（如有）

无。

## 已执行的检查项

- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告。
- 审查 `git diff origin/master...HEAD`，重点核对 `prepare/context.go`、`prepare/fill.go`、`prepare/prepare.go`、`prepare/alloc.go`、`prepare/allocator_runtime_metadata.go`、`prepare/asiocore.go`、`prepare/messiah.go`、`prepare/mosd.go`、`prepare/lang_python.go`、`prepare/lang_nodejs.go`、`prepare/gdb.go`、`prepare/cpp_analyzer.go`。
- 静态检索已删除 `Context` 镜像字段的残留引用。
- 执行 `./maze --build`。
- 执行 `go test ./qa/issue136`。
- 执行 `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source`。

## 未执行项（如有）

- 未执行更多真实 tcmalloc / mimalloc / asiocore / mosd 专项样本回归。
- 原因: 本轮关键 smoke、静态约束测试与至少一个 `testdata` 动态用例均已通过，未出现阻塞；但这些专项样本不在当前 test case 报告的必跑范围内，属于扩展回归。
- 可能影响的 AC: 若这些专项运行时存在仅在真实二进制中触发的版本/修订号解析差异，可能影响 AC2、AC5、AC6 的更广覆盖面，但当前无失败证据。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings

- 未发现新的功能性回归或明显范围遗漏。
- `resetCommonPrepareResults()` 已在 `InitContext()` 中统一重置 `S` / `Cpp`，避免多次 prepare 时遗留历史镜像数据。
- 新增的运行时元数据补齐路径已覆盖此前 dev report 中提到的遗漏字段：`S.Tcmalloc.Code`、`S.Tcmalloc.ClassToSize`、`S.Mimalloc.{Miversion,Version,Sversion}`、`S.Asiocore.Revision`、`S.Mosd.Revision`。

## Test Results

- `./maze --build`: PASS
- `go test ./qa/issue136`: PASS
- `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source`: PASS

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以继续推进；当前 QA 视角下满足本工单验收标准。后续若要提高置信度，建议补做真实 tcmalloc / mimalloc / asiocore / mosd 样本的扩展回归，但这不构成本轮阻塞。
