# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 `prim/maze#136` 的“prepare 重复结果字段收敛到 `common.S` / `common.Cpp`”进行了静态核对与动态 smoke 验证，未发现阻断发布的问题。

## 详细结果

### TC-001: Context 重复结果字段删除约束
- **结果**: PASS
- **实际行为**: `prepare/context.go` 中 `GDBManager`、`SymbolsManager`、`CppAnalyzer`、`PythonLangManager`、`TcmallocManager`、`JemallocManager`、`MimallocManager`、`NodejsLangManager`、`AsiocoreManager`、`MosdManager`、`TxosManager` 已删除计划内的重复结果字段，仅保留流程态/控制态字段。
- **关键指标**: `go test ./qa/issue136 -run TestContextStructRemovesConvergedResultFields` 通过。
- **备注**: 与 AC1、AC2 一致。

### TC-002: FillCommonVars 去同步化约束
- **结果**: PASS
- **实际行为**: `prepare/fill.go` 已移除本轮收敛字段的同步赋值逻辑，仅保留非等价字段转换（如 `ctx.GameObjType3 -> S.Mosd.PyGameObjectType3`）及通用结果默认值填充。
- **关键指标**: `go test ./qa/issue136 -run TestFillCommonVarsNoLongerCopiesConvergedFields` 通过。
- **备注**: 与 AC3、AC7 一致。

### TC-003: 已删除 Context 字段无残留引用
- **结果**: PASS
- **实际行为**: 仓库中未发现对本轮已删除 `ctx.*` 结果字段的残留读取/写入；关键采集路径改为直接写入 `S` / `Cpp`，例如 `prepare/gdb.go`、`prepare/lang_python.go`、`prepare/lang_nodejs.go`、`prepare/alloc.go`、`prepare/asiocore.go`、`prepare/mosd.go`、`prepare/txos.go`。
- **关键指标**: `go test ./qa/issue136 -run TestPreparePackageHasNoResidualReferencesToRemovedContextResultFields` 通过。
- **备注**: 与 AC4、AC6 一致。

### TC-004: C++ 快照导出单一来源约束
- **结果**: PASS
- **实际行为**: `prepare/prepare.go` 中 `buildLegacyCppSnapshot()` 已不再回退使用 `currentPrepareContext.GDB.ToJson*` 镜像结果，直接 `return structToMap(Cpp)`。
- **关键指标**: `go test ./qa/issue136 -run TestCppSnapshotExportUsesCommonCppInsteadOfGdbMirror` 通过。
- **备注**: 与 AC5、AC6 一致。

### TC-005: Python smoke 动态验收
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source`，Maze 完成 prepare、Cpp merge、snapshot 导出与结果生成，`maze-result.json` 产出正常，验证脚本通过。
- **关键指标**:
  - `prepare.Run took 5.463878486s`
  - `PyMemPool objects: 11158`
  - `Malloc objects: 130`
  - `CountGoroutineError: 0`
  - 测试框架输出 `✅ Test PASSED`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，也对 `storage.json` / 结果导出链路提供了主链路 smoke 证明。

## 失败的 Test Case（如有）

无。

## 已执行的低成本检查

- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告。
- 审查 `origin/master...HEAD` 关键改动，确认收敛范围已落到实际代码。
- 核对以下关键写入路径已直接落到 `S` / `Cpp`：
  - `prepare/gdb.go` 写入 `S.Sizeof` / `S.Structs`
  - `prepare/symbols.go`、`prepare/alloc.go`、`prepare/txos.go` 写入 `S.Symbols`
  - `prepare/lang_python.go` 写入 `S.Py.*`
  - `prepare/lang_nodejs.go` 写入 `S.Nodejs.*`
  - `prepare/alloc.go`、`prepare/allocator_runtime_metadata.go` 写入 `S.Tcmalloc.*` / `S.Jemalloc.*` / `S.Mimalloc.*`
  - `prepare/asiocore.go`、`prepare/messiah.go`、`prepare/mosd.go` 写入 `S.Asiocore.*` / `S.Mosd.*`
  - `prepare/cpp_analyzer.go`、`prepare/args.go`、`prepare/prepare.go` 使用 `Cpp.*`
- 核对 `prepare/context.go` 已删除计划内重复字段，`prepare/fill.go` 已缩减为非等价字段转换层。

## 未执行项（如有）

- 未进一步执行 Python 2.7、更多 glibc 版本、更多 Node.js 版本及大内存性能用例。
- 原因：本工单核心为结果字段收敛与导出链路切换；当前静态检查和关键 smoke 已覆盖主链路，且未发现阻断迹象，本轮按“低成本检查尽量完整 + 动态验证分阶段执行”策略停止在 smoke 阶段。
- 可能影响范围：兼容性与性能类扩展结论未在本轮新增动态证据，但不影响对本工单 AC1-AC7 的主结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 未发现违反需求文档 AC1-AC7 的问题。
- 当前实现满足“Context 删除镜像字段、采集逻辑直接写入 `S` / `Cpp`、`FillCommonVars()` 去同步化、`cpp.json` 从 `Cpp` 导出”的核心要求。
- 已通过 1 个真实 `testdata` smoke 用例和 4 个静态结构约束测试，功能主链路可用。

## Test Results
- 静态测试：`go test ./qa/issue136 -v` 全部通过（4/4）
- 动态测试：`python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source` 通过（1/1）

## 总结
- 通过: 5 / 5
- 失败: 0 / 5
- 建议: 可以通过本工单验收；若后续希望补强非功能性证据，可追加多版本兼容与性能回归，但不构成本轮发布阻塞。
