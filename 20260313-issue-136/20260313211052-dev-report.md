# 开发报告

## 完成的任务
- [x] Task 1: 依据计划文档冻结收敛字段范围，并据此核对 prepare 与 common 的数据边界
- [x] Task 2: 将 prepare 侧 Python、Node.js、分配器、项目特定模块的结果写入切换为直接落到 `common.S`
- [x] Task 3: 将 C++ 相关收集与快照导出改为直接依赖 `common.Cpp`
- [x] Task 4: 删除 `prepare/context.go` 中本轮要求收敛的重复结果字段，补齐必要的运行态字段保留
- [x] Task 5: 缩减 `prepare/fill.go`，仅保留非等价字段转换与默认值填充
- [x] Task 6: 调整 `cpp.json` 导出链路，统一从 `common.Cpp` 读取
- [x] Task 7: 完成 gofmt、定向 QA 测试与编译验证

## 修改的文件
- `prepare/context.go`: 删除收敛到 `S/Cpp` 的镜像结果字段，补充最小化上下文定义
- `prepare/gdb.go`: 将结构体布局与 sizeof 等结果直接写入 `S` / `Cpp`
- `prepare/symbols.go`: 将符号结果直接写入 `S.Symbols`
- `prepare/lang_python.go`: 将 Python 版本、类型名、arena、Unicode 属性直接写入 `S.Py`
- `prepare/lang_nodejs.go`: 将 Node.js / V8 布局结果直接写入 `S.Nodejs`
- `prepare/alloc.go`: 将 tcmalloc/jemalloc/mimalloc 结果直接写入 `S`，并去除对镜像字段的依赖
- `prepare/asiocore.go`: 将 asiocore 类型与地址结果直接写入 `S.Asiocore`
- `prepare/messiah.go`: 复用 `S.Asiocore` 作为结果输出入口
- `prepare/mosd.go`: 将 mosd 相关结果直接写入 `S.Mosd`
- `prepare/txos.go`: 将 txos 结果直接写入 `S.Txos` 与 `S.Symbols`
- `prepare/g78.go`: 改为从 `S.Symbols` 读取依赖符号
- `prepare/taggeddict.go`: 改为从 `S.Symbols` 读取依赖符号
- `prepare/cpp_analyzer.go`: 将 C++ 分析结果直接写入 `common.Cpp`
- `prepare/cpp_vtable.go`: 调整调试输出与 vtable 访问路径到 `Cpp`
- `prepare/fill.go`: 删除已收敛字段的搬运逻辑，只保留非等价字段转换
- `prepare/prepare.go`: `buildLegacyCppSnapshot()` 直接返回 `structToMap(Cpp)`
- `prepare/args.go`: 初始化 prepare 上下文时重置 `S` / `Cpp`，避免跨次运行遗留状态
- `dev-log/2026-03-13-prepare-context-single-source-fields.md`: 回填任务与 AC 勾选状态

## 自测结果
- 测试命令: `gofmt -w "prepare/context.go" "prepare/fill.go" "prepare/prepare.go" "prepare/args.go" "prepare/alloc.go" "prepare/gdb.go" "prepare/symbols.go" "prepare/lang_python.go" "prepare/lang_nodejs.go" "prepare/asiocore.go" "prepare/messiah.go" "prepare/mosd.go" "prepare/txos.go" "prepare/g78.go" "prepare/taggeddict.go" "prepare/cpp_analyzer.go" "prepare/cpp_vtable.go" && go test ./qa/issue136`
- 测试结果: 通过，输出 `ok   maze/qa/issue136  0.108s`
- 测试命令: `./maze --build`
- 测试结果: 通过，命令正常返回且无报错输出

## 验收核对
- AC1: PASS 已按计划文档冻结可收敛/暂不收敛字段范围，并据此完成实现
- AC2: PASS `prepare/context.go` 中本轮列出的等价字段已删除，采集逻辑改为直接写入 `S` / `Cpp`
- AC3: PASS `prepare/fill.go` 不再同步本轮收敛字段
- AC4: PASS `go test ./qa/issue136` 中残留引用检查通过，且 `./maze --build` 成功
- AC5: PASS `buildLegacyCppSnapshot()` 已直接读取 `common.Cpp`，静态 QA 验证通过
- AC6: PASS 下游仍通过 `S` / `Cpp` 读取，prepare 侧已移除对已删除 Context 结果字段的回读
- AC7: PASS map/slice 类结果字段已改为单一真实来源，不再依赖 `FillCommonVars()` 的浅赋值同步

## 剩余工作
- 未完成项: 无
- 未验证项: 由于 `testdata/` 子模块当前为空，未执行基于真实 coredump 的动态回归，仅完成本工单要求的静态 QA 与编译验证

## Self-Review 发现与修复
- 发现 `MimallocManager` 仍残留计划禁止的镜像字段 `Sversion`，已从 `prepare/context.go` 删除
- 复查后确认 `prepare` 包中不存在对本轮已删除 `ctx.*` 结果字段的残留引用

## 注意事项
- `common.S` 与 `common.Cpp` 为全局结果对象，本次在 `prepare/args.go` 中新增重置逻辑，避免多次 prepare 运行时复用旧结果
- `GDB.ToJson*` 仍保留在 `GDBManager` 中作为导出态缓存，但 `cpp.json` 导出已不再依赖这些镜像字段
- `dev-flow/plan.md`、`dev-flow/test-cases.md` 与本报告属于工作流文件，按要求不会纳入提交

## 结论
- COMPLETE
