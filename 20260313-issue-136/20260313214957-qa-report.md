# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮 QA retry #8 针对 issue #136 "prepare 结果字段收敛到 `common.S` / `common.Cpp`" 进行了完整验收，包括静态结构检查、单元测试和动态功能测试。所有验收标准均已满足，未发现新问题。

## 详细结果

### TC-001: Context 重复结果字段删除约束
- **结果**: PASS
- **实际行为**: `prepare/context.go` 中所有 manager 结构体已删除计划内的重复结果字段，仅保留运行态字段（如 `RefreshStructs`、`IsStaticLink`、`done` 等）
- **关键指标**: 单元测试 `TestContextStructRemovesConvergedResultFields` 通过
- **备注**: 满足 AC1

### TC-002: FillCommonVars 去同步化约束
- **结果**: PASS
- **实际行为**: `prepare/fill.go` 已不再包含本轮收敛字段的同步赋值语句，仅保留非等价字段转换逻辑
- **关键指标**: 单元测试 `TestFillCommonVarsNoLongerCopiesConvergedFields` 通过；手工检索 `ctx.GDB.Sizeof`、`ctx.PythonLang.Version`、`ctx.Tcmalloc` 等均无匹配
- **备注**: 满足 AC3

### TC-003: 已删除 Context 字段无残留引用
- **结果**: PASS
- **实际行为**: 全仓库扫描未发现对已删除 Context 结果字段的残留引用；检索 `ctx.Cpp.Vtables`、`ctx.NodejsLang.V8Constants` 等均无业务代码匹配
- **关键指标**: 单元测试 `TestPreparePackageHasNoResidualReferencesToRemovedContextResultFields` 通过
- **备注**: 满足 AC4

### TC-004: C++ 快照导出单一来源约束
- **结果**: PASS
- **实际行为**: `buildLegacyCppSnapshot()` 直接返回 `structToMap(Cpp)`，不再依赖 `GDB.ToJson*` 镜像字段
- **关键指标**: 单元测试 `TestCppSnapshotExportUsesCommonCppInsteadOfGdbMirror` 通过
- **备注**: 满足 AC5

### TC-005: 采集逻辑直接写入 S / Cpp
- **结果**: PASS
- **实际行为**: 
  - GDB: 直接写入 `S.Sizeof`、`S.Structs`、`S.Symbols`
  - Python: 直接写入 `S.Py.Version`、`S.Py.IVersion`
  - C++: 直接写入 `Cpp.Vtables`、`Cpp.AllTypeName` 等
  - Allocators: 通过新增的 `allocator_runtime_metadata.go` 直接写入 `S.Tcmalloc.*`、`S.Mimalloc.*`
  - Asiocore/Mosd: 通过新增的 `binary_metadata.go` 直接写入 `S.Asiocore.Revision`、`S.Mosd.Revision`
- **关键指标**: 代码审查确认所有采集点已迁移
- **备注**: 满足 AC2

### TC-006: 构建与功能 smoke 测试
- **结果**: PASS
- **实际行为**: 
  - 编译: `./maze --build` 成功
  - 单元测试: `go test ./qa/issue136` 全部通过 (4/4)
  - 动态测试: `python3 testdata/run_test.py python/20260313-qa-smoke-prepare-single-source` 通过
- **关键指标**: 
  - prepare.Run 耗时 5.35s
  - 成功生成 maze-result.json、maze-result.txt
  - 内存分析结果正常（11288 objects, 2.5M total）
- **备注**: 满足 AC6，证明字段收敛后主链路功能正常

## 失败的 Test Case

无。

## 已执行的检查项

**阶段 A: 低成本静态检查（已完整执行）**
- ✅ 读取需求文档、测试用例、开发报告
- ✅ 审查代码变更 (21 files changed, 614 insertions, 321 deletions)
- ✅ 检查 `prepare/context.go` 结构定义
- ✅ 检查 `prepare/fill.go` 同步逻辑
- ✅ 检查采集逻辑写入路径
- ✅ 检查 snapshot 导出实现
- ✅ 检查残留引用

**阶段 B: 关键 smoke test（已完整执行）**
- ✅ 编译测试: `./maze --build`
- ✅ 单元测试: `go test ./qa/issue136`
- ✅ 动态测试: Python 3.11 smoke test

**阶段 C: 扩展回归（未执行）**
- ⏭️ 真实 tcmalloc 样本测试
- ⏭️ 真实 jemalloc 样本测试
- ⏭️ 真实 mimalloc 样本测试
- ⏭️ 真实 asiocore 样本测试
- ⏭️ 真实 mosd 样本测试
- ⏭️ Node.js 多版本测试

## 未执行项

- **未执行项**: 真实 allocator 和业务框架专项样本的扩展回归测试
- **未执行原因**: 关键 smoke 和静态约束测试已全部通过，未出现阻塞；扩展回归不在本轮必跑范围内
- **可能影响的 AC**: 若存在仅在特定二进制中触发的边界情况，可能影响 AC2、AC5、AC6 的更广覆盖面，但当前无失败证据

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.11 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [x] Smoke 测试 prepare 耗时 5.35s（正常范围）
- [x] 内存分析功能正常

## 验收标准核对

| 验收标准 | 状态 | 证据 |
|---------|------|------|
| AC1: `prepare/context.go` 不再定义重复结果字段 | ✅ PASS | TC-001 通过；手工检查确认仅保留运行态字段 |
| AC2: `prepare` 采集逻辑直接写入 `S` / `Cpp` | ✅ PASS | TC-005 通过；代码审查确认所有写入点已迁移 |
| AC3: `prepare/fill.go` 不再同步这些字段 | ✅ PASS | TC-002 通过；手工检索无匹配 |
| AC4: 全仓库无残留引用 | ✅ PASS | TC-003 通过；手工检索无业务代码匹配 |
| AC5: `storage.json` 与 `cpp.json` 导出兼容 | ✅ PASS | TC-004 通过；`buildLegacyCppSnapshot()` 使用 `Cpp` |
| AC6: 下游分析模块继续依赖 `S` / `Cpp` | ✅ PASS | TC-006 通过；动态测试证明主链路正常 |

## Findings

- ✅ 所有计划内字段已成功收敛到 `common.S` / `common.Cpp`
- ✅ 新增 `allocator_runtime_metadata.go` 和 `binary_metadata.go` 提供运行时元数据收集
- ✅ `resetCommonPrepareResults()` 在 `InitContext()` 中统一重置 `S` / `Cpp`
- ✅ 字段收敛后主链路功能正常，无回归

## Test Results

```
编译: ./maze --build                                          ✅ PASS
单元: go test ./qa/issue136                                   ✅ PASS (4/4)
动态: python3 testdata/run_test.py python/20260313-...       ✅ PASS
```

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: **可以发布**。所有验收标准均已满足，字段收敛重构已完成且功能正常。
