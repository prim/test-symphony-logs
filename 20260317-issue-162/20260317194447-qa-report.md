# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮完成了计划文档、test case、最新开发报告与代码变更的静态核对，并执行了构建、issue 定向静态测试以及至少一个真实 `testdata` 用例。当前实现已基本满足 AC1～AC4，但 AC5 要求的 `refs=v0` / `refs=v1` CPU profile 回归未执行，无法验证“facade wrapper 成本进一步下降”这一性能验收项，因此本轮总体结果判定为 FAIL。

## 详细结果

### TC-001: facade 删除运行时 implementation 状态
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已删除旧的 `implementationBinding`、`boundImplementation`、`currentVMType`、`initialized`、`currentImplementation()` 等 facade 运行时绑定状态；热路径改为通过 `implGetMemory*` 直接转发。
- **关键指标**: `go test ./qa/issue162 -run TestFacadeRemovesRuntimeImplementationState` 通过（本次执行为 `go test ./qa/issue162 -run Test`，包含该用例）。
- **备注**: 相关兼容绑定状态仅保留在 `common/vm/test_compat_test.go` 的测试构件中，未污染正式构建路径。

### TC-002: GetMemory 热路径去除 runtime dispatch
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中 `GetMemory*` / `VMStat` 已改为薄 wrapper，直接调用 `implGetMemory*` / `implVMStat`，未发现 `currentImplementation()`、`dispatchGetMemory*()`、nil 检查式 runtime dispatch。
- **关键指标**: `go test ./qa/issue162 -run TestGetMemoryFacadeStopsRuntimeDispatch` 通过（包含于 `go test ./qa/issue162 -run Test`）。
- **备注**: `select_*.go` 以编译期单实现直绑方式初始化底层入口，符合 compile-time-only 目标。

### TC-003: compat 层停止运行时选择并明确重新编译切换
- **结果**: PASS
- **实际行为**: `common/vm.go` 中 `SetVMType()` 已不再依赖 `vm.HasImplementation`、`vm.SelectImplementation`、`vm.CurrentVMType()` 进行运行时选择；调用非当前编译产物类型会显式 panic，并提示需要“重新编译”。
- **关键指标**: `go test ./qa/issue162 -run TestCompatLayerStopsRuntimeSelectionAndDocumentsRebuildOnlySwitch` 通过（包含于 `go test ./qa/issue162 -run Test`）。
- **备注**: `selectPreferredVMType()` 已收敛到与编译期产物一致的回退策略。

### TC-004: vm_dual 被删除或降级为非常规模式
- **结果**: PASS
- **实际行为**: `common/vm/select_dual.go` 仍存在，但文件内已明确标注 `debug / 非常规构建模式`，且不再注册双实现，也不再执行旧式常规绑定流程。
- **关键指标**: `go test ./qa/issue162 -run TestVMDualIsRemovedOrExplicitlyDebugOnly` 通过（包含于 `go test ./qa/issue162 -run Test`）。
- **备注**: 当前 `vm_dual` 已满足“降级为非常规模式”的验收分支。

### TC-005: 各 build tag 选择文件只保留单实现直绑
- **结果**: PASS
- **实际行为**: `common/vm/select_default.go`、`select_lru.go`、`select_full.go`、`select_v3.go` 均改为 `selectedVMType` / `selectedVersion` + `selectedImplementation` 的单实现编译期绑定，不再调用 `registerImplementation`、`bindV*` 或共享状态版本写入。
- **关键指标**: `go test ./qa/issue162 -run TestCompileTimeSelectionFilesDirectlyBindSingleImplementation` 通过（包含于 `go test ./qa/issue162 -run Test`）。
- **备注**: 静态结构与 AC1/AC2 保持一致。

### 动态验收: 构建与至少一个真实 testdata 用例
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过，Maze 结果与 validate 校验一致。
- **关键指标**:
  - 构建命令：通过
  - `python/20260129-complex-types-311`：1/1 通过
  - 关键对象计数：`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500`、`Config=300`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

## 失败的 Test Case（如有）

本轮没有已执行并失败的 TC；总体 FAIL 的原因是存在未完成的关键验收项，见“未执行项”。

## 未执行项（如有）
- AC5 / 性能验收项未执行：未采集 `refs=v0` / `refs=v1` 场景下的 CPU profile，无法确认 facade wrapper 成本是否较前一版进一步下降。
- 未执行原因：该项需要额外的 profile 采集与对比基线，本轮开发交付与当前分支中均未提供对应结果；静态结构收敛不能替代性能实测结论。
- 受影响范围：直接影响计划文档中的 AC5；也影响是否可以确认“热路径成本进一步下降”这一最终验收结论。
- 日志附注：本轮真实 testdata 执行时未生成 `maze.py.log` 文件；`maze.log` 中还出现与 C++ 分析脚本相关的 `AttributeError: module 'gdb' has no attribute 'all_variables_symbol'` 调试日志，但该问题未阻断本次 Python 用例通过，且无法证明与 issue #162 改动直接相关，因此未单独记为本轮失败 TC。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [ ] `refs=v0` / `refs=v1` 场景 CPU profile 已确认 facade wrapper 成本进一步下降

## 执行记录
- 静态检查：已完成计划、测试用例、开发报告、关键代码文件与分支 diff 核对。
- 关键测试：`go test ./qa/issue162 -run Test` 通过。
- 构建验证：`./maze --build` 通过。
- 动态 testdata：`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。

## 总结
- 通过: 6 / 6（已执行项）
- 失败: 0 / 6（已执行项）
- 未完成关键验收项: 1（AC5 性能回归）
- 建议: **暂不判定可发布**。从正确性与结构收敛角度看，issue #162 的主要代码改动已经到位；但在补齐 `refs=v0` / `refs=v1` CPU profile 对比并证明 facade 热路径成本确有进一步下降之前，不应将本需求整体验收为 PASS。
