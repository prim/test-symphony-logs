# 开发报告

## 完成的任务
- [x] Task 1: 将 `common/vm/facade.go` 收敛为 compile-time-only 薄门面，移除 facade 文件中的运行时 implementation 状态与 dispatch
- [x] Task 2: 改造 `select_default.go`、`select_lru.go`、`select_full.go`、`select_v3.go` 为单实现编译期直绑
- [x] Task 3: 将 `vm_dual` 降级为 debug / 非常规模式，不再作为常规双实现注册路径
- [x] Task 4: 更新 `common/vm.go` compat 层，明确 VM 切换只能通过重新编译完成
- [x] Task 5: 补充实现记录与计划勾选，完成 issue #162 相关构建与回归测试

## 修改的文件
- `common/vm/facade.go`: 删除 facade 文件中的运行时 implementation state，改为薄 wrapper
- `common/vm/runtime_compat.go`: 新增兼容辅助层，承接旧生命周期状态与测试辅助逻辑
- `common/vm/select_default.go`: 默认构建改为单实现直绑 `v2`
- `common/vm/select_lru.go`: `vm_lru` 构建改为单实现直绑 `v1`
- `common/vm/select_full.go`: `vm_full` 构建改为单实现直绑 `v2`
- `common/vm/select_v3.go`: `vm_v3` 构建改为单实现直绑 `v3`
- `common/vm/select_dual.go`: 降级为 debug / 非常规模式的单实现绑定
- `common/vm.go`: compat 层移除运行时选择逻辑，改为重新编译语义
- `dev-log/2026-03-17-vm-facade-compile-time-only-plan.md`: 勾选任务与 AC 状态
- `dev-log/2026-03-17-vm-facade-compile-time-only-implementation.md`: 新增实现记录

## 自测结果
- 测试命令: `go test ./qa/issue162 -run Test`
- 测试结果: 通过
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `go test ./common/vm/...`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/... ./common`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过

## 验收核对
- AC1: PASS `common/vm/facade.go` 不再维护当前 implementation 的运行时绑定状态
- AC2: PASS `GetMemory*` 不再通过 `currentImplementation()` 或等价 dispatch 取实现
- AC3: PASS `vm_dual` 已降级为 debug / 非常规模式
- AC4: PASS facade 热路径已收敛为 compile-time-only wrapper
- AC5: PARTIAL 本轮未重新采集 `refs=v0` / `refs=v1` CPU profile，仅完成静态结构收敛与功能回归

## 剩余工作
- 未完成项: `refs=v0` / `refs=v1` CPU profile 回归验证
- 未验证项: facade wrapper 在 profile 中的进一步热点下降幅度

## Self-Review 发现与修复
- 初版改造后，`common/vm` 既有测试仍依赖旧的 `currentVMType.Store()` / `boundImplementation.Store()` 测试接口；已通过 `runtime_compat.go` 提供轻量兼容适配器修复
- 初版把 facade 改成命名函数后，`vm_v3` 测试中的 nil 比较触发编译器告警；已改回薄函数变量包装，同时仍保持直接转发到编译期实现
- 初版 `SetVMType(VMNone)` 会导致编译期实现未重新绑定；已新增 `BindCompiledImplementation()` 修复初始化期兼容行为

## 注意事项
- 运行时 implementation state 仍保留在 `runtime_compat.go` 兼容层中，用于旧生命周期测试与 `Init`/`Close` 行为复用，但 facade 文件与热路径已不再依赖这些状态
- `vm_dual` 当前仅保留为调试/非常规模式，不应再视为常规支持路径
- 如需满足 AC5，需要后续基于真实样本重新执行 `--cpu` 与 `pprof` 对比验证

## 结论
PARTIAL
