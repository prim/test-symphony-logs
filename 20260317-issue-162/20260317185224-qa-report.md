## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: facade 删除运行时 implementation 状态
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 已删除 `implementation` / `implementationBinding`、`boundImplementation`、`currentVMType`、`initialized`、`currentImplementation()` 等运行时状态与绑定逻辑。
- **关键指标**: `go test ./qa/issue162 -run TestFacadeRemovesRuntimeImplementationState` 通过。
- **备注**: 仅就 facade 文件本身看，去状态化已完成。

### TC-002: GetMemory 热路径去除 runtime dispatch
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中 `GetMemory*` 与 `VMStat` 已改为薄 wrapper，直接转发到 `implGetMemory*` / `implVMStat`，未再通过 `currentImplementation()` 或 `dispatchGetMemory*()`。
- **关键指标**: `go test ./qa/issue162 -run TestGetMemoryFacadeStopsRuntimeDispatch` 通过。
- **备注**: facade 文件热路径 dispatch 已移除。

### TC-003: compat 层停止运行时选择并明确重新编译切换
- **结果**: PASS
- **实际行为**: `common/vm.go` 的 `SetVMType()` 已不再调用 `vm.HasImplementation`、`vm.SelectImplementation`、`vm.CurrentVMType`，并明确声明切换 VM 需要“重新编译”。
- **关键指标**: `go test ./qa/issue162 -run TestCompatLayerStopsRuntimeSelectionAndDocumentsRebuildOnlySwitch` 通过。
- **备注**: compat 层文案与主流程行为符合设计意图。

### TC-004: vm_dual 被删除或降级为非常规模式
- **结果**: PASS
- **实际行为**: `common/vm/select_dual.go` 仍存在，但已改为单实现绑定，并明确标注 `debug / 非常规构建模式`。
- **关键指标**: `go test ./qa/issue162 -run TestVMDualIsRemovedOrExplicitlyDebugOnly` 通过。
- **备注**: 满足“删除或降级”为非常规模式的验收要求。

### TC-005: 各 build tag 选择文件只保留单实现直绑
- **结果**: PASS
- **实际行为**: `select_default.go`、`select_lru.go`、`select_full.go`、`select_v3.go` 已不再使用 `registerImplementation` / `bindV*` / `currentVersion =` 组织运行时选择，改为编译期单实现直绑。
- **关键指标**: `go test ./qa/issue162 -run TestCompileTimeSelectionFilesDirectlyBindSingleImplementation` 通过。
- **备注**: 选择文件层面符合 compile-time-only 方向。

## 失败的 Test Case（如有）

本轮按 `dev-flow/test-cases.md` 中 5 个 TC 执行时全部通过，但需求级验收仍然失败，原因如下。

### 需求级失败 1：`common/vm` 仍维护运行时 implementation 绑定状态
- **结果**: FAIL
- **预期行为**: 根据 AC1 与计划文档，`common/vm` 应“不再维护当前 implementation 的运行时绑定状态”。
- **实际行为**: 新增的 `common/vm/runtime_compat.go` 仍保留完整运行时状态与绑定机制，包括：
  - `compiledImplementations`
  - `activeBinding`
  - `activeVMType`
  - `currentVMType`
  - `boundImplementation`
  - `initialized`
  - `currentImplementation()`
  - `SelectImplementation()`
  - `bindImplementation()`
- **复现步骤**:
  1. 阅读 `common/vm/runtime_compat.go`
  2. 可见上述状态、绑定结构与选择函数仍存在
  3. 执行内容搜索：`grep` 命中 `common/vm/runtime_compat.go` 多处相关符号
- **可能原因**: 实现将运行时状态从 `facade.go` 迁移到 `runtime_compat.go`，从“热路径 facade 文件去状态化”角度看是收敛了，但从“`common/vm` 整体不再维护 runtime implementation state”角度看并未真正删除，只是挪位置。
- **建议修复**: 继续移除 `common/vm` 包内对 implementation 绑定状态的维护，至少应删除或彻底隔离 `runtime_compat.go` 中的运行时选择/绑定能力，避免与 AC1 语义冲突。

### 需求级失败 2：AC5 未完成验证，且当前变更无法证明 facade 运行时成本已进一步下降
- **结果**: FAIL
- **预期行为**: 根据 AC5，需要在 `refs=v0` / `refs=v1` 场景下用 CPU profile 证明 facade wrapper 成本进一步下降。
- **实际行为**: 开发报告明确标记 AC5 为 `PARTIAL`，本轮未重新采集 `refs=v0` / `refs=v1` CPU profile；QA 也未找到新的 profile 证据。
- **复现步骤**:
  1. 阅读 `dev-flow/20260317184746-dev-report.md`
  2. 查看“验收核对”与“剩余工作”部分
  3. 可见 AC5 未完成，仅有静态结构收敛与功能回归
- **可能原因**: 本次提交聚焦结构改造，没有同步补齐性能回归证据。
- **建议修复**: 补采 `refs=v0` / `refs=v1` 场景 CPU profile，对比 facade wrapper 开销，再提交完整性能验收材料。

## Findings

- `common/vm/facade.go` 虽已去掉运行时状态，但这些状态整体仍存在于新文件 `common/vm/runtime_compat.go` 中，因此需求中“`common/vm` 不再维护当前 implementation 的运行时绑定状态”并未真正满足。
- 现有新增 QA 测试仅约束 `facade.go` 与 `common/vm.go`，没有覆盖 `runtime_compat.go`，导致测试全部通过但仍遗漏需求级回归风险。
- `vm_dual` 已降级为非常规模式，build-tag 选择文件也已改成单实现直绑，这部分实现与需求一致。
- 至少一个真实 testdata 用例已执行：`python3 testdata/run_test.py python/20260129-complex-types-311` 通过，说明本次改造未直接打坏该主链路 smoke test。
- 但 AC5 依赖 CPU profile 证据，本轮没有完成，无法给出 PASS。

## Test Results

- `git diff origin/master...HEAD`：已审阅变更范围
- `go test ./qa/issue162 -run Test`：通过
- `./maze --build`：通过
- `go test ./common/vm/...`：通过
- `go test -tags vm_v3 ./common/vm/... ./common`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `grep` 静态检查：确认 `common/vm/runtime_compat.go` 仍存在运行时 implementation state 与选择/绑定逻辑

## 未执行项（如有）
- `refs=v0` / `refs=v1` CPU profile 回归验证未执行
- 未执行原因：代码与开发报告均未提供对应 profile 产物或脚本入口；在缺少基线与样本说明的前提下，本轮先完成低成本静态核对、构建与 smoke test
- 可能影响范围：直接影响 AC5，以及“facade wrapper 成本进一步下降”的最终性能结论

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 5 / 5（test case）
- 失败: 2 / 2（需求级关键发现）
- 建议: 当前**不建议发布**。虽然 facade 文件与 build-tag 选择文件改造方向正确，且基础构建与 smoke test 通过，但 `common/vm` 包内仍保留运行时 implementation state，且 AC5 缺少 CPU profile 证据，不能判定 issue #162 已完整满足。
