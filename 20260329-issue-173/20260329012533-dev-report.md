## Summary
本轮修复了最新 code review 指出的两个剩余问题：一是 `common/refs/v1` 无 RocksDB 回退实现的查询语义漂移，二是 `prepare/corefile.go` 跨设备重命名错误判断依赖错误字符串。portable GDB 抓 core 主链路保持不变，并重新完成构建与 prepare 主链路验证。

## Changes
- `common/refs/v1/refs_norocksdb.go`: 将 `GetReferents` / `GetReferrers` 调整为在无持久化存储时返回 `nil`，与真实 `v1` 契约保持一致
- `common/refs/v1/refs_norocksdb_test.go`: 更新回归测试，避免将错误的内存态查询行为固化为预期
- `prepare/corefile.go`: 用 `errors.Is(err, syscall.EXDEV)` 替代错误字符串匹配，稳健处理跨设备重命名失败
- `prepare/gcmd_test.go`: 增加 `EXDEV` 错误识别回归测试
- `dev-log/2026-03-29-issue-173-review-fix-v2.md`: 记录本轮修复背景、实现与验收结果

## Testing
- `go test ./common/refs/v1`
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand|TestCrossDeviceRenameUsesEXDEV'`
- `./maze --build`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
