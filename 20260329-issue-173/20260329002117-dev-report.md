## Summary

本轮修复了 QA 指出的技术分析文档残留问题，修正 `doc/maze_py_analysis.md` 中仍然展示系统 `gcore` 的伪代码与排障提示，使其与当前 `ResolveGDBBin()` + GDB `generate-core-file` 实现一致。

结论：`PARTIAL`。本轮仅完成文档残留修复；由于仓库中 `testdata/` 仍有多处历史 README 继续引用系统 `gcore`，且 `go test ./prepare` 仍受外部 `rocksdb/c.h` 缺失阻塞，尚不能宣称全部完成。

## Changes

- `doc/maze_py_analysis.md`: 将 Corefile 伪代码从系统 `gcore` 更新为通过解析后的 GDB 执行 `attach + generate-core-file + detach + quit`；同步更新 core dump 失败排障说明。
- `dev-log/2026-03-28-portable-gcore-plan.md`: 依据当前实际状态回退 `Task 7` 与 `AC8` 为未完成，避免计划文档错误宣称用户文案已全部迁移。

## Testing

- `./maze --build`
  - 结果：通过。
- `python3 testdata/run_test.py python/20260129-complex-types-311`
  - 结果：通过。
  - 关键结果：`pymempool_objects = 68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`。
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`
  - 结果：失败。
  - 原因：外部依赖缺失，编译阶段报错 `fatal error: rocksdb/c.h: No such file or directory`。

## 验收核对

- AC1: PASS - `prepare/corefile.go` 中未再调用系统 `gcore`。
- AC2: PASS - core dump 与 GDB Server 共用 `ResolveGDBBin()`。
- AC3: PARTIAL - 代码与新增测试覆盖了 portable GDB 优先级，但目标 Go 单测在当前环境未成功执行。
- AC4: PARTIAL - 代码与新增测试覆盖了 `MAZE_GDB_BIN`，但目标 Go 单测在当前环境未成功执行。
- AC5: PASS - `ctx.Corefile.Gcore(args.Pid)` 调用接口保持不变。
- AC6: PASS - `./maze --build` 成功。
- AC7: PASS - `python/20260129-complex-types-311` prepare 主链路测试通过。
- AC8: FAIL - `testdata/` 下仍有多处 README 继续推荐系统 `gcore`，本轮未清完。

## 已完成项

- 修正文档 `doc/maze_py_analysis.md` 中与当前实现相矛盾的 `gcore` 伪代码。
- 修正文档中的 core dump 排障说明，明确当前依赖的是可解析 GDB 而非系统 `gcore`。
- 回退计划文档中被过早勾选的 `Task 7` / `AC8` 状态。
- 重新完成构建与一个 prepare 主链路冒烟验证。

## 未完成项

- 清理 `testdata/README.md` 及其他 `testdata/**/README.md` 中残留的系统 `gcore` 指引。
- 根据原计划继续清理其余关键文档中的历史表述，直至满足 AC8。

## 未验证项

- `AC3`、`AC4` 对应的 Go 回归测试尚未在当前环境成功执行。

## Self-Review

- 发现上一轮将 `Task 7` / `AC8` 提前标记为完成，但 QA 已证明该结论不成立；本轮已修正计划状态，避免误导后续验收。
- 发现 `doc/maze_py_analysis.md` 注释与示例代码不一致；本轮已统一。

## 注意事项

- 按仓库约束，本轮没有修改 `testdata/` 下测试文件内容以外的验证脚本，但 `testdata` README 文档仍属需求范围中的待清理对象，后续需要继续处理。
- 当前 Go 单测阻塞点是环境缺少 RocksDB 头文件，不是本次 portable GDB 逻辑本身的编译错误。
