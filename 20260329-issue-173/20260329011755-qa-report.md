## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

本次针对 issue #173 的验收结论为通过。静态核对、定向回归测试、构建验证以及至少一个 prepare 主链路 testdata 用例均已执行并通过，满足需求文档中的 AC1-AC6。

## Findings

- `prepare/corefile.go` 已移除系统 `gcore` 直接调用，改为通过 `ResolveGDBBin()` 解析出的 GDB 执行 `attach + generate-core-file + detach + quit`。
- `prepare/gdb_server.go` 与 `prepare/corefile.go` 已共用 `ResolveGDBBin()`，路径优先级与需求一致：`MAZE_GDB_BIN` > `./gdb/prim-gdb/gdb-portable` > 系统 `gdb`。
- `prepare/gcmd_test.go` 已新增回归测试，覆盖环境变量优先级、portable GDB 优先级、禁止重新引入系统 `gcore`、以及 corefile 复用 `ResolveGDBBin()` / 构造 GDB 抓 core 命令等关键约束。
- `./maze --build` 成功，`python/20260129-complex-types-311` 用例通过，满足至少一个 prepare 主链路用例通过的要求。
- 本次 smoke test 的 `maze.log` 中仍可见既有非阻塞调试日志，如 `AttributeError: module 'gdb' has no attribute 'all_variables_symbol'`，但未导致本轮目标功能验收失败，也未阻断用例通过；建议后续单独跟踪，但不构成 issue #173 的拒收条件。

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: PASS
- **实际行为**: 定向回归测试通过，`ResolveGDBBin()` 在同时存在环境变量路径与 portable GDB 时返回 `MAZE_GDB_BIN` 指向路径。
- **关键指标**: `go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable` 通过（本次以定向测试集合执行，结果为 `ok   maze/prepare`）。
- **备注**: 满足 AC4。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: PASS
- **实际行为**: 定向回归测试通过，`MAZE_GDB_BIN` 为空且存在 `./gdb/prim-gdb/gdb-portable` 时，解析结果优先返回 portable 路径。
- **关键指标**: `go test ./prepare -run TestResolveGDBBinPrefersPortableBeforeSystem` 通过。
- **备注**: 满足 AC3。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态检查与回归测试确认 `prepare/corefile.go` 不包含 `exec.Command("gcore"`。
- **关键指标**: `prepare/corefile.go` 当前实现通过 `exec.Command(command, args...)` 调起 resolved GDB；`go test ./prepare -run TestCorefileGoDoesNotInvokeSystemGcore` 通过。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 中存在 `gdbBin := ResolveGDBBin()`，与 GDB Server 使用统一入口。
- **关键指标**: `go test ./prepare -run TestCorefileGoUsesResolveGDBBin` 通过。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 明确构造 `-ex attach <pid>`、`-ex generate-core-file <path>`、`-ex detach`、`-ex quit` 参数序列。
- **关键指标**: `go test ./prepare -run 'TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'` 通过。
- **备注**: 满足 AC2，并验证目标路径支持带空格时的参数引用。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 执行通过，Maze 完成 prepare 主链路分析并输出正确结果。
- **关键指标**:
  - 构建结果：成功
  - testdata 结果：1 passed, 0 failed
  - 样例指标：`pymempool_objects = 68626`，`PersonClass = 1000`，`GameEntity = 300`，`TreeNode = 600`
- **备注**: 满足 AC5、AC6。

## Test Results

- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`：通过
- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行“在线 attach 真实进程后实际生成 `core.<pid>`”的专门动态用例。
- 未执行原因：本轮需求文档与 test case 报告要求的最小动态验收范围已由构建 + prepare 主链路 smoke test + 定向回归测试覆盖；继续追加真实在线抓 core 用例属于扩展验证，不影响本轮 AC 判定。
- 可能影响范围：主要影响对 `CorefileManager.Gcore(pid)` 在线路径的更强动态信心，但不影响已验证的 AC1-AC6 结论。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以进入下一阶段；就 issue #173 的验收标准而言，本次实现已满足发布条件。另建议后续单独跟踪 `maze.log` 中与 C++ 符号扫描相关的既有调试异常，但该问题不阻塞本次需求验收。
