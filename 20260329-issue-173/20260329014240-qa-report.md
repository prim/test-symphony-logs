# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #173 的核心验收标准进行了静态核对、定向回归测试、构建验证以及至少一个 prepare 主链路用例验证，未发现阻塞发布的问题。

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: PASS
- **实际行为**: `go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable` 通过，说明当同时存在 `MAZE_GDB_BIN` 与 `./gdb/prim-gdb/gdb-portable` 时，core dump / GDB 链路会优先使用环境变量指定路径。
- **关键指标**: prepare 回归测试通过。
- **备注**: 满足 AC4。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: PASS
- **实际行为**: `go test ./prepare -run TestResolveGDBBinPrefersPortableBeforeSystem` 通过，确认 portable GDB 的优先级高于系统 `gdb`。
- **关键指标**: prepare 回归测试通过。
- **备注**: 满足 AC3。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态检查与 `TestCorefileGoDoesNotInvokeSystemGcore` 均确认 `prepare/corefile.go` 不再出现 `exec.Command("gcore", ...)`。
- **关键指标**: 源码已改为 `ResolveGDBBin()` + `exec.Command(command, args...)`。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 中存在 `gdbBin := ResolveGDBBin()`，并据此构造抓 core 命令；`prepare/gdb_server.go` 注释也明确要求 prepare 阶段所有 GDB 启动链路统一走该入口。
- **关键指标**: `TestCorefileGoUsesResolveGDBBin` 通过。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 构造了 `attach + generate-core-file + detach + quit` 的 `gdb --batch` 命令序列；`TestCorefileGoBuildsGenerateCoreCommand` 与 `TestBuildGenerateCoreCommand` 均通过。
- **关键指标**: 命令参数为 `-q -n --batch -ex attach <pid> -ex generate-core-file "<path>" -ex detach -ex quit`。
- **备注**: 满足 AC2，且路径参数带引号，覆盖空格路径场景。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过，验证至少一个 prepare 主链路用例可正常运行。
- **关键指标**: `pymempool_objects = 68626`；多个 Python 类型对象数量校验全部通过；测试汇总为 `1 passed, 0 failed`。
- **备注**: 满足 AC5、AC6。

## 静态检查补充结论

- `prepare/corefile.go` 已从“直接调用系统 `gcore`”切换为“解析 GDB 路径后，通过 GDB 的 `generate-core-file` 指令生成 core”。
- 生成目标路径为 `ctx.DirPath/core.<pid>`，符合需求文档。
- 抓 core 前仍会优先复用已有的 `ctx.DirPath/core.<pid>` 或历史 `./core.<pid>`，符合“兼容复用历史已有 core 文件”的要求。
- 新增了跨设备重命名兜底：`os.Rename` 遇到 `EXDEV` 时改为 copy + remove，可避免 core 文件跨文件系统移动失败。

## 日志检查

- 已检查 `maze.log`，本次 prepare 主链路运行完成并产出分析结果。
- 日志中存在若干历史性/环境性告警，例如符号下载 404、`gdb` Python 扩展缺少 `all_variables_symbol`，但未阻断本次用例通过，也不构成本 issue 的验收失败。
- 本轮未发现 `maze.py.log` 文件；不影响该用例通过。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未单独执行 `go test ./common/refs` 与 `-tags=refs_v1 ./common/refs`，因为它们属于本轮附带修复的 refs 范围，不是 issue #173 的核心 AC；本次已完成 issue 所要求的 prepare 核心回归、构建与主链路 smoke 验证。
- 未执行真实在线 `--pid` 抓 core 动态用例，因此未直接实测 portable GDB 在在线抓 core 场景下的实际进程 attach 行为；不过源码路径、回归测试与主链路 smoke 已证明需求修改已接线到位。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand|TestCrossDeviceRenameUsesEXDEV'` ✅
- `./maze --build` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以通过本 issue 的 QA 验收。就当前证据看，系统 `gcore` 依赖已从 `prepare/corefile.go` 移除，core dump 与 GDB Server 已统一复用 `ResolveGDBBin()`，并且构建和至少一个 prepare 主链路用例均已通过。
