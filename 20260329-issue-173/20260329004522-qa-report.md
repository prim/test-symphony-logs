## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: PASS
- **实际行为**: `go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable` 所覆盖逻辑已包含在本轮回归测试集中，并执行通过。
- **关键指标**: `ResolveGDBBin()` 在环境变量与 portable GDB 同时存在时优先返回 `MAZE_GDB_BIN`。
- **备注**: 通过源码检查与单元测试结果共同确认 AC4 生效。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: PASS
- **实际行为**: `go test ./prepare -run TestResolveGDBBinPrefersPortableBeforeSystem` 所覆盖逻辑已包含在本轮回归测试集中，并执行通过。
- **关键指标**: `ResolveGDBBin()` 优先返回 `./gdb/prim-gdb/gdb-portable`，未退回系统 `gdb`。
- **备注**: 满足 AC3。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态检查 `prepare/corefile.go`，未发现 `exec.Command("gcore"...)`；回归测试 `TestCorefileGoDoesNotInvokeSystemGcore` 通过。
- **关键指标**: `prepare/corefile.go` 改为 `ResolveGDBBin()` + `exec.Command(command, args...)`。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 在抓取 core 前显式调用 `ResolveGDBBin()`；回归测试 `TestCorefileGoUsesResolveGDBBin` 通过。
- **关键指标**: `gdbBin := ResolveGDBBin()` 位于 core 生成主链路。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 构造 `attach -> generate-core-file -> detach -> quit` 的 GDB `-ex` 参数；`TestCorefileGoBuildsGenerateCoreCommand` 与 `TestBuildGenerateCoreCommand` 通过。
- **关键指标**: 命令参数为 `-q -n --batch -ex attach <pid> -ex generate-core-file <path> -ex detach -ex quit`。
- **备注**: 与需求文档方案一致。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`，其余校验项全部通过。
- **备注**: 已满足“至少一个 prepare 主链路测试用例通过”的最低动态验收要求。

## Findings
- 未发现阻断本需求验收的问题。
- 本轮相对前次 QA 的关键修复点已生效：`go test ./prepare` 不再被 RocksDB 头文件缺失阻塞，prepare 定向回归测试可以在当前环境执行。
- `testdata/` 下未再发现把系统 `gcore` 作为操作指引的 README 文档；仅有历史 `devlog.md` 文字描述旧现象，不构成当前使用指引。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）
- 未执行更大范围的多语言/多分配器动态回归。
- 原因: 本需求的 AC 已被静态检查、prepare 定向回归测试、构建和至少一个主链路 smoke test 充分覆盖；继续扩展测试收益较低。
- 可能影响范围: 若存在与特定分配器或特定 GDB 版本耦合的问题，可能需要后续专项回归进一步覆盖，但不影响本次 AC1-AC6 的结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `./maze --build`: 通过
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`: 通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`: 通过

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以合入；当前实现已满足 issue #173 的验收标准，未发现需要阻塞发布的问题。
