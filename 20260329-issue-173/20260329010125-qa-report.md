## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: PASS
- **实际行为**: 执行 `go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable` 通过。
- **关键指标**: `ResolveGDBBin()` 在环境变量与 portable GDB 同时存在时优先返回 `MAZE_GDB_BIN` 指定路径。
- **备注**: 满足 AC4。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: PASS
- **实际行为**: 执行 `go test ./prepare -run TestResolveGDBBinPrefersPortableBeforeSystem` 通过。
- **关键指标**: `ResolveGDBBin()` 返回 `./gdb/prim-gdb/gdb-portable`，未回退到系统 `gdb`。
- **备注**: 满足 AC3。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态检查 `prepare/corefile.go`，未发现 `exec.Command("gcore"...)`；定向回归测试 `TestCorefileGoDoesNotInvokeSystemGcore` 通过。
- **关键指标**: `Gcore()` 已改为通过 `ResolveGDBBin()` + `exec.Command(command, args...)` 调起解析后的 GDB。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 在生成 core 前显式执行 `gdbBin := ResolveGDBBin()`；定向回归测试 `TestCorefileGoUsesResolveGDBBin` 通过。
- **关键指标**: core dump 与 GDB Server 共用同一解析入口。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 构造 `attach -> generate-core-file -> detach -> quit` 的 GDB 批处理命令；`TestCorefileGoBuildsGenerateCoreCommand` 与 `TestBuildGenerateCoreCommand` 通过。
- **关键指标**: 参数序列为 `-q -n --batch -ex attach <pid> -ex generate-core-file <path> -ex detach -ex quit`。
- **备注**: 与需求文档方案一致。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`，校验项全部通过。
- **备注**: 满足 AC5、AC6。

## Findings
- 本轮复验中，issue #173 核心实现链路成立：`prepare/corefile.go` 已停用系统 `gcore`，统一通过 `ResolveGDBBin()` 解析出的 GDB 生成 core。
- 上一轮阻塞项已解除：`go test ./prepare` 的目标回归测试在当前环境可以直接执行通过，不再受 `rocksdb/c.h` 缺失影响。
- `testdata/README.md` 范围内未检出 README 形式的系统 `gcore` 操作指引；`doc/` 中仅剩 `Corefile.gcore()` 这类函数名/流程名描述，不构成用户继续依赖系统 `gcore` 的现行操作说明。
- `maze.log` 中存在调试符号下载 404 后回退 S3 的 WARNING，但未导致本次 prepare 主链路测试失败，当前判定为非阻塞告警。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）
- 未执行更多语言/分配器组合的扩展动态回归。
- 原因: 本需求 AC 已由源码审查、定向 Go 回归测试、构建验证与至少一个 prepare 主链路 smoke test 覆盖；继续扩展执行对本 issue 的边际收益较低。
- 可能影响哪些 AC: 若存在特定 GDB 版本、特定分配器或特定语言 runtime 的组合问题，可能影响更广泛兼容性判断，但不影响本次 AC1-AC6 验收结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results
- `go test ./common/refs/v1`: 通过
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`: 通过
- `./maze --build`: 通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`: 通过

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以通过 issue #173 的本轮验收；当前实现满足需求文档 AC1-AC6，未发现阻塞发布的问题。
