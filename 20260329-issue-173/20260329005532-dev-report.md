## Summary

修复了 code review 指出的 `common/refs/v1` 默认构建语义漂移问题：将真实 v1 测试显式限制在 `grocksdb_clean_link` 场景下执行，并为无 RocksDB 依赖的降级实现补充独立回归测试，避免 `go test ./common/refs/v1` 在默认环境下编译失败。

本轮同时复核了 issue #173 的原始需求、最新 QA 报告与 code review 报告，确认 core dump 统一走 `ResolveGDBBin()` 的实现仍然成立，构建与 prepare 主链路验证继续通过。

## Changes

- `common/refs/v1/refs_test.go`: 为依赖真实 v1/GrocksDB 内部结构的测试补充 `grocksdb_clean_link` build tag，避免默认无 RocksDB 环境错误编译。
- `common/refs/v1/refs_norocksdb_test.go`: 新增无 RocksDB 回退实现测试，验证降级路径下 `Init/Configure/AddIndexes/AddReverseIndex/AddIndex/GetReferents/GetReferrers/Save/Check` 基本 API 可用。

## Testing

- `go test ./common/refs/v1`：通过
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`：通过
- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## Acceptance Check

- AC1: PASS - `prepare/corefile.go` 未再调用系统 `gcore`
- AC2: PASS - core dump 与 GDB Server 共用 `ResolveGDBBin()`
- AC3: PASS - portable GDB 优先级由回归测试覆盖
- AC4: PASS - `MAZE_GDB_BIN` 覆盖优先级由回归测试覆盖
- AC5: PASS - `./maze --build` 成功
- AC6: PASS - `python/20260129-complex-types-311` prepare 主链路通过

## Status

COMPLETE
