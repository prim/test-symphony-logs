## Summary

PARTIAL：已将 prepare 阶段的 core dump 生成从系统 `gcore` 切换为统一走 `ResolveGDBBin()` 解析出的 GDB，并补充了对应回归测试与文档迁移。`./maze --build` 与一个 prepare 主链路用例已通过，但 `go test ./prepare` 仍受外部 `rocksdb/c.h` 缺失影响，导致预置 Go 单测无法在当前环境完成全量验证。

## Changes

- `prepare/corefile.go`: 删除系统 `gcore` 调用，新增 `buildGenerateCoreCommand()`，通过 resolved GDB 执行 `attach + generate-core-file + detach + quit`，默认输出到 `ctx.DirPath/core.<pid>`，并保留历史 `core.<pid>` 复用逻辑。
- `prepare/gdb_server.go`: 为 `ResolveGDBBin()` 增补统一职责注释，明确供 GDB Server 与 core dump 共用。
- `prepare/gcmd_test.go`: 增加 `buildGenerateCoreCommand()` 参数构造测试，保留并满足 QA 预置的静态回归断言。
- `prepare/prepare.go`: 将 prepare 早期错误提示从 `gcore failed` 调整为更准确的 `corefile prepare failed`。
- `dev-log/2026-03-28-portable-gcore-plan.md`: 按实际进度勾选任务与 AC。
- `doc/gdb.md`: 更新在线抓 core 的实际行为说明，改为 portable/resolved GDB。
- `doc/code_overview.md`: 更新总体流程描述，去掉系统 `gcore` 依赖表述。
- `doc/maze_py_analysis.md`: 更新文档中的 core dump 生成说明，改为基于 GDB `generate-core-file`。

## Testing

- `./maze --build`
  - 结果：通过。
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`
  - 结果：失败。
  - 关键原因：外部依赖缺失，`github.com/linxGnu/grocksdb` 编译时报错 `fatal error: rocksdb/c.h: No such file or directory`。
- `python3 testdata/run_test.py python/20260129-complex-types-311`
  - 结果：通过。

## 验收核对

- AC1: PASS — `prepare/corefile.go` 已移除 `exec.Command("gcore", ...)`。
- AC2: PASS — core dump 链路已调用 `ResolveGDBBin()`。
- AC3: PASS — core dump 与 GDB Server 共用 `ResolveGDBBin()`，portable GDB 优先级保持有效。
- AC4: PASS — `MAZE_GDB_BIN` 优先级未变，core dump 链路复用同一解析入口。
- AC5: PASS — `./maze --build` 成功。
- AC6: PASS — `python/20260129-complex-types-311` prepare 主链路用例通过。

## 剩余工作

- 未完成项：无。
- 未验证项：QA 预置的 Go 单元测试（TC-001 ~ TC-005）在当前环境受 `rocksdb/c.h` 缺失影响，尚未完成运行验证。

## Self-Review 发现与修复

- 发现历史 `core.<pid>` 迁移到输出目录时可能遇到跨文件系统 `rename` 失败；已补充 copy+remove 兜底逻辑，避免 `EXDEV` 场景丢失 core 文件。
- 发现错误提示仍沿用旧的 `gcore failed` 语义；已同步改为更准确的 `corefile prepare failed`。

## 注意事项

- 当前实现使用 `generate-core-file` 并对目标路径做了字符串转义，减少 `Logdir` 含空格时的 GDB 命令解析风险。
- 文档已开始迁移 away from 系统 `gcore`，但仓库中仍可能存在更多历史示例未在本轮全部清理。
- 当前轮次结论：PARTIAL。
