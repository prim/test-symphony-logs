# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按需求文档、test case 报告与最新开发报告完成了静态核对、定向回归测试、构建验证与一个 prepare 主链路 smoke test。`prepare/corefile.go` 已不再直接调用系统 `gcore`，而是与 `prepare/gdb_server.go` 共享 `ResolveGDBBin()` 解析逻辑，通过 GDB `attach + generate-core-file + detach + quit` 生成 core；`./maze --build` 与指定 testdata 用例均通过，满足本 issue 的核心验收标准。

## Findings

- `prepare/corefile.go` 已移除 `exec.Command("gcore", ...)`，改为通过 `ResolveGDBBin()` + `buildGenerateCoreCommand()` 执行 GDB 抓 core。
- `prepare/gdb_server.go` 与 `prepare/corefile.go` 现在共用 `ResolveGDBBin()`，满足 core dump 与 GDB Server 统一走同一 GDB 解析入口的要求。
- `prepare/gcmd_test.go` 已覆盖 `MAZE_GDB_BIN` 优先级、portable GDB 优先级、禁止重新引入系统 `gcore`、要求复用 `ResolveGDBBin()`、以及 GDB 抓 core 命令构造等关键回归约束。
- 额外修复项 `EXDEV` 跨设备重命名判断已改为 `errors.Is(err, syscall.EXDEV)`，并有对应回归测试覆盖；该项不改变主需求结论，但提升了 core 文件复用/搬运异常路径稳健性。
- 文档与 testdata 范围内未发现继续把系统 `gcore` 作为当前操作指引的关键残留；剩余命中仅为历史 devlog 现象描述或函数名 `Corefile.gcore()`，不构成用户工作流回退。

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: PASS
- **实际行为**: 执行 `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|...'` 通过，其中 `TestResolveGDBBinPrefersEnvOverrideOverPortable` 通过。
- **关键指标**: `go test ./prepare` 定向回归结果为 `ok   maze/prepare`。
- **备注**: 证明 `MAZE_GDB_BIN` 对 core dump 生成链路生效。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: PASS
- **实际行为**: 同一组定向回归测试中，`TestResolveGDBBinPrefersPortableBeforeSystem` 通过。
- **关键指标**: `ResolveGDBBin()` 在存在 `./gdb/prim-gdb/gdb-portable` 时返回 portable 路径。
- **备注**: 满足 portable GDB 优先级要求。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态阅读 `prepare/corefile.go` 未发现 `exec.Command("gcore"...)`；`TestCorefileGoDoesNotInvokeSystemGcore` 通过。
- **关键指标**: 源码当前使用 `exec.Command(command, args...)`，其中 `command` 来自 `ResolveGDBBin()`。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 第 56 行显式调用 `ResolveGDBBin()`；`TestCorefileGoUsesResolveGDBBin` 通过。
- **关键指标**: core dump 与 GDB Server 共用同一个 GDB 解析入口。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 构造 `-ex attach`、`-ex generate-core-file`、`-ex detach`、`-ex quit` 参数；`TestCorefileGoBuildsGenerateCoreCommand` 与 `TestBuildGenerateCoreCommand` 通过。
- **关键指标**: 命令参数包含 `--batch` 与 `generate-core-file "<path>"`。
- **备注**: 与需求文档描述一致。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过，输出 `1 passed, 0 failed`。
- **关键指标**: `pymempool_objects = 68626`；多个关键类型数量校验通过，如 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`。
- **备注**: 满足 AC5、AC6；同时符合“每次验收至少执行一个 testdata 用例”的要求。

## Test Results

- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand|TestCrossDeviceRenameUsesEXDEV'`：通过
- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行更大范围兼容性矩阵（Python 2.7、不同 glibc、不同 Node.js 版本）动态验证；本 issue 的 test case 报告也未将其列为本轮必须执行项。
- 未执行性能类用例；本次需求聚焦 prepare 抓 core 路径切换与回归约束，且当前已通过构建与主链路 smoke test 验证核心功能。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以进入后续合并流程；就 issue #173 的核心需求而言，本轮验收已通过。
