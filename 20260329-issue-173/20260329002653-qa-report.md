# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮已完成需求文档、测试用例、最新开发报告、代码差异与关键文件的静态核对，并实际执行了构建与一个 prepare 主链路 testdata 用例。主链路冒烟通过，但 `prepare` 目标回归测试在当前环境因缺失 `rocksdb/c.h` 无法编译执行，且 `testdata/` 下仍存在大量系统 `gcore` 残留文案，因此本轮结论为 FAIL。

## Findings

- `prepare/corefile.go` 已移除 `exec.Command("gcore", ...)`，改为调用 `ResolveGDBBin()` 后通过 GDB `attach + generate-core-file + detach + quit` 生成 core，满足核心实现方向。
- `prepare/gdb_server.go` 与 `prepare/corefile.go` 已共用 `ResolveGDBBin()`，静态检查显示 `MAZE_GDB_BIN`、portable GDB、系统 `gdb` 的优先级链路已统一。
- `./maze --build` 成功，`python/20260129-complex-types-311` prepare 主链路测试通过，说明主流程未被本次改动破坏。
- 但 `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'` 失败，失败原因为外部依赖缺失：`fatal error: rocksdb/c.h: No such file or directory`。
- `testdata/README.md` 与多个 `testdata/**/README.md` 仍直接提示使用系统 `gcore`，与本 issue 的迁移方向不一致，文案清理未完成。

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: FAIL
- **实际行为**: 目标单元测试未进入断言阶段，`go test ./prepare` 在编译期即因缺少 `rocksdb/c.h` 失败。
- **关键指标**: 编译错误 `fatal error: rocksdb/c.h: No such file or directory`
- **备注**: 代码静态检查显示 `ResolveGDBBin()` 的环境变量优先级实现存在，但本环境未能完成自动化验证。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: FAIL
- **实际行为**: 同 TC-001，测试命令在编译期失败，未执行到优先级断言。
- **关键指标**: 编译错误 `fatal error: rocksdb/c.h: No such file or directory`
- **备注**: 静态代码显示 `ResolveGDBBin()` 会优先返回 `./gdb/prim-gdb/gdb-portable`。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: FAIL
- **实际行为**: 自动化回归测试未能执行；静态检查未发现 `exec.Command("gcore"`。
- **关键指标**: `prepare/corefile.go` 中已改为 `exec.Command(command, args...)`
- **备注**: 代码层面符合预期，但回归测试命令在当前环境失败。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: FAIL
- **实际行为**: 自动化回归测试未能执行；静态检查确认 `prepare/corefile.go:55` 调用了 `ResolveGDBBin()`。
- **关键指标**: `gdbBin := ResolveGDBBin()`
- **备注**: 代码层面符合预期，但回归测试命令在当前环境失败。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: FAIL
- **实际行为**: 自动化回归测试未能执行；静态检查确认 `prepare/corefile.go:23` 使用 `generate-core-file`。
- **关键指标**: `-ex", "generate-core-file " + quoteGDBCommandArg(targetPath)`
- **备注**: 代码层面符合预期，但回归测试命令在当前环境失败。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: `pymempool_objects = 68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`
- **备注**: 满足“至少一个 prepare 主链路测试用例通过”的要求。

## 失败的 Test Case

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: FAIL
- **预期行为**: 单元测试应成功编译并验证 `MAZE_GDB_BIN` 覆盖 portable GDB。
- **实际行为**: `go test ./prepare` 编译阶段失败，未执行测试断言。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable
     ```
  2. 观察编译输出。
- **可能原因**: 当前环境缺少 RocksDB 头文件，导致 `grocksdb` 依赖无法编译。
- **建议修复**: 补齐测试环境依赖，或隔离 `prepare` 目标回归测试对外部 RocksDB 头文件的编译依赖。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: FAIL
- **预期行为**: 单元测试应成功编译并验证 portable GDB 优先级。
- **实际行为**: 同一编译阻塞导致测试未运行。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     go test ./prepare -run TestResolveGDBBinPrefersPortableBeforeSystem
     ```
     
  2. 观察编译输出。
- **可能原因**: 当前环境缺少 RocksDB 头文件。
- **建议修复**: 补齐环境依赖，确保目标测试可编译执行。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: FAIL
- **预期行为**: 静态回归测试应成功执行并通过。
- **实际行为**: 测试命令因编译依赖缺失失败。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     go test ./prepare -run TestCorefileGoDoesNotInvokeSystemGcore
     ```
  2. 观察编译输出。
- **可能原因**: 当前环境缺少 RocksDB 头文件。
- **建议修复**: 补齐环境依赖，恢复回归测试可执行性。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: FAIL
- **预期行为**: 静态回归测试应成功执行并通过。
- **实际行为**: 测试命令因编译依赖缺失失败。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     go test ./prepare -run TestCorefileGoUsesResolveGDBBin
     ```
  2. 观察编译输出。
- **可能原因**: 当前环境缺少 RocksDB 头文件。
- **建议修复**: 补齐环境依赖，恢复回归测试可执行性。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: FAIL
- **预期行为**: 静态回归测试应成功执行并通过。
- **实际行为**: 测试命令因编译依赖缺失失败。
- **复现步骤**:
  1. 在仓库根目录执行：
     ```bash
     go test ./prepare -run TestCorefileGoBuildsGenerateCoreCommand
     ```
  2. 观察编译输出。
- **可能原因**: 当前环境缺少 RocksDB 头文件。
- **建议修复**: 补齐环境依赖，恢复回归测试可执行性。

## 未执行项

- 无额外未执行的核心检查项；阶段 A 静态检查已尽量完成，阶段 B 关键 smoke test 已执行。
- 由于 `go test ./prepare` 编译阻塞，未能获得 TC-001 ~ TC-005 的自动化通过结果。
- 该阻塞影响 AC3、AC4 的自动化证明强度，也影响对回归测试可持续性的最终确认。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 补充静态核对

- **AC1**: 通过。`prepare/corefile.go` 已不再直接调用系统 `gcore`。
- **AC2**: 通过。`prepare/corefile.go` 与 `prepare/gdb_server.go` 共用 `ResolveGDBBin()`。
- **AC3**: 代码实现看起来满足，但自动化测试因环境阻塞未完成。
- **AC4**: 代码实现看起来满足，但自动化测试因环境阻塞未完成。
- **AC5**: 通过。`./maze --build` 成功。
- **AC6**: 通过。已运行并通过一个 prepare 主链路 testdata 用例。
- **范围遗漏**: `testdata/README.md`、`testdata/python/20260129-complex-types/README.md`、多个 `testdata/nodejs/**/README.md` 与 `testdata/cpp/**/README.md` 仍残留系统 `gcore` 指引，和需求“逐步清理 testdata/ 和文档里对系统 gcore 的提示”不完全一致。

## Test Results

- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`：失败，错误为 `fatal error: rocksdb/c.h: No such file or directory`

## 总结

- 通过: 1 / 6
- 失败: 5 / 6
- 建议: 当前不建议作为“验收通过”关闭。核心代码改动方向基本正确，但回归测试未在当前环境跑通，且 `testdata/` 文案清理未完成，建议开发继续处理后再提交复验。
