# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮完成了需求文档、测试用例、开发报告、关键代码差异与动态冒烟验证。核心功能链路基本已切换到 `ResolveGDBBin()` + GDB `generate-core-file`，`./maze --build` 与一个 prepare 主链路 testdata 用例均通过；但文档/测试数据说明仍存在多处继续指导用户使用系统 `gcore` 的残留，且预置的 Go 回归测试在当前环境因 `rocksdb/c.h` 缺失无法执行，导致回归约束未完成实证闭环，因此本轮验收结论为 FAIL。

## 详细结果

### TC-001: MAZE_GDB_BIN 优先级覆盖 portable gdb
- **结果**: 未执行
- **实际行为**: 对应命令 `go test ./prepare -run TestResolveGDBBinPrefersEnvOverrideOverPortable` 所在包在编译阶段即被 `github.com/linxGnu/grocksdb` 依赖阻塞，报错 `fatal error: rocksdb/c.h: No such file or directory`。
- **关键指标**: Go 测试未进入目标测试函数执行阶段。
- **备注**: 从静态代码看，`prepare/corefile.go` 已通过 `ResolveGDBBin()` 取 GDB 路径，但该 TC 未完成动态/单测验证。

### TC-002: portable gdb 存在时优先于系统 gdb
- **结果**: 未执行
- **实际行为**: 同 TC-001，受 `rocksdb/c.h` 缺失阻塞，目标单测无法运行。
- **关键指标**: Go 测试包编译失败。
- **备注**: 静态审查显示 `ResolveGDBBin()` 仍保持 `MAZE_GDB_BIN` > `./gdb/prim-gdb/gdb-portable` > `gdb` 优先级。

### TC-003: corefile.go 禁止重新引入系统 gcore 调用
- **结果**: PASS
- **实际行为**: 静态检查 `prepare/corefile.go`，未发现 `exec.Command("gcore"`；实际改为 `ResolveGDBBin()` + `buildGenerateCoreCommand()` + `exec.Command(command, args...)`。
- **关键指标**: `prepare/corefile.go` 第 55-57 行通过 resolved GDB 构造命令，第 60-61 行错误信息也已改为 GDB 语义。
- **备注**: 满足 AC1。

### TC-004: corefile.go 必须复用 ResolveGDBBin
- **结果**: PASS
- **实际行为**: `prepare/corefile.go` 第 55 行明确调用 `ResolveGDBBin()`；`prepare/gdb_server.go` 注释也补充说明 prepare 阶段所有 GDB 启动链路必须统一经过该入口。
- **关键指标**: `ResolveGDBBin()` 在 core dump 与 GDB Server 两条链路均被复用。
- **备注**: 满足 AC2。

### TC-005: corefile.go 必须构造 GDB generate-core-file/gcore 命令
- **结果**: PASS
- **实际行为**: `buildGenerateCoreCommand()` 构造参数为 `-q -n --batch -ex "attach <pid>" -ex "generate-core-file <path>" -ex detach -ex quit`。
- **关键指标**: `prepare/corefile.go` 第 17-26 行存在明确的 `generate-core-file` 指令拼装。
- **备注**: 与方案描述一致，满足通过 GDB 抓 core 的要求。

### TC-006: prepare 构建与主链路回归
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: 冒烟用例校验输出显示 `pymempool_objects = 68626`，并识别出 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500` 等关键对象。
- **备注**: 满足 AC5、AC6。

## 失败的 Test Case（如有）

### TC-DOC-001: 文档与测试数据说明应完成关键迁移，不再继续推荐系统 gcore
- **结果**: FAIL
- **预期行为**: 依据需求文档“逐步清理 `testdata/` 和文档里对系统 `gcore` 的提示”与开发计划 AC8，关键用户可见文档不应继续给出系统 `gcore <pid>` 作为当前推荐操作路径，至少不应与本次实现形成直接矛盾。
- **实际行为**:
  - `doc/maze_py_analysis.md` 第 100-103 行注释已改为“使用 GDB generate-core-file 生成 core dump”，但示例代码仍写着 `run("gcore %s" % pid)` 与 `mv core.%s`，文档自相矛盾。
  - `testdata/README.md` 第 33、49 行仍明确指导用户执行 `gcore <pid>` / `gcore -o ...`。
  - `testdata/python/20260129-complex-types/README.md` 第 68-72 行仍指导“在另一个终端生成 coredump: gcore <pid>”。
- **复现步骤**:
  1. 打开 `doc/maze_py_analysis.md`，查看第 96-103 行。
  2. 打开 `testdata/README.md`，查看第 26-55 行。
  3. 打开 `testdata/python/20260129-complex-types/README.md`，查看第 62-72 行。
- **可能原因**: 本轮仅更新了部分总览/设计文档，但未同步清理测试数据 README 与部分技术分析文档中的历史示例。
- **建议修复**: 统一将这些示例迁移为 `maze-gen-coredump.py` 或 Maze 当前 resolved GDB 抓 core 工作流，并修正文档中的伪代码示例，避免用户继续依赖系统 `gcore`。

## 未执行项
- TC-001、TC-002 对应的 Go 单元测试未完成执行。
  - **原因**: `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'` 在编译阶段被外部依赖阻塞，错误为 `fatal error: rocksdb/c.h: No such file or directory`。
  - **影响范围**: 影响 AC3、AC4 的自动化实证覆盖，也影响新增回归测试是否可在当前 CI/本地环境稳定运行的判断。
- 更高成本的扩展回归未继续执行。
  - **原因**: 已完成阶段 A 静态检查与阶段 B 关键 smoke test，但当前主要剩余风险集中在文档残留与 Go 单测环境阻塞，继续追加更多动态 testdata 用例对本 issue 的定位收益有限。
  - **影响范围**: 不影响对主链路是否成立的基本判断，但会降低对回归覆盖完整性的信心。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 功能实现层面，`prepare/corefile.go` 已不再直接调用系统 `gcore`，并已改为通过 `ResolveGDBBin()` 解析后的 GDB 执行 `attach + generate-core-file + detach + quit`。
- 构建与至少一个 prepare 主链路 testdata 用例通过，说明核心链路具备基本可用性。
- 文档与测试数据说明仍残留多处系统 `gcore` 用法，和本次实现目标不一致，属于需求完整性问题。
- 新增 Go 回归测试受外部 `rocksdb/c.h` 缺失阻塞，当前无法在本环境形成完整自动化验收闭环。

## Test Results
- `./maze --build`：通过。
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过。
- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`：失败，因 `rocksdb/c.h` 缺失导致包编译失败。

## 总结
- 通过: 4 / 7
- 失败: 1 / 7
- 未执行: 2 / 7
- 建议: **暂不建议以“完全验收通过”状态关闭本 issue。** 在修正文档/测试数据中的系统 `gcore` 残留，并补齐可运行的 Go 回归测试环境后，再做一次回归验收更稳妥。
