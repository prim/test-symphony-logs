## Summary

修复了 prepare 目标回归测试在无 RocksDB 头文件环境下无法编译的问题，并继续清理 testdata 文档里对系统 `gcore` 的直接指引，使本次 portable GDB 抓 core 改动能够完成自动化验证。

## Changes

- `common/refs/v1/refs.go`: 增加 `grocksdb_clean_link` build tag，避免无 RocksDB 环境误编译真实 v1 实现。
- `common/refs/v1/refs_benchmark_test.go`: 增加相同 build tag，保持 benchmark 与真实 v1 RocksDB 实现一致。
- `common/refs/v1/refs_norocksdb.go`: 新增无 RocksDB 环境下的 v1 兼容实现，内部复用 v0 内存索引逻辑，解除 `go test ./prepare` 对 `rocksdb/c.h` 的编译依赖。
- `testdata/README.md` 与多个 `testdata/**/README.md`: 将系统 `gcore` 用法改为通过 Maze 解析出的 GDB / portable GDB 抓 core 的说明。

## Testing

- `go test ./prepare -run 'TestResolveGDBBinPrefersEnvOverrideOverPortable|TestResolveGDBBinPrefersPortableBeforeSystem|TestCorefileGoDoesNotInvokeSystemGcore|TestCorefileGoUsesResolveGDBBin|TestCorefileGoBuildsGenerateCoreCommand|TestBuildGenerateCoreCommand'`：通过
- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
