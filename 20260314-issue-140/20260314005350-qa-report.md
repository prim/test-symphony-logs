# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本次围绕 issue #140 的 refs 门面化重构执行了静态结构核对、构建验证、`common/refs/...` 单元测试，以及 Python / Node.js 两组关键 testdata 回归。当前实现满足计划文档中的 AC1~AC6，未发现阻断发布的问题。

## 详细结果

### TC-001: refs 门面目录骨架落地
- **结果**: PASS
- **实际行为**: `go test ./qa/issue140 -count=1` 通过；`common/refs`、`common/refs/v0`、`common/refs/v1`、`compatcommon` 及对应测试文件均已落地。
- **关键指标**: `qa/issue140` 测试通过，目录骨架完整。
- **备注**: 与 AC1 对应的结构文件均存在。

### TC-002: 默认版本绑定与双 tag 冲突保护
- **结果**: PASS
- **实际行为**: 静态检查确认 `select_default.go` 默认绑定 `v1`，`select_v0.go` / `select_v1.go` 使用互斥 build tag，`select_conflict.go` 提供双 tag 编译期冲突保护。
- **关键指标**: `go test ./qa/issue140 -count=1` 通过；`common/refs/facade_contract_test.go` 与版本选择文件均存在。
- **备注**: 满足 AC2。

### TC-003: common 仅保留 refs 兼容转发层
- **结果**: PASS
- **实际行为**: `common/refs.go` 已从原 600+ 行主实现收敛为薄兼容层，核心逻辑迁移到 `common/refs/v1/refs.go`；业务包未直接导入 `common/refs/v0`、`common/refs/v1`、`common/refs/compatcommon`。
- **关键指标**: `git diff --stat origin/master...HEAD` 显示 `common/refs.go` 大幅缩减；`go test ./qa/issue140 -count=1` 通过。
- **备注**: 满足 AC3。

### TC-004: Node.js 与 txos 现有 refs 主链路不变
- **结果**: PASS
- **实际行为**: 静态检查确认 `nodejs` 仍通过稳定入口调用 refs，`txos/refs_shim.go` 保持兼容层；`./maze --build` 成功，`python3 testdata/run_test.py nodejs/20260211-02-collections` 通过。
- **关键指标**: Node.js 回归用例通过；`Map=1002`、`Set=1001`、`WeakMap=1000`、`WeakSet=1000`、`DataView=1000`、`ArrayBuffer=4629`。
- **备注**: 满足 AC4。

### TC-005: Python 历史轻量 refs 实现抽离到 common/refs/v0
- **结果**: PASS
- **实际行为**: `python/globals.go` 与 `python/traverse.go` 不再保留历史 refs 存储结构；新增 `python/refs_shim.go` 通过兼容桥接访问抽离后的实现；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: Python 回归用例通过；`pymempool_objects=68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`。
- **备注**: 满足 AC5。

### TC-006: refs 直接测试与回归基线就绪
- **结果**: PASS
- **实际行为**: `go test ./common/refs/...` 通过，且 Python / Node.js 至少各执行一组 testdata 回归并通过。
- **关键指标**: `maze/common/refs`、`maze/common/refs/v0`、`maze/common/refs/v1` 单元测试均通过。
- **备注**: 满足 AC6。

## 失败的 Test Case（如有）

无。

## 已执行的检查项

- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告
- 审查 `git diff origin/master...HEAD`
- 核对 `common/refs` 门面、`v0` / `v1` 目录、build tag 选择文件、兼容层边界
- 运行 `go test ./qa/issue140 -count=1`
- 运行 `go test ./common/refs/...`
- 运行 `./maze --build`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 运行 `python3 testdata/run_test.py nodejs/20260211-02-collections`
- 检查 `maze.log`

## 未执行项（如有）

- 未执行 Python 2.7、不同 glibc 版本、不同 Node.js 版本的额外兼容性矩阵
- 未执行大内存进程（>1GB）性能专项测试
- **原因**: 本轮计划与测试用例未要求扩展矩阵；关键 AC 已由静态检查、构建、单元测试和两组主链路 testdata 回归覆盖
- **可能影响的 AC**: 不影响 AC1~AC6 的当前通过结论，但会影响更广义兼容性/性能结论的完备性

## 日志检查

- `maze.log` 未发现 panic / fatal 级阻断问题
- 发现少量 GDB `No symbol ... in current context` 调试日志，属于既有探测过程中的容错输出，本次两组回归均未受影响
- `maze.py.log` 本轮未生成单独文件，未影响测试通过判定

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 当前实现满足 issue #140 的验收标准，可以进入后续合并流程；如需形成更完整发布信心，建议另行补充兼容性矩阵与性能专项验证。
