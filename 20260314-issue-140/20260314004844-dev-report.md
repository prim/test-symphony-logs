## Summary

COMPLETE：已完成 refs 门面化重构，新增 `common/refs` 稳定门面、`v0`/`v1` 版本实现、build tag 选择文件，并将 Python 历史轻量 refs 存储从 `python` 包中抽离到 `common/refs/v0`。`common/refs.go` 已收敛为兼容转发层，Node.js / txos 既有稳定入口保持不变。

## Changes

- `common/refs.go`: 将旧主实现替换为 `maze/common/refs` 门面兼容转发层
- `common/refs/facade.go`: 新增稳定门面 API 与 Python 兼容桥接入口
- `common/refs/types.go`: 新增门面共享类型别名
- `common/refs/shared/types.go`: 新增 hooks / mode / logger / pool 抽象
- `common/refs/select_default.go`: 默认绑定 `v1`
- `common/refs/select_v0.go`: `refs_v0` tag 绑定 `v0`
- `common/refs/select_v1.go`: `refs_v1` tag 绑定 `v1`
- `common/refs/select_conflict.go`: 双 tag 冲突编译保护
- `common/refs/compatcommon/compat.go`: 保留兼容目录骨架
- `common/refs/v0/refs.go`: 新增 Python 历史轻量 refs 实现
- `common/refs/v1/refs.go`: 新增当前 RocksDB 持久化 refs 实现
- `common/refs/facade_contract_test.go`: 新增门面绑定测试
- `common/refs/v0/refs_test.go`: 新增 v0 直接测试
- `common/refs/v1/refs_test.go`: 新增 v1 直接测试
- `python/globals.go`: 删除历史 refs 存储结构定义
- `python/traverse.go`: 删除历史 refs 存储实现，改用兼容桥接函数
- `python/python.go`: 初始化时重置抽离后的 Python refs 状态
- `python/refs_shim.go`: 新增 Python refs 兼容桥接
- `dev-log/2026-03-14-refs-facade-implementation.md`: 记录实现方案、自测与验收结论

## Testing

- `go test ./qa/issue140 -count=1` ✅
- `go test ./common/refs/...` ✅
- `./maze --build` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260211-02-collections` ✅

## 验收核对

- AC1: PASS — `common/refs`、`common/refs/v0`、`common/refs/v1` 结构已落地
- AC2: PASS — 默认绑定 `v1`，双 tag 冲突保护生效
- AC3: PASS — `common` 包仅保留 refs 兼容转发层
- AC4: PASS — Node.js / txos 现有 refs 入口与回归行为保持不变
- AC5: PASS — Python 历史实现已迁入 `common/refs/v0`
- AC6: PASS — `go test ./common/refs/...` 与 Python/Node.js 回归均通过

## 已完成项

- [x] 门面层与版本目录结构搭建
- [x] `common/refs.go` 兼容层收敛
- [x] Python 历史轻量实现抽离到 `common/refs/v0`
- [x] v1 持久化实现迁入 `common/refs/v1`
- [x] QA、构建、Python/Node.js 回归验证

## 未完成项

- 无

## 未验证项

- 无
