## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: refs 门面目录骨架落地
- **结果**: PASS
- **实际行为**: `go test ./qa/issue140 -run TestRefsFacadePackageScaffoldExists -count=1` 通过；已确认 `common/refs`、`common/refs/v0`、`common/refs/v1`、`common/refs/compatcommon` 以及门面/选择/测试文件均已落地。
- **关键指标**: `common/refs/**/*.go` 覆盖门面、build tag 选择、compat 桥接、v0/v1 实现与测试文件。
- **备注**: 结构与计划文档 AC1 一致。

### TC-002: 默认版本绑定与双 tag 冲突保护
- **结果**: PASS
- **实际行为**: `go test ./qa/issue140 -run 'TestRefsVersionSelectionFilesUseBuildTags|TestRefsFacadeExportsUniformGlobalFunctions' -count=1` 所属整组 QA 合约测试通过；静态检查确认：默认构建 `select_default.go` 绑定 `v1`，`select_v0.go` / `select_v1.go` 使用互斥 build tag，`select_conflict.go` 通过未定义类型 `mustChooseExactlyOneRefsBuildTag` 提供双 tag 编译期失败保护。
- **关键指标**: `common/refs/facade.go` 已统一暴露 `AddReverseIndex`、`AddIndexes`、`AddIndex`、`GetReferrers`、`GetReferents`、`Save`、`Check`、`Init` 等稳定入口；`go test ./qa/issue140 -count=1` 通过。
- **备注**: 默认构建绑定 v1，Python 兼容读写桥接仍由 facade 统一注入 v0 能力，符合首轮结构整理目标。

### TC-003: common 仅保留 refs 兼容转发层
- **结果**: PASS
- **实际行为**: `common/refs.go` 已不再承载主实现，仅通过 `maze/common/refs` 暴露的 `CommonCompat*` 桥接入口进行转发；`qa/issue140` 中薄兼容层与禁止业务层直接依赖版本包/compatcommon 的检查均通过。
- **关键指标**: `common/refs.go` 未发现旧主实现中的 `iTask`、`ReverseIndexMap`、`TReverseIndex`、`saveI/saveRI/QILoop/QRILoop/InitTxosRefDB` 等实现残留。
- **备注**: 当前 `common/refs.go` 的注释仍提到 `compatcommon` 字样，但仅为说明文字，代码依赖已收敛到 `maze/common/refs`。

### TC-004: Node.js 与 txos 现有 refs 主链路不变
- **结果**: PASS
- **实际行为**: 静态检查确认 `nodejs/scan.go`、`nodejs/maze.go` 仍使用 `GAddReverseIndex` / `GetReferrers` 稳定入口，`txos/refs_shim.go` 仍通过 `GAddReverseIndex` / `GetReferents` / `GetReferrers` 兼容接入；`./maze --build` 成功，`python3 testdata/run_test.py nodejs/20260211-02-collections` 通过。
- **关键指标**: Node.js 回归校验通过，关键对象数量包括 `Map=1002`、`Set=1001`、`WeakMap=1000`、`WeakSet=1000`、`DataView=1000`、`ArrayBuffer=4629`。
- **备注**: 主链路行为未见回归迹象。

### TC-005: Python 历史轻量 refs 实现抽离到 common/refs/v0
- **结果**: PASS
- **实际行为**: 静态检查确认 `python/globals.go` 已移除 `ReverseIndex` / `ReverseIndex1` / `ReverseIndex2` / `TwoUint64` 等历史存储结构，`python/traverse.go` 不再直接承载历史 refs 存储实现；`python/refs_shim.go` 改为经 `maze/common` 兼容入口转发；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: Python 回归中 `pymempool_objects=68626`，`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`，`Player=500`，`Config=300`。
- **备注**: 已满足 AC5 的“抽离实现但首轮不强切行为”要求。

### TC-006: refs 直接测试与回归基线就绪
- **结果**: PASS
- **实际行为**: `go test ./common/refs/...` 通过；`common/refs/facade_contract_test.go`、`common/refs/v0/refs_test.go`、`common/refs/v1/refs_test.go` 存在且可运行；Python 与 Node.js 至少各执行一组 testdata 回归并通过。
- **关键指标**: `go test ./common/refs/...` 输出：`maze/common/refs`、`maze/common/refs/v0`、`maze/common/refs/v1` 均通过。
- **备注**: AC6 满足。

## Findings

- 未复现此前 QA 重试中提到的兼容层边界问题；本轮代码已将 `common/refs.go` 收敛为仅依赖 `maze/common/refs` 的单一桥接层，相关 QA 合约测试、单测与 testdata 回归均通过。
- `common/refs/select_default.go` 默认将主 refs 实现绑定到 `v1`，同时保留 Python 轻量历史索引到 `v0` 的兼容注入，符合需求中“默认 v1 + Python 首轮不强切行为”的拆分策略。

## Test Results

- `go test ./qa/issue140 -count=1` ✅
- `./maze --build` ✅
- `go test ./common/refs/...` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260211-02-collections` ✅

## 失败的 Test Case（如有）

- 无

## 未执行项（如有）

- 未执行跨 Python 2.7 / 多 glibc / 多 Node.js 版本矩阵验证；本轮仅按 AC6 最低要求完成至少一组 Python 与 Node.js testdata 回归。
- 未单独构造 `refs_v0 + refs_v1` 双 tag 编译失败命令做额外命令级验证；已由 `qa/issue140` 静态合约测试覆盖编译期冲突保护文件存在性与约束。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 当前实现满足计划文档 AC1-AC6，可通过本轮 QA 验收；后续如要提高发布信心，建议补充多版本兼容性与 build tag 组合的命令级验证。
