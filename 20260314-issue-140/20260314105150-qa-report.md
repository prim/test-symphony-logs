# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略完成验收。静态结构检查、`go test ./common/refs/...`、`./maze --build`，以及两组 testdata 回归（Python 与 Node.js）均通过，未发现阻断发布的问题。

## 详细结果

### TC-001: refs 门面目录骨架落地
- **结果**: PASS
- **实际行为**: `common/refs`、`common/refs/v0`、`common/refs/v1`、`common/refs/compatcommon` 目录与关键文件均存在；`qa/issue140` 静态测试通过。
- **关键指标**: `go test ./qa/issue140 -count=1` 通过
- **备注**: 目录结构与计划文档 AC1 一致。

### TC-002: 默认版本绑定与双 tag 冲突保护
- **结果**: PASS
- **实际行为**: `common/refs/select_default.go` 使用 `!refs_v0 && !refs_v1`，默认绑定 `v1`；`select_conflict.go` 存在编译期冲突保护符号；`facade.go` 暴露统一 API。
- **关键指标**: `go test ./qa/issue140 -count=1` 通过
- **备注**: 满足 AC2。

### TC-003: common 仅保留 refs 兼容转发层
- **结果**: PASS
- **实际行为**: `common/refs.go` 仅保留对 `maze/common/refs` 的薄转发；主实现已迁入 `common/refs/v1/refs.go`；未发现业务包直接依赖 `common/refs/v0` / `common/refs/v1`。
- **关键指标**: `go test ./qa/issue140 -count=1` 通过
- **备注**: 满足 AC3。

### TC-004: Node.js 与 txos 现有 refs 主链路不变
- **结果**: PASS
- **实际行为**: 静态检查显示 Node.js/txos 继续通过稳定入口接入 refs；`./maze --build` 成功；`python3 testdata/run_test.py nodejs/20260211-02-collections` 通过。
- **关键指标**: Node.js 用例中 `Map=1002`、`Set=1001`、`WeakMap=1000`、`WeakSet=1000`、`DataView=1000`、`ArrayBuffer=4629`
- **备注**: 满足 AC4。根据 human review 要求，本轮实际执行了多个 testdata 用例。

### TC-005: Python 历史轻量 refs 实现抽离到 common/refs/v0
- **结果**: PASS
- **实际行为**: `python/globals.go` 中已无历史 `ReverseIndex1` / `ReverseIndex2` / `ReverseIndex` 存储定义；Python 历史轻量实现位于 `common/refs/v0/refs.go`；`python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- **关键指标**: `pymempool_objects=68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`
- **备注**: 满足 AC5，且保持“首轮不强切行为”的要求。

### TC-006: refs 直接测试与回归基线就绪
- **结果**: PASS
- **实际行为**: `go test ./common/refs/...` 全部通过；Python 与 Node.js 两组 testdata 回归均通过。
- **关键指标**: `go test ./common/refs/...` 通过；2/2 动态回归通过
- **备注**: 满足 AC6。

## 失败的 Test Case（如有）

无。

## 已执行的检查项

- 阅读需求文档 `dev-flow/plan.md`
- 阅读 test case 报告 `dev-flow/test-cases.md`
- 阅读最新开发报告 `dev-flow/20260314104849-dev-report.md`
- 检查 `origin/master...HEAD` 改动范围
- 执行 `go test ./qa/issue140 -count=1`
- 阅读并核对以下关键文件：
  - `common/refs.go`
  - `common/refs/facade.go`
  - `common/refs/select_default.go`
  - `common/refs/select_conflict.go`
  - `common/refs/v0/refs.go`
  - `common/refs/compatcommon/compat.go`
  - `common/refs/compat_bridge.go`
  - `python/globals.go`
  - `python/traverse.go`
  - `python/refs_shim.go`
- 执行 `go test ./common/refs/...`
- 执行 `./maze --build`
- 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 执行 `python3 testdata/run_test.py nodejs/20260211-02-collections`
- 检查 `maze.log`

## 未执行项（如有）

- 未单独执行更多扩展 testdata（如额外 Node.js / C++ / 多版本兼容场景）
  - **原因**: 本轮需求核心是验证 issue #140 的 refs 门面化重构；阶段 A/B 与代表性 Python/Node.js 主链路回归均已通过，未发现继续扩大高成本验证的阻塞或必要性。
  - **可能影响范围**: 主要是更广泛兼容性与性能覆盖，不影响本轮对 AC1-AC6 的结论。
- 未读取 `maze.py.log`
  - **原因**: 本轮执行后未生成该文件路径。
  - **可能影响范围**: 不影响已通过的 testdata 结果判断；若后续需要追查 GDB Python 层异常，可再专项检查。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `go test ./qa/issue140 -count=1` ✅
- `go test ./common/refs/...` ✅
- `./maze --build` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260211-02-collections` ✅

## Findings

- 未发现新的功能性回归或结构性遗漏。
- human review 强调的“QA 需要执行多个 testdata 进行测试”已满足，本轮实际执行了 Python 与 Node.js 两组 testdata 回归。
- `common/refs/v0/refs.go` 现已去除误导性的包级配置持久状态；从当前验收结果看，不影响门面层统一接口与 Python 历史行为兼容。

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以通过本轮 QA 验收；若进入后续迭代，建议再补充 Python 2.7、更多 Node.js 版本与性能场景的扩展验证，但这不构成本轮发布阻塞。
