# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #164 的 refs v2（BitalosDB 后端）实现进行复验，重点关注上一轮发现的默认 `refs_v1` 路径命名漂移问题，以及 `v1` / `v2` 共存时的中性命名兼容性。静态检查、构建、定向单测与至少一个真实 `testdata` 冒烟用例均通过，未发现新的阻断性问题。

## 详细结果

### TC-001: refs v2 结构文件与门面绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 将门面函数正确绑定到 `v2` 实现，`currentVersion = "v2"`。
- **关键指标**: QA 静态测试 `TestRefsV2ScaffoldAndBuildSelectionExist` 通过。
- **备注**: 满足 AC1、AC2。

### TC-002: 默认版本保持 v1，build tag 互斥规则正确
- **结果**: PASS
- **实际行为**: `select_default.go` 默认仍排除 `refs_v0/refs_v1/refs_v2` 并指向 `v1`；`select_v0.go`、`select_v1.go` 均新增 `!refs_v2` 约束；`select_conflict.go` 已扩展为禁止任意两个 refs tag 同开。
- **关键指标**: QA 静态测试 `TestRefsBuildTagSelectionSupportsV2AndKeepsDefaultV1` 通过。
- **备注**: 满足 AC3、AC8。

### TC-003: CLI 支持 `--refs v2`，构建能切换到 refs_v2
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 可识别 `2/v2/refs_v2`；`build_maze()` 可追加 `refs_v2`；`./maze --build --refs v2` 实际构建成功。
- **关键指标**: `./maze --build` 通过；`./maze --build --refs v2` 通过。
- **备注**: 满足 AC4、AC5。

### TC-004: hooks 中性命名与 v1/v2 路径兼容性正确
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 同时保留 `RefsDBPath` 与 `RocksDBPath`；`common/refs.go` 仅在 `refs.CurrentVersion() == "v2"` 时注入 `RefsDBPath`，默认 `v1` 继续走 `RocksDBPath`。
- **关键指标**: `go test ./common` 通过；`TestRefsRuntimeHooksPathSelectionByVersion` 通过。
- **备注**: 满足 AC6、AC7，也符合 human guidance“中性命名同时支持 v1 v2”。

### TC-005: refs v2 使用 BitalosDB 且保留薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 依赖 `github.com/zuoyebang/bitalosdb`，未引入 `grocksdb`；存在 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数；对外仍暴露 `GetReferents`、`GetReferrers`、`Save`、`Check`。
- **关键指标**: QA 静态测试 `TestRefsV2UsesBitalosDBWithThinStorageAdapter` 通过；`go test ./common/refs/...` 通过。
- **备注**: 满足 AC9、AC10。

### TC-006: 默认 refs 路径回归与 Save 并发保护修复验证
- **结果**: PASS
- **实际行为**: 本轮新增的 `common/refs_runtime_hooks_test.go` 能区分 `v1` 与 `v2` 的路径注入策略；`common/refs/core/core_test.go` 覆盖 Save 后 map 重建及 Save 期间并发写入拒绝逻辑，测试通过。
- **关键指标**: `go test ./common/refs/...` 通过，其中 `maze/common/refs/core` 通过且耗时约 2s。
- **备注**: 该项对应“聚焦上轮问题”的回归验证，未发现回退。

### TC-007: 真实 testdata 冒烟验证（默认 refs）
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，`All validations passed!`。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`。
- **备注**: 满足“执行验收至少运行一个 testdata 用例”的要求。

### TC-008: 真实 testdata 冒烟验证（refs=v2）
- **结果**: PASS
- **实际行为**: `./maze --tar ... --refs v2 --text --json-output ...` 执行成功，输出包含 `system versions: mpm=v2 refs=v2 vm=full`；随后 `validate.py` 校验通过。
- **关键指标**: 同一份 Python 3.11 测试数据下，结果校验通过；运行日志中可见 `open bitalosdb success`。
- **备注**: 证明 `refs v2` 主链路可运行，且不会破坏该关键 Python 用例的结果。

## Test Results

- `./maze --build`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`：通过
- `./maze --build --refs v2`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500 && python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`：通过

## 失败的 Test Case（如有）

无。

## 未执行项

- Python 2.7 兼容性回归未执行；本轮仅验证了 Python 3.11 现有关键用例。
- 不同 glibc 版本兼容性未执行；当前环境仅覆盖单一 Linux 环境。
- 不同 Node.js 版本兼容性未执行；本轮聚焦 refs v2 与 Python 主链路。
- 大内存性能验证未执行；未进行 >1GB 目标进程的扫描耗时与内存占用测量。
- `refs=v1` 与 `refs=v2` 的逐项查询结果精确对照未单独展开；本轮以静态结构验证与关键 smoke test 为主。

上述未执行项不会推翻本轮“结构落位 + 主链路可运行 + 上轮问题已修复”的结论，但仍可能影响更广泛的兼容性与性能结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 就 issue #164 当前 AC 与“聚焦上一轮问题”的回归范围而言，可以判定通过；后续若准备默认切换或扩大发布范围，建议补做跨版本兼容性、`v1/v2` 结果对照与性能评估。
