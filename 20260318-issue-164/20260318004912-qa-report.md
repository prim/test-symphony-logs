# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #164 进行了需求、测试用例、最新开发报告、代码差异、构建、Go 测试和真实 `testdata` 用例的复验。此前 QA 发现的 `refs v2` 仍使用 `txos-rocksdb-*` 路径命名问题已经修复：运行时 hooks 现优先注入中性的 `RefsDBPath`，实际日志显示 `bitalosdb path ./db/.../txos-refsdb-ref` 与 `.../txos-refsdb-rref`。同时默认版本仍为 `v1`、`--refs v2` 构建/运行链路正常、`v2` 的 BitalosDB 薄适配层存在，且至少一个真实 `testdata` 用例与一个 `refs v2` smoke test 均通过，因此本轮结论为 **PASS**。

## 详细结果

### TC-001: refs v2 结构文件与门面绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在，`select_v2.go` 正确将 facade 绑定到 `v2` 实现。
- **关键指标**: `currentVersion = "v2"`，`AddIndexes` / `AddReverseIndex` / `AddIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` / `Init` / `Configure` / `SetMode` 均已接线。
- **备注**: 满足 `common/refs/v2` 与 `select_v2.go` 的结构落位要求。

### TC-002: build tag 选择与默认版本行为正确
- **结果**: PASS
- **实际行为**: `select_default.go` 保持默认选择 `v1`；`select_conflict.go` 已禁止任意两个 refs build tag 同开；`select_v2.go` 仅在 `refs_v2` 且未启用 `refs_v0/refs_v1` 时生效。
- **关键指标**: `select_default.go` 中为 `!refs_v0 && !refs_v1 && !refs_v2`；`select_conflict.go` 覆盖三组冲突组合。
- **备注**: 符合需求中“默认仍为 v1、显式支持 refs_v2”的版本策略。

### TC-003: `maze` CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2` / `v2` / `refs_v2`；`build_maze()` 支持追加 `refs_v2` build tag。
- **关键指标**: `./maze --build --refs v2` 通过；`./maze --tar ... --refs v2` 输出 `system versions: mpm=v2 refs=v2 vm=full`。
- **备注**: CLI 参数解析与构建链路正常。

### TC-004: Hooks 命名泛化与运行时命名收敛
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 已新增 `RefsDBPath`；`common/refs.go` 中 `RefsDBPath` 使用 `TxosRefsDBFileName`，`RocksDBPath` 保留兼容；运行 `refs v2` 后 `maze.log` 中记录为 `txos-refsdb-*` 路径。
- **关键指标**: `maze.log` 第 224-225 行为 `bitalosdb path ./db/coredump-default-2636550/txos-refsdb-ref` 与 `.../txos-refsdb-rref`，未再出现 `txos-rocksdb-*`。
- **备注**: 上一轮失败点已修复，满足命名/日志收敛要求。

### TC-005: refs v2 使用 BitalosDB 且包含薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 使用 `github.com/zuoyebang/bitalosdb`，且包含 `openDB`、`rangeScan`、`writeIndexBatch`、`writeReverseBatch`、`Flush`、`Close` 等薄适配层实现。
- **关键指标**: `grep` 命中 `openDB` / `rangeScan` / `writeIndexBatch` / `writeReverseBatch`；未见 `grocksdb` 依赖。
- **备注**: 满足“BitalosDB 后端 + 薄适配层”的 AC。

### TC-006: refs 相关 Go 测试回归
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...` 通过。
- **关键指标**: `common/refs`、`common/refs/core`、`common/refs/v0`、`common/refs/v1` 全部通过。
- **备注**: 共享核心与兼容层改动未引入回归。

### TC-007: issue164 静态 QA 测试
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...` 通过。
- **关键指标**: `ok   maze/qa/issue164`。
- **备注**: 结构性 AC 能被仓库内 QA 用例识别并验证。

### TC-008: Python 真实 testdata 用例 smoke test（默认 refs）
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，验证脚本输出 `All validations passed!`。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`；`TreeNode = 600`；`SimpleClass = 500`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-009: Python 真实 coredump 在 refs v2 下的直接运行 smoke test
- **结果**: PASS
- **实际行为**: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500` 成功运行，随后 `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json` 校验通过。
- **关键指标**: 控制台输出 `system versions: mpm=v2 refs=v2 vm=full`；`maze.log` 出现 `bitalosdb path .../txos-refsdb-ref` 与 `.../txos-refsdb-rref`；结果校验 `All validations passed!`。
- **备注**: 证明 `refs v2` 主链路可运行且结果与现有 Python testdata 校验兼容。

### TC-010: refs 核心 Save 生命周期保护回归
- **结果**: PASS
- **实际行为**: `common/refs/core/core_test.go` 中新增用例覆盖了 `Save()` 后索引 map 可继续写入，以及 `Save()` 期间并发写入会被 `errSaveInProgress` 拒绝。
- **关键指标**: 测试断言 `flushCount > 0` 且 `errors.Is(gotErr, errSaveInProgress)` 成立。
- **备注**: 属于针对本轮共享核心抽取的补充低成本静态/单测核对。

## 已执行的检查项

- 阅读最新开发报告 `dev-flow/20260318004553-dev-report.md`
- 阅读测试用例 `dev-flow/test-cases.md`
- 阅读需求文档 `dev-flow/plan.md`
- 查看 `git diff origin/master...HEAD`
- 静态检查 `common/refs.go`、`common/leveldb.go`、`common/refs/shared/types.go`、`common/refs/select_default.go`、`common/refs/select_conflict.go`、`common/refs/select_v2.go`、`common/refs/v2/refs.go`、`common/refs/core/core.go`、`common/refs/core/core_test.go`、`maze`
- 运行 `./maze --build`
- 运行 `./maze --build --refs v2`
- 运行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`
- 运行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 运行 `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`
- 运行 `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`
- 检查 `maze.log` 中 BitalosDB / refs DB 路径命名

## 未执行项

- Python 2.7 动态兼容性未执行
- 不同 glibc 版本兼容性未执行
- 不同 Node.js 版本兼容性未执行
- `refs=v1` 与 `refs=v2` 引用查询结果逐项对照专项回归未执行
- 大内存进程（>1GB）性能验证未执行

未执行原因：本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”的策略，优先复核上轮失败点并确认关键 AC、主链路构建和代表性 smoke test。上述项目不阻断本轮对关键功能等价迁移的验收结论，但会影响兼容性与性能覆盖面的完整性。

可能影响范围：主要影响兼容性、性能与更细粒度一致性评估，不影响本轮对 issue #164 当前实现满足核心 AC 的 PASS 结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 上轮 QA 指出的 `refs v2` RocksDB 命名残留已修复：运行时路径已从 `txos-rocksdb-*` 收敛为 `txos-refsdb-*`。
- `refs_v2` 版本包、build tag 选择、`--refs v2` CLI 支持、BitalosDB 后端与薄适配层均已落地。
- 共享核心抽取后的 Go 测试、静态 QA 测试及代表性 Python 真实 testdata 均通过，说明主链路可用。

## Test Results
- `./maze --build`：通过
- `./maze --build --refs v2`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`：通过
- `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`：通过

## 总结
- 通过: 10 / 10
- 失败: 0 / 10
- 建议: 当前实现已满足 issue #164 的核心验收要求，可以通过本轮 QA；后续建议补充 `refs v1/v2` 结果对照、跨版本兼容性与性能专项验证，以完善非阻断型覆盖。
