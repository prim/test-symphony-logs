## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings

- 已确认 `common/refs/v2/refs.go`、`common/refs/select_v2.go`、`refs_v2` build tag、`maze --refs v2` 入口支持均已落位。
- 已确认默认版本仍保持 `v1`，`select_conflict.go` 已扩展为禁止任意两个 refs tag 同开。
- 已确认 human review 要求的“不要抽象 core”已落实：`common/refs/core/` 不存在，`v1` 与 `v2` 为独立实现。
- 已确认 `v2` 使用 `BitalosDB`，保留 `openDB` / `writeIndexBatch` / `writeReverseBatch` / `rangeScan` 薄适配层。
- 已确认运行时 hooks 采用中性 `RefsDBPath`，同时保留 `RocksDBPath` 兼容字段；`v2` 注入 `RefsDBPath`，`v1` 保持旧路径语义稳定。
- 已完成关键 smoke test：默认 refs 与 `refs=v2` 在 `python/20260129-complex-types-311` 用例上均通过。

## 详细结果

### TC-001: refs v2 结构落位与门面绑定
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 将 facade 绑定到 `v2` 的 `AddReverseIndex`、`AddIndexes`、`AddIndex`、`GetReferrers`、`GetReferents`、`Save`、`Check`、`Init`、`Configure`、`SetMode`。
- **关键指标**: `currentVersion = "v2"`
- **备注**: 满足 AC1、AC2。

### TC-002: 默认版本与多 tag 冲突保护
- **结果**: PASS
- **实际行为**: `select_default.go` 仅在 `!refs_v0 && !refs_v1 && !refs_v2` 下启用，默认仍指向 `v1`；`select_conflict.go` 已覆盖 `(refs_v0 && refs_v1) || (refs_v0 && refs_v2) || (refs_v1 && refs_v2)`。
- **关键指标**: 默认版本维持 `v1`；冲突组合覆盖 3 组。
- **备注**: 满足 AC3、AC8。

### TC-003: maze CLI 支持 refs v2 且按版本决定 RocksDB 依赖
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `v2` / `refs_v2`；`build_maze()` 在 `refs_version == "v2"` 时追加 `refs_v2` tag 与 `GOEXPERIMENT=arenas`，并通过 `refs_requires_rocksdb()` 仅在默认/v1 下启用 RocksDB 依赖。
- **关键指标**: `.maze.build.meta` 显示 `{'mpm_version': 'v2', 'refs_version': 'v2', 'rocksdb_prefix': ''}`。
- **备注**: 满足 AC4、AC5，并验证了入口层对 RocksDB 的解耦。

### TC-004: hooks 中性命名与运行时路径注入
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 已新增 `RefsDBPath`；`common/refs.go` 中 `refsRuntimeHooks()` 保留 `RocksDBPath`，并仅在 `CurrentVersion() == "v2"` 时注入 `RefsDBPath`；`common/refs_runtime_hooks_test.go` 覆盖 `v0/v1/v2` 的路径选择断言。
- **关键指标**: `v2` 使用 `txos-refsdb-ref`；`v1` 保持 `txos-rocksdb-ref`。
- **备注**: 满足 AC6、AC7，也符合“中性命名同时支持 v1 v2”的人工要求。

### TC-005: refs v2 使用 BitalosDB 且保留薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 直接引入 `github.com/zuoyebang/bitalosdb`，未直接依赖 `grocksdb`；存在 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan`，并在 `rangeScan()` 中检查 `iter.Error()` 与 `iter.Close()` 错误。
- **关键指标**: `openDB` 使用 `bitalosdb.Open()`；批量提交使用 `batch.Commit()`；查询使用 `db.NewIter()`。
- **备注**: 满足 AC9、AC10。

### TC-006: 关键构建与 Go 回归测试
- **结果**: PASS
- **实际行为**:
  - `./maze --build` 通过
  - `./maze --build --refs v2` 通过
  - `go test ./qa/issue164/...` 通过
  - `GOEXPERIMENT=arenas go test -tags refs_v2 ./common/refs/... ./common -run TestRefsRuntimeHooksPathSelectionByVersion` 通过
  - 不带 `GOEXPERIMENT=arenas` 的 `go test -tags refs_v2 ./common/refs/...` 也可通过，说明 `noarenas.go` 与 `v1` 兜底路径工作正常
- **关键指标**: 相关测试命令全部返回 0。
- **备注**: 覆盖了近期 dev report 中提到的 arenas 构建前提与包级测试可运行性问题。

### TC-007: Python 主链路 smoke test（默认 refs）
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 后通过，`pymempool_objects = 68626`，各类对象计数符合预期。
- **关键指标**:
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
  - `Player = 500`
  - `Config = 300`
  - `Point = 400`
  - `Rectangle = 300`
  - `deque = 300`
  - `set objects = 2310`
- **备注**: 证明默认稳定主链路未被本次改动破坏。

### TC-008: Python 主链路 smoke test（refs v2）
- **结果**: PASS
- **实际行为**: 执行 `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311` 后通过，关键统计结果与默认 refs 一致。
- **关键指标**: 关键对象计数与 TC-007 一致，均满足验证脚本要求。
- **备注**: 证明 `refs=v2` 主链路可运行，且在该 smoke case 上未观察到行为回归。

## 失败的 Test Case（如有）

- 无。

## 未执行项（如有）

- Python 2.7 兼容性动态测试未执行。
- 不同 glibc 版本兼容性矩阵未执行。
- 不同 Node.js 版本兼容性矩阵未执行。
- 大内存进程（>1GB）性能与内存占用测试未执行。

未执行原因：本轮按“低成本检查尽量完整 + 高成本验证分阶段执行”策略完成了静态核对、构建验证、Go 回归测试与至少一个关键 testdata 用例；上述跨版本/性能矩阵不属于当前分支最核心阻断项，且仓内未见对应本轮新增自动化矩阵。

可能影响范围：主要影响兼容性与性能类扩展结论，不影响本次对 issue #164 结构落位、版本选择、入口切换、BitalosDB 后端替换和 smoke 主链路成立性的通过结论。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：PASS
- `./maze --build --refs v2`：PASS
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：PASS
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache GOEXPERIMENT=arenas go test -tags refs_v2 ./common/refs/... ./common -run TestRefsRuntimeHooksPathSelectionByVersion`：PASS
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 ./common/refs/...`：PASS
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 ./common -run TestRefsRuntimeHooksPathSelectionByVersion`：PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
- `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## 总结

- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本轮 QA。就 issue #164 的核心验收范围而言，`refs v2` 的结构、构建入口、BitalosDB 后端切换、human review 强约束以及关键 smoke 主链路均已满足；后续如需发布级信心，建议补充兼容性矩阵与性能基准测试。
