# 开发报告

## 完成的任务
- [x] Task 1: 新增 `common/refs/v2`，基于 BitalosDB 实现 refs v2 存储后端，并保留与 v1 一致的门面能力。
- [x] Task 2: 新增 `common/refs/select_v2.go`，扩展 refs build tag 选择与冲突保护，默认版本仍保持为 `v1`。
- [x] Task 3: 扩展 `maze --refs` 版本识别，支持 `2`、`v2`、`refs_v2`，并在 refs v2 构建时自动注入 `GOEXPERIMENT=arenas`。
- [x] Task 4: 将 hooks 路径命名从 RocksDB 专有语义泛化为 `RefsDBPath`，同时保留 `RocksDBPath` 兼容回退。
- [x] Task 5: 补充 refs 文档，说明 v2 后端、路径命名和版本选择方式。

## 修改的文件
- `common/refs/v2/refs.go`: 新增 refs v2 实现，使用 BitalosDB，补齐 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数。
- `common/refs/select_v2.go`: 新增 refs v2 的门面绑定。
- `common/refs/select_default.go`: 默认选择逻辑排除 `refs_v2`，继续默认绑定 `v1`。
- `common/refs/select_v0.go`: 增加对 `refs_v2` 的互斥约束。
- `common/refs/select_v1.go`: 增加对 `refs_v2` 的互斥约束。
- `common/refs/select_conflict.go`: 扩展任意两个 refs tag 同开时的冲突保护。
- `common/refs/shared/types.go`: hooks 新增 `RefsDBPath` 字段，保留 `RocksDBPath`。
- `common/refs.go`: 注入 `RefsDBPath`，并将注释改为 refs DB 中性命名。
- `common/refs/v1/refs.go`: 优先读取 `RefsDBPath`，为空时回退到 `RocksDBPath`。
- `maze`: 支持 refs v2 运行参数与构建 tag，并在构建 refs v2 时自动补充 `GOEXPERIMENT=arenas`。
- `doc/refs.md`: 增补 refs v2 设计说明、版本矩阵、Hooks 命名说明和构建示例。
- `go.mod`: 引入 `github.com/zuoyebang/bitalosdb` 及其所需模块依赖。
- `go.sum`: 补充 refs v2/BitalosDB 相关校验和。

## 自测结果
- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
- 测试结果: 通过。

- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`
- 测试结果: 通过。

- 测试命令: `GOEXPERIMENT=arenas GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 -mod=mod ./common/refs/...`
- 测试结果: 通过。

- 测试命令: `./maze --build`
- 测试结果: 通过。

- 测试命令: `./maze --build --refs v2`
- 测试结果: 通过。

- 测试命令: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2`
- 测试结果: 通过，输出中确认 `system versions: mpm=v2 refs=v2 vm=full`，BitalosDB 正常启动。

- 测试命令: `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`
- 测试结果: 通过，所有校验项均成功。

## 验收核对
- AC1: PASS 新增 `common/refs/v2/refs.go`，并使用 `github.com/zuoyebang/bitalosdb`。
- AC2: PASS 新增 `common/refs/select_v2.go`，门面已绑定到 v2。
- AC3: PASS 默认 refs 版本仍为 `v1`，`select_default.go` 已排除 `refs_v2`。
- AC4: PASS `select_conflict.go` 已覆盖 `refs_v0`、`refs_v1`、`refs_v2` 的两两冲突保护。
- AC5: PASS `maze --refs v2` 已支持 `2`、`v2`、`refs_v2` 等别名。
- AC6: PASS hooks 已新增 `RefsDBPath`，并保留 `RocksDBPath` 兼容路径。
- AC7: PASS `common/refs/v1/refs.go` 已优先读取 `RefsDBPath` 并兼容回退。
- AC8: PASS refs v2 已包含 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 薄适配函数。
- AC9: PASS refs v2 构建、静态 QA、自身测试和 Python 端到端验证均已通过。
- AC10: PASS 当前 key/value 编码保持为 uint64 小端序列，与 v1 的门面语义一致。

## 剩余工作
- 未完成项: 无。
- 未验证项: 无。

## Self-Review 发现与修复
- 发现 refs v1 仍只接受 `RocksDBPath`，已修复为优先使用 `RefsDBPath`，避免命名泛化后 v1/v2 行为不一致。
- 发现 refs v2 依赖 `GOEXPERIMENT=arenas`，若仅添加 build tag 会导致构建失败；已在 `maze` 构建流程中自动补齐，避免用户手工设置遗漏。
- 检查了 `go.mod`/`go.sum` 的额外依赖改动，当前改动来自 BitalosDB 及其传递依赖，且在 refs v2 编译/测试中确认为必需，未继续收缩以避免破坏可构建性。

## 注意事项
- BitalosDB 当前依赖 Go arenas 实验特性，因此 refs v2 不是普通 `-tags refs_v2` 就能独立构建的后端，必须同时具备 `GOEXPERIMENT=arenas`。
- 本轮未修改任何 `testdata/` 下测试文件，也未提交开发计划、测试设计或本报告文件。
- refs v2 采用 build tag 隔离，因此默认构建路径不会引入 BitalosDB 运行时代码。

## 结论
- COMPLETE: 所有 AC 已满足且已完成验证。
