## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings
- 已完成 issue #164 的关键验收点核对：`common/refs/v2` 已落位，`select_v2.go`、`refs_v2` build tag、`maze --refs v2`、默认版本保持 `v1`、冲突 tag 保护、`RefsDBPath` 中性命名兼容，以及 v2 基于 BitalosDB 的薄存储适配层均已存在且可工作。
- 关键动态验证通过：默认 refs 与 `refs=v2` 均可成功构建；`python/20260129-complex-types-311` 在默认 refs 与 `refs=v2` 下都通过，说明主链路未因后端切换发生明显回归。
- 日志核对通过：`maze.log` 中本次 `refs=v2` 运行记录为 `bitalosdb path ./db/.../txos-refsdb-*`，符合“中性命名同时支持 v1/v2”的要求，未见错误的 RocksDB 路径命名残留在 v2 主链路日志中。
- 额外观察：直接运行未注入 RocksDB 头文件/库路径的 `go test ./common/refs/...` 会因 `rocksdb/c.h` 缺失失败；在按仓库当前构建方式注入 RocksDB 环境并加 `grocksdb_clean_link` tag 后测试通过。该现象与本 issue 的 refs v2 实现正确性无直接冲突，但说明默认裸跑 `go test ./common/refs/...` 仍依赖外部 RocksDB 环境。

## 详细结果

### TC-001: refs v2 结构文件与版本绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 将 facade 绑定到 `v2` 的 `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` / `Init` / `Configure` / `SetMode`。
- **关键指标**: `currentVersion = "v2"`
- **备注**: 静态检查与 `qa/issue164/refs_v2_bitalosdb_test.go` 一致。

### TC-002: 默认版本仍为 v1，且 build tag 冲突保护覆盖 refs_v2
- **结果**: PASS
- **实际行为**: `select_default.go` 使用 `!refs_v0 && !refs_v1 && !refs_v2`；`select_v1.go` 排除了 `refs_v2`；`select_conflict.go` 已覆盖 `(refs_v0 && refs_v2)`、`(refs_v1 && refs_v2)` 等组合。
- **关键指标**: 默认 `currentVersion = "v1"`
- **备注**: 满足“默认不切换到 v2”的验收要求。

### TC-003: maze CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 的 `normalize_refs_version()` 已识别 `2` / `v2` / `refs_v2`；`build_maze()` 在 `refs_version == "v2"` 时追加 `refs_v2` tag，并注入 `GOEXPERIMENT=arenas`。
- **关键指标**: `./maze --build --refs v2` 实际执行成功
- **备注**: CLI 到构建入口链路打通。

### TC-004: Hooks 使用中性 `RefsDBPath`，并兼容 `RocksDBPath`
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 同时包含 `RefsDBPath` 与 `RocksDBPath`；`common/refs/core/core.go` 通过 `refsDBPathFunc()` 优先使用 `RefsDBPath`，否则回退 `RocksDBPath`；`common/refs.go` 在 `refs=v2` 时注入 `TxosRefsDBFileName`，其余版本保留 `TxosRocksDBFileName`。
- **关键指标**: 路径选择顺序为 `RefsDBPath` > `RocksDBPath`
- **备注**: 满足 human review 提到的“中性命名同时支持 v1 v2”。

### TC-005: refs v2 使用 BitalosDB 且具备薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 依赖 `github.com/zuoyebang/bitalosdb`，未依赖 `grocksdb`；存在 `openDB` / `writeIndexBatch` / `writeReverseBatch` / `rangeScan` 等薄适配函数，对外仍导出与 v1 对齐的 API。
- **关键指标**: 适配函数 4 个全部存在
- **备注**: 业务逻辑下沉到 `common/refs/core`，v1/v2 仅保留后端差异。

### TC-006: 关键 smoke test —— 默认 refs 构建与 testdata 用例
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`python3 testdata/run_test.py python/20260129-complex-types-311` 成功。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强制要求。

### TC-007: 关键 smoke test —— refs=v2 真实 Python 数据验证
- **结果**: PASS
- **实际行为**: `./maze --build --refs v2` 成功；对 `python/20260129-complex-types-311` 的 tarball 执行 `./maze --tar ... --refs v2 --text --json-output --rmlog --limit 500 && python3 validate.py maze-result.json` 成功。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`；日志中出现 `bitalosdb path ./db/coredump-default-2636550/txos-refsdb-ref`
- **备注**: 证明 refs v2 主链路可运行且结果与既有用例预期一致。

### TC-008: QA 静态测试文件执行
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...` 通过。
- **关键指标**: `ok   maze/qa/issue164`
- **备注**: issue 专属 QA 静态约束未发现回归。

### TC-009: refs 公共测试执行
- **结果**: PASS
- **实际行为**: 在注入 RocksDB 头文件/库路径并添加 `grocksdb_clean_link` tag 后，`go test -tags grocksdb_clean_link ./common/refs/...` 通过。
- **关键指标**: `ok   maze/common/refs`、`ok   maze/common/refs/core`、`ok   maze/common/refs/v0`、`ok   maze/common/refs/v1`
- **备注**: 裸跑 `go test ./common/refs/...` 因缺少 RocksDB 开发头失败，但在仓库当前推荐构建环境下通过。

## Test Results
- `./maze --build`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --build --refs v2`：通过
- `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500 && python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`：通过
- `CGO_CFLAGS='-I/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/include' CGO_CXXFLAGS='-I/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/include' CGO_LDFLAGS='-L/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/lib -lrocksdb -pthread -lstdc++ -ldl -lm -lz -lsnappy -llz4 -lzstd' GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags grocksdb_clean_link ./common/refs/...`：通过

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- Python 2.7 兼容性动态验证未执行；可能影响“兼容性验证”章节，不影响本次 issue #164 的核心 AC 判定。
- 不同 glibc 版本、不同 Node.js 版本兼容性验证未执行；当前轮次未扩展到多环境矩阵。
- 性能验证未执行；本轮重点是结构落位、构建入口与关键功能等价验证。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 9 / 9
- 失败: 0 / 9
- 建议: 可以进入合并/发布前的下一环节；就 issue #164 的核心需求而言，本轮验收通过。
