# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #164 的修复迭代进行了结构核对、构建验证、Go 静态 QA 测试、运行时 hooks 验证，以及至少一个实际 `testdata` 用例的动态验收。未发现阻断合并的问题。

## 详细结果

### TC-001: refs v2 结构文件与版本绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 正确将门面绑定到 `v2` 实现，`currentVersion = "v2"`。
- **关键指标**: `select_v2.go` 已绑定 `AddReverseIndex/GetReferents/GetReferrers/Save/Check/Init/Configure/SetMode`
- **备注**: 符合 AC1、AC2。

### TC-002: build tag 选择与默认版本保持 v1
- **结果**: PASS
- **实际行为**: `select_default.go` 排除了 `refs_v2`，默认版本仍指向 `v1`；`select_v0.go`、`select_v1.go` 均排除了 `refs_v2`；`select_conflict.go` 已扩展到任意两种 refs tag 冲突时报错。
- **关键指标**: 冲突条件覆盖 `refs_v0 && refs_v1`、`refs_v0 && refs_v2`、`refs_v1 && refs_v2`
- **备注**: 符合 AC3、AC8。

### TC-003: maze CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2/v2/refs_v2`；`build_maze()` 已支持附加 `refs_v2` build tag；默认构建与 `--refs v2` 构建均成功。
- **关键指标**:
  - `./maze --build`：通过
  - `./maze --build --refs v2`：通过
- **备注**: 符合 AC4、AC5。

### TC-004: hooks 中性命名与兼容回退
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 已新增 `RefsDBPath`；`common/refs.go` 中 `refsRuntimeHooks()` 对 `v2` 注入 `TxosRefsDBFileName`，同时保留 `RocksDBPath` 兼容字段；`common/leveldb.go` 提供 `TxosRefsDBFileName()`。
- **关键指标**:
  - `go test ./common/refs/... ./common -run TestRefsRuntimeHooksPathSelectionByVersion`：通过
  - `v1` 路径保持 `txos-rocksdb-*`
  - `v2` 路径使用 `txos-refsdb-*`
- **备注**: 符合 AC6、AC7，也满足“中性命名同时支持 v1 v2”的人工反馈。

### TC-005: refs v2 使用 BitalosDB 且不再抽象共享 core
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 独立实现并依赖 `github.com/zuoyebang/bitalosdb`；未发现 `grocksdb` 依赖或 `common/refs/core` 共享抽象残留；内部保留 `openDB` / `writeIndexBatch` / `writeReverseBatch` / `rangeScan` 薄适配函数。
- **关键指标**:
  - `common/refs/core/`：不存在
  - `unsafe.Pointer|core/` 搜索：未命中
  - `go test ./qa/issue164/...`：通过
- **备注**: 符合 AC9、AC10，也满足最新 human review“不要抽象 core”。

### TC-006: Python 主链路 smoke test（默认 refs v1）
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 用例执行通过，Maze 正常分析 coredump，验证项全部通过。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强约束。

### TC-007: Python 主链路 smoke test（refs v2）
- **结果**: PASS
- **实际行为**: 在 `MAZE_REFS_VERSION=v2` 环境下再次执行 `python/20260129-complex-types-311`，结果与默认版本一致，通过验证。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
  - `maze.log` 命中 `bitalosdb path ./db/coredump-default-2636550/txos-refsdb-ref`
- **备注**: 说明 `refs v2` 主链路至少在该 Python 样例上可正常初始化、落盘和读取。

## Findings

- 未发现阻断性问题。
- 低成本静态检查显示：`refs v2` 结构落位完整、默认版本仍为 `v1`、多 tag 冲突规则正确、hooks 命名已按要求兼容收敛。
- 动态 smoke test 显示：默认版本与 `refs v2` 在 `python/20260129-complex-types-311` 上均可正常工作。
- `maze.log` 中本轮 `refs v2` 运行日志使用 `bitalosdb` / `refsdb` 命名，未看到新的 RocksDB 命名残留问题。

## Test Results

- `./maze --build`：PASS
- `./maze --build --refs v2`：PASS
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：PASS
- `CGO_CFLAGS='-I/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/include' CGO_CXXFLAGS='-I/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/include' CGO_LDFLAGS='-L/home/hcjn0770/temp/worktrees/issue-164/3rd/deps/rocksdb/linux_amd64/debian12/10.10.1/install/lib -lrocksdb -pthread -lstdc++ -ldl -lm -lz -lsnappy -llz4 -lzstd' GOCACHE='/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache' go test ./common/refs/... ./common -run TestRefsRuntimeHooksPathSelectionByVersion`：PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
- `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## 失败的 Test Case（如有）

无。

## 未执行项

- Python 2.7 兼容性回归：未执行
- 不同 glibc 版本兼容性回归：未执行
- 不同 Node.js 版本兼容性回归：未执行
- 大内存进程性能回归：未执行

未执行原因：本轮按“低成本检查尽量完整 + 高成本验证分阶段早停”的策略，优先验证 issue #164 的结构约束、构建能力、refs v2 主链路和至少一个 testdata 实际用例；上述兼容性/性能项不属于本次 fix iteration 的最小阻塞集。

可能影响范围：主要影响扩展兼容性与性能相关结论，不影响本轮对 AC1~AC10 及 human review 强约束的通过判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以进入下一步集成/合并流程；若合并前仍需补强，可追加跨 Python/Node/glibc 版本兼容性与性能回归，但这不构成本轮阻塞。
