## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings
- 已完成 issue #164 的静态核对、构建验证、单元测试与至少一个 testdata 动态用例验证，当前分支满足计划中的关键验收点。
- `refs_v2` 已落位为独立版本包，`maze --refs v2` 可构建，默认版本仍保持 `v1`。
- 中性命名 `RefsDBPath` 已加入 hooks，且运行时对 `v1` / `v2` 保持兼容行为；本轮额外补齐了 `v0` 运行时 hooks 测试覆盖。
- 多 tag 冲突保护已生效；在 `refs_v1 refs_v2` 同开时，编译会因 `mustChooseExactlyOneRefsBuildTag` 未定义而失败，符合“禁止任意两个 refs tag 同开”的设计意图。

## 详细结果

### TC-001: refs v2 结构落位与门面绑定
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go`、`common/refs/select_v2.go` 均存在；`select_v2.go` 已将 facade 绑定到 `v2` 实现，`currentVersion = "v2"`。
- **关键指标**: `go test ./qa/issue164/...` 通过。
- **备注**: 覆盖 AC1、AC2。

### TC-002: 默认版本与 build tag 选择规则
- **结果**: PASS
- **实际行为**: `select_default.go` 保持默认 `v1`；`select_v0.go`、`select_v1.go` 已排除 `refs_v2`；`select_conflict.go` 已覆盖 `v0/v1`、`v0/v2`、`v1/v2` 三种冲突组合。
- **关键指标**: `go test -tags 'refs_v1 refs_v2' ./common/refs` 构建失败，报错 `undefined: mustChooseExactlyOneRefsBuildTag`，说明冲突保护生效。
- **备注**: 覆盖 AC3、AC8。

### TC-003: CLI 对 `--refs v2` 的支持
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2/v2/refs_v2`，`build_maze()` 会追加 `refs_v2` build tag，并在 `refs=v2` 时注入 `GOEXPERIMENT=arenas`。
- **关键指标**: `./maze --build --refs v2` 执行成功。
- **备注**: 覆盖 AC4、AC5。

### TC-004: Hooks 中性命名兼容 v1/v2
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 新增 `RefsDBPath`；`common/refs.go` 保留 `RocksDBPath` 兼容字段，并在 `refs.CurrentVersion() == "v2"` 时注入 `TxosRefsDBFileName`。
- **关键指标**: `go test -tags refs_v2 ./common/refs/core ./common` 通过；`go test -tags refs_v0 ./common -run TestRefsRuntimeHooksPathSelectionByVersion` 已在开发报告中通过，本轮静态核对一致。
- **备注**: 覆盖 AC6、AC7 与人工反馈“中性命名同时支持 v1 v2”。

### TC-005: refs v2 使用 BitalosDB 与薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 使用 `github.com/zuoyebang/bitalosdb`，无 `grocksdb` 依赖；存在 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 四个薄适配函数；对外仍保留 `GetReferents` / `GetReferrers` / `Save` / `Check` 合约。
- **关键指标**: `go test ./qa/issue164/...` 通过。
- **备注**: 覆盖 AC9、AC10。

### TC-006: 默认构建回归
- **结果**: PASS
- **实际行为**: 默认 refs 版本构建成功，未因 `v2` 引入破坏默认链路。
- **关键指标**: `./maze --build` 成功。
- **备注**: 验证默认仍指向 `v1`，且无明显回归。

### TC-007: 动态 smoke test（Python 复杂类型）
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 在默认 refs 配置下通过，随后在 `MAZE_REFS_VERSION=v2` 下再次通过；对象数量与类型统计均符合预期。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
  - `Player = 500`
  - `Config = 300`
- **备注**: 该项满足“每次验收至少执行一个 testdata 用例”的硬约束，并证明 `refs=v2` 未破坏主链路 smoke test。

## Test Results
- `./maze --build`：通过
- `./maze --build --refs v2`：通过
- `CGO_CFLAGS=... CGO_CXXFLAGS=... CGO_LDFLAGS=... GOCACHE=./tmp/go-build-cache go test ./qa/issue164/...`：通过
- `GOCACHE=./tmp/go-build-cache GOEXPERIMENT=arenas go test -tags refs_v2 ./common/refs/core ./common`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `git merge-tree $(git merge-base HEAD origin/master) HEAD origin/master`：无输出，未见文本级冲突
- `GOCACHE=./tmp/go-build-cache GOEXPERIMENT=arenas go test -tags 'refs_v1 refs_v2' ./common/refs`：按预期失败，用于验证多 tag 冲突保护

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- Python 2.7 / 多 glibc / 多 Node.js 版本兼容性未在本轮动态执行。
- 大内存进程（>1GB）性能验证未在本轮执行。
- 原因：本次需求核心是 refs v2 后端落位与版本选择/兼容语义；当前关键静态校验、构建链路与代表性 smoke test 已通过，无阻塞迹象，但上述扩展验证仍属于未覆盖范围。
- 可能影响范围：兼容性与性能相关声明，暂不影响本轮结构性 AC 的通过判定。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 日志检查
- `maze.log` 中观察到 `bitalosdb path ./db/coredump-default-2636550/txos-refsdb-ref` 与 `.../txos-refsdb-rref`，说明 `refs=v2` 已使用中性命名路径。
- `maze.log` 中存在若干 GDB `No symbol ...` 与 Python `AttributeError` 调试日志，这些为该 Python 测试样本分析过程中的既有调试输出，未导致本次测试失败；未发现新的 fatal / panic 阻塞迹象。
- 未生成 `maze.py.log`。

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 可以进入后续合并/发布流程；如需提升发布信心，建议后续补充多版本兼容性与性能对比回归。
