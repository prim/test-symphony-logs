# 开发报告

## 完成的任务
- [x] 修复 refs v2 运行时仍沿用 rocksdb 路径命名的问题
- [x] 保持 refs v1 兼容路径注入不变，新增中性 refs DB 路径函数供 v2 使用
- [x] 复测构建、Go 测试、默认 refs smoke test、refs v2 smoke test 与结果校验

## 修改的文件
- `common/leveldb.go`: 新增 `TxosRefsDBFileName()`，为 refs DB 提供中性路径命名 `txos-refsdb-*`
- `common/refs.go`: 将 `RefsDBPath` 从 `TxosRocksDBFileName` 切换为 `TxosRefsDBFileName`，保留 `RocksDBPath` 兼容字段

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `./maze --build --refs v2`
- 测试结果: 通过
- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`
- 测试结果: 通过
- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，`All validations passed!`
- 测试命令: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`
- 测试结果: 通过，运行时输出 `system versions: mpm=v2 refs=v2 vm=full`
- 测试命令: `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`
- 测试结果: 通过，`All validations passed!`
- 测试命令: `grep -nE 'bitalosdb path|txos-rocksdb|txos-refsdb' maze.log`
- 测试结果: 通过，仅出现 `bitalosdb path ./db/.../txos-refsdb-ref` 与 `.../txos-refsdb-rref`

## 验收核对
- AC1: PASS 已存在 `common/refs/v2/refs.go`
- AC2: PASS 已存在 `common/refs/select_v2.go`
- AC3: PASS `common/refs/select_conflict.go` 已禁止任意两个 refs tag 同开
- AC4: PASS `maze` 中 `normalize_refs_version()` 已支持 `v2`
- AC5: PASS `maze` 中 `build_maze()` 已支持 `refs_v2`
- AC6: PASS `common/refs/shared/types.go` 已新增 `RefsDBPath`
- AC7: PASS `common/refs.go` 中运行时 hooks 优先使用 `RefsDBPath` 语义，且本轮已切到中性路径函数
- AC8: PASS 默认版本仍为 `v1`，已通过构建与静态检查验证
- AC9: PASS `v2` 使用 `BitalosDB`，真实 smoke test 已验证
- AC10: PASS `v2` 内部薄存储适配层仍覆盖 open / batch write / range scan / close

## 已完成项
- 已将 refs v2 运行时路径从 `txos-rocksdb-*` 收敛为 `txos-refsdb-*`
- 已保留 `RocksDBPath` 兼容入口，未影响 refs v1 现有行为
- 已完成需求文档、QA 报告、code review 报告三方核对后的本轮目标修复与验证

## 未完成项
- 无

## 未验证项
- Python 2.7 动态兼容性
- 不同 glibc / Node.js 版本兼容性
- refs v1 与 refs v2 的逐项查询结果对照专项回归
- 大内存进程性能验证

## Self-Review 发现与修复
- 发现本轮实际需要修复的 QA 问题仅是 refs v2 路径命名残留；按“仅修复 QA 识别问题、最小改动”原则，只调整路径函数与 hooks 绑定，避免扩大改动面
- 复查后确认 `common/refs/core` 共享层与 Save 生命周期保护改动仍保留，相关 Go 测试继续通过

## 注意事项
- 本轮未修改任何 `testdata/` 测试文件
- `qa/issue164` 属于仓库内 QA 静态校验代码，本轮未保留对其修改
- 根据完成判定规则，本轮仍存在“未验证项”，因此结论为 `PARTIAL`，不能宣称全部范围已完全验证

## 结论
PARTIAL
