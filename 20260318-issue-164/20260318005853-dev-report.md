# 开发报告

## 完成的任务
- [x] Task 1: 阅读原始计划、最新 QA 报告与最新 code review 报告，确认本轮唯一必须修复项是默认 `refs_v1` 路径命名被共享 hooks 意外改写
- [x] Task 2: 将 `RefsDBPath` 注入范围收敛到 `refs_v2`，恢复默认 `refs_v1` 的 `txos-rocksdb-*` 行为稳定性
- [x] Task 3: 补充运行时路径断言测试，覆盖 `refs_v1` / `refs_v2` 的路径选择差异
- [x] Task 4: 修复共享核心测试暴露的 `Save()` / `Check()` 任务提交并发问题，消除 `WaitGroup` 复用 panic

## 修改的文件
- `common/refs.go`: 仅在 `refs.CurrentVersion() == "v2"` 时注入 `RefsDBPath`，保留 `v1` 使用 `RocksDBPath`
- `common/refs/facade.go`: 更新注释与术语，避免仍将实现描述为仅支持 `v0/v1` 或持久化到 RocksDB
- `common/refs/core/core.go`: 去掉 `RunInPool1` 外层多余 goroutine，修复测试池下的 `WaitGroup` 生命周期问题
- `common/refs_runtime_hooks_test.go`: 新增默认 `v1` 与 `v2` 路径命名断言
- `dev-log/2026-03-18-refs-v2-fix-iteration3.md`: 记录本轮修复背景、方案与验证

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过

- 测试命令: `./maze --build --refs v2`
- 测试结果: 通过

- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/... ./common -run 'TestRefsRuntimeHooksPathSelectionByVersion|TestSaveReinitializesIndexMaps|TestSaveRejectsConcurrentWrites'`
- 测试结果: 通过；覆盖路径选择断言与 Save 生命周期保护

- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common`
- 测试结果: 通过

- 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，验证输出 `All validations passed!`

- 测试命令: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500 && python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`
- 测试结果: 通过，`system versions: mpm=v2 refs=v2 vm=full`，校验脚本输出 `All validations passed!`

## 验收核对
- AC1: PASS 新增 `common/refs/v2/refs.go` 已存在，未在本轮回归中破坏
- AC2: PASS 新增 `common/refs/select_v2.go` 已存在，未在本轮回归中破坏
- AC3: PASS `common/refs/select_conflict.go` 仍禁止任意两个 refs tag 同开，`go test ./qa/issue164/...` 通过
- AC4: PASS `maze` 中 `normalize_refs_version()` 已支持 `v2`，`./maze --build --refs v2` 通过
- AC5: PASS `maze` 中 `build_maze()` 已支持 `refs_v2`，真实 smoke test 成功
- AC6: PASS `common/refs/shared/types.go` 仍保留中性字段 `RefsDBPath`
- AC7: PASS `common/refs.go` 运行时 hooks 已收敛：`v2` 优先使用 `RefsDBPath`，`v1` 保持 `RocksDBPath`
- AC8: PASS 默认版本仍为 `v1`，且本轮修复恢复默认 `txos-rocksdb-*` 路径稳定性
- AC9: PASS `v2` 仍使用 `BitalosDB`，`refs v2` 真实 smoke test 通过
- AC10: PASS `v2` 薄存储适配层仍覆盖 open / batch write / range scan / close

## 剩余工作
- 未完成项: 无（就本轮 QA/code review 指出的修复范围而言）
- 未验证项: Python 2.7、不同 glibc/Node.js 版本兼容性、大内存性能、`refs=v1` 与 `refs=v2` 逐项结果对照未在本轮执行

## Self-Review 发现与修复
- 发现 1: 若所有版本统一注入 `RefsDBPath`，核心层优先级会导致默认 `refs_v1` 外部路径命名发生漂移；已改为仅在 `refs_v2` 注入
- 发现 2: 为修复路径问题后执行定向测试时，`common/refs/core` 的测试池暴露出 `Save()` / `Check()` 里额外 goroutine 包装 `RunInPool1` 会触发 `WaitGroup` 复用 panic；已改为直接提交到池，保留并发能力同时消除生命周期竞态

## 注意事项
- 本轮遵循“只修最新 QA / code review 指出的问题并保持改动最小”的策略，没有改动测试数据和业务外 API
- `RefsDBPath` 仍作为兼容扩展字段保留，但默认稳定版 `refs_v1` 不应再无条件注入该字段
- 若后续要统一 `v1` / `v2` 路径命名，应作为单独兼容性变更推进，并补齐迁移说明与专项回归

## 结论
PARTIAL

## 已完成项
- 已修复 code review 指出的默认 `refs_v1` 路径命名漂移问题
- 已补充运行时路径断言测试
- 已修复本轮测试中暴露的共享核心并发提交问题
- 已完成构建、静态 QA、默认 smoke test 与 `refs_v2` smoke test 验证

## 未完成项
- 无（针对本轮 fix 范围）

## 未验证项
- Python 2.7 兼容性
- 不同 glibc / Node.js 版本兼容性
- `refs=v1` / `refs=v2` 逐项查询结果对照
- 大内存性能验证
