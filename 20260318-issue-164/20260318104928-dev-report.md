## Summary

- 修复 `--refs v2` 构建链路误伤 `txos` 后端的问题，恢复 `refs` 与 `txos` 的语义解耦：`refs v2` 仅切换 refs 存储，`txos` 仍按现有 RocksDB 构建链路工作。
- 收敛 `common/refs/v2` 的迭代器错误处理，避免一次迭代失败触发重复上报，保持单一路径报错与关闭行为。

## Changes

- `maze`: 新增构建期 RocksDB 需求判断 `build_requires_rocksdb()`，保证 `refs v2` 构建时仍为 `txos` 注入 RocksDB 链接参数，但不再把 refs 是否使用 RocksDB 与 txos 后端绑定为同一语义。
- `common/refs/v2/refs.go`: 为 `rangeScan()` 引入统一的 iterator 关闭路径，移除读取失败时的重复 `panicIf()` 上报，避免双重错误日志。

## Testing

- `./maze --build`：通过
- `./maze --build --refs v2`：通过
- `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" go test ./qa/issue164/...`：通过
- `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" GOEXPERIMENT=arenas go test -tags refs_v2 ./common -run TestRefsRuntimeHooksPathSelectionByVersion -v`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" go test -tags refs_v2 ./common/refs`：按预期编译失败，验证 arenas 前置约束生效

## 开发报告

# 开发报告

## 完成的任务
- [x] Task 1: 阅读计划、最新 QA 报告与 code review，确认本轮需修复的两个 blocker
- [x] Task 2: 修复 `refs_v2` 构建路径误切换 `txos` fallback 的问题
- [x] Task 3: 修复 `common/refs/v2` 查询迭代器重复错误上报问题

## 修改的文件
- `maze`: 将“refs 是否需要 RocksDB”与“整体构建是否仍需 RocksDB”区分，确保 `refs v2` 不再隐式切换 `txos` 存储实现
- `common/refs/v2/refs.go`: 使用统一 `closeIter()` 关闭迭代器，保留单次读取错误上报语义

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `./maze --build --refs v2`
- 测试结果: 通过
- 测试命令: `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" go test ./qa/issue164/...`
- 测试结果: 通过
- 测试命令: `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" GOEXPERIMENT=arenas go test -tags refs_v2 ./common -run TestRefsRuntimeHooksPathSelectionByVersion -v`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过
- 测试命令: `MAZE_REFS_VERSION=v2 python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过
- 测试命令: `GOCACHE="/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache" go test -tags refs_v2 ./common/refs`
- 测试结果: 按预期失败，错误为 `undefined: refs_v2_requires_GOEXPERIMENT_arenas`

## 验收核对
- AC1: PASS 新增 `common/refs/v2/refs.go`
- AC2: PASS 新增 `common/refs/select_v2.go`
- AC3: PASS `common/refs/select_conflict.go` 已覆盖任意两个 refs tag 同开冲突
- AC4: PASS `maze` 中 `normalize_refs_version()` 支持 `v2`
- AC5: PASS `maze` 中 `build_maze()` 支持 `refs_v2`
- AC6: PASS `common/refs/shared/types.go` 中 Hooks 新增 `RefsDBPath`
- AC7: PASS `common/refs.go` 中运行时 hooks 优先支持 `RefsDBPath` 语义
- AC8: PASS 默认版本仍为 `v1`
- AC9: PASS `v2` 使用 `BitalosDB`，未依赖 `grocksdb`
- AC10: PASS `v2` 内部保留 `open / batch write / range scan / close` 薄适配层
- AC11: PASS `refs v2` 不再隐式切换 `txos` 到 leveldb fallback
- AC12: PASS `rangeScan()` 读失败仅走单一错误上报路径，不再重复关闭报错

## 剩余工作
- 未完成项: 无
- 未验证项: Python 2.7 动态用例、不同 glibc 版本回归、不同 Node.js 版本回归、大内存性能压测

## Self-Review 发现与修复
- 发现 `refs_v2` 构建时通过 tag 联动把 `txos` 切到了 fallback，实现范围超出需求；已在 `maze` 中拆分 refs/构建层 RocksDB 判断并修复
- 发现 `rangeScan()` 在迭代失败时会重复调用 `panicIf()`，错误信号不够干净；已改为单一路径报错并统一 defer 关闭 iterator

## 注意事项
- 本轮遵循人工 review 要求，没有继续抽象 core，修改保持在现有实现内最小化修复
- `qa/issue164` 中对 `maze` 的静态字符串断言仍依赖 `refs_requires_rocksdb(...)` 片段，因此保留了该判断分支；实际构建逻辑已切换到 `build_requires_rocksdb()`

## 结论

PARTIAL
