# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #164 的重点回归项“`refs v2` 不应再被 RocksDB 依赖阻塞”已通过。静态检查、关键构建路径、issue 专项 QA 测试与至少一个真实 `testdata` 用例均已执行，未发现阻断合并的问题。

## 详细结果

### TC-001: refs v2 结构落位与门面绑定
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 正确将 `currentVersion` 绑定到 `v2`，并连接 `AddReverseIndex`、`AddIndexes`、`GetReferrers`、`GetReferents`、`Save`、`Check`、`Configure`、`SetMode`。
- **关键指标**: `qa/issue164/refs_v2_bitalosdb_test.go` 通过；`git diff` 显示 `common/refs/v2/refs.go` 新增约 815 行。
- **备注**: 满足 AC1、AC2。

### TC-002: refs build tag 选择与默认版本保持 v1
- **结果**: PASS
- **实际行为**: `select_default.go` 已扩展为排除 `refs_v2`；`select_v0.go`、`select_v1.go` 也都显式排除 `refs_v2`；`select_conflict.go` 已覆盖 `refs_v0 && refs_v2`、`refs_v1 && refs_v2` 等任意两种 tag 组合冲突。
- **关键指标**: `common/refs/select_conflict.go` 中包含三组冲突构建表达式；默认分支仍指向 `v1`。
- **备注**: 满足 AC3、AC8。

### TC-003: maze CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 的 `normalize_refs_version()` 已支持 `v2` / `refs_v2`；`build_maze()` 会追加 `refs_v2` tag；并新增 `refs_requires_rocksdb()`，仅在 `default/v1` 时解析与注入 RocksDB 依赖。
- **关键指标**: `./maze --build` 通过；`MAZE_ROCKSDB_PREFIX=./tmp/missing-rocksdb ./maze --build --refs v2` 通过。
- **备注**: 这是本轮 human review 指出的关键阻塞点，现已修复。满足 AC4、AC5。

### TC-004: Hooks 路径命名泛化
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 已新增 `RefsDBPath`，且仍保留 `RocksDBPath`；`common/refs.go` 在 `refs.CurrentVersion() == "v2"` 时注入 `TxosRefsDBFileName`，其余版本继续走 `TxosRocksDBFileName`。
- **关键指标**: `common/leveldb.go` 新增 `TxosRefsDBFileName()`；`common/refs_runtime_hooks_test.go` 覆盖不同版本路径选择。
- **备注**: 满足 AC6、AC7，也符合 human review “中性命名同时支持 v1 v2”。

### TC-005: refs v2 使用 BitalosDB 且保留薄存储边界
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 使用 `github.com/zuoyebang/bitalosdb`，未依赖 `grocksdb`；同时保留 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数；对外仍保留 `GetReferents`、`GetReferrers`、`Save`、`Check` 等门面语义。
- **关键指标**: `GOEXPERIMENT=arenas GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 ./common/refs ./common/refs/v2` 通过。
- **备注**: 满足 AC9、AC10。

### TC-006: 关键 smoke test - Python 复杂类型真实样例
- **结果**: PASS
- **实际行为**: 默认 refs 版本与 `MAZE_REFS_VERSION=v2` 两种路径下，`python/20260129-complex-types-311` 均通过；对象统计结果一致，说明主链路可运行。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
  - `Player = 500`
  - `Config = 300`
  - `Point = 400`
  - `Rectangle = 300`
  - `deque = 300`
  - set objects = `2310`
- **备注**: 已满足“每次验收至少执行一个 testdata 用例”的要求；本轮实际执行了默认 refs 和 refs v2 两次主链路样例。

## 额外检查记录

- 执行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：**通过**。
- 执行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 ./common/refs/...`：**在未设置 `GOEXPERIMENT=arenas` 时失败**，报错为 `imports arena: build constraints exclude all Go files in /usr/local/go/src/arena`；补充以 `GOEXPERIMENT=arenas` 重跑 `go test -tags refs_v2 ./common/refs ./common/refs/v2` 后通过。
- 该现象说明 `refs v2` 对 `arena` experiment 存在显式依赖；当前 `maze` 脚本已在 `--refs v2` 构建路径下注入 `GOEXPERIMENT=arenas`，因此不构成当前 issue 的阻断缺陷，但建议后续在文档或测试脚本层继续统一该约束，避免裸 `go test` 误用。

## 失败的 Test Case（如有）

无。

## 未执行项

- Python 2.7 兼容性验证未执行。
- 不同 glibc 版本兼容性验证未执行。
- 不同 Node.js 版本兼容性验证未执行。
- 大内存进程（>1GB）性能验证未执行。
- Node.js / C++ 语言链路的 refs v2 动态回归未执行。

未执行原因：本轮遵循“低成本检查尽量完整 + 高成本验证分阶段早停”策略，优先验证 human review 指出的关键阻断项与主链路 smoke test；主链路已通过，因此未继续扩展到高成本兼容性/性能矩阵。

可能影响范围：兼容性与性能相关结论目前仅能覆盖本机 Python 3.11 + 当前 Linux 环境，不能外推到 Python 2.7、其他 glibc、其他 Node.js 版本及大内存场景。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以合并当前修复；后续建议补充 `refs v2` 的兼容性矩阵与性能对比验证，并考虑把 `GOEXPERIMENT=arenas` 要求进一步文档化或收敛到测试入口，降低直接运行 `go test` 时的误用成本。
