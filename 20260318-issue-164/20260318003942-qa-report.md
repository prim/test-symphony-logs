# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮针对 issue #164 进行了需求文档、开发报告、测试用例、代码差异、构建、Go 测试以及真实 `testdata` 用例的验收。主链路构建与运行均已打通，`refs v2` 的结构落位、构建选择和 BitalosDB 后端基本成立；但仍发现一个与需求不一致的命名/路径残留问题：`refs v2` 实际运行时仍使用 `txos-rocksdb-*` 路径名，且日志中出现 `bitalosdb path ./.../txos-rocksdb-ref`。这与“避免让 v2 继续沿用 RocksDB 语义名称”“检查日志是否还有错误的 RocksDB 命名残留”的要求不符，因此本轮结论为 **FAIL**。

## 详细结果

### TC-001: refs v2 结构文件与门面绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均已存在；`select_v2.go` 将门面函数绑定到 `v2`。
- **关键指标**: `currentVersion = "v2"`，且 `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` 均已接线。
- **备注**: 满足 `common/refs/v2` 与 `select_v2.go` 落位要求。

### TC-002: build tag 选择与默认版本行为正确
- **结果**: PASS
- **实际行为**: `select_default.go` 仍默认选择 `v1`；`select_v0.go` / `select_v1.go` 已排除 `refs_v2`；`select_conflict.go` 已禁止任意两个 refs build tag 同开。
- **关键指标**: `select_default.go` 包含 `!refs_v0 && !refs_v1 && !refs_v2`；`select_conflict.go` 包含三组冲突组合。
- **备注**: 符合默认保持 `v1` 的策略。

### TC-003: `maze` CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2` / `v2` / `refs_v2`；`build_maze()` 在 `refs_version == "v2"` 时追加 `refs_v2`。
- **关键指标**: `./maze --build --refs v2` 执行成功；运行时输出 `system versions: mpm=v2 refs=v2 vm=full`。
- **备注**: CLI 与构建选择链路正常。

### TC-004: Hooks 命名泛化与运行时命名收敛
- **结果**: FAIL
- **实际行为**: `common/refs/shared/types.go` 已新增 `RefsDBPath`，`common/refs.go` 也开始同时注入 `RefsDBPath` 与 `RocksDBPath`；但 `RefsDBPath` 实际仍直接绑定 `TxosRocksDBFileName`，运行 `refs v2` 后 `maze.log` 中出现：`bitalosdb path ./db/.../txos-rocksdb-ref`、`bitalosdb path ./db/.../txos-rocksdb-rref`。
- **关键指标**: `maze.log` 第 224-225 行仍有 `txos-rocksdb-*` 路径名。
- **备注**: 说明 `v2` 运行时仍延续旧的 RocksDB 路径语义，未完成需求要求的命名收敛。

### TC-005: refs v2 使用 BitalosDB 且包含薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 使用 `github.com/zuoyebang/bitalosdb`，不存在 `grocksdb` 依赖；内部具备 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数。
- **关键指标**: 代码静态检查通过；动态运行日志出现 `open bitalosdb success`。
- **备注**: 满足“后端切换到 BitalosDB、保留薄适配层”的要求。

### TC-006: refs 相关 Go 测试回归
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...` 通过。
- **关键指标**: `common/refs`、`common/refs/core`、`common/refs/v0`、`common/refs/v1` 全部通过。
- **备注**: 共享核心重构未引入已有测试回归。

### TC-007: issue164 静态 QA 测试
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...` 通过。
- **关键指标**: `ok   maze/qa/issue164`。
- **备注**: 结构性验收点均能被静态测试识别。

### TC-008: Python 真实 testdata 用例 smoke test（默认 refs）
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，验证脚本输出 `All validations passed!`。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`；`TreeNode = 600`；`SimpleClass = 500`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求。

### TC-009: Python 真实 coredump 在 refs v2 下的直接运行 smoke test
- **结果**: PASS
- **实际行为**: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500` 运行成功，并生成 `maze-result.json` / `maze-result.txt`。
- **关键指标**: 控制台输出 `system versions: mpm=v2 refs=v2 vm=full`；日志出现 `open bitalosdb success`；随后 `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json` 通过。
- **备注**: 证明 refs v2 主链路可运行。

## 失败的 Test Case（如有）

### TC-004: Hooks 命名泛化与运行时命名收敛
- **结果**: FAIL
- **预期行为**: `refs v2` 运行时不应继续暴露明显的 RocksDB 语义命名；至少在 hooks/日志/路径语义上应完成中性化收敛，不应在 BitalosDB 路径日志中继续使用 `txos-rocksdb-*`。
- **实际行为**: `common/refs.go` 仍将 `RefsDBPath` 指向 `TxosRocksDBFileName`，导致 `refs v2` 运行时日志出现 `bitalosdb path ./db/coredump-default-2636550/txos-rocksdb-ref` 与 `.../txos-rocksdb-rref`。
- **复现步骤**:
  1. 执行 `./maze --build --refs v2`
  2. 执行 `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`
  3. 查看 `maze.log`
  4. 可见 `bitalosdb path ./.../txos-rocksdb-ref` 与 `bitalosdb path ./.../txos-rocksdb-rref`
- **可能原因**: 虽然 hooks 字段名已新增 `RefsDBPath`，但其实际值仍复用了旧的 `TxosRocksDBFileName`，导致运行时路径命名没有真正中性化。
- **建议修复**: 为 refs DB 提供独立的中性路径函数/命名，例如 `TxosRefsDBFileName`，并让 `RefsDBPath` 使用该中性路径；同时复查 `v2` 相关日志与错误信息，清除残留的 RocksDB 语义。

## 已执行的检查项

- 阅读最新开发报告 `dev-flow/20260318003503-dev-report.md`
- 阅读测试用例 `dev-flow/test-cases.md`
- 阅读需求文档 `dev-flow/plan.md`
- 检查 `git diff origin/master...HEAD -- common/refs common/refs.go maze qa/issue164`
- 静态检查 `common/refs/v2/refs.go`、`common/refs/select_v2.go`、`common/refs/select_default.go`、`common/refs/select_conflict.go`、`common/refs/shared/types.go`、`common/refs.go`、`maze`
- 运行 `./maze --build`
- 运行 `./maze --build --refs v2`
- 运行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
- 运行 `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 运行 `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`
- 运行 `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`
- 检查 `maze.log` 中 refs v2/BitalosDB 相关日志

## 未执行项

- Python 2.7 兼容性动态验证未执行
- 不同 glibc 版本兼容性未执行
- 不同 Node.js 版本兼容性未执行
- 大内存进程（>1GB）性能验证未执行
- `refs=v1` 与 `refs=v2` 的引用查询结果逐项对照专项用例未执行

未执行原因：本轮优先聚焦计划文档中的关键 AC 和上轮已通过主链路上的回归点，并针对“命名残留”进行复核。上述项目不影响本轮已发现失败问题的成立，但会影响兼容性和性能结论的完整性。

可能影响范围：主要影响兼容性/性能与更细粒度一致性评估，不影响本轮对“命名收敛未完成”这一失败结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- `refs v2` 的结构、构建标签、CLI 识别、BitalosDB 主链路与薄适配层均已落地。
- 共享核心重构后的 Go 测试与代表性 Python `testdata` 用例均通过，说明主要功能链路可用。
- 但运行时命名收敛未完成：`RefsDBPath` 仍落到 `TxosRocksDBFileName`，导致 BitalosDB 路径继续携带 `txos-rocksdb-*` 语义，违反需求中对命名/日志收敛的要求。

## Test Results
- `./maze --build`：通过
- `./maze --build --refs v2`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2 --rmlog --limit 500`：通过
- `python3 testdata/python/20260129-complex-types-311/validate.py maze-result.json`：通过

## 总结
- 通过: 8 / 9
- 失败: 1 / 9
- 建议: 当前不建议以“已完全满足 issue #164 验收要求”判定通过。请先修复 refs v2 运行时仍残留的 RocksDB 路径/命名语义，再进行复验。
