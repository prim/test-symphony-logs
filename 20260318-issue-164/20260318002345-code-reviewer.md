# Session Log (OpenCode)

| Field | Value |
|-------|-------|
| Session ID | `ses_303663894ffe9Xo13yQRvjiYxK` |
| Time | 16:21:10 - 16:23:45 (2m 34s) |
| Backend | OpenCode |
| Workspace | `/home/hcjn0770/temp/worktrees/issue-164` |
| Status | completed |
| Steps | 9 |
| Tool calls | 23 |
| Tokens | input: 537,427 (cached: 462,592), output: 7,205 (reasoning: 0), total: 544,632 |

## System Prompt

<details><summary>Prompt injected by maze-symphony</summary>

## Code Review Task

Review the code quality for:

**prim/maze#164**: refs: add v2 backend based on BitalosDB

# Refs v2 基于 BitalosDB 的后端演进方案

**日期**: 2026-03-17
**任务**: 基于现有 `common/refs/v1` 创建 `common/refs/v2`，保持 refs 门面 API 不变，但将底层持久化后端从 `RocksDB + grocksdb` 切换为 `BitalosDB`。

## 背景

当前 refs 后端现状如下：

- `common/refs/v0` 是纯内存版，不负责持久化。
- `common/refs/v1` 是默认版，负责正向/反向索引查询与落盘。
- `common/refs/v1/refs.go` 直接依赖 `github.com/linxGnu/grocksdb`，并把 DB 打开、批量写入、迭代查询、关闭都写死在实现内部。

从代码结构看，`refs v1` 目前有两个明显特点：

1. 业务语义已经稳定
   - `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` 这套门面已经被 `common/refs` 和 `common/refs.go` 兼容层稳定消费。
2. 存储实现仍然强耦合
   - `txosRefDB` / `txosRRefDB` 直接使用 `*grocksdb.DB`
   - `GetReferents` / `GetReferrers` 直接依赖 RocksDB iterator
   - `writeTasks` / `writeReverseTasks` 直接构造 RocksDB `WriteBatch`
   - Hooks 中路径函数仍命名为 `RocksDBPath`

这使得 refs 虽然已经完成“版本包 + 门面层”重构，但 `v1` 仍是“接口稳定、存储内核固定”的状态。现在如果要尝试 `BitalosDB`，最稳妥的路径不是原地替换 `v1`，而是：

- 保留 `v1` 作为当前稳定版
- 基于 `v1` 派生 `v2`
- 在 `v2` 中仅替换存储后端与相关初始化/读写实现
- 门面 API、兼容层调用方式、上层业务行为保持不变

## 外部参考

本方案基于以下公开资料制定：

- BitalosDB 官方仓库：<https://github.com/zuoyebang/bitalosdb/tree/main>
- BitalosDB 包文档：<https://pkg.go.dev/github.com/zuoyebang/bitalosdb>

从包文档可确认，`bitalosdb` 提供了本方案所需的核心能力：

- 打开数据库与 Options 配置
- 迭代器查询
- 批量写入
- `Flush` / `Compact` 等存储侧维护能力

其中具体 API 细节需要在正式实现 `refs/v2` 时按当前依赖版本再次对照源码确认；本文档中的“API 映射”部分属于基于公开接口名称的实现规划。

## 目标

本次 `refs v2` 的目标是：

1. 新增 `common/refs/v2`
   - 逻辑行为继承 `v1`
   - 后端数据库替换为 `BitalosDB`
2. 保持上层 API 不变
   - `common/refs` 门面不新增语言专用分支
   - `common/refs.go` 兼容层继续有效
3. 增加显式版本选择能力
   - 新增 `refs_v2` build tag
   - `./maze --refs v2` 支持构建/运行
4. 先做“功能等价迁移”，再评估性能收益
   - 第一阶段不同时重写 refs 的内存结构、批量策略、cache 语义
   - 优先验证存储替换不会改变查询结果与工作流

非目标：

- 不在第一阶段移除 `v1`
- 不在第一阶段修改 `v0`
- 不在第一阶段重新设计 refs 门面 API
- 不把本次工作扩展成“refs 全局存储抽象重写”

## 解决方案

### 1. 版本策略

新增第三个版本包：

```text
common/refs/
├── facade.go
├── select_default.go
├── select_v0.go
├── select_v1.go
├── select_v2.go
├── select_conflict.go
├── v0/
├── v1/
└── v2/
```

建议的选择策略：

- 默认继续绑定 `v1`
- `-tags=refs_v0` 绑定 `v0`
- `-tags=refs_v1` 绑定 `v1`
- `-tags=refs_v2` 绑定 `v2`
- 多个 refs build tag 同时启用时编译失败

原因：

- `v2` 在落地初期本质上是“替代性实验版本”
- 默认仍保持当前线上/日常分析路径稳定
- 便于对同一 PID 或同一测试集做 `v1` / `v2` 对照

### 2. 目录与文件调整

建议新增或修改以下文件：

#### `common/refs/v2/refs.go`

- 从 `common/refs/v1/refs.go` 拷贝初始化
- 保留 `RIM`、异步写入队列、query cache、编码格式、`dbKey()`、`rangeKeys()` 等行为
- 将 `grocksdb` 相关对象和调用替换为 `bitalosdb`

#### `common/refs/select_v2.go`

- 新增编译期选择文件
- `currentVersion = "v2"`
- 将门面函数绑定到 `v2`

#### `common/refs/select_conflict.go`

- 从“禁止 `refs_v0 + refs_v1` 同开”
- 扩展为“禁止任意两个 refs tag 同开”

#### `maze`

- `normalize_refs_version()` 增加 `v2`
- `build_maze()` 中支持追加 `refs_v2`
- `--refs v2` 进入与 `v0/v1` 一致的构建元数据流程

#### `common/refs/shared/types.go`

- 将 `RocksDBPath` 逐步泛化为更中性的命名，例如：
  - `RefsDBPath func(string) string`
- 第一阶段可保留兼容字段，避免一次性改动过大，例如同时支持：
  - `RocksDBPath`
  - `RefsDBPath`

#### `common/refs.go`

- `refsRuntimeHooks()` 处同步收敛命名
- 避免让 `v2` 继续沿用“RocksDB”语义名称

#### 文档与测试

- `doc/refs.md` 需要补充 `v2`
- 增加 `qa` 用例，校验：
  - `select_v2.go` 存在
  - `--refs v2` 可识别
  - 默认版本仍是 `v1`
  - 多 tag 冲突规则正确

### 3. 存储接口迁移原则

虽然本轮目标是“基于 v1 创建 v2”，但不建议简单把 `grocksdb` 调用逐行替换成 `bitalosdb` 调用。更稳妥的方式是先在 `v2` 内部抽一层很薄的存储适配。

建议在 `common/refs/v2` 内部约束为三类动作：

1. 打开/关闭 DB
2. 批量写入 key/value
3. 按 key 前缀范围迭代读取

这样可以把 `refs` 的业务语义和具体 DB API 之间留出一层边界，避免 `v2` 再次演变成新的“存储实现硬编码包”。

### 4. API 映射思路

以下映射基于当前 `v1` 的使用方式和 `bitalosdb` 公开文档能力制定：

| `v1` 中当前行为 | `v2` 中建议实现 |
|------|------|
| `openDB(path)` 打开 `grocksdb.DB` | 打开 `bitalosdb.DB`，保留 `CreateIfMissing` 等初始化语义 |
| `WriteBatch.Put()` 批量写 key/value | 使用 `bitalosdb` 的 batch 写入能力 |
| `db.Write(wo, wb)` 提交 batch | 改为 `v2` 的统一 `writeBatch()` 包装函数 |
| `NewIterator(ro)` + `Seek` + `Next` | 改为 `bitalosdb` iterator，并显式设置 lower/upper bound 或等价范围控制 |
| `DisableWAL(true)` | 在 `v2` 中按 `bitalosdb` 的写入/flush 语义做等价配置；若没有完全对应项，则保持“分析产物可丢弃、优先吞吐”的目标 |
| `Close()` | 替换为 `bitalosdb` 的关闭流程 |

需要特别注意的不是 API 名字，而是下面这几个语义是否一致：

- 批量写入何时真正可见
- iterator 遍历范围是否能稳定复用当前 `rangeKeys()` 方案
- 写入顺序和相同 key 的覆盖语义是否与 `v1` 一致
- 关闭前是否需要额外 `Flush()`

## 主要改动

### 1. 新增 `refs v2` 版本包

- 复制 `v1` 的 refs 行为模型
- 存储内核切换到 `BitalosDB`
- `CurrentVersion()` 返回 `v2`

### 2. 维持当前 key/value 编码协议

- 继续保留 `dbKey(k, ts)`
- 继续保留 `encodeSingleUint64` / `encodeUint64Pair` / `encodeUint64Slice`
- 这样可以避免存储引擎替换和数据格式替换同时发生

这很重要，因为如果第一阶段同时改：

- key 编码
- reverse index 紧凑表示
- DB 引擎

那么出现问题时很难判断是哪个层级引入了回归。

### 3. 泛化 hooks 与日志命名

当前 `refs` 的路径注入仍命名为 `RocksDBPath`，这在 `v2` 下已经失真。

建议第一阶段采取兼容式收敛：

```go
type Hooks struct {
    ...
    RocksDBPath func(string) string
    RefsDBPath  func(string) string
}
```

选择顺序建议为：

- 优先用 `RefsDBPath`
- 若为空，再回退到 `RocksDBPath`

这样可以让：

- `v1` 不受影响
- `v2` 不再把新的 DB 后端硬塞进旧名字
- 上层调用点可以渐进迁移

### 4. 控制默认版本变更节奏

建议分两步走：

1. 第一步
   - 增加 `refs_v2`
   - 默认仍为 `v1`
   - 通过单测、回归测试、实际 PID 分析对比 `v1` / `v2`
2. 第二步
   - 如果 `v2` 稳定且收益明确，再考虑把 `select_default.go` 指向 `v2`

这样比“直接让默认版本切到 v2”风险小得多。

## 关键代码

建议 `v2` 在内部先收敛成下面这种结构：

```go
type kvDB interface {
    Close() error
}

func openDB(path string) *bitalosdb.DB
func writeIndexBatch(db *bitalosdb.DB, tasks []iTask) error
func writeReverseBatch(db *bitalosdb.DB, tasks []riTask) error
func rangeScan(db *bitalosdb.DB, addr uint64) []uint64
```

查询函数保持业务侧不变，只替换底层扫描实现：

```go
func GetReferents(addr uint64) []uint64 {
    if ret := referentsCache.Get(addr); ret != nil {
        return ret
    }
    ret := rangeScan(txosRefDB, addr)
    referentsCache.Append(addr, ret)
    return ret
}
```

核心原则：

- `refs` 业务逻辑继续围绕“地址 -> 多版本片段值”工作
- DB 切换只影响 `open / batch write / range scan / close`
- `AddIndexes`、`AddReverseIndex`、`Save`、`Check` 的业务语义不改

## 风险与注意事项

### 1. 不要把 `v2` 做成 `v1` 的原地替换

如果直接在 `v1` 上改 DB：

- 很难回归对比
- 老问题和新问题会混在一起
- 文档、build tag、测试边界都会变得模糊

### 2. 需要重新确认 iterator 范围语义

`v1` 依赖：

- `dbKey(addr, ts)`
- `rangeKeys(addr)`
- iterator 从 `key0` seek 到 `key1` 前结束

`v2` 必须确保这一读取模型仍然成立，否则 `GetReferents()` / `GetReferrers()` 会漏数据或串 key。

### 3. 需要显式验证 flush/close 语义

`v1` 当前通过 batch 写入并关闭 DB 完成生命周期。切到 `BitalosDB` 后，需要确认：

- `Save()` 结束后查询是否立即可见
- 程序退出前是否必须额外 flush
- 测试与手工分析场景是否存在“写入成功但未持久化”的窗口

### 4. 命名和兼容层要一次收敛

如果 `v2` 已经不是 RocksDB，但 hooks、日志、错误信息仍写着 `rocksdb`，后续调试会很混乱。至少应在 `v2` 内做到：

- 新日志使用 `refsdb` 或 `bitalosdb`
- 新增中性 hooks 名称
- 保留旧字段仅用于兼容

## 实施阶段建议

### 阶段 1: 结构落位

- 新增 `common/refs/v2`
- 新增 `select_v2.go`
- 扩展 `maze --refs v2`
- 默认版本保持 `v1`

### 阶段 2: 存储替换

- 在 `v2` 中完成 DB 打开、batch 写入、range scan、关闭
- 保持编码协议和内存结构不变

### 阶段 3: 回归验证

- 跑 `common/refs` 和 `qa` 相关测试
- 对同一测试输入比较 `v1` / `v2`
- 验证 text 输出与查询结果一致

### 阶段 4: 性能评估

- 对比 `refs=v1` 与 `refs=v2` 的端到端耗时
- 关注 `SaveRefIndex`、`qriLoop`、查询延迟
- 如 `v2` 无显著收益，也至少保留为无 cgo 或替代性存储后端选项

## 测试验证

本文档只记录设计与实施计划，当前未执行代码级测试。

建议实现后按以下顺序验证：

```bash
./maze --build
./maze --build --refs v2
GOCACHE=./tmp/go-build-cache go test ./common/refs/...
GOCACHE=./tmp/go-build-cache go test ./qa/...
python3 testdata/run_test.py python/20260129-complex-types-311
```

如果要对比同一目标进程的行为，再分别执行：

```bash
./maze --build --refs v1 --pid <PID> --text
./maze --build --refs v2 --pid <PID> --text
```

验证重点：

- 构建是否通过
- `--refs v2` 是否能正确选中后端
- `GetReferents` / `GetReferrers` 结果是否与 `v1` 一致
- `SaveRefIndex` 后查询是否稳定
- `maze.log` 中是否还有错误的 RocksDB 命名残留

## 文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `dev-log/2026-03-17-refs-v2-bitalosdb-plan.md` | 新增 | 记录基于 `refs/v1` 派生 `refs/v2` 并切换到 `BitalosDB` 的设计与实施计划 |



> This is code review retry #3. Focus on previously found issues.




## Requirements Document

The requirements document is at `dev-flow/plan.md`. Read it to understand the full specification.

## Context

This code has already passed **functional QA** (tests pass, requirements met). Your job is to review
the **code quality** aspects that QA does not cover.

## Steps

1. Read the latest dev/fix report: `ls -t dev-flow/*-dev-report.md | head -1 | xargs cat`.
2. Read the latest QA report: `ls -t dev-flow/*-qa-report.md | head -1 | xargs cat`.
3. Read the requirements document: `cat dev-flow/plan.md`.
4. Review all changes: `git diff origin/master...HEAD` (use `origin/main` if the default branch is `main`)
5. Perform a thorough **code quality review**, focusing on:
   - **Architecture & Design** — Is the code well-structured? Proper separation of concerns? Appropriate abstractions?
   - **Readability & Maintainability** — Clear naming, adequate comments, no unnecessary complexity?
   - **Error Handling** — Are errors handled gracefully? No silent failures?
   - **Security** — Any injection risks, exposed secrets, unsafe operations?
   - **Performance** — Any obvious inefficiencies, N+1 queries, unnecessary allocations?
   - **Code Style** — Consistent with the project's conventions?
   - **DRY / SOLID** — No unnecessary duplication? Single responsibility respected?
   - **Type Safety** — Proper type annotations (if applicable)?
6. Write a code review report to `dev-flow/$(date +%Y%m%d%H%M%S)-code-review-report.md`:
   ```
   cat > "dev-flow/$(date +%Y%m%d%H%M%S)-code-review-report.md" << 'REPORT'
   ## Code Review Result: [PASS / FAIL]

   ## Architecture & Design
   - [Finding or "No issues"]

   ## Code Quality Findings
   - [Finding 1: description, file, severity (critical/major/minor)]
   - [Finding 2: ...]

   ## Recommendations
   - [Optional improvements that don't block approval]

   ## Verdict
   [Final assessment and reasoning]
   REPORT
   ```
7. **IMPORTANT**: The system reads the code review report to determine pass/fail. Make sure the report contains a clear status line like `## Code Review Result: PASS` or `## Code Review Result: FAIL`.

## Constraints

- Do NOT fix issues yourself when signaling failure — the fix agent handles that.
- If issues are truly minor (typos, trivial style), fix them, commit with `review:` prefix, and signal pass.
- Focus on code quality, NOT functional correctness (QA already verified that).
- Do NOT commit `dev-flow/` working files (`plan.md`, `test-cases.md`, `*-report.md`).
---

## CI/CD Agent 执行指令

你当前以自主 CI/CD Agent 身份运行。**提交代码是你任务的一部分，必须执行。**

### 必须执行的 Git 命令

任务完成后，立即执行：
```bash
git add .
git commit -m "<简洁描述本次修改>"
```

**不执行 commit 视为任务未完成。**

### 禁止操作
- 禁止 `git push` 或修改 remote
- 禁止提交工作文件：`dev-flow/plan.md`, `dev-flow/test-cases.md`, `dev-flow/*-dev-report.md`, `dev-flow/*-qa-report.md`, `dev-flow/*-code-review-report.md`

### 需要提交的内容
- 所有源代码修改
- `dev-log/` 下的正式文档（分析报告、设计文档等）

</details>

## Timeline

### `16:21:10` Step 1

#### `16:21:12` Tool: `glob` — dev-flow (completed)

Pattern: `*-dev-report.md`

```
/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001319-dev-report.md
```

#### `16:21:12` Tool: `glob` — dev-flow (completed)

Pattern: `*-qa-report.md`

```
/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001901-qa-report.md
```

#### `16:21:12` Tool: `bash` — 查看工作区状态 (completed)

```bash
git status --short
```

```
?? .symphony_prompt.md
?? dev-flow/20260318001319-dev-report.md
?? dev-flow/20260318001901-qa-report.md
?? dev-flow/plan.md
?? dev-flow/test-cases.md
?? tmp/go-build-cache/

```

#### `16:21:13` Tool: `bash` — 查看分支和默认基线 (completed)

```bash
git branch --show-current && git remote show origin
```

<details><summary>Output</summary>

```
symphony/164-refs-add-v2-backend-based-on-bitalosdb
* 远程 origin
  获取地址：https://x-access-token:***REDACTED***@github.com/prim/maze.git
  推送地址：https://x-access-token:***REDACTED***@github.com/prim/maze.git
  HEAD 分支：master
  远程分支：
    MobileServer2property                                                                         已跟踪
    auto_find_ptr                                                                                 已跟踪
    boost_queue                                                                                   已跟踪
    ccplus_auto                                                                                   已跟踪
    cpp_v1                                                                                        已跟踪
    cstruct_map_to_list                                                                           已跟踪
    dev                                                                                           已跟踪
    dmap_show_type                                                                                已跟踪
    f001                                                                                          已跟踪
    fix_classifyway                                                                               已跟踪
    fix_deepstruct_zero                                                                           已跟踪
    fix_enum                                                                                      已跟踪
    fix_gc_py3_11                                                                                 已跟踪
    fix_get_tsd_tls                                                                               已跟踪
    fix_goroutine                                                                                 已跟踪
    fix_malloc0                                                                    
... (truncated, 27220 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=25,382*

### `16:21:16` Step 2

#### `16:21:20` Tool: `read` — dev-flow/20260318001319-dev-report.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001319-dev-report.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001319-dev-report.md</path>
<type>file</type>
<content>1: # 开发报告
2: 
3: ## 完成的任务
4: - [x] Task 1: 新增 `common/refs/v2`，基于 BitalosDB 实现 refs v2 存储后端，并保留与 v1 一致的门面能力。
5: - [x] Task 2: 新增 `common/refs/select_v2.go`，扩展 refs build tag 选择与冲突保护，默认版本仍保持为 `v1`。
6: - [x] Task 3: 扩展 `maze --refs` 版本识别，支持 `2`、`v2`、`refs_v2`，并在 refs v2 构建时自动注入 `GOEXPERIMENT=arenas`。
7: - [x] Task 4: 将 hooks 路径命名从 RocksDB 专有语义泛化为 `RefsDBPath`，同时保留 `RocksDBPath` 兼容回退。
8: - [x] Task 5: 补充 refs 文档，说明 v2 后端、路径命名和版本选择方式。
9: 
10: ## 修改的文件
11: - `common/refs/v2/refs.go`: 新增 refs v2 实现，使用 BitalosDB，补齐 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数。
12: - `common/refs/select_v2.go`: 新增 refs v2 的门面绑定。
13: - `common/refs/select_default.go`: 默认选择逻辑排除 `refs_v2`，继续默认绑定 `v1`。
14: - `common/refs/select_v0.go`: 增加对 `refs_v2` 的互斥约束。
15: - `common/refs/select_v1.go`: 增加对 `refs_v2` 的互斥约束。
16: - `common/refs/select_conflict.go`: 扩展任意两个 refs tag 同开时的冲突保护。
17: - `common/refs/shared/types.go`: hooks 新增 `RefsDBPath` 字段，保留 `RocksDBPath`。
18: - `common/refs.go`: 注入 `RefsDBPath`，并将注释改为 refs DB 中性命名。
19: - `common/refs/v1/refs.go`: 优先读取 `RefsDBPath`，为空时回退到 `RocksDBPath`。
20: - `maze`: 支持 refs v2 运行参数与构建 tag，并在构建 refs v2 时自动补充 `GOEXPERIMENT=arenas`。
21: - `doc/refs.md`: 增补 refs v2 设计说明、版本矩阵、Hooks 命名说明和构建示例。
22: - `go.mod`: 引入 `github.com/zuoyebang/bitalosdb` 及其所需模块依赖。
23: - `go.sum`: 补充 refs v2/BitalosDB 相关校验和。
24: 
25: ## 自测结果
26: - 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`
27: - 测试结果: 通过。
28: 
29: - 测试命令: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`
30: - 测试结果: 通过。
31: 
32: - 测试命令: `GOEXPERIMENT=arenas GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test -tags refs_v2 -mod=mod ./common/refs/...`
33: - 测试结果: 通过。
34: 
35: - 测试命令: `./maze --build`
36: - 测试结果: 通过。
37: 
38: - 测试命令: `./maze --build --refs v2`
39: - 测试结果: 
... (truncated, 3737 chars total)
```
</details>

#### `16:21:20` Tool: `read` — dev-flow/20260318001901-qa-report.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001901-qa-report.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318001901-qa-report.md</path>
<type>file</type>
<content>1: # QA 验收报告
2: 
3: ## QA Result: PASS
4: 
5: ## 总体结果
6: 
7: **[STATUS: PASS]**
8: 
9: 本轮针对 issue #164 的 `refs v2`（BitalosDB 后端）进行了结构检查、构建验证、静态 QA 测试、`common/refs` 测试，以及至少一个真实 `testdata` 用例的动态验收。当前实现满足计划文档中的关键验收点，且未发现阻断发布的问题。
10: 
11: ## 详细结果
12: 
13: ### TC-001: refs v2 结构文件与门面绑定存在
14: - **结果**: PASS
15: - **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 已将 `currentVersion` 绑定为 `v2`，并将 `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` 等门面函数接到 `v2` 实现。
16: - **关键指标**: `qa/issue164/refs_v2_bitalosdb_test.go` 通过。
17: - **备注**: 符合“基于 v1 派生 v2，保持门面 API 不变”的要求。
18: 
19: ### TC-002: build tag 选择与默认版本行为正确
20: - **结果**: PASS
21: - **实际行为**: `select_default.go` 仍默认选择 `v1`；`select_v0.go` / `select_v1.go` 已排除 `refs_v2`；`select_conflict.go` 已覆盖 `refs_v0 && refs_v1`、`refs_v0 && refs_v2`、`refs_v1 && refs_v2` 的冲突保护。
22: - **关键指标**: 静态 QA 测试通过；源码检查确认 `currentVersion = "v1"` 仍在默认路径中。
23: - **备注**: 满足“默认版本仍为 v1”与“禁止任意两个 refs tag 同开”的 AC。
24: 
25: ### TC-003: `maze` CLI 支持 `--refs v2`
26: - **结果**: PASS
27: - **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2`、`v2`、`refs_v2`；`build_maze()` 在 `refs_version == "v2"` 时追加 `refs_v2` tag，并自动补充 `GOEXPERIMENT=arenas`。
28: - **关键指标**: `./maze --build --refs v2` 执行通过；`./maze --tar ... --text --json-output --refs v2` 实际输出 `system versions: mpm=v2 refs=v2 vm=full`。
29: - **备注**: CLI 与构建元数据流程均已打通。
30: 
31: ### TC-004: Hooks 命名已泛化为 `RefsDBPath`
32: - **结果**: PASS
33: - **实际行为**: `common/refs/shared/types.go` 新增 `RefsDBPath`，同时保留 `RocksDBPath`；`common/refs.go` 已同时注入二者；`common/refs/v1/refs.go` 已改为优先读取 `RefsDBPath`，为空再回退到 `RocksDBPath`。
34: - **关键指标**: 静态 QA 测试通过；源码检查确认命名收敛已落位。
35: - **备注**: 符合第一阶段“兼容式收敛”的设计要求。
36: 
37: ### TC-005: refs v2 使用 BitalosDB 且包含薄存储适配层
38: - **结果**: PASS
39: - **实际行为**: `common/refs/v2/refs.go` 依赖 `github.com/zuoyebang/bitalosdb`，未再依赖 `gro
... (truncated, 5703 chars total)
```
</details>

#### `16:21:20` Tool: `read` — dev-flow/plan.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/dev-flow/plan.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/dev-flow/plan.md</path>
<type>file</type>
<content>1: # prim/maze#164: refs: add v2 backend based on BitalosDB
2: 
3: ## 背景
4: 
5: 当前 refs 后端现状如下：
6: 
7: - `common/refs/v0` 是纯内存版，不负责持久化。
8: - `common/refs/v1` 是默认版，负责正向/反向索引查询与落盘。
9: - `common/refs/v1/refs.go` 直接依赖 `github.com/linxGnu/grocksdb`，并把 DB 打开、批量写入、迭代查询、关闭都写死在实现内部。
10: 
11: 从代码结构看，`refs v1` 目前有两个明显特点：
12: 
13: 1. 业务语义已经稳定
14:    - `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` 这套门面已经被 `common/refs` 和 `common/refs.go` 兼容层稳定消费。
15: 2. 存储实现仍然强耦合
16:    - `txosRefDB` / `txosRRefDB` 直接使用 `*grocksdb.DB`
17:    - `GetReferents` / `GetReferrers` 直接依赖 RocksDB iterator
18:    - `writeTasks` / `writeReverseTasks` 直接构造 RocksDB `WriteBatch`
19:    - Hooks 中路径函数仍命名为 `RocksDBPath`
20: 
21: 这使得 refs 虽然已经完成“版本包 + 门面层”重构，但 `v1` 仍是“接口稳定、存储内核固定”的状态。现在如果要尝试 `BitalosDB`，最稳妥的路径不是原地替换 `v1`，而是：
22: 
23: - 保留 `v1` 作为当前稳定版
24: - 基于 `v1` 派生 `v2`
25: - 在 `v2` 中仅替换存储后端与相关初始化/读写实现
26: - 门面 API、兼容层调用方式、上层业务行为保持不变
27: 
28: ## 目标
29: 
30: 1. 新增 `common/refs/v2`
31:    - 逻辑行为继承 `v1`
32:    - 后端数据库替换为 `BitalosDB`
33: 2. 保持上层 API 不变
34:    - `common/refs` 门面不新增语言专用分支
35:    - `common/refs.go` 兼容层继续有效
36: 3. 增加显式版本选择能力
37:    - 新增 `refs_v2` build tag
38:    - `./maze --refs v2` 支持构建/运行
39: 4. 先做“功能等价迁移”，再评估性能收益
40:    - 第一阶段不同时重写 refs 的内存结构、批量策略、cache 语义
41:    - 优先验证存储替换不会改变查询结果与工作流
42: 
43: ## 关键验收点
44: 
45: - [x] 新增 `common/refs/v2/refs.go`
46: - [x] 新增 `common/refs/select_v2.go`
47: - [x] `common/refs/select_conflict.go` 扩展为禁止任意两个 refs tag 同开
48: - [x] `maze` 中 `normalize_refs_version()` 增加 `v2`
49: - [x] `maze` 中 `build_maze()` 支持 `refs_v2`
50: - [x] `common/refs/shared/types.go` 中 Hooks 新增更中性的 `RefsDBPath`
51: - [x] `common/refs.go` 中运行时 hooks 优先使用 `RefsDBPath` 语义
52: - [x] 默认版本仍为 `v1`
53: - [x] `v2` 使用 `BitalosDB`，而不是 `grocksdb`
54: - [x] `v2` 内部应有薄存储适配层，覆盖 open / batch write / range scan / close

(End of file - total 54 lines)
</content>
```
</details>

#### `16:21:20` Tool: `bash` — 查看相对 master 的代码变更 (completed)

```bash
git diff --stat origin/master...HEAD && git diff origin/master...HEAD
```

<details><summary>Output</summary>

```
 common/refs.go                        |   5 +-
 common/refs/select_conflict.go        |   4 +-
 common/refs/select_default.go         |   4 +-
 common/refs/select_v0.go              |   4 +-
 common/refs/select_v1.go              |   4 +-
 common/refs/select_v2.go              |  20 +
 common/refs/shared/types.go           |   1 +
 common/refs/v1/refs.go                |   5 +-
 common/refs/v2/refs.go                | 776 ++++++++++++++++++++++++++++++++++
 doc/refs.md                           |  29 +-
 go.mod                                |  40 +-
 go.sum                                |  95 +++++
 maze                                  |  10 +
 qa/issue164/refs_v2_bitalosdb_test.go | 191 +++++++++
 14 files changed, 1155 insertions(+), 33 deletions(-)
diff --git a/common/refs.go b/common/refs.go
index 581c0663..9128e5d4 100644
--- a/common/refs.go
+++ b/common/refs.go
@@ -47,7 +47,7 @@ func GetReferents(addr uint64) []uint64 {
 	return refs.CommonCompatGetReferents(addr)
 }
 
-// SaveRefIndex 持久化引用索引到 RocksDB
+// SaveRefIndex 持久化引用索引到 refs DB
 func SaveRefIndex() {
 	refs.CommonCompatSaveRefIndex()
 }
@@ -76,7 +76,7 @@ func initRefs() {
 }
 
 // refsRuntimeHooks 构造运行时依赖注入配置
-// 提供日志、内存池、RocksDB 路径等运行时依赖
+// 提供日志、内存池、RefsDB 路径等运行时依赖
 func refsRuntimeHooks() refs.Hooks {
 	return refs.Hooks{
 		Debug:  Debug,
@@ -87,6 +87,7 @@ func refsRuntimeHooks() refs.Hooks {
 		PanicIf: func(err error, msg string) {
 			C(err, msg)
 		},
+		RefsDBPath:       TxosRocksDBFileName,
 		RocksDBPath:      TxosRocksDBFileName,
 		EnablePyMerge:    func() bool { return EnablePyMerge },
 		IsPostmanProfile: IsPostmanProfile,
diff --git a/common/refs/select_conflict.go b/common/refs/select_conflict.go
index 1489707e..cb90c8e0 100644
--- a/common/refs/select_conflict.go
+++ b/common/refs/select_conflict.go
@@ -1,5 +1,5 @@
-//go:build refs_v0 && refs_v1
-// +build refs_v0,refs_v1
+//go:build (refs_v0 && refs_v1) || (refs_v0 && refs_v2) || (refs_v1 && refs_v2)
+// +build refs_v0,refs_v1 r
... (truncated, 50346 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=42,920*

### `16:21:23` Step 3

#### `16:21:27` Tool: `read` — common/refs/v2/refs.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go</path>
<type>file</type>
<content>1: //go:build refs_v2
2: // +build refs_v2
3: 
4: package v2
5: 
6: import (
7: 	"bytes"
8: 	"encoding/binary"
9: 	"fmt"
10: 	"os"
11: 	"path/filepath"
12: 	"sync"
13: 	"sync/atomic"
14: 	"time"
15: 
16: 	"maze/common/refs/shared"
17: 
18: 	"github.com/zuoyebang/bitalosdb"
19: )
20: 
21: type iTask struct {
22: 	ts uint64
23: 	X  map[uint64][]uint64
24: }
25: 
26: type reverseIndexPair struct {
27: 	A uint64
28: 	B uint64
29: }
30: 
31: type riTask struct {
32: 	ts   uint64
33: 	one  map[uint64]uint64
34: 	two  map[uint64]reverseIndexPair
35: 	many map[uint64][]uint64
36: }
37: 
38: const (
39: 	IndexBatch  = 1024
40: 	RIndexBatch = 1024
41: 	ShortKey    = true
42: 	EnableCheck = false
43: 	nr          = 64
44: 	saveDrain   = 16
45: 	writerLoops = 4
46: )
47: 
48: type ReverseIndexMap struct {
49: 	sync.Mutex
50: 	Counter       int
51: 	RCounter      int
52: 	lastTs        uint64
53: 	lastRTs       uint64
54: 	Index         map[uint64][]uint64
55: 	ReverseIndex  map[uint64][]uint64
56: 	ReverseIndex1 map[uint64]uint64
57: 	ReverseIndex2 map[uint64]reverseIndexPair
58: 	CIndex        map[uint64][]uint64
59: 	CReverseIndex map[uint64][]uint64
60: }
61: 
62: type TReverseIndex [nr]ReverseIndexMap
63: 
64: type queryCache struct {
65: 	sync.Mutex
66: 	entries map[uint64][]uint64
67: 	order   []uint64
68: 	limit   int
69: }
70: 
71: var (
72: 	hooks shared.Hooks
73: 	mode  shared.Mode
74: 
75: 	txosRefDB  *bitalosdb.DB
76: 	txosRRefDB *bitalosdb.DB
77: 
78: 	RIM TReverseIndex
79: 
80: 	QI  = make(chan iTask, 1024)
81: 	QRI = make(chan riTask, 1024)
82: 
83: 	bI  uint64
84: 	eI  uint64
85: 	bRI uint64
86: 	eRI uint64
87: 
88: 	referentsCache TQueryCache
89: 	referrersCache TQueryCache
90: 
91: 	startLoops sync.Once
92: )
93: 
94: type TQueryCache [nr]queryCache
95: 
96: func (c *TQueryCache) Init(limit int) {
97: 	for i := 0; i < nr; i++ {
98: 		c[i].entries = make(map[u
... (truncated, 9806 chars total)
```
</details>

#### `16:21:27` Tool: `read` — common/refs/v2/refs.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go</path>
<type>file</type>
<content>421: 
422: func (m *ReverseIndexMap) takeReverseIndexTask(ts uint64) riTask {
423: 	if len(m.ReverseIndex) == 0 && len(m.ReverseIndex1) == 0 && len(m.ReverseIndex2) == 0 {
424: 		m.ReverseIndex = make(map[uint64][]uint64)
425: 		m.ReverseIndex1 = make(map[uint64]uint64)
426: 		m.ReverseIndex2 = make(map[uint64]reverseIndexPair)
427: 		return riTask{}
428: 	}
429: 	task := riTask{
430: 		ts:   ts,
431: 		one:  m.ReverseIndex1,
432: 		two:  m.ReverseIndex2,
433: 		many: m.ReverseIndex,
434: 	}
435: 
436: 	m.ReverseIndex = make(map[uint64][]uint64, len(task.many))
437: 	m.ReverseIndex1 = make(map[uint64]uint64, len(task.one))
438: 	m.ReverseIndex2 = make(map[uint64]reverseIndexPair, len(task.two))
439: 	return task
440: }
441: 
442: func qiLoop() {
443: 	batch := make([]iTask, 0, saveDrain)
444: 	for {
445: 		task := <-QI
446: 		batch = append(batch[:0], task)
447: 		batch = drainTasks(QI, batch)
448: 		saveITasks(batch)
449: 		atomic.AddUint64(&eI, uint64(len(batch)))
450: 	}
451: }
452: 
453: func qriLoop() {
454: 	batch := make([]riTask, 0, saveDrain)
455: 	for {
456: 		task := <-QRI
457: 		batch = append(batch[:0], task)
458: 		batch = drainTasks(QRI, batch)
459: 		saveRITasks(batch)
460: 		atomic.AddUint64(&eRI, uint64(len(batch)))
461: 	}
462: }
463: 
464: func drainTasks[T any](ch <-chan T, tasks []T) []T {
465: 	for len(tasks) < cap(tasks) {
466: 		select {
467: 		case task := <-ch:
468: 			tasks = append(tasks, task)
469: 		default:
470: 			return tasks
471: 		}
472: 	}
473: 	return tasks
474: }
475: 
476: func saveITasks(tasks []iTask) {
477: 	if txosRefDB == nil || len(tasks) == 0 {
478: 		return
479: 	}
480: 	panicIf(writeIndexBatch(txosRefDB, tasks), "refsdb batch")
481: }
482: 
483: func saveRITasks(tasks []riTask) {
484: 	if txosRRefDB == nil || len(tasks) == 0 {
485: 		return
486: 	}
487: 	panicIf(writeReverseBatch(txosRRefDB, tasks), "refsdb batch")
4
... (truncated, 9477 chars total)
```
</details>

#### `16:21:27` Tool: `read` — common/refs/v1/refs.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go</path>
<type>file</type>
<content>1: package v1
2: 
3: import (
4: 	"bytes"
5: 	"encoding/binary"
6: 	"fmt"
7: 	"os"
8: 	"path/filepath"
9: 	"sync"
10: 	"sync/atomic"
11: 	"time"
12: 
13: 	"maze/common/refs/shared"
14: 
15: 	"github.com/linxGnu/grocksdb"
16: )
17: 
18: type iTask struct {
19: 	ts uint64
20: 	X  map[uint64][]uint64
21: }
22: 
23: type reverseIndexPair struct {
24: 	A uint64
25: 	B uint64
26: }
27: 
28: type riTask struct {
29: 	ts   uint64
30: 	one  map[uint64]uint64
31: 	two  map[uint64]reverseIndexPair
32: 	many map[uint64][]uint64
33: }
34: 
35: const (
36: 	IndexBatch  = 1024
37: 	RIndexBatch = 1024
38: 	ShortKey    = true
39: 	EnableCheck = false
40: 	nr          = 64
41: 	saveDrain   = 16
42: 	writerLoops = 4
43: )
44: 
45: type ReverseIndexMap struct {
46: 	sync.Mutex
47: 	Counter       int
48: 	RCounter      int
49: 	lastTs        uint64
50: 	lastRTs       uint64
51: 	Index         map[uint64][]uint64
52: 	ReverseIndex  map[uint64][]uint64
53: 	ReverseIndex1 map[uint64]uint64
54: 	ReverseIndex2 map[uint64]reverseIndexPair
55: 	CIndex        map[uint64][]uint64
56: 	CReverseIndex map[uint64][]uint64
57: }
58: 
59: type TReverseIndex [nr]ReverseIndexMap
60: 
61: type queryCache struct {
62: 	sync.Mutex
63: 	entries map[uint64][]uint64
64: 	order   []uint64
65: 	limit   int
66: }
67: 
68: var (
69: 	hooks shared.Hooks
70: 	mode  shared.Mode
71: 
72: 	txosRefDB  *grocksdb.DB
73: 	txosRRefDB *grocksdb.DB
74: 
75: 	RIM TReverseIndex
76: 
77: 	QI  = make(chan iTask, 1024)
78: 	QRI = make(chan riTask, 1024)
79: 
80: 	bI  uint64
81: 	eI  uint64
82: 	bRI uint64
83: 	eRI uint64
84: 
85: 	referentsCache TQueryCache
86: 	referrersCache TQueryCache
87: 
88: 	startLoops sync.Once
89: )
90: 
91: type TQueryCache [nr]queryCache
92: 
93: func (c *TQueryCache) Init(limit int) {
94: 	for i := 0; i < nr; i++ {
95: 		c[i].entries = make(map[uint64][]uint64)
96: 		c[i].order = c[i].order[:0]
97: 	
... (truncated, 7575 chars total)
```
</details>

#### `16:21:27` Tool: `read` — qa/issue164/refs_v2_bitalosdb_test.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/qa/issue164/refs_v2_bitalosdb_test.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/qa/issue164/refs_v2_bitalosdb_test.go</path>
<type>file</type>
<content>1: package issue164
2: 
3: import (
4: 	"go/ast"
5: 	"go/parser"
6: 	"go/token"
7: 	"os"
8: 	"path/filepath"
9: 	"runtime"
10: 	"strings"
11: 	"testing"
12: )
13: 
14: func repoRoot(t *testing.T) string {
15: 	t.Helper()
16: 	_, file, _, ok := runtime.Caller(0)
17: 	if !ok {
18: 		t.Fatal("无法定位当前测试文件")
19: 	}
20: 	return filepath.Clean(filepath.Join(filepath.Dir(file), "..", ".."))
21: }
22: 
23: func mustReadFile(t *testing.T, path string) string {
24: 	t.Helper()
25: 	data, err := os.ReadFile(path)
26: 	if err != nil {
27: 		t.Fatalf("读取文件失败 %s: %v", path, err)
28: 	}
29: 	return string(data)
30: }
31: 
32: func mustParseFile(t *testing.T, path string) *ast.File {
33: 	t.Helper()
34: 	fset := token.NewFileSet()
35: 	file, err := parser.ParseFile(fset, path, nil, parser.ParseComments)
36: 	if err != nil {
37: 		t.Fatalf("解析 Go 文件失败 %s: %v", path, err)
38: 	}
39: 	return file
40: }
41: 
42: func funcExists(t *testing.T, file *ast.File, funcName string) bool {
43: 	t.Helper()
44: 	for _, decl := range file.Decls {
45: 		fn, ok := decl.(*ast.FuncDecl)
46: 		if ok && fn.Name != nil && fn.Name.Name == funcName {
47: 			return true
48: 		}
49: 	}
50: 	return false
51: }
52: 
53: func TestRefsV2ScaffoldAndBuildSelectionExist(t *testing.T) {
54: 	root := repoRoot(t)
55: 	requiredFiles := []string{
56: 		"common/refs/v2/refs.go",
57: 		"common/refs/select_v2.go",
58: 	}
59: 	for _, rel := range requiredFiles {
60: 		if _, err := os.Stat(filepath.Join(root, rel)); err != nil {
61: 			t.Fatalf("缺少 refs v2 结构文件: %s", rel)
62: 		}
63: 	}
64: 
65: 	selectV2 := mustReadFile(t, filepath.Join(root, "common", "refs", "select_v2.go"))
66: 	for _, required := range []string{
67: 		"refs_v2",
68: 		"import v2 \"maze/common/refs/v2\"",
69: 		"currentVersion = \"v2\"",
70: 		"getReferrersImpl = v2.GetReferrers",
71: 		"getReferentsImpl = v2.GetReferents",
72: 	} {
73: 		i
... (truncated, 5988 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=77,272*

### `16:21:33` Step 4

#### `16:21:39` Tool: `read` — maze (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/maze`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/maze</path>
<type>file</type>
<content>1: #!/bin/sh
2: """:"
3: if command -v python3 >/dev/null 2>&1; then
4:   exec python3 "$0" "$@"
5: fi
6: exec python "$0" "$@"
7: ":"""
8: 
9: from __future__ import print_function
10: 
11: import errno
12: import json
13: import os
14: import subprocess
15: import sys
16: import time
17: 
18: 
19: DEFAULT_MPM_VERSION = "v2"
20: DEFAULT_REFS_VERSION = "default"
21: META_FILE = ".maze.build.meta"
22: 
23: 
24: def normalize_mpm_version(value):
25:     if not value:
26:         return DEFAULT_MPM_VERSION
27:     value = value.strip().lower()
28:     if value in ("1", "v1", "mpm_v1"):
29:         return "v1"
30:     if value in ("2", "v2", "mpm_v2", "default"):
31:         return "v2"
32:     raise ValueError("unsupported MPM version: %s" % value)
33: 
34: 
35: def normalize_refs_version(value):
36:     if not value:
37:         return DEFAULT_REFS_VERSION
38:     value = value.strip().lower()
39:     if value in ("default", "mixed", "v1+py-v0", "refs_default"):
40:         return "default"
41:     if value in ("0", "v0", "refs_v0"):
42:         return "v0"
43:     if value in ("1", "v1", "pure-v1", "pure_v1", "refs_v1"):
44:         return "v1"
45:     if value in ("2", "v2", "refs_v2"):
46:         return "v2"
47:     raise ValueError("unsupported refs version: %s" % value)
48: 
49: 
50: def read_build_meta(script_dir):
51:     meta_path = os.path.join(script_dir, META_FILE)
52:     if not os.path.exists(meta_path):
53:         return {}
54:     try:
55:         with open(meta_path, "r") as f:
56:             return json.load(f)
57:     except Exception:
58:         return {}
59: 
60: 
61: def write_build_meta(script_dir, mpm_version, refs_version):
62:     meta_path = os.path.join(script_dir, META_FILE)
63:     with open(meta_path, "w") as f:
64:         json.dump({"mpm_version": mpm_version, "refs_version": refs_version}, f)
65: 
66: 
67: def build_maze(script_dir, mpm_version, re
... (truncated, 9376 chars total)
```
</details>

#### `16:21:39` Tool: `grep` — type Pool interface|RunInPool1|Wait\( (completed)

Pattern: `type Pool interface|RunInPool1|Wait\(`

<details><summary>Output</summary>

```
Found 59 matches
/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go:
  Line 265: 			wp.RunInPool1(fmt.Sprintf("SaveRefIndex index %d", i), false, func() {
  Line 273: 			wp.RunInPool1(fmt.Sprintf("SaveRefIndex reverse index %d", i), false, func() {
  Line 280: 	wp.Wait()
  Line 318: 			wp.RunInPool1(fmt.Sprintf("CheckRefs index %d", i), false, func() {
  Line 323: 			wp.RunInPool1(fmt.Sprintf("CheckRefs reverse index %d", i), false, func() {
  Line 328: 	wp.Wait()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go:
  Line 304: 			wp.RunInPool1(fmt.Sprintf("SaveRefIndex index %d", i), false, func() {
  Line 312: 			wp.RunInPool1(fmt.Sprintf("SaveRefIndex reverse index %d", i), false, func() {
  Line 319: 	wp.Wait()
  Line 355: 			wp.RunInPool1(fmt.Sprintf("CheckRefs index %d", i), false, func() {
  Line 360: 			wp.RunInPool1(fmt.Sprintf("CheckRefs reverse index %d", i), false, func() {
  Line 365: 	wp.Wait()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/shared/types.go:
  Line 8: type Pool interface {
  Line 9: 	RunInPool1(string, bool, func())
  Line 10: 	Wait()

/home/hcjn0770/temp/worktrees/issue-164/txos/results.go:
  Line 180: 	wp.Wait()
  Line 327: 	wp.Wait()
  Line 412: 	wp.Wait()

/home/hcjn0770/temp/worktrees/issue-164/txos/maze.go:
  Line 136: 	wp.Wait()
  Line 176: 	wp.Wait()

/home/hcjn0770/temp/worktrees/issue-164/python/gc.go:
  Line 176: 	wg.Wait()
  Line 194: 	wg.Wait()

/home/hcjn0770/temp/worktrees/issue-164/python/results.go:
  Line 125: 	wg.Wait()
  Line 294: 			wg.Wait()

/home/hcjn0770/temp/worktrees/issue-164/prepare/gdb_server.go:
  Line 172: 		s.cmd.Wait()

/home/hcjn0770/temp/worktrees/issue-164/mallocer/pymalloc/pymalloc.go:
  Line 250: 	wg.Wait()

/home/hcjn0770/temp/worktrees/issue-164/mallocer/tcmalloc/tcmalloc.go:
  Line 376: 	wg.Wait()

/home/hcjn0770/temp/worktrees/issue-164/mallocer/jemalloc/jemalloc.go:
  Line 154: 	wg.Wait()

/home/hcjn0770/temp/worktrees/issue-164/mallocer/mimalloc/mimalloc.go:
  Li
... (truncated, 4068 chars total)
```
</details>

#### `16:21:39` Tool: `grep` — cacheLimit\(|Save\(|Init\(|RemoveAll\(|NewIter\(|Flush\( (completed)

Pattern: `cacheLimit\(|Save\(|Init\(|RemoveAll\(|NewIter\(|Flush\(`

<details><summary>Output</summary>

```
Found 37 matches
/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go:
  Line 96: func (c *TQueryCache) Init(limit int) {
  Line 111: 			q.limit = cacheLimit()
  Line 131: 		q.limit = cacheLimit()
  Line 155: func cacheLimit() int {
  Line 159: func (m *TReverseIndex) Init() {
  Line 174: func Init() {
  Line 175: 	RIM.Init()
  Line 176: 	referentsCache.Init(cacheLimit())
  Line 177: 	referrersCache.Init(cacheLimit())
  Line 254: func Save() {
  Line 582: 	panicIf(os.RemoveAll(path), "refsdb rm dir")
  Line 593: 	iter := db.NewIter(&bitalosdb.IterOptions{
  Line 622: 	panicIf(db.Flush(), "refsdb flush")

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go:
  Line 93: func (c *TQueryCache) Init(limit int) {
  Line 108: 			q.limit = cacheLimit()
  Line 128: 		q.limit = cacheLimit()
  Line 152: func cacheLimit() int {
  Line 158: func (m *TReverseIndex) Init() {
  Line 173: func Init() {
  Line 174: 	RIM.Init()
  Line 175: 	referentsCache.Init(cacheLimit())
  Line 176: 	referrersCache.Init(cacheLimit())
  Line 293: func Save() {
  Line 638: 	panicIf(os.RemoveAll(path), "rocksdb rm dir")

/home/hcjn0770/temp/worktrees/issue-164/common/refs/facade.go:
  Line 68: func Save() {
  Line 80: func Init() {
  Line 102: 	Init()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/compat_bridge.go:
  Line 11: func CommonCompatInit(h Hooks) {

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs_test.go:
  Line 13: 	RIM.Init()
  Line 78: 	RIM.Init()
  Line 92: 	Init()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/compatcommon/compat.go:
  Line 39: 		api.Init()
  Line 68: 		currentAPI.Save()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v0/refs_test.go:
  Line 12: 	v0.Init()
  Line 30: 	v0.Init()

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v0/refs.go:
  Line 23: func Init() {
  Line 88: func Save() {
```
</details>

*Step finished: reason=tool-calls, tokens=118,558*

### `16:21:41` Step 5

#### `16:21:44` Tool: `read` — common/refs/v1/refs.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go</path>
<type>file</type>
<content>320: 
321: 	for {
322: 		done := true
323: 		bi := atomic.LoadUint64(&bI)
324: 		ei := atomic.LoadUint64(&eI)
325: 		if bi != ei {
326: 			resultf("SaveRefIndex bi %d ei %d\n", bi, ei)
327: 			done = false
328: 		}
329: 		bri := atomic.LoadUint64(&bRI)
330: 		eri := atomic.LoadUint64(&eRI)
331: 		if bri != eri {
332: 			resultf("SaveRefIndex bri %d eri %d\n", bri, eri)
333: 			done = false
334: 		}
335: 		if done {
336: 			debugf("SaveRefIndex done\n")
337: 			debugf("SaveRefIndex bi %d ei %d\n", bi, ei)
338: 			debugf("SaveRefIndex bri %d eri %d\n", bri, eri)
339: 			break
340: 		}
341: 		time.Sleep(time.Second)
342: 	}
343: }
344: 
345: func Check() {
346: 	if !EnableCheck || txosRefDB == nil || txosRRefDB == nil {
347: 		return
348: 	}
349: 	wp := getPool()
350: 	if wp == nil {
351: 		return
352: 	}
353: 	for i := range RIM {
354: 		go func(i int) {
355: 			wp.RunInPool1(fmt.Sprintf("CheckRefs index %d", i), false, func() {
356: 				checkIndex(i)
357: 			})
358: 		}(i)
359: 		go func(i int) {
360: 			wp.RunInPool1(fmt.Sprintf("CheckRefs reverse index %d", i), false, func() {
361: 				checkReverseIndex(i)
362: 			})
363: 		}(i)
364: 	}
365: 	wp.Wait()
366: }
367: 
368: func addIndex(from uint64, l []uint64) {
369: 	var task iTask
370: 	i := from / nr % nr
371: 	X := &RIM[i]
372: 	X.Lock()
373: 	r := X.Index[from]
374: 	for _, v := range l {
375: 		if v != 0 {
376: 			r = append(r, v)
377: 		}
378: 	}
379: 	X.Index[from] = r
380: 	if EnableCheck {
381: 		r = X.CIndex[from]
382: 		for _, v := range l {
383: 			if v != 0 {
384: 				r = append(r, v)
385: 			}
386: 		}
387: 		X.CIndex[from] = r
388: 	}
389: 	X.Counter++
390: 	if X.Counter >= IndexBatch {
391: 		X.Counter = 0
392: 		ts := uint64(time.Now().Unix())
393: 		if ts != X.lastTs {
394: 			X.lastTs = ts
395: 			task.ts = ts
396: 			task.X = X.Index
397: 			X.Index = make(map[uint64][]uint64, len(task.X))
398: 
... (truncated, 5786 chars total)
```
</details>

#### `16:21:44` Tool: `read` — common/parallel.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/parallel.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/parallel.go</path>
<type>file</type>
<content>95: }
96: 
97: func (wp *WorkerPool) RunInPool(do func()) {
98: 	what := GetFunctionName(do)
99: 	wp.RunInPool1(what, true, do)
100: }
101: 
102: func (wp *WorkerPool) RunInPool1(what string, timed bool, do func()) {
103: 	Debug.Printf("RunInPool1 %s", what)
104: 	wp.RunInPool2(what, true, true, do)
105: }
106: 
107: func (wp *WorkerPool) RunInPool2(what string, timed, checkRpeat bool, do func()) {
108: 	inited := true
109: 	wp.l.Lock()
110: 	_, repeat := wp.m[what]
111: 	wp.m[what] = true
112: 	if !wp.inited {
113: 		inited = false
114: 		wp.inited = true
115: 	}
116: 	wp.l.Unlock()
117: 
118: 	if repeat && checkRpeat {
119: 		panic(fmt.Sprintf("RunInPool1 repeat %s", what))
120: 	}
121: 
122: 	if !inited {
123: 		worker := func(idx int) {
124: 			Debug.Printf("Timed RunInPool1 begin %d", idx)
125: 			for {
126: 				nf := <-wp.Q
127: 				do := nf.Func
128: 				what := nf.Name
129: 				if do != nil {
130: 					// Result.Printf("beign %s", what)
131: 					if nf.Timed {
132: 						Timed(do)
133: 					} else {
134: 						do()
135: 					}
136: 					// Result.Printf("end %s", what)
137: 					wp.l.Lock()
138: 					delete(wp.m, what)
139: 					wp.l.Unlock()
140: 
141: 				} else {
142: 					// Result.Printf("break %d", idx)
143: 					break
144: 				}
145: 			}
146: 			wp.wg.Done()
147: 			Debug.Printf("Timed RunInPool1 done %d", idx)
148: 		}
149: 
150: 		for i := 0; i < wp.N; i++ {
151: 			wp.wg.Add(1)
152: 			go worker(i)
153: 		}
154: 
155: 		if wp.stat {
156: 			go PoolStat(wp)
157: 		}
158: 	}
159: 
160: 	wp.Q <- NamedFunc{what, do, timed}
161: }
162: 
163: func (wp *WorkerPool) Wait() {
164: 	// 产生结束标志
165: 	// Result.Printf("gen end")
166: 	for i := 0; i < wp.N; i++ {
167: 		wp.Q <- NamedFunc{"", nil, false}
168: 	}
169: 	// Result.Printf("wait begin")
170: 	wp.wg.Wait()
171: 	// Result.Printf("wait end")
172: }
173: 
174: func PoolStat(wp *WorkerPool) {
175: 	n := 0
1
... (truncated, 2350 chars total)
```
</details>

#### `16:21:44` Tool: `read` — common/refs/facade.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/facade.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/facade.go</path>
<type>file</type>
<content>1: // facade.go — Refs 函数指针门面层
2: //
3: // 本文件通过函数指针实现版本切换，支持运行时或编译期选择 v0/v1 实现。
4: // 函数指针由 select_*.go 文件在 init() 时绑定到具体版本。
5: //
6: // 设计目标：
7: //   - 业务代码统一依赖本包，不直接依赖 v0/v1
8: //   - 通过 build tags 编译期选择版本
9: //   - 支持运行时配置（Hooks 依赖注入）
10: //
11: // 版本选择：
12: //   - select_default.go: 默认使用 v1
13: //   - select_v0.go: -tags=refs_v0 使用 v0
14: //   - select_v1.go: -tags=refs_v1 使用 v1
15: package refs
16: 
17: // 函数指针变量：由 select_*.go 在 init() 时绑定到 v0 或 v1 实现
18: var (
19: 	currentVersion      = "unknown"
20: 	addReverseIndexImpl func(uint64, []uint64) // 添加反向索引
21: 	addIndexesImpl      func(uint64, []uint64) // 批量添加正向索引
22: 	addIndexImpl        func(uint64, []uint64) // 单次添加正向索引
23: 	getReferrersImpl    func(uint64) []uint64  // 查询引用者
24: 	getReferentsImpl    func(uint64) []uint64  // 查询被引用对象
25: 	saveImpl            func()                 // 持久化到 RocksDB
26: 	checkImpl           func()                 // 校验一致性
27: 	initImpl            func()                 // 初始化
28: 	configureImpl       func(Hooks)            // 配置运行时依赖
29: 	setModeImpl         func(Mode)             // 设置运行模式
30: )
31: 
32: func CurrentVersion() string {
33: 	return currentVersion
34: }
35: 
36: // 索引管理 API
37: 
38: // AddReverseIndex 添加反向索引（from 被 l 中的对象引用）
39: func AddReverseIndex(from uint64, l []uint64) {
40: 	addReverseIndexImpl(from, l)
41: }
42: 
43: // AddIndexes 批量添加正向索引（from 引用 l 中的对象）
44: func AddIndexes(from uint64, l []uint64) {
45: 	addIndexesImpl(from, l)
46: }
47: 
48: // AddIndex 单次添加正向索引
49: func AddIndex(from uint64, l []uint64) {
50: 	addIndexImpl(from, l)
51: }
52: 
53: // 查询 API
54: 
55: // GetReferrers 查询引用了指定对象的所有对象（反向查询）
56: func GetReferrers(addr uint64) []uint64 {
57: 	return getReferrersImpl(addr)
58: }
59: 
60: // GetReferents 查询指定对象引用的所有对象（正向查询）
61: func GetReferents(addr uint64) []uint64 {
62: 	return getReferentsImpl(addr)
63: }
64: 
65: // 持久化与校验 API
66: 
67: // S
... (truncated, 3215 chars total)
```
</details>

#### `16:21:44` Tool: `read` — common/refs/shared/types.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-164/common/refs/shared/types.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-164/common/refs/shared/types.go</path>
<type>file</type>
<content>1: package shared
2: 
3: type Logger interface {
4: 	Printf(string, ...interface{})
5: 	Println(...interface{})
6: }
7: 
8: type Pool interface {
9: 	RunInPool1(string, bool, func())
10: 	Wait()
11: }
12: 
13: type Hooks struct {
14: 	Debug            Logger
15: 	Result           Logger
16: 	GetPool          func() Pool
17: 	PanicIf          func(error, string)
18: 	RefsDBPath       func(string) string
19: 	RocksDBPath      func(string) string
20: 	EnablePyMerge    func() bool
21: 	IsPostmanProfile func() bool
22: 	MazeProfileTimes func() int
23: }
24: 
25: type Mode struct {
26: 	Postman      bool
27: 	ProfileTimes int
28: }

(End of file - total 28 lines)
</content>
```
</details>

*Step finished: reason=tool-calls, tokens=135,435*

### `16:21:53` Step 6

#### `16:22:08` Tool: `grep` — func C\(|func PanicIf\(|PanicIf\(err, msg\)|C\(err, msg\) (completed)

Pattern: `func C\(|func PanicIf\(|PanicIf\(err, msg\)|C\(err, msg\)`

<details><summary>Output</summary>

```
Found 3 matches
/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go:
  Line 746: 		hooks.PanicIf(err, msg)

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go:
  Line 752: 		hooks.PanicIf(err, msg)

/home/hcjn0770/temp/worktrees/issue-164/common/refs.go:
  Line 88: 			C(err, msg)
```
</details>

#### `16:22:08` Tool: `grep` — GetReferents\(|GetReferrers\(|SaveRefIndex\( (completed)

Pattern: `GetReferents\(|GetReferrers\(|SaveRefIndex\(`

<details><summary>Output</summary>

```
Found 58 matches
/home/hcjn0770/temp/worktrees/issue-164/common/refs/v2/refs.go:
  Line 230: func GetReferents(addr uint64) []uint64 {
  Line 242: func GetReferrers(addr uint64) []uint64 {
  Line 644: 		l := GetReferents(k)
  Line 653: 		l := GetReferrers(k)

/home/hcjn0770/temp/worktrees/issue-164/common/refs/v1/refs.go:
  Line 229: func GetReferents(addr uint64) []uint64 {
  Line 261: func GetReferrers(addr uint64) []uint64 {
  Line 650: 		l := GetReferents(k)
  Line 659: 		l := GetReferrers(k)

/home/hcjn0770/temp/worktrees/issue-164/common/refs.go:
  Line 39: func GetReferrers(addr uint64) []uint64 {
  Line 40: 	return refs.CommonCompatGetReferrers(addr)
  Line 46: func GetReferents(addr uint64) []uint64 {
  Line 47: 	return refs.CommonCompatGetReferents(addr)
  Line 51: func SaveRefIndex() {
  Line 52: 	refs.CommonCompatSaveRefIndex()

/home/hcjn0770/temp/worktrees/issue-164/qa/issue164/refs_v2_bitalosdb_test.go:
  Line 182: 		"func GetReferents(addr uint64) []uint64",
  Line 183: 		"func GetReferrers(addr uint64) []uint64",

/home/hcjn0770/temp/worktrees/issue-164/txos/refs_shim.go:
  Line 15: 	return GetReferents(pyo)
  Line 19: 	return GetReferrers(pyo)

/home/hcjn0770/temp/worktrees/issue-164/qa/issue156/refs_api_unification_test.go:
  Line 87: 		"func GetReferrers(addr uint64) []uint64",
  Line 88: 		"func GetReferents(addr uint64) []uint64",
  Line 241: 		"return GetReferrers(pyo)",
  Line 286: 		"func GetReferrers(pyo uint64) []uint64",
  Line 287: 		"func GetReferents(pyo uint64) []uint64",

/home/hcjn0770/temp/worktrees/issue-164/qa/issue140/refs_facade_migration_test.go:
  Line 341: 	for _, marker := range []string{"GetReferrers("} {
  Line 346: 	for _, marker := range []string{"GAddReverseIndex(", "GetReferents(", "GetReferrers("} {

/home/hcjn0770/temp/worktrees/issue-164/python/traverse.go:
  Line 1053: 			v := GetReferrers(pyo)

/home/hcjn0770/temp/worktrees/issue-164/python/refs_shim.go:
  Line 6: 	return GetReferrers(pyo)

/home/hcjn0770/temp/wo
... (truncated, 4213 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=145,118*

### `16:22:14` Step 7

#### `16:22:29` Tool: `bash` — 生成审查报告时间戳 (completed)

```bash
date +%Y%m%d%H%M%S
```

```
20260318002229

```

*Step finished: reason=tool-calls, tokens=151,936*

### `16:22:48` Step 8

#### `16:23:40` Tool: `apply_patch` — Success. Updated the following files:
A dev-flow/20260318002229-code-review-report.md (completed)

```json
{
  "patchText": "*** Begin Patch\n*** Add File: /home/hcjn0770/temp/worktrees/issue-164/dev-flow/20260318002229-code-review-report.md\n+## Code Review Result: FAIL\n+\n+## 架构与设计\n+- 发现重大问题：`common/refs/v2/refs.go` 基本整份复制了 `common/refs/v1/refs.go` 的业务层实现，仅在局部替换底层 DB 调用。这样虽然表面上保留了 `openDB / writeIndexBatch / writeReverseBatch / rangeScan` 四个薄适配函数，但实际把缓存、分片、异步写入、Save/Check 生命周期、编码协议等稳定语义再次复制到 v2，导致后续任何对 refs 核心行为的修复都需要在 v1/v2 双份维护，已经违背了“仅替换存储后端、留出清晰边界”的设计目标。\n+\n+## 代码质量发现\n+- 发现 1：`refs v2` 通过整文件复制 `v1` 逻辑来接入 BitalosDB，未把稳定的 refs 业务语义与存储实现真正分层，造成大面积重复代码和后续演进分叉风险。当前 `common/refs/v2/refs.go` 与 `common/refs/v1/refs.go` 在缓存、分片、批处理、Save/Check、编码等核心逻辑上高度重复，已经出现注释/细节轻微漂移，后续很容易出现“一边修复、另一边遗漏”的维护事故。文件：`common/refs/v2/refs.go`、`common/refs/v1/refs.go`。严重程度：major\n+- 发现 2：`Save()` 在未持有 shard 锁的情况下直接摘走并置空 `X.Index`，同时 `AddIndex()`/`addReverseIndex()` 仍会并发读写这些 map；`AddIndex()` 还默认假设 `X.Index` 一定非 nil。也就是说，一旦调用方在索引写入尚未完全停止时触发 `Save()`，这里会出现竞态窗口，轻则读写不一致，重则触发对 nil map 的写入 panic。v2 在新实现中完整复制了这套生命周期约束但没有任
... (truncated, 1605 chars total)
```

```
Success. Updated the following files:
A dev-flow/20260318002229-code-review-report.md
```

*Step finished: reason=tool-calls, tokens=156,473*

### `16:23:42` Step 9

已完成代码审查，报告已写入：

`dev-flow/20260318002229-code-review-report.md`

结论：**FAIL**

核心问题有两点：
1. `refs v2` 通过大段复制 `v1` 实现完成迁移，未真正把稳定业务逻辑与存储后端分层，后续维护和修复容易分叉。
2. `Save()` 与并发写入之间仍存在隐含竞态窗口，`Save()` 会在未加锁情况下摘走并置空 map，复制到了 v2，存在并发安全风险。

我没有修复代码，只提交了审查报告。

*Step finished: reason=stop, tokens=154,130*
