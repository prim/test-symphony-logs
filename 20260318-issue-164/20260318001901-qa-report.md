# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #164 的 `refs v2`（BitalosDB 后端）进行了结构检查、构建验证、静态 QA 测试、`common/refs` 测试，以及至少一个真实 `testdata` 用例的动态验收。当前实现满足计划文档中的关键验收点，且未发现阻断发布的问题。

## 详细结果

### TC-001: refs v2 结构文件与门面绑定存在
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 与 `common/refs/select_v2.go` 均存在；`select_v2.go` 已将 `currentVersion` 绑定为 `v2`，并将 `AddIndexes` / `AddReverseIndex` / `GetReferents` / `GetReferrers` / `Save` / `Check` 等门面函数接到 `v2` 实现。
- **关键指标**: `qa/issue164/refs_v2_bitalosdb_test.go` 通过。
- **备注**: 符合“基于 v1 派生 v2，保持门面 API 不变”的要求。

### TC-002: build tag 选择与默认版本行为正确
- **结果**: PASS
- **实际行为**: `select_default.go` 仍默认选择 `v1`；`select_v0.go` / `select_v1.go` 已排除 `refs_v2`；`select_conflict.go` 已覆盖 `refs_v0 && refs_v1`、`refs_v0 && refs_v2`、`refs_v1 && refs_v2` 的冲突保护。
- **关键指标**: 静态 QA 测试通过；源码检查确认 `currentVersion = "v1"` 仍在默认路径中。
- **备注**: 满足“默认版本仍为 v1”与“禁止任意两个 refs tag 同开”的 AC。

### TC-003: `maze` CLI 支持 `--refs v2`
- **结果**: PASS
- **实际行为**: `maze` 中 `normalize_refs_version()` 已识别 `2`、`v2`、`refs_v2`；`build_maze()` 在 `refs_version == "v2"` 时追加 `refs_v2` tag，并自动补充 `GOEXPERIMENT=arenas`。
- **关键指标**: `./maze --build --refs v2` 执行通过；`./maze --tar ... --text --json-output --refs v2` 实际输出 `system versions: mpm=v2 refs=v2 vm=full`。
- **备注**: CLI 与构建元数据流程均已打通。

### TC-004: Hooks 命名已泛化为 `RefsDBPath`
- **结果**: PASS
- **实际行为**: `common/refs/shared/types.go` 新增 `RefsDBPath`，同时保留 `RocksDBPath`；`common/refs.go` 已同时注入二者；`common/refs/v1/refs.go` 已改为优先读取 `RefsDBPath`，为空再回退到 `RocksDBPath`。
- **关键指标**: 静态 QA 测试通过；源码检查确认命名收敛已落位。
- **备注**: 符合第一阶段“兼容式收敛”的设计要求。

### TC-005: refs v2 使用 BitalosDB 且包含薄存储适配层
- **结果**: PASS
- **实际行为**: `common/refs/v2/refs.go` 依赖 `github.com/zuoyebang/bitalosdb`，未再依赖 `grocksdb`；内部存在 `openDB`、`writeIndexBatch`、`writeReverseBatch`、`rangeScan` 等薄适配函数；查询侧继续通过 `GetReferents` / `GetReferrers` 暴露既有语义。
- **关键指标**: 静态 QA 测试通过；源码检查确认 `dbKey()` / `rangeKeys()` / 编码函数均沿用既有协议。
- **备注**: 满足“只替换存储后端，不同时重写业务语义”的 AC。

### TC-006: refs 相关 Go 测试回归
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...` 通过，`common/refs`、`v0`、`v1` 均通过。
- **关键指标**: 相关包测试全部成功，无新增失败。
- **备注**: 说明默认 refs 路径未因本次引入 v2 而回归。

### TC-007: issue164 静态 QA 测试
- **结果**: PASS
- **实际行为**: `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...` 通过。
- **关键指标**: `ok   maze/qa/issue164`
- **备注**: 已覆盖本 issue 对结构、build tag、CLI、hooks、适配层的核心验收点。

### TC-008: Python 真实 testdata 用例 smoke test（默认 refs）
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 执行通过，验证脚本所有断言通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`；`TreeNode = 600`；`SimpleClass = 500`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的强制要求。

### TC-009: Python 真实 coredump 在 refs v2 下的直接运行 smoke test
- **结果**: PASS
- **实际行为**: `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2` 运行成功，控制台输出明确显示 `system versions: mpm=v2 refs=v2 vm=full`，并出现 BitalosDB 启动日志 `open bitalosdb success`。
- **关键指标**: 结果对象统计与默认 refs 路径一致；输出文件成功生成 `maze-result.json`。
- **备注**: 证明 refs v2 构建与运行主链路成立。

## 失败的 Test Case（如有）

无。

## 已执行的检查项

- 阅读最新开发报告 `dev-flow/20260318001319-dev-report.md`
- 阅读测试用例 `dev-flow/test-cases.md`
- 阅读需求文档 `dev-flow/plan.md`
- 检查 `git diff origin/master...HEAD`
- 静态检查关键文件与绑定关系
- 运行 `go test ./qa/issue164/...`
- 运行 `go test ./common/refs/...`
- 运行 `./maze --build`
- 运行 `./maze --build --refs v2`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 运行 `./maze --tar ... --refs v2 --text --json-output`
- 查看 `maze.log` 与文本输出，确认 refs v2/BitalosDB 实际生效

## 未执行项

- Python 2.7 兼容性动态验证未执行
- 不同 glibc 版本兼容性未执行
- 不同 Node.js 版本兼容性未执行
- 大内存进程（>1GB）性能验证未执行
- 与 `refs=v1` 的引用查询逐条对照测试未单独构造专项用例执行

未执行原因：本轮以计划文档中的关键 AC 为主，优先完成低成本静态检查、关键构建链路和代表性 smoke test。上述项目不阻塞本次结构性验收结论，但仍属于后续增强回归范围。

可能影响范围：主要影响兼容性与性能维度结论，不影响本轮对 `refs_v2` 结构落位、CLI 选择、后端切换与主链路可运行性的通过判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 未发现违反计划文档关键验收点的实现遗漏。
- `refs v2` 已完成从 `grocksdb` 到 `bitalosdb` 的后端切换，并保留薄适配层边界，方向符合需求。
- 默认版本仍为 `v1`，降低了将实验版后端直接切为默认路径的风险。
- `testdata/run_test.py` 本身没有 refs 版本专用参数，本轮对 `refs v2` 的动态验证采用 `./maze --tar ... --refs v2` 直接执行完成；后续若需要将 refs 版本切换纳入统一测试框架，可再补充测试工具层增强。

## Test Results
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./qa/issue164/...`：通过
- `GOCACHE=/home/hcjn0770/temp/worktrees/issue-164/tmp/go-build-cache go test ./common/refs/...`：通过
- `./maze --build`：通过
- `./maze --build --refs v2`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --json-output --refs v2`：通过

## 总结
- 通过: 9 / 9
- 失败: 0 / 9
- 建议: 可以进入下一阶段；若要推动默认版本切换到 `v2`，建议补充 `refs=v1` / `refs=v2` 结果对照、更多语言/版本兼容性验证及性能基准测试后再决策。
