## Summary
Fixed `--kill-loop` process matching so it only targets `maze --loop` processes and no longer terminates unrelated commands that happen to include a `--loop` argument.

## Changes
- `maze`: added `is_maze_loop_process(cmdline)` helper and tightened `do_kill_loop()` matching logic to require both `--loop` and a `maze` executable/script entry in the command line.

## Testing
- `python3 -m pytest tests/test_maze_loop_integration.py` — passed (8/8)
- `./maze --help` — passed, verified `--loop` and `--kill-loop` help output remains intact

## Self-Review
- Verified the new matcher still accepts expected loop invocations such as `./maze --loop` while excluding unrelated commands like `python3 worker.py --loop`.
