# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮验收结论为 FAIL。

阶段 A 静态检查发现迁移完整性仍存在明显缺口；阶段 B 动态验证中，`./maze --build` 因环境 `/tmp` 空间不足失败，阻塞了基于当前源码的可靠重编译验证。尽管使用仓库内现有 `.maze` 二进制执行了 1 个 Python 主链路用例且通过，但由于无法确认其一定对应本次最新源码产物，因此不能据此放行全部 AC。

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `go test ./qa/issue110 -run TestPreparePackageScaffoldExists` 通过，`prepare/prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等关键文件存在。
- **关键指标**: 仓库级迁移守护测试通过。
- **备注**: 仅证明脚手架存在，不代表职责已完整迁移。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，并在 `maze()` 中调用 `prepare.Run()`。
- **关键指标**: `go test ./qa/issue110 -run TestMainUsesPrepareRun` 通过。
- **备注**: 入口接线已完成。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: `common/common.go` 使用 `initPreparedRuntime()`，`common/var.go` 未再读取 `.storage.json` / `.cpp.json`。
- **关键指标**: `go test ./qa/issue110 -run TestCommonInitNoLongerDependsOnLegacyJSONBootstrap` 通过。
- **备注**: 旧 JSON bootstrap 已移除。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py` 与 `maze-py/` 已删除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留。
- **关键指标**: 对应迁移守护测试通过。
- **备注**: 符合 AC9、AC10 的文件层要求。

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: FAIL
- **实际行为**: 文件级迁移并非一一对应；部分职责虽被合并进 `prepare/alloc.go`、`prepare/prepare.go` 等，但仍存在被删除模块没有实质 Go 实现承接的情况。
- **关键指标**:
  - 已删除 `maze-py/messiahserver.py`，但仓库中不存在 `prepare/messiah.go`，且仅有空壳 `type MessiahManager struct{}`，未找到对应方法实现。
  - 已删除 `maze-py/cpp_vtable.py`，但仓库中不存在独立 `prepare/cpp_vtable.go`；虽上下文中存在 `CppVtableManager`，但未见对应迁移文件。
  - 已删除 `maze-py/maze_collect.py`，但 `CollectMallocLib` 当前仅有空实现 `InitIndexFile()/UpdateIndexFile()` 返回 `nil`。
- **备注**: 违反了本轮人审要求“删除的 Python 文件与新增 Go 文件需检查对应关系及逻辑承接”。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **实际行为**: 主链路已有迁移，但多处仍是部分迁移或占位实现，未达到“全部重写”的完整性要求。
- **关键指标**:
  - `prepare.Run()` 未按计划文档调用 `Collect.InitIndexFile()`、`ParseDwarf.DownloadUseParseDwarf()`、`Collect.UpdateIndexFile()`、`ParseDwarf.UploadUseParseDwarf()`。
  - `MessiahManager` 无任何核心逻辑实现。
  - `CollectMallocLib` 仅空返回，未见 malloc 库索引管理逻辑。
  - `ParseDwarfManager` 有部分逻辑，但未接入 `Run()` 主流程。
- **备注**: 说明迁移仍未完全覆盖 `.maze.py`/`maze-py/*.py` 全部核心职责，影响 AC1、AC6、AC7、AC9。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 已为 shell wrapper，内部调用 Go 二进制 `.maze`，不再是 Python shebang，也不包含旧 `do_build` / `do_tar` / `do_release` 逻辑。
- **关键指标**: `go test ./qa/issue110 -run TestMazeEntryIsNoLongerPythonWrapper` 通过。
- **备注**: 符合统一入口方向。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍包含 `import gdb`。
- **关键指标**: `go test ./qa/issue110 -run TestGdbScriptsStillUsePythonAPI` 通过。
- **备注**: GDB 内部 Python 脚本保留正确。

### TC-007: Python 主流程回归
- **结果**: PASS（仅 smoke）
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过，`prepare.Run()` 成功，validate 全量通过。
- **关键指标**: `68626` 个 pymalloc 对象；关键类型 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`。
- **备注**: 由于 `./maze --build` 在本环境失败，此用例实际依赖仓库内已存在 `.maze` 二进制运行，只能作为烟雾信号，不能完全替代基于当前源码重编译后的严格验收。

### TC-008: C++ 多线程栈变量回归
- **结果**: 未执行
- **实际行为**: 未执行。
- **关键指标**: 无。
- **备注**: 被 `./maze --build` 阻塞；继续使用现有 `.maze` 二进制执行会引入“二进制可能不是当前源码产物”的不确定性。

### TC-008A: 代表性分配器用例回归（jemalloc / mimalloc）
- **结果**: 未执行
- **实际行为**: 未执行。
- **关键指标**: 无。
- **备注**: 同上，受构建阻塞影响。

### TC-008B: Node.js 代表性回归
- **结果**: 未执行
- **实际行为**: 未执行。
- **关键指标**: 无。
- **备注**: 同上，受构建阻塞影响。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: PASS（部分）
- **实际行为**: 在 Python smoke 用例中，`run_test.py` 已间接调用 `maze --tar ... --text --json-output` 并生成 `maze-result.json`。
- **关键指标**: `maze-result.json` 生成，validate 通过。
- **备注**: 仅在 Python 场景得到验证，C++/Node.js 场景未完成。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **实际行为**: 文件清理与入口切换基本完成，但主流程编排与历史模块职责迁移仍不完整。
- **关键指标**:
  - `.maze.py` / `maze-py` 已删除，GDB 脚本已保留。
  - 但 `prepare.Run()` 与需求文档中的完整流程仍不一致，无法确认 standalone / postmanprofile 所有职责均已无损迁移。
- **备注**: AC7、AC9、AC10 只能部分确认，不能判定完全通过。

## 失败的 Test Case

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: FAIL
- **预期行为**: 不存在“Python 文件已删除但没有 Go 对应模块承接职责”的遗漏。
- **实际行为**: 至少 `messiahserver.py`、`maze_collect.py` 相关职责未见完整 Go 承接；`cpp_vtable.py` 也未形成明确迁移文件。
- **复现步骤**:
  1. 查看 `git diff --stat origin/master...HEAD`
  2. 查看 `prepare/` 目录文件列表
  3. 检查 `prepare/context.go`、`prepare/prepare.go`
  4. 搜索 `MessiahManager`、`CollectMallocLib`、`CppVtableManager`
- **可能原因**: 迁移过程将多个模块合并到少量 Go 文件中，但部分历史模块仅保留了结构体或占位实现，未完成逻辑迁移。
- **建议修复**: 补齐缺失模块职责，至少保证被删除的 Python 模块在 Go 侧有明确实现或明确说明已废弃且不再需要。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **预期行为**: `.maze.py` / `maze-py/*.py` 的主链路关键职责均已在 Go 中落地。
- **实际行为**: `prepare.Run()` 未接入部分计划中的关键步骤，多个模块仍为部分迁移或空实现。
- **复现步骤**:
  1. 阅读 `prepare/prepare.go`
  2. 对照计划文档中的 `Run()` 顺序检查 `Collect/ParseDwarf` 等步骤
  3. 阅读 `prepare/context.go` 与 `prepare/prepare.go` 中相关 manager 实现
- **可能原因**: 当前实现优先打通主链路，但尚未完成全部历史职责回填。
- **建议修复**: 先补齐 `Run()` 编排缺口，再补全空壳 manager 的实质逻辑，并增加对应回归测试。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **预期行为**: standalone/postmanprofile 逻辑迁移完整，删除遗留 Python 预处理后不影响全部模式。
- **实际行为**: 文件层迁移通过，但职责层仍有缺口，不能确认所有模式行为与旧实现一致。
- **复现步骤**:
  1. 对照 `dev-flow/plan.md` 中 `prepare.Run()` 设计流程
  2. 阅读 `prepare/prepare.go`
  3. 比对缺失调用与空实现模块
- **可能原因**: 迁移收尾尚未完成。
- **建议修复**: 补齐模式相关的剩余准备阶段逻辑，并用 testdata 覆盖验证。

## 阻塞与环境问题

- `./maze --build` 失败，错误为 `/usr/bin/ld: 最后的链结失败: 设备上没有空间`。
- 进一步检查显示 `/tmp` 仅剩约 `59M` 可用空间，而 Go/cgo 链接阶段使用 `/tmp/go-link-*`，导致本轮无法完成基于当前源码的可靠重编译。
- 仓库根目录所在分区仍有充足空间；阻塞点是系统 `/tmp` 分区而非项目目录本身。

## 未执行项

- TC-008 `cpp/20260304-stack-multithread`
  - **原因**: 构建阻塞后，继续使用现有 `.maze` 二进制执行会引入版本不确定性。
  - **可能影响 AC**: AC1、AC4、AC5、AC6、AC7。
- TC-008A `cpp/20260211-jemalloc-5-3-0-multithread` / `cpp/20260211-mimalloc-3-2-8-multithread`
  - **原因**: 同上。
  - **可能影响 AC**: AC1、AC5、AC6、AC7。
- TC-008B `nodejs/20260211-comprehensive`
  - **原因**: 同上。
  - **可能影响 AC**: AC1、AC5、AC6、AC8、AC10。
- 基于 `qa/issue110` 的 AC1 历史基线对比子测试
  - **原因**: 子测试内部也依赖 `./maze --build`，同样被 `/tmp` 空间问题阻塞。
  - **可能影响 AC**: AC1。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过（仅 `python/20260129-complex-types-311` smoke）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 发现 1：静态检查显示迁移完整性不足，至少 `MessiahManager`、`CollectMallocLib` 等仍为缺失/占位状态，和“全部重写为 Go prepare 包”的目标不符。
- 发现 2：`prepare.Run()` 主流程未完全对齐计划文档，若干历史职责未接入。
- 发现 3：构建验证被环境 `/tmp` 空间耗尽阻塞，导致无法完成基于当前源码二进制的全量动态回归。
- 发现 4：在现有 `.maze` 二进制基础上，Python 主链路 smoke 用例可以通过，说明主链路并非完全不可用，但不足以抵消上述结构性缺口与构建阻塞。

## 总结
- 通过: 7 / 10（含部分通过 1 项，不含未执行项）
- 失败: 3 / 10
- 未执行: 3 项动态扩展回归 + 1 项 AC1 基线比对
- 建议: **暂不发布**。请先补齐缺失的 Go 迁移职责，并在清理 `/tmp` 空间或改造构建临时目录后重新执行 `./maze --build`、AC1 基线对比、C++/分配器/Node.js 回归。
