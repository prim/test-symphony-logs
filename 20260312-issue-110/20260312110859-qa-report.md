# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #110 的 `prepare` 迁移进行了仓库级静态审查、Go 守护测试、构建验证以及 Python / C++ / 非 ptmalloc 分配器 / Node.js 代表性用例回归。

结论：

- `prepare.Run()` 已接入 `main.go` 主流程；
- `common.S` / `common.Cpp` 的初始化已不再依赖 `.storage.json` / `.cpp.json`；
- `.maze.py` 与 `maze-py/*.py` 已删除，保留了 GDB 内部 Python 脚本；
- Python、C++、ptmalloc、jemalloc、mimalloc 代表性回归均通过；
- Node.js 主链路已由 `nodejs/20260211-01-basic-types` 通过验证；
- 但优先指定的两个 Node.js 用例存在测试数据侧问题：`20260211-comprehensive` 缺少 tarball，`20260226-container-size` 的 `validate.py` 不符合 `run_test.py` 约定。它们未构成本次迁移实现失败的证据，但降低了对 Node.js 扩展场景的覆盖完整度。

## 执行范围

### 阶段 A：低成本 / 静态检查

已执行：

- 阅读需求：`dev-flow/plan.md`
- 阅读测试用例：`dev-flow/test-cases.md`
- 阅读最新开发报告：`dev-flow/20260312110009-dev-report.md`
- 审查改动：`git diff origin/master...HEAD`
- 阅读关键实现文件：
  - `main.go`
  - `maze`
  - `common/common.go`
  - `common/var.go`
  - `prepare/prepare.go`
  - `prepare/fill.go`
  - `prepare/process.go`
  - `prepare/gdb.go`
  - `prepare/gdb_client.go`
  - `prepare/gdb_server.go`
  - `prepare/symbols.go`
  - `prepare/lang_python.go`
  - `prepare/lang_nodejs.go`
  - `prepare/lang_lua.go`
  - `prepare/alloc.go`
  - `prepare/cpp_analyzer.go`
  - `prepare/legacy_mapping.go`
  - `qa/issue110/prepare_migration_test.go`

### 阶段 B：关键 smoke test

已执行：

- `go test ./qa/issue110`
- `./maze --build`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
- `python3 testdata/run_test.py cpp/20260304-stack-multithread`

### 阶段 C：扩展回归

已执行：

- `python3 testdata/run_test.py cpp/20260201-basic-malloc`
- `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
- `python3 testdata/run_test.py cpp/20260211-mimalloc-3-2-8-multithread`
- `python3 testdata/run_test.py nodejs/20260211-01-basic-types`

额外尝试但受测试数据问题影响：

- `python3 testdata/run_test.py nodejs/20260211-comprehensive`
- `python3 testdata/run_test.py nodejs/20260226-container-size`

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `go test ./qa/issue110 -run TestPreparePackageScaffoldExists` 所覆盖的守护测试通过；`prepare/` 下存在 `prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等关键文件。
- **关键指标**: `go test ./qa/issue110` 整体 PASS
- **备注**: 满足 prepare 包脚手架存在性要求。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，并在主流程中调用 `prepare.Run()`。
- **关键指标**: 守护测试通过；代码检查确认调用链存在。
- **备注**: 满足 AC1、AC2、AC7、AC8 的入口集成要求。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: `common/common.go` 不再以旧 JSON 引导初始化；`common/var.go` 中 legacy JSON 读取逻辑已移除，仅保留兼容空壳初始化入口。
- **关键指标**: 守护测试通过；代码检查未发现 `.storage.json` / `.cpp.json` 初始化依赖。
- **备注**: 满足 AC1、AC9。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py` 与 `maze-py/*.py` 已删除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留。
- **关键指标**: 守护测试通过。
- **备注**: 满足迁移删除范围与保留范围要求。

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: PASS
- **实际行为**: 通过 `git diff origin/master...HEAD` 与 `prepare/legacy_mapping.go` 核对，历史 Python 模块到 Go 模块存在明确承接关系，例如：
  - `maze-py/process.py` → `prepare/process.go`
  - `maze-py/symbols.py` → `prepare/symbols.go`
  - `maze-py/gdb_wrapper.py` → `prepare/gdb.go` / `prepare/gdb_client.go` / `prepare/gdb_server.go`
  - `maze-py/python_lang.py` / `nodejs.py` / `lua.py` / `c_lang.py` → `prepare/lang_*.go`
  - `maze-py/*malloc*.py` → `prepare/alloc.go`
  - `maze-py/cpp_analyzer.py` / `cpp_vtable.py` → `prepare/cpp_analyzer.go` / `prepare/cpp_vtable.go`
  - `.maze.py` / `maze-py/storage.py` → `prepare/prepare.go` / `prepare/fill.go`
- **关键指标**: 未发现“历史 Python 文件被删但 Go 侧无对应承接”的明显缺口。
- **备注**: `prepare/alloc.go` 采用聚合实现而非逐文件拆分，但职责承接存在。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: PASS
- **实际行为**: 代码检查确认以下关键职责已在 Go 中落地，而非仅空壳占位：
  - `prepare.Run()` 主流程编排
  - `prepare/fill.go` 直接填充 `common.S` / `common.Cpp`
  - `prepare/gdb_client.go` 实现 GDB Socket Client 协议
  - `prepare/gdb_server.go` 负责 GDB Server 启动与连接
  - `prepare/process.go` 处理目标进程 / core / maps / exe 信息
  - `prepare/symbols.go` 处理符号与外部工具调用
  - `prepare/lang_*.go` 负责语言识别
  - `prepare/alloc.go` 负责分配器识别
  - `prepare/cpp_analyzer.go` 负责 C++ 分析准备
- **关键指标**: 主链路动态测试均可运行并产出正确结果。
- **备注**: 满足 AC1、AC5、AC6、AC7、AC8、AC9 的核心职责迁移要求。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 已变更为 shell wrapper，不再以 Python shebang 开头；其职责为构建并转发参数至 `.maze` 二进制。
- **关键指标**: `./maze --build` 成功；代码检查确认不再包含旧 Python wrapper 主逻辑。
- **备注**: 虽未直接内联到单一 Go 源文件，但对外已不依赖 Python 解释器。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍保留并继续使用 GDB Python API。
- **关键指标**: 守护测试通过；Node.js 代表性用例可联动通过。
- **备注**: 满足 AC10。

### TC-007: Python 主流程回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 执行通过，`validate.py` 断言满足。
- **关键指标**: 用例 PASS
- **备注**: 说明 Go prepare 已能替代原 Python 预处理完成 Python 主链路准备。

### TC-008: C++ 多线程栈变量回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260304-stack-multithread` 执行通过。
- **关键指标**: 用例 PASS
- **备注**: 多线程 C++ 栈变量分析未因迁移退化。

### TC-008A: 代表性分配器用例回归（jemalloc / mimalloc）
- **结果**: PASS
- **实际行为**:
  - `python3 testdata/run_test.py cpp/20260201-basic-malloc` → PASS
  - `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread` → PASS
  - `python3 testdata/run_test.py cpp/20260211-mimalloc-3-2-8-multithread` → PASS
- **关键指标**: ptmalloc / jemalloc / mimalloc 代表性用例均通过。
- **备注**: 本轮实际结果显示 mimalloc 用例可通过，与最新 dev report 中记录的失败现象不一致；QA 以本轮重跑结果为准。

### TC-008B: Node.js 代表性回归
- **结果**: PASS
- **实际行为**: 由于优先用例 `nodejs/20260211-comprehensive` 缺少 tarball，`nodejs/20260226-container-size` 的 `validate.py` 又不符合 `run_test.py` 约定，本轮改用仓库内可执行的代表性用例 `python3 testdata/run_test.py nodejs/20260211-01-basic-types`，结果通过。
- **关键指标**: `nodejs/20260211-01-basic-types` PASS
- **备注**: 证明 Node.js 主链路、V8 相关准备流程以及保留的 GDB Python 脚本可正常配合；但优先扩展用例未能完成，详见“未执行项”。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: PASS
- **实际行为**: 通过 `run_test.py` 间接执行 `maze --tar ... --text --json-output`，Python/C++/Node.js 代表性用例均可走通单入口分析。
- **关键指标**: 多个 `run_test.py` 用例 PASS
- **备注**: 满足 AC2、AC8。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: PASS
- **实际行为**: 代码检查确认遗留 `.maze.py` / `maze-py` 已删除、GDB Python 脚本保留、`main.go` 主入口已迁移至 Go prepare 主链路。
- **关键指标**: 仓库级守护测试通过；动态回归未发现 prepare/GDB 致命错误。
- **备注**: `standalone` / `postmanprofile` 未做独立高成本动态启动验证，本项主要基于代码路径与现有回归覆盖给出通过结论；残余风险见下文。

## 失败的 Test Case（如有）

本轮正式验收选择的 TC 均通过，无正式 TC 失败。

以下为额外尝试时发现的测试数据问题，未判定为本次迁移实现失败：

### 附加发现 1: nodejs/20260211-comprehensive 无法通过 run_test.py 执行
- **结果**: FAIL（测试数据侧）
- **预期行为**: 目录内应包含可被 `run_test.py` 识别的 `coredump-*.tar.gz`。
- **实际行为**: 目录中仅有 `README.md`、`test.js`、`validate.py`，缺少 tarball。
- **复现步骤**: `python3 testdata/run_test.py nodejs/20260211-comprehensive`
- **可能原因**: 测试数据目录未完整提交或 tarball 遗失。
- **建议修复**: 补齐该目录 tarball，或将文档型目录与可执行测试目录分离命名。

### 附加发现 2: nodejs/20260226-container-size 的 validate.py 不符合 run_test.py 约定
- **结果**: FAIL（测试数据侧）
- **预期行为**: `validate.py` 应提供 `validate(data)` 并返回布尔值。
- **实际行为**: `run_test.py` 报错：`module 'validate' has no attribute 'validate'`。
- **复现步骤**: `python3 testdata/run_test.py nodejs/20260226-container-size`
- **可能原因**: 测试脚本接口未按当前测试框架标准实现。
- **建议修复**: 按 `run_test.py` 契约补齐 `validate(data)` 入口。

## 未执行项

- **Node.js 优先扩展用例 `nodejs/20260211-comprehensive` 的有效动态验证未完成**
  - 原因：缺少 tarball，无法进入 Maze 分析阶段。
  - 影响范围：TC-008B 的优先路径覆盖不足，降低对 Node.js 综合场景的信心。

- **Node.js 优先扩展用例 `nodejs/20260226-container-size` 的有效动态验证未完成**
  - 原因：`validate.py` 接口不兼容 `run_test.py`。
  - 影响范围：TC-008B 中容器大小相关准确性未完成自动化验收。

- **`standalone` / `postmanprofile` 未做独立动态模式专项验证**
  - 原因：本轮以低成本静态核对 + `--tar` 主链路回归为主，且未发现会阻断主链路的实现问题。
  - 影响范围：主要影响 AC7 的动态覆盖完整度；当前结论更多依赖代码检查与现有回归间接证明。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 10 / 10
- 失败: 0 / 10
- 建议: **可以继续推进，但建议后续补齐 Node.js 扩展测试数据并增加 AC7 动态专项验证。**
