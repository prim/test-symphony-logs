# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮完成了低成本静态核对、迁移守护测试、Python/C++/jemalloc 主链路回归，以及 standalone 冒烟验证；但 Node.js 代表性验收未形成可通过闭环，且删除的部分 Python 模块未能在 Go 侧看到清晰的一一对应实现与职责承接，因此总体结论为 **FAIL**。

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `go test ./qa/issue110` 通过，`prepare/prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等核心文件均存在。
- **关键指标**: `go test ./qa/issue110` = PASS
- **备注**: 满足基础脚手架要求。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，并在 `maze()` 主流程中调用 `prepare.Run()`，失败时会直接退出。
- **关键指标**: `qa/issue110/TestMainUsesPrepareRun` = PASS
- **备注**: 入口主链路已切换到 Go prepare。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: 守护测试通过；静态检查中未见 `common/common.go` 再调用旧 `initConfig()`，`common/var.go` 也未继续从 `.storage.json` / `.cpp.json` 做旧式启动初始化。
- **关键指标**: `qa/issue110/TestCommonInitNoLongerDependsOnLegacyJSONBootstrap` = PASS
- **备注**: AC1/AC9 对应的旧 JSON bootstrap 已去除。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py` 与 `maze-py/` 已删除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留。
- **关键指标**: `qa/issue110/TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain` = PASS
- **备注**: 满足“删除预处理层、保留 GDB 内部 Python 脚本”的最低要求。

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: FAIL
- **实际行为**: 虽然大部分职责迁入 `prepare/`，但静态核对发现并非所有被删除 Python 模块都能在 Go 侧看到清晰的一一对应文件或等价职责承接：
  - `maze-py/asiocore.py` 已删除，但当前 `prepare/` 中未见 `asiocore.go`，也未检索到 `AsiocoreManager.Address()` / `Mb2DetectPyType()` 等核心方法。
  - `maze-py/mosd.py`、`maze-py/txos.py` 已删除，但当前仅在 `alloc.go` 中可见部分类型/符号处理；未检索到计划文档列出的 `TestRobinMap()`、`FindCppNames()`、`Misc()` 等关键承接逻辑。
  - 计划文档声明的包结构中包括 `prepare/asiocore.go`、`prepare/mosd.go`、`prepare/txos.go`、`prepare/g78.go`、`prepare/taggeddict.go` 等拆分文件，但当前提交并未完整落成该对应结构。
- **关键指标**: `git diff --name-status origin/master...HEAD` 显示删除了 22 个 `maze-py/*.py`；新增 Go 文件主要集中在 `prepare/alloc.go`、`prepare/lang_*.go`、`prepare/process.go`、`prepare/gdb*.go` 等，模块级映射不完整。
- **备注**: 这直接影响人审要求“删除的 Python 文件与新增 Go 文件是否一一对应”。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **实际行为**: 主链路核心职责已明显迁移，包括 `prepare.Run()` 编排、`FillCommonVars()` 直填 `common.S/common.Cpp`、GDB Socket Client/Server、process、symbols、语言检测、分配器检测等；但对项目特定模块的迁移完整性不足，存在“字段存在、职责未见完整落地”的情况。
- **关键指标**:
  - 已确认存在：`prepare.Run()`、`FillCommonVars()`、`GDBClient.Execute/ExecuteBatch/Evaluate/Ping`、`GDBServer.Start/Stop`、`ProcessManager.Init/DoMaps/Vms/setupLanguage`、`SymbolsManager.Run/DownloadSymbolFiles`
  - 未确认或缺失：`AsiocoreManager.Address()` / `Mb2DetectPyType()`、`MosdManager.TestRobinMap()`、`TxosManager.FindCppNames()` / `Misc()`
- **备注**: 对 AC1/AC7/AC9 的“完全重写”仍存在范围风险。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 入口已变为 shell wrapper，负责构建并执行 Go 二进制 `./.maze`；不再是 Python shebang，也未保留 `do_build`、`do_tar`、`do_release` 等旧 Python wrapper 逻辑。
- **关键指标**: `qa/issue110/TestMazeEntryIsNoLongerPythonWrapper` = PASS
- **备注**: 入口不再依赖 Python wrapper。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍包含 `import gdb`，说明 GDB 内部脚本仍走 Python API。
- **关键指标**: `qa/issue110/TestGdbScriptsStillUsePythonAPI` = PASS
- **备注**: AC10 静态要求满足。

### TC-007: Python 主流程回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；Go prepare 能完成 Python 版本识别、类型与符号准备，validate 中的对象数量断言全部通过。
- **关键指标**: `pymempool_objects = 68626`，`PersonClass = 1000`，`GameEntity = 300`，`TreeNode = 600`，`SimpleClass = 500`
- **备注**: AC3 通过。

### TC-008: C++ 多线程栈变量回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260304-stack-multithread` 通过；分析主流程可正常完成。
- **关键指标**: validate 输出“Analysis completed successfully”；总结果项 `10`
- **备注**: validate 提示 `ThreadObject` 未找到，但该用例定义仍判定通过，因此当前按 PASS 记录；建议开发继续关注多线程栈局部变量识别精度。

### TC-008A: 代表性分配器用例回归（jemalloc）
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread` 通过；jemalloc 5.3.0 代表性场景可完成分析与验证。
- **关键指标**: 10/10 个 size bucket 满足阈值，`Overall ratio = 200.05%`
- **备注**: 至少一个非 ptmalloc 分配器链路已验证通过。

### TC-008B: Node.js 代表性回归
- **结果**: FAIL
- **实际行为**:
  1. 按优先用例执行 `python3 testdata/run_test.py nodejs/20260211-comprehensive`，测试目录缺少 tar.gz，命令直接失败。
  2. 改测备选用例 `python3 testdata/run_test.py nodejs/20260226-container-size`，Maze 主分析流程可跑通，日志中可见 `Node.js version: 20.20.0`、`Loaded 88 V8 constants`、`Found V8 Isolate at 0x403d8bc0`，但最终验证阶段因测试脚本接口错误 `module 'validate' has no attribute 'validate'` 失败。
- **关键指标**: Node.js 主流程能输出分析结果，但没有形成 `run_test.py` 验收通过闭环。
- **备注**: 该项导致 Node.js 类别无法确认 AC1/AC5/AC6/AC8/AC10 在现有验收框架下真正通过。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: PASS
- **实际行为**: Python、C++、jemalloc、Node.js 备选用例均通过 `run_test.py` 间接触发 `maze --tar ... --text --json-output`；除 Node.js 验证脚本问题外，`--tar` 主链路均能成功启动与输出结果。
- **关键指标**: `python/20260129-complex-types-311`、`cpp/20260304-stack-multithread`、`cpp/20260211-jemalloc-5-3-0-multithread` 均通过 `--tar` 方式完成
- **备注**: AC8 在 Python/C++ 主链路上成立。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: PASS
- **实际行为**: 
  - `main.go` 已保留 `standalone` / `postmanprofile` 模式分发；
  - `--tar` 会切换到 `postmanprofile` 处理；
  - 额外执行 standalone 冒烟命令：
    `./maze --pid <临时 Python 进程 PID> --mode standalone --text --json-output --limit 50`
    成功完成分析并输出结果。
- **关键指标**: standalone 冒烟测试通过；`prepare.Run()` 在 standalone 模式可运行成功
- **备注**: AC7 的 standalone / postmanprofile 两条模式主链路均已至少做过一次动态验证。

## 失败的 Test Case

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: FAIL
- **预期行为**: 不存在“Python 文件已删除但没有 Go 对应模块承接职责”的遗漏。
- **实际行为**: 项目特定模块存在删除后未看到清晰 Go 对应实现或完整职责承接的情况，尤其是 `asiocore`、`mosd`、`txos` 相关能力。
- **复现步骤**:
  1. 执行 `git diff --name-status origin/master...HEAD`
  2. 核对删除的 `maze-py/*.py` 文件与新增 `prepare/*.go`
  3. 在 `prepare/` 中搜索 `AsiocoreManager.Address`、`Mb2DetectPyType`、`TestRobinMap`、`FindCppNames`、`Misc`
- **可能原因**: 当前实现把部分项目特定逻辑集中/折叠进了 `alloc.go` 或其他模块，但没有完整迁移原 Python 侧能力，或至少没有形成可核验的等价承接。
- **建议修复**: 对照删除文件清单补齐模块映射表，并将缺失职责明确迁移到 Go 侧可检索实现中；至少保证 QA 可从代码层确认“删除文件 → Go 目标模块 → 关键方法”的闭环。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **预期行为**: 主链路关键职责均已在 Go 中落地；如仅为空壳/占位，需判定失败。
- **实际行为**: 核心通用链路已落地，但项目特定模块职责仍有明显空洞，无法证明是“全部重写完成”。
- **复现步骤**:
  1. 阅读 `prepare/prepare.go`、`prepare/fill.go`、`prepare/process.go`、`prepare/gdb*.go`、`prepare/symbols.go`
  2. 再搜索 `AsiocoreManager`、`MosdManager`、`TxosManager` 的关键方法
  3. 对照 `dev-flow/plan.md` 的 Phase 4 与包结构设计核对
- **可能原因**: 当前提交优先打通了 Python/C++ 主链路，但项目特定分支尚未全部完成。
- **建议修复**: 补齐未落地的项目特定逻辑，或在代码结构中显式承接并补充对应测试。

### TC-008B: Node.js 代表性回归
- **结果**: FAIL
- **预期行为**: 至少 1 个 Node.js 代表性用例通过 `python3 testdata/run_test.py ...` 完成验收。
- **实际行为**: 优先用例缺少 tarball；备选用例主分析成功但验证脚本报 `module 'validate' has no attribute 'validate'`，未能形成 PASS。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py nodejs/20260211-comprehensive`
  2. 执行 `python3 testdata/run_test.py nodejs/20260226-container-size`
- **可能原因**:
  - `20260211-comprehensive`：测试数据目录不完整；
  - `20260226-container-size`：测试脚本未按 `run_test.py` 约定提供 `validate(data)`。
- **建议修复**: 补齐可执行的 Node.js 代表性 testdata，并确保 `validate.py` 符合当前测试框架接口；随后重新执行 Node.js 类别验收。

## 已执行的检查项
- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告
- 审查 `git diff origin/master...HEAD`、`git diff --stat`
- 运行 `./maze --build`
- 运行 `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 运行 `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- 运行 `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
- 运行 `python3 testdata/run_test.py nodejs/20260211-comprehensive`
- 运行 `python3 testdata/run_test.py nodejs/20260226-container-size`
- 运行 standalone 冒烟：对临时 Python 进程执行 `./maze --pid <PID> --mode standalone --text --json-output --limit 50`

## 未执行项
- 未额外执行第二个 Python 用例 `python/20260201-py-merge`
  - 原因: 主 Python 主链路已通过，本轮优先覆盖人审新增的 Node.js、非 ptmalloc、standalone 范围
  - 影响范围: 对 AC1/AC3 的 Python 扩展覆盖度有限，但不影响主链路已通过结论
- 未额外执行 mimalloc 用例
  - 原因: 已完成 jemalloc 非 ptmalloc 代表性覆盖，Node.js 类别已出现阻塞，不再继续扩展高成本动态回归
  - 影响范围: mimalloc 兼容性仍未单独验证，影响 AC6/AC7 的扩展覆盖度

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30 秒
- [ ] 内存占用 < 分析目标内存的 10%

## AC 核对结论
- **AC1**: FAIL / PARTIAL — Python/C++ 主链路与迁移守护测试通过，但“全部重写完成”仍受 TC-004A/004B 与 Node.js 验收未闭环影响
- **AC2**: PASS — `./maze --build` 通过
- **AC3**: PASS — `python/20260129-complex-types-311` 通过
- **AC4**: PASS — `cpp/20260304-stack-multithread` 通过
- **AC5**: PARTIAL — GDB Socket 主链路在 Python/C++/jemalloc/Node.js 备选场景可运行，但 Node.js 代表性验收未闭环
- **AC6**: PARTIAL — 外部命令链路在已执行用例中正常，但 Node.js 类别未完成正式通过
- **AC7**: PASS — `postmanprofile`（tar）与 `standalone` 均已动态验证
- **AC8**: PARTIAL — `--tar` 主链路在已执行场景可用，但 Node.js 正式验收未通过
- **AC9**: FAIL — Python 旧文件虽然删除，但模块级对应与职责承接仍不完整
- **AC10**: PARTIAL — GDB Python 脚本保留且 Node.js 备选分析能运行，但缺少一个通过的 Node.js 代表性 `run_test.py` 验收结果

## 总结
- 通过: 8 / 11
- 失败: 3 / 11
- 建议: **暂不建议发布**。请优先修复两类问题：
  1. 补齐删除 Python 模块与 Go 实现之间的模块级对应和缺失职责；
  2. 修复/补齐 Node.js 代表性验收数据与验证脚本，形成至少一个可通过的 `run_test.py` 闭环。
