# 开发报告

## 结论

`PARTIAL`

本轮已按最新 QA 报告补齐缺失的 Go 迁移承接模块，并把遗漏的 collect / parse-dwarf 流程接入 `prepare.Run()`；代表性构建与回归已执行。但 AC1 的历史基线比对仍未通过，且 AC7 仅完成部分验证，因此本轮只能判定为部分修复。

## 完成的任务
- [x] Task 1: 阅读 `dev-flow/plan.md`、`.symphony_prompt.md` 与最新 QA 报告，确认本轮修复范围
- [x] Task 2: 为已删除的 Python 模块补齐明确的 Go 承接文件：`prepare/collect.go`、`prepare/messiah.go`、`prepare/cpp_vtable.go`
- [x] Task 3: 补全 `CollectMallocLib`、`MessiahManager` 的核心行为，并在 `prepare/context.go` 中补齐初始化状态
- [x] Task 4: 在 `prepare.Run()` 中接入 `Collect.InitIndexFile()`、`ParseDwarf.DownloadUseParseDwarf()`、`Collect.UpdateIndexFile()`、`ParseDwarf.UploadUseParseDwarf()`
- [x] Task 5: 在 `prepare/process.go` 中把 `asiocore` 路径处理接线到 `Messiah.Do(...)`
- [x] Task 6: 执行编译与代表性 testdata 回归，验证本轮修复不破坏主链路

## 修改的文件
- `prepare/context.go`: 为 `MessiahManager`、`CollectMallocLib`、`ParseDwarfManager` 增加状态字段，并补齐默认初始化
- `prepare/prepare.go`: 将 collect / parse-dwarf 生命周期步骤接入 `Run()`，并保留 prepare 快照导出逻辑
- `prepare/process.go`: 在 `DoMaps()` 中为 `asiocore` 接入 `Messiah.Do(...)`
- `prepare/collect.go`: 新增 malloc 库索引初始化、更新、上传逻辑，承接原 `maze_collect.py`
- `prepare/messiah.go`: 新增 Messiah 相关缓存/GDB/地址回填逻辑，承接原 `messiahserver.py`
- `prepare/cpp_vtable.go`: 新增显式 Go 模块，承接原 `cpp_vtable.py` 的禁用态接口

## 自测结果
- 测试命令: `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp ./maze --build`
  - 测试结果: 通过
- 测试命令: `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110 -run 'TestPreparePackageScaffoldExists|TestMainUsesPrepareRun|TestCommonInitNoLongerDependsOnLegacyJSONBootstrap|TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain|TestMazeEntryIsNoLongerPythonWrapper|TestGdbScriptsStillUsePythonAPI'`
  - 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
  - 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py cpp/20260304-stack-multithread`
  - 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py nodejs/20260211-01-basic-types`
  - 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
  - 测试结果: 通过
- 测试命令: `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110`
  - 测试结果: 失败；`TestPrepareRunMatchesLegacyJSONBaseline` 在 `deep_loop_structs` 历史基线对比中仍有差异

## 验收核对
- AC1: `FAIL` 历史 JSON 基线对比 `TestPrepareRunMatchesLegacyJSONBaseline` 仍失败，尚不能确认 `common.S` / `common.Cpp` 与旧 `.storage.json` / `.cpp.json` 完全一致
- AC2: `PASS` `./maze --build` 已通过
- AC3: `PASS` `python/20260129-complex-types-311` 已通过
- AC4: `PASS` `cpp/20260304-stack-multithread` 已通过
- AC5: `PASS` 静态与运行链路已覆盖 GDB Socket Client 主流程，代表性用例可正常执行
- AC6: `PASS` 本轮构建与回归中 `readelf`、`nm`、`strings`、`md5sum` 等外部命令链路可正常工作
- AC7: `PARTIAL` standalone / postmanprofile 相关代码路径已保留，但本轮未分别做完整双模式人工回归
- AC8: `PASS` `--tar` 路径已通过多个 testdata 用例间接验证
- AC9: `PARTIAL` 文件层删除已完成，且缺失模块承接已补齐；但受 AC1 未过影响，仍不能宣称迁移后全量行为完全等价
- AC10: `PASS` `gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留且相关守护测试通过

## 已完成项
- 已补齐 QA 指出的三个缺失/空壳迁移点：`messiahserver.py`、`maze_collect.py`、`cpp_vtable.py`
- 已补齐 `prepare.Run()` 中遗漏的 collect / parse-dwarf 生命周期调用
- 已完成构建与 Python/C++/Node.js/jemalloc 代表性回归

## 未完成项
- 修复 AC1 历史基线比对中的 `deep_loop_structs` 差异
- 补做 AC7 的 standalone / postmanprofile 显式分模式验证

## 未验证项
- 不同 glibc 版本兼容性
- 不同 Node.js 版本兼容性
- Python 2.7 兼容性
- 大内存进程性能指标

## 剩余工作
- 未完成项: 修复 AC1 基线差异；补做 AC7 双模式验证
- 未验证项: Python 2.7、跨 glibc/Node.js 版本、性能指标尚未覆盖

## Self-Review 发现与修复
- 发现 `CollectMallocLib` 之前仅为空返回，无法体现原 Python 模块职责；已补充索引初始化、更新与上传逻辑
- 发现 `MessiahManager` 之前仅有空结构体，无任何迁移承接；已补充缓存读取、GDB 回退执行与 Asiocore 地址回填
- 发现 `prepare.Run()` 未按计划接入 collect / parse-dwarf 关键步骤；已补充并在构建与回归中验证未引入明显回归
- 发现 `cpp_vtable.py` 虽历史上基本禁用，但缺少显式 Go 文件会导致迁移对应关系不清晰；已增加 `prepare/cpp_vtable.go` 保持接口承接

## 注意事项
- 本轮严格未修改 `testdata/` 下任何测试文件
- `go test ./qa/issue110` 的剩余失败集中在 AC1 历史快照等价性，不属于最新 QA 报告中“缺少模块承接/流程接线”的同类问题
- 仓库当前工作区存在大量与本任务无关的未跟踪文件与历史修改，提交时必须只暂存本轮目标源码文件，且不能提交 `dev-flow/plan.md`、`dev-flow/test-cases.md`、各类 `*-dev-report.md`、`*-qa-report.md`
