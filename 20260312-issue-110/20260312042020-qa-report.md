# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

阶段 A 的低成本检查已尽量完成，并已执行构建、迁移守护测试、Python 主链路 smoke、C++ 主链路 smoke。结果显示当前实现仍不满足本需求的关键验收标准，尤其是：

- AC1 仍失败：`prepare.Run()` 产物与历史 `.storage.json` / `.cpp.json` 基线不一致。
- AC3 失败：Python 代表性用例实际运行时 panic。
- AC4 失败：C++ 代表性用例在 GDB 初始化阶段即失败。
- 因主链路已被 smoke test 证明不成立，按早停策略未继续执行 Node.js 与更多分配器动态回归。

## 已执行检查项

### 阶段 A：低成本 / 静态检查

- 已阅读：
  - `dev-flow/plan.md`
  - `dev-flow/test-cases.md`
  - 最新开发报告 `dev-flow/20260312041302-dev-report.md`
- 已检查 `git diff origin/master...HEAD` 变更范围。
- 已核对删除的 Python 预处理文件与新增 Go 文件对应关系。
- 已阅读关键 Go 迁移文件：
  - `prepare/prepare.go`
  - `prepare/fill.go`
  - `prepare/context.go`
  - `prepare/gdb.go`
  - `prepare/gdb_client.go`
  - `prepare/gdb_server.go`
  - `prepare/process.go`
  - `prepare/symbols.go`
  - `prepare/alloc.go`
  - `prepare/corefile.go`
  - `prepare/lang_python.go`
  - `prepare/lang_nodejs.go`
  - `prepare/collect.go`
  - `prepare/messiah.go`
  - `prepare/cpp_vtable.go`
  - `main.go`
  - `common/common.go`
  - `common/var.go`
- 已执行仓库级迁移守护测试。

### 阶段 B：关键 smoke test

- `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp ./maze --build`
- `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110 -run 'TestPreparePackageScaffoldExists|TestMainUsesPrepareRun|TestCommonInitNoLongerDependsOnLegacyJSONBootstrap|TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain|TestMazeEntryIsNoLongerPythonWrapper|TestGdbScriptsStillUsePythonAPI|TestPrepareRunMatchesLegacyJSONBaseline'`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
- `python3 testdata/run_test.py cpp/20260304-stack-multithread`

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `prepare/` 下已存在 `prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等关键文件。
- **关键指标**: `go test` 静态守护项通过。
- **备注**: 文件存在性满足要求。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，并在 `maze()` 中调用 `prepare.Run()`。
- **关键指标**: 主流程接线可见于 `main.go:226-239`。
- **备注**: 满足入口集成要求。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: `common/common.go` 不再调用旧 `initConfig()`；`common/var.go` 中不再读取 `.storage.json` / `.cpp.json` 初始化 `S` / `Cpp`。
- **关键指标**: 静态守护测试通过。
- **备注**: 旧 JSON bootstrap 已移除。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py`、`maze-py/` 已删除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留。
- **关键指标**: `git diff --name-status origin/master...HEAD` 与守护测试一致。
- **备注**: 文件级清理与保留范围基本正确。

### TC-004A: 删除的 Python 模块与新增 Go 模块一一对应
- **结果**: PASS
- **实际行为**: 历史删除模块均能找到对应 Go 承接文件或聚合承接文件，例如：
  - `.maze.py` → `prepare/prepare.go` + `prepare/fill.go` + `main.go`
  - `maze-py/gdb_wrapper.py` → `prepare/gdb.go` + `prepare/gdb_client.go` + `prepare/gdb_server.go`
  - `maze-py/process.py` → `prepare/process.go`
  - `maze-py/symbols.py` → `prepare/symbols.go`
  - `maze-py/python_lang.py` / `lua.py` / `nodejs.py` / `c_lang.py` → `prepare/lang_*.go`
  - `maze-py/libc.py` / `tcmalloc.py` / `jemalloc.py` / `mimalloc.py` / `openblas.py` / `g78.py` / `taggeddict.py` / `mosd.py` / `txos.py` / `asiocore.py` → `prepare/alloc.go`
  - `maze-py/cpp_analyzer.py` → `prepare/cpp_analyzer.go`
  - `maze-py/cpp_vtable.py` → `prepare/cpp_vtable.go`
  - `maze-py/maze_collect.py` → `prepare/collect.go`
  - `maze-py/messiahserver.py` → `prepare/messiah.go`
  - `maze-py/maze_db.py` → `prepare/db.go`
  - `maze-py/corefile.py` → `prepare/corefile.go`
  - `maze-py/maze_utils.py` / `maze_globals.py` → `prepare/utils.go` / `prepare/context.go`
- **关键指标**: `git diff --name-status origin/master...HEAD` 中删除 24 个 Python 预处理文件，新增 21 个 `prepare/*.go` 文件。
- **备注**: 文件级一一对应基本成立，但“有文件承接”不代表“行为完全等价”。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **实际行为**: 主链路关键逻辑虽已落地到 Go，但存在明确功能缺口或行为不一致：
  1. `prepare.Run()` 仍通过 `dumpPreparedSnapshot()` 导出 legacy 快照，但与历史基线不一致。
  2. `prepare/gdb.go` 的 `InitServerForCorefile()` 在 tar/postmanprofile 场景无法可靠解析原始 exe 到解压后实际路径，导致 C++ smoke 失败。
  3. Python 主链路进入分析引擎后在 `ptmalloc.initPtmallloc()` 处 panic，说明 prepare 填充到 `common.S` / `common.Cpp` 的 ctypes / sizeof 数据不足以支撑旧分析逻辑。
- **关键指标**:
  - `go test ./qa/issue110` 失败：`storage.deep_loop_structs.1 仅存在于历史基线`
  - Python smoke 失败：`panic: initPtmalloc sizeof _heap_info`
  - C++ smoke 失败：`failed to resolve executable path for gdb`
- **备注**: 文件不再是空壳，但 AC1/AC3/AC4 级别的等价性未达标。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 不再是 Python shebang 的旧 wrapper，守护测试通过。
- **关键指标**: 旧 `do_build` / `do_tar` / `do_release` 标记未再出现在入口文件中。
- **备注**: 入口形式满足要求。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍包含 `import gdb`。
- **关键指标**: 守护测试通过。
- **备注**: GDB 内部 Python 脚本保留正确。

### TC-007: Python 主流程回归
- **结果**: FAIL
- **实际行为**: `prepare.Run()` 本身可完成，但随后进入 Go 分析阶段后 panic，测试整体失败。
- **关键指标**:
  - 命令：`python3 testdata/run_test.py python/20260129-complex-types-311`
  - 失败位置：`maze/mallocer/ptmalloc.initPtmallloc()`
  - 报错：`panic: initPtmalloc sizeof _heap_info`
- **备注**: 这直接表明 Go prepare 生成的数据未能满足 Python 主链路对 ptmalloc ctypes 的依赖。

### TC-008: C++ 多线程栈变量回归
- **结果**: FAIL
- **实际行为**: 用例在 GDB 初始化 corefile 阶段即失败，未进入后续 C++ 分析。
- **关键指标**:
  - 命令：`python3 testdata/run_test.py cpp/20260304-stack-multithread`
  - 报错：`prepare.Run() failed: failed to init GDB for corefile: failed to resolve executable path for gdb: /home/hcjn0770/github/codemaker-maze/testdata/cpp/20260304-stack-multithread/stack_multithread_test`
- **备注**: tar/postmanprofile 场景的可执行文件路径解析存在阻塞问题。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: FAIL
- **实际行为**: `--tar` 路径虽然能解包并启动 prepare，但两个代表性 smoke 均失败：Python 用例在分析阶段 panic，C++ 用例在 GDB 初始化阶段失败。
- **关键指标**: `mainTarMode()` 入口可达，但主链路不成立。
- **备注**: AC8 当前不能判定通过。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **实际行为**: 文件清理层面通过，但 `postmanprofile` 模式在 C++ tar 用例上被阻塞；standalone 模式本轮未独立完成有效正向动态验证。
- **关键指标**: `prepare.Run()` 输出 `Mode: postmanprofile` 时在 C++ 用例失败。
- **备注**: AC7 仍不能通过。

## 失败的 Test Case

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: FAIL
- **预期行为**: 主链路关键职责在 Go 中完整落地，且与历史逻辑行为等价。
- **实际行为**: 存在历史基线差异、tar 模式 GDB 初始化失败、Python 主链路 panic。
- **复现步骤**:
  1. `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110 -run TestPrepareRunMatchesLegacyJSONBaseline`
  2. 观察 python 子用例基线差异与 cpp 子用例 GDB 初始化失败。
- **可能原因**:
  - `FillCommonVars()` / GDB ctypes 采集结果与旧 JSON 结构仍有遗漏。
  - `resolveExistingElfPath` / `ResolveExistingPath` 对 tar 解压后二进制映射处理不完整。
- **建议修复**:
  - 逐字段对比历史 `.storage.json` / `.cpp.json`，补齐 `deep_loop_structs` 等缺失数据。
  - 修正 tar/postmanprofile 模式下 exe 映射到解压后真实文件的逻辑，并增加回归测试。

### TC-007: Python 主流程回归
- **结果**: FAIL
- **预期行为**: `python/20260129-complex-types-311` 应通过 validate。
- **实际行为**: 进入 `mallocer/ptmalloc` 后 panic，测试退出码为 2。
- **复现步骤**:
  1. 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  2. 观察 `panic: initPtmalloc sizeof _heap_info`
- **可能原因**:
  - prepare 阶段未正确填充 ptmalloc 所需的结构体 sizeof/offset。
  - `GDB.Ctypes()` 缓存读取或实时收集不完整，导致 `common.S.Structs/Sizeof` 缺项。
- **建议修复**:
  - 检查 `prepare/gdb.go` 中 ctypes 合并与 `isValidLibcCtypes` 的实际结果。
  - 对照 legacy `.storage.json` 核验 `_heap_info`、`malloc_state` 等关键 ctypes 是否存在。

### TC-008: C++ 多线程栈变量回归
- **结果**: FAIL
- **预期行为**: `cpp/20260304-stack-multithread` 应完成分析并通过 validate。
- **实际行为**: GDB 初始化 corefile 前即失败，无法加载 exe。
- **复现步骤**:
  1. 运行 `python3 testdata/run_test.py cpp/20260304-stack-multithread`
  2. 观察 `failed to resolve executable path for gdb`
- **可能原因**:
  - tar 包中记录的是历史原始路径，prepare 未能稳定映射到解压目录下的 md5 命名 ELF。
  - `InitServerForCorefile()` 对 exe 解析失败后直接返回，没有继续使用已知解压文件回退。
- **建议修复**:
  - 补强 tar/postmanprofile 的 exe 映射与回退策略。
  - 为 `ResolveExistingPath` / `resolveExistingElfPath` 增加专门回归测试覆盖 C++ tar 场景。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: FAIL
- **预期行为**: `--tar` 一键分析对代表性 Python/C++ 用例可稳定工作。
- **实际行为**: Python 与 C++ 两条代表性链路均失败。
- **复现步骤**:
  1. 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  2. 运行 `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- **可能原因**:
  - prepare 与分析引擎的接口数据未完全等价。
  - postmanprofile tar 场景路径解析存在阻塞问题。
- **建议修复**:
  - 先修复 AC1 与 C++ exe 路径映射，再重跑 tar 主链路回归。

## 低成本代码核对结论

### 1. 删除的 Python 文件与新增 Go 文件模块级对应关系

结论：**文件级对应关系基本完整，但行为级等价性未达标。**

### 2. 主流程接线情况

结论：**Go prepare 已接入主流程，但主链路仍存在功能性阻塞。**

- `main.go` 中 `maze()` 已先调用 `prepare.Run()`，再调用 `maze0()/maze1()`。
- `common/common.go` 已改为依赖 prepare 直接填充 `common.S` / `common.Cpp`。
- `prepare/prepare.go` 已接入 corefile、process、GDB、symbols、collect、fill 等流程。

### 3. 明显问题点

- `go test ./qa/issue110` 已证明 AC1 未达成。
- `python/20260129-complex-types-311` 已证明 prepare 输出不足以支撑后续 ptmalloc 初始化。
- `cpp/20260304-stack-multithread` 已证明 tar/postmanprofile 的 GDB 初始化主链路不成立。

## 未执行项

基于早停策略，以下高成本动态验证未继续执行：

- TC-008A：非 ptmalloc 分配器用例（如 `cpp/20260211-jemalloc-5-3-0-multithread`）
- TC-008B：Node.js 代表性用例（如 `nodejs/20260211-comprehensive`）
- 更完整的 standalone / postmanprofile 双模式人工动态回归

### 未执行原因

- 关键 smoke test 已明确失败：
  - Python 主链路 panic
  - C++ 主链路在 GDB 初始化阶段失败
- 继续执行更多动态用例大概率只会重复暴露相同 prepare 主链路问题，收益低于先修复阻塞项。

### 可能影响的 AC / TC 范围

- **AC5 / AC7 / AC8 / AC10**：Node.js 与非 ptmalloc 分配器相关路径尚未进一步确认。
- **TC-008A / TC-008B / TC-010**：未完成扩展回归，不能宣称兼容性通过。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 已执行代表性用例，但结果失败
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp ./maze --build`：通过
- `TMPDIR=/home/hcjn0770/temp/worktrees/issue-110/tmp go test ./qa/issue110 -run '...|TestPrepareRunMatchesLegacyJSONBaseline'`：失败
  - python 基线差异：`storage.deep_loop_structs.1 仅存在于历史基线`
  - cpp tar 路径失败：`failed to resolve executable path for gdb`
- `python3 testdata/run_test.py python/20260129-complex-types-311`：失败
  - `panic: initPtmalloc sizeof _heap_info`
- `python3 testdata/run_test.py cpp/20260304-stack-multithread`：失败
  - `failed to resolve executable path for gdb`

## Findings

- Finding 1: AC1 仍未满足，prepare 输出与历史 JSON 基线不一致。
- Finding 2: Python 主链路存在功能性回归，ptmalloc 初始化直接 panic。
- Finding 3: C++ tar/postmanprofile 主链路存在路径解析阻塞，GDB 无法加载目标 exe。
- Finding 4: 文件级迁移对应关系已基本补齐，但不能据此判定迁移完成。

## 总结

- 通过: 6 / 10
- 失败: 4 / 10
- 建议: **当前不建议发布。** 应优先修复 AC1 基线等价问题、Python ptmalloc ctypes 缺失问题，以及 tar/postmanprofile 场景的 exe 路径解析问题；修复后再重跑 Python / C++ / Node.js / 非 ptmalloc 分配器回归。
