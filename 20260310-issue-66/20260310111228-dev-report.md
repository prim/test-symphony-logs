## Summary

Implemented issue #66 by bulk-migrating non-test Go code away from the legacy result aggregation and classify boundary identifiers. All upstream callers were updated to use the renamed result collection APIs and new classify base constants, with no legacy identifier wrappers left behind in non-test Go sources.

## Changes
- `common/result_items.go`: renamed the global result table, helper APIs, merge helpers, and related temporary state used by result aggregation.
- `common/classifier.go`: replaced legacy classify boundary constants with the new base constant names.
- `common/callback.go`, `common/webui.go`, `common/txos.go`, `common/c.go`, `common/utils.go`: migrated shared common-layer callers to the renamed result APIs/state.
- `python/*.go`: migrated Python analysis code to the renamed result APIs and fixed affected logging format issues exposed by validation/build.
- `txos/*.go`: migrated TXOS analysis code to the renamed result APIs and fixed affected logging/call-site issues.
- `mallocer/**/*.go`: migrated allocator walkers and merge logic to the renamed result APIs/constants.
- `nodejs/*.go`, `cpp/*.go`, `lua/*.go`, `main.go`, `http.go`: updated remaining upstream callers and fixed formatting/build issues uncovered during verification.
- `dev-flow/plan.md`: marked the plan items as completed.

## Testing
- `go test ./common -run 'TestLegacy(ResultAggregationIdentifiers|CustomClassifyBoundaryConstants)Removed'`
  - Passed.
- `./maze --build`
  - Passed (exit code 0).
- `git submodule update --init --recursive`
  - Initialized missing `testdata` and `prim-gdb` submodules for regression testing.
- `python3 testdata/run_test.py python/20260129-complex-types-311`
  - Passed.

## Notes
- `go test ./...` is not a reliable project-wide gate in this repository because unrelated pre-existing packages/scripts fail independently (for example `scripts/`, `sub-repos/maze-web-server/`, and `sub-repos/lua-bak/`). The issue-specific legacy-removal tests passed, and the project build plus one real regression case also passed.
- The legacy identifier verification still only appears in `common/legacy_scheme_removal_test.go`, which is expected because the test intentionally scans for forbidden names.
