# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本次验证覆盖了需求文档中的全部验收标准：
- `.maze.py` 已缩减到 1000 行以内（实际 254 行）
- Python 回归用例通过：`python3 testdata/run_test.py python/20260129-complex-types-311`
- C++ 回归用例通过：`python3 testdata/run_test.py cpp/20260304-stack-multithread`
- 环境中可确认同时存在 Python 2.7 与 Python 3.x，且源码结构测试未发现明显破坏 Python 2.7 兼容性的语法

## 详细结果

### TC-001: 剩余类均已拆分为独立模块
- **结果**: PASS
- **实际行为**: `maze-py/` 目录下已存在 `storage.py`、`corefile.py`、`symbols.py`、`lua.py`、`libc.py`、`c_lang.py`、`openblas.py`、`jemalloc.py`、`tcmalloc.py`、`mimalloc.py`、`python_lang.py`、`gdb_wrapper.py`、`process.py`、`cpp_analyzer.py`、`cpp_vtable.py`
- **关键指标**: 目标模块 15/15 存在
- **备注**: 通过 `tests.test_issue_94_split_remaining_classes` 验证

### TC-002: `.maze.py` 不再保留剩余类内联定义
- **结果**: PASS
- **实际行为**: `.maze.py` 中未发现 `Storage/Corefile/Symbols/Lua/Libc/C/OpenBLAS/Jemalloc/Tcmalloc/Mimalloc/Python/GDB/Process/Cpp/CppVtable` 的内联类定义
- **关键指标**: 结构测试通过
- **备注**: 通过 `tests.test_issue_94_split_remaining_classes` 验证

### TC-003: builtins 注入清单完整
- **结果**: PASS
- **实际行为**: `.maze.py` 中保留了 Python 2/3 兼容的 `builtins` 导入逻辑，并显式注册了测试要求的 builtins 契约项
- **关键指标**: 结构测试通过
- **备注**: 额外观察到 `builtins.builtins = builtins` 已显式暴露

### TC-004: 导入顺序满足依赖层级
- **结果**: PASS
- **实际行为**: `.maze.py` 中拆分模块导入顺序满足 `storage -> corefile -> symbols -> lua/libc/c_lang/openblas -> jemalloc/tcmalloc/mimalloc -> python_lang -> gdb_wrapper -> process -> cpp_analyzer -> cpp_vtable`
- **关键指标**: 结构测试通过
- **备注**: 通过 `tests.test_issue_94_split_remaining_classes` 验证

### TC-005: 拆分模块无需显式导入基础运行时依赖
- **结果**: PASS
- **实际行为**: 已抽查 `maze-py/storage.py`、`maze-py/process.py`、`maze-py/python_lang.py`，并结合结构测试确认未显式导入 `builtins/__builtin__/maze_globals/maze_utils/maze_db/maze_collect` 等基础运行时依赖
- **关键指标**: 结构测试通过
- **备注**: 符合 builtins-only 依赖约束

### TC-006: 仅靠 builtins 注入即可导入拆分模块
- **结果**: PASS
- **实际行为**: 单元测试已在最小 builtins 桩对象环境下动态导入拆分模块成功
- **关键指标**: 结构测试通过
- **备注**: 验证了拆分模块的独立可导入性

### TC-007: 缺少关键 builtins 时快速失败
- **结果**: PASS
- **实际行为**: 单元测试覆盖了移除 `builtins.singleton` 后导入失败的路径
- **关键指标**: 结构测试通过
- **备注**: 说明模块依赖契约清晰，不会静默走错路径

### TC-008: `.maze.py` 体积达到目标
- **结果**: PASS
- **实际行为**: `.maze.py` 当前总行数为 254 行
- **关键指标**: 254 <= 1000
- **备注**: 满足 AC 中的行数目标

### TC-009: 源码保持 Python 2.7 / 3.x 兼容语法边界
- **结果**: PASS
- **实际行为**: 结构测试通过，未发现 `async def`、类型注解赋值、f-string 等明显 Python 3 专属语法；环境中同时存在 `Python 2.7.18` 与 `Python 3.11.2`
- **关键指标**: 结构测试通过
- **备注**: 本轮未实际执行完整 Python 2.7 回归用例，仅完成环境可用性与源码语法边界验证

### TC-010: Python 分析回归用例通过
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 执行通过，结果校验全部通过
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`；`TreeNode = 600`；`SimpleClass = 500`；`Player = 500`；`Config = 300`；`Point = 400`；`Rectangle = 300`
- **备注**: 运行日志中虽出现 `./bin/s3: 没有那个文件或目录` 提示，但未影响本用例分析与校验结果

### TC-011: C++ 分析回归用例通过
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260304-stack-multithread` 执行通过，成功识别多线程栈对象
- **关键指标**: `ThreadObject amount = 30`，满足 `>= 20` 的验证门槛；总结果项数 `12`
- **备注**: GDB server 自动启动成功；运行日志中同样存在 `./bin/s3: 没有那个文件或目录` 提示，但未影响最终结果

## 失败的 Test Case（如有）

无。

## 兼容性验证
- [x] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

说明：
- Python 2.7：本轮完成了 `python2 --version` 环境确认与源码兼容性边界检查；未单独执行 Python 2.7 专项运行用例。
- Python 3.x：已实际执行 Python 3.11 环境下的结构测试与两个 `run_test.py` 回归用例。
- glibc / Node.js 多版本兼容性：当前任务需求与已提供用例未覆盖，故不在本轮通过范围内。

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

说明：本轮未设计或执行 >1GB 大内存专项性能验收，用例输出中可见 C++ 回归过程 RSS 峰值约 1.2G，但该结果不对应需求文档中的正式性能验收口径，因此不判定为性能通过项。

## Findings
- 未发现阻塞发布的问题。
- 结构测试、Python 回归测试、C++ 回归测试均通过，说明 `.maze.py` 拆分后的 builtins 注入方案在当前验收范围内工作正常。
- 两个回归用例运行期间均出现 `./bin/s3: 没有那个文件或目录` 提示，当前未影响结果，但建议后续关注符号下载路径在精简环境中的稳健性。

## Test Results
- `python3 -m unittest tests.test_issue_94_split_remaining_classes`：通过（9/9）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py cpp/20260304-stack-multithread`：通过
- `.maze.py` 行数统计：254
- Python 版本环境：2.7.18 / 3.11.2

## 总结
- 通过: 11 / 11
- 失败: 0 / 11
- 建议: 可以发布；当前验收标准均已满足。
