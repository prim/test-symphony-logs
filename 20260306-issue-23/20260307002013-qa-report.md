## QA Result: FAIL

## Findings
- 文档覆盖不完整：`dev-log/2026-03-07-resultitem-usage-analysis.md` 没有覆盖“所有上层使用”。源码中仍有多组直接上层调用未被纳入分析，例如 `mallocer/tcmalloc/tcmalloc.go`、`mallocer/mimalloc/mimalloc.go`、`mallocer/ptmalloc/ptmalloc.go`、`cpp/cpp.go`、`cpp/openblas.go`、`lua/lua.go`、`common/c.go` 等都存在 `map[uint64]*ResultItem` / `GetResultItem` / `MergeResultItems` / 回调透传模式，但文档正文与文件清单均未说明。
- 文档对“所有上层使用”的表述与源码不一致：文档将上层主要归纳为 Python / Node.js / TxOS / 其他模块（mallocer），但实际 grep 结果显示 ResultItem 的上层接入面更广，至少还包括 C/C++、Lua、多个 mallocer 子实现（不仅 jemalloc/pymalloc），因此当前交付不满足 `plan.md` 中“识别所有上层使用方”的要求。
- 交付边界存在偏差：当前工作区仍包含 `dev-log/plan.md` 修改，且新分析文档与 `dev-log/dev-report.md` 处于未跟踪状态；虽然本 issue 是文档任务，但交付状态仍不整洁，和开发报告中“提交前会按任务要求排除”的描述不一致。

## Test Results
- TC-001 ResultItem 定义定位完整: PASS
  - 文档正确定位 `common/result_items.go`，并解释了 `Amount`、`Sizeof`、`Objects`、`ObjSize`。
- TC-002 核心辅助函数覆盖完整: PASS
  - 文档覆盖 `NewResultItem`、`GetResultItem`、`AddToResultItems`、`AddToResultItems1`、`MergeResultItems`、`AddResult`，与 `common/result_items.go` 基本一致。
- TC-003 上层调用链分析完整: FAIL
  - 文档覆盖了 Python / Node.js / TxOS / jemalloc / pymalloc 的主要模式，但遗漏多组实际调用方，未达到“所有上层使用”的要求。
- TC-004 分支行为说明准确: PASS
  - 文档对 `EnablePyMerge`、`Http`、`MergeResultItems` 互斥合并的描述可追溯到 `common/result_items.go`。
- TC-005 任务边界说明清晰: PASS
  - 文档明确这是分析/文档任务，没有宣称做了功能修改或新增测试能力。

## Evidence
- `common/result_items.go`: 定义 `ResultItem`、`GetResultItem`、`AddToResultItems`、`AddToResultItems1`、`MergeResultItems`、`AddResult`。
- `grep "make\(map\[uint64\]\*ResultItem\)"`: 命中 `cpp/cpp.go`、`cpp/openblas.go`、`lua/lua.go`、`common/c.go`、`mallocer/mimalloc/mimalloc.go`、`mallocer/ptmalloc/ptmalloc.go`、`mallocer/tcmalloc/tcmalloc.go` 等文件。
- `grep "\b(AddToResultItems1|AddToResultItems|GetResultItem|MergeResultItems|NewResultItem)\b"`: 进一步确认上述遗漏模块存在直接上层使用。

## Conclusion
- 本次交付的文档质量整体较好，但“所有上层使用”这一核心要求尚未满足，因此验收结论为 FAIL。
