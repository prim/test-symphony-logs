# 开发报告

## 完成的任务
- [x] Task 1: 修复 GDB 调试符号搜索路径，支持同时使用 `./debug` 与 `/usr/lib/debug`
- [x] Task 2: 修复 libc ctypes 缓存误复用问题，检测到不完整 ptmalloc 结构缓存时强制重新提取
- [x] Task 3: 完成 QA 指定的 Python/C++ 验收测试、自测与回归验证

## 修改的文件
- `.maze.py`: 增加系统 debug symbol 路径候选、补充 `main_arena/mp_` 回退逻辑、为 libc ctypes 缓存增加版本与有效性校验、在 GDB 初始化时配置统一的 `debug-file-directory`

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `pytest tests/test_issue_79_move_postman_scripts.py`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，成功生成 `maze-result.json`
- 测试命令: `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- 测试结果: 通过，成功生成 `maze-result.json`

## Self-Review 发现与修复
- 发现 libc ctypes 缓存可能带有 `version` 字段但缺少 `struct _heap_info` 等 ptmalloc 必需结构，导致 Go 端在 `initPtmalloc` 期间 panic；已增加缓存有效性校验，不满足条件时强制刷新
- 发现仅在 Python 侧查找系统 debug symbol 仍不足以支持 gdb server 提取类型信息；已同步修复两条 GDB 初始化路径的 `debug-file-directory`

## 注意事项
- 本次仅修改源码 `.maze.py`，未修改任何 `testdata/` 下测试文件
- 运行测试时会刷新本地 `maze-db/ctypes-*` 缓存文件，但这些生成物不应提交
- libc 相关硬编码依赖调试符号可用性，后续若适配更多发行版，需继续验证 `/usr/lib/debug/.build-id/...` 布局兼容性
