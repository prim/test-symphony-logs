## QA Result: PASS

## Findings
- AC1 and AC5 satisfied: repository root no longer contains `.postman_lz4.py` or `.postman_prelude.py`, while the migrated runtime-visible scripts exist at `cmd/postman_lz4.py` and `cmd/postman_prelude.py`.
- AC2 satisfied: `maze` updates all relevant postman helper references to `cmd/` paths for update/download, release packaging, and tar analysis flows.
- AC3 satisfied: `cmd/postman.py` now invokes `python cmd/postman_prelude.py` and no longer uses the old root-level path.
- AC4 satisfied: `postman/postman-main.go` now invokes `exec.Command("python", "cmd/.postman_prelude.py", ...)`.
- Hidden wrapper scripts required by the existing packaging/runtime flow are present under `cmd/.postman_prelude.py` and `cmd/.postman_lz4.py`, matching the updated call sites.
- Previously reported acceptance instability is not reproducible in this retry: the requested Python and C++ `testdata` runs both passed, and libc symbol lookup succeeded in the inspected runtime log.

## Test Results
- PASS: static review of `maze`, `cmd/postman.py`, `postman/postman-main.go`, `cmd/.postman_prelude.py`, `cmd/.postman_lz4.py`, `cmd/postman_prelude.py`, and `cmd/postman_lz4.py`
- PASS: `python3 -m pytest tests/test_issue_79_move_postman_scripts.py tests/test_issue_78_gdb_directory_reorganization.py`
- PASS: `./maze --build`
- PASS: `python3 testdata/run_test.py python/20260129-complex-types-311`
- PASS: `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- INFO: acceptance runs still emit missing local `./bin/s3` helper warnings during symbol download attempts, but those warnings did not block analysis or validation in this environment

## Notes
- This branch includes merged changes beyond issue #79 (notably issue #78 related reorganization). The combined branch state remained healthy in the regression/build/acceptance coverage executed above.
- Workspace-local scratch files under `tmp/` can contain copied `.postman*.py` files; those were excluded from acceptance checks so only actual implementation locations were evaluated.
