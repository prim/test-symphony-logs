## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings
- 已复验此前失败的 submodule 元数据残留问题；当前 worktree 的 git common config 中仅保留 `submodule.testdata.*` 与 `submodule.gdb/prim-gdb.*`，未再发现旧的 `submodule.prim-gdb.*`。
- 目录迁移、路径重写、release 打包路径、历史测试断言更新均与计划文档一致。
- `./maze --build` 成功，且实际执行了至少 1 个 `testdata` 用例，满足本次 QA 的执行要求。

## 详细结果

### TC-001: 三个目标目录迁移到新位置
- **结果**: PASS
- **实际行为**: 仓库中存在 `3rd/misc/cpp-db/`、`gdb/maze-db/`、`gdb/prim-gdb/` 三个目标目录。
- **关键指标**: 目标目录 3/3 存在
- **备注**: 满足 AC1。

### TC-002: 根目录旧目录被彻底移除
- **结果**: PASS
- **实际行为**: 根目录下未发现 `cpp-db/`、`maze-db/`、`prim-gdb/` 三个旧目录。
- **关键指标**: 旧目录 0/3 残留
- **备注**: 满足 AC2。

### TC-003: 核心模块与 GDB 脚本全部改用新路径
- **结果**: PASS
- **实际行为**: 已检查 `maze-py/maze_db.py`、`maze-py/gdb_wrapper.py`、`maze-py/cpp_analyzer.py`、`maze-py/messiahserver.py`、`gdb/gdb-scripts/az_cpp.py`、`gdb/gdb-scripts/az_netease_messiah.py`、`3rd/misc/query-cpp-db.py`，核心运行路径均已切换到新目录。
- **关键指标**: `python3 -m pytest tests/test_issue_107_directory_migration.py tests/test_issue_85_maze_py_split_phase1.py` 结果为 `15 passed in 0.04s`
- **备注**: 满足 AC3。

### TC-004: .gitmodules 正确迁移 prim-gdb submodule
- **结果**: PASS
- **实际行为**: `.gitmodules` 中已使用 `[submodule "gdb/prim-gdb"]` 与 `path = gdb/prim-gdb`；执行 `./maze --build` 与后续 testdata 用例后，再检查 `$(git rev-parse --git-common-dir)/config`，仅存在 `submodule.gdb/prim-gdb.*`，未见旧 `submodule.prim-gdb.*` 残留。
- **关键指标**: git common config 中旧 submodule 配置 0 条残留
- **备注**: 满足 AC4。

### TC-005: release 脚本按新目录结构打包缓存目录
- **结果**: PASS
- **实际行为**: `maze` 脚本中的 release 目录创建与复制逻辑已使用 `release/gdb/maze-db`、`release/3rd/misc/cpp-db` 等新路径，旧顶层缓存目录打包逻辑已移除。
- **关键指标**: release 相关路径断言全部通过
- **备注**: 满足 AC3、AC5 的相关要求。

### TC-006: 既有测试同步更新新路径断言
- **结果**: PASS
- **实际行为**: `tests/test_issue_85_maze_py_split_phase1.py` 已同步使用新 DB/CDB 路径断言，并与 issue 107 测试一起通过。
- **关键指标**: 回归测试通过，`15 passed`
- **备注**: 满足 AC6。

### TC-007: GDB portable 路径迁移后仍可被运行时发现
- **结果**: PASS
- **实际行为**: `maze-py/gdb_wrapper.py` 已默认使用 `./gdb/prim-gdb/gdb-portable`，并在缺失时执行针对 `gdb/prim-gdb` 的 submodule sync/init/update 流程。实际执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 时，日志输出包含 `* GDB: Server started successfully`，分析流程可继续完成并通过验证。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 输出 `All validations passed!`，汇总为 `Total: 1 passed, 0 failed`
- **备注**: 运行中仍出现 `./bin/s3: 没有那个文件或目录` 提示，但未导致本次验收用例失败。

## Test Results
- `python3 -m pytest tests/test_issue_107_directory_migration.py tests/test_issue_85_maze_py_split_phase1.py`：通过，`15 passed in 0.04s`
- `./maze --build`：通过
- `git submodule status`：通过，显示 `gdb/prim-gdb` 与 `testdata` 均处于正常状态
- `git config --file "$(git rev-parse --git-common-dir)/config" --get-regexp '^submodule\.'`：通过，未发现 `submodule.prim-gdb.*`
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，`Total: 1 passed, 0 failed`

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: 当前可按 PASS 验收。本轮复验中，之前阻塞 AC4 的 submodule 元数据残留问题已消除，目录迁移、构建与基础回归验证均已通过。
