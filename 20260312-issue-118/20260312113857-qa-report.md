## QA Result: FAIL

## Findings
- AC1 已满足：`prepare/corefile.go` 新增了 `CorefileManager.Gcore`，`./maze --build` 在本地验证中成功。
- AC2 仅部分满足：`python3 maze --help` 已不再触发 `SyntaxError`，说明旧入口兼容层已恢复；但默认 testdata 主链路仍未恢复可用。
- 关键阻断仍存在：执行默认用例 `python/20260129-complex-types-311` 时，Maze 已能进入分析流程，但随后在 `mallocer/ptmalloc/init.go:52` 因 `panic: initPtmalloc sizeof _heap_info` 失败，导致 `testdata/run_test.py` 返回退出码 2。
- AC3 未完全满足：新增了 `tests/test_issue_118_prepare_merge_regression.py` 覆盖构建与入口兼容回归，但缺少对“默认 testdata 用例可继续启动并完成主链路”的自动化覆盖，因此无法防止当前这类动态回归漏检。

## Test Results
- PASS: `./maze --build`
- PASS: `python3 -m unittest tests.test_issue_118_prepare_merge_regression`
- PASS: `python3 maze --help`
- FAIL: `python3 testdata/run_test.py python/20260129-complex-types-311`
  - 实际行为：成功解包 tarball、成功进入 `prepare.Run()` 并连接 GDB，但主流程在 ptmalloc 初始化阶段崩溃。
  - 关键报错：`panic: initPtmalloc sizeof _heap_info`
  - 位置：`mallocer/ptmalloc/init.go:52`
  - 影响：默认回归测试仍不可用，AC2 不成立。

## Requirement Mapping
- AC1（构建链路恢复可用）：PASS
- AC2（默认测试入口恢复可用）：FAIL
- AC3（问题需可通过自动化回归测试覆盖）：FAIL

## Reproduction
1. `git submodule update --init testdata`
2. `python3 testdata/run_test.py python/20260129-complex-types-311`
3. 观察 Maze 在 `prepare.Run()` 完成后进入主流程，并在 ptmalloc 初始化时 panic。

## Notes
- 本次按要求至少实际执行了一个 testdata 用例。
- `testdata` 初始为空子模块，已先执行初始化后再验收。
- 由于关键 smoke test 已证明默认主链路不成立，未继续执行更高成本的扩展动态回归；这可能影响对其他 testdata 场景的 AC2/AC3 进一步覆盖判断。
