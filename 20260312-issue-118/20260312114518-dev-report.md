## Summary
修复了默认 testdata 主链路仍然失败的问题，根因是 GDB socket 模式下 `core-file` / `file` 命令路径被额外双引号包裹，导致 core 未正确加载，进而触发 ptmalloc ctypes 缺失。并补充了针对默认 Python smoke 用例的自动化回归测试。

## Changes
- `prepare/gdb.go`: 调整 GDB 路径转义逻辑，避免在 socket server 命令模式下把双引号写入实际路径，恢复 core-file 正常加载
- `tests/test_issue_118_prepare_merge_regression.py`: 新增默认用例 `python/20260129-complex-types-311` 的动态 smoke test，覆盖默认 testdata 主链路回归
- `dev-log/2026-03-12-issue-118-prepare-regression-fix.md`: 更新修复记录，补充根因分析、测试结果与 AC 核对

## Testing
- `./maze --build`
- `python3 -m unittest tests.test_issue_118_prepare_merge_regression`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
