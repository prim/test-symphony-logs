## Summary
实现了 issue #118 的两个基础修复：补齐 `prepare` 缺失的 `CorefileManager.Gcore` 实现，并将 `maze` 入口改为兼容 `python3 maze ...` 调用的 Python 启动器。

## Changes
- `maze`: 将 shell 入口改为 Python 启动器，保留自动构建和 `--build` 逻辑，并增加执行 `.maze` 时的 `ETXTBSY` 重试
- `prepare/corefile.go`: 新增 `CorefileManager.Gcore`，支持复用已有 core 文件或回退到 `gcore` 生成
- `dev-log/2026-03-12-issue-118-prepare-regression-fix.md`: 记录问题、修复方案、测试结果与 AC 核对

## Testing
- `./maze --build`
- `python3 -m unittest tests.test_issue_118_prepare_merge_regression`
