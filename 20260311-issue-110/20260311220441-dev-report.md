# 开发报告

## 结论

PARTIAL

本轮继续推进 issue #110 的剩余验证工作，补做了 `postmanprofile` 实际运行验证，并新增执行了一个 mimalloc 回归用例。当前 `prepare.Run()` 在 `standalone`、`--tar`、`postmanprofile` 三条主链路上都能完成分析，但 AC1 的逐字段一致性仍未完成，且 mimalloc 扩展回归未通过，因此本轮结论仍然只能是 `PARTIAL`。

## 完成的任务
- [x] 复核计划文档与当前未完成 AC，明确本轮继续推进范围。
- [x] 执行 `postmanprofile` 模式的实际运行验证，确认 prepare 主链路可正常完成。
- [x] 执行一个 mimalloc 回归用例，补充扩展兼容性验证。
- [x] 更新 `.symphony_prompt.md` 中已验证通过的 AC 勾选状态（AC2-AC10）。

## 修改的文件
- `.symphony_prompt.md`：将已经完成验证的 AC2-AC10 从未勾选更新为已勾选，保留 AC1 未勾选状态。
- `dev-flow/20260311220441-dev-report.md`：新增本轮开发报告。

## 自测结果
- 测试命令: `./maze --mode postmanprofile --project coredump --user default --profileId 2636550 --pid 2636550 --text --json-output --rmlog --limit 50`
- 测试结果: 通过。`prepare.Run()` 成功，成功复用 `postman-db/coredump-default-2636550/core.2636550`，输出文本结果与 `maze-result.json`，文本结果中 `PyMemPool objects = 68626`，说明 `postmanprofile` 主链路可运行。

- 测试命令: `python3 testdata/run_test.py cpp/20260211-mimalloc-2-0-9-multithread`
- 测试结果: 失败。分析流程本身成功完成，但校验脚本失败：多个大小档位的对象数量约为期望值的 2 倍，例如 `16/32/64` 字节分别为 `40196/40092/40040`，而预期为 `20000/20000/20000`；`1MB/2MB/3MB` 大块也均统计为 `200`，预期为 `100`。当前现象更像是对象被重复统计，而不是 prepare 主流程崩溃。

## 验收核对
- AC1: PARTIAL `common.S` / `common.Cpp` 仍未与历史 `.storage.json` / `.cpp.json` 做逐字段一致性比对；目前只有端到端用例间接证明关键字段可用。
- AC2: PASS `./maze --build` 已在前序回归中通过，且当前仓库保持可构建状态。
- AC3: PASS `python3 testdata/run_test.py python/20260129-complex-types-311` 已通过。
- AC4: PASS `python3 testdata/run_test.py cpp/20260304-stack-multithread` 已通过。
- AC5: PASS 当前主链路中 GDB Socket Client 能连接并驱动现有 GDB Socket Server，`postmanprofile` 实测也印证了该链路可用。
- AC6: PASS 当前覆盖到的 `readelf`、`nm`、Build ID 等外部命令链路工作正常，未再见此前真实路径问题导致的阻塞。
- AC7: PASS `standalone`/`--tar` 主链路此前已回归通过，本轮新增 `postmanprofile` 实测通过。
- AC8: PASS `--tar` 一键分析功能已由 Python/C++ 回归用例验证通过。
- AC9: PASS `.maze.py` 与 `maze-py/*.py` 已删除，分析流程仍正常。
- AC10: PASS `gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留且工作正常。

## 已完成项
- 已完成 `postmanprofile` 实际运行验证，补齐 AC7 缺口。
- 已确认 issue #110 的 prepare 主链路在 Python/C++/postmanprofile 场景下均能跑通。
- 已确认 `.symphony_prompt.md` 中 AC2-AC10 的当前状态应更新为已完成。

## 未完成项
- AC1 的历史 JSON 逐字段一致性比对仍未完成。
- 失败的 mimalloc 扩展回归尚未定位与修复。
- 回归清单中的 tcmalloc 用例仍未找到可执行测试数据。

## 未验证项
- tcmalloc 用例：仓库内未找到对应 testdata，尚无法执行。
- Lua 用例：仓库内未找到 `testdata/lua`，尚无法执行。
- AC1 字段级 golden diff：尚未建立可复用对照数据或旧链路输出。

## Self-Review 发现与修复
- 发现 `postmanprofile` 实际运行此前虽然在 QA 报告中已有记录，但本轮仍需亲自复测后才能将 AC7 由 `PARTIAL` 提升为 `PASS`；已完成该复测。
- 发现 mimalloc 用例不是 prepare 崩溃，而是结果数量系统性翻倍，说明问题更可能位于 mimalloc walker / owner 标注 / 分类汇总阶段，而非 GDB 初始化阶段；本轮先记录现象，未在缺少进一步定位前贸然修改源码。

## 注意事项
- 本轮没有修改任何 `testdata/` 下测试文件。
- mimalloc 用例当前失败表现为“统计翻倍”，后续应优先排查 `mallocer/mimalloc` 与 mmap owner 归属链路是否存在重复遍历或重复记账。
- 由于仍存在未完成项与未验证项，且 mimalloc 扩展回归未通过，本轮不能宣称全部完成。
