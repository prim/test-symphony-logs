# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮完成了阶段 A 静态检查、关键 smoke test 与两个主链路回归用例执行。`go test ./qa/issue110 -v`、`./maze --build`、`python/20260129-complex-types-311`、`cpp/20260304-stack-multithread` 均通过，但需求文档中的 AC1 仍未被充分验证：当前缺少 `common.S` / `common.Cpp` 与历史 `.storage.json` / `.cpp.json` 的逐字段一致性比对证据，因此不能判定整体验收通过。

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `go test ./qa/issue110 -run TestPreparePackageScaffoldExists` 所在测试集通过；`prepare/prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等关键文件存在。
- **关键指标**: 仓库级迁移守护测试通过
- **备注**: 满足脚手架存在性要求。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，且 `maze()` 中会先调用 `prepare.Run()` 再进入原分析链路。
- **关键指标**: `grep` 命中 `prepare.Run()`；守护测试通过
- **备注**: 入口集成已落地。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: `common/common.go` 不再调用旧 JSON bootstrap；`common/var.go` 已移除历史读取逻辑，改为 `initPreparedRuntime()`。
- **关键指标**: `TestCommonInitNoLongerDependsOnLegacyJSONBootstrap` 通过
- **备注**: 旧中间 JSON 初始化依赖已解除。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py` 与 `maze-py/` 已删除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 保留。
- **关键指标**: 迁移守护测试通过
- **备注**: 仅保留 GDB 内部 Python 脚本，符合约束。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 当前为 shell wrapper，负责构建并执行 `./.maze`，不再包含旧 Python 逻辑。
- **关键指标**: 文件首行 `#!/bin/bash`；守护测试通过
- **备注**: 满足“非 Python wrapper”要求。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍保留 `import gdb`。
- **关键指标**: `TestGdbScriptsStillUsePythonAPI` 通过
- **备注**: GDB 内部脚本兼容性保持。

### TC-007: Python 主流程回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；`prepare.Run()`、GDB Socket Server 连接、`--tar` 分析、validate 校验均成功。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`
- **备注**: 说明 Python 主链路在迁移后可正常工作。

### TC-008: C++ 多线程栈变量回归
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py cpp/20260304-stack-multithread` 通过；分析主流程完成，无崩溃。
- **关键指标**: validate 输出 `TC-B004 PASSED`；总 items=9
- **备注**: `ThreadObject` 未在结果中出现，但该用例的 validate 规则允许“分析完成成功”判通过，因此本 TC 按既定用例结果计为 PASS。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: PASS
- **实际行为**: 两个 `run_test.py` 用例均经由 `maze --tar ... --text --json-output` 路径执行成功，并生成 `maze-result.json` / `maze-result.txt`。
- **关键指标**: Python/C++ 两个 tar 回归均通过
- **备注**: `--tar` 单入口能力有效。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **实际行为**: 文件清理、入口迁移、GDB 脚本保留、standalone 主链路均已验证；但需求 AC1 要求 `prepare.Run()` 后 `common.S` / `common.Cpp` 与历史 `.storage.json` / `.cpp.json` 数据一致，当前仓库与本轮验证均未提供逐字段比对结果。开发报告也明确标记 AC1 为 `PARTIAL`。
- **关键指标**: 开发报告 `20260311222115-dev-report.md` 明确写明“AC1 未做逐字段一致性比对”；`prepare/prepare.go` 中 `Collect/ParseDwarf` 若干方法仍为空实现 `return nil`
- **备注**: 该缺口直接影响 AC1 的验收闭环，因此总体结果必须判 FAIL。

## 失败的 Test Case

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **预期行为**: 除了完成模式迁移与遗留依赖清理外，还应能证明 `prepare.Run()` 直接填充的 `common.S` / `common.Cpp` 与历史 `.storage.json` / `.cpp.json` 在字段层面一致，从而满足 AC1。
- **实际行为**: 目前只有仓库结构检查与端到端回归通过，未见任何 golden diff、字段比对脚本或对照产物；最新开发报告明确承认 AC1 仅为 `PARTIAL`。
- **复现步骤**:
  1. 阅读 `dev-flow/plan.md` 中 AC1 要求。
  2. 阅读 `dev-flow/20260311222115-dev-report.md`，可见“尚未与历史 `.storage.json` / `.cpp.json` 做逐字段一致性比对”。
  3. 检查本轮测试与仓库文件，未发现针对 AC1 的对照脚本或结果报告。
- **可能原因**: 迁移工作重点放在主链路可运行与回归通过，但未补齐与旧产物的字段级对照验证；同时 `prepare/prepare.go` 中部分 `Collect/ParseDwarf` 相关方法仍为空实现，增加了数据完整性未完全覆盖的风险。
- **建议修复**: 增加可重复执行的 AC1 对照验证：针对至少一个 Python 用例和一个 C++ 用例，同时生成历史基线与 prepare 结果，对 `common.S` / `common.Cpp` 关键字段及全量字段做差异报告，并将允许差异显式文档化。

## 未执行项

- 未执行 AC1 的历史 JSON 字段级 diff 验证
  - **原因**: 当前仓库与开发交付中未提供相应验证脚本/基线产物，本轮仅能确认主链路回归，不足以替代字段一致性验收。
  - **影响范围**: AC1；并间接影响 TC-010 与总体 PASS/FAIL 结论。
- 未执行 postmanprofile 动态复测
  - **原因**: 本轮优先执行低成本静态检查与最关键 smoke test；且已有最新开发报告声称通过，但 QA 本轮未重复实际运行该高成本场景。
  - **影响范围**: AC7 的 postmanprofile 部分仍主要依赖开发自测与代码/日志声明，独立 QA 证据不足。
- 未执行兼容性/性能扩展项（Python 2.7、不同 glibc、不同 Node.js、大内存性能）
  - **原因**: 需求中的核心阻塞点已在 AC1 处暴露，继续高成本扩展验证收益有限。
  - **影响范围**: 兼容性与性能结论不能宣称完成。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- 已执行检查：仓库级迁移守护测试、`./maze --build`、`python/20260129-complex-types-311`、`cpp/20260304-stack-multithread`
- 结果汇总：9 个 TC 通过，1 个 TC 失败
- 阻塞结论：AC1 缺少字段级一致性验证证据，当前版本不能签署通过

## Findings

- 发现 1：核心阻塞不是主链路崩溃，而是 **AC1 缺证**。虽然 Python/C++ 关键回归已通过，但尚不能证明 prepare 直接填充出的 `common.S` / `common.Cpp` 与历史 JSON 产物完全一致。
- 发现 2：仓库仍保留大量 Python 脚本文件（如 `cmd/*.py`、`tests/*.py`、`gdb/gdb-server/gdb_client.py` 等）。这些文件不一定都属于 `.maze.py` 预处理迁移范围，但 human review 提醒“所有 python 脚本文件都需要被转换为 golang 文件”，建议开发方进一步澄清并收敛范围，否则后续仍可能因范围认定不一致被驳回。
- 发现 3：`prepare/prepare.go` 中 `CollectMallocLib.InitIndexFile/UpdateIndexFile` 与 `ParseDwarfManager.DownloadUseParseDwarf/UploadUseParseDwarf` 仍为空实现，当前未形成失败，但属于完整性风险点。

## 总结
- 通过: 9 / 10
- 失败: 1 / 10
- 建议: **暂不可以发布**。请先补齐 AC1 的字段级一致性验证，并明确 human review 对“所有 Python 脚本文件”的最终迁移范围，再发起下一轮 QA。
