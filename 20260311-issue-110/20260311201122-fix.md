# Session Log (OpenCode)

| Field | Value |
|-------|-------|
| Session ID | `ses_32335a758ffeErKGcJKh0NgUNy` |
| Time | 12:06:22 - 12:11:22 (4m 59s) |
| Backend | OpenCode |
| Workspace | `/home/hcjn0770/temp/worktrees/issue-110` |
| Status | completed |
| Steps | 27 |
| Tool calls | 38 |
| Tokens | input: 1,514,915 (cached: 1,357,568), output: 10,168 (reasoning: 0), total: 1,525,083 |

## System Prompt

<details><summary>Prompt injected by maze-symphony</summary>

## Fix Task

QA found issues with the implementation for:

**prim/maze#110**: 将 .maze.py 全部重写为 Go prepare 包

# Feature: 将 .maze.py 全部重写为 Go prepare 包

## 背景与目标

### 背景

当前 Maze 项目采用**两阶段管道架构**：

```
阶段 1: Python (.maze.py)  →  .storage.json / .cpp.json  →  阶段 2: Go (.maze 二进制)
```

Python 层 (`.maze.py` + `maze-py/*.py` + `maze_*.py`) 共约 **4900 行 Python 代码**，负责所有预处理工作：
- 生成 core dump、解析 `/proc/pid/maps`
- 与 GDB 交互获取结构体偏移/sizeof/符号/vtable
- 检测语言运行时（Python/Lua/Node.js/C）和内存分配器（ptmalloc/tcmalloc/jemalloc/mimalloc）
- C++ DWARF 类型分析
- 汇总数据并输出 JSON 文件供 Go 分析引擎消费

这套架构存在以下问题：
1. **双语言维护成本** — 需要同时维护 Python 和 Go 两个技术栈
2. **进程间通信开销** — 通过 JSON 文件序列化/反序列化传递数据
3. **Python 2.7 兼容包袱** — 大量 Python 2/3 兼容代码
4. **部署复杂度** — 需要确保目标环境有 Python 和各种依赖
5. **builtins 注入反模式** — Python 层通过 `builtins` 注入实现全局单例共享，依赖关系完全隐式化

### 目标

将 `.maze.py` 及其所有相关 Python 模块**完全重写为 Go `prepare` 包**，实现：
1. **单一语言** — 整个 Maze 分析流程由 Go 统一实现（GDB 内部脚本除外）
2. **内存直传** — prepare 阶段直接填充 `common.S` 和 `common.Cpp` 全局变量，不再生成中间 JSON 文件
3. **入口统一** — Python `maze` 入口脚本的功能并入 `main.go`，最终只有一个 Go 二进制
4. **消除 Python 依赖** — 运行时不再依赖 Python 环境（`gdb-scripts/*.py` 保留，它们在 GDB 内部运行）

## 技术方案

### 整体架构

迁移后的架构：

```
main.go
 ├── parseArgs()                          ← 合并 Python maze 脚本的参数解析
 ├── prepare.Run()                        ← 原 .maze.py 全部逻辑
 │    ├── corefile.Gcore(pid)
 │    ├── process.Init(pid)
 │    ├── gdb.GenerateDebugScript()
 │    ├── symbols.DownloadSymbolFiles()
 │    ├── gdb.Do()                        ← 通过 GDB Socket Client 执行 GDB 命令
 │    ├── process.Vms(pid)
 │    ├── cpp.Do()
 │    ├── process.DoMaps()
 │    ├── gdb.Ctypes(language)
 │    ├── ...（语言/分配器特定逻辑）
 │    └── fillCommonVars()                ← 直接填充 common.S 和 common.Cpp
 └── maze1()                              ← 原有 Go 分析引擎（不变）
      ├── InitAllMemoryPieceSize()
      ├── python.Main() / lua.Main() / ...
      └── PrintMemorySumary()
```

### 关键设计决策

#### 1. GDB 交互：Go 实现 GDB Socket Client

复用现有的 GDB Socket Server（`gdb/gdb-server/gdb_socket_server.py`，在 GDB 内部运行，必须保持 Python），Go 侧实现 TCP 客户端。

**通信协议**（已有，保持不变）：
- TCP 连接到 `127.0.0.1:9999`
- 帧格式：`<4字节大端序长度><UTF-8 JSON 数据>`
- 请求：`{"type": "command|eval|ping", "data": "..."}`
- 响应：`{"success": bool, "output|value": "...", "error": "..."}`

```go
// prepare/gdb_client.go
type GDBClient struct {
    conn    net.Conn
    host    string
    port    int
    timeout time.Duration
}

func (c *GDBClient) Execute(command string) (*GDBResponse, error)
func (c *GDBClient) ExecuteBatch(commands []string) ([]*GDBResponse, error)
func (c *GDBClient) Evaluate(expression string) (*GDBEvalResponse, error)
func (c *GDBClient) Ping() bool
```

#### 2. 外部命令调用：保持 exec.Command

所有外部命令（`readelf`、`nm`、`strings`、`gcore`、`md5sum`、`ldd`、`./bin/s3`、`./parse_dwarf`）暂不替换为 Go 原生实现，统一通过 `exec.Command` 调用。

```go
// prepare/cmd.go — 统一的外部命令执行封装
func RunCommand(name string, args ...string) (string, error)
func RunCommandSilent(name string, args ...string) error
func CheckOutput(name string, args ...string) (string, error)
```

#### 3. 数据传递：直接填充 common.S 和 common.Cpp

不再生成 `.storage.json` 和 `.cpp.json`，prepare 包直接操作 `common` 包的全局变量：

```go
// prepare 包直接 import common，填充数据
import . "maze/common"

func fillCommonVars(ctx *Context) {
    S.Process.Pid = ctx.Process.Pid
    S.Process.Type = ctx.Language
    S.Process.Mallocer = ctx.Process.Mallocer
    S.Process.Maps = ctx.Process.Maps
    S.Sizeof = ctx.GDB.Sizeof
    S.Structs = ctx.GDB.Structs
    S.Symbols = ctx.Symbols.Symbols
    // ...
    Cpp.Vtables = ctx.Cpp.Vtables
    Cpp.DeepLoopStructs = ctx.Cpp.DeepLoopStructs
    // ...
}
```

#### 4. 上下文管理：Context 结构体替代 builtins 注入

```go
// prepare/context.go
type Context struct {
    Args      *Args
    DirPath   string
    Mode      string
    Project   string
    User      string
    ProfileId string
    Language  string  // "python" | "lua" | "c" | "nodejs" | "txos"

    Process   *ProcessInfo
    Corefile  *CorefileInfo
    Symbols   *SymbolsManager
    GDB       *GDBManager
    Cpp       *CppAnalyzer
    Storage   *StorageManager

    // 内存分配器
    Libc      *LibcInfo
    Tcmalloc  *TcmallocInfo
    Jemalloc  *JemallocInfo
    Mimalloc  *MimallocInfo

    // 语言运行时
    Python    *PythonLang
    Lua       *LuaLang
    Nodejs    *NodejsLang

    // 项目特定
    Asiocore  *AsiocoreInfo
    Mosd      *MosdInfo
    Txos      *TxosInfo
    G78       *G78Info
    Messiah   *MessiahServerInfo

    // 数据库
    DB        *DBC
    CDB       *DBC
}
```

#### 5. 入口脚本合并策略

Python `maze` 脚本（592 行）的功能分类处理：

| 功能 | 处理方式 |
|------|----------|
| `--build` | 改为 Makefile 或 shell wrapper，不并入 Go |
| `--format` | 改为 Makefile 或 shell wrapper，不并入 Go |
| `--release` | 改为 Makefile 或 shell wrapper，不并入 Go |
| `--update` | 并入 `main.go` |
| `--loop` / `--kill-loop` | 并入 `main.go` |
| `--tar` | 并入 `main.go`（一键分析 tar.gz） |
| `--retry` | 并入 `main.go` |
| `--rmlog` / `--cleanup` | 并入 `main.go`（已部分存在） |
| standalone/postmanprofile 模式分发 | 并入 `main.go`（prepare.Run → maze1） |

### 包结构设计

```
prepare/
├── prepare.go          # 入口：Run() 函数，主流程编排（原 main_()）
├── context.go          # Context 结构体定义
├── args.go             # 命令行参数解析（合并 Python maze 脚本的参数）
├── cmd.go              # 外部命令执行封装（exec.Command）
├── utils.go            # 工具函数（hexi、soName、isElfFile、getBuildId、md5sum 等）
│
├── gdb_client.go       # GDB Socket Client（TCP 协议实现）
├── gdb.go              # GDB 命令编排（原 gdb_wrapper.py 的 do/ctypes/execute）
├── gdb_server.go       # GDB Server 进程管理（启动/停止/健康检查）
│
├── corefile.go          # Core dump 管理（原 corefile.py）
├── process.go           # 进程信息收集（原 process.py，610 行 → ~600 行 Go）
├── symbols.go           # 符号地址定位（原 symbols.py，293 行 → ~300 行 Go）
├── storage.go           # 数据填充到 common.S / common.Cpp（原 storage.py）
│
├── lang_python.go       # Python 运行时检测（原 python_lang.py，393 行）
├── lang_lua.go          # Lua 运行时检测（原 lua.py，80 行）
├── lang_nodejs.go       # Node.js 运行时检测（原 nodejs.py，80 行）
├── lang_c.go            # C 语言占位（原 c_lang.py，11 行）
│
├── alloc_libc.go        # ptmalloc 符号定位（原 libc.py，62 行）
├── alloc_tcmalloc.go    # tcmalloc 检测和符号定位（原 tcmalloc.py，245 行）
├── alloc_jemalloc.go    # jemalloc 检测和符号定位（原 jemalloc.py，188 行）
├── alloc_mimalloc.go    # mimalloc 检测和符号定位（原 mimalloc.py，56 行）
├── alloc_openblas.go    # OpenBLAS 符号定位（原 openblas.py，46 行）
│
├── cpp_analyzer.go      # C++ DWARF/vtable 分析（原 cpp_analyzer.py，449 行）
├── cpp_vtable.go        # C++ VTable 管理（原 cpp_vtable.py，73 行）
│
├── asiocore.go          # Asiocore 游戏框架（原 asiocore.py，181 行）
├── messiah.go           # Messiah Server（原 messiahserver.py，65 行）
├── mosd.go              # MOSD 游戏引擎（原 mosd.py，55 行）
├── txos.go              # TXOS/MudOS 引擎（原 txos.py，164 行）
├── g78.go               # G78 项目特定（原 g78.py，14 行）
├── taggeddict.go        # TaggedDict 扩展（原 taggeddict.py，14 行）
│
├── db.go                # 键值数据库（原 maze_db.py，81 行）
├── collect.go           # malloc 库收集/DWARF 管理（原 maze_collect.py，109 行）
│
└── fill.go              # 将 Context 数据填充到 common.S 和 common.Cpp
```

## 任务拆分

### Phase 0: 基础设施（优先级最高）

- [ ] Task 0.1: 创建 `prepare/` 包目录和基础文件
  - 涉及文件: `prepare/context.go`, `prepare/args.go`, `prepare/cmd.go`, `prepare/utils.go`
  - 关键实现:
    - 定义 `Context` 结构体及所有子结构体
    - 实现 `Args` 参数解析（合并 Python `maze` 脚本和 `.maze.py` 的参数定义）
    - 封装 `RunCommand`/`CheckOutput` 等外部命令执行函数
    - 迁移工具函数：`hexi`（十六进制转整数）、`soName`（提取 .so 名称）、`isElfFile`、`getBuildId`（调用 readelf -n）、`md5sum`
  - 技术栈: Go

- [x] Task 0.2: 实现 GDB Socket Client
  - 涉及文件: `prepare/gdb_client.go`
  - 关键实现:
    - TCP 连接管理（connect/disconnect/reconnect）
    - 长度前缀帧协议：4 字节大端序长度 + UTF-8 JSON 数据
    - 三种请求类型：`command`、`eval`、`ping`
    - 精确字节读取 `recvExact(n)`
    - 连接超时 5 秒、请求超时 120 秒
    - 错误处理：连接关闭检测、超时重试
  - 技术栈: Go (net, encoding/binary, encoding/json)
  - 验证: 用现有的 `start_server.gdb` 启动 GDB Server，Go 客户端连接并执行 `info files` 命令

- [x] Task 0.3: 实现 GDB Server 进程管理
  - 涉及文件: `prepare/gdb_server.go`
  - 关键实现:
    - 启动 GDB 子进程：`<gdb_bin> -q -n -x .gdbcommand.py -x gdb/gdb-server/gdb_socket_server.py -ex "start_gdb_server 9999"`
    - GDB 二进制路径解析优先级：环境变量 `MAZE_GDB_BIN` > `./prim-gdb/gdb-portable` > 自动 submodule 构建
    - 启动等待：每 0.2 秒轮询 TCP 端口，最多等待 5 秒
    - 进程生命周期管理：启动、健康检查、优雅停止（terminate）
    - Corefile 初始化幂等性：缓存 `(exe_path, core_path)` 避免重复加载
  - 技术栈: Go (os/exec, net, time)

- [ ] Task 0.4: 实现键值数据库
  - 涉及文件: `prepare/db.go`
  - 关键实现:
    - `DBC` 结构体：基于文件系统 + S3 的 JSON 键值存储
    - `Set(key, value)` — 保存到本地文件并上传 S3（`./bin/s3 upload`）
    - `Get(key, version)` — 本地查找或从 S3 下载（`./bin/s3 download`）
    - 两个全局实例：`DB`（maze-db/）、`CDB`（cpp-db/）
  - 技术栈: Go (os, encoding/json, os/exec)

### Phase 1: 核心模块

- [ ] Task 1.1: 实现 Corefile 管理
  - 涉及文件: `prepare/corefile.go`
  - 关键实现:
    - standalone 模式：调用 `gcore` 生成 core dump
    - 保存 `/proc/{pid}/maps`、`/proc/{pid}/smaps`、`/proc/{pid}/exe` 信息
    - 记录 core 文件路径和大小
  - 原始代码: `maze-py/corefile.py`（38 行）
  - 技术栈: Go (os/exec, os)

- [x] Task 1.2: 实现进程信息收集
  - 涉及文件: `prepare/process.go`
  - 关键实现:
    - `Init(pid)` — 初始化进程信息入口
    - `cacheProcMaps(pid)` — 解析 `/proc/pid/maps` 文件，构建内存映射数据结构 `[]Mmap`
    - `setupLanguage()` — 语言自动检测：
      - 通过 `readelf -s` 检查符号（`PyLong_Type` → Python，`lua_isnumber` → Lua）
      - 通过 maps 中的 .so 名判断（`libpython` → Python，`lua` → Lua）
      - 通过可执行文件名判断（`txos` → Txos，`node` → Nodejs，其他 → C）
    - `gotpltOffset(filepath, offset)` / `loadOffset(filepath)` — 使用 `readelf -a` 计算 LOAD 段偏移
    - `doMaps()` — 遍历 maps 定位各类库（libc、tcmalloc、jemalloc、mimalloc 等），调用对应模块的 `Address()` 方法
    - `vms(pid)` — 计算虚拟内存统计（VMS、heap、stack、mmap）
    - `setupFileMap()` — postman 模式下的文件映射（md5 匹配、xy/ 目录符号链接创建）
    - `realpath(path)` / `originpath(path)` — postman-db/elf 路径映射
  - 原始代码: `maze-py/process.py`（610 行）
  - 技术栈: Go (os, os/exec, strings, strconv, regexp)

- [x] Task 1.3: 实现符号地址定位
  - 涉及文件: `prepare/symbols.go`
  - 关键实现:
    - `locateNames(filepath, sectionAddr, names, sectionOffset)` — 使用 `readelf -s` 批量定位符号地址
    - `locateByNm(name, filename, sectionAddr, sectionOffset, flags)` — 使用 `nm -C`/`nm -gDC` 定位单个符号
    - 符号地址计算：`address = sectionAddr + symbolOffset - sectionOffset`
    - `downloadSymbol(buildId, path)` — 从 S3 下载调试符号文件到 `debug/.build-id/`
    - `downloadSymbolFiles()` — 批量下载所有需要的 .so 的调试符号
    - `run(language)` — 通过 GDB `info address` 命令获取符号地址
    - `debugSymbolCandidates(filepath)` — 生成调试符号文件的候选路径列表
  - 原始代码: `maze-py/symbols.py`（293 行）
  - 技术栈: Go (os/exec, strings, strconv, path/filepath)

- [x] Task 1.4: 实现 GDB 命令编排
  - 涉及文件: `prepare/gdb.go`
  - 关键实现:
    - `Do()` — 执行核心 GDB 命令序列：
      - `info files` — 解析 `.rodata`/`.bss` 段地址
      - `info threads` — 获取线程数
      - `info address main_arena` — 获取符号地址
      - 收集 pthread 栈信息
    - `Ctypes(language)` — 核心方法（原始约 400 行），通过 GDB 获取所有结构体偏移量和 sizeof：
      - 对当前语言（Python/Lua/C/Nodejs/Txos）的类型列表执行 GDB `ptype` 或自定义 `offsets-of` 命令
      - 对当前分配器（ptmalloc/tcmalloc/jemalloc/mimalloc）的类型列表执行同上
      - 支持缓存：以 `ctypes-{build_id}` 为键存入 DB
      - 支持重试：缓存 miss 时通过 GDB 获取
    - `Execute(command)` — 封装单个 GDB 命令执行，使用 GDB Socket Client
    - `generateDebugScript()` — 生成 `gdb.sh` 调试脚本
    - 三级回退策略：连接已有 Server → 自动启动 Server → 错误退出（不再有进程模式回退）
  - 原始代码: `maze-py/gdb_wrapper.py`（993 行）
  - 技术栈: Go (strings, regexp, encoding/json)
  - 注意: 这是最大最复杂的模块，建议进一步拆分 ctypes 解析逻辑

### Phase 2: 语言运行时检测

- [ ] Task 2.1: 实现 Python 语言检测
  - 涉及文件: `prepare/lang_python.go`
  - 关键实现:
    - 定义 Python 类型列表（~70+ 类型：`PyDictObject`、`PyListObject` 等）和符号名列表（~40+ 符号）
    - `Address(realpath, sectionAddr, fileOffset)` — 定位 Python 符号地址
    - `FindVersion(path)` — 多策略版本检测：
      - Build ID 硬编码映射
      - 执行 `python --version`
      - `strings` 命令搜索版本字符串
      - 二进制文件直接搜索
    - `SetVersion(version)` — 设置版本号（含 iversion 整数版本号计算）
    - `Obmalloc()` — Python 3.12+ 的 obmalloc 信息提取（通过 GDB 执行 `py312` 命令）
    - `Unicode()` — 通过 GDB 获取 `PyASCIIObject.state` 位域布局
  - 原始代码: `maze-py/python_lang.py`（393 行）
  - 技术栈: Go (os/exec, strings, os)

- [ ] Task 2.2: 实现 Lua 语言检测
  - 涉及文件: `prepare/lang_lua.go`
  - 关键实现:
    - 定义 Lua 类型列表（~30 个：`global_State`、`lua_State`、`TValue` 等）
    - `Address(realpath, sectionAddr, fileOffset)` — 定位 Lua 符号
    - `FindVersion(path)` — 版本检测（当前仅支持硬编码项目）
  - 原始代码: `maze-py/lua.py`（80 行）
  - 技术栈: Go

- [ ] Task 2.3: 实现 Node.js 运行时检测
  - 涉及文件: `prepare/lang_nodejs.go`
  - 关键实现:
    - `Address(realpath, sectionAddr, fileOffset)` — 执行 GDB 命令 `node-init-all`（触发 gdb-scripts/ 中的 Python 脚本）
    - 从 JSON 文件加载 V8 信息：`node_version.json`、`v8_constants.json`、`v8_isolate.json`、`v8_heap_layout.json`
  - 原始代码: `maze-py/nodejs.py`（80 行）
  - 技术栈: Go (encoding/json, os)

- [ ] Task 2.4: 实现 C 语言占位
  - 涉及文件: `prepare/lang_c.go`
  - 关键实现: 空实现，仅日志
  - 原始代码: `maze-py/c_lang.py`（11 行）

### Phase 3: 内存分配器检测

- [ ] Task 3.1: 实现 ptmalloc (Libc) 支持
  - 涉及文件: `prepare/alloc_libc.go`
  - 关键实现:
    - 定义类型列表：`cmd_struct_malloc_state`、`struct _heap_info`、`struct tcache_perthread_struct` 等
    - `Address()` — 定位 `main_arena` 和 `mp_` 符号
    - `ShouldSkip()` — 非 ptmalloc 时跳过
    - `IsValidCtypes(data)` — 验证缓存有效性
  - 原始代码: `maze-py/libc.py`（62 行）

- [ ] Task 3.2: 实现 tcmalloc 支持
  - 涉及文件: `prepare/alloc_tcmalloc.go`
  - 关键实现:
    - 版本检测：从二进制搜索 `gperftools 2.x` 版本标识
    - 版本特定偏移量表：`pageheapLockOffset`（2.4.0 → 2.8.0）
    - `Address(realpath, sectionAddr, fileOffset)` — 定位 `pageheap_`、`thread_heaps_`、`central_cache_` 符号
    - `PrintClassToSize()` — 通过 GDB 获取 size class 映射表
    - `UploadS3(realpath)` — 上传 tcmalloc 库到 S3
    - `CompareVersion(a, b)` — 语义化版本比较
  - 原始代码: `maze-py/tcmalloc.py`（245 行）

- [ ] Task 3.3: 实现 jemalloc 支持
  - 涉及文件: `prepare/alloc_jemalloc.go`
  - 关键实现:
    - 版本检测：Build ID 硬编码 或 `strings` 提取
    - `Address(realpath, sectionAddr, fileOffset)` — 定位 12 个 jemalloc 符号（`je_extents_rtree`、`je_bin_infos` 等）
    - `FindLastMmap()` — 从 proc_maps 找到 jemalloc 管理的 mmap 区域
    - `CollectTsdTls()` — 通过 GDB 执行 `jemalloc` 命令收集 TSD/TLS 和 arena 列表
    - 支持 jemalloc 5.x 和 4.5.0 两种结构体类型列表
  - 原始代码: `maze-py/jemalloc.py`（188 行）

- [ ] Task 3.4: 实现 mimalloc 支持
  - 涉及文件: `prepare/alloc_mimalloc.go`
  - 关键实现:
    - 版本检测：原来通过 `ctypes.cdll.LoadLibrary` 调用 `mi_version()`，Go 中改为通过 `strings` 或 `nm` 检测
    - `Address(realpath, sectionAddr, fileOffset)` — 设置 mimalloc 为当前分配器
    - 类型列表：`mi_segment_t`、`mi_page_t`、`mi_heap_t`
  - 原始代码: `maze-py/mimalloc.py`（56 行）
  - 注意: `ctypes.cdll.LoadLibrary` 调用需要改为其他方式（如 `nm` 查找 `mi_version` 符号后通过 GDB 读取）

- [ ] Task 3.5: 实现 OpenBLAS 支持
  - 涉及文件: `prepare/alloc_openblas.go`
  - 关键实现:
    - `Address(realpath, sectionAddr, fileOffset)` — 定位 `memory` 符号，多个 .so 时用序号重命名
  - 原始代码: `maze-py/openblas.py`（46 行）

### Phase 4: C++ 分析和项目特定模块

- [ ] Task 4.1: 实现 C++ DWARF/vtable 分析
  - 涉及文件: `prepare/cpp_analyzer.go`
  - 关键实现:
    - `Do()` — 遍历 proc_maps 中所有 ELF 文件：
      - 获取 `.rodata` 段偏移（`readelf -S`）
      - 对每个文件调用 `handleFile` 分析
      - `merge` 合并多个 ELF 文件的类型名称、结构体、vtable、符号（核心约 160 行）
      - `collectStackLocals()` — 通过 GDB 收集所有线程栈帧的局部变量类型信息
    - `handleFile(filename, realpath)` — 查缓存或通过 GDB/parse_dwarf 获取类型信息
    - `merge(L)` — 合并多个 ELF 文件的分析结果，处理类型索引重映射
    - `updateSymbolZeroAddress(nowSymbols, zeroSymbols)` — 通过 GDB 解析零地址符号
  - 原始代码: `maze-py/cpp_analyzer.py`（449 行）
  - 技术栈: Go (encoding/json, os/exec, strings)

- [ ] Task 4.2: 实现 C++ VTable 管理
  - 涉及文件: `prepare/cpp_vtable.go`
  - 关键实现: 当前主逻辑已禁用（`handle` 和 `handleFile` 直接 return），保持禁用状态迁移
  - 原始代码: `maze-py/cpp_vtable.py`（73 行）

- [ ] Task 4.3: 实现 Asiocore 游戏框架
  - 涉及文件: `prepare/asiocore.go`
  - 关键实现:
    - `Asiocore.Address()` — 通过 nm 定位 vtable 符号
    - `AsiocoreNew.Address()` — 使用 `strings` 判断 messiah/mobileserver
    - `Asiocore.Mb2DetectPyType()` — MobileServer 2.0 专用 Python 类型地址获取
  - 原始代码: `maze-py/asiocore.py`（181 行）

- [ ] Task 4.4: 实现 Messiah Server
  - 涉及文件: `prepare/messiah.go`
  - 关键实现:
    - `Do(realpath, sectionAddr, fileOffset)` — 通过 GDB 或缓存获取结构体信息
  - 原始代码: `maze-py/messiahserver.py`（65 行）

- [ ] Task 4.5: 实现 MOSD 游戏引擎
  - 涉及文件: `prepare/mosd.go`
  - 关键实现:
    - `Address()` — 定位 4 个 MOSD 类型符号
    - `TestRobinMap()` — 通过 GDB 检测 PropGeneralMap 的 hash map 实现
  - 原始代码: `maze-py/mosd.py`（55 行）

- [ ] Task 4.6: 实现 TXOS/MudOS 引擎
  - 涉及文件: `prepare/txos.go`
  - 关键实现:
    - `Address()` — 定位 30+ 个游戏引擎符号
    - `FindCppNames()` — 通过 GDB 定位 C++ mangled 符号
    - `Misc()` — 通过 GDB 获取额外信息
  - 原始代码: `maze-py/txos.py`（164 行）

- [ ] Task 4.7: 实现 G78 和 TaggedDict
  - 涉及文件: `prepare/g78.go`, `prepare/taggeddict.go`
  - 关键实现:
    - G78: 定位 `PyDict3_Type` 符号
    - TaggedDict: 定位 `TaggedDictType` 符号
  - 原始代码: `maze-py/g78.py`（14 行）, `maze-py/taggeddict.py`（14 行）

- [ ] Task 4.8: 实现 malloc 库收集
  - 涉及文件: `prepare/collect.go`
  - 关键实现:
    - `CollectMallocLib` — malloc 库 S3 索引管理
    - `ParseDwarf` — DWARF 解析 build_id 列表管理
  - 原始代码: `maze_collect.py`（109 行）

### Phase 5: 数据填充与集成

- [ ] Task 5.1: 实现数据填充层
  - 涉及文件: `prepare/fill.go`, `prepare/storage.go`
  - 关键实现:
    - `fillCommonVars(ctx)` — 将 Context 中收集的所有数据填充到 `common.S` 和 `common.Cpp`：
      - `S.Process` — pid、type、mallocer、maps、exe、bss、min/max_ptr
      - `S.Corefile` — file、coreSize
      - `S.Sizeof` / `S.Structs` — 结构体偏移量和 sizeof
      - `S.Symbols` — 符号名→地址映射
      - `S.Py` — Python 版本、类型名、arena 信息、unicode 属性
      - `S.Tcmalloc` / `S.Jemalloc` / `S.Mimalloc` — 分配器参数
      - `S.Pthread` — pthread 栈信息
      - `S.Stat` — VMS 统计
      - `S.Nodejs` — V8 常量、heap layout、isolate 地址
      - `S.Asiocore` / `S.Mosd` / `S.Txos` — 项目特定数据
      - `Cpp.Vtables` / `Cpp.DeepLoopStructs` / `Cpp.AllTypeName` / `Cpp.GdbSymbols` / `Cpp.StackLocals`
    - 兼容 `common.S` 已有的 JSON tag 和字段命名
  - 技术栈: Go

- [ ] Task 5.2: 实现 prepare.Run() 主流程编排
  - 涉及文件: `prepare/prepare.go`
  - 关键实现:
    - `Run()` 函数，对应 Python `.maze.py` 的 `main_()`：
      ```go
      func Run() error {
          ctx := NewContext()
          ctx.Init()                             // 初始化参数和环境
          ctx.Corefile.Gcore(ctx.Args.Pid)       // 生成/定位 core 文件
          ctx.Process.Init(ctx.Args.Pid)         // 初始化进程信息
          ctx.GDB.GenerateDebugScript()          // 生成 GDB 调试脚本
          ctx.Symbols.DownloadSymbolFiles()      // 下载符号文件
          ctx.Collect.InitIndexFile()            // 初始化 malloc 库索引
          ctx.ParseDwarf.DownloadUseParseDwarf() // 下载 DWARF 解析配置
          ctx.GDB.Do()                           // 执行 GDB 核心命令
          ctx.Process.Vms(ctx.Args.Pid)          // VMS 统计
          ctx.Cpp.Do()                           // C++ DWARF 分析
          ctx.Process.DoMaps()                   // 处理内存映射
          ctx.GDB.Ctypes(ctx.Language)           // 获取类型信息
          ctx.Asiocore.Mb2DetectPyType()         // 特定语言后处理
          ctx.Python.Obmalloc()
          ctx.Python.Unicode()
          ctx.Collect.UpdateIndexFile()
          ctx.ParseDwarf.UploadUseParseDwarf()
          fillCommonVars(ctx)                    // 直接填充 common.S 和 common.Cpp
          return nil
      }
      ```
  - 技术栈: Go

- [ ] Task 5.3: 修改 main.go 集成 prepare 包
  - 涉及文件: `main.go`
  - 关键实现:
    - 合并 Python `maze` 脚本的参数解析到 `parseArgs()`
    - 新增 `--tar` 功能（tar.gz 一键分析）
    - 修改 `maze()` 函数：在 `maze0()` 之前调用 `prepare.Run()`
    - 移除 `initConfig()` 中从 JSON 文件读取 `S` 和 `Cpp` 的逻辑（prepare 已直接填充）
    - 新增 `--build`、`--format` 等开发命令的处理（通过 `exec.Command` 调用 `go build` 等）
  - 技术栈: Go (flag, os/exec)

### Phase 6: 清理和迁移

- [ ] Task 6.1: 删除废弃的 Python 文件
  - 涉及文件:
    - `.maze.py` — 删除
    - `maze-py/*.py` — 全部 22 个文件删除
    - `maze_globals.py`, `maze_utils.py`, `maze_db.py`, `maze_collect.py` — 删除
    - `maze` (Python 入口脚本) — 替换为编译后的 Go 二进制或简单的 shell wrapper
  - 关键实现: 确保所有测试通过后再删除

- [ ] Task 6.2: 更新 release 和部署流程
  - 涉及文件: release 脚本（原在 Python `maze` 中的 `do_release()`）
  - 关键实现:
    - 更新 release 打包逻辑：不再包含 `.maze.py` 和 `maze-py/`
    - 更新 `do_update()` 逻辑
    - 确保 `gdb-scripts/*.py` 仍被打包（GDB 内部脚本必须保留）

- [ ] Task 6.3: 更新文档和 AGENTS.md
  - 涉及文件: `AGENTS.md`, `doc/`, `README.md`
  - 关键实现:
    - 更新项目结构说明
    - 更新构建和运行命令
    - 更新开发流程文档

## 接口/数据结构定义

### GDB Socket Client 接口

```go
// prepare/gdb_client.go

type GDBResponse struct {
    Success bool   `json:"success"`
    Output  string `json:"output"`
    Error   string `json:"error"`
}

type GDBEvalResponse struct {
    Success bool   `json:"success"`
    Value   string `json:"value"`
    Type    string `json:"type"`
    Error   string `json:"error"`
}

type GDBClient struct {
    conn           net.Conn
    host           string
    port           int
    timeout        time.Duration
    connectTimeout time.Duration
}

func NewGDBClient(host string, port int) *GDBClient
func (c *GDBClient) Connect() error
func (c *GDBClient) Disconnect()
func (c *GDBClient) IsConnected() bool
func (c *GDBClient) Execute(command string) (*GDBResponse, error)
func (c *GDBClient) ExecuteBatch(commands []string) ([]string, error)
func (c *GDBClient) Evaluate(expression string) (*GDBEvalResponse, error)
func (c *GDBClient) Ping() bool
func (c *GDBClient) InitForCorefile(exePath, corePath string, isPostman bool) error
```

### Context 核心结构体

```go
// prepare/context.go

type Context struct {
    // 全局配置
    Args      *Args
    DirPath   string
    Mode      string  // "standalone" | "postmanprofile" | "postman"
    Project   string
    User      string
    ProfileId string
    Language  string  // "python" | "lua" | "c" | "nodejs" | "txos"
    LogFile   string

    // 核心模块
    Process   *ProcessManager
    Corefile  *CorefileManager
    Symbols   *SymbolsManager
    GDB       *GDBManager
    Cpp       *CppAnalyzer
    CppVtable *CppVtableManager

    // 内存分配器
    Libc      *LibcManager
    Tcmalloc  *TcmallocManager
    Jemalloc  *JemallocManager
    Mimalloc  *MimallocManager
    OpenBLAS  *OpenBLASManager

    // 语言运行时
    PythonLang *PythonLangManager
    LuaLang    *LuaLangManager
    NodejsLang *NodejsLangManager
    CLang      *CLangManager

    // 项目特定
    Asiocore    *AsiocoreManager
    AsiocoreNew *AsiocoreNewManager
    Messiah     *MessiahManager
    Mosd        *MosdManager
    Txos        *TxosManager
    G78         *G78Manager
    Taggeddict  *TaggeddictManager

    // 数据库
    DB         *DBC
    CDB        *DBC

    // 收集器
    Collect    *CollectMallocLib
    ParseDwarf *ParseDwarfManager
}
```

### ProcessManager 核心字段

```go
type ProcessManager struct {
    ctx          *Context
    Exe          string            // 可执行文件路径
    OriginExe    string            // 原始可执行文件路径
    Libc         string            // libc 库路径
    Mallocer     string            // "ptmalloc" | "tcmalloc" | "jemalloc" | "mimalloc"
    Pid          int
    ProcMaps     []MmapEntry       // 解析后的内存映射
    ProcMapsOutput string          // 原始 maps 文件内容
    FileMaps     map[string]string // 文件路径映射
    PthreadStack map[uint64]uint64 // begin → end
    MinPtr       uint64
    MaxPtr       uint64

    done         sync.Once         // 替代 Python 的 @once 装饰器
}
```

## 验收标准（AC）

- [ ] AC1: `prepare.Run()` 成功执行后，`common.S` 和 `common.Cpp` 包含与原 `.storage.json`/`.cpp.json` 完全一致的数据
- [ ] AC2: `./maze --build` 可以编译成功（Go 二进制包含 prepare 包）
- [ ] AC3: `python3 testdata/run_test.py python/20260129-complex-types-311` 测试通过（使用 Go prepare 替代 Python .maze.py）
- [ ] AC4: `python3 testdata/run_test.py cpp/20260304-stack-multithread` 测试通过
- [ ] AC5: GDB Socket Client 可以正确连接到现有 GDB Socket Server 并执行命令
- [ ] AC6: 所有外部命令调用（readelf、nm、strings、gcore、md5sum 等）正常工作
- [ ] AC7: standalone 模式和 postmanprofile 模式都可以正常运行
- [ ] AC8: `--tar` 一键分析功能正常工作
- [ ] AC9: `.maze.py` 和 `maze-py/*.py` 全部删除后，分析流程不受影响
- [ ] AC10: `gdb-scripts/*.py` 保留且正常工作（GDB 内部脚本不受影响）

### 回归验证清单

迁移过程中，每完成一个 Phase 都需要执行以下验证：

| 测试用例 | 覆盖点 |
|----------|--------|
| `python/20260129-complex-types-311` | Python 语言检测、pymalloc、ptmalloc、类型/符号/sizeof |
| `cpp/20260304-stack-multithread` | C++ DWARF 分析、vtable、栈变量收集、多线程 |
| 至少一个 tcmalloc 用例 | tcmalloc 版本检测和符号定位 |
| 至少一个 jemalloc 用例 | jemalloc TSD/TLS 收集 |
| 至少一个 mimalloc 用例 | mimalloc 版本检测 |
| 至少一个 Node.js 用例 | V8 常量加载、heap layout |
| 至少一个 Lua 用例（如有） | Lua 符号定位 |

## 约束与注意事项

### 技术约束

1. **GDB Scripts 必须保留 Python** — `gdb-scripts/` 下的脚本在 GDB 内部运行，使用 GDB Python API（`gdb.execute`、`gdb.parse_and_eval`），无法改写为 Go。包括：
   - `gdb-scripts/gdbcommand.py` — `offsets-of`、`maze-call-global-function` 等 GDB 自定义命令
   - `gdb-scripts/v8_heap_layout.py` — V8 堆布局导出
   - `gdb-scripts/az_v8_isolate.py` — Isolate 地址猜测
   - `gdb-scripts/az_node_init.py` — Node.js 初始化
   - `gdb/gdb-server/gdb_socket_server.py` — GDB Socket Server

2. **外部命令暂不替换** — `readelf`、`nm`、`strings`、`gcore` 等外部命令通过 `exec.Command` 调用，后续可逐步替换为 Go 原生实现（`debug/elf` 标准库等）。

3. **mimalloc 版本检测变更** — 原来通过 `ctypes.cdll.LoadLibrary` 调用 `mi_version()`，Go 中需要改为其他方式：
   - 方案 A: 通过 GDB `p mi_version()` 获取
   - 方案 B: 通过 `strings` 搜索版本标识
   - 方案 C: 通过 `nm` 查找 `mi_version` 后用 `readelf` 读取附近数据

4. **Python 2.7 兼容性不再需要** — Go 代码无需考虑 Python 2.7 兼容性。但 GDB Scripts 仍需兼容（GDB 内置的 Python 版本可能是 2.7 或 3.x）。

5. **`@once` 装饰器等价实现** — Go 中使用 `sync.Once` 替代 Python 的 `@once` 装饰器，确保初始化方法只执行一次。

### 性能考虑

1. **GDB 命令合并** — 原 Python 层逐条发送 GDB 命令，Go 实现时考虑批量发送（`execute_batch`）减少网络往返。
2. **并发初始化** — 无依赖关系的初始化步骤（如符号下载、版本检测）可以使用 goroutine 并发执行。
3. **缓存复用** — 保持现有的 `ctypes-{build_id}` 缓存机制，避免重复执行 GDB 命令。

### 迁移策略

1. **Phase 0-1 先行** — 完成基础设施和核心模块后，即可进行端到端验证
2. **双轨并行期** — 在完全迁移前，可以保留 `.maze.py`，通过环境变量或参数切换使用 Python 层还是 Go prepare
3. **逐步替换** — 每完成一个模块，运行回归测试确认无误
4. **最后删除** — 所有测试通过后，再执行 Phase 6 的清理工作

### 代码量预估

| 模块 | Python 行数 | Go 预估行数 | 说明 |
|------|------------|------------|------|
| 基础设施 (context/args/cmd/utils) | ~425 | ~500 | Go 类型声明更冗长 |
| GDB Client + Server 管理 | ~993 | ~800 | 协议实现更简洁，但 ctypes 解析复杂 |
| Process | ~610 | ~650 | 逻辑对等 |
| Symbols | ~293 | ~300 | 逻辑对等 |
| C++ Analyzer + VTable | ~522 | ~500 | 类型合并逻辑复杂但 Go 更清晰 |
| Python/Lua/Nodejs/C 语言检测 | ~564 | ~550 | 逻辑对等 |
| 5 个分配器 | ~597 | ~550 | 逻辑对等 |
| 项目特定 (Asiocore/Messiah/Mosd/Txos/G78/Taggeddict) | ~493 | ~450 | 逻辑对等 |
| DB/Collect/Storage/Fill | ~300 | ~350 | 增加类型定义 |
| 入口集成 (prepare.go + main.go 修改) | ~254+592 | ~600 | 合并和重构 |
| **合计** | **~4900** | **~5250** | Go 代码略多约 7% |

## 风险与缓解措施

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| GDB 命令输出解析差异 | 高 | 中 | 对照 Python 和 Go 的解析结果逐字段比对 |
| ctypes 缓存格式不兼容 | 中 | 低 | 保持 JSON 格式一致；或增加版本号区分 |
| 外部命令输出编码问题 | 中 | 中 | 统一使用 UTF-8，处理非 ASCII 字符 |
| GDB Server 通信稳定性 | 高 | 低 | 已有成熟协议；Go Client 增加重试和超时 |
| 项目特定逻辑遗漏 | 中 | 中 | 优先保证核心流程（Python/C++ 用例）；项目特定逻辑后补 |
| `common.S` 字段遗漏或类型不匹配 | 高 | 中 | 写入后立即与原 JSON 输出对比验证 |
| 工期过长影响其他开发 | 中 | 高 | 分 Phase 交付，Phase 0-1 后即可投入使用 |

## 参考文档

- [doc/maze_py_analysis.md](../doc/maze_py_analysis.md) — .maze.py 详细分析
- [dev-log/2026-03-11-maze-py-split-remaining-classes.md](2026-03-11-maze-py-split-remaining-classes.md) — Python 层拆分计划（现已被本方案取代）
- [dev-log/2026-01-28-gdb-socket-server-integration.md](2026-01-28-gdb-socket-server-integration.md) — GDB Socket Server 集成文档
- [dev-log/2026-03-10-gdb-directory-reorganization.md](2026-03-10-gdb-directory-reorganization.md) — GDB 目录重组
- [dev-log/2026-01-30-main-function-analysis.md](2026-01-30-main-function-analysis.md) — main.go 主函数流程分析



> This is fix iteration #1. Be more thorough — address every QA finding.



## QA Feedback


---
**@maze-qa-agent[bot]** (2026-03-11T12:05:57+00:00):

### qa report

## QA Result: FAIL

## Findings

### 阻塞性问题：GDB `file` 命令使用了原始 maps 路径，导致无法加载调试符号（影响 AC1、AC3、AC4、AC5、AC6、AC8）

- **问题**: `prepare/gdb.go:626` 的 `InitServerForCorefile()` 向 GDB 发送 `"file " + ctx.Process.Exe`，使用的是 maps 文件中的原始路径（如 `/usr/local/bin/python3.11` 或 `/home/.../stack_multithread_test`）。在 tar 模式下，这些路径在本地不存在（文件已被 md5 重命名解压到临时目录），导致 GDB 无法加载可执行文件和调试符号。
- **后果链**:
  1. GDB `file` 命令加载失败 → GDB 没有调试符号信息
  2. `offsets-of struct _heap_info` 等命令返回空 → `Sizeof`/`Structs` 数据缺失
  3. `FillCommonVars()` 填充了空的 sizeof/structs 数据到 `common.S`
  4. Go 分析引擎因缺少关键数据 panic
- **证据 1 — Python 测试 panic**:
  ```
  panic: runtime error: index out of range [7] with length 0
  at python/dict.go:315 → PointerM(m, ...) 中 m 为空切片
  ```
  PyTypeObject 的结构体偏移可能不正确或缺失，导致计算出错误的内存地址。
- **证据 2 — C++ 测试 panic**:
  ```
  panic: initPtmalloc sizeof _heap_info
  at mallocer/ptmalloc/init.go:52
  ```
  `S.Sizeof["struct _heap_info"]` 为空，说明 GDB `Ctypes()` 没有成功获取 libc 结构体信息。
- **证据 3 — 两个测试均有 readelf 错误**:
  ```
  readelf：错误：无法找到程序解释器名称
  ```
  这表明 readelf 读取了不正确的文件（可能是 core dump 文件而非 ELF 可执行文件）。
- **根因分析**:
  - `process.go:initExe()` 将 `p.Exe` 设为 maps 中的原始路径（设计正确，保留原始信息）
  - 其他代码正确使用了 `Realpath()`: `gdb.go:167` (`GetBuildId`)、`symbols.go:15`、`process.go:347` (`setupLanguage`)
  - 但 `gdb.go:626` 和 `gdb.go:516` (GenerateDebugScript) 直接使用了 `ctx.Process.Exe` 而非 `ctx.Process.Realpath(ctx.Process.Exe)`
- **影响范围**: AC1, AC3, AC4, AC5, AC6, AC8 全部未通过
- **建议修复**:
  ```go
  // gdb.go:626 修改为:
  initCmds = append(initCmds, "file "+ctx.Process.Realpath(ctx.Process.Exe))

  // gdb.go:516 (GenerateDebugScript) 也需要同样修复
  ```

### 次要问题 1：`readelf` 错误消息 "无法找到程序解释器名称"

- **问题**: 两个测试输出中都有 `readelf：错误：无法找到程序解释器名称`
- **位置**: 推测来自 `process.go` 中的 `LoadOffset()` 调用 `readelf -a`，或 `Symbols.DownloadSymbolFiles()` 中的 `readelf -n`
- **影响**: 不确定是否导致数据缺失（可能只是 warning），但说明某些 readelf 调用仍然使用了错误路径或对 core 文件而非 ELF 文件调用
- **建议**: 检查所有 `readelf` 调用是否都使用了 `Realpath()` 解析后的路径

### 次要问题 2：`Libc.Address()` 中的冗余/无效代码

- **位置**: `alloc.go:165-174`
- **问题**: 回退逻辑中 `if _, ok := ctx.Symbols.Symbols["main_arena"]; !ok { if addr, ok2 := ctx.Symbols.Symbols["main_arena"]; ok2 { ... } }` — 从同一个 map 检查同一个 key，内层条件永远为 false。`mp_` 也是同样的问题。
- **影响**: 低，逻辑等价于无操作
- **建议**: 这段代码原意可能是从 GDB `info address` 的结果回退，但当前实现无效

## 已验证通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC2 | ✅ PASS | `./maze --build` 编译成功，生成 29MB ELF 二进制 |
| AC9 | ✅ PASS | `.maze.py` 已删除、`maze-py/` 目录已删除，编译和启动不受影响 |
| AC10 | ✅ PASS | `gdb/gdb-scripts/*.py`（30 个文件）和 `gdb/gdb-server/gdb_socket_server.py` 全部保留 |

## 未通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC1 | ❌ FAIL | GDB `file` 命令使用错误路径，导致 sizeof/structs 数据缺失 |
| AC3 | ❌ FAIL | Python 测试 panic: `index out of range [7] with length 0` |
| AC4 | ❌ FAIL | C++ 测试 panic: `initPtmalloc sizeof _heap_info` |
| AC5 | ⚠️ PARTIAL | GDB Socket Client 连接成功，但 GDB 未加载正确的可执行文件 |
| AC6 | ⚠️ PARTIAL | 外部命令能执行，但 readelf 有错误输出 |
| AC7 | ❌ 未验证 | 无法在 tar 模式下产生有效结果 |
| AC8 | ❌ FAIL | tar 模式因 GDB 加载错误路径导致分析 panic |

## Test Results

| 测试用例 | 状态 | 说明 |
|---------|------|------|
| TC-001 | ✅ PASS | prepare 包基础脚手架存在（19+ 个 Go 文件） |
| TC-002 | ✅ PASS | main.go 正确导入 `maze/prepare` 并调用 `prepare.Run()` |
| TC-003 | ✅ PASS | common 初始化不再依赖 `.storage.json` / `.cpp.json` |
| TC-004 | ✅ PASS | `.maze.py` 和 `maze-py/` 已删除，GDB 脚本保留 |
| TC-005 | ✅ PASS | maze 入口已从 Python wrapper 改为 Bash 脚本 |
| TC-006 | ✅ PASS | GDB 脚本仍基于 GDB Python API（`import gdb` 存在） |
| TC-007 | ❌ FAIL | Python 主流程回归：`panic: index out of range [7] with length 0` |
| TC-008 | ❌ FAIL | C++ 多线程栈变量回归：`panic: initPtmalloc sizeof _heap_info` |
| TC-009 | ❌ FAIL | `--tar` 能力回归：因 GDB 加载错误路径导致 panic |
| TC-010 | ⚠️ PARTIAL | 源码检查通过（模式分发、文件清理正确），运行验证失败 |

### 自动化测试通过

```
$ go test -count=1 ./qa/issue110 -v
=== RUN   TestPreparePackageScaffoldExists
--- PASS: TestPreparePackageScaffoldExists (0.00s)
=== RUN   TestMainUsesPrepareRun
--- PASS: TestMainUsesPrepareRun (0.00s)
=== RUN   TestCommonInitNoLongerDependsOnLegacyJSONBootstrap
--- PASS: TestCommonInitNoLongerDependsOnLegacyJSONBootstrap (0.00s)
=== RUN   TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain
--- PASS: TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain (0.00s)
=== RUN   TestMazeEntryIsNoLongerPythonWrapper
--- PASS: TestMazeEntryIsNoLongerPythonWrapper (0.00s)
=== RUN   TestGdbScriptsStillUsePythonAPI
--- PASS: TestGdbScriptsStillUsePythonAPI (0.00s)
PASS
ok      maze/qa/issue110        0.002s
```

### 构建测试通过

```
$ ./maze --build
building .maze ...
$ ls -la .maze
-rwxr-xr-x 1 hcjn0770 hcjn0770 30229680 .maze
$ file .maze
.maze: ELF 64-bit LSB executable, x86-64, with debug_info, not stripped
```

### TC-007 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 2636550
Python version: 3.11.0 (iversion: 31100)
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
readelf：错误：无法找到程序解释器名称
prepare.Run() completed successfully
maze !_.zz
CPU 34.5s Mem 2.9G Rss 120.8M goroutines 14
WARNING MarkMmapOwner owner pymalloc ptr 7fdbf1100000 size 100000 not in any anon mmap
...
panic: runtime error: index out of range [7] with length 0
goroutine 1 [running]:
encoding/binary.littleEndian.Uint64(...)
maze/common.PointerM(...)
maze/python.FindPyTypeAllBases(0x7fdbf2a5e6a0, 0xc0001eb5e0)
        python/dict.go:315 +0x3a7

❌ Test ERROR: python/20260129-complex-types-311
   Maze analysis failed with exit code 2
```

### TC-008 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 68446
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
readelf：错误：无法找到程序解释器名称
prepare.Run() completed successfully
maze !_.zz
CPU 44.2s Mem 3.5G Rss 48.5M goroutines 14
panic: initPtmalloc sizeof _heap_info
goroutine 1 [running]:
maze/mallocer/ptmalloc.initPtmallloc()
        mallocer/ptmalloc/init.go:52 +0x102b

❌ Test ERROR: cpp/20260304-stack-multithread
   Maze analysis failed with exit code 2
```

## 与上次 QA 的进步对比

| 方面 | 上次 QA (第10轮) | 本次 QA (第11轮) |
|------|------------------|------------------|
| GDB Server | ❌ 启动后立即退出 | ✅ **成功启动并保持运行** |
| prepare.Run() | ❌ 因 GDB 连接超时失败 | ✅ **完整执行成功** |
| tar 模式 readelf | ❌ 使用原始路径 | ✅ **setupLanguage 已修复** |
| Cpp.merge() | ❌ 空实现 | ✅ **完整实现** |
| cmd_struct_malloc_state | ❌ 未解析 | ✅ **已支持解析** |
| GDB file 命令路径 | （未测试到此步骤） | ❌ **新发现：使用了错误路径** |
| 分析引擎执行 | ❌ 未到达此步骤 | ⚠️ **到达但 panic** |

**总结**: 上一轮 QA 发现的 3 个问题（GDB Server 退出、readelf 路径、merge 未实现）已全部修复。系统已经能够完整执行 `prepare.Run()`，但 GDB 初始化时 `file` 命令使用了错误路径（原始 maps 路径而非 Realpath 解析后的路径），导致 GDB 无法加载调试符号，sizeof/structs 数据为空，分析引擎 panic。

## 建议修复优先级

1. **P0 — `gdb.go:626` 使用 Realpath 解析 exe 路径**: `"file "+ctx.Process.Realpath(ctx.Process.Exe)` — 这是当前唯一的核心阻塞问题
2. **P0 — `gdb.go:516` GenerateDebugScript 同样修复**: 保持一致性
3. **P1 — 排查 readelf "无法找到程序解释器名称" 错误**: 检查是否有 readelf 调用使用了未解析的路径
4. **P2 — 修复 `alloc.go:165-174` 冗余回退逻辑**: 当前代码从同一 map 检查同一 key，等价于无操作

## 结论

当前实现**已完成重大里程碑**：
- ✅ GDB Server 进程管理修复，成功启动并保持运行
- ✅ `prepare.Run()` 主流程 13 个步骤全部连接且执行成功
- ✅ GDB Socket Client 正常通信
- ✅ tar 模式文件解压、symlink 创建、结果文件复制回原始目录均正常
- ✅ Python/Lua/Nodejs/C 语言检测正常
- ✅ Cpp.merge() 完整实现

**唯一的核心阻塞问题**: `InitServerForCorefile` 中 GDB `file` 命令使用了未经 `Realpath()` 解析的原始 maps 路径，导致 GDB 无法加载正确的可执行文件和调试符号。修复这一个点后，所有 sizeof/structs 数据预计可以正常获取，分析引擎应能完成。

**通过: 6 / 10 | 失败: 3 / 10 | 部分: 1 / 10**

无法发布。修复 `gdb.go:626` 的 `file` 命令路径问题后，可解锁全部功能测试。


---
*Archived: [20260311192200-qa-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311192200-qa-report.md)*


---
**@maze-developer-agent[bot]** (2026-03-11T12:02:48+00:00):

### fix report

## Summary

Fixed three QA-identified issues blocking GDB interaction in the prepare package:

1. **GDB Server process exits immediately** — The GDB process was started without stdin, causing it to exit after executing `-ex` commands. Fixed by providing a stdin pipe (`cmd.StdinPipe()`) to keep GDB in interactive mode, allowing `gdb.post_event()` based socket server to function.

2. **tar mode readelf path** — In tar mode, `setupLanguage()` used the original file paths from maps (e.g., `/home/.../python3.11`) for `readelf`, but files were renamed by MD5 hash. Fixed by loading `{pid}.md5` JSON file to create file path mappings, and using `Realpath()` in `setupLanguage()`.

3. **Cpp.merge() simplified implementation** — The merge function was a no-op stub. Implemented the full type index remapping logic ported from the original Python `cpp_analyzer.py`, including vtable address offset, symbol address offset, type struct field remapping, and deduplication.

4. **cmd_struct_malloc_state parsing** — The `cmd_struct_malloc_state` GDB command output was executed but not parsed, causing `struct malloc_state` data to be missing. Fixed by parsing `cmd_` prefixed command output the same way as `offsets-of` output.

## Changes

- `prepare/gdb_server.go`: Added `stdinPipe` and `logFile` fields to `GDBServer`; `Start()` now creates stdin pipe to keep GDB alive; `Stop()` closes pipe gracefully; increased timeout to 10s; fixed `IsRunning()` which used invalid `os.Signal(nil)`
- `prepare/process.go`: Added `loadMd5FileMap()` to load `{pid}.md5` JSON mapping for tar mode; `setupLanguage()` now uses resolved paths via `Realpath()` with fallback for missing files; added `encoding/json` import
- `prepare/gdb.go`: `Ctypes()` now parses `cmd_` prefixed command output (e.g., `cmd_struct_malloc_state`) using the same struct/sizeof parsing logic as `offsets-of`
- `prepare/cpp_analyzer.go`: Implemented full `merge()` with type index remapping, vtable/symbol address offset application, type struct field remapping, and conversion to `common.CppStruct`/`SymbolInfo` types; added helper functions for JSON data extraction

## Testing

- `go test ./qa/issue110 -v`: 6/6 PASS
- `./maze --build`: Build successful (30MB binary)
- `python3 testdata/run_test.py python/20260129-complex-types-311`: GDB server starts, struct data flows, ptmalloc initializes correctly. Remaining failure is in Python type analysis (struct field access) which requires further investigation of GDB `offsets-of` output for specific Python types.


---
*Archived: [20260311190353-dev-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311190353-dev-report.md)*


---
**@prim** (2026-03-11T11:39:51+00:00):

PR #113 was rejected by human review. Routing to fix agent (human fix iteration 3).

PR: https://github.com/prim/maze/pull/113

## Human Review Feedback

- **prim**: 继续 fix 要求通过 qa 所有测试项目。
需要注意 所有 python 脚本文件都需要被转换为 golang 文件。qa 要注意测试这一点。


---
**@maze-developer-agent[bot]** (2026-03-11T11:26:26+00:00):

All checks passed. Ready for human review.

PR: https://github.com/prim/maze/pull/113


---
**@maze-qa-agent[bot]** (2026-03-11T11:26:13+00:00):

### qa report

## QA Result: FAIL

## Findings

### 阻塞性问题：GDB `file` 命令使用了原始 maps 路径，导致无法加载调试符号（影响 AC1、AC3、AC4、AC5、AC6、AC8）

- **问题**: `prepare/gdb.go:626` 的 `InitServerForCorefile()` 向 GDB 发送 `"file " + ctx.Process.Exe`，使用的是 maps 文件中的原始路径（如 `/usr/local/bin/python3.11` 或 `/home/.../stack_multithread_test`）。在 tar 模式下，这些路径在本地不存在（文件已被 md5 重命名解压到临时目录），导致 GDB 无法加载可执行文件和调试符号。
- **后果链**:
  1. GDB `file` 命令加载失败 → GDB 没有调试符号信息
  2. `offsets-of struct _heap_info` 等命令返回空 → `Sizeof`/`Structs` 数据缺失
  3. `FillCommonVars()` 填充了空的 sizeof/structs 数据到 `common.S`
  4. Go 分析引擎因缺少关键数据 panic
- **证据 1 — Python 测试 panic**:
  ```
  panic: runtime error: index out of range [7] with length 0
  at python/dict.go:315 → PointerM(m, ...) 中 m 为空切片
  ```
  PyTypeObject 的结构体偏移可能不正确或缺失，导致计算出错误的内存地址。
- **证据 2 — C++ 测试 panic**:
  ```
  panic: initPtmalloc sizeof _heap_info
  at mallocer/ptmalloc/init.go:52
  ```
  `S.Sizeof["struct _heap_info"]` 为空，说明 GDB `Ctypes()` 没有成功获取 libc 结构体信息。
- **证据 3 — 两个测试均有 readelf 错误**:
  ```
  readelf：错误：无法找到程序解释器名称
  ```
  这表明 readelf 读取了不正确的文件（可能是 core dump 文件而非 ELF 可执行文件）。
- **根因分析**:
  - `process.go:initExe()` 将 `p.Exe` 设为 maps 中的原始路径（设计正确，保留原始信息）
  - 其他代码正确使用了 `Realpath()`: `gdb.go:167` (`GetBuildId`)、`symbols.go:15`、`process.go:347` (`setupLanguage`)
  - 但 `gdb.go:626` 和 `gdb.go:516` (GenerateDebugScript) 直接使用了 `ctx.Process.Exe` 而非 `ctx.Process.Realpath(ctx.Process.Exe)`
- **影响范围**: AC1, AC3, AC4, AC5, AC6, AC8 全部未通过
- **建议修复**:
  ```go
  // gdb.go:626 修改为:
  initCmds = append(initCmds, "file "+ctx.Process.Realpath(ctx.Process.Exe))

  // gdb.go:516 (GenerateDebugScript) 也需要同样修复
  ```

### 次要问题 1：`readelf` 错误消息 "无法找到程序解释器名称"

- **问题**: 两个测试输出中都有 `readelf：错误：无法找到程序解释器名称`
- **位置**: 推测来自 `process.go` 中的 `LoadOffset()` 调用 `readelf -a`，或 `Symbols.DownloadSymbolFiles()` 中的 `readelf -n`
- **影响**: 不确定是否导致数据缺失（可能只是 warning），但说明某些 readelf 调用仍然使用了错误路径或对 core 文件而非 ELF 文件调用
- **建议**: 检查所有 `readelf` 调用是否都使用了 `Realpath()` 解析后的路径

### 次要问题 2：`Libc.Address()` 中的冗余/无效代码

- **位置**: `alloc.go:165-174`
- **问题**: 回退逻辑中 `if _, ok := ctx.Symbols.Symbols["main_arena"]; !ok { if addr, ok2 := ctx.Symbols.Symbols["main_arena"]; ok2 { ... } }` — 从同一个 map 检查同一个 key，内层条件永远为 false。`mp_` 也是同样的问题。
- **影响**: 低，逻辑等价于无操作
- **建议**: 这段代码原意可能是从 GDB `info address` 的结果回退，但当前实现无效

## 已验证通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC2 | ✅ PASS | `./maze --build` 编译成功，生成 29MB ELF 二进制 |
| AC9 | ✅ PASS | `.maze.py` 已删除、`maze-py/` 目录已删除，编译和启动不受影响 |
| AC10 | ✅ PASS | `gdb/gdb-scripts/*.py`（30 个文件）和 `gdb/gdb-server/gdb_socket_server.py` 全部保留 |

## 未通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC1 | ❌ FAIL | GDB `file` 命令使用错误路径，导致 sizeof/structs 数据缺失 |
| AC3 | ❌ FAIL | Python 测试 panic: `index out of range [7] with length 0` |
| AC4 | ❌ FAIL | C++ 测试 panic: `initPtmalloc sizeof _heap_info` |
| AC5 | ⚠️ PARTIAL | GDB Socket Client 连接成功，但 GDB 未加载正确的可执行文件 |
| AC6 | ⚠️ PARTIAL | 外部命令能执行，但 readelf 有错误输出 |
| AC7 | ❌ 未验证 | 无法在 tar 模式下产生有效结果 |
| AC8 | ❌ FAIL | tar 模式因 GDB 加载错误路径导致分析 panic |

## Test Results

| 测试用例 | 状态 | 说明 |
|---------|------|------|
| TC-001 | ✅ PASS | prepare 包基础脚手架存在（19+ 个 Go 文件） |
| TC-002 | ✅ PASS | main.go 正确导入 `maze/prepare` 并调用 `prepare.Run()` |
| TC-003 | ✅ PASS | common 初始化不再依赖 `.storage.json` / `.cpp.json` |
| TC-004 | ✅ PASS | `.maze.py` 和 `maze-py/` 已删除，GDB 脚本保留 |
| TC-005 | ✅ PASS | maze 入口已从 Python wrapper 改为 Bash 脚本 |
| TC-006 | ✅ PASS | GDB 脚本仍基于 GDB Python API（`import gdb` 存在） |
| TC-007 | ❌ FAIL | Python 主流程回归：`panic: index out of range [7] with length 0` |
| TC-008 | ❌ FAIL | C++ 多线程栈变量回归：`panic: initPtmalloc sizeof _heap_info` |
| TC-009 | ❌ FAIL | `--tar` 能力回归：因 GDB 加载错误路径导致 panic |
| TC-010 | ⚠️ PARTIAL | 源码检查通过（模式分发、文件清理正确），运行验证失败 |

### 自动化测试通过

```
$ go test -count=1 ./qa/issue110 -v
=== RUN   TestPreparePackageScaffoldExists
--- PASS: TestPreparePackageScaffoldExists (0.00s)
=== RUN   TestMainUsesPrepareRun
--- PASS: TestMainUsesPrepareRun (0.00s)
=== RUN   TestCommonInitNoLongerDependsOnLegacyJSONBootstrap
--- PASS: TestCommonInitNoLongerDependsOnLegacyJSONBootstrap (0.00s)
=== RUN   TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain
--- PASS: TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain (0.00s)
=== RUN   TestMazeEntryIsNoLongerPythonWrapper
--- PASS: TestMazeEntryIsNoLongerPythonWrapper (0.00s)
=== RUN   TestGdbScriptsStillUsePythonAPI
--- PASS: TestGdbScriptsStillUsePythonAPI (0.00s)
PASS
ok      maze/qa/issue110        0.002s
```

### 构建测试通过

```
$ ./maze --build
building .maze ...
$ ls -la .maze
-rwxr-xr-x 1 hcjn0770 hcjn0770 30229680 .maze
$ file .maze
.maze: ELF 64-bit LSB executable, x86-64, with debug_info, not stripped
```

### TC-007 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 2636550
Python version: 3.11.0 (iversion: 31100)
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
readelf：错误：无法找到程序解释器名称
prepare.Run() completed successfully
maze !_.zz
CPU 34.5s Mem 2.9G Rss 120.8M goroutines 14
WARNING MarkMmapOwner owner pymalloc ptr 7fdbf1100000 size 100000 not in any anon mmap
...
panic: runtime error: index out of range [7] with length 0
goroutine 1 [running]:
encoding/binary.littleEndian.Uint64(...)
maze/common.PointerM(...)
maze/python.FindPyTypeAllBases(0x7fdbf2a5e6a0, 0xc0001eb5e0)
        python/dict.go:315 +0x3a7

❌ Test ERROR: python/20260129-complex-types-311
   Maze analysis failed with exit code 2
```

### TC-008 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 68446
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
readelf：错误：无法找到程序解释器名称
prepare.Run() completed successfully
maze !_.zz
CPU 44.2s Mem 3.5G Rss 48.5M goroutines 14
panic: initPtmalloc sizeof _heap_info
goroutine 1 [running]:
maze/mallocer/ptmalloc.initPtmallloc()
        mallocer/ptmalloc/init.go:52 +0x102b

❌ Test ERROR: cpp/20260304-stack-multithread
   Maze analysis failed with exit code 2
```

## 与上次 QA 的进步对比

| 方面 | 上次 QA (第10轮) | 本次 QA (第11轮) |
|------|------------------|------------------|
| GDB Server | ❌ 启动后立即退出 | ✅ **成功启动并保持运行** |
| prepare.Run() | ❌ 因 GDB 连接超时失败 | ✅ **完整执行成功** |
| tar 模式 readelf | ❌ 使用原始路径 | ✅ **setupLanguage 已修复** |
| Cpp.merge() | ❌ 空实现 | ✅ **完整实现** |
| cmd_struct_malloc_state | ❌ 未解析 | ✅ **已支持解析** |
| GDB file 命令路径 | （未测试到此步骤） | ❌ **新发现：使用了错误路径** |
| 分析引擎执行 | ❌ 未到达此步骤 | ⚠️ **到达但 panic** |

**总结**: 上一轮 QA 发现的 3 个问题（GDB Server 退出、readelf 路径、merge 未实现）已全部修复。系统已经能够完整执行 `prepare.Run()`，但 GDB 初始化时 `file` 命令使用了错误路径（原始 maps 路径而非 Realpath 解析后的路径），导致 GDB 无法加载调试符号，sizeof/structs 数据为空，分析引擎 panic。

## 建议修复优先级

1. **P0 — `gdb.go:626` 使用 Realpath 解析 exe 路径**: `"file "+ctx.Process.Realpath(ctx.Process.Exe)` — 这是当前唯一的核心阻塞问题
2. **P0 — `gdb.go:516` GenerateDebugScript 同样修复**: 保持一致性
3. **P1 — 排查 readelf "无法找到程序解释器名称" 错误**: 检查是否有 readelf 调用使用了未解析的路径
4. **P2 — 修复 `alloc.go:165-174` 冗余回退逻辑**: 当前代码从同一 map 检查同一 key，等价于无操作

## 结论

当前实现**已完成重大里程碑**：
- ✅ GDB Server 进程管理修复，成功启动并保持运行
- ✅ `prepare.Run()` 主流程 13 个步骤全部连接且执行成功
- ✅ GDB Socket Client 正常通信
- ✅ tar 模式文件解压、symlink 创建、结果文件复制回原始目录均正常
- ✅ Python/Lua/Nodejs/C 语言检测正常
- ✅ Cpp.merge() 完整实现

**唯一的核心阻塞问题**: `InitServerForCorefile` 中 GDB `file` 命令使用了未经 `Realpath()` 解析的原始 maps 路径，导致 GDB 无法加载正确的可执行文件和调试符号。修复这一个点后，所有 sizeof/structs 数据预计可以正常获取，分析引擎应能完成。

**通过: 6 / 10 | 失败: 3 / 10 | 部分: 1 / 10**

无法发布。修复 `gdb.go:626` 的 `file` 命令路径问题后，可解锁全部功能测试。


---
*Archived: [20260311192200-qa-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311192200-qa-report.md)*


---
**@maze-developer-agent[bot]** (2026-03-11T11:06:38+00:00):

### fix report

## Summary

Fixed three QA-identified issues blocking GDB interaction in the prepare package:

1. **GDB Server process exits immediately** — The GDB process was started without stdin, causing it to exit after executing `-ex` commands. Fixed by providing a stdin pipe (`cmd.StdinPipe()`) to keep GDB in interactive mode, allowing `gdb.post_event()` based socket server to function.

2. **tar mode readelf path** — In tar mode, `setupLanguage()` used the original file paths from maps (e.g., `/home/.../python3.11`) for `readelf`, but files were renamed by MD5 hash. Fixed by loading `{pid}.md5` JSON file to create file path mappings, and using `Realpath()` in `setupLanguage()`.

3. **Cpp.merge() simplified implementation** — The merge function was a no-op stub. Implemented the full type index remapping logic ported from the original Python `cpp_analyzer.py`, including vtable address offset, symbol address offset, type struct field remapping, and deduplication.

4. **cmd_struct_malloc_state parsing** — The `cmd_struct_malloc_state` GDB command output was executed but not parsed, causing `struct malloc_state` data to be missing. Fixed by parsing `cmd_` prefixed command output the same way as `offsets-of` output.

## Changes

- `prepare/gdb_server.go`: Added `stdinPipe` and `logFile` fields to `GDBServer`; `Start()` now creates stdin pipe to keep GDB alive; `Stop()` closes pipe gracefully; increased timeout to 10s; fixed `IsRunning()` which used invalid `os.Signal(nil)`
- `prepare/process.go`: Added `loadMd5FileMap()` to load `{pid}.md5` JSON mapping for tar mode; `setupLanguage()` now uses resolved paths via `Realpath()` with fallback for missing files; added `encoding/json` import
- `prepare/gdb.go`: `Ctypes()` now parses `cmd_` prefixed command output (e.g., `cmd_struct_malloc_state`) using the same struct/sizeof parsing logic as `offsets-of`
- `prepare/cpp_analyzer.go`: Implemented full `merge()` with type index remapping, vtable/symbol address offset application, type struct field remapping, and conversion to `common.CppStruct`/`SymbolInfo` types; added helper functions for JSON data extraction

## Testing

- `go test ./qa/issue110 -v`: 6/6 PASS
- `./maze --build`: Build successful (30MB binary)
- `python3 testdata/run_test.py python/20260129-complex-types-311`: GDB server starts, struct data flows, ptmalloc initializes correctly. Remaining failure is in Python type analysis (struct field access) which requires further investigation of GDB `offsets-of` output for specific Python types.


---
*Archived: [20260311190353-dev-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311190353-dev-report.md)*


---
**@prim** (2026-03-11T10:47:34+00:00):

PR #112 was rejected by human review. Routing to fix agent (human fix iteration 2).

PR: https://github.com/prim/maze/pull/112

## Human Review Feedback

- **prim**: 继续 fix。


---
**@maze-developer-agent[bot]** (2026-03-11T10:32:01+00:00):

All checks passed. Ready for human review.

PR: https://github.com/prim/maze/pull/112


---
**@maze-qa-agent[bot]** (2026-03-11T10:31:48+00:00):

### qa report

## QA Result: FAIL

## Findings

### 阻塞性问题：GDB Server 启动后立即退出，导致连接超时（影响 AC1、AC3、AC4、AC5、AC6、AC8）

- **问题**: `prepare.Run()` 在尝试连接 GDB Socket Server 时，报错 `GDB server failed to start within timeout`
- **根因分析**:
  - `prepare/gdb_server.go` 的 `Start()` 通过 `exec.Command` 启动 GDB，传入 `-x gdb_socket_server.py -ex "start_gdb_server 9999"`
  - GDB 成功加载了脚本，并输出 "GDB Socket Server started on 127.0.0.1:9999"（在 `server_process.log` 中确认）
  - 但 GDB 进程在执行完所有 `-ex` 命令后**立即退出**，没有持续运行
  - 原因：GDB 在非交互模式下（没有 `-batch` 但也没有后续交互命令），执行完传入的命令序列后会退出
  - GDB Socket Server 的 Python 脚本虽然启动了 socket 监听线程，但 GDB 主进程退出后，所有子线程和 socket 也随之关闭
  - Go 的 `waitForServer()` 轮询 TCP 端口 5 秒，始终无法连接
  - **QA 手动验证**：在临时目录下执行相同的 GDB 命令，日志确认 server 输出了 "started"，但 `ps` 确认进程已退出，`ss -tlnp` 确认端口未开放
- **影响范围**:
  - TC-007: Python 测试 → FAIL（`prepare.Run() failed: failed to connect to GDB`）
  - TC-008: C++ 测试 → FAIL（同上）
  - TC-009: `--tar` 能力 → FAIL（同上）
  - AC1, AC3, AC4, AC5, AC6, AC8 全部未通过
- **建议修复**:
  - 方案 A：在 GDB 命令序列末尾添加类似 `set interactive-mode` 或使用 `sleep infinity` 类 GDB 命令防止 GDB 退出
  - 方案 B：参考原有 Python 层启动 GDB Server 的方式（`start_maze_gdb_server.sh`），确保 GDB 进入交互循环（`gdb_socket_server.py` 中的 `start_gdb_server` 命令可能需要 GDB 保持事件循环来处理 socket）
  - 方案 C：检查 `gdb_socket_server.py` 中是否有 GDB event loop 支持，确保在非交互模式下也能保持进程活跃

### 次要问题 1：tar 模式下 `readelf` 使用了 maps 中的原始路径

- **问题**: 在 C++ 测试中，`readelf` 尝试读取 maps 文件里记录的原始路径 `/home/.../stack_multithread_test`，但 tar 解压后文件已被 md5 重命名为 `519f9acc4b24d86bcbe85e0806f67cf0`
- **错误信息**: `readelf：错误：'/home/.../stack_multithread_test': No such file`
- **影响**: 语言检测 (`setupLanguage`) 中的 `readelf -s` 失败，导致语言默认为 "c" 而非正确检测
- **建议修复**: 在 tar 模式下，`p.Exe` 应使用解压后的实际文件路径，而非 maps 中的原始路径

### 次要问题 2：Cpp.merge() 为简化实现

- **问题**: `prepare/cpp_analyzer.go` 的 `merge()` 方法注释为 "简化实现"，实际只打印了日志，没有合并多个 ELF 文件的类型索引和数据
- **影响**: 即使 GDB Server 正常工作，C++ DWARF 分析的 vtable、类型名、栈变量合并逻辑也不完整
- **原始代码**: Python `cpp_analyzer.py` 的 `merge()` 约 160 行，负责类型索引重映射

## 已验证通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC2 | ✅ PASS | `./maze --build` 编译成功，生成 30MB ELF 二进制 |
| AC9 | ✅ PASS | `.maze.py` 已删除、`maze-py/` 目录已删除，编译和启动不受影响 |
| AC10 | ✅ PASS | `gdb/gdb-scripts/*.py`（30 个文件）和 `gdb/gdb-server/gdb_socket_server.py` 全部保留且包含 `import gdb` |

## 未通过的项

| AC | 状态 | 说明 |
|----|------|------|
| AC1 | ❌ FAIL | `common.S` 和 `common.Cpp` 未被 GDB 数据填充（GDB Server 启动失败） |
| AC3 | ❌ FAIL | `python/20260129-complex-types-311` 测试失败（GDB Server 连接超时） |
| AC4 | ❌ FAIL | `cpp/20260304-stack-multithread` 测试失败（同上 + readelf 路径错误） |
| AC5 | ❌ FAIL | GDB Socket Client 存在且实现正确，但无法连接到 GDB Server（Server 启动后立即退出） |
| AC6 | ❌ FAIL | 外部命令封装存在但 readelf 在 tar 模式下使用了错误路径 |
| AC7 | ❌ 未验证 | standalone 模式在 tar 模式下无法产生有效结果 |
| AC8 | ❌ FAIL | `--tar` 模式因 GDB Server 启动失败而整体失败 |

## Test Results

| 测试用例 | 状态 | 说明 |
|---------|------|------|
| TC-001 | ✅ PASS | prepare 包基础脚手架存在（19 个 Go 文件） |
| TC-002 | ✅ PASS | main.go 正确导入 `maze/prepare` 并调用 `prepare.Run()` |
| TC-003 | ✅ PASS | common 初始化不再依赖 `.storage.json` / `.cpp.json` |
| TC-004 | ✅ PASS | `.maze.py` 和 `maze-py/` 已删除，GDB 脚本保留 |
| TC-005 | ✅ PASS | maze 入口已从 Python wrapper 改为 Bash 脚本 |
| TC-006 | ✅ PASS | GDB 脚本仍基于 GDB Python API（`import gdb` 存在） |
| TC-007 | ❌ FAIL | Python 主流程回归：`GDB server failed to start within timeout` |
| TC-008 | ❌ FAIL | C++ 多线程栈变量回归：`GDB server failed to start within timeout` + readelf 路径错误 |
| TC-009 | ❌ FAIL | `--tar` 能力回归：因 GDB Server 问题无法完成分析 |
| TC-010 | ⚠️ PARTIAL | 源码检查通过（模式分发、文件清理正确），运行验证失败 |

### 自动化测试通过

```
$ go test -count=1 ./qa/issue110 -v
=== RUN   TestPreparePackageScaffoldExists
--- PASS: TestPreparePackageScaffoldExists (0.00s)
=== RUN   TestMainUsesPrepareRun
--- PASS: TestMainUsesPrepareRun (0.00s)
=== RUN   TestCommonInitNoLongerDependsOnLegacyJSONBootstrap
--- PASS: TestCommonInitNoLongerDependsOnLegacyJSONBootstrap (0.00s)
=== RUN   TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain
--- PASS: TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain (0.00s)
=== RUN   TestMazeEntryIsNoLongerPythonWrapper
--- PASS: TestMazeEntryIsNoLongerPythonWrapper (0.00s)
=== RUN   TestGdbScriptsStillUsePythonAPI
--- PASS: TestGdbScriptsStillUsePythonAPI (0.00s)
PASS
ok      maze/qa/issue110        0.003s
```

### 构建测试通过

```
$ ./maze --build
building .maze ...
$ ls -la .maze
-rwxr-xr-x 1 hcjn0770 hcjn0770 30196968 .maze
$ file .maze
.maze: ELF 64-bit LSB executable, x86-64, with debug_info, not stripped
```

### TC-007 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 2636550
Python version: 3.11.0 (iversion: 31100)
prepare.Run() failed: failed to connect to GDB: failed to start GDB server: GDB server failed to start within timeout

❌ Test ERROR: python/20260129-complex-types-311
   Maze analysis failed with exit code 1
```

### TC-008 失败输出

```
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 68446
readelf：错误：'/home/.../stack_multithread_test': No such file
readelf：错误：'/home/.../stack_multithread_test': No such file
prepare.Run() failed: failed to connect to GDB: failed to start GDB server: GDB server failed to start within timeout

❌ Test ERROR: cpp/20260304-stack-multithread
   Maze analysis failed with exit code 1
```

## 详细分析

### GDB Server 进程生命周期问题

`prepare/gdb_server.go` 中 GDB 启动命令：

```go
cmd := exec.Command(
    gdbBin,
    "-q",
    "-n",
    "-x", gdbCommandScript,       // .gdbcommand.py
    "-x", serverScript,            // gdb_socket_server.py
    "-ex", "start_gdb_server 9999",
)
```

GDB 在执行完 `start_gdb_server 9999` 命令后，由于没有后续命令需要执行，且没有交互式输入，GDB 进入"命令执行完毕"状态并退出。虽然 `gdb_socket_server.py` 中的 `start_gdb_server` 启动了 socket 监听线程，但 GDB 主线程退出后，整个进程（包括 daemon 线程）都终止了。

**QA 手动验证过程**：

1. 创建临时目录，创建 gdb 和 .gdbcommand.py 的 symlinks
2. 执行与 `gdb_server.go` 相同的 GDB 命令
3. GDB 日志显示 "GDB Socket Server started on 127.0.0.1:9998"
4. 但 `ps -p $GDB_PID` 显示进程已退出
5. `ss -tlnp | grep 9998` 确认端口未开放

这证实了 GDB 进程在输出启动消息后立即退出的结论。

### 与上次 QA 的进步对比

| 方面 | 上次 QA | 本次 QA |
|------|---------|---------|
| 构建 | ❌ flag 冲突 panic | ✅ 编译成功 |
| prepare.Run() | 骨架实现（核心步骤全部注释） | 完整实现（GDB Do/Ctypes/Fill 全部就绪） |
| GDB Client | 存在但未被调用 | 存在且被正确调用 |
| 数据填充 | 完全为空 | FillCommonVars 逻辑完整 |
| 核心阻塞 | 骨架实现 | GDB Server 进程管理 bug |

## 建议修复优先级

1. **P0 — GDB Server 进程保活**：GDB 执行 `start_gdb_server` 后需要保持运行。建议参考 `start_maze_gdb_server.sh` 或在 `-ex` 命令后追加一个让 GDB 保持活跃的方式（如 `-ex "while 1\nshell sleep 10\nend"` 或使用其他 GDB 事件循环机制）
2. **P1 — tar 模式下 readelf 路径修复**：`process.go` 中的 `setupLanguage()` 使用了 `p.Exe`（maps 中的原始路径），但在 tar 模式下文件已被 md5 重命名，需要使用解压后的实际路径
3. **P2 — Cpp.merge() 完整实现**：当前为简化实现，实际需要类型索引重映射逻辑

## 结论

当前实现**相比上次有重大进步**：从"骨架版本"升级为"功能逻辑完整版本"：
- ✅ prepare 包所有核心模块（19 个 Go 文件）已实现，替换了原 24 个 Python 文件
- ✅ `prepare.Run()` 主流程完整（12 个步骤全部连接）
- ✅ GDB Socket Client 实现完整
- ✅ 数据填充层 (FillCommonVars) 完整映射了所有 common.S 和 common.Cpp 字段
- ✅ 构建和入口脚本问题已修复
- ❌ 唯一核心阻塞：GDB Server 进程启动后立即退出，无法建立连接

**通过: 6 / 10 | 失败: 3 / 10 | 部分: 1 / 10**

无法发布。修复 GDB Server 进程管理 bug 后，即可解锁全部功能测试。这是当前唯一的阻塞性问题。


---
*Archived: [20260311182739-qa-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311182739-qa-report.md)*


---
**@maze-developer-agent[bot]** (2026-03-11T10:17:59+00:00):

### fix report

# 开发报告

## 完成的任务
- [x] Task 5.3: 修复 `prepare.ParseArgs()` 与 `main.go parseArgs()` 的职责边界，避免重复注册 flag 导致 `flag redefined: pid` panic
- [x] Task 5.3: 调整 `maze` 入口脚本对 `--build` 的处理，确保 `./maze --build` 只执行编译而不会继续把 `--build` 透传给 `.maze`
- [x] Task 5.3: 在计划文件中将已完成的 `Task 5.3` 标记为已完成

## 修改的文件
- `prepare/args.go`: 删除 prepare 层重复 flag 注册/解析逻辑，改为直接从 `common` 全局参数构造 `Args`
- `maze`: 新增 `--build` 特判，先构建 `.maze`，仅在存在其余参数时再继续执行二进制
- `.symphony_prompt.md`: 将 `Task 5.3` 勾选为完成

## 自测结果
- 测试命令: `./maze --build`
  - 测试结果: 通过，输出 `building .maze ...`，未再出现 `flag redefined: pid` panic
- 测试命令: `go test ./qa/issue110 -v`
  - 测试结果: 通过，6 个迁移守护测试全部 PASS
- 测试命令: `./maze --help`
  - 测试结果: 通过，二进制可正常启动并输出帮助信息，说明构建产物可执行

## Self-Review 发现与修复
- 发现 `prepare` 包仍试图自行解析命令行参数，这与 `main.go` 的统一参数解析设计冲突；已修复为仅消费 `common` 中已解析好的全局变量
- 发现 `maze` shell 入口此前只在缺少 `.maze` 时自动构建，`./maze --build` 实际会把 `--build` 继续传给 Go 二进制；已修复为在 shell 层消费 `--build`，保证该命令行为符合 AC2

## 注意事项
- 本次修复仅解决 QA 阻塞项：构建阶段的 flag 重复定义与 `--build` 入口行为问题
- Python/C++ 主流程回归、`--tar` 端到端行为仍需后续按测试用例继续验证
- 严格未修改 `testdata/` 下任何测试文件


---
*Archived: [20260311171637-dev-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260311-issue-110/20260311171637-dev-report.md)*


---


## Requirements Document

The requirements document is at `dev-flow/plan.md`. Read it to understand the original requirements when fixing issues.

## Steps

1. Read the latest QA feedback: `ls -t dev-flow/*-qa-report.md | head -1 | xargs cat`.
2. Read the requirements document: `cat dev-flow/plan.md`.
3. Review current diff: `git diff origin/master...HEAD` (use `origin/main` if the default branch is `main`)
4. Fix each issue identified by QA.
5. Run the test suite.
6. **You MUST commit your changes.** You are an autonomous agent and the user explicitly requires you to commit.
   Commit: `fix: address QA feedback for #110`
7. Write fix report to `dev-flow/$(date +%Y%m%d%H%M%S)-dev-report.md`:
   ```
   cat > "dev-flow/$(date +%Y%m%d%H%M%S)-dev-report.md" << 'REPORT'
   ## Summary
   [What was fixed]

   ## Changes
   - [file]: [what changed]

   ## Testing
   [How to verify, which tests pass]
   REPORT
   ```

## Constraints

- Fix ONLY the issues identified by QA.
- Keep changes minimal.
---

## CI/CD Agent 执行指令

你当前以自主 CI/CD Agent 身份运行。**提交代码是你任务的一部分，必须执行。**

### 必须执行的 Git 命令

任务完成后，立即执行：
```bash
git add .
git commit -m "<简洁描述本次修改>"
```

**不执行 commit 视为任务未完成。**

### 禁止操作
- 禁止 `git push` 或修改 remote
- 禁止提交工作文件：`dev-flow/plan.md`, `dev-flow/test-cases.md`, `dev-flow/*-dev-report.md`, `dev-flow/*-qa-report.md`

### 需要提交的内容
- 所有源代码修改
- `dev-log/` 下的正式文档（分析报告、设计文档等）

</details>

## Timeline

### `12:06:22` Step 1

#### `12:06:34` Tool: `todowrite` — 4 todos (completed)

```json
{
  "todos": [
    {
      "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
      "priority": "high",
      "status": "in_progress"
    },
    {
      "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
      "priority": "high",
      "status": "pending"
    },
    {
      "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
      "priority": "high",
      "status": "pending"
    },
    {
      "content": "进行 Self-Review，编写开发报告到 dev-log",
      "priority": "medium",
      "status": "pending"
    }
  ]
}
```

<details><summary>Output</summary>

```
[
  {
    "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
    "priority": "high",
    "status": "in_progress"
  },
  {
    "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
    "priority": "high",
    "status": "pending"
  },
  {
    "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
    "priority": "high",
    "status": "pending"
  },
  {
    "content": "进行 Self-Review，编写开发报告到 dev-log",
    "priority": "medium",
    "status": "pending"
  }
]
```
</details>

*Step finished: reason=tool-calls, tokens=43,756*

### `12:06:40` Step 2

#### `12:06:40` Tool: `grep` — file \+ctx\.Process\.Exe|file \+ ctx\.Process\.Exe|GenerateDebugScript|InitServerForCorefile|readelf -a|readelf -n|main_arena|mp_ (completed)

Pattern: `file \+ctx\.Process\.Exe|file \+ ctx\.Process\.Exe|GenerateDebugScript|InitServerForCorefile|readelf -a|readelf -n|main_arena|mp_`

<details><summary>Output</summary>

```
Found 99 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 160: 	names := []string{"main_arena", "mp_"}

/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go:
  Line 27: 	chunkMainArena := g.Execute("info address main_arena")
  Line 28: 	chunkSbrkBase := g.Execute("p mp_.sbrk_base")
  Line 115: 	// Parse main_arena
  Line 121: 			ctx.Symbols.Symbols["main_arena"] = mainArena
  Line 122: 			Debug.Printf("GDB.Do main_arena = 0x%x\n", mainArena)
  Line 512: // GenerateDebugScript 生成 gdb.sh 调试脚本
  Line 513: func (g *GDBManager) GenerateDebugScript(ctx *Context) {
  Line 514: 	Debug.Printf("* GDB.GenerateDebugScript\n")
  Line 597: // InitServerForCorefile 初始化 GDB Server 以分析 core 文件
  Line 598: func (g *GDBManager) InitServerForCorefile(ctx *Context) error {
  Line 602: 	Debug.Printf("* GDB.InitServerForCorefile\n")

/home/hcjn0770/temp/worktrees/issue-110/prepare/prepare.go:
  Line 42: 	ctx.GDB.GenerateDebugScript(ctx)
  Line 54: 	if err := ctx.GDB.InitServerForCorefile(ctx); err != nil {
  Line 109: 		ctx.GDB.InitServerForCorefile(ctx)

/home/hcjn0770/temp/worktrees/issue-110/txos/txos-obj.go:
  Line 215: 					tmp_str := Cstr(iValue)
  Line 216: 					s1 = BuddySize(uint64(len(tmp_str)))

/home/hcjn0770/temp/worktrees/issue-110/txos/g18.go:
  Line 443: 	//     Tx2_Obj_t *temp_props;
  Line 478: 				if d, ok := M["temp_props"]; ok {

/home/hcjn0770/temp/worktrees/issue-110/python/mobile_server2_classify.go:
  Line 56: 	tmp_size := MemoryPieceSize(c_obj)
  Line 58: 	if tmp_size > 0 {
  Line 59: 		Debug.Printf("lmy==MemoryPieceSize ServerActor %s c_obj %x size %d class %d sizeof %d\n", Cstr(tp_name), c_obj, tmp_size, class, sizeof)
  Line 110: 	tmp_size := MemoryPieceSize(c_obj)
  Line 112: 	if tmp_size > 0 {
  Line 113: 		Debug.Printf("lmy==MemoryPieceSize ClientActor %s c_obj %x size %d class %d sizeof %d\n", Cstr(tp_name), c_obj, tmp_size, class, sizeof)
  Line 166: 	tmp_size := MemoryPieceSize(c_obj)
  Line 168: 	if tmp_size > 0 {
  Line 169: 	
... (truncated, 7254 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=61,801*

### `12:06:46` Step 3

#### `12:06:46` Tool: `read` — prepare/gdb.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go</path>
<type>file</type>
<content>500: 		sizeof["PyGC_Head"] = 24
501: 	}
502: 
503: 	g.Sizeof = sizeof
504: 	g.Structs = make(map[string]CStruct)
505: 	for k, v := range structs {
506: 		g.Structs[k] = CStruct(v)
507: 	}
508: 
509: 	return nil
510: }
511: 
512: // GenerateDebugScript 生成 gdb.sh 调试脚本
513: func (g *GDBManager) GenerateDebugScript(ctx *Context) {
514: 	Debug.Printf("* GDB.GenerateDebugScript\n")
515: 	exePath := ctx.Process.Exe
516: 	if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {
517: 		exePath = resolved
518: 	}
519: 	code := fmt.Sprintf(`%s -x .gdbcommand.py -ex "set auto-load safe-path ./xy" -ex "set solib-search-path ./xy" -ex "set solib-absolute-prefix ./xy" -ex "set libthread-db-search-path ./xy" -ex "set debug-file-directory %s" -ex "file %s" -ex "core-file %s" -ex "set print static-members off"
520: `, g.GDBBinPath, GDB_DEBUG_FILE_DIRECTORY, exePath, ctx.Corefile.Path)
521: 
522: 	os.WriteFile("gdb.sh", []byte(code), 0644)
523: }
524: 
525: // Execute 通过 GDB Socket Client 执行命令
526: func (g *GDBManager) Execute(command string) string {
527: 	if g.ServerClient == nil || !g.ServerClient.IsConnected() {
528: 		if err := g.ensureConnected(); err != nil {
529: 			Debug.Printf("GDB.Execute: cannot connect: %v\n", err)
530: 			return ""
531: 		}
532: 	}
533: 
534: 	resp, err := g.ServerClient.Execute(command)
535: 	if err != nil {
536: 		Debug.Printf("GDB.Execute error for '%s': %v\n", command, err)
537: 		return ""
538: 	}
539: 
540: 	if resp == nil {
541: 		return ""
542: 	}
543: 
544: 	if !resp.Success {
545: 		Debug.Printf("GDB.Execute error for '%s': %s\n", command, resp.Error)
546: 		return resp.Output
547: 	}
548: 
549: 	return resp.Output
550: }
551: 
552: // ensureConnected 确保连接到 GDB Server
553: func (g *GDBManager) ensureConnected() error {
554: 	if g.ServerClient != nil && g.ServerClient.IsConnected() {
555: 		return nil
556: 	}
557: 
558: 	host := g.ServerH
... (truncated, 5538 chars total)
```
</details>

#### `12:06:46` Tool: `read` — prepare/process.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go</path>
<type>file</type>
<content>1: package prepare
2: 
3: import (
4: 	"bufio"
5: 	"encoding/json"
6: 	"fmt"
7: 	"os"
8: 	"path/filepath"
9: 	"strconv"
10: 	"strings"
11: 
12: 	. "maze/common"
13: )
14: 
15: // Init 初始化进程信息
16: func (p *ProcessManager) Init(pid string) error {
17: 	pidInt, err := strconv.Atoi(pid)
18: 	if err != nil {
19: 		return fmt.Errorf("invalid pid: %w", err)
20: 	}
21: 	p.Pid = pidInt
22: 
23: 	if err := p.cacheProcMaps(pid); err != nil {
24: 		return err
25: 	}
26: 
27: 	p.initExe()
28: 	p.setupLanguage()
29: 	p.doMapsMisc()
30: 
31: 	return nil
32: }
33: 
34: // cacheProcMaps 解析 maps 文件
35: func (p *ProcessManager) cacheProcMaps(pid string) error {
36: 	mapsPath := ""
37: 
38: 	// 查找 maps 文件
39: 	dirpath := p.ctx.DirPath
40: 	candidates := []string{
41: 		filepath.Join(dirpath, pid+".maps"),
42: 		"maps",
43: 		fmt.Sprintf("/proc/%s/maps", pid),
44: 	}
45: 
46: 	// 也查找 maps.gdb 文件
47: 	gdbPattern := fmt.Sprintf("./postman-db/*-%s.maps.gdb", pid)
48: 	if files, _ := filepath.Glob(gdbPattern); len(files) > 0 {
49: 		candidates = append([]string{files[0]}, candidates...)
50: 	}
51: 
52: 	for _, c := range candidates {
53: 		if _, err := os.Stat(c); err == nil {
54: 			mapsPath = c
55: 			break
56: 		}
57: 	}
58: 
59: 	if mapsPath == "" {
60: 		return fmt.Errorf("maps file not found for pid %s", pid)
61: 	}
62: 
63: 	data, err := os.ReadFile(mapsPath)
64: 	if err != nil {
65: 		return fmt.Errorf("failed to read maps from %s: %w", mapsPath, err)
66: 	}
67: 
68: 	p.ProcMapsOutput = nil
69: 	p.ProcMaps = nil
70: 	p.Maps = nil
71: 
72: 	minPtr := uint64(MAX_POINTER)
73: 	maxPtr := uint64(0)
74: 
75: 	procMapsMap := make(map[string][][]string) // filepath -> []fields
76: 
77: 	scanner := bufio.NewScanner(strings.NewReader(string(data)))
78: 	for scanner.Scan() {
79: 		line := strings.TrimSpace(scanner.Text())
80: 		if line == "" {
81: 			continue
82: 		}
83: 
84: 		// 忽略 vsyscall
85:
... (truncated, 6578 chars total)
```
</details>

#### `12:06:46` Tool: `read` — prepare/alloc.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go</path>
<type>file</type>
<content>130: 	case "nodejs":
131: 		return getNodejsTypes()
132: 	case "txos":
133: 		return getTxosTypes()
134: 	default:
135: 		return nil
136: 	}
137: }
138: 
139: // --- Libc ---
140: 
141: // Address 定位 libc 符号
142: func (l *LibcManager) Address(ctx *Context) {
143: 	if l.done {
144: 		return
145: 	}
146: 	if ctx.Process.Mallocer != "ptmalloc" {
147: 		Debug.Printf("skip libc address, mallocer=%s\n", ctx.Process.Mallocer)
148: 		return
149: 	}
150: 	if l.AddressInfo == nil {
151: 		return
152: 	}
153: 
154: 	realpath := l.AddressInfo[0]
155: 	sectionAddress := l.AddressInfo[1]
156: 	fileOffset := l.AddressInfo[2]
157: 
158: 	ctx.Process.Libc = realpath
159: 
160: 	names := []string{"main_arena", "mp_"}
161: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
162: 	locateNames(ctx, realpath, sectionAddress, names, sectionOffset)
163: 
164: 	l.done = true
165: }
166: 
167: // --- Tcmalloc ---
168: 
169: // Address 定位 tcmalloc 符号
170: func (t *TcmallocManager) Address(ctx *Context, realpath, sectionAddress, fileOffset string) {
171: 	if t.done {
172: 		return
173: 	}
174: 
175: 	if !t.findVersion(realpath) {
176: 		return
177: 	}
178: 
179: 	Debug.Printf("* Tcmalloc.address\n")
180: 	t.Enabled = true
181: 	ctx.Process.TcmallocPath = realpath
182: 	ctx.Process.Mallocer = "tcmalloc"
183: 
184: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
185: 
186: 	// 定位符号
187: 	addr, _ := LocateByNm("tcmalloc::ThreadCache::thread_heaps_", realpath, sectionAddress, sectionOffset, "-DC")
188: 	if addr == 0 {
189: 		addr, _ = LocateByNm("tcmalloc::ThreadCache::thread_heaps_", realpath, sectionAddress, sectionOffset, "-C")
190: 	}
191: 	t.ThreadHeaps = addr
192: 
193: 	addr, _ = LocateByNm("tcmalloc::Static::central_cache_", realpath, sectionAddress, sectionOffset, "-DC")
194: 	if addr == 0 {
195: 		addr, _ = LocateByNm("tcmalloc::Static::central_cache_", realpath, sec
... (truncated, 3428 chars total)
```
</details>

#### `12:06:46` Tool: `read` — .symphony_prompt.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/.symphony_prompt.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/.symphony_prompt.md</path>
<type>file</type>
<content>235: ## 任务拆分
236: 
237: ### Phase 0: 基础设施（优先级最高）
238: 
239: - [ ] Task 0.1: 创建 `prepare/` 包目录和基础文件
240:   - 涉及文件: `prepare/context.go`, `prepare/args.go`, `prepare/cmd.go`, `prepare/utils.go`
241:   - 关键实现:
242:     - 定义 `Context` 结构体及所有子结构体
243:     - 实现 `Args` 参数解析（合并 Python `maze` 脚本和 `.maze.py` 的参数定义）
244:     - 封装 `RunCommand`/`CheckOutput` 等外部命令执行函数
245:     - 迁移工具函数：`hexi`（十六进制转整数）、`soName`（提取 .so 名称）、`isElfFile`、`getBuildId`（调用 readelf -n）、`md5sum`
246:   - 技术栈: Go
247: 
248: - [ ] Task 0.2: 实现 GDB Socket Client
249:   - 涉及文件: `prepare/gdb_client.go`
250:   - 关键实现:
251:     - TCP 连接管理（connect/disconnect/reconnect）
252:     - 长度前缀帧协议：4 字节大端序长度 + UTF-8 JSON 数据
253:     - 三种请求类型：`command`、`eval`、`ping`
254:     - 精确字节读取 `recvExact(n)`
255:     - 连接超时 5 秒、请求超时 120 秒
256:     - 错误处理：连接关闭检测、超时重试
257:   - 技术栈: Go (net, encoding/binary, encoding/json)
258:   - 验证: 用现有的 `start_server.gdb` 启动 GDB Server，Go 客户端连接并执行 `info files` 命令
259: 
260: - [ ] Task 0.3: 实现 GDB Server 进程管理
261:   - 涉及文件: `prepare/gdb_server.go`
262:   - 关键实现:
263:     - 启动 GDB 子进程：`<gdb_bin> -q -n -x .gdbcommand.py -x gdb/gdb-server/gdb_socket_server.py -ex "start_gdb_server 9999"`
264:     - GDB 二进制路径解析优先级：环境变量 `MAZE_GDB_BIN` > `./prim-gdb/gdb-portable` > 自动 submodule 构建
265:     - 启动等待：每 0.2 秒轮询 TCP 端口，最多等待 5 秒
266:     - 进程生命周期管理：启动、健康检查、优雅停止（terminate）
267:     - Corefile 初始化幂等性：缓存 `(exe_path, core_path)` 避免重复加载
268:   - 技术栈: Go (os/exec, net, time)
269: 
270: - [ ] Task 0.4: 实现键值数据库
271:   - 涉及文件: `prepare/db.go`
272:   - 关键实现:
273:     - `DBC` 结构体：基于文件系统 + S3 的 JSON 键值存储
274:     - `Set(key, value)` — 保存到本地文件并上传 S3（`./bin/s3 upload`）
275:     - `Get(key, version)` — 本地查找或从 S3 下载（`./bin/s3 download`）
276:     - 两个全局实例：`DB`（maze-db/）、`CDB`（cpp-db/）
277:   - 技术栈: Go (os, encoding/json, os/exec)
278: 
279: ### Phase 1: 核心模块
280: 
281: - [ ] Task 1.1: 实现 Corefile 管理
282:   - 涉及文件: `prepare/corefile.
... (truncated, 5537 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=64,447*

### `12:06:52` Step 4

#### `12:06:55` Tool: `grep` — CheckOutput\("readelf"|RunCommand\("readelf"|readelf (completed)

Pattern: `CheckOutput\("readelf"|RunCommand\("readelf"|readelf`

<details><summary>Output</summary>

```
Found 8 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/cmd.go:
  Line 42: 	// 将 stderr 重定向到 /dev/null，避免 readelf 等工具的警告信息输出到控制台

/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 539: 	output, err := CheckOutput("readelf", "-s", path)
  Line 541: 		output, _ = CheckOutput("readelf", "-s", filepath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 83: 	output, err := CheckOutput("readelf", "-S", realpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 325: 	output, err := CheckOutput("readelf", "-a", filepath)
  Line 365: 	output, err := CheckOutput("readelf", "-s", exePathForReadelf)

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 154: 	output, err := CheckOutput("readelf", "-s", fpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/utils.go:
  Line 72: 	output, err := CheckOutput("readelf", "-n", path)
```
</details>

*Step finished: reason=tool-calls, tokens=75,230*

### `12:07:01` Step 5

#### `12:07:01` Tool: `read` — prepare/cmd.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/cmd.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/cmd.go</path>
<type>file</type>
<content>1: package prepare
2: 
3: import (
4: 	"fmt"
5: 	"os"
6: 	"os/exec"
7: 	"strings"
8: )
9: 
10: func devNullFile() (*os.File, error) {
11: 	return os.Open(os.DevNull)
12: }
13: 
14: // RunCommand 执行外部命令并返回输出
15: func RunCommand(name string, args ...string) (string, error) {
16: 	cmd := exec.Command(name, args...)
17: 	cmd.Stderr = os.Stderr
18: 	output, err := cmd.Output()
19: 	if err != nil {
20: 		return "", fmt.Errorf("command failed: %s %v: %w", name, args, err)
21: 	}
22: 	return string(output), nil
23: }
24: 
25: // RunCommandSilent 静默执行外部命令（不输出到控制台）
26: func RunCommandSilent(name string, args ...string) error {
27: 	nullFile, err := os.Open(os.DevNull)
28: 	if err != nil {
29: 		return err
30: 	}
31: 	defer nullFile.Close()
32: 
33: 	cmd := exec.Command(name, args...)
34: 	cmd.Stdout = nullFile
35: 	cmd.Stderr = nullFile
36: 	return cmd.Run()
37: }
38: 
39: // CheckOutput 执行命令并返回输出（兼容 Python check_output）
40: func CheckOutput(name string, args ...string) (string, error) {
41: 	cmd := exec.Command(name, args...)
42: 	// 将 stderr 重定向到 /dev/null，避免 readelf 等工具的警告信息输出到控制台
43: 	nullFile, err := devNullFile()
44: 	if err != nil {
45: 		return "", err
46: 	}
47: 	defer nullFile.Close()
48: 	cmd.Stderr = nullFile
49: 	output, err := cmd.Output()
50: 	if err != nil {
51: 		return "", err
52: 	}
53: 	// Go 的 Output() 返回 []byte，需要转换为 string
54: 	return strings.TrimSpace(string(output)), nil
55: }
56: 
57: // SafeRun 安全执行命令，出错时返回空字符串
58: func SafeRun(command string) string {
59: 	parts := strings.Fields(command)
60: 	if len(parts) == 0 {
61: 		return ""
62: 	}
63: 
64: 	cmd := exec.Command(parts[0], parts[1:]...)
65: 	cmd.Stderr = os.Stderr
66: 	output, err := cmd.Output()
67: 	if err != nil {
68: 		return ""
69: 	}
70: 	return string(output)
71: }
72: 
73: // RunQuiet 静默运行命令
74: func RunQuiet(command string) error {
75: 	nullFile, err := os.Open(os.DevNull)
76: 	if err != nil {
... (truncated, 2828 chars total)
```
</details>

#### `12:07:01` Tool: `read` — prepare/process.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go</path>
<type>file</type>
<content>300: 		return mapped
301: 	}
302: 	return path
303: }
304: 
305: // originpath 返回原始路径
306: func (p *ProcessManager) originpath(path string) string {
307: 	for k, v := range p.FileMaps {
308: 		if path == v {
309: 			return k
310: 		}
311: 	}
312: 	return path
313: }
314: 
315: // xyToRealPath 转换 xy 路径到实际路径
316: func (p *ProcessManager) xyToRealPath(path string) string {
317: 	if mapped, ok := p.XyFileMaps[path]; ok {
318: 		return mapped
319: 	}
320: 	return path
321: }
322: 
323: // LoadOffset 获取 LOAD 段偏移量（对应 Python gotplt_offset/load_offset）
324: func (p *ProcessManager) LoadOffset(filepath string, realFileOffset string) uint64 {
325: 	output, err := CheckOutput("readelf", "-a", filepath)
326: 	if err != nil {
327: 		return 0
328: 	}
329: 
330: 	for _, line := range strings.Split(output, "\n") {
331: 		if strings.Contains(line, "LOAD") {
332: 			ps := strings.Fields(strings.TrimSpace(line))
333: 			if len(ps) >= 3 {
334: 				return Hexi(ps[2])
335: 			}
336: 		}
337: 	}
338: 	return 0
339: }
340: 
341: // setupLanguage 检测进程使用的编程语言
342: func (p *ProcessManager) setupLanguage() {
343: 	Debug.Printf("* Process.setupLanguage\n")
344: 
345: 	// 在 tar 模式下，原始 exe 路径（来自 maps）可能不存在，
346: 	// 需要使用 Realpath 获取解压后的实际文件路径
347: 	exePathForReadelf := p.Realpath(p.Exe)
348: 	if _, err := os.Stat(exePathForReadelf); err != nil {
349: 		// 如果 realpath 也不存在，尝试直接使用当前目录下的文件
350: 		// 这在 tar 模式下可能发生，因为文件被 md5 重命名
351: 		Debug.Printf("  exe path not found: %s, trying to find alternative\n", exePathForReadelf)
352: 		// 遍历 ProcMaps 查找可用的 exe 文件
353: 		for _, entry := range p.ProcMaps {
354: 			if entry.File == p.Exe || entry.File == p.OriginExe {
355: 				rp := p.Realpath(entry.File)
356: 				if _, err := os.Stat(rp); err == nil {
357: 					exePathForReadelf = rp
358: 					break
359: 				}
360: 			}
361: 		}
362: 	}
363: 
364: 	// 优先通过 exe 来寻找语言
365: 	output, err := CheckOutput("readelf", "
... (truncated, 4275 chars total)
```
</details>

#### `12:07:01` Tool: `read` — prepare/utils.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/utils.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/utils.go</path>
<type>file</type>
<content>1: package prepare
2: 
3: import (
4: 	"fmt"
5: 	"os"
6: 	"path/filepath"
7: 	"strconv"
8: 	"strings"
9: )
10: 
11: // Hexi 将十六进制字符串转换为整数（兼容 Python 的 hexi）
12: func Hexi(s string) uint64 {
13: 	s = strings.TrimSpace(s)
14: 	if strings.HasPrefix(s, "0x") {
15: 		v, _ := strconv.ParseUint(s[2:], 16, 64)
16: 		return v
17: 	}
18: 	v, _ := strconv.ParseUint(s, 16, 64)
19: 	return v
20: }
21: 
22: // SoName 提取 .so 文件的基础名称
23: func SoName(path string) string {
24: 	base := filepath.Base(path)
25: 	// 去掉扩展名
26: 	parts := strings.Split(base, ".")
27: 	if len(parts) > 0 {
28: 		base = parts[0]
29: 	}
30: 	// 去掉版本号
31: 	parts = strings.Split(base, "-")
32: 	if len(parts) > 0 {
33: 		base = parts[0]
34: 	}
35: 	return base
36: }
37: 
38: // IsElfFile 通过文件路径简单判断是否是 ELF 文件
39: func IsElfFile(path string) bool {
40: 	// 排除已知非 ELF 文件
41: 	if path == "/usr/lib/locale/locale-archive" {
42: 		return false
43: 	}
44: 	if strings.HasPrefix(path, "/dev/shm/mongoc") {
45: 		return false
46: 	}
47: 	if strings.HasSuffix(path, ".bin") {
48: 		return false
49: 	}
50: 	if strings.HasSuffix(path, "gconv-modules.cache") {
51: 		return false
52: 	}
53: 	if strings.HasSuffix(path, ".sharedtable") {
54: 		return false
55: 	}
56: 	if strings.HasSuffix(path, ".fast") {
57: 		return false
58: 	}
59: 	if strings.Contains(path, "setting/map") {
60: 		if strings.HasSuffix(path, ".tile") {
61: 			return false
62: 		}
63: 		if strings.HasSuffix(path, ".col_linux64") {
64: 			return false
65: 		}
66: 	}
67: 	return true
68: }
69: 
70: // GetBuildId 获取 ELF 文件的 Build ID
71: func GetBuildId(path string) (string, error) {
72: 	output, err := CheckOutput("readelf", "-n", path)
73: 	if err != nil {
74: 		return "", err
75: 	}
76: 
77: 	lines := strings.Split(output, "\n")
78: 	for _, line := range lines {
79: 		if strings.Contains(line, "Build ID") {
80: 			parts := strings.Fields(line)
81: 			if len(parts) > 0 {
82
... (truncated, 3054 chars total)
```
</details>

#### `12:07:01` Tool: `read` — prepare/symbols.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go</path>
<type>file</type>
<content>130: 
131: 	// 移动到正确位置
132: 	if _, err := os.Stat(key); err == nil {
133: 		os.MkdirAll(fmt.Sprintf("debug/.build-id/%s", a), 0755)
134: 		os.Rename(key, localName)
135: 	}
136: }
137: 
138: // DownloadSymbol 从 S3 下载符号文件
139: func DownloadSymbol(buildId, path string) error {
140: 	downloadSymbol(buildId, path)
141: 	return nil
142: }
143: 
144: // DebugSymbolCandidates 生成调试符号文件的候选路径
145: func DebugSymbolCandidates(fpath string) []string {
146: 	_, candidates := debugSymbolCandidates(fpath)
147: 	return candidates
148: }
149: 
150: // LocateNames 批量定位符号地址（保持向后兼容）
151: func LocateNames(fpath string, sectionAddr uint64, names []string, sectionOffset uint64) map[string]uint64 {
152: 	results := make(map[string]uint64)
153: 
154: 	output, err := CheckOutput("readelf", "-s", fpath)
155: 	if err != nil {
156: 		return results
157: 	}
158: 
159: 	for _, line := range strings.Split(output, "\n") {
160: 		fields := strings.Fields(line)
161: 		if len(fields) < 8 {
162: 			continue
163: 		}
164: 
165: 		for _, name := range names {
166: 			if strings.Contains(line, name) {
167: 				offset := Hexi(fields[1])
168: 				address := sectionAddr + offset - sectionOffset
169: 				results[name] = address
170: 			}
171: 		}
172: 	}
173: 
174: 	return results
175: }
176: 
177: // LocateByNmLegacy 使用 nm 定位符号（保持向后兼容的旧接口）
178: func LocateByNmLegacy(name, filename string, sectionAddr, sectionOffset uint64, flags string) (uint64, error) {
179: 	args := []string{flags, filename}
180: 	output, err := CheckOutput("nm", args...)
181: 	if err != nil {
182: 		return 0, err
183: 	}
184: 
185: 	for _, line := range strings.Split(output, "\n") {
186: 		if strings.Contains(line, name) {
187: 			fields := strings.Fields(line)
188: 			if len(fields) >= 3 {
189: 				offset := Hexi(fields[0])
190: 				return sectionAddr + offset - sectionOffset, nil
191: 			}
192: 		}
193: 	}
194: 
195: 	return 0, fmt.Errorf("
... (truncated, 2305 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=83,901*

### `12:07:04` Step 6

#### `12:07:08` Tool: `grep` — func \(p \*ProcessManager\) Realpath|Realpath\( (completed)

Pattern: `func \(p \*ProcessManager\) Realpath|Realpath\(`

<details><summary>Output</summary>

```
Found 13 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go:
  Line 167: 	exeBuildId, _ := GetBuildId(ctx.Process.Realpath(ctx.Process.Exe))
  Line 516: 	if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {
  Line 631: 		if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 39: 		realpath := ctx.Process.Realpath(filename)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 292: 			p.ProcMaps[i].RealFile = p.Realpath(file)
  Line 298: func (p *ProcessManager) Realpath(path string) string {
  Line 347: 	exePathForReadelf := p.Realpath(p.Exe)
  Line 355: 				rp := p.Realpath(entry.File)
  Line 370: 				realpath := p.Realpath(p.Exe)
  Line 402: 					realpath := p.Realpath(path)
  Line 505: 		realpath := p.Realpath(filename)

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 15: 	realpath := ctx.Process.Realpath(ctx.Process.Exe)
  Line 39: 			realpath := ctx.Process.Realpath(fp)
```
</details>

#### `12:07:08` Tool: `grep` — Task 0\.1|Task 1\.2|Task 1\.4|Task 5\.2|Task 5\.3|Task 6\.1 (completed)

Pattern: `Task 0\.1|Task 1\.2|Task 1\.4|Task 5\.2|Task 5\.3|Task 6\.1`

```
No files found
```

#### `12:07:08` Tool: `grep` — ctx\.Process\.Exe|Process\.Exe|OriginExe (completed)

Pattern: `ctx\.Process\.Exe|Process\.Exe|OriginExe`

<details><summary>Output</summary>

```
Found 20 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/context.go:
  Line 108: 	OriginExe      string

/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 362: 		if filepath_ == ctx.Process.OriginExe {

/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go:
  Line 167: 	exeBuildId, _ := GetBuildId(ctx.Process.Realpath(ctx.Process.Exe))
  Line 499: 	if strings.Contains(ctx.Process.Exe, "mosd") {
  Line 515: 	exePath := ctx.Process.Exe
  Line 516: 	if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {
  Line 629: 	if ctx.Process.Exe != "" {
  Line 630: 		exePath := ctx.Process.Exe
  Line 631: 		if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 211: 			p.OriginExe = path
  Line 233: 			p.OriginExe = path
  Line 354: 			if entry.File == p.Exe || entry.File == p.OriginExe {
  Line 420: 	if strings.HasSuffix(p.OriginExe, "txos") || strings.HasSuffix(p.OriginExe, "driver") {
  Line 424: 	if strings.HasSuffix(p.OriginExe, "node") {
  Line 529: 		if filename == p.OriginExe {

/home/hcjn0770/temp/worktrees/issue-110/prepare/fill.go:
  Line 14: 		S.Process.Exe = ctx.Process.Exe
  Line 15: 		S.Process.RealExe = ctx.Process.OriginExe

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 15: 	realpath := ctx.Process.Realpath(ctx.Process.Exe)
  Line 18: 		downloadSymbol(bid, ctx.Process.Exe)

/home/hcjn0770/temp/worktrees/issue-110/prepare/lang_python.go:
  Line 96: 	if strings.Contains(ctx.Process.OriginExe, "python3.11") {
```
</details>

*Step finished: reason=tool-calls, tokens=89,302*

### `12:07:11` Step 7

#### `12:07:13` Tool: `read` — prepare/process.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go</path>
<type>file</type>
<content>284: 	// 简化实现 - postman 模式的文件映射
285: }
286: 
287: // setMapsRealfile 设置 maps 的 realFile 字段
288: func (p *ProcessManager) setMapsRealfile() {
289: 	for i := range p.ProcMaps {
290: 		file := p.ProcMaps[i].File
291: 		if file != "" {
292: 			p.ProcMaps[i].RealFile = p.Realpath(file)
293: 		}
294: 	}
295: }
296: 
297: // Realpath 返回文件的实际路径
298: func (p *ProcessManager) Realpath(path string) string {
299: 	if mapped, ok := p.FileMaps[path]; ok {
300: 		return mapped
301: 	}
302: 	return path
303: }
304: 
305: // originpath 返回原始路径
306: func (p *ProcessManager) originpath(path string) string {
307: 	for k, v := range p.FileMaps {
308: 		if path == v {
309: 			return k
310: 		}
311: 	}
312: 	return path
313: }
314: 
315: // xyToRealPath 转换 xy 路径到实际路径
316: func (p *ProcessManager) xyToRealPath(path string) string {
317: 	if mapped, ok := p.XyFileMaps[path]; ok {
318: 		return mapped
319: 	}
320: 	return path
321: }
322: 
323: // LoadOffset 获取 LOAD 段偏移量（对应 Python gotplt_offset/load_offset）
324: func (p *ProcessManager) LoadOffset(filepath string, realFileOffset string) uint64 {
325: 	output, err := CheckOutput("readelf", "-a", filepath)
326: 	if err != nil {
327: 		return 0
328: 	}
329: 
330: 	for _, line := range strings.Split(output, "\n") {
331: 		if strings.Contains(line, "LOAD") {
332: 			ps := strings.Fields(strings.TrimSpace(line))
333: 			if len(ps) >= 3 {
334: 				return Hexi(ps[2])
335: 			}
336: 		}
337: 	}
338: 	return 0
339: }
340: 
341: // setupLanguage 检测进程使用的编程语言
342: func (p *ProcessManager) setupLanguage() {
343: 	Debug.Printf("* Process.setupLanguage\n")
344: 
345: 	// 在 tar 模式下，原始 exe 路径（来自 maps）可能不存在，
346: 	// 需要使用 Realpath 获取解压后的实际文件路径
347: 	exePathForReadelf := p.Realpath(p.Exe)
348: 	if _, err := os.Stat(exePathForReadelf); err != nil {
349: 		// 如果 realpath 也不存在，尝试直接使用当前目录下的文件
350: 		// 这在 tar 模式下可能发生，因为文件被 md5 重命名
351: 		Debug.Printf("  exe path not fo
... (truncated, 2448 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=94,542*

### `12:07:17` Step 8

#### `12:07:20` Tool: `read` — prepare/process.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go</path>
<type>file</type>
<content>244: // loadMd5FileMap 从 {pid}.md5 文件加载 md5 映射（tar 模式的文件重命名）
245: func (p *ProcessManager) loadMd5FileMap() {
246: 	md5Path := fmt.Sprintf("%s/%d.md5", p.ctx.DirPath, p.Pid)
247: 	data, err := os.ReadFile(md5Path)
248: 	if err != nil {
249: 		// 也尝试不带目录前缀
250: 		md5Path = fmt.Sprintf("%d.md5", p.Pid)
251: 		data, err = os.ReadFile(md5Path)
252: 		if err != nil {
253: 			return
254: 		}
255: 	}
256: 
257: 	// 解析 JSON: {"原始路径": "md5值", ...}
258: 	var md5Map map[string]string
259: 	if err := json.Unmarshal(data, &md5Map); err != nil {
260: 		Debug.Printf("loadMd5FileMap: failed to parse: %v\n", err)
261: 		return
262: 	}
263: 
264: 	// 建立原始路径 → 本地 md5 文件名的映射
265: 	for origPath, md5val := range md5Map {
266: 		localPath := md5val
267: 		// 检查本地文件是否存在
268: 		if _, err := os.Stat(localPath); err == nil {
269: 			p.FileMaps[origPath] = localPath
270: 			Debug.Printf("loadMd5FileMap: %s → %s\n", origPath, localPath)
271: 		} else {
272: 			// 也检查 dirpath 下是否有
273: 			dirPath := filepath.Join(p.ctx.DirPath, md5val)
274: 			if _, err := os.Stat(dirPath); err == nil {
275: 				p.FileMaps[origPath] = dirPath
276: 				Debug.Printf("loadMd5FileMap: %s → %s\n", origPath, dirPath)
277: 			}
278: 		}
279: 	}
280: }
281: 
282: // setupFileMap 设置文件映射（postman 模式）
283: func (p *ProcessManager) setupFileMap() {
284: 	// 简化实现 - postman 模式的文件映射
285: }
286: 
287: // setMapsRealfile 设置 maps 的 realFile 字段
288: func (p *ProcessManager) setMapsRealfile() {
289: 	for i := range p.ProcMaps {
290: 		file := p.ProcMaps[i].File
291: 		if file != "" {
292: 			p.ProcMaps[i].RealFile = p.Realpath(file)
293: 		}
294: 	}
295: }
296: 
297: // Realpath 返回文件的实际路径
298: func (p *ProcessManager) Realpath(path string) string {
299: 	if mapped, ok := p.FileMaps[path]; ok {
300: 		return mapped
301: 	}
302: 	return path
303: }
304: 
305: // originpath 返回原始路径
306: func (p *ProcessManager) originpath(path string) stri
... (truncated, 3759 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=96,570*

### `12:07:26` Step 9

#### `12:07:33` Tool: `grep` — FileMaps:|make\(map\[string\]string\)|XyFileMaps (completed)

Pattern: `FileMaps:|make\(map\[string\]string\)|XyFileMaps`

<details><summary>Output</summary>

```
Found 10 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/context.go:
  Line 115: 	XyFileMaps     map[string]string
  Line 339: 			FileMaps:   make(map[string]string),
  Line 340: 			XyFileMaps: make(map[string]string),

/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 547: 	symbols := make(map[string]string) // name -> offset

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 151: 	toJsonTypename := make(map[string]string)        // index → type_name
  Line 157: 	jsonTypename := make(map[string]string) // type_name → index (reverse map)
  Line 173: 		mapping := make(map[string]string)
  Line 191: 		newTypename := make(map[string]string)
  Line 366: 	result := make(map[string]string)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 317: 	if mapped, ok := p.XyFileMaps[path]; ok {
```
</details>

*Step finished: reason=tool-calls, tokens=99,366*

### `12:07:38` Step 10

#### `12:07:43` Tool: `grep` — LoadOffset\( (completed)

Pattern: `LoadOffset\(`

<details><summary>Output</summary>

```
Found 11 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 161: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 184: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 276: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 432: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 445: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 459: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 471: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 499: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 324: func (p *ProcessManager) LoadOffset(filepath string, realFileOffset string) uint64 {

/home/hcjn0770/temp/worktrees/issue-110/prepare/lang_lua.go:
  Line 30: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)

/home/hcjn0770/temp/worktrees/issue-110/prepare/lang_python.go:
  Line 206: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
```
</details>

*Step finished: reason=tool-calls, tokens=100,531*

### `12:07:49` Step 11

#### `12:07:58` Tool: `bash` — 查看关键文件当前差异 (completed)

```bash
git diff -- prepare/gdb.go prepare/process.go prepare/alloc.go
```

<details><summary>Output</summary>

```
diff --git a/prepare/alloc.go b/prepare/alloc.go
index c06b71b0..54080c76 100644
--- a/prepare/alloc.go
+++ b/prepare/alloc.go
@@ -161,18 +161,6 @@ func (l *LibcManager) Address(ctx *Context) {
 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
 	locateNames(ctx, realpath, sectionAddress, names, sectionOffset)
 
-	// 回退到 GDB 获取的地址
-	if _, ok := ctx.Symbols.Symbols["main_arena"]; !ok {
-		if addr, ok2 := ctx.Symbols.Symbols["main_arena"]; ok2 {
-			ctx.Symbols.Symbols["main_arena"] = addr
-		}
-	}
-	if _, ok := ctx.Symbols.Symbols["mp_"]; !ok {
-		if addr, ok2 := ctx.Symbols.Symbols["mp_"]; ok2 {
-			ctx.Symbols.Symbols["mp_"] = addr
-		}
-	}
-
 	l.done = true
 }
 
diff --git a/prepare/gdb.go b/prepare/gdb.go
index faa48a45..b7f09fab 100644
--- a/prepare/gdb.go
+++ b/prepare/gdb.go
@@ -512,8 +512,12 @@ func (g *GDBManager) Ctypes(ctx *Context) error {
 // GenerateDebugScript 生成 gdb.sh 调试脚本
 func (g *GDBManager) GenerateDebugScript(ctx *Context) {
 	Debug.Printf("* GDB.GenerateDebugScript\n")
+	exePath := ctx.Process.Exe
+	if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {
+		exePath = resolved
+	}
 	code := fmt.Sprintf(`%s -x .gdbcommand.py -ex "set auto-load safe-path ./xy" -ex "set solib-search-path ./xy" -ex "set solib-absolute-prefix ./xy" -ex "set libthread-db-search-path ./xy" -ex "set debug-file-directory %s" -ex "file %s" -ex "core-file %s" -ex "set print static-members off"
-`, g.GDBBinPath, GDB_DEBUG_FILE_DIRECTORY, ctx.Process.Exe, ctx.Corefile.Path)
+`, g.GDBBinPath, GDB_DEBUG_FILE_DIRECTORY, exePath, ctx.Corefile.Path)
 
 	os.WriteFile("gdb.sh", []byte(code), 0644)
 }
@@ -623,7 +627,11 @@ func (g *GDBManager) InitServerForCorefile(ctx *Context) error {
 	)
 
 	if ctx.Process.Exe != "" {
-		initCmds = append(initCmds, "file "+ctx.Process.Exe)
+		exePath := ctx.Process.Exe
+		if resolved := ctx.Process.Realpath(ctx.Process.Exe); resolved != "" {
+			exePath = resolved
+		}
+		initCmds = append(initCmds, "file "+exePath)
 	}
... (truncated, 2095 chars total)
```
</details>

#### `12:07:58` Tool: `bash` — 查看工作区变更状态 (completed)

```bash
git status --short
```

<details><summary>Output</summary>

```
 M common/common.go
 M common/mmap_owner.go
 M common/var.go
 M prepare/alloc.go
 M prepare/args.go
 M prepare/cmd.go
 M prepare/context.go
 M prepare/gdb.go
 M python/dict.go
 m testdata
?? !
?? .symphony_prompt.md
?? dev-flow/20260311164300-dev-report.md
?? dev-flow/20260311165147-qa-report.md
?? dev-flow/20260311171637-dev-report.md
?? dev-flow/20260311173200-qa-report.md
?? dev-flow/20260311182739-qa-report.md
?? dev-flow/20260311190353-dev-report.md
?? dev-flow/20260311192200-qa-report.md
?? dev-flow/plan.md
?? dev-flow/test-cases.md
?? dev-log/2026-03-11-prepare-ac1-ac6-postmanprofile-validation.md
?? dev-log/2026-03-11-prepare-runtime-init-regression-fix.md
?? qa-report.md

```
</details>

*Step finished: reason=tool-calls, tokens=101,156*

### `12:08:05` Step 12

#### `12:08:05` Tool: `read` — prepare/alloc.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go</path>
<type>file</type>
<content>520: // --- Helper: locateNames ---
521: 
522: // locateNames 批量定位符号地址（对应 Python Symbols.locate_names）
523: func locateNames(ctx *Context, filepath string, sectionAddress string, names []string, sectionOffset uint64) {
524: 	if strings.HasPrefix(sectionAddress, "0x") {
525: 		sectionAddress = sectionAddress[2:]
526: 	}
527: 
528: 	// 尝试使用调试符号文件
529: 	path := filepath
530: 	buildId, candidates := debugSymbolCandidates(filepath)
531: 	_ = buildId
532: 	for _, p := range candidates {
533: 		if info, err := os.Stat(p); err == nil && info.Size() > 0 {
534: 			path = p
535: 			break
536: 		}
537: 	}
538: 
539: 	output, err := CheckOutput("readelf", "-s", path)
540: 	if err != nil || output == "" {
541: 		output, _ = CheckOutput("readelf", "-s", filepath)
542: 	}
543: 	if output == "" {
544: 		return
545: 	}
546: 
547: 	symbols := make(map[string]string) // name -> offset
548: 	for _, name := range names {
549: 		symbols[name] = "0"
550: 	}
551: 
552: 	for _, line := range strings.Split(output, "\n") {
553: 		line = strings.TrimSpace(line)
554: 		if line == "" {
555: 			continue
556: 		}
557: 		ps := strings.Fields(line)
558: 		if len(ps) < 8 {
559: 			continue
560: 		}
561: 		name := ps[len(ps)-1]
562: 		if strings.Contains(name, ".") {
563: 			name = strings.Split(name, ".")[0]
564: 		}
565: 
566: 		if _, ok := symbols[name]; ok {
567: 			if symbols[name] == "0" {
568: 				symbols[name] = ps[1]
569: 			}
570: 		}
571: 	}
572: 
573: 	secAddr, _ := strconv.ParseUint(sectionAddress, 16, 64)
574: 
575: 	for name, offsetStr := range symbols {
576: 		offset, _ := strconv.ParseUint(offsetStr, 16, 64)
577: 		if offset == 0 {
578: 			continue
579: 		}
580: 		address := secAddr + offset - sectionOffset
581: 
582: 		// 如果已有值，不覆盖
583: 		if existing, ok := ctx.Symbols.Symbols[name]; ok && existing != 0 {
584: 			Debug.Printf("locateNames %s keep old 0x%x\n", name, existing)
585: 			contin
... (truncated, 3745 chars total)
```
</details>

#### `12:08:05` Tool: `read` — prepare/symbols.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go</path>
<type>file</type>
<content>1: package prepare
2: 
3: import (
4: 	"fmt"
5: 	"os"
6: 	"path/filepath"
7: 	"strings"
8: )
9: 
10: // DownloadSymbolFiles 下载符号文件
11: func (s *SymbolsManager) DownloadSymbolFiles(ctx *Context) error {
12: 	os.MkdirAll("debug/.build-id", 0755)
13: 
14: 	// 下载 exe 的符号
15: 	realpath := ctx.Process.Realpath(ctx.Process.Exe)
16: 	bid, _ := GetBuildId(realpath)
17: 	if bid != "" {
18: 		downloadSymbol(bid, ctx.Process.Exe)
19: 	}
20: 
21: 	// 下载关键库的符号
22: 	done := make(map[string]bool)
23: 	for _, line := range ctx.Process.ProcMapsOutput {
24: 		ps := strings.Fields(line)
25: 		if len(ps) < 6 {
26: 			continue
27: 		}
28: 		fp := ps[5]
29: 		soname := SoName(fp)
30: 		if done[soname] {
31: 			continue
32: 		}
33: 
34: 		if strings.Contains(fp, "/usr/share/locale/zh_CN") {
35: 			continue
36: 		}
37: 
38: 		if needDownload(soname, ctx) {
39: 			realpath := ctx.Process.Realpath(fp)
40: 			bid, _ := GetBuildId(realpath)
41: 			if bid != "" {
42: 				downloadSymbol(bid, fp)
43: 			}
44: 		}
45: 		done[soname] = true
46: 	}
47: 
48: 	return nil
49: }
50: 
51: // SymbolsRun 通过 GDB 获取符号地址
52: func (s *SymbolsManager) Run(ctx *Context) {
53: 	var typeNames []string
54: 	switch ctx.Language {
55: 	case "python":
56: 		typeNames = getPythonTypeNames()
57: 	case "lua":
58: 		typeNames = getLuaTypeNames()
59: 	}
60: 
61: 	data := make(map[string]uint64)
62: 	for _, typename := range typeNames {
63: 		command := "info address " + typename
64: 		chunk := ctx.GDB.Execute(command)
65: 		line := strings.TrimSpace(chunk)
66: 		if line == "" || strings.Contains(line, "unresolved") || strings.Contains(line, "Auto-loading safe path") {
67: 			continue
68: 		}
69: 		ps := strings.Fields(line)
70: 		if len(ps) >= 2 {
71: 			addrStr := ps[len(ps)-1]
72: 			addrStr = strings.TrimSuffix(addrStr, ".")
73: 			address := Hexi(addrStr)
74: 			name := strings.Trim(ps[1], "\"'")
75: 			data[name] = addres
... (truncated, 2142 chars total)
```
</details>

#### `12:08:05` Tool: `read` — prepare/context.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/context.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/context.go</path>
<type>file</type>
<content>320: 	done           bool
321: }
322: 
323: // DBC 数据库连接
324: type DBC struct {
325: 	Prefix string
326: }
327: 
328: // CollectMallocLib malloc 库收集
329: type CollectMallocLib struct{}
330: 
331: // ParseDwarfManager DWARF 解析管理
332: type ParseDwarfManager struct{}
333: 
334: // NewContext 创建新的上下文
335: func NewContext() *Context {
336: 	return &Context{
337: 		Args: &Args{},
338: 		Process: &ProcessManager{
339: 			FileMaps:   make(map[string]string),
340: 			XyFileMaps: make(map[string]string),
341: 			Mallocer:   "ptmalloc",
342: 		},
343: 		Corefile: &CorefileManager{},
344: 		Symbols:  &SymbolsManager{Symbols: make(map[string]uint64)},
345: 		GDB: &GDBManager{
346: 			ServerHost:   "127.0.0.1",
347: 			ServerPort:   9999,
348: 			RodataOffset: make(map[string]uint64),
349: 		},
350: 		Cpp: &CppAnalyzer{
351: 			Vtables:         make(map[uint64]uint64),
352: 			DeepLoopStructs: make(map[uint64]CppStruct),
353: 			AllTypeName:     make(map[uint64]string),
354: 			GdbSymbols:      make(map[uint64]SymbolInfo),
355: 			StackLocals:     make(map[uint64]StackLocalInfo),
356: 			DbVersion:       "n9",
357: 			DwarfVersion:    "v1",
358: 		},
359: 		CppVtable: &CppVtableManager{
360: 			Vtables: make(map[uint64]uint64),
361: 			Seen:    make(map[string]bool),
362: 		},
363: 		Libc:     &LibcManager{Ver: "libc_v2"},
364: 		Tcmalloc: &TcmallocManager{DbVersion: "v0"},
365: 		Jemalloc: &JemallocManager{},
366: 		Mimalloc: &MimallocManager{},
367: 		OpenBLAS: &OpenBLASManager{SeenPaths: make(map[string]bool)},
368: 		PythonLang: &PythonLangManager{
369: 			TypeNames: getPythonTypeNames(),
370: 		},
371: 		LuaLang:     &LuaLangManager{},
372: 		NodejsLang:  &NodejsLangManager{},
373: 		CLang:       &CLangManager{},
374: 		Asiocore:    &AsiocoreManager{},
375: 		AsiocoreNew: &AsiocoreNewManager{},
376: 		Messiah:     &MessiahManager{},
377: 		Mosd:        &MosdManager{},
378: 		Tx
... (truncated, 2395 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=103,031*

### `12:08:10` Step 13

#### `12:08:16` Tool: `read` — prepare/symbols.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go</path>
<type>file</type>
<content>80: 		s.Symbols[k] = v
81: 	}
82: }
83: 
84: func needDownload(soname string, ctx *Context) bool {
85: 	if soname == "libc" {
86: 		return true
87: 	}
88: 	if strings.Contains(soname, "pthread") {
89: 		return true
90: 	}
91: 	if strings.Contains(soname, "libstdc++") {
92: 		return true
93: 	}
94: 	if strings.Contains(soname, "jemalloc") {
95: 		return true
96: 	}
97: 	if strings.Contains(soname, "tcmalloc") {
98: 		return true
99: 	}
100: 	if strings.Contains(soname, "libpython") && !ctx.PythonLang.IsStaticLink {
101: 		return true
102: 	}
103: 	if strings.Contains(soname, "openblas") || strings.Contains(soname, "libblas") {
104: 		return true
105: 	}
106: 	return false
107: }
108: 
109: func downloadSymbol(buildId, path string) {
110: 	if buildId == "" {
111: 		return
112: 	}
113: 	a, b := buildId[:2], buildId[2:]
114: 	localName := fmt.Sprintf("debug/.build-id/%s/%s.debug", a, b)
115: 	if _, err := os.Stat(localName); err == nil {
116: 		return // already exists
117: 	}
118: 	systemName := fmt.Sprintf("/usr/lib/debug/.build-id/%s/%s.debug", a, b)
119: 	if _, err := os.Stat(systemName); err == nil {
120: 		return // system already has it
121: 	}
122: 
123: 	// 尝试从 S3 下载
124: 	key := buildId + ".debug"
125: 	binPath := "./bin/s3"
126: 	if _, err := os.Stat(binPath); err == nil {
127: 		command := fmt.Sprintf("./bin/s3 download symbols/%s %s", key, key)
128: 		RunShell(command)
129: 	}
130: 
131: 	// 移动到正确位置
132: 	if _, err := os.Stat(key); err == nil {
133: 		os.MkdirAll(fmt.Sprintf("debug/.build-id/%s", a), 0755)
134: 		os.Rename(key, localName)
135: 	}
136: }
137: 
138: // DownloadSymbol 从 S3 下载符号文件
139: func DownloadSymbol(buildId, path string) error {
140: 	downloadSymbol(buildId, path)
141: 	return nil
142: }
143: 
144: // DebugSymbolCandidates 生成调试符号文件的候选路径
145: func DebugSymbolCandidates(fpath string) []string {
146: 	_, candidates := debugSymbolCandidates(fpat
... (truncated, 2480 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=107,147*

### `12:08:23` Step 14

#### `12:08:26` Tool: `grep` — GetBuildId\(|LoadOffset\(|CheckOutput\("readelf", "-a"|CheckOutput\("readelf", "-n"|CheckOutput\("readelf", "-s" (completed)

Pattern: `GetBuildId\(|LoadOffset\(|CheckOutput\("readelf", "-a"|CheckOutput\("readelf", "-n"|CheckOutput\("readelf", "-s"`

<details><summary>Output</summary>

```
Found 32 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 161: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 184: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 276: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 285: 	buildId, _ := GetBuildId(path)
  Line 432: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 445: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 459: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 471: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 499: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
  Line 539: 	output, err := CheckOutput("readelf", "-s", path)
  Line 541: 		output, _ = CheckOutput("readelf", "-s", filepath)
  Line 594: 	buildId, _ := GetBuildId(filepath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/gdb.go:
  Line 167: 	exeBuildId, _ := GetBuildId(ctx.Process.Realpath(ctx.Process.Exe))
  Line 205: 		libcBuildId, _ := GetBuildId(ctx.Process.Libc)
  Line 220: 		tcBuildId, _ := GetBuildId(ctx.Process.TcmallocPath)
  Line 235: 		jeBuildId, _ := GetBuildId(ctx.Process.JemallocPath)
  Line 250: 		miBuildId, _ := GetBuildId(ctx.Process.MimallocPath)
  Line 265: 		tdBuildId, _ := GetBuildId(ctx.Process.TaggeddictPath)
  Line 280: 		mosdBuildId, _ := GetBuildId(ctx.Process.MosdPath)
  Line 295: 		g78BuildId, _ := GetBuildId(ctx.Process.G78Path)

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 104: 	buildId, err := GetBuildId(realpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 324: func (p *ProcessManager) LoadOffset(filepath string, realFileOffset string) uint64 {
  Line 325: 	output, err := CheckOutput("readelf", "-a", filepath)
  Line 365: 	output, err := CheckOutput("readelf", "-s", exePathForReadelf)

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 16: 	bid, _ := GetBuildI
... (truncated, 2618 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=110,857*

### `12:08:43` Step 15

#### `12:08:48` Tool: `grep` — readelf (completed)

Pattern: `readelf`

<details><summary>Output</summary>

```
Found 19 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/cmd.go:
  Line 42: 	// 将 stderr 重定向到 /dev/null，避免 readelf 等工具的警告信息输出到控制台

/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 539: 	output, err := CheckOutput("readelf", "-s", path)
  Line 541: 		output, _ = CheckOutput("readelf", "-s", filepath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 83: 	output, err := CheckOutput("readelf", "-S", realpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 325: 	output, err := CheckOutput("readelf", "-a", filepath)
  Line 365: 	output, err := CheckOutput("readelf", "-s", exePathForReadelf)

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 154: 	output, err := CheckOutput("readelf", "-s", fpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/utils.go:
  Line 72: 	output, err := CheckOutput("readelf", "-n", path)

/home/hcjn0770/temp/worktrees/issue-110/common/vm.go:
  Line 329: 		// 改为使用外部的 readelf 去解析
  Line 330: 		L = readelfSections(L)
  Line 334: 		// 在处理coredump时，有些coredump文件没有节头表，readelf -S 的内容是空的
  Line 336: 		// 改为使用外部的 readelf -l 去解析，效果和readelf -S 一致
  Line 337: 		L = readelfProgram(L)
  Line 347: func readelfSections(L [][3]uint64) [][3]uint64 {
  Line 348: 	Debug.Println("go debug/elf parse failed, try readelf")
  Line 356: 	output, err := exec.Command("readelf", "-S", S.Corefile.File).Output()
  Line 409: func readelfProgram(L [][3]uint64) [][3]uint64 {
  Line 410: 	Debug.Println("go debug/elf parse failed, try readelf -l")
  Line 418: 	output, err := exec.Command("readelf", "-l", S.Corefile.File).Output()
```
</details>

*Step finished: reason=tool-calls, tokens=113,204*

### `12:08:54` Step 16

#### `12:09:00` Tool: `read` — prepare/alloc.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go</path>
<type>file</type>
<content>260: 	}
261: 
262: 	Debug.Printf("* Jemalloc.address %s\n", realpath)
263: 	j.Enabled = true
264: 	ctx.Process.JemallocPath = realpath
265: 	ctx.Process.Mallocer = "jemalloc"
266: 
267: 	md5, _ := Md5Sum(realpath)
268: 	j.Md5 = md5
269: 
270: 	names := []string{
271: 		"je_extents_rtree", "je_bin_infos", "je_arenas",
272: 		"je_sz_index2size_tab", "mallctl", "je_arena_emap_global",
273: 		"je_arena_bin_info", "je_chunks_rtree", "je_chunksize",
274: 		"je_map_bias", "je_map_misc_offset", "je_nhbins",
275: 	}
276: 	sectionOffset := ctx.Process.LoadOffset(realpath, fileOffset)
277: 	locateNames(ctx, realpath, sectionAddress, names, sectionOffset)
278: 
279: 	j.findLastMmap(ctx)
280: 	j.collectTsdTls(ctx)
281: 	j.done = true
282: }
283: 
284: func (j *JemallocManager) findVersion(path string) bool {
285: 	buildId, _ := GetBuildId(path)
286: 
287: 	knownVersions := map[string]string{
288: 		"3b66d506781d234a75f222ae1c15698fa7ba4a28": "5.1.0",
289: 		"2863cec4a9f7f56fb28b319e5360bfaa0724b350": "5.3.0",
290: 		"9eafb8b4e833a3df695f9fe9b4f3bf50bd23dd6a": "5.3.0",
291: 		"bc18b48627d1d71f999e46ce3995fca1fae9dbf6": "5.2.1",
292: 		"3624f54a957d7210121121bd82bfceb06c632b91": "5.2.1",
293: 	}
294: 
295: 	if v, ok := knownVersions[buildId]; ok {
296: 		j.Version = v
297: 		j.Sversion = v
298: 		return true
299: 	}
300: 
301: 	output, err := CheckOutput("strings", path)
302: 	if err != nil {
303: 		return false
304: 	}
305: 
306: 	var major, minor, bugfix string
307: 	ok := false
308: 	for _, line := range strings.Split(output, "\n") {
309: 		if strings.Contains(line, "JEMALLOC_VERSION") {
310: 			ok = true
311: 			if strings.Contains(line, "MAJOR") {
312: 				ps := strings.Fields(line)
313: 				if len(ps) >= 2 {
314: 					major = ps[1]
315: 				}
316: 			}
317: 			if strings.Contains(line, "MINOR") {
318: 				ps := strings.Fields(line)
319: 				if len(ps) >= 2 {
320: 					minor = ps[
... (truncated, 2445 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=113,686*

### `12:09:06` Step 17

#### `12:09:15` Tool: `grep` — exec\.Command\("readelf"|RunCommand\("readelf"|CheckOutput\("readelf" (completed)

Pattern: `exec\.Command\("readelf"|RunCommand\("readelf"|CheckOutput\("readelf"`

<details><summary>Output</summary>

```
Found 9 matches
/home/hcjn0770/temp/worktrees/issue-110/prepare/alloc.go:
  Line 539: 	output, err := CheckOutput("readelf", "-s", path)
  Line 541: 		output, _ = CheckOutput("readelf", "-s", filepath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/cpp_analyzer.go:
  Line 83: 	output, err := CheckOutput("readelf", "-S", realpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/process.go:
  Line 325: 	output, err := CheckOutput("readelf", "-a", filepath)
  Line 365: 	output, err := CheckOutput("readelf", "-s", exePathForReadelf)

/home/hcjn0770/temp/worktrees/issue-110/prepare/symbols.go:
  Line 154: 	output, err := CheckOutput("readelf", "-s", fpath)

/home/hcjn0770/temp/worktrees/issue-110/prepare/utils.go:
  Line 72: 	output, err := CheckOutput("readelf", "-n", path)

/home/hcjn0770/temp/worktrees/issue-110/common/vm.go:
  Line 356: 	output, err := exec.Command("readelf", "-S", S.Corefile.File).Output()
  Line 418: 	output, err := exec.Command("readelf", "-l", S.Corefile.File).Output()
```
</details>

*Step finished: reason=tool-calls, tokens=115,861*

### `12:09:21` Step 18

#### `12:09:58` Tool: `apply_patch` — Success. Updated the following files:
M prepare/process.go
M prepare/gdb.go
M prepare/symbols.go
M .symphony_prompt.md (completed)

```json
{
  "patchText": "*** Begin Patch\n*** Update File: /home/hcjn0770/temp/worktrees/issue-110/prepare/process.go\n@@\n // Realpath 返回文件的实际路径\n func (p *ProcessManager) Realpath(path string) string {\n \tif mapped, ok := p.FileMaps[path]; ok {\n \t\treturn mapped\n \t}\n \treturn path\n }\n+\n+// ResolveExistingPath 返回当前环境中可访问的实际文件路径。\n+// tar/postman 模式下 maps 中记录的是原始路径，本地分析时需要映射到解压后的文件。\n+func (p *ProcessManager) ResolveExistingPath(path string) string {\n+\tif path == \"\" {\n+\t\treturn \"\"\n+\t}\n+\n+\tresolved := p.Realpath(path)\n+\tif resolved != \"\" {\n+\t\tif _, err := os.Stat(resolved); err == nil {\n+\t\t\treturn resolved\n+\t\t}\n+\t}\n+\n+\tif _, err := os.Stat(path); err == nil {\n+\t\treturn path\n+\t}\n+\n+\tbase := filepath.Base(path)\n+\tif base != \"\" && p.ctx != nil && p.ctx.DirPath != \"\" {\n+\t\tcandidate := filepath.Join(p.ctx.DirPath, base)\n+\t\tif _, err := os.Stat(candidate); err == nil {\n+\t\t\treturn candidate\n+\t\t}\n+\t}\n+\n+\treturn resolved\n+}\n@@\
... (truncated, 4462 chars total)
```

```
Success. Updated the following files:
M prepare/process.go
M prepare/gdb.go
M prepare/symbols.go
M .symphony_prompt.md
```

*Step finished: reason=tool-calls, tokens=118,888*

### `12:10:00` Step 19

#### `12:10:02` Tool: `todowrite` — 3 todos (completed)

```json
{
  "todos": [
    {
      "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
      "priority": "high",
      "status": "in_progress"
    },
    {
      "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
      "priority": "high",
      "status": "pending"
    },
    {
      "content": "进行 Self-Review，编写开发报告到 dev-log",
      "priority": "medium",
      "status": "pending"
    }
  ]
}
```

<details><summary>Output</summary>

```
[
  {
    "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
    "priority": "high",
    "status": "in_progress"
  },
  {
    "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
    "priority": "high",
    "status": "pending"
  },
  {
    "content": "进行 Self-Review，编写开发报告到 dev-log",
    "priority": "medium",
    "status": "pending"
  }
]
```
</details>

*Step finished: reason=tool-calls, tokens=118,505*

### `12:10:06` Step 20

#### `12:10:10` Tool: `bash` — 格式化代码并执行构建 (completed)

```bash
gofmt -w "prepare/process.go" "prepare/gdb.go" "prepare/symbols.go" && ./maze --build
```

```
building .maze ...

```

*Step finished: reason=tool-calls, tokens=121,517*

### `12:10:13` Step 21

#### `12:10:18` Tool: `bash` — 运行 Python 回归测试用例 (completed)

```bash
python3 testdata/run_test.py python/20260129-complex-types-311
```

<details><summary>Output</summary>

```
args  defproj defuser  standalone
============================================================
Maze One-Click Analysis
============================================================
Input: /home/hcjn0770/temp/worktrees/issue-110/testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz
Project: coredump, PID: 2636550
============================================================
Extracted to: /tmp/maze-tar-3631944126
Core file: /tmp/maze-tar-3631944126/core.2636550
Exe file: /tmp/maze-tar-3631944126/42b1f1b7c851b28e7563947595e2320e
============================================================
Starting Analysis
============================================================
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 2636550
Python version: 3.11.0 (iversion: 31100)
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
prepare.Run() completed successfully
maze !_.zz
CPU 35.19177937973313 Mem 2.9G Rss 121.1M goroutines 14
CPU 107.87179566531333 Mem 3.2G Rss 966.8M goroutines 62
* PrintMemorySumary
- vms                             30.1M
  - vms.heap                       3.1M  10.3%
  - vms.stack                        0B   0.0%
  - vms.file_mmap                 18.9M  62.9%
  - vms.system                       0B   0.0%
  - vms.mmap                       8.1M  26.8%

Memory Pool Summary      pymalloc     ptmalloc        total
  objects                68626         1045        69671
  total                   7.0M         3.1M        10.1M
  malloced                6.3M         3.1M         9.5M
  idle                   19.4k       303.0k       322.4k
  empty                  43.6k       399.9k       443.5k
  use%                   90.2%       101.5%        93.7%
  idle%                   0.3%         9.5%         3.1%
  empty%                  0.6%        12.6%         4.3%
CountGoroutineError: 38
* main.maze1 took 1.065374115s since 2026-03-11 20:10:17.441125301 +0800 HKT m=+1.825056352

===
... (truncated, 40083 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=120,933*

### `12:10:21` Step 22

#### `12:10:26` Tool: `bash` — 运行 C++ 多线程回归测试 (completed)

```bash
python3 testdata/run_test.py cpp/20260304-stack-multithread
```

<details><summary>Output</summary>

```
args  defproj defuser  standalone
============================================================
Maze One-Click Analysis
============================================================
Input: /home/hcjn0770/temp/worktrees/issue-110/testdata/cpp/20260304-stack-multithread/coredump-68446-1772628024.tar.gz
Project: coredump, PID: 68446
============================================================
Extracted to: /tmp/maze-tar-2021645980
Core file: /tmp/maze-tar-2021645980/core.68446
Exe file: /tmp/maze-tar-2021645980/519f9acc4b24d86bcbe85e0806f67cf0
============================================================
Starting Analysis
============================================================
prepare.Run() started
Mode: standalone, Project: coredump, User: defuser, ProfileId: 68446
* GDB: Server started successfully
* GDB: Connected to auto-started gdb_server
prepare.Run() completed successfully
maze !_.zz
CPU 30.925323844890407 Mem 3.5G Rss 48.7M goroutines 14
* InitAllMemoryPieceSize
* MergeUnknowMemory
* PrintMemorySumary
- vms                            725.6M
  - vms.heap                     132.0k   0.0%
  - vms.stack                        0B   0.0%
  - vms.file_mmap                  5.2M   0.7%
  - vms.system                       0B   0.0%
  - vms.mmap                     720.3M  99.3%

Memory Pool Summary      pymalloc     ptmalloc        total
  objects                    0            0            0
  total                     0B         1.4M         1.4M
  malloced                  0B        86.9k        86.9k
  idle                      0B         1.3M         1.3M
  empty                     0B         1.3M         1.3M
  use%                     -           6.0%         6.0%
  idle%                    -          92.5%        92.5%
  empty%                   -          94.0%        94.0%
CountGoroutineError: 0

================================================================================
Memory Analysis Results (Top Objects by Size)
=================================
... (truncated, 4770 chars total)
```
</details>

*Step finished: reason=tool-calls, tokens=134,274*

### `12:10:30` Step 23

#### `12:10:32` Tool: `bash` — 查看本轮修复代码差异 (completed)

```bash
git diff -- prepare/process.go prepare/gdb.go prepare/symbols.go .symphony_prompt.md
```

<details><summary>Output</summary>

```
diff --git a/prepare/gdb.go b/prepare/gdb.go
index faa48a45..7080d6fc 100644
--- a/prepare/gdb.go
+++ b/prepare/gdb.go
@@ -512,8 +512,12 @@ func (g *GDBManager) Ctypes(ctx *Context) error {
 // GenerateDebugScript 生成 gdb.sh 调试脚本
 func (g *GDBManager) GenerateDebugScript(ctx *Context) {
 	Debug.Printf("* GDB.GenerateDebugScript\n")
+	exePath := ctx.Process.ResolveExistingPath(ctx.Process.Exe)
+	if exePath == "" {
+		exePath = ctx.Process.Exe
+	}
 	code := fmt.Sprintf(`%s -x .gdbcommand.py -ex "set auto-load safe-path ./xy" -ex "set solib-search-path ./xy" -ex "set solib-absolute-prefix ./xy" -ex "set libthread-db-search-path ./xy" -ex "set debug-file-directory %s" -ex "file %s" -ex "core-file %s" -ex "set print static-members off"
-`, g.GDBBinPath, GDB_DEBUG_FILE_DIRECTORY, ctx.Process.Exe, ctx.Corefile.Path)
+`, g.GDBBinPath, GDB_DEBUG_FILE_DIRECTORY, exePath, ctx.Corefile.Path)
 
 	os.WriteFile("gdb.sh", []byte(code), 0644)
 }
@@ -623,7 +627,11 @@ func (g *GDBManager) InitServerForCorefile(ctx *Context) error {
 	)
 
 	if ctx.Process.Exe != "" {
-		initCmds = append(initCmds, "file "+ctx.Process.Exe)
+		exePath := ctx.Process.ResolveExistingPath(ctx.Process.Exe)
+		if exePath == "" {
+			exePath = ctx.Process.Exe
+		}
+		initCmds = append(initCmds, "file "+exePath)
 	}
 	if ctx.Corefile.Path != "" {
 		initCmds = append(initCmds, "core-file "+ctx.Corefile.Path)
diff --git a/prepare/process.go b/prepare/process.go
index a3369b4a..3d70a7d3 100644
--- a/prepare/process.go
+++ b/prepare/process.go
@@ -302,6 +302,35 @@ func (p *ProcessManager) Realpath(path string) string {
 	return path
 }
 
+// ResolveExistingPath 返回当前环境中可访问的实际文件路径。
+// tar/postman 模式下 maps 中记录的是原始路径，本地分析时需要映射到解压后的文件。
+func (p *ProcessManager) ResolveExistingPath(path string) string {
+	if path == "" {
+		return ""
+	}
+
+	resolved := p.Realpath(path)
+	if resolved != "" {
+		if _, err := os.Stat(resolved); err == nil {
+			return resolved
+		}
+	}
+
+	if _, err := os.Stat(path); err == nil {
+		retur
... (truncated, 5147 chars total)
```
</details>

#### `12:10:32` Tool: `bash` — 再次查看工作区变更状态 (completed)

```bash
git status --short
```

<details><summary>Output</summary>

```
 M common/common.go
 M common/mmap_owner.go
 M common/var.go
 M prepare/alloc.go
 M prepare/args.go
 M prepare/cmd.go
 M prepare/context.go
 M prepare/gdb.go
 M prepare/process.go
 M prepare/symbols.go
 M python/dict.go
 m testdata
?? !
?? .symphony_prompt.md
?? dev-flow/20260311164300-dev-report.md
?? dev-flow/20260311165147-qa-report.md
?? dev-flow/20260311171637-dev-report.md
?? dev-flow/20260311173200-qa-report.md
?? dev-flow/20260311182739-qa-report.md
?? dev-flow/20260311190353-dev-report.md
?? dev-flow/20260311192200-qa-report.md
?? dev-flow/plan.md
?? dev-flow/test-cases.md
?? dev-log/2026-03-11-prepare-ac1-ac6-postmanprofile-validation.md
?? dev-log/2026-03-11-prepare-runtime-init-regression-fix.md
?? qa-report.md

```
</details>

*Step finished: reason=tool-calls, tokens=148,911*

### `12:10:40` Step 24

#### `12:10:42` Tool: `todowrite` — 1 todos (completed)

```json
{
  "todos": [
    {
      "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "进行 Self-Review，编写开发报告到 dev-log",
      "priority": "medium",
      "status": "in_progress"
    }
  ]
}
```

<details><summary>Output</summary>

```
[
  {
    "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "进行 Self-Review，编写开发报告到 dev-log",
    "priority": "medium",
    "status": "in_progress"
  }
]
```
</details>

*Step finished: reason=tool-calls, tokens=153,232*

### `12:10:47` Step 25

#### `12:11:01` Tool: `apply_patch` — Success. Updated the following files:
A dev-log/2026-03-11-gdb-realpath-readelf-fix.md (completed)

```json
{
  "patchText": "*** Begin Patch\n*** Add File: /home/hcjn0770/temp/worktrees/issue-110/dev-log/2026-03-11-gdb-realpath-readelf-fix.md\n+# 开发报告\n+\n+## 完成的任务\n+- [x] Task 0.2: GDB Socket Client 已用于稳定执行 `file` / `core-file` 初始化命令并完成本轮回归验证\n+- [x] Task 0.3: GDB Server 进程管理已在回归测试中稳定启动并保持连接\n+- [x] Task 1.2: 修复进程路径解析，新增 `ResolveExistingPath()`，确保 tar 模式下原始 maps 路径能解析为本地可访问 ELF 文件\n+- [x] Task 1.3: 修复符号下载阶段对可执行文件和共享库的 Build ID 读取路径，统一使用可访问实际路径\n+- [x] Task 1.4: 修复 GDB 初始化与 `gdb.sh` 生成时的 `file` 路径，避免 GDB 加载原始 maps 路径失败\n+\n+## 修改的文件\n+- `prepare/process.go`: 新增 `ResolveExistingPath()`，并在 `LoadOffset()`、`setupLanguage()` 中统一使用实际存在的 ELF 路径\n+- `prepare/gdb.go`: 修复 `GenerateDebugScript()` 和 `InitServerForCorefile()` 的 `file` 命令路径解析逻辑\n+- `prepare/symbols.go`: 修复下载符号文件时对 exe/so 的 Build ID 读取路径，避免对不存在原始路径执行 `readelf`\n+- `.symphony_prompt.md`: 将本轮已完成并验证过的相关任务标记为已完成\n+\n+## 自测结果\n+- 测试命令: `./maze --build`\n+- 测试结果: 通过，成功构建 Go 二进制\n+\n+- 测试命令: `python3 testdata/run_test.py python/20260129-complex-
... (truncated, 1641 chars total)
```

```
Success. Updated the following files:
A dev-log/2026-03-11-gdb-realpath-readelf-fix.md
```

*Step finished: reason=tool-calls, tokens=156,161*

### `12:11:03` Step 26

#### `12:11:05` Tool: `todowrite` — 0 todos (completed)

```json
{
  "todos": [
    {
      "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
      "priority": "high",
      "status": "completed"
    },
    {
      "content": "进行 Self-Review，编写开发报告到 dev-log",
      "priority": "medium",
      "status": "completed"
    }
  ]
}
```

<details><summary>Output</summary>

```
[
  {
    "content": "检查 QA 指出的 GDB 路径与 readelf 路径问题，定位相关代码",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "修复 prepare 中错误路径与冗余回退逻辑，并更新计划文档勾选状态",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "执行构建与指定回归测试，确认 Python/C++ 用例结果",
    "priority": "high",
    "status": "completed"
  },
  {
    "content": "进行 Self-Review，编写开发报告到 dev-log",
    "priority": "medium",
    "status": "completed"
  }
]
```
</details>

*Step finished: reason=tool-calls, tokens=156,176*

### `12:11:11` Step 27

已按附件要求完成修复、验证与开发报告记录。

## 本轮完成内容

### 1. 修复 GDB `file` 命令错误路径
已修复以下位置，确保 tar 模式下不再使用 maps 中不存在的原始路径，而是使用本地可访问的真实 ELF 路径：

- `prepare/gdb.go`
  - `GenerateDebugScript()`
  - `InitServerForCorefile()`

### 2. 修复 readelf / Build ID 路径解析
为避免再次对不存在的原始路径执行 `readelf`，新增统一路径解析入口：

- `prepare/process.go`
  - 新增 `ResolveExistingPath()`
  - `LoadOffset()`
  - `setupLanguage()`

并在以下模块切换为使用实际存在的路径：

- `prepare/symbols.go`
  - exe 符号下载
  - so 符号下载

### 3. 清理/确认 QA 提到的冗余逻辑
- `prepare/alloc.go` 中 QA 提到的 `main_arena/mp_` 同 key 冗余回退逻辑，当前工作区里已是清理后的版本，我未重新引入。

### 4. 更新计划文档勾选状态
已在 `.symphony_prompt.md` 中将本轮明确完成并验证过的相关任务标记为已完成：

- Task 0.2
- Task 0.3
- Task 1.2
- Task 1.3
- Task 1.4

---

## 自测结果

### 构建
执行：
```bash
./maze --build
```

结果：
- 通过

### Python 回归
执行：
```bash
python3 testdata/run_test.py python/20260129-complex-types-311
```

结果：
- 通过
- 验证输出包含：`All validations passed!`

### C++ 回归
执行：
```bash
python3 testdata/run_test.py cpp/20260304-stack-multithread
```

结果：
- 通过
- 验证输出包含：`TC-B004 PASSED`

---

## 开发报告
已写入：

```text
dev-log/2026-03-11-gdb-realpath-readelf-fix.md
```

---

## 本轮关键结论

QA 本轮指出的核心阻塞问题已修复，且两个指定回归用例均已通过：

- `python/20260129-complex-types-311` ✅
- `cpp/20260304-stack-multithread` ✅

如果你需要，我下一步可以继续：
1. 整理成可提交的最小变更说明  
2. 继续补跑更多 AC/回归用例  
3. 帮你准备提交信息或 PR 描述

*Step finished: reason=stop, tokens=79,666*
