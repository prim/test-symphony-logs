# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按阶段完成了静态核对、关键 smoke test 与部分扩展回归。结果显示：

- 迁移范围方向基本正确：`.maze.py` 与 `maze-py/*.py` 已删除，`prepare/` 已接管主流程，GDB 内部 Python 脚本仍保留。
- 主链路构建与若干代表性回归可通过：`./maze --build`、Python、C++、jemalloc 代表性用例均通过。
- 但 AC1 要求的**字段级等价性**仍不满足：`go test -count=1 ./qa/issue110 -v` 中 `TestPrepareRunMatchesLegacyJSONBaseline` 失败，差异集中在 `deep_loop_structs`。

因此总体结论为 **FAIL**，当前不建议发布。

## 详细结果

### TC-001: prepare 包基础脚手架存在
- **结果**: PASS
- **实际行为**: `go test ./qa/issue110 -run TestPreparePackageScaffoldExists` 通过，`prepare/prepare.go`、`context.go`、`args.go`、`cmd.go`、`gdb_client.go`、`gdb_server.go`、`process.go`、`symbols.go`、`fill.go` 等核心文件均存在。
- **关键指标**: 仓库级守护测试通过
- **备注**: 基础迁移骨架已落地。

### TC-002: main.go 集成 prepare.Run
- **结果**: PASS
- **实际行为**: `main.go` 已导入 `maze/prepare`，主流程中调用 `prepare.Run()`。
- **关键指标**: `go test ./qa/issue110 -run TestMainUsesPrepareRun` 通过
- **备注**: 入口切换完成。

### TC-003: common 初始化不再依赖 .storage.json / .cpp.json
- **结果**: PASS
- **实际行为**: `common/common.go` 与 `common/var.go` 不再以旧 `.storage.json` / `.cpp.json` 作为运行时初始化入口。
- **关键指标**: `go test ./qa/issue110 -run TestCommonInitNoLongerDependsOnLegacyJSONBootstrap` 通过
- **备注**: 运行时 bootstrap 迁移方向正确。

### TC-004: 遗留 Python 预处理被删除且 GDB 脚本保留
- **结果**: PASS
- **实际行为**: `.maze.py` 与 `maze-py/` 已从当前实现路径移除；`gdb/gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍存在。
- **关键指标**: `go test ./qa/issue110 -run TestLegacyPythonPreprocessIsRemovedButGdbScriptsRemain` 通过
- **备注**: 满足“仅移除运行时预处理 Python，不误删 GDB 内部脚本”的强约束。

### TC-004A: 删除的 Python 模块与新增 Go 模块对应关系检查
- **结果**: PASS
- **实际行为**: 已完成静态映射核对，关键对应关系成立：
  - `maze-py/process.py` → `prepare/process.go`
  - `maze-py/symbols.py` → `prepare/symbols.go`
  - `maze-py/gdb_wrapper.py` → `prepare/gdb.go` + `prepare/gdb_client.go` + `prepare/gdb_server.go`
  - `maze-py/python_lang.py` / `lua.py` / `nodejs.py` / `c_lang.py` → `prepare/lang_*.go`
  - 多个分配器与环境识别逻辑 → `prepare/alloc.go`
  - `maze-py/storage.py` / `.maze.py` 的主流程与填充职责 → `prepare/prepare.go` + `prepare/fill.go`
- **关键指标**: 未发现“文件已删但完全无 Go 承接模块”的明显遗漏
- **备注**: 部分历史 Python 模块为多对一收敛，而非严格一文件对一文件；从模块职责角度可接受。

### TC-004B: Python→Go 对应模块核心逻辑已迁移
- **结果**: PASS
- **实际行为**: 已核对 `prepare/prepare.go`、`prepare/fill.go`、`prepare/process.go`、`prepare/gdb*.go`、`prepare/symbols.go` 等关键实现，确认以下主链路职责存在：
  - `prepare.Run()` 主流程编排
  - 直接填充 `common.S` / `common.Cpp`
  - GDB Socket Client/Server 协作
  - 进程信息、maps、exe、language、mallocer 处理
  - 符号定位与若干外部命令调用
- **关键指标**: 主路径存在，非空壳
- **备注**: 该项仅说明“功能已迁移到 Go”；不代表字段级行为已完全等价，后者由 TC-010 判定失败。

### TC-005: maze 入口不再是 Python wrapper
- **结果**: PASS
- **实际行为**: `maze` 当前为 shell wrapper，不再是 Python shebang，旧 `do_build` / `do_tar` / `do_release` Python 包装逻辑已退出实现入口。
- **关键指标**: `go test ./qa/issue110 -run TestMazeEntryIsNoLongerPythonWrapper` 通过
- **备注**: 当前实现为 shell wrapper + Go 二进制 `.maze`。

### TC-006: GDB 脚本仍基于 GDB Python API
- **结果**: PASS
- **实际行为**: `gdb/gdb-server/gdb_socket_server.py` 仍保留 `import gdb`。
- **关键指标**: `go test ./qa/issue110 -run TestGdbScriptsStillUsePythonAPI` 通过
- **备注**: 满足 AC10。

### TC-007: Python 主流程回归
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；日志显示 `prepare.Run()` 启动并完成，`validate.py` 断言通过。
- **关键指标**: `pymempool_objects=68626`，并检测到 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`
- **备注**: 说明 Go prepare 已可支撑代表性 Python 主链路。

### TC-008: C++ 多线程栈变量回归
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py cpp/20260304-stack-multithread` 通过；主流程分析完成，validate 接受结果。
- **关键指标**: 结果项数量 `9`
- **备注**: 在当前用例定义下通过。

### TC-008A: 代表性分配器用例回归（jemalloc）
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread` 通过。
- **关键指标**: 非 ptmalloc 代表性用例通过 1 个
- **备注**: 说明迁移未明显破坏 jemalloc 主链路。

### TC-008B: Node.js 代表性回归
- **结果**: FAIL
- **实际行为**:
  - `python3 testdata/run_test.py nodejs/20260211-comprehensive` 无法执行到 Maze 分析阶段，因为测试目录缺少 tarball。
  - `python3 testdata/run_test.py nodejs/20260226-container-size` 中 Maze 实际分析过程成功完成，能够加载 V8 常量、定位 isolate 并输出结果，但测试框架随后因 `validate.py` 缺少 `validate()` 函数而失败。
- **关键指标**:
  - 用例一失败原因：缺少 `coredump-*.tar.gz`
  - 用例二失败原因：`module 'validate' has no attribute 'validate'`
- **备注**: 该项失败主要暴露的是 testdata/harness 问题，不能直接证明 prepare 主逻辑出错；但按“验收用例通过”标准，本 TC 仍应记为 FAIL。

### TC-009: `--tar` 单入口分析能力回归
- **结果**: PASS
- **实际行为**: Python、C++、jemalloc 用例均经由 `run_test.py` 间接调用 `maze --tar ... --text --json-output` 完成分析并产出结果文件。
- **关键指标**: 至少 3 个 tar 场景通过
- **备注**: 满足 AC8 主链路要求。

### TC-010: 模式兼容与遗留依赖清理检查
- **结果**: FAIL
- **实际行为**: 虽然迁移范围、入口切换与遗留依赖清理方向正确，但仓库级一致性测试 `TestPrepareRunMatchesLegacyJSONBaseline` 失败，表明 Go prepare 生成结果与历史 JSON 基线仍不完全一致。
- **关键指标**:
  - Python 基线失败：`storage.deep_loop_structs.1 数组长度不同: 4 != 1`
  - C++ 基线失败：`storage.deep_loop_structs.1 仅存在于历史基线`
- **备注**: 该失败直接命中 AC1，足以阻断发布。

## 失败的 Test Case

### TC-008B: Node.js 代表性回归
- **结果**: FAIL
- **预期行为**: 至少 1 个 Node.js 代表性 `testdata` 用例应能通过 `run_test.py` 完整验收。
- **实际行为**:
  - `20260211-comprehensive` 缺少 tarball，无法运行
  - `20260226-container-size` 的 Maze 分析成功，但测试目录中的 `validate.py` 不符合 `run_test.py` 约定，导致最终失败
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py nodejs/20260211-comprehensive`
  2. 观察缺少 tarball 的报错
  3. 执行 `python3 testdata/run_test.py nodejs/20260226-container-size`
  4. 观察最终报错 `module 'validate' has no attribute 'validate'`
- **可能原因**:
  - 现有 Node.js testdata 质量问题，非本次 prepare 迁移直接引入
  - Node.js 类别的自动验收资产不完整，导致 QA 无法获得“完整通过”的证据
- **建议修复**:
  - 补齐 `20260211-comprehensive` 的 tarball，或替换为可运行用例
  - 修正 `20260226-container-size/validate.py`，补充标准 `validate(data)` 入口

### TC-010: 模式兼容与遗留依赖清理检查（聚焦 AC1 字段级一致性）
- **结果**: FAIL
- **预期行为**: `prepare.Run()` 直接填充得到的运行时快照，应与历史 `.storage.json` / `.cpp.json` 在 AC1 关注字段上保持字段级一致。
- **实际行为**: `go test -count=1 ./qa/issue110 -v` 中 `TestPrepareRunMatchesLegacyJSONBaseline` 失败，差异集中在 `deep_loop_structs`。
- **复现步骤**:
  1. 在仓库根目录执行 `go test -count=1 ./qa/issue110 -v`
  2. 查看 `TestPrepareRunMatchesLegacyJSONBaseline`
  3. 观察 `python-complex-types-311` 与 `cpp-stack-multithread` 子用例失败信息
- **可能原因**:
  - `prepare` → 兼容快照转换层未完整复现历史 Python 实现对 `deep_loop_structs` 的组织方式
  - `prepare/fill.go` 或 `buildLegacyStorageSnapshot()` 对该字段的结构、层级、聚合方式存在差异
  - 由于 Python 与 C++ 两类基线都出现类似差异，问题更像公共填充/兼容层问题，而非单语言特定逻辑
- **建议修复**:
  - 逐字段对照历史 `.storage.json` / `.cpp.json` 的生成逻辑，重点核查 `deep_loop_structs`
  - 修复后至少重跑：
    - `go test -count=1 ./qa/issue110 -v`
    - `python3 testdata/run_test.py python/20260129-complex-types-311`
    - `python3 testdata/run_test.py cpp/20260304-stack-multithread`
    - `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`

## 未执行项

- `postmanprofile` 动态验证
  - **原因**: AC1 字段级一致性已明确失败，继续执行更高成本模式回归不能改变总体 FAIL 结论
  - **影响范围**: AC7
- Lua / tcmalloc / mimalloc 扩展回归
  - **原因**: 本轮已完成 Python、C++、jemalloc 和 Node.js 代表性检查；在 AC1 阻塞下未继续扩展更多高成本用例
  - **影响范围**: AC5、AC6、AC7 的扩展覆盖面
- Python 2.7 / 多 glibc / 多 Node.js 版本兼容性
  - **原因**: 当前环境与本轮验收范围未覆盖
  - **影响范围**: 兼容性维度结论不完整

## 已执行的检查项

- 阅读 `dev-flow/plan.md`
- 阅读 `dev-flow/test-cases.md`
- 阅读最新开发报告 `dev-flow/20260311222115-dev-report.md`
- 审查 `git diff origin/master...HEAD`
- 静态核对删除的 Python 预处理文件与新增 Go `prepare` 文件的职责映射
- 检查 `main.go`、`common/common.go`、`common/var.go`、`prepare/prepare.go`、`prepare/fill.go`、`prepare/context.go`
- 执行 `go test -count=1 ./qa/issue110 -v`
- 执行 `./maze --build`
- 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 执行 `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- 执行 `python3 testdata/run_test.py cpp/20260211-jemalloc-5-3-0-multithread`
- 执行 `python3 testdata/run_test.py nodejs/20260211-comprehensive`
- 执行 `python3 testdata/run_test.py nodejs/20260226-container-size`

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 10 / 12
- 失败: 2 / 12
- 建议: **暂不可以发布**。优先修复 AC1 对应的字段级一致性问题；同时补齐 Node.js 测试数据/校验脚本后，再进行下一轮完整 QA。
