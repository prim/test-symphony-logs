# 开发报告

## 结论

PARTIAL

本轮针对 issue #110 的最新 QA 阻塞项进行了复核与回归验证。当前工作区内与 GDB `file`/`core-file`、`readelf`、`nm` 真实路径解析相关的修复已经生效，指定阻塞用例已通过；但仍有若干验收项未在本轮完成充分验证，因此只能给出 `PARTIAL` 结论。

## 完成的任务
- [x] 复核 `prepare/gdb.go` 中 `GenerateDebugScript()` 与 `InitServerForCorefile()` 的可执行文件路径解析，确认已统一走真实 ELF 路径。
- [x] 复核 `prepare/process.go`、`prepare/symbols.go`、`prepare/alloc.go`、`prepare/utils.go` 中 `readelf` / `nm` / Build ID 路径解析链路，确认 tar 解压场景下不会继续使用 maps 原始路径访问本地 ELF。
- [x] 执行构建与两个关键回归测试，确认此前 QA 报告中的 panic 与 `readelf` 告警未复现。

## 修改的文件
- `无`：本轮未新增源码修改；复核结果表明当前工作区内已有修复已覆盖本次 QA 指出的核心阻塞点。

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过，成功生成 `.maze`。

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过。`prepare.Run()` 正常完成，验证脚本全部通过，未出现 `readelf：错误：无法找到程序解释器名称`，未出现 `index out of range [7] with length 0` panic。

- 测试命令: `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- 测试结果: 通过。`prepare.Run()` 正常完成，验证脚本通过，未出现 `initPtmalloc sizeof _heap_info` panic。

## 验收核对
- AC1: PARTIAL 已通过 Python/C++ 两个关键回归用例间接验证 `common.S` / `common.Cpp` 关键字段足够驱动分析流程，但尚未逐字段对比原 `.storage.json` / `.cpp.json` 的完全一致性。
- AC2: PASS `./maze --build` 成功。
- AC3: PASS `python3 testdata/run_test.py python/20260129-complex-types-311` 通过。
- AC4: PASS `python3 testdata/run_test.py cpp/20260304-stack-multithread` 通过。
- AC5: PASS 本轮回归中 GDB Socket Client 能成功连接现有 GDB Socket Server，并完成完整分析流程。
- AC6: PASS 本轮覆盖到的 `readelf`、`nm`、Build ID 相关外部命令工作正常，且未再出现此前的 `readelf` 错误输出。
- AC7: PARTIAL standalone/tar 路径已验证；`postmanprofile` 模式本轮未实际回归。
- AC8: PASS `--tar` 一键分析能力已通过两个 tar 用例验证。
- AC9: PASS 当前仓库仍保持 `.maze.py` 与 `maze-py/*.py` 已删除状态，且上述回归通过。
- AC10: PASS `gdb-scripts/*.py` 与 `gdb/gdb-server/gdb_socket_server.py` 仍保留且本轮 GDB 相关回归正常。

## 已完成项
- 已确认 GDB 初始化使用真实 ELF 路径，而非 maps 原始路径。
- 已确认 tar 模式下 Python 关键回归用例恢复通过。
- 已确认 tar 模式下 C++ 多线程关键回归用例恢复通过。
- 已确认此前 QA 报告中的 `readelf` 告警与两个 panic 在本轮复测中未复现。

## 未完成项
- 对 AC1 进行原始 `.storage.json` / `.cpp.json` 的逐字段一致性比对。
- 对 `postmanprofile` 模式进行实际运行验证。
- 对回归清单中 tcmalloc / jemalloc / mimalloc / Node.js / Lua 用例进行补充验证。

## 未验证项
- `postmanprofile` 模式端到端结果。
- 至少一个 tcmalloc 用例。
- 至少一个 jemalloc 用例。
- 至少一个 mimalloc 用例。
- 至少一个 Node.js 用例。
- 至少一个 Lua 用例（若仓库中存在可用用例）。

## Self-Review 发现与修复
- 复核发现当前工作区中的真实路径修复已经覆盖本轮 QA 指出的核心问题，因此本轮无需额外源码改动。
- 复核发现 `alloc.go` 中此前 QA 提到的“同一 map 同 key 冗余回退逻辑”在当前版本已不存在；`LibcManager.Address()` 现为直接定位 `main_arena`、`mp_`，未见该无效分支残留。

## 注意事项
- maps 中的原始路径仍需保留用于展示和语义信息，但凡涉及本地 ELF 访问（如 `GDB file`、`readelf`、`nm`、Build ID）都必须先做真实路径映射与存在性校验。
- 本轮未修改任何 `testdata/` 下测试文件。
- 由于仍存在未完成项与未验证项，本轮不能宣称全部完成。
