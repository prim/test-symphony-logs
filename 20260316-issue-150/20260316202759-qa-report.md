# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #150 的最新修复进行了静态核对、Go 测试、性能基准以及至少一个以上 testdata 动态回归验证。未发现新的阻塞问题；本轮重点关注的人审反馈“不要改 `cmd/*.py`”也已满足，变更集中在 `common/vm` 与 `dev-log`，未见新增对 `cmd/*.py` 的修改。

## Findings

- 未发现阻塞发布的问题。
- `common/vm/v3` 已具备无锁加载、页表映射、批量读取 API 与 benchmark/contract test 覆盖。
- 最新修复已将 V3 页表克隆限制在非线性布局路径，线性布局下避免了无意义页表复制；同时对比基准已移除不适合作为门槛的冷启动 compare 项，改为结果一致性守护，符合此前人审关注点。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/select_v3.go` 存在，`TestVMV3BuildTagCompilesAndSelectsV3` 与 `go test -tags vm_v3 ./common/vm/... -count=1` 均通过，V3 可注册并切换为当前实现。
- **关键指标**: `go test -tags vm_v3 ./common/vm/... -count=1` 通过。
- **备注**: 静态检查确认 `common/vm/v3/` 目录完整，包含 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go`。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过；并发首访场景未报 data race。
- **关键指标**: race test 通过。
- **备注**: 静态检查 `Segment.ensureLoaded()` 使用 `atomic.Pointer`、`atomic.Uint64` 与 CAS 流程实现加载协作。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 批量读取、短缓冲区、跨 segment iterator、页表 fallback、未对齐与跨 segment range 读取相关测试全部通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: 静态检查确认 `GetMemoryBatch8/4`、`MemoryIterator` 已实现。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中存在 issue 要求的全部 benchmark：`BenchmarkGetMemory8/4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **关键指标**: 14 个目标 benchmark 均可在源码中定位。
- **备注**: 覆盖范围满足 AC8。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中保留了 steady-state compare benchmark 与 `TestV3CompareContractGuard`；对比 benchmark 实际运行通过。
- **关键指标**: compare benchmark 全部通过，且 V3 在保留场景下均优于 V2。
- **备注**: 冷启动 compare 已移出门槛集合，避免将高噪声场景误用为合并门槛。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: 实际执行了 Python 与 Node.js 两个 testdata 用例，均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：PASS
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，本轮实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过；静态检查显示 `PageTableMemoryBytes()` 对 nil 页表有安全处理，线性布局下无需复制页表。
- **关键指标**: 内存开销测试通过。
- **备注**: 最新修复点与该 TC 强相关，验证结果符合预期。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: V3 核心 benchmark 与 compare benchmark 实际运行通过，关键指标达到 issue 中目标。
- **关键指标**:
  - `BenchmarkSequentialScan-48 = 2.874 ns/op`（目标 < 3ns）
  - `BenchmarkRandomAccess-48 = 3.888 ns/op`（目标 < 4ns）
  - `BenchmarkConcurrentAccess-48 = 0.2332 ns/op`（目标 < 1.5ns）
  - Compare:
    - GetMemory8: V2 19.50ns/op vs V3 13.82ns/op
    - GetMemory4: V2 19.58ns/op vs V3 16.45ns/op
    - SequentialScan: V2 19.24ns/op vs V3 16.47ns/op
    - SequentialScanPageTable: V2 28.78ns/op vs V3 20.67ns/op
    - RandomAccess: V2 19.89ns/op vs V3 16.50ns/op
    - RandomAccessPageTable: V2 27.93ns/op vs V3 21.23ns/op
    - ConcurrentAccess: V2 3.210ns/op vs V3 2.048ns/op
    - ConcurrentAccessPageTable: V2 3.746ns/op vs V3 2.759ns/op
- **备注**: AC3/AC4/AC5/AC9 可判定通过。

## Test Results

- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm/... -count=1`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead' -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 ./common/vm -run '^$' -bench 'Benchmark(SequentialScan|RandomAccess|ConcurrentAccess)$|BenchmarkCompareV3(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable)$' -benchtime=100ms -count=1 -benchmem`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 失败的 Test Case（如有）

- 无

## 未执行项

- Python 2.7 动态回归：当前轮未执行。
- 不同 glibc 版本动态回归：当前单机环境未覆盖。
- 不同 Node.js 版本动态回归：当前轮未执行多版本矩阵。

以上未执行项不构成本轮修复范围的阻塞；对本轮 AC1-AC9 主结论影响有限，但会影响更广泛兼容性背书强度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [x] 大内存进程扫描核心指标达到 issue 指定门槛（随机/顺序/并发）
- [x] 页表额外内存开销约束测试通过

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本轮 QA 验收；若进入更高发布门槛，建议后续补充 Python 2.7、不同 glibc、不同 Node.js 版本的兼容性矩阵复测。
