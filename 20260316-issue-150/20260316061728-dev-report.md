## Summary
本轮修复了 VM V3 顺序扫描热路径的常数开销问题，通过优化 `MemoryIterator` 的对齐读取路径，使 `BenchmarkSequentialScan` 与 `BenchmarkSequentialScanPageTable` 在复测中稳定低于 `3ns/op`。

## Changes
- `common/vm/v3/batch.go`: 抽取 `bindSegmentData()`，改为缓存 `wordBase` 指针并直接执行 8 字节顺序读取，减少 iterator 热路径开销
- `dev-log/2026-03-16-vm-v3-qa-fix-iteration-31.md`: 记录本轮修复、测试结果与验收状态

## Testing
- `./maze --build`
- `go test -tags vm_v3 ./common/vm/v3 -run 'Test(ConcurrentLazyLoadRaceFree|BatchReadAPIContract|BatchReadHandlesShortResultsBuffer|MemoryIteratorTraversesAcrossSegments)$' -count=1`
- `go test -tags vm_v3 ./common/vm -run 'TestV3(ComparePerformanceGuard|CompareBenchmarksPresent|SparseFixturesExerciseNonLinearPaths)$' -count=1`
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- `go test -tags vm_v3 ./common/vm/...`
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|ConcurrentAccess|SequentialScan|SequentialScanPageTable)$' -benchtime=200ms -count=5`
- `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3' -benchtime=80ms -count=1`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`

结论：PARTIAL。已修复最新 QA 的 AC4 问题，但完整回归矩阵仍未全部验证。
