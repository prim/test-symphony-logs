## QA Result: FAIL

## Findings
- 发现 1：`common/vm/v3/` 目录、`common/vm/select_v3.go`、页表映射、批量读取 API、compare benchmark 与 race 测试均已就位；`GOFLAGS='-tags=vm_v3' ./maze --build`、`go test -tags vm_v3 ./common/vm/...`、`go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`、`go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` 全部通过，说明 AC1、AC2、AC6、AC7、AC8、AC9 的主链路成立。
- 发现 2：性能门槛仍未完全满足。`go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3` 三次结果分别为 `3.377 ns/op`、`3.394 ns/op`、`3.464 ns/op`，持续高于 issue 要求的 `< 3ns/op`，因此 AC4 失败。
- 发现 3：本轮按要求实际执行了 testdata 回归，`python/20260129-complex-types-311` 与 `nodejs/20260225-maze-vs-heapsnapshot` 在 `GOFLAGS='-tags=vm_v3'` 下均通过，未见 Python / Node.js 主链路退化。

## Test Results

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，包含 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go`、`test_helpers.go`、`v3_contract_test.go`；`common/vm/select_v3.go` 已注册 `VMV3` 实现并在 `init()` 中绑定；`GOFLAGS='-tags=vm_v3' ./maze --build` 成功，`go test -tags vm_v3 ./common/vm/...` 通过。
- **关键指标**: 构建成功；`TestVMV3BuildTagCompilesAndSelectsV3`、`TestVMV3TypeRegisteredWhenBuiltWithTag`、`TestSwitchToV3BindsImplementation` 所在测试包整体通过。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，未出现 data race。
- **关键指标**: race 检测通过，退出码为 0。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/...` 整体通过，覆盖 `TestBatchReadAPIContract`、`TestBatchReadHandlesShortResultsBuffer`、`TestMemoryIteratorTraversesAcrossSegments`、`TestPageTableLookupAndFallback`、`TestGetMemoryRangeHandlesUnalignedAndCrossSegment`、`TestDirectReadHandlesUnalignedAddress`、`TestClosePublishesNewStateBeforeClosingOldReader` 等关键合约测试。
- **关键指标**: v3 包测试全部通过。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 静态检查 `common/vm/v3/v3_contract_test.go`，确认已定义 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **关键指标**: AC8 要求的场景均存在。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在 `BenchmarkCompareV3GetMemory8/4`、`BenchmarkCompareV3SequentialScan`、`BenchmarkCompareV3RandomAccess`、`BenchmarkCompareV3ConcurrentAccess`、`BenchmarkCompareV3GetMemoryRange`、`BenchmarkCompareV3ColdStart`；`go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` 通过。
- **关键指标**: compare guard 测试通过，执行时长约 70s。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: `GOFLAGS='-tags=vm_v3' python3 testdata/run_test.py python/20260129-complex-types-311` 通过；`GOFLAGS='-tags=vm_v3' python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 通过。
- **关键指标**: Python 用例 `pymempool_objects = 68626`；Node.js 用例 `derived total_objects = 52307`，目标类型统计均满足阈值。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/...` 已覆盖 `TestPageTableMemoryOverhead` 并通过。
- **关键指标**: 页表开销测试通过，未触发 `overhead > limit` 失败条件。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 随机访问与并发访问相关守护测试通过，但顺序扫描基准持续高于门槛。
- **关键指标**:
  - `BenchmarkSequentialScan`: `3.377 / 3.394 / 3.464 ns/op`
  - 目标门槛: `< 3 ns/op`

## 失败详情

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: V3 顺序扫描延迟应稳定低于 `< 3ns/op`，满足 AC4。
- **实际行为**: 多次 benchmark 结果均高于门槛，无法证明 AC4 达成。
- **复现步骤**:
  1. 在项目根目录执行 `GOFLAGS='-tags=vm_v3' ./maze --build`
  2. 执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3`
  3. 观察 `BenchmarkSequentialScan` 输出约 `3.37~3.46 ns/op`
- **可能原因**: 当前 `MemoryIterator.Next8()` 与 runtime fast path 虽已优化，但顺序扫描热路径仍未达到 issue 设定的绝对阈值；从结果看已接近目标，但仍存在稳定性/常数项开销。
- **建议修复**: 继续聚焦顺序扫描热路径的常数开销优化，并在同一基准环境下复测到稳定低于 `3ns/op` 后再关闭 AC4。

## 未执行项
- 无。阶段 A 静态检查、阶段 B 关键 smoke、阶段 C 代表性回归均已执行；本轮未做额外高成本的 Node.js comprehensive 数据重建，符合 human guidance 中“忽略 Node.js comprehensive 回归数据生成链路”的说明。

## 执行记录
- `./maze --build`
- `GOFLAGS='-tags=vm_v3' ./maze --build`
- `go test -tags vm_v3 ./common/vm/...`
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1`
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3`
- `GOFLAGS='-tags=vm_v3' python3 testdata/run_test.py python/20260129-complex-types-311`
- `GOFLAGS='-tags=vm_v3' python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`

## 结论
- 当前实现已满足大部分功能性、并发安全、回归兼容性与比较测试要求。
- 但 AC4（顺序扫描 `< 3ns/op`）仍未达标，因此本轮 QA 结论为 **FAIL**。
