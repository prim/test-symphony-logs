# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

阶段 A 静态检查与阶段 B 关键 smoke test 已执行。`./maze --build` 通过，`go test -tags vm_v3 ./common/vm/...`、并发生命周期 race 检查、V3 vs V2 对比守护测试均通过；但关键 Python 主链路 smoke test `python/20260129-complex-types-311` 运行失败，Maze 在分析过程中发生 panic，因此 AC7 不成立，整体验收失败。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 存在且带 `vm_v3` build tag；`./maze --build` 成功，`go test -tags vm_v3 ./common/vm/... -count=1` 成功。
- **关键指标**: `./maze --build` 通过；`go test -tags vm_v3 ./common/vm/...` 用时约 94.687s，`maze/common/vm` 与 `maze/common/vm/v3` 均通过。
- **备注**: 相对 `origin/master...HEAD` 未发现 `cmd/*.py` 改动，满足人审“不要改 cmd/*.py”约束。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: 并发生命周期与首访窗口相关 race 检查通过，未报告 data race。
- **关键指标**: `go test -race -tags vm_v3 ./common/vm -run 'TestSwitchKeepsPreviousImplementationReadableUntilNewInitCompletes|TestDispatchInitKeepsCurrentImplementationReadableUntilReinitCompletes|TestCloseCurrentWaitsForInflightReaders|TestSwitchWaitsForInflightReadersBeforeClosingPreviousImplementation|TestDispatchInitWaitsForInflightReadersBeforeReinit|TestDispatchInitCanInitializeBeforeInflightReadersDrain|TestConcurrentFacadeReadsDuringSwitch' -count=1` 通过，用时约 1.130s。
- **备注**: 静态审查显示 `common/vm/v3/nocopy.go` 与 `common/vm/facade.go` 使用原子快照/关闭期排空机制承载无锁读取与切换。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 该类 contract 测试包含在 `go test -tags vm_v3 ./common/vm/... -count=1` 中并整体通过，说明 batch API、页表回退与跨 segment iterator 基本契约成立。
- **关键指标**: 相关测试函数存在：`TestBatchReadAPIContract`、`TestBatchReadHandlesShortResultsBuffer`、`TestMemoryIteratorTraversesAcrossSegments`、`TestPageTableLookupAndFallback`、`TestGetMemoryRangeHandlesUnalignedAndCrossSegment`。
- **备注**: 本轮未单独拆跑这些测试，但已被全量 VM 测试覆盖。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 静态检查确认 V3 benchmark 覆盖随机访问、顺序扫描、并发访问、热点访问、未对齐、跨 segment、cache miss、安全读取、混合负载、冷启动、Python 对象遍历、V8 堆扫描等场景；对比测试中也存在覆盖校验。
- **关键指标**: 在 `common/vm/v3/v3_contract_test.go` 中找到 17 个目标 benchmark；`TestV3CompareBenchmarksPresent` 通过。
- **备注**: 场景覆盖满足 AC8 的“存在性/完整性”要求。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: V3 vs V2 对比守护测试通过，说明 compare benchmark 集合存在且可执行，稀疏页表 fixture 也能覆盖非线性路径。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1` 通过，用时约 96.602s。
- **备注**: 该结果可支撑 AC9 的一部分，但是否“所有场景都优于 V2”仍以具体 benchmark 输出为准。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: FAIL
- **实际行为**: Python 主链路 smoke test 失败。执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 时，Maze 在分析 tarball 过程中 panic 退出，未生成通过结果。
- **关键指标**: 退出码 2；日志 `tmp/python-20260129-complex-types-311.maze-output.log` 第 55 行出现 `panic: runtime error: invalid memory address or nil pointer dereference`。
- **备注**: 调用栈显示崩溃点位于 `maze/common/vm/v2.GetMemoryRange` → `maze/common/vm/v2.GetMemory4` → `maze/common/vm.dispatchGetMemory4`，随后在 `mallocer/pymalloc.locateMemoryPool()` 路径触发，属于主链路阻塞性失败。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: 页表内存开销约束测试已包含在 VM 全量单测中并通过。
- **关键指标**: `TestPageTableMemoryOverhead` 存在且包含在 `go test -tags vm_v3 ./common/vm/... -count=1` 的通过集合中。
- **备注**: 本轮未单独输出百分比明细，但测试未失败。

### TC-008: 性能目标门槛校验
- **结果**: 未执行
- **实际行为**: 未执行专门 benchmark 命令去复核 AC3/AC4/AC5 的 ns/op 阈值。
- **关键指标**: 无。
- **备注**: 因关键 smoke test 已证明 Python 主链路不成立，继续执行高成本性能基准对当前验收结论帮助有限，按早停策略停止阶段 C 的高成本动态验证。

## 失败的 Test Case

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: FAIL
- **预期行为**: `./maze --build` 后，`python3 testdata/run_test.py python/20260129-complex-types-311` 与 Node.js 主链路用例应通过，且无明显统计退化。
- **实际行为**: Python 用例在分析过程中 panic，报错为 `panic: runtime error: invalid memory address or nil pointer dereference`，程序退出码为 2。
- **复现步骤**:
  1. 在仓库根目录执行 `./maze --build`
  2. 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  3. 观察输出与日志 `tmp/python-20260129-complex-types-311.maze-output.log`
- **可能原因**: 从调用栈看，`dispatchGetMemory4()` 最终进入 `common/vm/v2.GetMemoryRange()` 时发生空指针解引用。说明当前默认/回退 VM 路径在该 Python tarball 的读取场景下存在未初始化 reader、segment 映射失效，或 facade 切换/绑定后的实现状态不完整问题。
- **建议修复**:
  - 优先检查 `common/vm/v2/vm.go:51` 附近 `GetMemoryRange` 的 reader/segment 生命周期前置条件；
  - 检查 `common/vm.go` 中 VM 选择与 fallback 逻辑是否导致 Python 主链路仍走到不完整的 V2 状态；
  - 复核 `common/vm/facade.go` 改动后，`dispatchGetMemory4` 在主流程初始化期间是否可能访问到未完全准备好的实现。

## 未执行项

- TC-006 的 Node.js 回归用例 `testdata/nodejs/20260225-maze-vs-heapsnapshot`
  - **未执行原因**: Python 主链路 smoke test 已发生阻塞性 panic，已足以证明 AC7 不成立；继续执行 Node.js 高成本动态测试对本轮 PASS/FAIL 结论无改变。
  - **可能影响 AC**: AC7
- TC-008 的性能门槛 benchmark 复测
  - **未执行原因**: 主链路阻塞下继续执行性能基准收益较低，按“低成本检查尽量完整 + 高成本验证分阶段早停”策略停止。
  - **可能影响 AC**: AC3、AC4、AC5、AC9

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试已执行（但主链路失败）
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- 通过的检查：`./maze --build`、`go test -tags vm_v3 ./common/vm/... -count=1`、`go test -race -tags vm_v3 ./common/vm ...`、`go test -tags vm_v3 ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1`
- 失败的检查：`python3 testdata/run_test.py python/20260129-complex-types-311`
- 静态附加结论：未发现 `cmd/*.py` 改动，满足人审约束。

## 总结
- 通过: 6 / 8
- 失败: 1 / 8
- 未执行: 1 / 8
- 建议: **当前不建议发布。** 需要先修复 Python 主链路 panic，再继续执行 Node.js 回归与性能门槛复测，才能确认 AC3/AC4/AC5/AC7/AC9 的最终验收状态。
