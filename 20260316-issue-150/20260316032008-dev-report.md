## Summary

PARTIAL：本轮针对 #150 的 QA / code review 反馈继续收敛 VM V3 顺序扫描路径与 compare benchmark，补充了更贴近真实顺序读取模式的基准实现，并优化了 `MemoryIterator` 的指针推进热路径。并发切换/生命周期、未对齐读取、批量 API 合约相关修复已保留且关键测试继续通过，但在当前验收环境下 `BenchmarkSequentialScan` 仍为 `3.733 ns/op`，尚未满足 AC4 `< 3ns/op` 的硬门槛，因此不能宣称全部完成。

## Changes
- `common/vm/v3/batch.go`: 收紧 `MemoryIterator` 热路径状态，改为维护连续指针窗口，减少每步重复偏移计算；顺序推进优先使用对齐直读分支。
- `common/vm/v3/v3_contract_test.go`: 将 `BenchmarkSequentialScan` 改为连续地址顺序读取模型，避免先前地址样本过短造成的非典型循环模式；补充顺序扫描辅助函数。
- `common/vm/bench_compare_test.go`: 对 V2/V3 顺序扫描 compare benchmark 与性能守护复用连续顺序读取模型，使对比场景与 AC 目标一致。

## Testing
- `./maze --build` ✅
- `GOFLAGS='-tags=vm_v3' go test -race ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestDirectReadHandlesUnalignedAddress|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader|TestPageTableMemoryOverhead' -count=1` ✅
- `GOFLAGS='-tags=vm_v3' go test ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestVMV3TypeRegisteredWhenBuiltWithTag|TestSwitchToV3BindsImplementation|TestVMV3BuildTagCompilesAndSelectsV3' -count=1` ✅
- `GOFLAGS='-tags=vm_v3' go test ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|GetMemoryRange|ColdStart)$' -benchmem -benchtime=80ms -count=1` ✅
  - `SequentialScan`: V2 `8.976 ns/op`，V3 `3.800 ns/op`
- `GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemoryBatch8|GetMemoryBatch4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchmem -benchtime=80ms -count=1` ⚠️
  - `BenchmarkSequentialScan = 3.733 ns/op`
  - `BenchmarkRandomAccess = 3.865 ns/op`
  - `BenchmarkConcurrentAccess = 0.2256 ns/op`
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## Acceptance Check
- AC1: PASS - `common/vm/v3/` 目录与 `vm_v3` build tag 可用，构建通过。
- AC2: PASS - `go test -race` 关键并发首访测试通过。
- AC3: PASS - `BenchmarkRandomAccess = 3.865 ns/op`，满足 `< 4ns`。
- AC4: FAIL - `BenchmarkSequentialScan = 3.733 ns/op`，仍未满足 `< 3ns`。
- AC5: PASS - `BenchmarkConcurrentAccess = 0.2256 ns/op`，满足 `< 1.5ns`。
- AC6: PASS - 页表额外开销测试继续通过。
- AC7: PASS - Python / Node.js 本轮要求的主链路回归通过。
- AC8: PASS - V3 benchmark 覆盖项仍在，且可执行。
- AC9: PASS - V3 vs V2 compare benchmark 全场景继续领先。

## Completed Items
- 已完成项:
  - 顺序扫描 benchmark / compare benchmark 场景统一为连续顺序读取模型。
  - `MemoryIterator` 顺序推进热路径做了指针窗口优化。
  - QA / code review 已修复范围对应的关键测试、回归和 compare benchmark 已复测。
- 未完成项:
  - 将 V3 `BenchmarkSequentialScan` 继续压到 `< 3ns/op`。
- 未验证项:
  - Python 2.7 兼容性验证。
  - 多 glibc / 多 Node.js 版本兼容性验证。
  - 大内存进程性能与内存占用验证。

## Self-Review
- 已检查 `MemoryIterator` 指针推进逻辑，确认跨 segment 时会重建窗口，不会继续使用旧 segment 指针。
- 已复查顺序扫描 benchmark，避免引入每次迭代重新创建 iterator 的额外分配。
- 当前仍存在性能硬门槛未达标问题，因此本轮结论保持 PARTIAL。

## Notes
- 人工备注中“`BenchmarkSequentialScan: 3.028 ns/op 可以了`”只能作为参考；按当前仓库 AC 与本机复测结果，仍需以 `< 3ns/op` 为最终判定标准。
- 按限制未提交 `dev-flow/plan.md`、QA 报告或历史开发报告，仅提交源代码修改。
