## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #150 最近一次修复（主要是 VM V3 facade 绑定与关闭期 reader 生命周期保护）进行了静态核对、关键 Go 测试、race 验证、性能抽样以及 Python / Node.js testdata 回归。未发现新的阻断问题。

## Findings
- 本轮修复已将 `common/vm/select_v3.go` 的 VMV3 注册入口绑定到 `v3.GetMemory*` / `v3.GetMemoryRange` 公共受保护入口，避免关闭期间绕过 `acquireReadState()` 协议。
- 代码变更范围未包含 `cmd/*.py`，符合人审反馈“不要改 cmd/*.py”的强约束。
- 关键功能、并发安全、性能门槛抽样及 Python / Node.js 主链路回归均通过。
- `maze.py.log` 文件未生成；本轮已检查 `maze.log`，未见 fatal / panic。`maze.py.log` 缺失未影响本轮用例通过，但作为观测项记录。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，包含 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go` 等核心文件；`common/vm/select_v3.go` 存在且在 `vm_v3` build tag 下注册 VMV3。
- **关键指标**: `./maze --build` 成功；`go test -tags vm_v3 ./common/vm` 相关选择测试通过。
- **备注**: 静态核对与动态构建均满足 AC1。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，无 race 报告。
- **关键指标**: race 检测通过；测试内部断言 segment 仅加载一次。
- **备注**: 满足 AC2。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 通过阅读 `common/vm/v3/v3_contract_test.go` 确认存在 `TestBatchReadAPIContract`、`TestBatchReadHandlesShortResultsBuffer`、`TestMemoryIteratorTraversesAcrossSegments`、`TestPageTableLookupAndFallback`、`TestGetMemoryRangeHandlesUnalignedAndCrossSegment` 等契约测试；本轮同时运行了 V3 合约测试子集并通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestCloseWaitsForInFlightFastPathReader|TestCloseWaitsForInFlightPublicGetMemoryReader|TestClosePublishesNewStateBeforeClosingOldReader|TestConcurrentLazyLoadRaceFree|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: API 合约覆盖到短缓冲区、跨 segment、未对齐和 fallback 路径。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 通过静态检查 `common/vm/bench_compare_test.go` 与 `common/vm/v3/v3_contract_test.go`，确认 V3 benchmark / compare benchmark 已覆盖随机访问、顺序扫描、并发访问、未对齐、跨 segment、cache miss、安全读取、mixed workload、cold start、Python/V8 场景等要求。
- **关键指标**: `BenchmarkCompareV3*` 在 `common/vm/bench_compare_test.go` 中共识别到 10 个；`TestV3CompareBenchmarksPresent` 通过。
- **备注**: 满足 AC8 的覆盖要求。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在对比基准与 `TestV3CompareBenchmarksPresent`、`TestV3SparseFixturesExerciseNonLinearPaths`；本轮执行对应测试均通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1` 通过。
- **备注**: 本轮以静态存在性和关键非线性路径验证为主，未重跑全部 compare benchmark。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 各执行 1 个 testdata 用例，均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：PASS
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，且覆盖 AC7 的两条主语言链路。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过，验证 V3 元数据额外开销未超过阈值。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run '...|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: 满足 AC6。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 本轮抽样执行 V3 关键 benchmark，结果均达到 issue 中的目标门槛。
- **关键指标**:
  - `BenchmarkRandomAccess-48`: **3.865 ns/op**，满足 `< 4ns`
  - `BenchmarkSequentialScanPageTable-48`: **2.832 ns/op**，满足 `< 3ns`
  - `BenchmarkConcurrentAccessPageTable-48`: **0.9258 ns/op**，满足 `< 1.5ns`
- **备注**: 对应 AC3 / AC4 / AC5 抽样通过。

## Test Results
- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm -run 'TestVMV3ImplementationUsesProtectedPublicEntryPoints|TestFacadeDispatchUsesCurrentImplementationWithoutRebindingFunctions|TestSwitchToV3BindsImplementation|TestCloseCurrentUsesBoundImplementation' -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestCloseWaitsForInFlightFastPathReader|TestCloseWaitsForInFlightPublicGetMemoryReader|TestClosePublishesNewStateBeforeClosingOldReader|TestConcurrentLazyLoadRaceFree|TestPageTableMemoryOverhead' -count=1`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`：通过
- `go test -tags vm_v3 ./common/vm -run 'TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchtime=200ms -count=1`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 失败的 Test Case（如有）

无。

## 未执行项
- 未重跑 `go test -tags vm_v3 ./common/vm/...` 全量测试集。
- 未重跑全部 compare benchmark 子项，仅执行覆盖存在性测试与关键性能抽样。
- 未执行更高成本的大内存进程（>1GB）扫描性能测试。
- 未执行 Python 2.7、不同 glibc 版本、不同 Node.js 版本的跨环境兼容性复测。

### 未执行原因
- 依据“低成本检查尽量完整 + 高成本验证分阶段早停”策略，本轮在关键构建、关键功能、race、性能抽样和双语言主链路回归均通过后，未继续扩展到全部高成本环境与全量性能回归。

### 可能影响范围
- 主要影响 AC7 的跨版本兼容性广度结论，以及 AC8 / AC9 的全量 benchmark 完整性确认粒度。
- 但不影响本轮对最近修复点（public entrypoint 绑定与关闭期 reader 保护）的验收结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] 随机访问延迟 < 4ns
- [x] 顺序扫描延迟 < 3ns
- [x] 并发访问延迟 < 1.5ns

## 代码与范围核对
- 已核对 `git diff origin/master...HEAD` 变更范围。
- 本轮变更未涉及 `cmd/*.py`，符合人审限制。
- `common/vm/select_v3.go` 已将绑定切换到 `v3.GetMemory` / `v3.GetMemorySafe` / `v3.GetMemory4` / `v3.GetMemory4Safe` / `v3.GetMemoryRange` 公共入口。
- `common/vm/facade_dispatch_test.go` 新增了对 VMV3 绑定目标的显式校验，可防止再次误绑到无关闭保护入口。
- `common/vm/v3/v3_contract_test.go` 新增公共 `GetMemory` 与关闭并发等待场景，覆盖最近修复点。

## 日志检查
- 已检查 `maze.log`，未见 fatal / panic；存在少量 `No symbol ... in current context` 的 DEBUG 级别 GDB 信息，属于既有探测类日志，不影响本轮 testdata 用例通过。
- `maze.py.log` 本轮未生成，故无法进行对应日志核对。

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本轮 QA 验收。若要形成更强发布信心，后续可补跑全量 compare benchmark 与跨版本兼容性矩阵，但这不构成本轮阻塞。
