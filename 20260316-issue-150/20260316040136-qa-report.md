# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮完成了需求文档、测试用例、最新开发报告、关键代码路径与动态测试的核对。`common/vm/v3` 的目录结构、build tag、无锁懒加载 race 检查、页表开销约束、批量读取/迭代器合约、Python/Node.js 主链路回归均通过；但 **AC4 / TC-008 的顺序扫描性能目标仍未达标**，因此总体结论为 FAIL。

## Findings

- 发现 1：`BenchmarkSequentialScan` 实测仍高于需求阈值 `< 3ns/op`，本轮两次实测分别为 `3.299 ns/op`（覆盖矩阵跑法）与 `3.386 / 3.394 / 3.389 ns/op`（复测 3 次），不满足 AC4。
- 发现 2：`go test -tags vm_v3 ./common/vm/...`、`go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree`、`go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard` 均通过，说明 V3 的注册、切换、无锁加载与核心性能守护链路基本成立。
- 发现 3：Python 与 Node.js 关键回归用例通过；按人工指导，未执行 `Node.js comprehensive` 数据生成链路。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，包含 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go` 等关键文件；`select_v3.go` 已注册 `VMV3` 实现并在 `init()` 中绑定；`go test -tags vm_v3 ./common/vm/...` 通过。
- **关键指标**: `maze/common/vm` 测试通过；`maze/common/vm/v3` 测试通过。
- **备注**: 静态检查确认 `SwitchToV3()`、`bindV3()`、`VMV3` 类型均已接线。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，未出现 data race。
- **关键指标**: race 测试通过，执行时长约 `1.030s`。
- **备注**: 代码中 `Segment.ensureLoaded()` 使用 `atomic.Pointer` / `atomic.Uint64` 控制加载状态，符合无锁首访加载设计方向。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 合约与边界测试通过，包括批量读取、短结果缓冲区、跨 segment 迭代、页表查找 fallback、未对齐与跨段范围读取、关闭期状态发布等。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead|TestDirectReadHandlesUnalignedAddress|TestClosePublishesNewStateBeforeClosingOldReader' -count=1` 通过。
- **备注**: `GetMemoryBatch8/4`、`MemoryIterator.Next8()`、`PageTableMemoryBytes()` 等接口均已落地并被测试覆盖。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `v3_contract_test.go` 中存在并成功执行以下 benchmark：`BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **关键指标**: 基准矩阵命令完整通过。
- **备注**: 覆盖范围满足 AC8 列出的必需/应有/可选场景。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 存在 `BenchmarkCompareV3GetMemory8/4`、`BenchmarkCompareV3SequentialScan`、`BenchmarkCompareV3RandomAccess`、`BenchmarkCompareV3ConcurrentAccess`、`BenchmarkCompareV3GetMemoryRange`、`BenchmarkCompareV3ColdStart` 以及 `TestV3ComparePerformanceGuard`；守护测试执行通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` 通过，执行时长约 `69.736s`。
- **备注**: 本 TC 的“存在且可执行”目标已满足；但 AC9 的“所有场景优于 V2”仍主要依赖守护测试范围，而本轮失败主因不在此项。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 复杂类型用例与 Node.js 准确性对比用例均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅
- **备注**: 按人工指导，忽略 `Node.js comprehensive` 回归数据生成链路；此次未将其作为阻塞项。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过，说明页表附加开销未突破测试阈值。
- **关键指标**: 页表内存开销测试通过。
- **备注**: 静态代码显示 `PageTable.MemoryBytes()` 仅统计填充 entry 与 sub table 结构，测试结果与 AC6 一致。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 随机访问与并发访问满足目标，但顺序扫描持续高于目标阈值。
- **关键指标**:
  - `BenchmarkRandomAccess`: `3.872 ns/op`，满足 AC3 `< 4ns/op`
  - `BenchmarkSequentialScan`: `3.299 ns/op`，**不满足** AC4 `< 3ns/op`
  - `BenchmarkConcurrentAccess`: `0.2370 ns/op`，满足 AC5 `< 1.5ns/op`
  - 复测 `BenchmarkSequentialScan` 3 次：`3.386 / 3.394 / 3.389 ns/op`
- **备注**: 该失败直接阻断 issue #150 的性能验收。

## 失败的 Test Case

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: `BenchmarkSequentialScan` 稳定低于 `3ns/op`。
- **实际行为**: 本轮矩阵 benchmark 为 `3.299 ns/op`；单项复测 3 次为 `3.386 / 3.394 / 3.389 ns/op`，持续超标。
- **复现步骤**:
  1. 在项目根目录执行 `./maze --build`
  2. 执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3`
  3. 观察输出中的 `BenchmarkSequentialScan` 指标
- **可能原因**: `MemoryIterator.Next8()` 热路径已优化，但边界判断、跨 segment 推进或非线性分支开销仍未压到目标线以下。
- **建议修复**: 继续聚焦顺序扫描热路径，优先缩减 `Next8()` 中循环/分支/边界检查成本，并在修复后重新提交稳定的多次 benchmark 证明。

## 未执行项

- `Node.js comprehensive` 数据生成链路未执行；原因：人工指导明确要求忽略；影响范围：不作为 AC7 阻塞项。
- 未做跨 Python 2.7 / 多 glibc / 多 Node.js 版本的环境矩阵复测；原因：当前轮次优先验证 issue #150 的主链路与性能门槛；影响范围：兼容性勾选项暂不能置为通过。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] 随机访问延迟 < 4ns
- [ ] 顺序扫描延迟 < 3ns
- [x] 并发访问延迟 < 1.5ns
- [x] 页表额外内存开销 < 20%

## Test Results

- `./maze --build` ✅
- `go test -tags vm_v3 ./common/vm/...` ✅
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead|TestDirectReadHandlesUnalignedAddress|TestClosePublishesNewStateBeforeClosingOldReader' -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchmem -benchtime=120ms -count=1` ✅（但顺序扫描指标未达门槛）
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3` ✅（结果持续超标）
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## 总结
- 通过: 7 / 8
- 失败: 1 / 8
- 建议: **暂不可以发布**。除顺序扫描性能门槛外，其余主链路与关键回归基本通过；请优先修复 AC4 对应性能问题后再发起 QA 复验。
