# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 已注册 VMV3 实现；`./maze --build` 成功，`go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestSwitchToV3BindsImplementation' -count=1` 通过。
- **关键指标**: build 成功；VMV3 选择测试通过。
- **备注**: AC1 从功能角度满足。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- **关键指标**: race detector 无 data race；并发首访测试通过。
- **备注**: AC2 满足。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: `TestBatchReadAPIContract`、`TestBindSourceInvalidatesStaleSegmentData`、`TestClosePublishesNewStateBeforeClosingOldReader` 均通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: 批量 API、source 重新绑定与关闭时状态发布行为均已覆盖。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 在 `common/vm/v3/v3_contract_test.go` 中静态核对到 17 个 benchmark，覆盖 issue/AC 要求的随机访问、顺序扫描、并发访问、热点访问、未对齐、跨 segment、cache miss、安全读取、混合负载、冷启动、Python 对象遍历、V8 heap 扫描等场景。
- **关键指标**: 命中 `BenchmarkGetMemory8/4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **备注**: AC8 的覆盖性要求满足。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在 V3 对比 benchmark 与性能守护测试；`go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestSwitchToV3BindsImplementation|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths|TestV3ComparePerformanceGuard' -count=1` 通过。
- **关键指标**: compare benchmark 名称与 page-table 非线性 fixture 检查通过；性能守护测试通过。
- **备注**: AC9 从测试实现与本地执行结果看满足。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 用例 `python/20260129-complex-types-311` 与 Node.js 用例 `nodejs/20260225-maze-vs-heapsnapshot` 均通过。
- **关键指标**: Python `pymempool_objects = 68626`；Node.js `derived total_objects = 52307`，关键类型计数均达阈值。
- **备注**: AC7 满足。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: AC6 满足。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 主性能 benchmark 达到 issue 中 AC3/AC4/AC5 目标。
- **关键指标**:
  - `BenchmarkRandomAccess`: **3.921 ns/op** `< 4ns`
  - `BenchmarkSequentialScan`: **2.841 ns/op** `< 3ns`
  - `BenchmarkConcurrentAccess`: **0.2251 ns/op** `< 1.5ns`
- **备注**: 主指标满足要求。补充观察：`PageTable` 场景下 `BenchmarkRandomAccessPageTable = 13.43 ns/op`，该结果不属于 issue 明确 AC 阈值，但提示页表路径随机访问仍明显慢于主随机访问路径，建议后续持续关注。

### 阶段 A 额外静态检查: 人审强约束与范围核对
- **结果**: FAIL
- **实际行为**: 尽管最新开发报告声明“未触碰 human review 明确禁止修改的 `cmd/*.py`”，但相对基线分支的变更列表中仍包含 `cmd/maze-gen-coredump.py` 与 `cmd/maze-tar-coredump.py`，与人审反馈“不要改 cmd/*.py”直接冲突。
- **关键指标**: `git diff --name-only origin/master...HEAD` 包含上述两个文件；`git diff --stat origin/master...HEAD -- cmd/maze-gen-coredump.py cmd/maze-tar-coredump.py` 显示共 **152 insertions / 60 deletions**。
- **备注**: 这是明确的范围/约束违例。按 QA 规则，即使动态测试通过，整体仍应判定为 FAIL。

## 失败的 Test Case（如有）

### TC-009: 人审禁止修改 cmd/*.py 约束
- **结果**: FAIL
- **预期行为**: 本轮修复不应修改任何 `cmd/*.py` 文件，至少相对基线提交不应再包含 `cmd/maze-gen-coredump.py`、`cmd/maze-tar-coredump.py` 的改动。
- **实际行为**: 分支差异仍包含上述两个 Python 脚本修改。
- **复现步骤**:
  1. 在仓库根目录执行 `git diff --name-only origin/master...HEAD`。
  2. 观察输出中存在 `cmd/maze-gen-coredump.py` 与 `cmd/maze-tar-coredump.py`。
  3. 执行 `git diff --stat origin/master...HEAD -- cmd/maze-gen-coredump.py cmd/maze-tar-coredump.py`。
  4. 可见这两个文件仍有实质性代码改动。
- **可能原因**: 修复迭代虽清理了 `common/vm/v3/vm.go` 的热缓存遗留逻辑，但此前引入的 `cmd/*.py` 变更未被回退，导致分支仍违反人审范围约束。
- **建议修复**: 由开发者回退或拆分 `cmd/maze-gen-coredump.py`、`cmd/maze-tar-coredump.py` 的改动，确保当前 issue/PR 仅保留 VM V3 相关实现与必要测试/文档改动后再重新提交 QA。

## Findings
- 人审强约束被违反：分支仍修改了 `cmd/maze-gen-coredump.py` 和 `cmd/maze-tar-coredump.py`，与“不要改 cmd/*.py”冲突，这是本轮 FAIL 的主因。
- VM V3 主体功能、关键回归、race test、compare guard 与主性能阈值在本地均已通过，说明当前主要问题是范围合规性，而非核心功能正确性。
- 页表路径代表性补充 benchmark 中，`BenchmarkRandomAccessPageTable` 为 13.43ns/op，虽不直接违反 issue AC，但值得继续优化或至少在设计/文档中解释。

## Test Results
- `./maze --build` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader|TestPageTableMemoryOverhead' -count=1` ✅
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestSwitchToV3BindsImplementation|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths|TestV3ComparePerformanceGuard' -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem -benchtime=200ms -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccessPageTable|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchmem -benchtime=200ms -count=1` ✅（补充观测）

## 未执行项（如有）
- 未执行完整的全部 benchmark 矩阵与所有 `go test ./...` 全仓回归。
- 原因：本轮已通过低成本静态核对与关键动态验证，且已发现明确的人审范围违例；继续扩大全量高成本回归不会改变本轮 FAIL 结论。
- 可能影响范围：不会影响对本轮主阻塞问题（人审约束违例）的判断；但未执行的全量回归仍可能隐藏与 AC7/AC8/AC9 无关的边缘问题。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 9
- 失败: 1 / 9
- 建议: **当前不建议合入/发布。** 在核心功能与主要回归基本通过的前提下，需先处理人审明确禁止的 `cmd/*.py` 改动，再进入下一轮 QA。
