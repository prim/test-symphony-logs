# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #150 的最近修复点进行了阶段化验收：
- 阶段 A（低成本静态检查）已完成：核对需求、test case、最新开发报告、变更范围与人审约束。
- 阶段 B（关键 smoke test）已完成：`./maze --build`、VM V3 关键 Go 测试、race 测试、Python 与 Node.js 主链路 testdata 用例均通过。
- 阶段 C（扩展回归）已完成关键性能与页表开销验证：性能门槛与 V2 对比基准满足 AC3/AC4/AC5/AC9，本轮未执行更大兼容性矩阵。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 在 `vm_v3` build tag 下注册并绑定 `VMV3`；`go test -tags vm_v3 ./common/vm/...` 通过，`v3_select_tag_test.go` / `v3_select_test.go` 覆盖选择逻辑。
- **关键指标**: `go test -tags vm_v3 ./common/vm/...` 通过
- **备注**: 同时检查到未修改 `cmd/*.py`，符合人审约束。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad|TestMemoryIteratorReleasesReadOwnerAfterExhaustion|TestMemoryIteratorCloseReleasesReadOwner|TestLinearSpanShortRangeDoesNotUnderflow'` 通过；覆盖首访并发、陈旧加载失效、iterator 生命周期与短 span 边界。
- **关键指标**: race 检测通过，无 data race
- **备注**: `Segment.ensureLoaded()` 的代际校验与 read-state 生命周期修复已被回归测试覆盖。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/...` 通过，`v3_contract_test.go` 中批量读取、短结果缓冲区、跨 segment iterator、未对齐/跨 segment range 读取等用例均可通过。
- **关键指标**: 合约测试集通过
- **备注**: `MemoryIterator` 已具备 `Close()` 与耗尽后释放 owner 的行为验证。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 静态检查确认 `common/vm/v3/v3_contract_test.go` 中已定义 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **关键指标**: 必需 benchmark 场景齐全
- **备注**: 满足 AC8 要求的覆盖范围。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 包含全部 CompareV3 benchmark；执行 `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchmem` 通过，V3 在已测对比项中优于 V2。
- **关键指标**:
  - GetMemory8: V2 `19.08 ns/op`，V3 `17.36 ns/op`
  - SequentialScanPageTable: V2 `28.10 ns/op`，V3 `22.88 ns/op`
  - ConcurrentAccessPageTable: V2 `3.529 ns/op`，V3 `3.009 ns/op`
- **备注**: `TestV3ComparePerformanceGuard` 也已通过。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: 依次执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 与 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 均通过。
- **关键指标**:
  - Python: `pymempool_objects = 68626`，关键类型数量均命中预期
  - Node.js: `derived total_objects = 52307`，Map/Set/TypedArray/Promise 等统计均达阈值
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，本轮实际执行了 2 个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/v3 -run TestPageTableMemoryOverhead` 通过。
- **关键指标**: 页表元数据额外开销未超过 V2 基线的 20%
- **备注**: `TestPageTableMemoryOverhead` 同时校验 fixture 不退化为 linear span。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem`，关键指标均达到 issue 中阈值。
- **关键指标**:
  - RandomAccess: `3.941 ns/op`，满足 `< 4ns`
  - SequentialScan: `2.870 ns/op`，满足 `< 3ns`
  - ConcurrentAccess: `0.2344 ns/op`，满足 `< 1.5ns`
- **备注**: 本轮硬件环境下三项 AC3/AC4/AC5 均通过；并发值显著优于门槛，但仍应注意不同机器上的基准噪声。

## Findings
- 未发现阻断发布的功能性问题。
- 本轮变更范围内未出现 `cmd/*.py` 修改，符合人审“不要改 cmd/*.py”要求。
- 工作树存在与本任务无关的未提交改动：`common/utils.go`、`nodejs/map_info.go` 以及若干 `dev-flow/`、`tmp/` 文件；本次 QA 未将其视为 issue #150 的失败项，但开发提交时应避免混入。

## Test Results
- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm/...`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad|TestMemoryIteratorReleasesReadOwnerAfterExhaustion|TestMemoryIteratorCloseReleasesReadOwner|TestLinearSpanShortRangeDoesNotUnderflow'`：通过
- `go test -tags vm_v3 ./common/vm -run 'TestSwitchClosesPreviousImplementation|TestSwitchPublishesOnlyAfterInit|TestCloseCurrentUsesBoundImplementation|TestV3ComparePerformanceGuard'`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run TestPageTableMemoryOverhead`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem`：通过
- `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchmem`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- Python 2.7 兼容性矩阵：本轮未执行
- 多 glibc 版本矩阵：本轮未执行
- 多 Node.js 版本矩阵：本轮未执行
- 全量 CompareV3 benchmark 子项逐一实测：本轮未全部执行，仅执行关键代表场景与守护测试
- **未执行原因**: 本轮以“最近修复点回归 + 主链路 + 关键性能门槛复测”为主，当前无阻塞性失败，故未扩大到完整环境矩阵
- **可能影响的 AC**: 主要影响兼容性置信度补强，不影响本轮对 AC1~AC9 在当前环境中的通过结论

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] 随机访问延迟 < 4ns
- [x] 顺序扫描延迟 < 3ns
- [x] 并发访问延迟 < 1.5ns
- [x] 页表额外内存开销 < 20%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 当前可判定为 **PASS**。就 issue #150 的当前修复内容与本轮复测范围而言，可以继续推进；若准备最终发布，建议后续单独补做 Python 2.7 / 多 glibc / 多 Node.js 版本矩阵验证。
