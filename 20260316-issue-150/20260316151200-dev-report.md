## Summary

修复了本轮 code review 指出的 4 个核心问题：
- 调整 `common/vm/facade.go` 的实现切换发布顺序，确保新实现完成 `Init()` 后才对并发读路径可见，且不再先关闭旧实现再尝试切换。
- 为 `common/vm/v3/vm.go` 的懒加载补齐代际校验，阻止旧 source 的并发加载结果在 `bindSource()/release()` 后重新写回。
- 修复 `linearSpan` 在长度小于 8/4 字节时的 `limit8/limit4` 下溢风险，避免越界快路径命中。
- 为 `common/vm/v3/batch.go` 的 `MemoryIterator` 增加 read-state 生命周期持有与释放机制，并补强对比性能守护测试的稳健性。

结论：**PARTIAL**

## Changes

- `common/vm/facade.go`: 抽出实现查询/发布辅助函数，改为“先 Init，再发布，再关闭旧实现”的两阶段切换。
- `common/vm/facade_lifecycle_test.go`: 新增切换发布时序测试，验证 `Init` 期间不会提前看到新实现。
- `common/vm/v3/vm.go`: 为 `Segment.ensureLoaded()` 增加 loader 代际校验；修复短 linear span 的边界计算。
- `common/vm/v3/batch.go`: 为 `MemoryIterator` 接入 `acquireReadState()/releaseReadState()` 生命周期管理，并增加 `Close()`。
- `common/vm/v3/v3_contract_test.go`: 新增 iterator 生命周期、并发陈旧加载、短 span 边界等回归测试。
- `common/vm/bench_compare_test.go`: 收紧 V3 vs V2 性能守护逻辑，改为更保守的轮次与噪声判定。

## 完成的任务
- [x] 修复 facade 实现切换生命周期发布顺序问题
- [x] 修复 V3 segment 懒加载的代际失效并发问题
- [x] 修复 linear span 在超短 segment 上的下溢边界问题
- [x] 修复 `MemoryIterator` 未受 read-state 保护的生命周期问题
- [x] 补充对应回归测试并完成核心用例验证

## 修改的文件
- `common/vm/facade.go`: 修正全局实现切换状态机
- `common/vm/facade_lifecycle_test.go`: 增加切换发布顺序测试
- `common/vm/v3/vm.go`: 增加 segmentData 代际保护与 short span 边界修复
- `common/vm/v3/batch.go`: 为 iterator 增加 owner 引用与释放逻辑
- `common/vm/v3/v3_contract_test.go`: 增补 iterator/并发/边界回归测试
- `common/vm/bench_compare_test.go`: 改善性能守护稳定性判断

## Testing
- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm/...`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad|TestMemoryIteratorReleasesReadOwnerAfterExhaustion|TestMemoryIteratorCloseReleasesReadOwner|TestLinearSpanShortRangeDoesNotUnderflow'`：通过
- `go test -tags vm_v3 ./common/vm -run 'TestSwitchClosesPreviousImplementation|TestSwitchPublishesOnlyAfterInit|TestCloseCurrentUsesBoundImplementation|TestV3ComparePerformanceGuard'`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/...`
- 测试结果: 通过
- 测试命令: `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad|TestMemoryIteratorReleasesReadOwnerAfterExhaustion|TestMemoryIteratorCloseReleasesReadOwner|TestLinearSpanShortRangeDoesNotUnderflow'`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm -run 'TestSwitchClosesPreviousImplementation|TestSwitchPublishesOnlyAfterInit|TestCloseCurrentUsesBoundImplementation|TestV3ComparePerformanceGuard'`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
- 测试结果: 通过

## 验收核对
- AC1: PASS - `common/vm/v3/` 与 `vm_v3` build/tag 路径未回退，`go test -tags vm_v3 ./common/vm/...` 通过
- AC2: PASS - `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad'` 通过
- AC3: PARTIAL - 本轮未重新采集完整随机访问 benchmark 数值，但未修改其主路径算法，且历史 QA 结果为 PASS
- AC4: PARTIAL - 本轮未重新采集完整顺序扫描 benchmark 数值，但相关路径经回归测试未退化，历史 QA 结果为 PASS
- AC5: PARTIAL - 本轮未重新采集完整并发访问 benchmark 数值，但竞态修复与 race 测试已通过，历史 QA 结果为 PASS
- AC6: PASS - 本轮修改未新增页表结构；`go test -tags vm_v3 ./common/vm/...` 覆盖相关测试通过
- AC7: PASS - Python 与 Node.js 各执行 1 个回归用例并通过
- AC8: PASS - benchmark 覆盖定义仍在，相关 VM 测试通过
- AC9: PASS - `TestV3ComparePerformanceGuard` 通过，且守护逻辑已收紧

## 已完成项
- 已修复 code review 指出的 facade 生命周期发布缺陷
- 已修复 segment 懒加载的陈旧代际写回问题
- 已修复 short linear span 边界下溢问题
- 已修复 iterator 生命周期未纳入 read-state 的问题
- 已完成对应单测、race 测试与 Python/Node.js 回归测试

## 未完成项
- 无新增源码待修项

## 未验证项
- 未在本轮重新完整采集 AC3/AC4/AC5 的全量 benchmark 结果，仅保留历史 QA 数据与局部守护测试结论
- 未进行 Python 2.7 / 多 glibc / 多 Node.js 版本矩阵验证

## 剩余工作
- 未完成项: 无
- 未验证项: 重新在稳定环境采集全量性能 benchmark；执行更广的兼容性矩阵验证

## Self-Review 发现与修复
- 发现 `switchImplementation()` 初版修复测试中因为未注册旧实现，导致“Init 期间看到旧实现”断言失真；已在测试中补齐旧实现注册，确保覆盖真实切换场景。
- 复查 `MemoryIterator` 后补充 `Close()` 与自动耗尽释放，避免调用方长期持有已关闭 VM 的底层引用。

## 注意事项
- 本轮严格未修改 `cmd/*.py`，符合人审约束。
- 工作树仍存在与本任务无关的未提交修改：`common/utils.go`、`nodejs/map_info.go`，提交时需避免混入。
- `dev-flow/*.md` 报告文件按要求生成，但不会加入 git 提交。
