# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按“低成本静态检查 → 关键 smoke test → 扩展回归/基准”的顺序完成验收。`common/vm/v3/` 目录、build tag 接线、门面层导出、无锁加载并发测试、Python/Node.js 主链路回归、V3 场景 benchmark 与 V3/V2 对比 benchmark 均已执行并通过，未发现阻断发布的问题。

## Findings

- `common/vm/v3/` 目录与 `common/vm/select_v3.go` 已存在，`common.VMV3` 导出与 `SetVMType(VMV3)` 公共切换入口已补齐，解决了此前“底层实现存在但公共门面不可配置”的接线缺口。
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，说明 V3 首访懒加载在当前测试覆盖下未观察到 data race。
- Python 与 Node.js 代表性 testdata 用例均通过，说明引入 V3 后主链路未出现明显回归。
- 基准结果显示主要目标达成：随机访问 3.812ns/op、顺序扫描 2.831ns/op、并发访问 0.2253ns/op，满足 issue 中 AC3/AC4/AC5 的主指标阈值。
- 对比 benchmark 显示 V3 在已纳入比较的场景下均优于 V2；其中 page-table 路径虽然仍明显慢于 linear-span 路径，但相对 V2 仍保持优势，且守护测试 `TestV3ComparePerformanceGuard` 已通过。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `./maze --build` 通过；`go test -tags vm_v3 ./common/vm/...` 通过；`common/vm/v3/`、`common/vm/select_v3.go`、`common/vm_v3_export_test.go`、`common/vm/v3_select_tag_test.go` 均存在。
- **关键指标**: build 成功；VMV3 已注册并可切换。
- **备注**: 静态检查确认 `common.VMV3` 与 `common/vm.VMV3` 对齐，门面层 `SetVMType()` 已显式支持 `VMV3`。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- **关键指标**: race detector 未报 data race；测试断言 segment 首次并发加载仅触发 1 次。
- **备注**: 满足 AC2 的核心并发验收要求。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 批量读取、短缓冲区处理、跨 segment iterator、页表 lookup/fallback、未对齐/跨段 range 读取等合约测试全部通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestDirectReadHandlesUnalignedAddress|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader' -count=1` 通过。
- **备注**: 说明 `GetMemoryBatch8/4`、`MemoryIterator` 与 source 切换失效机制基本可用。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中已覆盖 GetMemory8/4、SequentialScan、RandomAccess、ConcurrentAccess、Hotspot、Unaligned、CrossSegment、CacheMiss、GetMemorySafe、MixedWorkload、ColdStart、PythonObjectTraverse、V8HeapScan 等基准函数。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchmem -count=1` 通过。
- **备注**: 覆盖面符合 AC8 要求，且额外补充了 page-table 变体 benchmark。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在 `BenchmarkCompareV3*` 系列基准与 `TestV3ComparePerformanceGuard` 守护测试；本轮执行通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestVMV3TypeRegisteredWhenBuiltWithTag|TestSwitchToV3BindsImplementation|TestV3ComparePerformanceGuard' -count=1` 通过；对比 benchmark 全部成功执行。
- **备注**: 满足 AC9 的“存在且可执行”与“守护测试通过”要求。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 代表性 testdata 用例均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：PASS
- **备注**: 已满足“每次验收至少执行一个 testdata 用例”的强制要求，本轮实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过，page-table fixture 保持非 linear span，且页表额外开销未超过测试阈值。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestPageTableMemoryOverhead|TestBenchmarkPageTableFixturesStayNonLinear' -count=1` 通过。
- **备注**: 满足 AC6。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 关键 benchmark 达到 issue 目标；V3/V2 对比 benchmark 中 V3 在已比较场景下均优于 V2。
- **关键指标**:
  - `BenchmarkRandomAccess`: **3.812 ns/op**（满足 < 4ns）
  - `BenchmarkSequentialScan`: **2.831 ns/op**（满足 < 3ns）
  - `BenchmarkConcurrentAccess`: **0.2253 ns/op**（满足 < 1.5ns）
  - `BenchmarkCompareV3GetMemory8`: V2 19.08 ns/op / V3 12.88 ns/op
  - `BenchmarkCompareV3SequentialScan`: V2 19.72 ns/op / V3 12.58 ns/op
  - `BenchmarkCompareV3ConcurrentAccess`: V2 2.431 ns/op / V3 1.755 ns/op
- **备注**: page-table 变体 `BenchmarkRandomAccessPageTable=13.19 ns/op` 与 `BenchmarkConcurrentAccessPageTable=1.183 ns/op` 说明稀疏查址路径仍明显慢于 linear-span 热路径，但当前 issue/AC 的主指标基准已满足，且 compare benchmark 中 V3 仍优于 V2。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- Python 2.7 兼容矩阵未执行；当前仓库与已给 testdata 主要覆盖 Python 3.11 主链路。
- 不同 glibc 版本矩阵未执行；本轮仅在当前 Linux 环境完成验收。
- 不同 Node.js 版本矩阵未执行；本轮仅基于现有 coredump/testdata 验证 Node.js 主链路。
- 未对 `common/utils.go`、`nodejs/map_info.go` 等与本 issue 无关的工作树脏改动做功能验收；这些文件不在本轮需求变更范围内。

上述未执行项不阻断本轮对 AC1-AC9 的主结论，但会影响更广泛的跨版本兼容性置信度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

说明：本轮已验证 VM V3 微基准性能目标与页表开销约束，但未执行 >1GB 真实进程扫描压测，因此该两项保持未勾选。

## 测试命令记录

```bash
./maze --build
python3 testdata/run_test.py python/20260129-complex-types-311
go test -tags vm_v3 ./common/vm/...
go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1
python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot
go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead|TestDirectReadHandlesUnalignedAddress|TestBindSourceInvalidatesStaleSegmentData|TestBenchmarkPageTableFixturesStayNonLinear|TestClosePublishesNewStateBeforeClosingOldReader' -count=1
go test -tags vm_v3 ./common -run TestVMV3ExportMatchesCoreType -count=1
go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestVMV3TypeRegisteredWhenBuiltWithTag|TestSwitchToV3BindsImplementation|TestV3ComparePerformanceGuard' -count=1
go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchmem -count=1
go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|GetMemoryRange|ColdStart)$' -benchmem -count=1
```

## 日志检查

- `maze.log` 中未发现 panic / fatal 级主流程崩溃。
- 日志中存在少量包含 `error` 字样的 DEBUG 文本，主要是 GDB 符号未命中提示与业务对象字段名包含 `Error` 的正常日志，不构成本轮失败依据。
- 未发现 `maze.py.log`。

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以进入后续合并/发布流程；若要进一步提高发布置信度，建议补做跨 Python/glibc/Node.js 版本矩阵与大内存真实进程压测。
