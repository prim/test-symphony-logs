## Summary
修复了最新 QA 报告指出的人审范围违例问题：将 `cmd/maze-gen-coredump.py` 与 `cmd/maze-tar-coredump.py` 回退到 `origin/master` 版本，消除“不要改 cmd/*.py”约束冲突。

结论：COMPLETE

## Changes
- `cmd/maze-gen-coredump.py`: 回退分支中的非 VM V3 改动，恢复为主分支版本
- `cmd/maze-tar-coredump.py`: 回退分支中的非 VM V3 改动，恢复为主分支版本
- `dev-log/2026-03-16-vm-v3-qa-fix-iteration-33.md`: 记录本轮 QA 修复背景、处理方式与验收核对

## Testing
- `./maze --build`
- `git diff --name-only origin/master...HEAD`

## Acceptance Check
- AC1: PASS，`common/vm/v3/` 目录结构与 `vm_v3` build tag 保持可用
- AC2: PASS，无锁加载实现未改动，上一轮 `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 结果仍有效
- AC3: PASS，随机访问性能实现未改动，上一轮 benchmark 结果仍有效
- AC4: PASS，顺序扫描性能实现未改动，上一轮 benchmark 结果仍有效
- AC5: PASS，并发访问性能实现未改动，上一轮 benchmark 结果仍有效
- AC6: PASS，页表内存开销实现未改动，上一轮验证结果仍有效
- AC7: PASS，Python / Node.js 回归链路实现未改动，上一轮验证结果仍有效
- AC8: PASS，benchmark 覆盖实现未改动
- AC9: PASS，V3 vs V2 对比测试实现未改动

## Completed Items
- 已完成项:
  - 回退两个 `cmd/*.py` 文件到 `origin/master`
  - 消除分支相对主分支的人审范围违例
  - 补充本轮 dev-log 文档

## Remaining Work
- 未完成项: 无
- 未验证项: 无

## Self-Review
- 检查确认本轮未改动 `common/vm/v3`、benchmark、回归测试与门面接线代码
- 检查确认修复策略为最小化范围变更，仅处理 QA 指出的约束违例

## Notes
- 本轮仅修复 QA 指出的范围合规问题，不涉及 VM V3 功能逻辑调整
- `dev-flow/*.md` 报告文件按流程生成，但不会纳入 git 提交
