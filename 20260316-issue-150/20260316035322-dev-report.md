## Summary

本轮修复了 V3 runtime 在关闭过程中的状态发布问题，并继续优化 `MemoryIterator.Next8()` 热路径，降低顺序扫描的边界判断与状态更新开销。

结论为 **PARTIAL**：关闭生命周期问题得到进一步收敛，关键单测、race 测试、Python/Node.js 回归和 compare guard 通过，但 AC4 的顺序扫描 `< 3ns/op` 目标仍未满足。

## Changes
- `common/vm/v3/vm.go`: 为 runtimeState 增加 closing 状态；`Close()` 先发布关闭状态，再清空 runtime 指针并关闭旧 reader；`currentState/currentReadState` 屏蔽 closing runtime。
- `common/vm/v3/batch.go`: 精简 `MemoryIterator` 字段，新增 `fastEnd`，线性顺序扫描直接走快速读取分支。
- `dev-log/2026-03-16-vm-v3-qa-fix-iteration-28.md`: 记录本轮修复方案、验证命令与结论。

## Testing
- `./maze --build` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestClosePublishesNewStateBeforeClosingOldReader|TestDirectReadHandlesUnalignedAddress|TestConcurrentLazyLoadRaceFree' -count=1` ✅
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkSequentialScan -benchmem -benchtime=120ms -count=3` ⚠️ `3.364 / 3.364 / 3.397 ns/op`，未达到 `< 3ns/op`
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## Acceptance Check
- AC1: PASS，目录结构与 `vm_v3` build tag 已可用。
- AC2: PASS，`go test -race` 并发懒加载测试通过。
- AC3: PARTIAL，本轮未单独复测随机访问门槛，但上一轮 QA 已记录通过；当前未回归验证完整数据。
- AC4: FAIL，顺序扫描仍高于 `< 3ns/op`。
- AC5: PARTIAL，本轮未单独复测并发访问门槛；当前仅保留历史通过证据。
- AC6: PARTIAL，本轮未复测页表开销测试。
- AC7: PASS，Python / Node.js 主链路回归通过；按人工指导忽略 Node.js comprehensive 数据生成链路。
- AC8: PARTIAL，benchmark 场景代码覆盖存在，但本轮未全量执行矩阵。
- AC9: PARTIAL，`TestV3ComparePerformanceGuard` 本轮通过，但未逐项给出“所有场景优于 V2”的完整实测证明。

## Remaining Work
- 未完成项: 继续优化顺序扫描热路径，使 `BenchmarkSequentialScan` 稳定低于 `3ns/op`。
- 未验证项: AC3/AC5/AC6/AC8/AC9 的本轮完整复测与稳定性证明。
