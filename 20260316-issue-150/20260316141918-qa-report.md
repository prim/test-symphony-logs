# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #150 的 VM V3 高性能版本进行了静态核对、Go 测试、性能守护验证，以及 Python / Node.js 主链路 testdata 回归。未发现阻塞发布的问题；本轮也确认 human review 指出的约束“不要改 `cmd/*.py`”已满足。

## Findings

- `common/vm/v3/` 目录、`common/vm/select_v3.go`、facade 切换与 build tag 入口均已落地，`go test -tags vm_v3 ./common/vm/...` 通过。
- 无锁加载关键并发用例 `TestConcurrentLazyLoadRaceFree` 在 `-race` 下通过，未观察到 data race。
- 最新修复点已覆盖：已排序 batch 输入不再退化为逐项标量读取；大 core 初始化改为跳过 mmap fast-public-path 退化路径，并有守护测试覆盖。
- 对比基准抽样显示 V3 优于 V2：`GetMemory8`、`SequentialScanPageTable`、`ConcurrentAccessPageTable` 均满足“V3 更快”。
- 未检测到 `cmd/*.py` 变更，满足人工审查限制。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/`、`select_v3.go`、facade 相关切换逻辑存在；`go test -tags vm_v3 ./common/vm/...` 全部通过。
- **关键指标**: `maze/common/vm`、`maze/common/vm/v3` 测试通过。
- **备注**: 额外核对到 `SwitchToV3()`、`CurrentVMType()`、`VMV3` 已完成接线。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- **关键指标**: race detector 未报告 data race。
- **备注**: 满足 AC2。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 关键合约测试通过，包括短结果缓冲区、跨 segment iterator、页表回退、跨 segment range 读取、已排序/未排序 workspace 行为。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'Test(BatchReadSortedInputFastPath|BatchReadBuildWorkspacePreservesSortedOrderWithoutExtraSort|BatchReadBuildWorkspaceSortsUnorderedInput|InitLargeCoreSkipsMmapToPreserveFastPublicReadPath|PageTableMemoryOverhead|BatchReadHandlesShortResultsBuffer|MemoryIteratorTraversesAcrossSegments|PageTableLookupAndFallback|GetMemoryRangeHandlesUnalignedAndCrossSegment)$' -count=1` 通过。
- **备注**: 已覆盖本轮开发报告中提到的最近修复点。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 基准项存在且可运行，覆盖随机访问、顺序扫描、并发访问、热点访问、未对齐、跨 segment、cache miss、安全读取、混合负载、冷启动、Python/V8 场景，以及已排序 batch 基准。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan|GetMemoryBatch8Sorted|GetMemoryBatch4Sorted)$' -benchtime=100ms -count=1` 通过。
- **备注**: 满足 AC8 覆盖要求。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: 对比性能守护测试通过，且抽样 benchmark 中 V3 均优于 V2。
- **关键指标**:
  - `BenchmarkCompareV3GetMemory8`: V2 19.08 ns/op，V3 17.44 ns/op
  - `BenchmarkCompareV3SequentialScanPageTable`: V2 27.78 ns/op，V3 23.11 ns/op
  - `BenchmarkCompareV3ConcurrentAccessPageTable`: V2 4.607 ns/op，V3 3.595 ns/op
- **备注**: `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` 通过。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**:
  - `python/20260129-complex-types-311` 通过，Python 复杂类型统计正常。
  - `nodejs/20260225-maze-vs-heapsnapshot` 通过，Node.js 准确性对比正常。
- **关键指标**:
  - Python: `pymempool_objects = 68626`，关键类型 `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`
  - Node.js: `derived total_objects = 52307`，`Map=518`、`Set=520`、`Promise=1066`
- **备注**: 满足 AC7；也满足“每次验收至少执行一个 testdata 用例”的硬性要求。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: 页表内存开销测试通过。
- **关键指标**: `TestPageTableMemoryOverhead` 通过。
- **备注**: 代码中 `PageTable.MemoryBytes()` 已实现统计入口，满足 AC6 的验证基础。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 关键 V3 benchmark 达到 issue 目标；对比守护测试也通过。
- **关键指标**:
  - 随机访问 `BenchmarkRandomAccess`: **3.922 ns/op**（目标 < 4ns）
  - 顺序扫描 `BenchmarkSequentialScanPageTable`: **2.832 ns/op**（目标 < 3ns）
  - 并发访问 `BenchmarkConcurrentAccessPageTable`: **0.8628 ns/op**（目标 < 1.5ns）
- **备注**: 满足 AC3 / AC4 / AC5。

## Test Results

- `./maze --build` ✅
- `go test -tags vm_v3 ./common/vm/...` ✅
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run 'Test(BatchReadSortedInputFastPath|BatchReadBuildWorkspacePreservesSortedOrderWithoutExtraSort|BatchReadBuildWorkspaceSortsUnorderedInput|InitLargeCoreSkipsMmapToPreserveFastPublicReadPath|PageTableMemoryOverhead|BatchReadHandlesShortResultsBuffer|MemoryIteratorTraversesAcrossSegments|PageTableLookupAndFallback|GetMemoryRangeHandlesUnalignedAndCrossSegment)$' -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan|GetMemoryBatch8Sorted|GetMemoryBatch4Sorted)$' -benchtime=100ms -count=1` ✅
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` ✅
- `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchtime=100ms -count=1` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未单独运行完整的 `common/vm/bench_compare_test.go` 全量所有 compare benchmark 子项，仅执行了性能守护测试和关键抽样对比 benchmark。
- 未执行原因：本轮关键 AC 已由守护测试、覆盖性 benchmark、抽样 compare benchmark 和双语言 testdata 回归共同验证，继续全量跑更多高成本 benchmark 收益较低。
- 可能影响范围：对 AC9 的“所有场景优于 V2”仍主要依赖当前 compare 守护测试与抽样 benchmark；不过本轮未发现反例。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过（基于现有 Node.js coredump 回归用例）

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本轮 QA 验收；若上线前还需进一步保守确认，建议在更稳定机器上补跑全量 compare benchmark 与更大 core 的真实扫描性能测试。
