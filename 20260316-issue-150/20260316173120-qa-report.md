# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 prim/maze#150（VM V3 高性能版本）执行了静态核对、关键 Go 测试、性能守护测试、基准覆盖检查，以及 Python / Node.js 主链路回归。未发现新的阻塞性问题；本轮修复点也满足 human review 中“不要改 `cmd/*.py`”的约束。

## Findings

- 未发现违反 human review 约束的提交范围：`origin/master...HEAD` 中无 `cmd/*.py` 差异。
- `common/vm/v3/` 目录、`common/vm/select_v3.go`、批量 API、页表与关闭协议相关测试均存在并通过。
- V3 基准覆盖已包含 AC8 要求的全部 benchmark 名称。
- 关键对比基准抽样中，V3 在随机访问、顺序扫描页表路径、并发访问页表路径均优于 V2。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 下存在 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go` 等关键文件；`common/vm/select_v3.go` 已注册 V3 实现并绑定入口。
- **关键指标**: `./maze --build` 通过；`go test -tags vm_v3 ./common/vm/...` 相关子集通过。
- **备注**: 静态核对与构建结果一致，满足 AC1。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- **关键指标**: race detector 未报告 data race。
- **备注**: 满足 AC2。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 批量读取、短结果缓冲区、跨 segment iterator、页表查找 fallback、未对齐与跨 segment 范围读取等关键合约测试通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestClosingStateDisablesFastReadPath|TestCloseWaitsForInFlightPublicReadersWithoutCloser|TestInitLargeCoreSkipsMmapToPreserveFastPublicReadPath|TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead' -count=1` 通过。
- **备注**: 覆盖 API 合约与关闭窗口关键边界。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 在 `common/vm/v3/v3_contract_test.go` 中确认存在 14 个要求的 benchmark，并成功执行。
- **关键指标**:
  - `BenchmarkGetMemory8`: 3.917 ns/op
  - `BenchmarkGetMemory4`: 3.786 ns/op
  - `BenchmarkSequentialScan`: 2.850 ns/op
  - `BenchmarkRandomAccess`: 3.907 ns/op
  - `BenchmarkConcurrentAccess`: 0.2305 ns/op
- **备注**: AC8 要求的 benchmark 名称均已覆盖并可运行。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在 V3 对比测试与性能守护；抽样对比基准成功运行。
- **关键指标**:
  - `BenchmarkCompareV3GetMemory8`: V2 19.02 ns/op，V3 17.70 ns/op
  - `BenchmarkCompareV3SequentialScanPageTable`: V2 28.42 ns/op，V3 19.46 ns/op
  - `BenchmarkCompareV3ConcurrentAccessPageTable`: V2 4.400 ns/op，V3 3.535 ns/op
- **备注**: 抽样结果显示 V3 优于 V2，且 `TestV3ComparePerformanceGuard` 通过。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 代表性 testdata 用例均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311` 通过
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 通过
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，且 AC7 通过。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过。
- **关键指标**: 相关 Go 测试通过，未见页表开销守护失败。
- **备注**: 本轮未单独抓取详细占用数值，但现有自动化守护通过，可接受为 AC6 证据。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 本轮执行了 V3 benchmark 和 V3 vs V2 对比 benchmark；关键指标满足 issue/plan 中目标。
- **关键指标**:
  - 随机访问: 3.907 ns/op（目标 < 4ns）
  - 顺序扫描: 2.850 ns/op（目标 < 3ns）
  - 并发访问: 0.2305 ns/op（目标 < 1.5ns）
  - 对比抽样均优于 V2
- **备注**: 受共享环境影响，ns/op 仍应视为基准环境下的抽样证据；但当前结果满足 AC3/AC4/AC5。

## Test Results

- `./maze --build` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestClosingStateDisablesFastReadPath|TestCloseWaitsForInFlightPublicReadersWithoutCloser|TestInitLargeCoreSkipsMmapToPreserveFastPublicReadPath|TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead' -count=1` ✅
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` ✅
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchtime=100ms -count=1` ✅
- `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchtime=100ms -count=1` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未单独执行完整的 `go test -tags vm_v3 ./common/vm/...` 全量回归；本轮采用关键子集 + 性能守护 + 代表性 benchmark 抽样策略。
- 未单独执行 Python 2.7、不同 glibc 版本、不同 Node.js 版本的环境矩阵。
- 未逐项运行 `bench_compare_test.go` 中全部 V3 对比 benchmark，仅对关键主链路进行了抽样验证。

可能影响范围：
- 兼容性矩阵相关结论仍以已有自动化覆盖与本轮主链路回归为主，无法替代完整多版本环境验收。
- 未抽样到的对比 benchmark 场景若存在局部回退，本报告无法完全覆盖。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [x] 大内存进程扫描性能门槛相关基准通过抽样验证
- [x] 随机访问 < 4ns
- [x] 顺序扫描 < 3ns
- [x] 并发访问 < 1.5ns
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以继续流转；当前未发现阻塞发布的 QA 问题，但若要形成更强发布证据，建议后续补充多版本兼容性矩阵与更完整的对比 benchmark 全量记录。
