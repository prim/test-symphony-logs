## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本静态检查 → 关键 smoke → 扩展回归”的顺序执行验收。已完成关键代码范围核对、V3 关键合约测试、竞态测试、至少一个 `testdata/run_test.py` 用例以及 Python / Node.js 主链路回归。

结论：**AC9 仍不满足**。最新修复虽然恢复了稀疏页表路径正确性，并通过了多数功能与回归测试，但 `TestV3ComparePerformanceGuard` 仍稳定失败，说明 V3 在关键页表路径下尚未实现“所有场景优于 V2”的要求。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/`、`common/vm/select_v3.go` 均存在；`go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3' -count=1` 通过。
- **关键指标**: `HasImplementation(VMV3)=true`，`SwitchToV3()` 后 `CurrentVMType()=VMV3`
- **备注**: 静态 diff 中未发现 `cmd/*.py` 改动，符合最新 human review 约束。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过；并发首访只触发一次 segment 加载。
- **关键指标**: race detector 通过；`LoadCount()==1`
- **备注**: `common/vm/v3/vm.go` 中读者计数与关闭期保护逻辑可对应测试覆盖。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 批量读取、短结果缓冲区、跨 segment iterator、页表 fallback、未对齐/跨段范围读取等关键合约测试均通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestLinearSpanShortReadDisablesFastPath|TestSegmentShortReadReturnsFailure|TestCrossSegmentUint64ShortSecondSegmentFails|TestCrossSegmentUint32ShortSecondSegmentFails' -count=1` ✅
- **备注**: 同时验证了此前 QA 关注的短读伪成功问题未复现。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: V3 benchmark 可被 Go benchmark 发现并执行，覆盖随机访问、顺序扫描、并发访问、hotspot、未对齐、跨 segment、cache miss、安全读取、mixed workload、cold start、Python/V8 场景。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchtime=1x` 全部被发现并执行
- **备注**: 本次仅验证覆盖与可执行性，非正式性能定标。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **实际行为**: 对比测试文件存在，稀疏 fixture 正确性测试通过，但性能守护测试失败。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestV3SparseFixturesExerciseNonLinearPaths|TestV3OutperformsV2Placeholders|TestV3BenchmarkCoverage' -count=1` ✅；`go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1` ❌
- **备注**: 失败点集中在 `GetMemory8PageTable` 场景。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 的 `testdata/run_test.py` 主链路回归均通过。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，本轮实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/v3 -run TestPageTableMemoryOverhead -count=1` 通过。
- **关键指标**: 页表内存开销约束测试通过
- **备注**: 结合 `pagetable.go` 中 `MemoryBytes()` 与 `subBytes` 统计，未见明显遗漏。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 虽然 V3 benchmark 场景存在，但关键对比守护测试已证明在页表路径上 V3 仍慢于 V2，因此不能判定 AC3/AC4/AC5 全部满足。
- **关键指标**: `TestV3ComparePerformanceGuard` 中 `GetMemory8PageTable` 中位数 `v2=28.0675ns/op`、`v3=32.5238ns/op`，`faster_rounds=0/7`
- **备注**: 该失败足以否定“V3 在所有对比场景优于 V2”的要求；继续做完整高成本性能矩阵不会改变当前 FAIL 结论。

## 失败的 Test Case

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **预期行为**: `common/vm/bench_compare_test.go` 中的 V3 vs V2 对比测试全部通过，且 V3 在所有场景优于 V2。
- **实际行为**: `TestV3ComparePerformanceGuard` 稳定失败，V3 在 `GetMemory8PageTable` 场景下慢于 V2。
- **复现步骤**:
  1. 在项目根目录执行 `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1`
  2. 观察失败输出中的 `GetMemory8PageTable` 指标
  3. 可见 `v2-median=28.0675ns/op`，`v3-median=32.5238ns/op`
- **可能原因**: 最新修复优先解决了稀疏页表子表指针正确性问题，但页表热路径仍存在额外开销；从当前数据看，`segmentAt` / 页表命中后的读取路径仍未达到设计目标中的“高性能版本”要求。
- **建议修复**: 开发侧应继续剖析页表命中热路径，重点检查 `Lookup`、segment 缓存命中、读路径分支与额外间接层的成本，再重新提交性能守护测试结果。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: 随机访问、顺序扫描、并发访问均满足 issue #150 的目标阈值，且整体相对 V2 有明确提升。
- **实际行为**: 由于对比守护测试已证明关键页表路径未达成“V3 优于 V2”，因此当前不能确认性能目标全部成立。
- **复现步骤**:
  1. 执行 `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1`
  2. 如需辅助观察覆盖，可执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchtime=1x`
  3. 对照结果判断当前无法证明 AC3/AC4/AC5 全部满足
- **可能原因**: 当前实现已修复正确性，但性能优化尚不充分；尤其页表路径读址仍有稳定负担。
- **建议修复**: 先保证 `bench_compare_test.go` 的性能守护测试通过，再补充正式 benchmark 结果证明 AC3~AC5。

## 未执行项

- 未继续执行更完整的高成本性能矩阵（例如更长时间 `-benchmem` 正式定标）
- **未执行原因**: `TestV3ComparePerformanceGuard` 已明确证明 AC9 不成立，继续扩展性能测试只会重复暴露同一主因，不影响本轮 FAIL 结论
- **影响范围**: AC3 / AC4 / AC5 / AC9

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 8
- 失败: 2 / 8
- 建议: **当前不建议合入。** 正确性与主链路回归基本恢复，但 AC9 对应的性能守护测试仍失败，说明 VM V3 尚未稳定达到“所有场景优于 V2”的验收要求。
