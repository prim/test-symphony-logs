# 开发报告

## 完成的任务
- [x] 修复 `common/initVirtualMemory()` 在纯 `vm_v3` 构建下未将 VMV3 纳入初始化回退路径的问题
- [x] 补充 VM 公共初始化层集成测试，覆盖纯 V3 构建下的选择、初始化与读取路径
- [x] 扩展 VM 启动日志，明确输出 `vm init v3`

## 修改的文件
- `common/vm.go`: 抽离 VM 选择逻辑，新增对 `VMV3` 的默认回退与日志输出，确保公共初始化层可正确初始化 V3
- `common/vm/init_v3_integration_test.go`: 新增 `common/vm` 包集成测试，直接覆盖纯 `vm_v3` 构建时的回退与初始化契约
- `common/vm_init_integration_test.go`: 新增 `common` 层集成测试，验证 `initVirtualMemory()` 能在纯 V3 构建下完成初始化并成功读取 segment

## 自测结果
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common -run TestInitVirtualMemorySelectsAndInitializesV3InV3OnlyBuild -count=1`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm -run 'TestV3OnlyBuildFallsBackToV3AndInitializesCurrentImplementation|TestVMV3ImplementationUsesProtectedPublicEntryPoints|TestFacadeDispatchUsesCurrentImplementationWithoutRebindingFunctions|TestSwitchToV3BindsImplementation|TestCloseCurrentUsesBoundImplementation' -count=1`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run 'TestCloseWaitsForInFlightFastPathReader|TestCloseWaitsForInFlightPublicGetMemoryReader|TestClosePublishesNewStateBeforeClosingOldReader|TestConcurrentLazyLoadRaceFree|TestPageTableMemoryOverhead' -count=1`
- 测试结果: 通过
- 测试命令: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench BenchmarkRandomAccess -benchtime=300ms -count=3`
- 测试结果: 通过，`BenchmarkRandomAccess` 三次分别为 `3.899/3.950/3.911 ns/op`

## 验收核对
- AC1: PASS 已验证 `common/vm/v3/` 构建与纯 `vm_v3` 初始化路径
- AC2: PASS `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过
- AC3: PASS `BenchmarkRandomAccess` 复测三次均 `< 4ns`
- AC4: PARTIAL 本轮未重新复测完整顺序扫描门槛，仅沿用上一轮 QA 报告中的通过结论
- AC5: PARTIAL 本轮未重新复测完整并发访问门槛，仅沿用上一轮 QA 报告中的通过结论
- AC6: PASS `TestPageTableMemoryOverhead` 通过
- AC7: PASS Python / Node.js 主链路回归通过
- AC8: PASS 既有 benchmark 覆盖测试仍在，且本轮新增公共初始化路径集成测试
- AC9: PARTIAL 本轮未重跑全量 `common/vm/bench_compare_test.go` 对比基准，仅保留既有覆盖与历史 QA 结论

## 剩余工作
- 未完成项: 无（本轮 QA / code review 指出的修复项已完成）
- 未验证项: AC4、AC5、AC9 未在本轮重新做全量复测

## Self-Review 发现与修复
- 发现初版 `common` 层测试直接依赖未导出 facade 内部快照，导致编译失败；已改为分别在 `common/vm` 和 `common` 两层补充测试，避免新增仅测试可见的生产导出接口。
- 发现直接调用 `initVirtualMemory()` 的轻量测试会因缺失真实 ELF 文件触发 `InitCorefileSegments()` 打开空路径失败；已改为使用当前测试二进制作为可解析 ELF，覆盖真实公共初始化路径。

## 注意事项
- 本轮严格未修改 `cmd/*.py`，符合人审限制。
- 仅修复 QA / code review 指出的公共初始化接线缺口，未触碰 `common/utils.go`、`nodejs/map_info.go` 等与本次问题无关的已有工作区改动。
- 结论: PARTIAL
