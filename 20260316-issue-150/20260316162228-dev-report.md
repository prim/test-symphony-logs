## 结论

PARTIAL

## 开发报告

### 完成的任务
- [x] 修复 V3 公共读取入口在 `closer == nil` 路径下缺少统一关闭期读者保护的问题，`GetMemory` / `GetMemorySafe` / `GetMemory4` / `GetMemory4Safe` / `GetMemoryRange` 统一走带 reader 引用计数的公共获取逻辑。
- [x] 修复 `Close()` 在重复关闭或测试清理顺序下可能对同一 `runtimeState` 重复扣减 `readers` 导致永久等待的问题。
- [x] 为 `shared` 增加当前 hooks 快照读取接口，并在 V3 初始化相关测试中恢复 hooks，消除跨测试污染导致的全量 `go test` 卡死问题。
- [x] 保持 human review 约束：未修改 `cmd/*.py`，未修改任何测试数据与 `testdata/` 校验脚本。

### 修改的文件
- `common/vm/v3/vm.go`: 统一公共读取入口的关闭期读者保护；为 `Close()` 增加幂等保护，避免重复关闭导致 reader 计数失衡。
- `common/vm/shared/helpers.go`: 新增 `CurrentHooks()`，用于测试场景保存/恢复 shared hooks。
- `common/vm/v3/v3_contract_test.go`: 初始化相关测试保存并恢复 hooks，避免共享状态污染。

### 自测结果
- 测试命令: `./maze --build`
  - 结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run 'TestCloseWaitsForInFlightPublicReadersWithoutCloser|TestInitSmallCoreUsesMmapCloserWhenAvailable|TestInitLargeCoreSkipsMmapToPreserveFastPublicReadPath' -count=1 -timeout 180s`
  - 结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -v -timeout 180s`
  - 结果: 通过
- 测试命令: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
  - 结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/...`
  - 结果: 失败
  - 关键输出: `TestV3ComparePerformanceGuard` 失败，`GetMemory8PageTable` 中 V3 中位数约 `31.2867ns/op`，慢于 V2 约 `28.1506ns/op`

### 验收核对
- AC1: PASS - `common/vm/v3/` 与 `vm_v3` build tag 相关结构已存在，本轮未回退。
- AC2: PASS - `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- AC3: FAIL - 当前全量性能守护测试中 `GetMemory8PageTable` 仍未达到 V3 优于 V2。
- AC4: PARTIAL - 本轮未重新完成完整顺序扫描门槛验收；仅确认生命周期卡死问题已修复。
- AC5: PARTIAL - 本轮未重新完成完整并发门槛验收；仅确认公共关闭协议回归通过。
- AC6: PASS - 页表开销相关断言代码与对应测试仍在，`go test -tags vm_v3 ./common/vm/v3 -v` 通过。
- AC7: PARTIAL - 本轮聚焦修复 QA 指出的 Go 生命周期问题，未重新执行 Python / Node.js testdata 回归。
- AC8: PASS - benchmark 覆盖相关代码与存在性测试未回退，`common/vm/v3` 全量测试通过。
- AC9: FAIL - `go test -tags vm_v3 ./common/vm/...` 中 V3 vs V2 性能守护仍失败。

### 已完成项
- 已修复公共 fast path 在无 `closer` 路径下的关闭协议不一致问题。
- 已修复 `TestInitSmallCoreUsesMmapCloserWhenAvailable` 在整包执行时的卡死问题。
- 已补齐测试 hooks 恢复，降低跨测试状态污染。

### 未完成项
- `common/vm/bench_compare_test.go` 中 `TestV3ComparePerformanceGuard` 仍失败，V3 尚未在全部对比场景中稳定优于 V2。

### 未验证项
- Python 2.7 兼容性未复测。
- Python / Node.js testdata 主链路本轮未复测。
- AC4 / AC5 的完整性能门槛本轮未单独补跑确认。

### Self-Review 发现与修复
- 发现公共读取入口此前只在部分路径启用 in-flight reader 保护，已统一改为所有公共入口都通过 `acquirePublicReadState()` 获取受保护状态。
- 发现 `Close()` 在测试清理与 `Init()` 前置关闭叠加时，可能对同一 state 重复执行 `closingReaders` 扣减，已改为 `closing.Swap(1)` 幂等防护。
- 发现初始化测试直接覆写 `shared.Configure(...)` 但未恢复旧 hooks，已补充恢复逻辑。

### 注意事项
- 本轮为 **partial fix**：生命周期与测试稳定性问题已修复，但性能验收尚未全部达标，不能宣称全部完成。
- 仍需后续继续分析 `TestV3ComparePerformanceGuard` 中 V3 在 page table 对比场景慢于 V2 的原因。
- `cmd/*.py` 未被修改，符合 human review 约束。
