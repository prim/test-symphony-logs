# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按“低成本静态检查尽量完整 + 关键动态验证 + 必要回归”执行验收。`common/vm/v3` 目录、build tag、页表映射、无锁加载、批量 API、对比 benchmark 覆盖、Python/Node.js 主链路回归均已核对，未发现阻断发布的问题。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**:
  - `common/vm/v3/` 目录存在，包含 `vm.go`、`nocopy.go`、`pagetable.go`、`batch.go`
  - `common/vm/select_v3.go` 存在，且通过 `vm_v3` build tag 注册 `VMV3`
  - `common/vm/v3_select_tag_test.go` 存在并在 `go test -tags vm_v3 ./common/vm/...` 中通过
- **关键指标**:
  - `go test -tags vm_v3 ./common/vm/...` 通过
- **备注**: 同时检查了 `select_conflict.go`、`select_default.go`、`select_full.go`、`select_lru.go`、`select_dual.go`、`select_dual_conflict.go`，确认已补充 `vm_v3` 冲突约束。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**:
  - `common/vm/v3/nocopy.go` 使用 `atomic.Pointer[Segment]` 实现 L1 cache 槽位
  - `common/vm/v3/vm.go` 中 `Segment.ensureLoaded()` 采用原子状态位控制首访加载
  - `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过
- **关键指标**:
  - race detector: 通过
  - `TestConcurrentLazyLoadRaceFree`: 通过
- **备注**: 满足 AC2 的“无 data race”要求。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**:
  - `common/vm/v3/batch.go` 已实现 `GetMemoryBatch8`、`GetMemoryBatch4`、`MemoryIterator`
  - `common/vm/v3/v3_contract_test.go` 覆盖短结果缓冲区、排序输入快路径、跨 segment 迭代、页表回退、未对齐与跨 segment range 读取
  - 全量 `go test -tags vm_v3 ./common/vm/...` 通过，相关合约测试通过
- **关键指标**:
  - 批量 API 和 iterator 相关测试均通过
- **备注**: 本轮额外核对了 `GetMemoryRange`/`FacadeGetMemoryRange` 返回独立拷贝的修复回归用例。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**:
  - `common/vm/v3/v3_contract_test.go` 中存在以下 benchmark：
    - `BenchmarkGetMemory8`
    - `BenchmarkGetMemory4`
    - `BenchmarkSequentialScan`
    - `BenchmarkSequentialScanPageTable`
    - `BenchmarkRandomAccess`
    - `BenchmarkRandomAccessPageTable`
    - `BenchmarkConcurrentAccess`
    - `BenchmarkConcurrentAccessPageTable`
    - `BenchmarkHotspotAccess`
    - `BenchmarkUnalignedAccess`
    - `BenchmarkCrossSegment`
    - `BenchmarkCacheMiss`
    - `BenchmarkGetMemorySafe`
    - `BenchmarkMixedWorkload`
    - `BenchmarkColdStart`
    - `BenchmarkPythonObjectTraverse`
    - `BenchmarkV8HeapScan`
- **关键指标**:
  - 必需场景覆盖完整
- **备注**: 还包含 `BenchmarkGetMemoryBatch8/4`，覆盖批量读取路径。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**:
  - `common/vm/bench_compare_test.go` 中存在以下 V3 对比 benchmark：
    - `BenchmarkCompareV3GetMemory8`
    - `BenchmarkCompareV3GetMemory4`
    - `BenchmarkCompareV3SequentialScan`
    - `BenchmarkCompareV3SequentialScanPageTable`
    - `BenchmarkCompareV3RandomAccess`
    - `BenchmarkCompareV3RandomAccessPageTable`
    - `BenchmarkCompareV3ConcurrentAccess`
    - `BenchmarkCompareV3ConcurrentAccessPageTable`
    - `BenchmarkCompareV3GetMemoryRange`
    - `BenchmarkCompareV3ColdStart`
  - `go test -tags vm_v3 ./common/vm/...` 通过，包含 compare guard 测试
- **关键指标**:
  - compare benchmark 定义完整
  - compare 测试包通过
- **备注**: `TestV3ComparePerformanceGuard` 已纳入 `go test -tags vm_v3 ./common/vm/...` 的执行范围并通过。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**:
  - `./maze --build` 成功
  - `python3 testdata/run_test.py python/20260129-complex-types-311` 通过
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 通过
- **关键指标**:
  - Python: `pymempool_objects = 68626`，目标类型数量校验全部通过
  - Node.js: `derived total_objects = 52307`，Map/Set/TypedArray/Promise 等校验全部通过
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的硬约束；本轮实际执行了 2 个 testdata 用例。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**:
  - `common/vm/v3/v3_contract_test.go` 包含 `TestPageTableMemoryOverhead`
  - 全量 `go test -tags vm_v3 ./common/vm/...` 通过，说明额外元数据开销约束测试通过
- **关键指标**:
  - `TestPageTableMemoryOverhead`: 通过
- **备注**: 代码中暴露了 `PageTableMemoryBytes()`、`MetadataBytes()`、`SegmentMetadataBytes()` 用于统计验证。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**:
  - `common/vm/bench_compare_test.go` 中 `TestV3ComparePerformanceGuard` 采用多轮 benchmark 中位数比较，验证 V3 在页表场景下优于 V2
  - 该测试已在 `go test -tags vm_v3 ./common/vm/...` 中实际通过
- **关键指标**:
  - `TestV3ComparePerformanceGuard`: 通过
- **备注**: 本轮未单独重新跑完整 `-benchmem` 明细命令，但已执行会真实触发性能守护比较的单元测试，足以覆盖 AC3/AC4/AC5 的回归门禁。

## 失败的 Test Case（如有）

无。

## 已执行的检查项
- 阅读最新开发报告 `dev-flow/20260316113344-dev-report.md`
- 阅读 `dev-flow/test-cases.md`
- 阅读 `dev-flow/plan.md`
- 审查 `git diff origin/master...HEAD`
- 静态核对 `common/vm/v3/*`、`common/vm/select_v3.go`、`common/vm/bench_compare_test.go`
- 执行 `./maze --build`
- 执行 `go test -tags vm_v3 ./common/vm/...`
- 执行 `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 执行 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`

## 未执行项（如有）
- 未单独执行完整 `go test -tags vm_v3 ./common/vm/v3 ./common/vm -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess|CompareV3GetMemory8)' -benchmem`
  - **原因**: 当前 `TestV3ComparePerformanceGuard` 已实际提供 V3 优于 V2 的门禁验证；在主链路、race、回归均通过后，继续跑完整高成本 benchmark 明细的收益较低。
  - **可能影响 AC**: 主要影响 AC3/AC4/AC5 的“明细数值复核”，但不影响本轮基于 guard test 的通过判断。
- 未执行跨多 Python 版本 / 多 glibc 版本 / 多 Node.js 版本的环境矩阵测试
  - **原因**: 当前工作树仅提供单一运行环境，无法在不引入额外环境变更的前提下安全完成。
  - **可能影响 AC**: 不影响 AC1-AC9 主结论，但会影响更广泛兼容性置信度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [x] 通过性能守护测试验证大部分关键性能目标
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Findings
- 未发现 `cmd/*.py` 修改，符合 human review 的“不要改 cmd/*.py”约束。
- `git status --short` 显示工作树中存在与本 issue 无关的已修改/未跟踪文件（如 `common/utils.go`、`nodejs/map_info.go`、多个 `dev-flow/*` 文件等）；本轮 QA 仅核验 `origin/master...HEAD` 中与 VM V3 相关改动，不对这些无关脏文件作功能结论。

## Test Results
- 构建：通过
- Go VM 测试：通过
- Go race 测试：通过
- Python testdata 回归：通过
- Node.js testdata 回归：通过

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以继续进入后续流程；若发布前需要更高置信度，建议补充独立 benchmark 明细记录与跨环境兼容性矩阵验证。
