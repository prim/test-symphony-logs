# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`go test -tags vm_v3 ./common/vm/...` 与 `common/vm/v3_select_tag_test.go`、`common/vm/v3_select_test.go` 相关测试通过，`VMV3` 已注册且可切换。
- **关键指标**: `go test -tags vm_v3 ./common/vm/...` 通过；`common/vm/v3/` 目录与 `common/vm/select_v3.go` 存在。
- **备注**: 同时核对了 build tag 冲突文件，`select_conflict.go` 已包含 `vm_v3` 冲突保护。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，未报告 data race。
- **关键指标**: race 测试通过；`TestConcurrentLazyLoadRaceFree` 断言首访仅加载 1 次。
- **备注**: 静态阅读 `common/vm/v3/vm.go` 中 `Segment.ensureLoaded()`，确认使用 `atomic` 控制加载状态。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 关键合约测试通过，覆盖 batch API、短结果缓冲、跨 segment iterator、关闭释放、快路径排序复用等行为。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'Test(BatchReadAPIContract|BatchReadHandlesShortResultsBuffer|BindSourceInvalidatesStaleSegmentData|PrepareVMReusesTemplateButReturnsFreshInstance|ClosePublishesNewStateBeforeClosingOldReader|CloseWaitsForInFlightFastPathReader|CloseReleasesLoadedSegmentAndLinearReferences|BatchReadSortedInputFastPath|ConcurrentLazyLoadRaceFree|PageTableMemoryOverhead)$' -count=1` 通过。
- **备注**: 已重点复核最近修复点：模板复用返回新实例、Close 释放 segment/linear 引用。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中存在并可执行随机访问、顺序扫描、并发访问、热点、未对齐、跨 segment、cache miss、安全读取、mixed workload、cold start、Python/V8 场景 benchmark。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchmem -count=1` 通过。
- **备注**: 额外发现同文件还提供了 page-table 专项 benchmark。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中 V3 对比 benchmark 与守护测试存在，`TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths` 通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1` 通过。
- **备注**: 对比覆盖了 page-table 稀疏布局场景。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 的 testdata 主链路用例均通过，未见明显统计回退。
- **关键指标**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；`python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 通过。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，本轮实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过，页表额外开销未超过测试设定阈值。
- **关键指标**: 已包含在 V3 关键合约测试命令中并通过。
- **备注**: 静态阅读 `common/vm/v3/pagetable.go`，`MemoryBytes()` 已统计 `pageEntry` 与 sub-table 开销。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 基准测试可运行，但随机访问和 8/4 字节随机读取结果仍高于 issue 指定阈值。顺序扫描与并发访问满足目标。
- **关键指标**:
  - `BenchmarkGetMemory8-48 = 4.043 ns/op`
  - `BenchmarkGetMemory4-48 = 4.045 ns/op`
  - `BenchmarkRandomAccess-48 = 4.018 ns/op`
  - `BenchmarkSequentialScan-48 = 2.844 ns/op`
  - `BenchmarkConcurrentAccess-48 = 0.2654 ns/op`
- **备注**: 根据 issue / plan 的 AC3 要求，随机访问需 `< 4ns`；当前结果略高于门槛，因此不能判定 AC3 通过。

## 失败的 Test Case

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: V3 随机访问延迟 `< 4ns`，并满足相对 V2 30%+ 提升；顺序扫描 `< 3ns`；并发访问 `< 1.5ns`。
- **实际行为**: 顺序扫描与并发访问通过，但 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkRandomAccess` 分别为 `4.043 ns/op`、`4.045 ns/op`、`4.018 ns/op`，均未严格低于 `< 4ns` 门槛。
- **复现步骤**:
  1. 在仓库根目录执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess)$' -benchmem -count=1`。
  2. 观察输出中的 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkRandomAccess` 数值。
- **可能原因**: 最近修复主要聚焦生命周期隔离、Close 释放与 batch 快路径；随机读取热路径仍接近但未越过 AC3 的严格阈值，可能还存在页表/缓存命中路径上的固定开销。
- **建议修复**: 优先继续针对随机访问热路径做定向优化，并在稳定环境下重复基准验证，确保结果留出足够裕量而不是仅贴线通过。

## Findings
- 发现 1：**AC3 当前不满足**。尽管 V3 已明显接近目标，但本轮实测 `BenchmarkRandomAccess = 4.018ns/op`，仍未满足 `< 4ns` 的硬门槛，因此本轮验收必须判定 FAIL。
- 发现 2：针对人审反馈“不要改 `cmd/*.py`”，本次分支相对 `origin/master...HEAD` 的变更列表中未包含 `cmd/*.py` 文件，约束已满足。
- 发现 3：最近一轮修复针对缓存模板复用与 Close 生命周期释放的问题已得到测试覆盖，相关回归在本轮未复现。

## Test Results
- 构建：`./maze --build` 通过
- VM 单元/集成：`go test -tags vm_v3 ./common/vm/... -count=1` 通过
- V3 合约测试：通过
- race 测试：通过
- V3 vs V2 对比守护：通过
- Python testdata：通过
- Node.js testdata：通过
- 性能门槛：**部分失败（随机访问 > 4ns）**

## 未执行项
- 无。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [x] 顺序扫描 `< 3秒/等价门槛` 对应 AC4 已通过（2.844 ns/op）
- [x] 并发访问 `< 1.5ns` 对应 AC5 已通过（0.2654 ns/op）
- [ ] 随机访问 `< 4ns` 对应 AC3 通过
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 8
- 失败: 1 / 8
- 建议: **暂不建议发布**。主链路功能、回归与最近修复点均已稳定，但 AC3 性能硬指标尚未达标，需要继续优化随机访问热路径后再复验。
