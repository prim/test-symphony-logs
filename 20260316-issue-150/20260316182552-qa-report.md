# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #150 最近一次修复进行复验，重点核对了 human review 指出的范围约束、VM facade 生命周期修复、V3 显式公共入口绑定、关键 Go 回归、关键性能门槛，以及 Python / Node.js 主链路回归。已执行构建、静态检查、Go 单测、race 测试、关键 benchmark 与 2 个 testdata 用例，未发现阻塞发布的问题。

## Findings

- 未发现 `cmd/*.py` 变更，满足 human review “不要改 cmd/*.py” 的强约束。
- `common/vm/select_v3.go` 已改为显式注册 `PublicGetMemory*` / `PublicGetMemoryRange`，不再依赖函数指针推断绑定语义。
- `common/vm/facade.go` 已实现 snapshot + inflight 生命周期治理，`CloseCurrent`、`SwitchTo*`、`dispatchInit`、`dispatchClose` 具备等待在途读请求完成后再关闭旧实现的保护。
- `common/vm/shared/helpers.go` 中 `isPrivateMmap` 缺失映射时已降级为 debug 日志回退，不再直接 panic；对应 Node.js 回归本轮实际通过。
- 关键性能抽样满足 issue 阈值：随机访问约 3.818~3.856ns/op，顺序扫描页表路径约 2.840~2.879ns/op，并发页表路径约 0.571~0.587ns/op。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 存在且 `//go:build vm_v3 && !vm_lru && !vm_full && !vm_dual` 正确；`go test -tags vm_v3 ./common/vm/...` 通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/...` 全部通过。
- **备注**: `common/vm/v3_select_tag_test.go` 与 `common/vm/init_v3_integration_test.go` 均存在并覆盖 V3 选择逻辑。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `TestConcurrentLazyLoadRaceFree` 存在并通过；额外执行 facade 生命周期相关 race 测试通过，未见数据竞争。
- **关键指标**: `go test -race -tags vm_v3 ./common/vm -run 'TestCloseCurrentWaitsForInflightReaders|TestSwitchWaitsForInflightReadersBeforeClosingPreviousImplementation|TestDispatchInitWaitsForInflightReadersBeforeReinit|TestSwitchPublishesOnlyAfterInit' -count=1` 通过。
- **备注**: 本轮 race 主要覆盖最新修复涉及的并发关闭/切换路径；`TestConcurrentLazyLoadRaceFree` 通过普通单测复验。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 批量读取、短结果缓冲区、iterator 跨 segment、页表 fallback、未对齐/跨 segment 范围读取相关测试均存在且通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm/v3 -run 'TestPageTableMemoryOverhead|TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment' -count=1` 通过。
- **备注**: 从测试名和实现看，API 合约覆盖了短缓冲区、边界地址与 fallback 行为。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 在 `common/vm/v3/v3_contract_test.go` 中确认存在 14 个 required benchmark，包括随机访问、顺序扫描、并发访问、未对齐、跨 segment、cache miss、安全读取、mixed workload、cold start、Python/V8 场景。
- **关键指标**: grep 命中 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **备注**: AC8 所需覆盖项齐全。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中已包含 CompareV3 系列 benchmark 与性能守卫测试；`TestV3ComparePerformanceGuard`、`TestV3CompareBenchmarksPresent`、`TestV3SparseFixturesExerciseNonLinearPaths` 实际运行通过。
- **关键指标**: `go test -tags vm_v3 ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1` 通过。
- **备注**: 文件中存在 `requireV3FasterThanV2` 守卫逻辑，不再是纯占位测试。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: Python 与 Node.js 两个 testdata 用例均通过，Node.js 用例未出现之前提到的 `isPrivateMmap` 缺失映射 panic。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311` 通过
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 通过
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，本轮实际执行了 2 个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 存在并通过。
- **关键指标**: 目标测试包含在 `go test -tags vm_v3 ./common/vm/v3 -run 'TestPageTableMemoryOverhead|...' -count=1` 且通过。
- **备注**: 本轮未单独输出数值样本，但对应门槛测试未失败。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 实际执行关键 benchmark 抽样 3 轮，结果均达到 issue 中 AC3/AC4/AC5 阈值。
- **关键指标**:
  - 随机访问 `BenchmarkRandomAccess`: 3.818 / 3.848 / 3.856 ns/op，满足 **< 4ns**
  - 顺序扫描 `BenchmarkSequentialScanPageTable`: 2.879 / 2.842 / 2.840 ns/op，满足 **< 3ns**
  - 并发访问 `BenchmarkConcurrentAccessPageTable`: 0.5820 / 0.5709 / 0.5867 ns/op，满足 **< 1.5ns**
- **备注**: 关键门槛达到要求；未在本轮单独输出与 V2 基线的完整表格，但 compare guard 测试已通过。

## 失败的 Test Case（如有）

本轮无失败项。

## Test Results

- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm/...`：通过
- `go test -race -tags vm_v3 ./common/vm -run 'TestCloseCurrentWaitsForInflightReaders|TestSwitchWaitsForInflightReadersBeforeClosingPreviousImplementation|TestDispatchInitWaitsForInflightReadersBeforeReinit|TestSwitchPublishesOnlyAfterInit' -count=1`：通过
- `go test -tags vm_v3 ./common/vm -run 'TestV3ComparePerformanceGuard|TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths' -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestPageTableMemoryOverhead|TestConcurrentLazyLoadRaceFree|TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment' -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScanPageTable|ConcurrentAccessPageTable)$' -benchmem -count=3`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 未执行项（如有）

- 未逐个执行 `TC-004` 中全部 benchmark 的真实跑数，只做了静态存在性核对与关键门槛抽样。
- 未单独运行 test case 文档中列出的完整正则 benchmark 命令；但已通过 grep + compare/guard 测试 + 关键 benchmark 抽样形成替代证据。
- 未执行更多语言/分配器/大内存进程场景；这些不影响本次 issue #150 最近修复点的主结论，但会影响更广泛兼容性与长期性能置信度。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [x] 随机访问 < 4ns
- [x] 顺序扫描 < 3ns
- [x] 并发访问 < 1.5ns
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以继续推进；从本轮复验结果看，最新修复已满足 issue #150 当前验收要求，且 human review 指出的 `cmd/*.py` 范围约束未再被违反。
