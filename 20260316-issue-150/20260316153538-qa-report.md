# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #150 的最近修复点进行了静态核对与分阶段动态验收，关键结论如下：
- `common/vm/v3`、`select_v3.go`、facade 生命周期与 V3 初始化路径均已落地，`vm_v3` 构建可用。
- 人工 review 指出的“不要改 `cmd/*.py`”约束已满足，本次分支相对 `origin/master` 无 `cmd/*.py` 变更。
- 关键 Go 测试、race 测试、性能守护测试，以及 Python/Node.js 主链路 testdata 用例均通过。
- 性能门槛复测结果满足 AC3/AC4/AC5；页表开销测试通过，满足 AC6。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 已注册 `VMV3`，`./maze --build` 成功，`go test -tags vm_v3 ./common/vm/...` 通过。
- **关键指标**: 构建成功；`TestVMV3BuildTagCompilesAndSelectsV3`、`TestV3OnlyBuildFallsBackToV3AndInitializesCurrentImplementation` 通过。
- **备注**: 同时静态核对了 `select_conflict.go`、`select_default.go` 等 build tag 互斥逻辑，已纳入 `vm_v3`。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run 'TestConcurrentLazyLoadRaceFree|TestBindSourceDropsStaleConcurrentLoad|TestMemoryIteratorReleasesReadOwnerAfterExhaustion|TestMemoryIteratorCloseReleasesReadOwner|TestLinearSpanShortRangeDoesNotUnderflow|TestInitSmallCoreUsesMmapCloserWhenAvailable'` 通过，无竞态报告。
- **关键指标**: race detector 通过；退出码 0。
- **备注**: 本轮重点回归了小 core `OpenMmap()` + closer 路径，与开发报告声称修复点一致。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 该类合约测试包含在 `go test -tags vm_v3 ./common/vm/...` 中整体通过，未发现 batch/iterator 回归。
- **关键指标**: `maze/common/vm/v3` 包测试通过。
- **备注**: 本轮未单独拆分子测试命令，但已通过包级测试覆盖对应合约。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中已存在 14 个必需 benchmark；执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchtime=1x` 成功。
- **关键指标**: 14/14 必需 benchmark 可被发现并执行。
- **备注**: 覆盖了 AC8 中 Must/Should/Nice 场景集合。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: `common/vm/bench_compare_test.go` 中存在 `BenchmarkCompareV3GetMemory8/4`、`SequentialScan`、`RandomAccess`、`ConcurrentAccess`、`GetMemoryRange`、`ColdStart` 及 page table 变体；`go test -tags vm_v3 ./common/vm -run 'TestV3CompareBenchmarksPresent|TestV3SparseFixturesExerciseNonLinearPaths|TestV3ComparePerformanceGuard'` 通过。
- **关键指标**: compare guard 测试通过，耗时约 91.740s。
- **备注**: 已验证对比测试不是空壳，且包含非线性 page table 路径固件检查。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 与 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 均通过。
- **关键指标**: Python 用例 1/1 通过；Node.js 用例 1/1 通过。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的硬性要求，且本轮实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `go test -tags vm_v3 ./common/vm/v3 -run TestPageTableMemoryOverhead` 通过。
- **关键指标**: 单测通过；退出码 0。
- **备注**: 报告中未直接打印百分比，但 AC6 守护测试已通过，可接受为自动化验收结果。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem`，结果满足 issue 阈值。
- **关键指标**:
  - `BenchmarkSequentialScan`: **2.903 ns/op** `< 3ns`
  - `BenchmarkRandomAccess`: **3.900 ns/op** `< 4ns`
  - `BenchmarkConcurrentAccess`: **0.2347 ns/op** `< 1.5ns`
- **备注**: 本轮在当前环境下满足 AC3/AC4/AC5。

## 失败的 Test Case（如有）

- 无

## 已执行的检查项

- 阅读最新开发报告、test case、需求文档，核对 AC 与 TC 映射。
- 检查相对 `origin/master` 的代码变更范围。
- 静态确认 `common/vm/v3`、`common/vm/select_v3.go`、facade 生命周期测试、V3 初始化集成测试均存在。
- 静态确认 compare benchmark 与 V3 benchmark 覆盖要求已落地。
- 静态确认未修改 `cmd/*.py`，符合人工 review 约束。
- 动态执行：`./maze --build`。
- 动态执行：`go test -tags vm_v3 ./common/vm/...`。
- 动态执行：V3 race 测试。
- 动态执行：V3 compare/performance guard 测试。
- 动态执行：V3 benchmark 覆盖检查。
- 动态执行：Python 与 Node.js testdata 回归。
- 动态执行：页表内存开销测试。

## 未执行项（如有）

- Python 2.7 兼容矩阵未在本轮单独执行。
- 多 glibc 版本矩阵未在本轮单独执行。
- 多 Node.js 版本矩阵未在本轮单独执行。
- 未对 >1GB 真实大进程进行独立扫描时间/内存占用压测。

未执行原因：当前工作树与现有自动化用例主要覆盖单机默认环境；本轮按“低成本检查尽量完整 + 高成本验证分阶段早停”策略，优先完成与 review 修复点、主链路正确性及关键性能门槛直接相关的自动化验证。

可能影响范围：主要影响扩展兼容性与真实大规模性能结论，不影响本轮对 AC1~AC9 在当前环境下的通过判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 当前分支在本轮验收范围内可判定为通过；如进入更高置信度发布前阶段，建议补做 Python 2.7、多 glibc、多 Node.js 版本矩阵，以及 >1GB 真实目标进程性能压测。
