## Summary
本轮为性能门槛修复，聚焦处理最新 QA 失败项 TC-008。保留既有生命周期隔离与内存释放修复，仅对 `common/vm/v3/batch.go` 的线性 span 热路径地址换算做最小改动，将 `uintptr(address) + span.bias` 改为基于 `span.base` 与相对偏移的直接寻址，降低随机读取固定开销，使 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkRandomAccess` 重新稳定低于 `< 4ns` 门槛。

结论：**COMPLETE**

# 开发报告

## 完成的任务
- [x] Task 1: 阅读最新 QA、code review 与需求文档，确认本轮仅剩 AC3 性能门槛问题需要修复
- [x] Task 2: 分析 V3 随机访问热路径，定位线性 span 快路径中的地址换算固定开销
- [x] Task 3: 在 `common/vm/v3/batch.go` 中以最小改动优化 8/4 字节读取快路径
- [x] Task 4: 执行构建、race、合约回归、性能 benchmark 与 Python 主链路回归验证

## 修改的文件
- `common/vm/v3/batch.go`: 将 linear span 快路径改为 `span.base + offset` 直接寻址，减少随机访问热路径上的地址换算成本

## 自测结果
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|RandomAccess)$' -benchmem -count=1`
- 测试结果: 通过；`BenchmarkGetMemory8=3.818 ns/op`，`BenchmarkGetMemory4=3.927 ns/op`，`BenchmarkRandomAccess=3.956 ns/op`
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run 'Test(BatchReadAPIContract|BatchReadHandlesShortResultsBuffer|BindSourceInvalidatesStaleSegmentData|PrepareVMReusesTemplateButReturnsFreshInstance|ClosePublishesNewStateBeforeClosingOldReader|CloseWaitsForInFlightFastPathReader|CloseReleasesLoadedSegmentAndLinearReferences|BatchReadSortedInputFastPath|ConcurrentLazyLoadRaceFree|PageTableMemoryOverhead)$' -count=1`
- 测试结果: 通过
- 测试命令: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- 测试结果: 通过
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过

## 验收核对
- AC1: PASS 已在本轮前通过，且本轮 `./maze --build` 未回归
- AC2: PASS 本轮复测 `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过
- AC3: PASS 本轮复测 `BenchmarkGetMemory8/4/RandomAccess` 分别为 `3.818/3.927/3.956 ns/op`，均严格低于 `< 4ns`
- AC4: PASS 沿用最新 QA 报告结论，顺序扫描 `2.844 ns/op`，本轮未改相关路径
- AC5: PASS 沿用最新 QA 报告结论，并发访问 `0.2654 ns/op`
- AC6: PASS 沿用最新 QA 报告结论，页表额外内存开销测试通过
- AC7: PASS 本轮执行 Python 主链路回归；Node.js 以最新 QA 报告通过结果为准，本轮修改未涉及 Node.js 逻辑
- AC8: PASS 基准覆盖项已在最新 QA 中核对通过，本轮未删除或改弱 benchmark
- AC9: PASS 最新 QA 已验证 V3 vs V2 compare tests 通过；本轮仅优化热路径，不影响 compare 用例结构

## 剩余工作
- 未完成项: 无
- 未验证项: 无

## Self-Review 发现与修复
- 发现快路径使用 `uintptr(address) + span.bias` 会在随机读取路径引入额外地址换算成本；已改为基于 `span.base` 与 `address - span.begin` 的直接偏移寻址
- 复核未引入新的生命周期耦合，也未回退之前对 Close 释放与模板实例隔离的修复

## 注意事项
- 本轮严格遵循 human review 约束，未修改 `cmd/*.py`
- 未修改任何 `testdata/` 下测试文件
- 仓库中存在与本任务无关的其他未提交改动（如 `common/utils.go`、`nodejs/map_info.go`），提交时应显式排除

## Changes
- `common/vm/v3/batch.go`: 优化 linear span 热路径地址计算，降低随机访问固定开销

## Testing
- 已按上述命令完成构建、race、合约回归、性能 benchmark 与 Python testdata 验证
