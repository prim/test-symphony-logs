# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮已完成静态审查、构建、关键单测、race 检查、性能基准以及至少一个 `testdata` 用例验证。结果显示：
- 构建、V3 目录接线、无锁首访并发、批量 API / iterator 合约、页表开销、Python/Node.js 主链路回归均通过；
- 但 **随机访问绝对性能仍未达到 AC3 门槛**；
- 同时 **V3 vs V2 对比覆盖范围未达到 AC9“所有场景”要求**，目前仅补了 5 个 V3 对比 benchmark，未覆盖现有 compare suite 的全部场景。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 已注册 `VMV3`，`v3_select_tag_test.go` / `v3_select_test.go` 存在且 `GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 ./common/vm ./common/vm/shared` 通过。
- **关键指标**: build tag `vm_v3` 编译通过；`SwitchToV3()` 可切换到 `VMV3`。
- **备注**: `facade.go` 已改为通过原子快照分发实现，避免切换时直接重绑全局函数指针的热路径问题。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `GOFLAGS='-tags=vm_v3' go test -race ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过。
- **关键指标**: race detector 无 data race；测试断言 segment 首次并发访问仅加载 1 次。
- **备注**: `Segment.ensureLoaded()` 使用 `atomic.Pointer` 与 `atomic.Uint32` 实现无锁懒加载。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: `TestBatchReadAPIContract`、`TestBatchReadHandlesShortResultsBuffer`、`TestMemoryIteratorTraversesAcrossSegments`、`TestPageTableLookupAndFallback`、`TestGetMemoryRangeHandlesUnalignedAndCrossSegment` 均通过。
- **关键指标**: 批量读取短结果缓冲区可安全截断处理；iterator 可跨 segment 继续遍历；未对齐与跨 segment range 读取通过。
- **备注**: `batch.go` 已实现 `GetMemoryBatch8/4` 与 `MemoryIterator`。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中已定义以下 benchmark：`BenchmarkGetMemory8`、`BenchmarkGetMemory4`、`BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess`、`BenchmarkHotspotAccess`、`BenchmarkUnalignedAccess`、`BenchmarkCrossSegment`、`BenchmarkCacheMiss`、`BenchmarkGetMemorySafe`、`BenchmarkMixedWorkload`、`BenchmarkColdStart`、`BenchmarkPythonObjectTraverse`、`BenchmarkV8HeapScan`。
- **关键指标**: 覆盖 AC8 所列 must/should/nice 场景。
- **备注**: 覆盖性满足，但这不等价于全部性能门槛满足。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **实际行为**: `TestV3ComparePerformanceGuard` 通过，且 `BenchmarkCompareV3GetMemory8/GetMemory4/SequentialScan/RandomAccess/ConcurrentAccess` 可执行并显示 V3 快于 V2；但静态审查发现 `common/vm/bench_compare_test.go` 的 V3 compare benchmark 只补了 5 个场景，未覆盖该文件现有 compare suite 的全部场景。
- **关键指标**: 已实现 V3 compare 场景数 5；现有 compare benchmark 总数 12，其中旧 compare 场景至少还包括 `BenchmarkCompareGetMemoryRange`、`BenchmarkCompareColdStart` 等未提供对应 V3 compare 版本。
- **备注**: 不满足 AC9 中“V3 vs V2 对比测试全部通过，且 V3 在所有场景优于 V2”的完整覆盖要求。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 与 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 均通过。
- **关键指标**: Python 复杂类型统计通过；Node.js heapsnapshot 对比用例通过。
- **备注**: 按人类备注，忽略 Node.js comprehensive 回归数据生成链路；本轮已执行至少一个 testdata 用例，且实际执行了两个。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过。
- **关键指标**: 页表额外开销未超过测试内定义的 20% 阈值。
- **备注**: `PageTable.MemoryBytes()` 与 `VM.PageTableMemoryBytes()` 已提供统计能力。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 关键 benchmark 已执行，但绝对性能仅部分满足门槛。
- **关键指标**:
  - `BenchmarkSequentialScan-48`: **3.415 ns/op**
  - `BenchmarkRandomAccess-48`: **4.886 ns/op**
  - `BenchmarkConcurrentAccess-48`: **0.2351 ns/op**
  - `BenchmarkCompareV3RandomAccess/random-access/v3-48`: **13.69 ns/op** vs V2 **18.83 ns/op**
- **备注**: 并发访问满足 AC5；随机访问未满足 AC3 `< 4ns`。顺序扫描方面，人类备注提到 `3.028 ns/op 可以了`，但本轮复测结果为 `3.415 ns/op`，高于备注中的可接受值，也高于原始 AC4 `< 3ns` 门槛，因此本轮仍判定未达标。

## 失败的 Test Case

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **预期行为**: `common/vm/bench_compare_test.go` 中应完成 V3 vs V2 的全场景对比覆盖，并能证明 V3 在所有要求场景优于 V2。
- **实际行为**: 当前仅有 5 个 V3 compare benchmark；未见对 `GetMemoryRange`、`ColdStart` 等 compare 场景的 V3 对应覆盖。
- **复现步骤**:
  1. 查看 `common/vm/bench_compare_test.go` 中所有 `BenchmarkCompare*` 定义。
  2. 对比其中 V3 专属条目与已有 compare suite 总量。
  3. 可见仅存在 `BenchmarkCompareV3GetMemory8/GetMemory4/SequentialScan/RandomAccess/ConcurrentAccess`。
- **可能原因**: 本轮修复重点放在性能守护测试与主链路稳定性，未补齐 compare suite 的完整 V3 映射。
- **建议修复**: 补齐剩余 compare 场景的 V3 对比 benchmark，并将“全部场景优于 V2”的断言落实到测试中。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: 随机访问 < 4ns；顺序扫描 < 3ns（或至少达到人类备注中可接受的约 3.028ns）；并发访问 < 1.5ns。
- **实际行为**: 随机访问 4.886ns/op，顺序扫描 3.415ns/op，并发访问 0.2351ns/op。
- **复现步骤**:
  1. 执行：`GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 ./common/vm -run '^$' -bench 'Benchmark(SequentialScan|RandomAccess|ConcurrentAccess|CompareV3(GetMemory8|GetMemory4|SequentialScan|RandomAccess|ConcurrentAccess))$' -benchmem -benchtime=40ms -count=1`
  2. 观察 `maze/common/vm/v3` 包下输出的 `SequentialScan` 与 `RandomAccess` 数值。
- **可能原因**: 当前线性 span / 页表 / L1 cache 路径虽已显著优于 V2，但热路径仍未压到 issue 目标；顺序扫描和随机访问仍存在额外分发、地址模式或局部缓存命中方面的常数开销。
- **建议修复**: 继续分析 `GetMemory` 热路径的分发与缓存命中成本，重点优化随机访问和顺序扫描场景；修复后重新在相同 benchtime 下复测。

## Findings

- 发现 1：**AC3 未通过**。本轮随机访问 benchmark 为 `4.886 ns/op`，高于 `< 4ns` 门槛。
- 发现 2：**AC4 未通过**。虽然人类备注允许 `3.028 ns/op`，但本轮顺序扫描为 `3.415 ns/op`，仍未达到备注可接受值。
- 发现 3：**AC9 未通过**。V3 vs V2 compare suite 仅覆盖 5 个场景，未覆盖 `bench_compare_test.go` 现有全部 compare 场景。

## Test Results

- 构建：`./maze --build` ✅
- 单测：`GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 ./common/vm ./common/vm/shared` ✅
- race：`GOFLAGS='-tags=vm_v3' go test -race ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` ✅
- 合约/开销：`GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead' -count=1` ✅
- 对比守护：`GOFLAGS='-tags=vm_v3' go test ./common/vm -run TestV3ComparePerformanceGuard -count=1` ✅
- 性能基准：关键 benchmark 已执行，部分门槛未达标 ❌
- testdata：
  - `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` ✅

## 未执行项

- 未执行 `testdata/nodejs/20260211-comprehensive`：根据人类备注“忽略 Node.js comprehensive 回归数据生成链路”，本轮未将其作为阻塞项。
- 未逐项执行所有 V3 benchmark 名称的独立 `go test -bench` 命令：已通过静态审查确认 AC8 场景覆盖，并通过关键 benchmark 验证主性能链路；其余不影响当前 FAIL 结论。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过（基于现有 Node.js coredump 用例主链路）

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结

- 通过: 6 / 8
- 失败: 2 / 8
- 建议: **当前不建议发布。** 需至少修复随机访问与顺序扫描绝对性能未达标问题，并补齐 AC9 要求的全场景 V3 vs V2 compare 覆盖后，再次验收。
