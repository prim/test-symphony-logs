# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 `prim/maze#150` 的最新修复执行了静态核对、Go 构建/测试、关键性能基准，以及 Python / Node.js 主链路 testdata 回归。当前未复现阻塞性问题；同时已确认满足最新 human review 强约束：相对 `origin/master...HEAD` 未修改 `cmd/*.py`。

## Findings

- 静态核对确认本轮修复点集中在 `common/vm/facade.go`、`common/vm/shared/helpers.go` 与对应测试，未越界修改 `cmd/*.py`。
- `facade` 生命周期切换已调整为“先解绑/排空/关闭旧实现，再初始化并发布新实现”，与需求中的切换安全性一致。
- `IsPrivateMmap` 已恢复为缺失映射时显式失败，避免异常 maps 被静默误判为普通可读映射，符合准确性优先原则。
- V3 关键性能指标实测满足 AC3/AC4/AC5；V3 vs V2 对比基准本轮亦均表现为 V3 更快。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `./maze --build` 成功；`go test -tags vm_v3 ./common/vm/... -count=1` 通过；`common/vm/select_v3.go` 存在且 `VMV3` 已注册绑定。
- **关键指标**: `common/vm`、`common/vm/v3` 测试均通过。
- **备注**: 满足 AC1。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过，未报告 data race。
- **关键指标**: race detector 通过。
- **备注**: 满足 AC2。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: 已通过 `go test -tags vm_v3 ./common/vm/... -count=1` 覆盖 V3 合约测试集；静态检查确认 `common/vm/v3/batch.go` 提供 `GetMemoryBatch8/4` 与顺序窗口化批量读取逻辑。
- **关键指标**: V3 包测试通过。
- **备注**: 对应 AC8 的 API 合约部分已覆盖。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: PASS
- **实际行为**: 静态核对 `common/vm/v3/v3_contract_test.go` 中存在 `BenchmarkSequentialScan`、`BenchmarkRandomAccess`、`BenchmarkConcurrentAccess` 及页表扩展基准；`common/vm/bench_compare_test.go` 中存在 V3 对比基准。
- **关键指标**: grep 命中所需 benchmark 定义。
- **备注**: 满足 AC8 的核心覆盖要求。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: PASS
- **实际行为**: 运行对比基准成功，V3 在已执行场景中均优于 V2。
- **关键指标**:
  - `CompareV3GetMemory8`: V2 `19.56ns/op`，V3 `16.43ns/op`
  - `CompareV3GetMemory4`: V2 `19.50ns/op`，V3 `13.57ns/op`
  - `CompareV3SequentialScan`: V2 `19.24ns/op`，V3 `16.40ns/op`
  - `CompareV3ConcurrentAccess`: V2 `2.571ns/op`，V3 `2.066ns/op`
  - `CompareV3ConcurrentAccessPageTable`: V2 `3.739ns/op`，V3 `3.328ns/op`
- **备注**: 满足 AC9。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 与 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 均通过。
- **关键指标**:
  - Python: `pymempool_objects = 68626`，多类对象计数全部通过校验
  - Node.js: `derived total_objects = 52307`，Map/Set/TypedArray/Promise 等均达阈值
- **备注**: 满足 AC7。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: 本轮通过 `go test -tags vm_v3 ./common/vm/... -count=1` 间接覆盖；静态检查确认 `PageTable.MemoryBytes()` 与 `TestPageTableMemoryOverhead` 相关实现存在，最新开发报告亦记录该项通过。
- **关键指标**: 单测集通过，未见页表结构缺失。
- **备注**: 满足 AC6。

### TC-008: 性能目标门槛校验
- **结果**: PASS
- **实际行为**: 关键基准命令通过，随机访问、顺序扫描、并发访问三项均达到 issue 阈值。
- **关键指标**:
  - `BenchmarkRandomAccess = 3.912ns/op`（< 4ns）
  - `BenchmarkSequentialScan = 2.846ns/op`（< 3ns）
  - `BenchmarkConcurrentAccess = 0.2291ns/op`（< 1.5ns）
- **备注**: 满足 AC3、AC4、AC5。

## Test Results

- `./maze --build`：通过
- `go test -tags vm_v3 ./common/vm/... -count=1`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 ./common/vm -run '^$' -bench 'Benchmark(SequentialScan|RandomAccess|ConcurrentAccess)$|BenchmarkCompareV3(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable)$' -benchtime=100ms -count=1 -benchmem`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- Python 2.7 兼容性动态回归未执行；本轮未提供对应 testdata 与隔离运行矩阵。
- 不同 glibc 版本兼容性矩阵未执行；当前工作树环境为单一 Linux 运行环境。
- 不同 Node.js 版本兼容性矩阵未执行；本轮仅使用现有 Node.js coredump 用例做回归。
- `AC8` 表格中的部分 “Should/Nice” benchmark 名称未逐一单独跑命令验收，本轮采用静态存在性核对 + 核心 benchmark 动态验证的组合策略。

上述未执行项不阻塞本轮对最新修复点与核心 AC 的验收，但会影响更广泛兼容性声明范围。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] 随机访问延迟 < 4ns
- [x] 顺序扫描延迟 < 3ns
- [x] 并发访问延迟 < 1.5ns
- [x] 页表额外内存开销 < 20%

## 总结

- 通过: 8 / 8
- 失败: 0 / 8
- 建议: 可以通过本轮 QA。当前实现满足 issue #150 的核心验收标准，且最新修复已消除人审指出的范围问题与生命周期/准确性风险；后续如需发布级兼容性声明，建议再补 Python 2.7、glibc/Node.js 版本矩阵验证。
