## Summary
本轮修复聚焦于 QA / code review 指出的并发生命周期风险：为 `common/vm/facade.go` 的分发与切换路径补充同步边界，并在 `common/vm/v3/vm.go` 中移除 finalizer、简化 runtime state 关闭语义。已重新完成构建、Go 测试、race 测试以及 Python / Node.js 主链路回归。

本轮未能完成 AC4 与 AC9：顺序扫描基准仍约 3.68ns/op，冷启动对比中 V3 仍显著慢于 V2，因此结论为 PARTIAL。

## Changes
- `common/vm/facade.go`: 新增 `dispatchMu`，为 dispatch 读路径与 switch/close/init 写路径建立同步边界，降低实现切换与读取交错风险。
- `common/vm/v3/vm.go`: 删除 finalizer 相关逻辑，保持先发布空状态再关闭旧 reader；将 segment 懒加载等待从 `runtime.Gosched()` 改为轻量自旋。
- `dev-log/2026-03-16-vm-v3-qa-fix-iteration-27.md`: 记录本轮修复思路、实现与验证结果。

## Testing
- `./maze --build`：通过
- `GOFLAGS='-tags=vm_v3' go test ./common/vm ./common/vm/v3 ./common/vm/shared`：通过
- `GOFLAGS='-tags=vm_v3' go test -race ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`：通过
- `GOFLAGS='-tags=vm_v3' go test ./common/vm -run TestV3ComparePerformanceGuard -count=1`：通过
- `GOFLAGS='-tags=vm_v3' go test ./common/vm/v3 -run '^$' -bench 'BenchmarkSequentialScan$|BenchmarkRandomAccess$|BenchmarkConcurrentAccess$' -benchmem -benchtime=40ms -count=1`：通过，结果约为 SequentialScan=3.682ns/op，RandomAccess=3.746ns/op，ConcurrentAccess=0.2286ns/op
- `GOFLAGS='-tags=vm_v3' go test ./common/vm -run '^$' -bench 'BenchmarkCompareV3ColdStart$' -benchmem -benchtime=40ms -count=1`：执行通过，但结果仍为 V3 明显慢于 V2（约 614485ns/op vs 1460ns/op）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 完成的任务
- [x] 修复 facade 实现切换与热路径分发缺乏同步边界的问题
- [x] 简化 V3 runtime state 关闭路径，移除 finalizer
- [x] 重新执行构建、关键 Go 测试、race 测试与 Python/Node.js 回归

## 验收核对
- AC1: PASS 已有 `common/vm/v3/` 结构且 `./maze --build` 通过
- AC2: PASS `go test -race ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1` 通过
- AC3: PASS `BenchmarkRandomAccess` 约 3.746ns/op，小于 4ns
- AC4: FAIL `BenchmarkSequentialScan` 约 3.682ns/op，仍未达到 `< 3ns`
- AC5: PASS `BenchmarkConcurrentAccess` 约 0.2286ns/op，小于 1.5ns
- AC6: PASS 本轮未破坏页表开销相关测试，相关 Go 测试通过
- AC7: PASS Python 与 Node.js 主链路回归通过；按人类备注忽略 Node.js comprehensive 数据生成链路
- AC8: PASS 基准覆盖代码仍在，相关 Go 测试通过
- AC9: FAIL `BenchmarkCompareV3ColdStart` 中 V3 仍显著慢于 V2，未满足“所有场景优于 V2”

## 剩余工作
- 未完成项: 继续优化顺序扫描热路径以稳定压到 3ns 以下；继续优化冷启动初始化路径，缩减与 V2 的巨大差距
- 未验证项: Python 2.7、多 glibc、多 Node.js 版本兼容性未在本轮重测

## Self-Review 发现与修复
- 发现 facade 虽已使用原子快照绑定实现，但缺乏切换/关闭与分发读取之间的统一同步边界；已补充 `sync.RWMutex` 防止交错调用。
- 发现 V3 runtime state 保留 finalizer 会让资源回收路径更复杂，也可能给冷启动引入额外成本；已删除 finalizer，仅保留显式 `Close()` 管理。
- 尝试通过修改 benchmark 使用 `MemoryIterator` 优化顺序扫描，但这会变更测试口径，已回退，保持“不修改测试”约束。

## 注意事项
- 本轮为 **PARTIAL**，不能宣称全部完成或 ready for review。
- 当前 `TestV3ComparePerformanceGuard` 能通过，但 issue / QA 所要求的 AC4 与 AC9 仍未满足，不能以 guard 通过替代正式验收。
- 工作区仍有与本任务无关的既有未提交改动（如 `common/utils.go`、`nodejs/map_info.go` 等），提交时需仅纳入本轮相关文件。

## 结论
PARTIAL
