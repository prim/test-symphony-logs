## 结论

PARTIAL

# 开发报告

## 完成的任务
- [x] 修正 `common/vm/bench_compare_test.go` 的稀疏布局顺序扫描模型，避免跨 gap 线性递增导致的伪失败
- [x] 将 V3 vs V2 性能守护切换为非线性 page-table 固件，覆盖页表映射与 L1 cache 热路径
- [x] 删除 compare 测试中的重复/未使用辅助逻辑，收敛基准辅助函数
- [x] 清理 `common/vm/v3/batch.go` 中 code review 指出的重复窗口抽象，仅保留一套 `batchWindow`
- [x] 优化 V3 页表路径读取热路径，减少状态检查与不必要的 atomic 热点写入
- [x] 调整 L1 cache 索引散列与槽位规模，改善稀疏页表路径命中率
- [x] 修复 `bindSource()` 后缓存失效逻辑，确保切换 source 时不会命中旧 segment 数据

## 修改的文件
- `common/vm/bench_compare_test.go`: 修复稀疏顺序扫描 compare 基准；新增真正覆盖非线性页表路径的 sparse fixtures 与顺序地址生成
- `common/vm/v3/vm.go`: 优化 segment 热路径读取；减少 page-table 路径额外慢速分派；切换 source 时清理旧 segment 数据与缓存
- `common/vm/v3/nocopy.go`: 改进 L1 cache 索引散列并扩大槽位数量，降低并发热点冲突

## 自测结果
- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run 'Test(BatchReadAPIContract|BatchReadHandlesShortResultsBuffer|MemoryIteratorTraversesAcrossSegments|PageTableLookupAndFallback|GetMemoryRangeHandlesUnalignedAndCrossSegment|DirectReadHandlesUnalignedAddress|BindSourceInvalidatesStaleSegmentData|ClosePublishesNewStateBeforeClosingOldReader|PageTableMemoryOverhead|ConcurrentLazyLoadRaceFree)$' -count=1`
- 测试结果: 通过

- 测试命令: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
- 测试结果: 通过

- 测试命令: `go test -tags vm_v3 ./common/vm -run 'Test(V3ComparePerformanceGuard|V3CompareBenchmarksPresent|V3SparseFixturesExerciseNonLinearPaths)$' -count=1`
- 测试结果: 通过

- 测试命令: `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|GetMemoryRange|ColdStart)$' -benchtime=50ms -count=1`
- 测试结果: 单次通过；关键结果包括 `SequentialScanPageTable v2=27.77ns/op, v3=22.39ns/op`，`RandomAccessPageTable v2=27.44ns/op, v3=24.04ns/op`

- 测试命令: `go test -tags vm_v3 ./common/vm -run '^$' -bench 'BenchmarkCompareV3ConcurrentAccessPageTable' -benchtime=50ms -count=3`
- 测试结果: 不稳定；三次 V3 分别为 `1.515ns/op / 3.292ns/op / 0.8917ns/op`，V2 为 `0.9978ns/op / 0.9966ns/op / 0.9955ns/op`

- 测试命令: `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(GetMemory8|GetMemory4|SequentialScan|SequentialScanPageTable|RandomAccess|RandomAccessPageTable|ConcurrentAccess|ConcurrentAccessPageTable|HotspotAccess|UnalignedAccess|CrossSegment|CacheMiss|GetMemorySafe|MixedWorkload|ColdStart|PythonObjectTraverse|V8HeapScan)$' -benchtime=80ms -count=1`
- 测试结果: 通过；关键结果 `BenchmarkGetMemory8=3.892ns/op`，`BenchmarkSequentialScan=2.949ns/op`，`BenchmarkConcurrentAccess=0.2316ns/op`

- 测试命令: `./maze --build`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过

- 测试命令: `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
- 测试结果: 通过

## 验收核对
- AC1: PASS 已验证 `vm_v3` build tag、目录结构与主程序构建
- AC2: PASS 已验证 `go test -race` 无数据竞争
- AC3: PASS 已验证 `BenchmarkGetMemory8=3.892ns/op < 4ns`
- AC4: PASS 已验证 `BenchmarkSequentialScan=2.949ns/op < 3ns`
- AC5: PASS 已验证 `BenchmarkConcurrentAccess=0.2316ns/op < 1.5ns`
- AC6: PASS 页表内存开销测试通过
- AC7: PARTIAL 已验证 Python/Node.js 各 1 个主链路用例通过，但未覆盖更广版本矩阵
- AC8: PASS 必需 benchmark 场景命令均可执行
- AC9: PARTIAL 单次 compare 命令已通过，且顺序/随机 page-table 路径优于 V2；但 `ConcurrentAccessPageTable` 多次复跑仍存在明显抖动，尚不能证明“所有场景稳定优于 V2”

## 已完成项
- 修复 QA 报告指出的 `BenchmarkCompareV3SequentialScanPageTable` 伪失败
- 补强 code review 指出的非线性页表路径性能守护缺口
- 清理重复批量窗口实现
- 让 page-table 顺序/随机 compare 基准在单次正式命令下优于 V2

## 未完成项
- 稳定优化 `BenchmarkCompareV3ConcurrentAccessPageTable`，使其在重复基准中稳定优于 V2
- 完成文档更新与更完整兼容性矩阵验证

## 未验证项
- 更大规模进程真实性能验证
- Python 2.7 兼容性回归
- 多 glibc / 多 Node.js 版本矩阵

## Self-Review 发现与修复
- 发现 compare 顺序扫描基准在稀疏布局下跨越未映射 gap，导致 benchmark 本身失真；已改为按 segment 边界生成顺序地址
- 发现 V3 热路径在 page-table 场景下仍经由额外 slow-path 分派；已在已发布 runtime state 存在时直接走 `fastReadUint64/32`
- 发现 `bindSource()` 未主动清空旧 `segment.data`，潜在保留旧 source 数据；已在换源时清空 data/loading 与缓存
- 发现 `hot` 原子写在高并发页表路径下可能放大抖动；已从查找热路径中移除这类写入

## 注意事项
- 本轮修复后，QA 报告中的顺序扫描 page-table 失败已解决，但并发 page-table compare 仍有抖动
- 当前分支存在其他非本轮改动文件（如 `common/utils.go`、`nodejs/map_info.go`），本次提交未纳入这些文件
- 由于 AC9 仍为 PARTIAL，本轮不能宣称全部完成
