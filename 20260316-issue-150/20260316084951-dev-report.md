## Summary
- 清理 `common/vm/v3/vm.go` 中未接入热路径的遗留 `VM.hot` 字段与 `resetCaches()` 方法，消除误导性的未使用缓存状态。
- 保持其余 VM V3 逻辑不变，未触碰 human review 明确禁止修改的 `cmd/*.py`。

## Changes
- `common/vm/v3/vm.go`: 删除未使用的 `VM.hot` 字段与 `resetCaches()` 方法，保留实际生效的 L1 cache 与页表/linear-span 查址路径。

## Testing
- `./maze --build`
- `go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBindSourceInvalidatesStaleSegmentData|TestClosePublishesNewStateBeforeClosingOldReader' -count=1`
- `go test -tags vm_v3 ./common/vm -run 'TestVMV3BuildTagCompilesAndSelectsV3|TestSwitchToV3BindsImplementation|TestV3CompareBenchmarksPresent' -count=1`
