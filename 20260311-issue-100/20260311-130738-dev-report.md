# 开发报告

## 完成的任务
- [x] 将运行时 `cpp-db/` 目录迁移到 `3rd/misc/cpp-db/`
- [x] 更新 Python、GDB 脚本、发布流程中的硬编码路径，统一使用 `3rd/misc/cpp-db`
- [x] 保持兼容性修复，避免因 `bin/s3` 缺失或 `cpp.symbol_zero.output` 缺失导致流程异常
- [x] 通过指定构建与回归测试，并补充 `.symphony_prompt.md` 勾选状态

## 修改的文件
- `.maze.py`：调整拆分后模块的导入顺序，保持既有测试兼容性
- `maze_db.py`：将 `CDB` 路径改为 `3rd/misc/cpp-db`，补充嵌套目录创建、`bin/s3` 绝对路径计算与缺失保护
- `maze-py/cpp_analyzer.py`：更新 `cpp-db` 相关输出路径，并在缺少 `cpp.symbol_zero.output` 时安全返回
- `maze-py/messiahserver.py`：更新 `cpp.output` 路径
- `gdb/gdb-scripts/az_cpp.py`：更新 C++ 分析输出目录到 `3rd/misc/cpp-db`
- `gdb/gdb-scripts/az_netease_messiah.py`：更新 `cpp.output` 输出路径
- `3rd/misc/query-cpp-db.py`：更新示例查询路径
- `maze`：更新 release 打包目录结构，复制 `3rd/misc/cpp-db/*` 到发布目录
- `3rd/misc/cpp-db/uploader.py`：从旧目录迁移到新目录，并修正相对 `bin/s3` 路径
- `cpp-db/uploader.py`：删除旧位置文件

## 自测结果
- 测试命令: `python3 -m unittest tests.test_issue_100_move_cpp_db`
- 测试结果: 通过

- 测试命令: `python3 -m unittest tests.test_issue_85_maze_py_split_phase1`
- 测试结果: 通过

- 测试命令: `./maze --build && python3 -c "print('BUILD_OK')"`
- 测试结果: 通过，输出 `BUILD_OK`

- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过

## Self-Review 发现与修复
- 发现 `maze_db.py` 在 `CDB` 迁移到更深目录后，原先相对 `../bin/s3` 路径不再正确；已改为计算仓库内 `bin/s3` 的绝对路径
- 发现当前环境下 `bin/s3` 可能不存在，直接执行会产生噪音或失败；已增加存在性判断，无可执行文件时跳过上传下载
- 发现某些回归路径下不会生成 `3rd/misc/cpp-db/cpp.symbol_zero.output`；已在 `maze-py/cpp_analyzer.py` 中增加缺失保护，避免异常中断

## 注意事项
- `tests/test_issue_100_move_cpp_db.py`、`dev-flow/plan.md`、`dev-flow/test-cases.md`、本报告文件均位于工作区中，但根据要求不应提交时需排除
- 当前工作区还有 `maze-db/ctypes-c047672cae7964324658491e7dee26748ae5d2f8` 的附带变更，仅新增 `version` 字段；提交前应确认该变更是否确属本次需求范围
- `tmp/` 目录为临时文件目录，如无必要不应提交
