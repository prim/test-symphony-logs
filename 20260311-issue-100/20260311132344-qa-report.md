## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings

- 本次 issue #100 的核心目标已达成：`cpp-db/` 已迁移到 `3rd/misc/cpp-db/`，根目录旧目录已删除，运行时代码、GDB 脚本与 release 脚本的关键路径均已切换到新位置。
- 本轮实际回归中，之前阻塞验收的 `./bin/s3` 缺失 shell 报错未再复现；在 `bin/s3` 不存在的环境下，系统改为在 `maze.py.log` 中记录 `skip s3 ... missing /home/hcjn0770/temp/worktrees/issue-100/bin/s3`，表现为可接受的降级行为，无控制台噪音失败。
- 指定构建与强制 testdata 回归均通过，未观察到由 `cpp-db` 迁移引入的功能回归。

## 详细结果

### TC-001: 目录迁移到 3rd/misc
- **结果**: PASS
- **实际行为**: `3rd/misc/cpp-db/` 目录存在，根目录 `cpp-db/` 不存在，`uploader.py` 位于 `3rd/misc/cpp-db/uploader.py`。
- **关键指标**: `python3 -m unittest tests.test_issue_100_move_cpp_db tests.test_issue_85_maze_py_split_phase1` 通过；静态检查结果与目录结构一致。
- **备注**: 满足 AC1、AC2。

### TC-002: 运行时代码路径全部切换到新目录
- **结果**: PASS
- **实际行为**: `maze_db.py` 中 `CDB = DBC("3rd/misc/cpp-db")`；`maze-py/cpp_analyzer.py`、`maze-py/messiahserver.py`、`gdb/gdb-scripts/az_netease_messiah.py`、`3rd/misc/query-cpp-db.py` 的运行时路径均已切换到新目录。
- **关键指标**: 单元测试通过；代码审查未发现运行时代码中残留旧的 `"cpp-db` / `'cpp-db` / `release/cpp-db` 路径字面量。
- **备注**: 满足 AC3、AC4。

### TC-003: GDB 输出路径切换到新目录
- **结果**: PASS
- **实际行为**: `gdb/gdb-scripts/az_cpp.py` 中 `ensure_gdb_output_dirs()` 创建 `3rd/misc/cpp-db`；`cpp.output`、`cpp.symbol_zero.output`、`cpp.stack_locals.output` 的写入路径均已更新。
- **关键指标**: 单元测试通过；静态检查确认对应 open 路径全部指向 `3rd/misc/cpp-db/`。
- **备注**: 满足 AC5。

### TC-004: Release 脚本创建新的发布目录结构
- **结果**: PASS
- **实际行为**: `maze` 中已创建 `release/3rd/misc/cpp-db`，并从 `3rd/misc/cpp-db/*` 复制缓存数据，不再创建 `release/cpp-db`。
- **关键指标**: 单元测试通过；脚本静态检查通过。
- **备注**: 满足 AC6。

### TC-005: 构建验证
- **结果**: PASS
- **实际行为**: 执行 `./maze --build` 成功完成，无编译错误。
- **关键指标**: 构建命令返回成功。
- **备注**: 满足 AC7。

### TC-006: 指定 testdata 回归验证
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 后，输出 `All validations passed!` 与 `✅ Test PASSED`；控制台未出现 `./bin/s3: No such file or directory` 或 `没有那个文件或目录` 报错。
- **关键指标**: 用例验证通过；`maze.py.log` 中记录 6 条 `skip s3` 降级日志，说明缺失 `bin/s3` 时已安全跳过上传/下载。
- **备注**: 满足 AC8；同时确认此前 QA 失败点已消除。

## 失败的 Test Case（如有）

- 无

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `python3 -m unittest tests.test_issue_100_move_cpp_db tests.test_issue_85_maze_py_split_phase1`：通过
- `./maze --build`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `postman-db/coredump-default-2636550/maze.py.log`：确认 `bin/s3` 缺失时仅记录 `skip s3` 日志，无 shell 报错噪音

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 本次需求可判定为验收通过。
