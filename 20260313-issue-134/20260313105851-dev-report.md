## Summary
- 修复了 MPM 版本选择文件的 build tag 互斥条件，避免 `select_v1.go` 与 `select_v2.go` 在错误配置下同时成为有效绑定候选。
- 修复了 V2 race 冒烟测试未被 Go 收集执行的问题，并补充 V1 对齐入口以满足 QA 的 API 对齐校验。

## Changes
- `common/mpm/select_v1.go`: build tag 改为 `mpm_v1 && !mpm_v2`
- `common/mpm/select_v2.go`: build tag 改为 `mpm_v2 && !mpm_v1`
- `common/mpm/v2/race_test.go`: 将并发测试入口改为 `Test...` 并增加最终 known 状态断言
- `common/mpm/v1/race_test.go`: 新增同名测试入口，使用 `Skip` 保持 V1/V2 测试接口对齐
- `dev-log/2026-03-13-mpm-facade-fix-iteration-2.md`: 记录本轮修复方案、验证与验收结果

## Testing
- `./maze --build`
- `go test ./qa/issue134 -count=1`
- `go test ./common/mpm/...`
- `go test -race ./common/mpm/...`
- `go test ./common/mpm/v2 -run Test -count=1`
- `go test ./common/mpm -run '^$' -bench 'Benchmark' -benchmem`
- `go test -vet=off ./common -run 'TestSplitCrossPagePieces|TestMPMV2Race|TestMPMBenchmarkDatasetShape' -count=1`
- `go test -tags 'mpm_v1 mpm_v2' ./common/mpm`（预期编译失败，实际符合预期）
- `python3 testdata/run_test.py python/20260129-complex-types-311`
- `python3 testdata/run_test.py cpp/20260304-stack-multithread`
- `python3 testdata/run_test.py nodejs/20260211-02-collections`

结论：相关修复与回归验证均通过。
