# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 `prim/maze#134` 执行了阶段 A 静态检查、MPM 直接相关单测 / race / benchmark 冒烟验证，以及 3 个 `testdata` 回归。未发现阻断发布的问题。

## 详细结果

### TC-001: 门面目录骨架与职责分层存在性检查
- **结果**: PASS
- **实际行为**: `go test ./qa/issue134 -count=1` 通过；`common/mpm`、`common/mpm/shared`、`common/mpm/v1`、`common/mpm/v2` 及 `select_*`/`facade.go`/`select_conflict.go` 等关键文件均存在。
- **关键指标**: `ok   maze/qa/issue134 0.068s`
- **备注**: 目录分层与门面结构满足 AC1。

### TC-002: 业务层仅依赖 `maze/common/mpm`
- **结果**: PASS
- **实际行为**: 静态扫描与 `qa/issue134` 校验通过；业务层未发现直接依赖 `maze/common/mpm/v1`、`maze/common/mpm/v2`、`maze/common/mpm/shared` 的违规引用。
- **关键指标**: `go test ./qa/issue134 -count=1` 通过；额外 grep 结果仅出现在门面层、兼容层与测试文件中。
- **备注**: 满足 AC2。

### TC-003: V1/V2 导出全局函数 API 完全一致
- **结果**: PASS
- **实际行为**: `qa/issue134` 中的导出函数签名对比测试通过；`select_v1.go` 与 `select_v2.go` 的转发函数绑定集合一致。
- **关键指标**: `go test ./qa/issue134 -count=1` 通过
- **备注**: 满足 AC3。

### TC-004: 门面选择不再依赖环境变量切换
- **结果**: PASS
- **实际行为**: `facade.go`、`select_default.go`、`select_v1.go`、`select_v2.go`、`select_conflict.go` 中未发现 `os.Getenv`、`MAZE_MPM_V2`、`EnableMPMV2`、`MMV2` 等旧切换标记；默认实现绑定到 V2，V1/V2 通过 build tags 选择。
- **关键指标**: 相关静态检查通过；`go test -tags 'mpm_v1 mpm_v2' ./common/mpm` 按预期编译失败，证明双 tag 冲突保护生效。
- **备注**: 满足 AC1、AC3、AC4。双 tag 失败信息为 `undefined: mustChooseExactlyOneMPMBuildTag`，属于预期负向验证结果。

### TC-005: 不引入双全局状态，并保留 V2 关键能力验证入口
- **结果**: PASS
- **实际行为**: `common/mpm/v2/mpm.go` 中保留 `splitCrossPagePieces`、`SwapKnown`、`ResetKnownBits`、`IterPage`；`common/mpm/facade_contract_test.go`、`common/mpm/v2/race_test.go`、`common/mpm/benchmark_smoke_test.go` 存在且测试可执行。
- **关键指标**:
  - `go test ./common/mpm/...` 通过
  - `go test -race ./common/mpm/...` 通过
  - `go test ./common/mpm -run '^$' -bench 'Benchmark' -benchmem` 通过
- **备注**: 满足 AC4、AC5。

### TC-006: MPM 单测 / race / benchmark 冒烟验证
- **结果**: PASS
- **实际行为**: 编译、单测、race、benchmark 冒烟均通过。
- **关键指标**:
  - `./maze --build`: 通过
  - `go test ./common/mpm/...`: 通过
  - `go test -race ./common/mpm/...`: 通过
  - `BenchmarkFacadeSmoke-48`: `752.3 ns/op`, `456 B/op`, `12 allocs/op`
- **备注**: 直接覆盖本次重构核心模块。

### TC-007: Python testdata 回归
- **结果**: PASS
- **实际行为**: `python/20260129-complex-types-311` 通过，复杂类型对象统计正常。
- **关键指标**:
  - `pymempool_objects = 68626`
  - `PersonClass = 1000`
  - `GameEntity = 300`
  - `TreeNode = 600`
  - `SimpleClass = 500`
- **备注**: 满足 QA 对 Python 用例覆盖要求。

### TC-008: C++ testdata 回归
- **结果**: PASS
- **实际行为**: `cpp/20260304-stack-multithread` 通过，主链路分析完成。
- **关键指标**:
  - `Total items: 10`
- **备注**: 验证多线程/栈对象相关路径；验证脚本提示 `ThreadObject not found in results`，但该用例设计允许此情况且整体判定通过。

### TC-009: Node.js testdata 回归
- **结果**: PASS
- **实际行为**: `nodejs/20260211-02-collections` 通过，集合类对象计数满足阈值。
- **关键指标**:
  - `Map = 1002`
  - `Set = 1001`
  - `WeakMap = 1000`
  - `WeakSet = 1000`
  - `DataView = 1000`
  - `ArrayBuffer = 4629`
- **备注**: 作为大块对象/遍历相关回归补充覆盖。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未单独执行 `go test -vet=off ./common -run 'TestSplitCrossPagePieces|TestMPMV2Race|TestMPMBenchmarkDatasetShape' -count=1`；原因是本轮已通过 `common/mpm/...`、`qa/issue134`、`race`、`benchmark` 以及 3 个 `testdata` 完成核心验收，且该命令属于开发报告中的补充验证，不在本轮 test case 强制项内。
- 未执行更多 glibc / Node.js / Python 多版本矩阵；原因是当前工作树与现有 testdata 更适合做主链路回归，非阻塞发布，但兼容性清单仍未完整覆盖。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] MPM benchmark 冒烟可执行，未出现异常失败

## 静态检查补充结论

- `common/mpm` 门面层仅做 API 与版本绑定转发，未承载具体算法实现。
- `common/mpm/shared` 已承接共享类型与常量。
- `common/mpm.go`、`common/mpm_switch.go`、`common/mpm_v2.go` 已收敛为兼容层，未再保留旧环境变量切换逻辑。
- `select_v1.go` 与 `select_v2.go` 已通过互斥 build tags 收紧选择条件。

## 日志检查

- `maze.py.log`：本轮未生成可检查文件。
- `maze.log`：未发现本轮执行导致的 panic / fatal；grep 命中的大量 `TypeError` / `RangeError` / `HideStackFramesError` 为被分析 Node.js 对象字段名，不构成运行异常。

## 总结
- 通过: 9 / 9
- 失败: 0 / 9
- 建议: 可以发布。本轮 retry #5 关注的两个历史问题——版本选择 build tag 互斥与 V2 race 测试实际执行——均已验证通过。
