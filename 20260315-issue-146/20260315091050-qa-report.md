# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 中已删除 `flag.BoolVar(&RunAfterBuild, "run", ...)`，帮助输出 `./maze --help` 中也不再包含 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；`./maze --help` 输出未见 `--run`。
- **备注**: 与 AC1 一致。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 中 `BuildMode` 分支已简化为 `if !hasAnalysisIntent() { os.Exit(0) }`，未再依赖 `RunAfterBuild`。
- **关键指标**: 静态检查 `main.go:159-163` 符合预期；`go test ./qa/issue146` 通过。
- **备注**: 与 AC2 一致。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已删除 `RunAfterBuild` 定义。
- **关键指标**: 静态检查 `common/var.go` 未见 `RunAfterBuild`；`go test ./qa/issue146` 通过。
- **备注**: 与 AC3 一致。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 的 `doLoop()` 默认命令已变更为 `./maze --build --mode postman --tag dev`；`tests/test_maze_loop_integration.py` 断言已同步更新。
- **关键指标**: 静态检查 `main.go:351-356` 与测试文件断言一致；`go test ./qa/issue146` 通过。
- **备注**: 与 AC4 一致。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: FAIL
- **实际行为**: 已清理 `doc/build_and_run.md`、`tests/test_maze_loop_integration.py`、`cmd/postman.py`、`AGENTS.md` 等活跃路径，但仓库中仍存在用户可参考的 CLI 文档 `dev-log/2026-02-11-maze-cli-options.md`，其“常用命令示例”仍保留 `./maze --build --run --pid <PID> --text`。
- **关键指标**: `dev-log/2026-02-11-maze-cli-options.md:194` 仍包含 `--run`。
- **备注**: 开发报告称“文档和测试脚本中的 `--run` 引用需要清理”且已更新该文档，但当前文档内部仍有一处旧示例，范围清理不完整。

## 失败的 Test Case

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: FAIL
- **预期行为**: 文档和测试脚本中的命令示例应完成统一清理，用户不应再看到已删除参数 `--run` 的推荐用法。
- **实际行为**: `dev-log/2026-02-11-maze-cli-options.md` 虽将 `--run` 移入“已删除的选项”表格，但同文件后续“常用命令示例”仍保留旧命令 `./maze --build --run --pid <PID> --text`。
- **复现步骤**:
  1. 打开 `dev-log/2026-02-11-maze-cli-options.md`。
  2. 定位到“常用命令示例”章节。
  3. 查看第 194 行附近内容。
  4. 观察到示例仍包含 `--run`。
- **可能原因**: 本次测试代码 `qa/issue146/remove_run_option_test.go` 仅覆盖 `doc/build_and_run.md`、`tests/test_maze_loop_integration.py`、`cmd/postman.py`、`AGENTS.md`，未覆盖该 CLI 选项文档中的残留示例，导致范围清理遗漏未被自动化拦截。
- **建议修复**: 清理 `dev-log/2026-02-11-maze-cli-options.md` 中残留的旧示例，并补充对应自动化断言，防止后续文档再次出现已删除参数。

## 未执行项
- 无。已完成阶段 A 静态检查、阶段 B 关键 smoke test，以及至少一个实际 `testdata` 用例执行。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试结果摘要
- `go test ./qa/issue146`：通过
- `./maze --build`：通过
- `./maze --help`：通过，帮助输出不包含 `--run`
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 总结
- 通过: 4 / 5
- 失败: 1 / 5
- 建议: **暂不发布**。核心 CLI 删除逻辑已完成，但文档清理仍有残留，用户仍可能复制到失效参数 `--run` 的旧示例；同时建议扩展自动化测试覆盖该文档路径，避免再次遗漏。
