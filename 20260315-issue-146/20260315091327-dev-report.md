## Summary

本轮针对 QA 报告中唯一失败项 TC-005 做了补充修复，清理 `dev-log/2026-02-11-maze-cli-options.md` 中残留的 `./maze --build --run --pid <PID> --text` 旧示例，避免用户继续看到已删除参数的推荐用法。

结论：`COMPLETE`

## Changes

- `dev-log/2026-02-11-maze-cli-options.md`: 将“编译并运行”旧示例改为基于分析意图自动继续执行的新命令说明与示例

## Testing

- `./maze --build`：通过
- `go test ./qa/issue146`：通过
- `./maze --help`：通过，输出中不包含 `--run`
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 验收核对

- AC1: PASS `main.go` 已不再注册 `--run`，帮助输出已清理（结合既有实现与本轮回归验证）
- AC2: PASS `--build` 后退出逻辑仅依赖 `hasAnalysisIntent()`（结合 `go test ./qa/issue146`）
- AC3: PASS `common/var.go` 已移除 `RunAfterBuild`（结合 `go test ./qa/issue146`）
- AC4: PASS loop 主命令不再拼接 `--run`（结合 `go test ./qa/issue146`）
- AC5: PASS 本轮已清理 QA 指出的 CLI 文档残留示例，用户可见活跃文档/脚本不再示例已删除参数

## 已完成项

- [x] 修复 QA 报告中的文档残留问题
- [x] 回归构建、QA 自动化检查、帮助输出与一个实际 Python 用例

## 未完成项

- 无

## 未验证项

- 无

## Self-Review 发现与修复

- 发现上轮修复虽然已将 `--run` 移入“已删除选项”说明，但同一文档后文仍保留旧命令示例；本轮已同步更正。

## 注意事项

- 本轮严格按 QA 指定范围最小化修复，仅调整文档残留，不修改测试数据与非相关逻辑。
