# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #146“删除多余的 `--run` 选项”执行了需求核对、代码范围审查、专项自动化测试、主流程构建验证、真实 `testdata` 回归以及 loop 集成测试复核。当前验收范围内未发现阻塞问题，需求与测试结果一致。

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 已删除 `flag.BoolVar(&RunAfterBuild, "run", ...)`；`./maze --help` 输出中未出现 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；帮助输出仅保留 `--loop` / `--kill-loop` 示例与现有有效选项。
- **备注**: CLI 可见面已完成移除。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 中 `BuildMode` 分支已使用 `if !hasAnalysisIntent() { os.Exit(0) }`；`hasAnalysisIntent()` 仅在存在 `--pid`、`--tar` 或显式非默认模式时返回 true，不再把默认 `standalone` 视为分析意图。
- **关键指标**: `main.go:159-176` 静态核对通过；`go test ./qa/issue146` 通过；`./maze --build` 实际执行成功并正常结束。
- **备注**: 满足“仅构建时退出、存在明确分析目标时继续”的需求语义。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已不存在 `RunAfterBuild` 定义。
- **关键指标**: 静态阅读 `common/var.go` 通过；`go test ./qa/issue146` 通过。
- **备注**: 全局状态已同步清理。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 中 `doLoop()` 默认命令为 `./maze --build --mode postman --tag dev`；Python 包装脚本 `maze` 的 `do_loop()` 也保持同一命令，不再附带 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；`pytest tests/test_maze_loop_integration.py` 结果为 `8 passed`。
- **备注**: 既满足 issue 范围，也验证了 loop 相关兼容接口未被破坏。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: PASS
- **实际行为**: 已核对 `doc/build_and_run.md`、`doc/prepare.md`、`cmd/postman.py`、`cmd/test.py`、`AGENTS.md`、`tests/test_maze_loop_integration.py` 等活跃路径，相关命令示例均已去除 `--run`。
- **关键指标**: `git diff origin/master...HEAD` 显示上述路径已完成清理；`go test ./qa/issue146` 通过对应文本断言。
- **备注**: 历史性说明文档中可保留“已删除选项”的说明，不构成当前 AC 失败。

### 真实回归用例: Python 复杂类型分析
- **结果**: PASS
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 执行成功，Maze 正常分析 tarball，`validate.py` 全部断言通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`；`GameEntity=300`；`TreeNode=600`；`SimpleClass=500`；集合对象数 `2288`。
- **备注**: 证明移除 `--run` 未破坏常见 Python testdata 主链路。

## 失败的 Test Case（如有）

无。

## 已执行检查项

- 阅读 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新开发报告 `dev-flow/20260315093333-dev-report.md`
- 审查 `git diff origin/master...HEAD`
- 静态核对 `main.go`、`common/var.go`、`maze`、`tests/test_maze_loop_integration.py`、文档与脚本更新范围
- 执行 `./maze --build`
- 执行 `go test ./qa/issue146`
- 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 执行 `./maze --help`
- 执行 `pytest tests/test_maze_loop_integration.py`

## 未执行项

- Python 2.7 动态验证未执行
- 不同 glibc 版本矩阵未执行
- 不同 Node.js 版本矩阵未执行
- 性能类大内存扫描指标未执行

未执行原因：本 issue 变更集中于 CLI 选项删除与命令入口清理，当前已通过静态范围检查、专项测试与至少一个真实 testdata 用例验证主链路；兼容性矩阵与性能测试不属于本次改动的高收益首要阻断项。

可能影响范围：主要影响扩展兼容性声明，不影响本轮 AC1-AC5 的通过判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试命令与结果摘要

- `./maze --build`：通过
- `go test ./qa/issue146`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --help`：通过
- `pytest tests/test_maze_loop_integration.py`：通过（8 passed）

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: **可以发布本 issue 对应改动**。当前 `--run` 已从 CLI、变量、loop 命令以及活跃文档/脚本中完成清理，且主流程、专项测试和真实 testdata 回归均通过。
