# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮围绕 issue #146“删除多余的 `--run` 选项”进行了静态范围核对、专项自动化测试、主流程构建验证、至少一个真实 `testdata` 用例回归，以及相关集成测试文件执行。

结论：**issue 专项 AC 已满足，但本轮执行到的相关测试文件 `tests/test_maze_loop_integration.py` 仍整体失败，因此本次 QA 总体结果判定为 FAIL。**

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 中已删除 `flag.BoolVar(&RunAfterBuild, "run", ...)`；`./maze --help` 输出中也未出现 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；帮助输出仅包含 `--loop`、`--kill-loop` 等现有参数。
- **备注**: 满足 AC1。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 中 `BuildMode` 分支已改为 `if !hasAnalysisIntent() { os.Exit(0) }`；`hasAnalysisIntent()` 仅在 `--pid` / `--tar` / 显式非默认模式时返回 true。
- **关键指标**: `go test ./qa/issue146` 通过；额外执行 `python3 -c 'import subprocess; ... ./maze --build ...'` 返回码为 0，且无额外输出，符合“仅构建直接退出”边界预期。
- **备注**: 满足 AC2。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已无 `RunAfterBuild` 定义。
- **关键指标**: `go test ./qa/issue146` 通过；静态核对 `common/var.go` 未发现残留标识符。
- **备注**: 满足 AC3。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 的 `doLoop()` 默认命令已切换为 `./maze --build --mode postman --tag dev`。
- **关键指标**: `go test ./qa/issue146` 通过；`tests/test_maze_loop_integration.py` 中的命令断言文本也已同步为无 `--run` 版本。
- **备注**: 满足 AC4。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: PASS
- **实际行为**: 已核对 `doc/build_and_run.md`、`cmd/postman.py`、`AGENTS.md`、`tests/test_maze_loop_integration.py`，均未再保留 test case 指定的活跃 `--run` 示例。
- **关键指标**: `go test ./qa/issue146` 通过；`git diff origin/master...HEAD` 显示相关示例已同步清理。
- **备注**: 满足 AC5。

## 附加动态验证

### 关键 smoke / 主链路验证
- `./maze --build`：通过
- `./maze --help`：通过
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过

### testdata 用例结果
- **结果**: PASS
- **实际行为**: Python 复杂类型测试完整通过，`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500`、`Config=300` 等关键计数均符合预期。
- **关键指标**: `pymempool_objects = 68626`，validate 全部通过。
- **备注**: 说明删除 `--run` 后未破坏常见 Python tarball 分析主流程。

## 失败的 Test Case / 检查项

### 相关集成测试文件：`tests/test_maze_loop_integration.py`
- **结果**: FAIL
- **预期行为**: 相关测试应在当前仓库入口模型下通过，至少不应在本轮回归中出现整组失配。
- **实际行为**: `pytest tests/test_maze_loop_integration.py` 结果为 **7 failed, 1 passed**。
- **复现步骤**:
  1. 在仓库根目录执行 `pytest tests/test_maze_loop_integration.py`
  2. 观察失败详情
- **可能原因**:
  - 该测试文件仍按 Python 包装脚本接口断言 `maze` 模块具备 `init_log`、`do_loop`、`do_kill_loop` 等符号；
  - 当前仓库实际入口已是 Go 主流程 + `.maze` 工作流，测试模型与实现长期失配；
  - 同时首个用例假定 `./maze --help` 输出走 stdout，但当前帮助信息位于 stderr，也与现实现不一致。
- **建议修复**:
  - 由开发侧明确该测试文件是否仍属于活跃回归范围；
  - 若属于活跃范围，应按当前 Go 入口行为重写测试；
  - 若不再适用，应删除或迁移为新的集成测试。

## 未执行项
- Python 2.7 动态验证未执行：当前轮次已完成 issue 专项验证与至少一个真实 testdata 用例，但未扩展到 Python 2.7。
- 不同 glibc 版本验证未执行：当前环境单一，未覆盖跨 glibc 版本。
- 不同 Node.js 版本验证未执行：本轮未涉及 Node.js 多版本矩阵。
- 更大规模性能验证未执行：issue #146 属 CLI/流程清理，当前未观察到必须执行 >1GB 目标性能压测的信号。

这些未执行项主要影响兼容性与性能结论，不影响本轮已确认的 issue 专项 AC 判断，但会限制更广义发布信心。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 6 / 7
- 失败: 1 / 7
- 建议: **暂不作为“整体回归无异常”通过发布。** issue #146 本身的实现与验收标准已基本满足，但当前仓库中被本轮触达的相关集成测试文件仍存在明显失配并实际失败，需由开发侧处理该测试资产后再获得完整 PASS。
