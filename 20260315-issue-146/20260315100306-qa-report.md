# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 已删除 `flag.BoolVar(&RunAfterBuild, "run", ...)`，`./maze --help` 输出中也未出现 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；帮助输出仅显示 `-h` / `-help` 与当前有效选项。
- **备注**: 同时确认新增 `--help` 别名和统一帮助输出未重新引入旧参数。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 在 `BuildMode` 分支中执行 `if !hasAnalysisIntent() { os.Exit(0) }`；`hasAnalysisIntent()` 仅在显式提供 `--pid` / `--tar` 或显式非默认 `RunMode` 时返回 true。
- **关键指标**: `go test ./qa/issue146` 通过；`./maze --build` 实际执行成功并直接返回。
- **备注**: 已覆盖此前 QA 指出的默认 `standalone` 被误判为分析意图的问题。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已删除 `RunAfterBuild`；当前公共变量列表与 `--run` 无耦合残留。
- **关键指标**: `go test ./qa/issue146` 通过。
- **备注**: 分支 diff 未见同名变量在其他功能代码中回流。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 中 `doLoop()` 默认命令为 `./maze --build --mode postman --tag dev`；Python 包装层 `maze` 的 `do_loop()` / `do_kill_loop()` 仅转发到 Go 主程序，并已修复 `--loop --mpm v1` 与 `--mpm v1 --loop` 的参数顺序一致性。
- **关键指标**: `pytest tests/test_maze_loop_integration.py` 11/11 通过。
- **备注**: 说明此前多轮 QA / code review 指出的包装层参数顺序敏感问题已被覆盖并修复。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: PASS
- **实际行为**: 已核对 `doc/build_and_run.md`、`doc/prepare.md`、`cmd/postman.py`、`cmd/test.py`、`AGENTS.md`、`postman/postman-main.go`、`tests/test_maze_loop_integration.py` 等活跃路径，相关命令示例均已去除 `--run`。
- **关键指标**: 分支 diff 与 `go test ./qa/issue146` 均通过。
- **备注**: 仓库中仍存在历史 `dev-flow/` / `dev-log/` 记录文本提及 `--run`，属于历史文档，不构成当前实现残留。

## 失败的 Test Case（如有）

- 无

## 未执行项（如有）

- Python 2.7 兼容性验证：本轮未执行，当前环境仅完成 Python 3.11 相关验证。可能影响兼容性扩展结论，但不影响 issue #146 主验收链路。
- 不同 glibc 版本验证：本轮未执行，当前仅在单一 Linux 环境完成验证。可能影响跨环境兼容性判断。
- 不同 Node.js 版本验证：本轮未执行，当前 issue 目标为 CLI 参数清理，且未发现 Node.js 主链路相关改动。
- 性能验证：本 issue 不涉及性能路径变更，本轮未执行大内存性能压测。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试执行记录

### 已执行命令
```bash
./maze --build
go test ./qa/issue146
pytest tests/test_maze_loop_integration.py
python3 testdata/run_test.py python/20260129-complex-types-311
./maze --help
```

### 结果摘要
- `./maze --build`：通过
- `go test ./qa/issue146`：通过
- `pytest tests/test_maze_loop_integration.py`：通过（11 passed）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --help`：通过，帮助输出未出现 `--run`

### 日志检查
- `maze.py.log`：本轮未生成文件，未见 Python 包装层单独报错日志。
- `maze.log`：主流程未见 fatal / panic。
- 备注：在 Python testdata 回归期间，`maze.log` 中出现一条 C++ 分析相关 `AttributeError: module 'gdb' has no attribute 'all_variables_symbol'` 调试日志，但未阻断本次 Python 用例通过，也不属于 issue #146 本次改动引入的 `--run` 清理回归。

## 总结
- 通过: 5 / 5
- 失败: 0 / 5
- 建议: **可以发布本 issue 对应改动**。当前 `--run` 已从 CLI、公共变量、loop 默认命令及活跃文档/脚本中完成清理；此前反复暴露的包装层参数顺序问题已通过集成测试验证修复；且至少一个真实 `testdata` 用例回归通过，主链路未见因本次改动产生的新阻塞。
