## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

本轮重点复核此前唯一失败项 TC-005。经静态核对、issue 专项自动化测试、帮助输出检查以及至少一个真实 `testdata` 用例回归后，issue #146“删除多余的 `--run` 选项”对应的验收范围已满足。

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 中已不存在 `flag.BoolVar(&RunAfterBuild, "run", ...)`；`main.go` 也不再引用 `RunAfterBuild`。
- **关键指标**: `go test ./qa/issue146` 通过；`./maze --help` 输出中未出现 `--run`。
- **备注**: 与 AC1 一致。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 中 `BuildMode` 分支的退出条件已简化为 `if !hasAnalysisIntent() { os.Exit(0) }`。
- **关键指标**: `main.go:159-163`；`go test ./qa/issue146` 通过。
- **备注**: 已不再依赖 `RunAfterBuild`。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已删除 `RunAfterBuild` 全局变量定义。
- **关键指标**: `common/var.go` 未检出 `RunAfterBuild`；`go test ./qa/issue146` 通过。
- **备注**: 与 AC3 一致。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 中 `doLoop()` 默认命令已更新为 `./maze --build --mode postman --tag dev`。
- **关键指标**: `main.go:353-355`；`go test ./qa/issue146` 通过。
- **备注**: 旧命令片段 `--build --run --mode postman --tag dev` 未再出现于目标实现位置。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: PASS
- **实际行为**: 已核对 `doc/build_and_run.md`、`tests/test_maze_loop_integration.py`、`cmd/postman.py`、`AGENTS.md`，不再包含 test case 指定的活跃 `--run` 示例；此前 QA 指出的 `dev-log/2026-02-11-maze-cli-options.md` 旧示例也已清理。
- **关键指标**: `go test ./qa/issue146` 通过；静态 `grep` 未在上述活跃路径中检出旧示例。
- **备注**: 此前 retry 的核心失败点已修复。

## 额外检查

- `./maze --build`：通过。
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过，说明移除 `--run` 后主链路未破坏常见 Python testdata 分析流程。
- `pytest tests/test_maze_loop_integration.py`：失败（7 failed, 1 passed）。失败点集中在该测试文件仍按 Python 包装脚本接口断言 `init_log`、`do_loop`、`do_kill_loop` 等符号，而当前仓库主入口已是 Go/`.maze` 工作流；该组失败与本 issue 的 `--run` 删除 AC 不直接对应，也未阻断本轮 issue 专项验收结论，但建议后续单独清理或重写这组测试，避免持续产生噪音。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- Python 2.7 兼容性专项动态验证：本轮未执行。
- 不同 glibc 版本专项验证：本轮未执行。
- 不同 Node.js 版本专项验证：本轮未执行。
- 大内存性能专项验证：本 issue 为 CLI 选项清理，未涉及性能路径，本轮未执行。

上述未执行项不会改变本次 issue #146 的专项结论，但仍可能影响更广义的跨版本/跨环境兼容性判断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `./maze --build`：PASS
- `go test ./qa/issue146`：PASS
- `./maze --help`：PASS（未出现 `--run`）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
- `pytest tests/test_maze_loop_integration.py`：FAIL（范围外遗留问题，未纳入 issue #146 判定）

## Findings

- issue #146 的 5 个 test case 本轮全部通过，之前阻塞的文档残留问题已消除。
- 仓库内仍存在若干历史 `dev-log/` 文档保留 `--run` 旧命令，这些属于历史记录，不在本轮 AC 定义的“活跃文档与测试脚本”清理范围内。
- `tests/test_maze_loop_integration.py` 当前与现仓库主入口实现不一致，建议后续单独收敛测试口径。

## 总结
- 通过: 5 / 5
- 失败: 0 / 5
- 建议: **可以发布本 issue 对应改动**。`--run` 删除逻辑、退出条件、loop 命令以及活跃文档/脚本清理均已满足验收要求；另有一组范围外 Python loop 集成测试处于失配状态，建议后续单独处理。
