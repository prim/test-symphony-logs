## Summary
实现删除多余的 `--run` 选项：从 Go CLI 中移除 flag 与 `RunAfterBuild` 变量，构建后退出逻辑改为仅依据 `hasAnalysisIntent()` 判断，并同步清理 loop/postman/文档中的旧命令示例。

## Changes
- `main.go`: 删除 `--run` flag 注册、移除 `RunAfterBuild` 退出条件、更新 loop 默认命令
- `common/var.go`: 删除 `RunAfterBuild` 变量定义
- `doc/build_and_run.md`: 更新 Node.js 分析示例，不再使用 `--run`
- `doc/prepare.md`: 更新 prepare 阶段示例命令
- `cmd/postman.py`: 删除 postman 命令中的 `--run`
- `cmd/test.py`: 清理旧命令注释中的 `--run`
- `postman/postman-main.go`: 清理注释中的旧命令
- `AGENTS.md`: 更新目标进程测试示例命令
- `tests/test_maze_loop_integration.py`: 同步 loop 默认命令断言
- `dev-log/2026-02-11-maze-cli-options.md`: 将 `--run` 挪到已删除选项说明
- `dev-log/2026-03-15-remove-run-option.md`: 新增本次开发日志

## Testing
- `./maze --build`：通过
- `go test ./qa/issue146`：通过
- `./maze --help`：通过，帮助输出不再包含 `--run`

## 验收核对
- AC1: PASS `main.go` 已删除 `--run` flag 与帮助文案
- AC2: PASS `BuildMode` 分支退出条件已简化为 `!hasAnalysisIntent()`
- AC3: PASS `common/var.go` 已删除 `RunAfterBuild`
- AC4: PASS `doLoop()` 默认命令已移除 `--run`
- AC5: PASS 活跃文档与脚本中的 `--run` 示例已清理

## 剩余工作
- 未完成项: 无
- 未验证项: 无

## Self-Review 发现与修复
- 发现 `doc/prepare.md`、`cmd/test.py`、`postman/postman-main.go` 仍保留旧示例，已一并清理，避免仓库内继续传播已删除参数。

## 注意事项
- 额外执行 `pytest tests/test_maze_loop_integration.py` 时发现该测试文件存在与当前 Python 包装脚本实现不一致的既有失败；该失败不属于 issue #146 预置验收用例，本次未修改测试文件。

## 结论
COMPLETE
