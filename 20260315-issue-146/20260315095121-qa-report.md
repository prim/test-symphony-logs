# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

## 详细结果

### TC-001: CLI 不再注册 `--run` 选项
- **结果**: PASS
- **实际行为**: `main.go` 的 `parseArgs()` 中已删除 `flag.BoolVar(&RunAfterBuild, "run", ...)`；`common/var.go` 中也未再定义 `RunAfterBuild`。执行 `./maze --help` 后，帮助输出未出现 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；`./maze --help` 输出仅包含 `--loop`、`--kill-loop` 等现有选项。
- **备注**: Go 入口已新增 `--help` flag，并统一由 `flag.Usage` 输出帮助信息。

### TC-002: `--build` 后退出逻辑仅依赖分析意图
- **结果**: PASS
- **实际行为**: `handleCommandMode()` 在 `BuildMode` 分支内使用 `if !hasAnalysisIntent() { os.Exit(0) }`；`hasAnalysisIntent()` 仅在存在 `--tar` / `--pid` 或显式非默认 `RunMode` 时返回 true，不再把默认 `standalone` 直接视为分析意图。
- **关键指标**: `go test ./qa/issue146` 通过；静态审查确认退出条件文本与专项测试一致。
- **备注**: 额外执行 `python3 ./maze --build` 验证了“仅构建”路径可直接结束，未进入分析流程。

### TC-003: 公共变量定义移除 `RunAfterBuild`
- **结果**: PASS
- **实际行为**: `common/var.go` 中已删除 `RunAfterBuild` 变量；分支 diff 与专项测试均未发现残留引用。
- **关键指标**: `go test ./qa/issue146` 通过。
- **备注**: 与 `main.go` 中的 flag 清理保持一致。

### TC-004: loop 主命令不再拼接 `--run`
- **结果**: PASS
- **实际行为**: `main.go` 中 `doLoop()` 默认命令为 `./maze --build --mode postman --tag dev`；Python 包装脚本 `maze` 中的 `do_loop()` / `do_kill_loop()` 仅负责转发到 Go 主程序，不再复制旧的 loop 业务逻辑，也不再附带 `--run`。
- **关键指标**: `go test ./qa/issue146` 通过；`pytest tests/test_maze_loop_integration.py` 通过（9 passed）。
- **备注**: 本轮也验证了包装层对 Go 入口的转发契约与 ETXTBSY 重试逻辑。

### TC-005: 活跃文档与测试脚本不再示例 `--run`
- **结果**: PASS
- **实际行为**: 已核对 `doc/build_and_run.md`、`cmd/postman.py`、`AGENTS.md`、`tests/test_maze_loop_integration.py` 等活跃路径，相关命令示例均已去除 `--run`。分支 diff 还显示 `doc/prepare.md`、`cmd/test.py`、`postman/postman-main.go` 等路径同步完成清理。
- **关键指标**: `go test ./qa/issue146` 通过；针对源码路径的 `--run` 搜索未发现活跃实现/脚本残留。
- **备注**: 历史 `dev-log/` 中保留对已删除选项的说明文字属于历史记录，不视为本 AC 失败。

### 真实 testdata 回归: Python 复杂类型主链路
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 后，Maze 成功分析 tarball，`validate.py` 全量断言通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`，`GameEntity=300`，`TreeNode=600`，`SimpleClass=500`，其余 dataclass / NamedTuple / set 校验均通过。
- **备注**: 证明删除 `--run` 后未破坏常见 Python testdata 分析主链路。

## 失败的 Test Case（如有）

- 无

## 未执行项（如有）
- Python 2.7 动态验证未执行；本轮优先完成 issue 相关低成本静态检查、专项自动化测试、主流程构建与至少一个真实 testdata 回归。
- 不同 glibc 版本验证未执行；当前环境仅单一 Linux 运行环境。
- 不同 Node.js 版本矩阵未执行；本轮未扩展到多版本兼容矩阵。
- 大内存进程性能验证未执行；本 issue 聚焦 CLI 参数与命令路径清理，不涉及性能改动。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 测试执行记录
- `./maze --build`：通过
- `go test ./qa/issue146`：通过
- `pytest tests/test_maze_loop_integration.py`：通过（9 passed）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `./maze --help`：通过（帮助输出未出现 `--run`）

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: **可以发布本 issue 对应改动**。当前 `--run` 已从 CLI、公共变量、loop 默认命令以及活跃文档/脚本中完成清理；关键专项测试、集成测试、构建验证和至少一个真实 testdata 用例均通过。
