# 开发报告

## 完成的任务
- [x] Task 1: 为 `maze` CLI 增加 `--byebye` 一等参数
- [x] Task 2: 在常规 standalone 参数校验前短路处理 `--byebye`，输出 `byebye` 并正常退出

## 修改的文件
- `maze`: 新增 `--byebye` 参数，并在 `parse_known_args()` 后立即短路输出 `byebye`
- `dev-log/plan.md`: 按要求将已完成条目标记为 `- [x]`

## 自测结果
- 测试命令: `python3 -m pytest test/py/test_maze_cli_byebye.py`
- 测试结果: 通过，2/2 用例通过
- 测试命令: `python3 maze --byebye`
- 测试结果: 通过，标准输出为 `byebye`

## Self-Review 发现与修复
- 无

## 注意事项
- `--byebye` 在帮助参数解析后、正常业务分支前直接返回，避免落入默认 help 输出或 standalone 的 `--pid` 校验逻辑
- 本次变更仅涉及 Python 启动脚本，无需重新编译 Go 二进制
