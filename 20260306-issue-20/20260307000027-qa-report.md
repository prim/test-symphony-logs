## QA Result: PASS

## Findings
- `maze` now registers `--byebye` as a first-class CLI flag and short-circuits before standalone `--pid` validation.
- Manual verification confirmed `python3 maze --byebye` exits with code 0, prints exactly `byebye\n`, and emits no stderr.
- Regression coverage in `test/py/test_maze_cli_byebye.py` passes and also verifies the command does not fall back to help output.

## Test Results
- Requirements checked against `dev-log/plan.md`: both acceptance criteria satisfied (`输出 byebye`, `正常退出`).
- Code review checked `maze` around argument parsing: `--byebye` is added via argparse and handled immediately after `parse_known_args()`.
- Automated test run: `python3 -m pytest test/py/test_maze_cli_byebye.py` -> PASS (2 passed).
- Manual test run: `python3 maze --byebye` -> PASS (`stdout='byebye\n'`, `stderr=''`, exit code 0).
- Comparison run: `python3 maze` still prints normal help output when no `--pid` is provided, showing `--byebye` behavior is isolated.
