# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮相比前次已有明显改进：Python / Node.js / C++ 三组 `testdata` 回归现已可实际执行且全部通过，旧 type id 常量区间判断也已从 Go 主代码中移除；但核心需求是“两个都不保留，直接改写成新的设计”。当前业务主路径仍大量经由 `common/collector_results.go` 暴露的兼容层别名/API（如 `ResultRecord`、`GetResultRecord`、`MergeResults`、`CollectResults`）工作，因此“旧 common result item/collector 外壳彻底移除”这一核心验收项仍未满足。

## Findings

- 已阅读 `dev-flow/20260309153849-dev-report.md`、`dev-flow/test-cases.md`、`dev-flow/plan.md`，并复核当前工作树实现与回归结果。
- 正向进展：`testdata/run_test.py` 及三个指定回归用例均可执行，`python/20260129-complex-types-311`、`nodejs/20260225-maze-vs-heapsnapshot`、`cpp/20260201-basic-malloc` 全部通过，说明此前 QA 报告中“缺少回归证据”的问题已被解决。
- 正向进展：旧 type id 方案相关标识 `CustomClassifyTypeBegin|CustomClassifyTypeEnd|ResultTypeBase|PythonTypeBase|TXOSTypeBase|NodejsTypeBase|IsPythonType|IsLpcType|IsLuaType|IsNodejsType` 在 Go 主代码中已无命中，type id 切换已满足预期。
- 核心未完成项：`common/collector_results.go` 仍保留兼容层文件与旧风格接口，且业务代码仍广泛通过 `ResultRecord` / `GetResultRecord` 等 common 层 API 驱动本地收集与合并。虽然底层实现已委托给 `common/results.Entry` 与 `results.MergeUint64()`，但从需求“两个都不保留”来看，这仍属于“新内核 + 旧外壳”的并存状态，而非彻底改写。
- 证据：`common/collector_results.go` 仍包含 `type ResultRecord = results.Entry`、`func NewResultRecord()`、`func GetResultRecord()`、`func MergeResults()`、`func CollectResults()` 等兼容入口；仓库 grep 也显示大量业务文件继续以 `func(s uint64) *ResultRecord` 形式传递和使用该兼容类型。

## 详细结果

### TC-001: TypeID 位域编码与解析正确
- **结果**: PASS
- **实际行为**: `go test ./common/results -run TestMakeTypeIDEncodesModuleSubtypeAndSequence` 包含在 `go test ./common/results` 中通过。
- **关键指标**: `module/subtype/sequence` 编解码断言通过。
- **备注**: 新 TypeID 基础能力正常。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，且旧 type id 区间常量/判断函数在 Go 主代码中 grep 无命中。
- **关键指标**: `CustomClassifyType*`、`ResultTypeBase`、`PythonTypeBase`、`IsPythonType()` 等命名无命中。
- **备注**: type id 旧方案已完成切换。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: collector/TopN 相关单测通过。
- **备注**: 新 collector 能力本身可用。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: merge 相关单测通过。
- **备注**: 并行局部结果合并能力可用。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: unknown type fallback 格式相关单测通过。
- **备注**: registry fallback 符合预期。

### TC-006: Result String 输出可读的调试信息
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: 调试字符串包含 `module/subtype/seq`。
- **备注**: 可读性满足要求。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: PASS
- **实际行为**: 三组指定回归均可通过 `run_test.py` 执行并完成自动校验。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
  - `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS
- **备注**: issue 中“qa 需要多执行几个 testdata 回归”的要求本轮已满足。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 新 `common/results` 已提供主状态与数据结构，但业务主调用方仍继续使用 common 层兼容别名/API；`common/collector_results.go` 仍是业务收集主入口之一。
- **关键指标**:
  - `common/collector_results.go:56` 仍存在 `type ResultRecord = results.Entry`；
  - `common/collector_results.go` 仍存在 `NewResultRecord`、`GetResultRecord`、`MergeResults`、`CollectResults`、`RunWithResults`；
  - 仓库 grep `ResultRecord|GetResultRecord` 在多处业务代码中仍有大量命中（如 `mallocer/ptmalloc/ptmalloc.go:350-352`、`lua/lua.go:227-229`、`txos/*`、`python/*` 等）。
- **备注**: 当前状态仍属于兼容层保留，不符合“两个都不保留”的严格要求。

## 失败的 Test Case

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item / collector 外壳应被新 `common/results` 原生 API 和数据结构直接替代，不再保留兼容别名与旧风格调用接口作为业务主链路。
- **实际行为**: 虽然旧 `ResultRecord` 已退化为 `results.Entry` 的别名，底层合并也已委托给 `common/results`，但兼容层文件和旧风格入口仍被主业务代码广泛使用。
- **复现步骤**:
  1. 阅读 `dev-flow/plan.md`，确认需求为“两个都不保留，直接改写成新的设计”
  2. 阅读 `common/collector_results.go`，可见 `type ResultRecord = results.Entry` 与 `GetResultRecord` / `MergeResults` / `CollectResults` 等兼容接口仍存在
  3. 搜索 `ResultRecord|GetResultRecord`，可见 `mallocer/ptmalloc/ptmalloc.go`、`lua/lua.go`、`txos/*`、`python/*` 等仍沿用兼容接口
- **可能原因**: 本轮完成了数据结构内核切换与回归补齐，但尚未把各业务调用方继续下沉到 `common/results` 原生 API。
- **建议修复**: 继续将调用方从 `ResultRecord` / `GetResultRecord` / `CollectResults` 等 common 兼容入口迁移到 `common/results.Entry` 与对应原生 helper，最终删除或仅剩极薄且非主路径的适配层后再回归验收。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## Test Results

- `go test ./common/results`: PASS
- `./maze --build`: PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS

## 总结

- 通过: 7 / 8
- 失败: 1 / 8
- 建议: **暂不可发布**。回归测试问题已解决，但“旧设计彻底移除”这一核心验收标准仍未满足，需要 developer 继续迁移调用方并收敛/删除 common 兼容层后再重新验收。
