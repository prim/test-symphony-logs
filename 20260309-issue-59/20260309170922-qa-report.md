## QA Result: FAIL

## Findings
- 已按要求复核当前实现、工作树状态与关键代码路径。`common/results` 新包已存在，但当前提交仍未满足 issue 要求的“common result item 和 type id 两个旧方案都不保留，直接改写成新的设计”。
- `type id` 方向有实质进展：`common/classifier.go` 已接入 `maze/common/results`，`IsTypePython()` / `IsTypeTXOS()` 走 `results.TypeID(...).IsPython()/IsTXOS()`；`common/callback.go` 也已接入 `results.Registry().Format(t)` 作为默认格式化 fallback，旧 `IsPythonType` / `IsLpcType` / `ResultTypeBase` / `PythonTypeBase` / `TXOSTypeBase` / `NodejsTypeBase` 在 Go 主代码中已无命中。
- 但 `common result item` 旧设计并未被彻底移除。当前业务主路径仍大量依赖 `common/result_items.go` 暴露的包装层与全局视图：`ResultItemsView`、`MergeLocalResultItems`、`CollectResultItems`、`AddResult`、`AddResultWithLimit` 等仍被 Python / Node.js / C++ / mallocer / TXOS / webui 主代码广泛直接调用；grep 仍有 119 处相关命中。这说明现状仍是“新底层 collector + common 包包装层并存”，不是“直接改写成新的设计”。
- 旧策略语义也仍保留：`EnablePyMerge` 在主代码中仍有 32 处命中，并直接决定结果收集/对象保留行为；这与计划中以新的 Collector/Strategy 语义替代旧行为开关的目标不一致。
- 当前实现还存在明显构建/测试问题：`go test ./common/results` 直接失败，原因是 `common/results/collector_test.go` 仍按旧签名调用 `Merge(local, keepTopN, mergeBySize)`，而实际实现已改为 `Merge(local)`；说明本轮提交连新包自测都未保持通过。
- `./maze --build` 也未真正构建成功。命令输出包含 `begin go build`，但同时出现编译错误：`common/classifier.go:28:71: undefined: AggregatedItem` 与 `common/classifier.go:29:71: undefined: AggregatedItem`。这表明当前工作树存在未收敛的类型引用错误，不能视为构建通过。
- issue 明确要求 QA “多执行几个 testdata 回归”，但当前工作树 `testdata/` 目录为空，`testdata/run_test.py` 缺失；因此 Python / Node.js / C++ 三组回归命令都无法启动，无法提供真实回归证据。
- 当前工作树还缺少 `dev-flow/plan.md`、`dev-flow/test-cases.md`、最新 `*-dev-report.md` 原始文件；本次验收只能依据 `.symphony_prompt.md` 中归档内容、当前代码和实际命令结果进行。即便按最宽松标准，本轮仍因“未彻底移除旧设计 + 构建/单测失败 + testdata 回归缺失”而必须判定 FAIL。

## Test Results
- `go test ./common/results`: FAIL
  - 错误: `common/results/collector_test.go` 第 13/14/26/27 行调用 `Merge` 参数过多，测试代码与实现签名不一致。
- `./maze --build`: FAIL
  - 实际输出含 `begin go build`，但同时报错：`common/classifier.go:28:71: undefined: AggregatedItem`、`common/classifier.go:29:71: undefined: AggregatedItem`。
- `python3 testdata/run_test.py python/20260129-complex-types-311`: FAIL，`testdata/run_test.py` 不存在。
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: FAIL，`testdata/run_test.py` 不存在。
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`: FAIL，`testdata/run_test.py` 不存在。

## Detailed Results

### TC-001: TypeID 位域编码与解析正确
- **结果**: FAIL
- **实际行为**: 无法完成该项自动化验证，因为 `go test ./common/results` 整体已在编译阶段失败。
- **关键指标**: `collector_test.go` 编译错误阻断整个 package test。
- **备注**: 不是 TypeID 逻辑本身已确认失败，而是本轮提交未保持测试可执行性。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: PASS
- **实际行为**: `common/classifier.go` 中 Python/TXOS 判断已切换到 `results.TypeID(t).IsPython()` / `IsTXOS()`；旧 `IsPythonType` / `IsLpcType` / `ResultTypeBase` / `PythonTypeBase` / `TXOSTypeBase` / `NodejsTypeBase` 在 Go 代码中无命中。
- **关键指标**: 旧 type-id 相关命名 grep 无命中；新模块判断路径已接入业务代码。
- **备注**: 该方向完成度高于上轮。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: FAIL
- **实际行为**: 无法通过自动化单测验证，因为 `common/results/collector_test.go` 调用签名已过时，package test 编译失败。
- **关键指标**: `too many arguments in call to Merge`。
- **备注**: 新 collector 能力是否正确，当前提交无法以测试证明。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: FAIL
- **实际行为**: 同 TC-003，相关 merge 单测所在文件编译失败。
- **关键指标**: `collector_test.go` 第 26/27 行报错。
- **备注**: 不能视为通过。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: PASS
- **实际行为**: `common/callback.go` 的默认格式化已接入 `results.Registry().Format(t)`。
- **关键指标**: 未注册类型 fallback 已进入业务代码默认路径。
- **备注**: fallback 接入已落地。

### TC-006: Result String 输出可读的调试信息
- **结果**: FAIL
- **实际行为**: 虽然 `typeid.go` 实现了 `String()`，但本轮 package test 无法通过编译，无法完成该项自动化验收。
- **关键指标**: `go test ./common/results` 整体失败。
- **备注**: 不能以“实现存在”替代“验收通过”。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **实际行为**: Python / Node.js / C++ 三个 `run_test.py` 命令均直接失败，报错 `python3: can't open file '.../testdata/run_test.py': [Errno 2] No such file or directory`。
- **关键指标**: `testdata/` 目录为空；`testdata/run_test.py` 缺失。
- **备注**: 未满足 issue 中“qa 需要多执行几个 testdata 回归”的明确要求。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 虽然底层已引入 `common/results`，但业务主路径仍广泛依赖 `common/result_items.go` 的包装层和全局结果视图，而非直接使用新的 Collector/Strategy/Registry 设计。
- **关键指标**:
  - `ResultItemsView|MergeLocalResultItems|CollectResultItems|LocalResultItems|EnsureResultItem` 相关命中共 119 处；
  - `EnablePyMerge` 在主代码中仍有 32 处命中，并直接参与策略分支；
  - `common/result_items.go` 仍保留全局 map、快照回填、dump、兼容 Add/Merge 包装逻辑。
- **备注**: 当前更接近“新底层 + 旧包装层并存”，未达到“两个都不保留”的严格要求。

## Failed Test Cases

### TC-001 / TC-003 / TC-004 / TC-006: `common/results` 包自测不可执行
- **结果**: FAIL
- **预期行为**: `go test ./common/results` 应通过，以证明新 TypeID / Collector / Merge / Stringify 设计可用。
- **实际行为**: `collector_test.go` 仍按旧函数签名调用 `Merge(local, keepTopN, mergeBySize)`，导致 package 编译失败。
- **复现步骤**:
  1. 在仓库根目录执行 `go test ./common/results`
  2. 观察报错 `too many arguments in call to Merge`
- **可能原因**: 本轮重构更新了 `Merge` API，但未同步更新测试代码。
- **建议修复**: 保持实现与测试签名一致后重新运行全部 `common/results` 单测。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **预期行为**: 至少执行 Python / Node.js / C++ 三组 `run_test.py` 回归，并确认输出未被重构破坏。
- **实际行为**: 仓库缺少 `testdata/run_test.py` 与对应测试数据内容，三个回归命令均无法启动。
- **复现步骤**:
  1. 在仓库根目录执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  2. 观察报错 `python3: can't open file .../testdata/run_test.py: [Errno 2] No such file or directory`
  3. 对 Node.js / C++ 用例重复执行，现象相同
- **可能原因**: 当前工作树未包含可执行的 `testdata` 测试框架内容。
- **建议修复**: 补齐并展开可执行 `testdata` 后，重新按 test case 执行 Python / Node.js / C++ 三组回归。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item 与旧 type-id 方案应被新 `common/results` Collector/Strategy/Registry 设计完全替代，不再保留业务主路径上的 common 包包装层与旧行为开关语义。
- **实际行为**: 业务主路径仍通过 `common/result_items.go` 中的全局 map/包装 API/快照回填逻辑访问结果；`EnablePyMerge` 仍作为行为分支存在，说明新设计尚未完全取代旧设计思路。
- **复现步骤**:
  1. 阅读 `common/result_items.go`，确认仍存在 `ResultItemsView`、`MergeLocalResultItems`、`CollectResultItems`、`AddResult`、`AddResultWithLimit`
  2. 搜索 `ResultItemsView|MergeLocalResultItems|CollectResultItems|LocalResultItems|EnsureResultItem`，确认主代码中仍有大量命中
  3. 搜索 `EnablePyMerge`，确认其仍在主代码中参与结果收集策略分支
- **可能原因**: 本轮完成的是“底层 collector 接管 + common 层包装适配”，但尚未推进到业务调用层直接使用新设计、并删除 common 包兼容外壳。
- **建议修复**: 继续把业务调用链迁移到 `common/results` 的显式 Collector/Strategy/Registry 语义，删除 `common/result_items.go` 中不再必要的包装层与旧行为开关后，再做回归验收。

### Additional Failure: 当前工作树无法成功构建
- **结果**: FAIL
- **预期行为**: `./maze --build` 应无 Go 编译错误。
- **实际行为**: 构建报错 `undefined: AggregatedItem`。
- **复现步骤**:
  1. 在仓库根目录执行 `./maze --build`
  2. 观察 `common/classifier.go:28:71: undefined: AggregatedItem` 与 `common/classifier.go:29:71: undefined: AggregatedItem`
- **可能原因**: `common/classifier.go` 中函数类型签名已切到 `AggregatedItem`，但当前提交未提供与之匹配的顶层类型定义或引用未统一。
- **建议修复**: 统一 `classifier.go` 中回调签名使用的结果项类型，并重新验证全仓编译。

## Summary
- 通过: 2 / 8
- 失败: 6 / 8
- 建议: **暂不可发布**。需先修复 `common/results` 单测编译失败、解决 `./maze --build` 的真实编译错误、补齐并执行要求中的多组 `testdata` 回归，并继续完成旧 common result 包装层/旧策略语义的收敛移除后，再重新验收。
