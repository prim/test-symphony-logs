## QA Result: FAIL

## Findings
- 已按任务要求复核当前工作树、`git diff origin/master...HEAD`、关键实现文件以及附带提示中的最新 dev report / testcase / plan 摘要信息。
- 当前实现未满足 issue 要求“common result item 和 type id 两个旧方案都不保留，直接改写成新的设计”。从真实代码看，当前是**旧设计的批量重命名/同义替换**，而不是切换到新 `common/results` 设计：仓库中不存在 `common/results/` 目录，`go test ./common/results` 直接失败，业务代码也没有任何 `maze/common/results` 接入痕迹。
- `common/result_items.go` 仍然保留完整的全局聚合实现（全局 map、对象池、优先队列、merge、dump、本地收集函数等），只是将 `ResultItems/ResultItem` 等旧名重命名为 `AggregatedResults/AggregatedItem`；这不符合“旧方案不保留”的验收要求。
- `common/classifier.go` 仍保留旧式基于 magic number 区间的 type id 方案，只是把常量名从 `CustomClassifyType*` 改成 `ResultType*`；同时 `IsPythonType()` / `IsLpcType()` 仍存在并被业务代码调用，说明旧判断模型仍在主路径中运行。
- issue 还要求 QA “多执行几个 testdata 回归”，但当前工作树下 `testdata/` 目录为空，`testdata/run_test.py` 缺失，无法执行 Python / Node.js / C++ 回归，缺少真实回归证据。

## Test Results
- `./maze --build`: PASS
- `go test ./common/results`: FAIL（`stat .../common/results: directory not found`）
- `ls testdata`: PASS（目录存在但为空）
- `python3 testdata/run_test.py python/20260129-complex-types-311`: FAIL（文件不存在，未能启动）
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: FAIL（文件不存在，未能启动）
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`: FAIL（文件不存在，未能启动）

## Detailed Results

### TC-001: TypeID 位域编码与解析正确
- **结果**: FAIL
- **实际行为**: 当前仓库不存在 `common/results/typeid.go` 与对应测试目录，`go test ./common/results` 无法执行。
- **关键指标**: `common/results` 目录缺失。
- **备注**: 无法证明新 TypeID 位域设计已落地。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: FAIL
- **实际行为**: `common/classifier.go` 仍保留区间判断实现，`IsPythonType()` / `IsLpcType()` 仍存在；`python/results.go`、`txos/results.go` 仍在调用这些旧判断函数。
- **关键指标**:
  - `common/classifier.go:111` `func IsPythonType(t uint64) bool`
  - `common/classifier.go:119` `func IsLpcType(t uint64) bool`
  - `python/results.go:210` 调用 `IsPythonType`
  - `txos/results.go:253` 调用 `IsLpcType`
- **备注**: 旧 type id 判断路径未移除。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: FAIL
- **实际行为**: 当前代码没有新 `common/results` Collector/Strategy 实现可供验证；`common/result_items.go` 仍使用旧的全局聚合 + `PriorityQueue` 模型。
- **关键指标**: `common/result_items.go` 中仍存在 `AggregatedResults` 全局 map、`AggregatedItemPool`、`PriorityQueue`、`MergeAggregatedResults()`。
- **备注**: 新设计未实际落地。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: FAIL
- **实际行为**: 当前仍由旧式 `MergeAggregatedResults()` 合并局部结果，没有证据表明已切换到新包 `Collector.Merge` 语义。
- **关键指标**: `common/result_items.go:158` 仍定义 `func MergeAggregatedResults(y map[uint64]*AggregatedItem)`。
- **备注**: 这是旧设计改名后的延续，不是新架构接管。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: FAIL
- **实际行为**: 当前仓库中无 `common/results/registry.go`，也无 `results.Registry()` 接入代码，无法验证 registry fallback 新设计。
- **关键指标**: 搜索 `common/results|results.Registry` 无命中。
- **备注**: AC 未完成。

### TC-006: Result String 输出可读的调试信息
- **结果**: FAIL
- **实际行为**: 当前没有新 `TypeID.String()` 能力所在目录与测试，无法证明调试字符串新设计已实现。
- **关键指标**: `go test ./common/results` 失败；`common/results` 目录不存在。
- **备注**: AC 未完成。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **实际行为**: 无法执行任何 `run_test.py` 回归，Python / Node.js / C++ 三组命令都因 `testdata/run_test.py` 缺失而无法启动。
- **关键指标**: `testdata/` 为空。
- **备注**: 未满足 issue 中“qa 需要多执行几个 testdata 回归”的明确要求。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 旧设计未移除，仅发生了名称替换：
  - `common/result_items.go` 仍保留旧结果收集/合并主实现；
  - `common/classifier.go` 仍保留旧 magic number 区间模型；
  - 业务侧继续直接依赖这些实现。
- **关键指标**:
  - `common/result_items.go` 存在完整实现；
  - `common/classifier.go` 保留 `ResultTypeModuleBase` / `ResultTypePythonModuleBase` / `ResultTypeTXOSModuleBase` / `ResultTypeNodejsModuleBase` 区间式常量；
  - `IsPythonType` / `IsLpcType` 仍有业务调用；
  - `maze/common/results` / `results.Init` / `results.Merge` / `results.Registry` 搜索无命中。
- **备注**: 与“两个都不保留，直接改写成新的设计”明显不符。

## Failed Test Cases

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **预期行为**: 至少执行 Python / Node.js / C++ 三组 `run_test.py` 回归，并确认输出未被重构破坏。
- **实际行为**: `testdata/run_test.py` 不存在，且 `testdata/` 为空，所有回归命令均无法启动。
- **复现步骤**:
  1. 在仓库根目录执行 `ls testdata`
  2. 观察目录为空
  3. 执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  4. 观察报错 `No such file or directory`
- **可能原因**: 当前工作树未包含可执行的 testdata 子模块/测试框架。
- **建议修复**: 补齐 `testdata/run_test.py` 与所需测试数据后，重新执行 Python / Node.js / C++ 多组回归。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item 与旧 type id 方案应被新 `common/results` Collector/Strategy/Registry + TypeID 位域设计完全替换，不再保留旧主路径实现。
- **实际行为**: 当前没有新 `common/results` 目录；旧实现仍在 `common/result_items.go` 与 `common/classifier.go` 中承担主逻辑，只是批量改名。
- **复现步骤**:
  1. 阅读 `common/result_items.go`，确认仍存在全局聚合与 merge 主实现
  2. 阅读 `common/classifier.go`，确认仍存在基于区间的 type 判断与旧兼容函数
  3. 搜索 `common/results|results.Init|results.Merge|results.Registry`
  4. 观察无命中
  5. 执行 `go test ./common/results`
  6. 观察目录不存在报错
- **可能原因**: 当前实现只进行了命名重构，没有完成设计级替换。
- **建议修复**: 真正引入并接管 `common/results` 新包，迁移调用方，删除旧主实现后再做全量回归。

## Summary
- 通过: 1 / 8
- 失败: 7 / 8
- 建议: **暂不可发布**。需要先完成真正的新设计切换（而非重命名兼容），并补齐且执行 issue 要求的多组 `testdata` 回归后，再重新验收。
