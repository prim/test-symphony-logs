# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## 详细结果

### TC-001: TypeID 位域编码与解析正确
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，覆盖了 TypeID 编码/解码相关单测。
- **关键指标**: `module/subtype/sequence` 编解码断言通过。
- **备注**: 新 TypeID 基础能力正常。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: PASS
- **实际行为**: 新位域判断单测通过，旧判断命名在 Go 主代码中已无命中。
- **关键指标**: 旧 `CustomClassifyType*` / `IsPythonType` 等 grep 无命中。
- **备注**: type id 判断路径已完成主要切换。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: collector / TopN 相关单测通过。
- **备注**: 新 collector 能力可用。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过。
- **关键指标**: merge 相关单测通过。
- **备注**: 并行合并能力可用。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: PASS
- **实际行为**: registry fallback 单测通过。
- **关键指标**: unknown type fallback 格式正常。
- **备注**: 兜底格式化符合预期。

### TC-006: Result String 输出可读的调试信息
- **结果**: PASS
- **实际行为**: TypeID 字符串单测通过。
- **关键指标**: 输出包含 `module/subtype/seq`。
- **备注**: 调试可读性满足要求。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: PASS
- **实际行为**: Python / Node.js / C++ 三组回归现均能通过 `run_test.py` 统一框架完成验收。
- **关键指标**:
  - `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
  - `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
  - `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS
- **备注**: 本轮已为 `testdata/nodejs/20260225-maze-vs-heapsnapshot/` 新增符合规范的 `validate.py`。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 新 `common/results` 已接入初始化、merge、snapshot 与 registry fallback，但业务主调用方仍广泛经由 `common/collector_results.go` 中的兼容层 API/全局视图工作。
- **关键指标**:
  - `results.Init/Reset/Merge/Snapshot/Registry` 已命中，说明新包已接管部分底层能力；
  - `GlobalResults|GetResultEntry|AddResultObject|MergeLocalResults|CollectLocalResults|ResultEntry` 仍有大量命中（此前检查为 418 处）；
  - `common/collector_results.go` 仍保留完整桥接逻辑。
- **备注**: 当前仍是“新内核 + 旧 common 兼容外壳”状态，不符合“两个都不保留”的严格要求。

## 失败的 Test Case

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item / collector 外壳与旧 type id 方案都应被直接替换，不再保留兼容层作为业务主调用接口。
- **实际行为**: 新 `common/results` 虽已接入，但业务代码仍主要依赖 `GlobalResults` / `ResultEntry` / `GetResultEntry` / `AddResultObject` / `MergeLocalResults` 等 common 层兼容接口；`common/collector_results.go` 仍承担结果转换与全局快照职责。
- **复现步骤**:
  1. 阅读 `dev-flow/plan.md`，确认需求为“两个都不保留，直接改写成新的设计”
  2. 搜索 `results\.(Init|Reset|Merge|Snapshot|Registry)\(`，确认新包已接入
  3. 搜索 `GlobalResults|GetResultEntry|AddResultObject|AddResultLimited|MergeLocalResults|CollectLocalResults|RunWithLocalResults|ResultEntry`，确认仍有大量命中
  4. 阅读 `common/collector_results.go`，确认兼容层仍保留完整桥接逻辑
- **可能原因**: 本轮完成了旧命名清理和底层 collector 接管，但未继续推进到调用方 API 与 common 层外壳的彻底删除。
- **建议修复**: 继续将调用方迁移到 `common/results` 原生 API/数据结构，并删除或极限收敛 `common/collector_results.go` 的兼容层后再回归验收。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [x] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 8
- 失败: 1 / 8
- 建议: **暂不可发布**。Node.js 回归框架问题已补齐并通过，但核心需求“旧设计彻底移除”仍未满足，需要 developer 继续收敛/删除 common 兼容层后再重新验收。
