## QA Result: FAIL

## Findings
- 已阅读 `dev-flow/20260309153849-dev-report.md`、`dev-flow/test-cases.md`、`dev-flow/plan.md`，并复核当前工作树实现与 `git diff origin/master...HEAD`。
- 相比前几轮，`testdata` 回归问题已解决：`testdata/run_test.py` 存在且本轮按要求实际执行了 Python / Node.js / C++ 三组回归，三者均通过。
- 新 TypeID 位域设计已真实接入主代码：`common/classifier.go` 通过 `results.MakeTypeID(...)` 与 `results.TypeID(...).IsPython/IsTXOS/IsPiece/IsBigSize/IsVtable` 完成主要模块判断，旧 `IsPythonType` / `IsLpcType` / `ResultTypeBase` / `PythonTypeBase` / `TXOSTypeBase` / `NodejsTypeBase` 在主 Go 代码中已无命中。
- 但需求是“两个都不保留、直接改写成新的设计”。当前结果收集主路径仍未直接切到 `common/results` 的 `Add/Finalize/Get` 这套 Collector API，而是保留了 `common/results_runtime.go` 这一层兼容运行时：业务侧仍大量调用 `CollectObject` / `CollectLocalResults` / `MergeLocalResults` 并操作 `FinalResults map[uint64]*results.Entry`。这说明旧的全局收集架构虽然换了底层数据结构名，但仍以包装层形态保留在主路径中。
- `common/results/collector.go` 中的 `Strategy` 抽象（如 `TopNStrategy` / `KeepOrderStrategy`）并未成为业务主采集路径的唯一入口；`common/results_runtime.go` 仍直接向 `Entry.Objects` / `Entry.ObjSize` / `Count` / `TotalSize` 写入，再通过 `MergeUint64()` 汇总。当前状态更接近“新数据模型 + 兼容收集运行时”，尚不足以证明“旧 common result item 方案已完全删除”。
- 类型 registry 也未完成真正落地：全仓未发现 `.Register(...)` 调用，`common/callback.go` 与 `nodejs/maze.go` 虽已接入 `results.Registry()` fallback，但该 registry 目前主要只能输出 `unknown{module=...,subtype=...}` 兜底字符串，而不是一个完成迁移后的统一注册式格式化体系。
- 因此，本轮虽然回归测试全部通过、兼容性明显改善，但核心验收项“旧设计完全移除，直接改为新 Collector/Strategy/Registry 设计”仍未完全满足，结论维持 FAIL。

## Test Results
- `go test ./common/results`: PASS
- `./maze --build`: PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`: PASS
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: PASS
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`: PASS

## Detailed Results

### TC-001: TypeID 位域编码与解析正确
- **结果**: PASS
- **实际行为**: `go test ./common/results -run TestMakeTypeIDEncodesModuleSubtypeAndSequence` 包含在 `go test ./common/results` 中通过。
- **关键指标**: `module/subtype/sequence` 编解码断言通过。
- **备注**: 新 TypeID 基础能力正常。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: PASS
- **实际行为**: 主代码 `common/classifier.go` 已切到 `results.TypeID(...).IsXxx()` 判断，旧 type-id 判断命名在主 Go 代码中无命中。
- **关键指标**: `IsPythonType` / `IsLpcType` / `ResultTypeBase` / `PythonTypeBase` / `TXOSTypeBase` / `NodejsTypeBase` grep 无命中。
- **备注**: type id 迁移基本完成。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: PASS
- **实际行为**: `go test ./common/results` 中 collector 相关单测通过。
- **关键指标**: `Count=3`、`TotalSize=448`、`AvgSize=149`。
- **备注**: 新 collector 包内部能力可用。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: PASS
- **实际行为**: `go test ./common/results` 中 merge 单测通过。
- **关键指标**: 合并后 `Count=3`、`TotalSize=390`、`AvgSize=130`。
- **备注**: 新 merge 能力本身可用。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: PASS
- **实际行为**: registry fallback 单测通过，未知类型字符串可输出 `unknown{module=...,subtype=...}`。
- **关键指标**: `unknown{module=0x28,subtype=0x321}`。
- **备注**: 兜底可用，但统一注册式格式化尚未完整落地。

### TC-006: Result String 输出可读的调试信息
- **结果**: PASS
- **实际行为**: `go test ./common/results` 中调试字符串单测通过。
- **关键指标**: 输出 `TypeID{module=0x28, subtype=0x456, seq=99}`。
- **备注**: 可读性满足要求。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: PASS
- **实际行为**: Python / Node.js / C++ 三组 `run_test.py` 回归均成功执行并通过验证。
- **关键指标**:
  - Python: `pymempool_objects = 68626`，多类对象数量断言通过
  - Node.js: representative checks `14 / 14` 通过
  - C++: total chunks `640012`，small chunk distribution 校验通过
- **备注**: issue 中“qa 需要多执行几个 testdata 回归”的要求本轮已满足。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 旧 `common/result_items.go` 已删除，旧 type-id 命名也已基本移除；但结果收集主业务路径仍通过 `common/results_runtime.go` 中的全局/包装式接口工作，而非直接以新 `common/results` Collector API 为唯一主入口。
- **关键指标**:
  - 主代码仍大量命中 `CollectLocalResults` / `MergeLocalResults` / `CollectObject` / `FinalResults`
  - `common/results_runtime.go` 仍维护 `FinalResults` 全局视图与 `RefreshFinalResults()` 快照同步
  - 全仓未发现 `.Register(...)` 调用，说明 registry 设计尚未完整取代旧格式化链路
- **备注**: 当前更像“新结构承载 + 兼容运行时桥接”，未完全达到“两个都不保留、直接改写成新的设计”。

## Failed Test Cases

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item 与旧全局收集主路径应完全移除，业务代码直接使用新 `common/results` 的 Collector / Strategy / Registry 设计作为唯一主实现。
- **实际行为**: 主代码虽然切到了新 `results.Entry` / 新 TypeID，但仍保留 `common/results_runtime.go` 这一层兼容式全局收集运行时；业务侧继续通过 `CollectObject` / `CollectLocalResults` / `MergeLocalResults` / `FinalResults` 访问和汇总结果，而不是直接基于新 Collector API 工作。
- **复现步骤**:
  1. 阅读 `dev-flow/plan.md`，确认要求为“两个都不保留，直接改写成新的设计”
  2. 阅读 `common/results_runtime.go`
  3. 搜索 `FinalResults|CollectObject\(|CollectObjectWithLimit\(|CollectObjectLimited\(|CollectLocalResults|MergeLocalResults`
  4. 观察大量生产代码仍依赖该包装层主路径
  5. 搜索 `\.Register\(`，观察无 registry 注册调用
- **可能原因**: 本轮已完成底层数据结构替换和旧命名清理，但为了兼容现有业务模块与展示链路，仍保留了旧式全局收集架构的包装层，没有继续收敛到“新 API 直连”状态。
- **建议修复**: 将各分析模块逐步从 `common/results_runtime.go` 包装层迁移到 `common/results` 原生 Collector/Strategy/Registry API，并在迁移完成后删除 `FinalResults`/`CollectObject`/`CollectLocalResults` 这类兼容式全局入口。

## Summary
- 通过: 7 / 8
- 失败: 1 / 8
- 建议: **暂不可发布**。回归测试已恢复并通过，但仍需继续完成结果收集主路径对新设计的彻底收敛/删除兼容运行时后，再重新验收。
