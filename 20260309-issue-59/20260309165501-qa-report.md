## QA Result: FAIL

## Findings
- 已执行要求中的核心验证：`go test ./common/results` 通过，`./maze --build` 通过；但 issue 明确要求的多组 `testdata` 回归仍无法执行，因为当前工作树缺少 `testdata/run_test.py`，且 `testdata/` 目录为空。
- 当前实现相比前几轮继续推进：旧 `CustomClassifyTypeBegin` / `ResultTypeBase` / `PythonTypeBase` / `TXOSTypeBase` / `NodejsTypeBase` / `IsPythonType` / `IsLpcType` 在 Go 代码中已无命中；`common/classifier.go` 已切到 `maze/common/results` 的 `TypeID` 模块判断；`common/callback.go` 也已通过 `results.Registry()` 做未知类型格式化 fallback。
- 但“两个都不保留，直接改写成新的设计”这一核心要求仍未完全满足。当前业务主路径仍主要经由 `common/result_items.go` 暴露的兼容/包装层工作：该文件仍保留全局 `AggregatedResults`、`CurrentObjectSizeMap`、`PriorityQueue`、`AddToAggregatedResults`、`MergeAggregatedResults`、`CollectLocalResults`、快照回填等结构；全仓对这些包装 API/全局状态仍有大量调用（grep 命中 254 处）。这说明当前状态更接近“新 `common/results` 接管底层聚合，但业务层继续依赖 common 包包装外壳”，而不是“直接改写成新的设计”。
- `EnablePyMerge` 仍然深度参与结果收集语义选择，并直接传入 `results.Merge(..., EnablePyMerge)` 控制对象保留策略；而本轮设计目标是以新的 Collector/Strategy 方案替代旧行为开关式设计。该旧语义开关在主代码中仍有 30+ 处命中，说明重构尚未彻底收敛。
- `dev-flow/test-cases.md`、`dev-flow/plan.md`、最新 `*-dev-report.md` 在当前工作树中不存在；本次验收只能结合 `.symphony_prompt.md` 中归档内容、当前代码实现和实际命令结果完成验证。即使按较宽松口径判断，回归测试缺失这一项也足以导致本次验收为 FAIL。

## Test Results
- `go test ./common/results`: PASS
- `./maze --build`: PASS（输出 `begin go build`）
- `python3 testdata/run_test.py python/20260129-complex-types-311`: FAIL，`testdata/run_test.py` 不存在
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`: FAIL，`testdata/run_test.py` 不存在
- `python3 testdata/run_test.py cpp/20260201-basic-malloc`: FAIL，`testdata/run_test.py` 不存在

## Detailed Results

### TC-001: TypeID 位域编码与解析正确
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，包含 `TypeID` 相关单测。
- **关键指标**: `module/subtype/sequence` 编解码单测通过。
- **备注**: 新 TypeID 基础能力可用。

### TC-002: TypeID 模块判断方法可替代旧区间判断
- **结果**: PASS
- **实际行为**: `common/classifier.go` 中 Python/TXOS 判断已改为 `results.TypeID(t).IsPython()` / `IsTXOS()`；旧 `IsPythonType` / `IsLpcType` 等旧命名已无命中。
- **关键指标**: 旧 type-id 相关旧命名 grep 无命中。
- **备注**: 类型判断迁移有实质进展。

### TC-003: Collector + TopNStrategy 能保留最大对象并输出统计
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，collector 包单测通过。
- **关键指标**: `common/results` 包测试整体通过。
- **备注**: 新 collector 包本身可用。

### TC-004: 并行局部结果可通过 Merge 合并
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，merge 行为相关单测通过。
- **关键指标**: `common/results` 包测试整体通过。
- **备注**: 新 merge 基础能力可用。

### TC-005: 未注册类型可输出结构化未知类型名
- **结果**: PASS
- **实际行为**: `common/callback.go` 的默认格式化已接入 `results.Registry().Format(t)`。
- **关键指标**: 未注册类型将走 registry fallback，而不是返回固定占位字符串。
- **备注**: fallback 路径已进入业务代码。

### TC-006: Result String 输出可读的调试信息
- **结果**: PASS
- **实际行为**: `go test ./common/results` 通过，`TypeID.String()` 调试输出单测通过。
- **关键指标**: 字符串包含 module/subtype/seq 解码信息。
- **备注**: 调试可读性满足要求。

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **实际行为**: Python / Node.js / C++ 三个 `run_test.py` 命令均直接失败，报错 `python3: can't open file '.../testdata/run_test.py': [Errno 2] No such file or directory`。
- **关键指标**: `testdata/` 目录为空；`testdata/run_test.py` 缺失。
- **备注**: 未满足 issue 中“qa 需要多执行几个 testdata 回归”的明确要求。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **实际行为**: 新 `common/results` 已接入底层能力，但业务主代码仍广泛依赖 `common/result_items.go` 的包装层和全局结果视图，而非直接使用新的 Collector/Strategy/Registry 设计。
- **关键指标**:
  - `AggregatedResults|EnsureAggregatedItem|AddToAggregatedResults|MergeAggregatedResults|CollectLocalResults|InitAggregatedResults` 在 Go 代码中命中 254 处；
  - `common/result_items.go` 仍保留全局 `AggregatedResults`、`CurrentObjectSizeMap`、`PriorityQueue`、快照回填、dump 等包装实现；
  - `EnablePyMerge` 仍在主代码中多处参与行为分支，并直接影响 `results.Merge(..., EnablePyMerge)` 的策略语义。
- **备注**: 当前更接近“新底层 + 旧包装层并存”，未达到“两个都不保留”的严格要求。

## Failed Test Cases

### TC-007: 回归验证现有 testdata 不因重构破坏输出
- **结果**: FAIL
- **预期行为**: 至少执行 Python / Node.js / C++ 三组 `run_test.py` 回归，并确认输出未被重构破坏。
- **实际行为**: 仓库缺少 `testdata/run_test.py` 与对应测试数据内容，三个回归命令均无法启动。
- **复现步骤**:
  1. 在仓库根目录执行 `python3 testdata/run_test.py python/20260129-complex-types-311`
  2. 观察报错 `python3: can't open file .../testdata/run_test.py: [Errno 2] No such file or directory`
  3. 对 Node.js / C++ 用例重复执行，现象相同
- **可能原因**: 当前工作树未包含可执行的 `testdata` 测试框架内容。
- **建议修复**: 补齐并展开可执行 `testdata` 后，重新按 test case 执行 Python / Node.js / C++ 三组回归。

### TC-008: 旧设计已被彻底移除
- **结果**: FAIL
- **预期行为**: 旧 common result item 与旧 type-id 方案应被新 `common/results` Collector/Strategy/Registry 设计完全替代，不再保留业务主路径上的 common 包包装层与旧行为开关语义。
- **实际行为**: 业务主路径仍通过 `common/result_items.go` 中的全局 map/包装 API/快照回填逻辑访问结果；`EnablePyMerge` 仍作为行为分支存在，说明新设计尚未完全取代旧设计思路。
- **复现步骤**:
  1. 阅读 `common/result_items.go`，确认仍存在 `AggregatedResults`、`CurrentObjectSizeMap`、`PriorityQueue`、`AddToAggregatedResults`、`MergeAggregatedResults`、`CollectLocalResults`
  2. 搜索 `AggregatedResults|EnsureAggregatedItem|AddToAggregatedResults|MergeAggregatedResults|CollectLocalResults|InitAggregatedResults`，确认在主代码中仍有大量命中
  3. 搜索 `EnablePyMerge`，确认其仍在主代码中参与结果收集策略分支
- **可能原因**: 本轮完成的是“底层 collector 接管 + 旧接口重命名/包装”，但尚未推进到业务调用层直接使用新设计、并删除 common 包兼容外壳。
- **建议修复**: 继续把业务调用链迁移到 `common/results` 的显式 Collector/Strategy/Registry 语义，删除 `common/result_items.go` 中不再必要的全局包装状态与旧行为开关后，再做回归验收。

## Summary
- 通过: 6 / 8
- 失败: 2 / 8
- 建议: **暂不可发布**。需先补齐并执行要求中的多组 `testdata` 回归，并继续完成旧 common result 聚合包装层/旧策略语义的收敛移除后，再重新验收。
