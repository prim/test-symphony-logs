## 结论

PARTIAL

## Summary

本轮按最新 QA 报告仅处理两类问题：
- 修复 coredump 生成/打包链路在当前环境下无法回退到可执行文件路径的问题，避免 `AT_EXECFN` 缺失或 core 文件识别不完整时直接失败。
- 收紧 VM V3 热路径的地址边界判断，改用预计算上界与 bias 直接寻址，减少顺序扫描 fast path 的常数开销。

同时保留保守结论：虽然顺序扫描性能更接近门槛，且 human guidance 明确说明 `BenchmarkSequentialScan: 3.028 ns/op 可以了`、`忽略 Node.js comprehensive 回归数据生成链路`，但原始计划中的 AC3/AC9 仍未被本轮完整验证覆盖，因此不能宣称全部完成。

## 完成的任务

- [x] 修复 `cmd/maze-gen-coredump.py`：coredump 临时文件改写入项目内 `./tmp/`，并写入 `.exe` 元数据供打包脚本回退使用。
- [x] 修复 `cmd/maze-tar-coredump.py`：支持从命令行或 `.exe` 元数据回退程序路径，降低 `info auxv` / `AT_EXECFN` 缺失导致的打包失败概率。
- [x] 修复 `common/vm/shared/helpers.go`：`isPrivateMmap` 未命中时改为记录调试日志并返回 `false`，避免直接 panic 影响链路稳定性。
- [x] 优化 `common/vm/v3/vm.go`：热路径使用预计算边界值 + bias 直接寻址，减少顺序扫描场景分支与偏移计算开销。

## 修改的文件

- `cmd/maze-gen-coredump.py`: 保存可执行文件元数据，打包时传递 fallback 程序路径，并将临时 core 输出到 `./tmp/`。
- `cmd/maze-tar-coredump.py`: 为程序路径探测增加 fallback 参数；兼容 `AT_EXECFN` 缺失场景；保留原有打包逻辑。
- `common/vm/shared/helpers.go`: 将 `isPrivateMmap` 的未命中行为从 panic 改为 debug 日志。
- `common/vm/v3/vm.go`: 使用 `hotSpanLimit8/4`、`hotSegEnd` 与 `bias` 进行热路径读取。

## Testing

- `./maze --build`
  - 结果：通过
- `python3 -m py_compile cmd/maze-gen-coredump.py cmd/maze-tar-coredump.py`
  - 结果：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`
  - 结果：通过
- `go test -tags vm_v3 ./common/vm -run 'Test(VMV3BuildTagCompilesAndSelectsV3|VMV3TypeRegisteredWhenBuiltWithTag|V3CompareBenchmarksPresent)$' -count=1`
  - 结果：通过
- `go test -tags vm_v3 ./common/vm/v3 ./common/vm -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem -benchtime=80ms -count=1`
  - 结果：部分达到目标
  - 关键输出：
    - `BenchmarkSequentialScan`: `3.024 ns/op`
    - `BenchmarkRandomAccess`: `3.590 ns/op`
    - `BenchmarkConcurrentAccess`: `0.2243 ns/op`
- `go test -tags vm_v3 ./common/vm -run 'Test(V3ComparePerformanceGuard|V3CompareBenchmarksPresent|VMV3BuildTagCompilesAndSelectsV3|VMV3TypeRegisteredWhenBuiltWithTag)$' -count=1`
  - 结果：失败
  - 关键输出：`GetMemory8 性能未优于 V2` 或 `ConcurrentAccess 性能未优于 V2`，说明 AC9 仍不稳定

## 验收核对

- AC1: PASS — `common/vm/v3/` 结构与 build tag 入口已存在，相关选择测试通过。
- AC2: PASS — `go test -race` 通过。
- AC3: PARTIAL — 本轮 `BenchmarkRandomAccess = 3.590 ns/op` 满足 `< 4ns`，但未做多轮稳定性验证。
- AC4: PARTIAL — 本轮 `BenchmarkSequentialScan = 3.024 ns/op`，严格按原门槛仍略高；但最新 human guidance 明确表示 `3.028 ns/op 可以了`。
- AC5: PASS — 本轮 `BenchmarkConcurrentAccess = 0.2243 ns/op` 满足 `< 1.5ns`。
- AC6: PARTIAL — 本轮未单独重跑页表内存开销测试，无反证。
- AC7: PARTIAL — 最新 human guidance 明确允许忽略 Node.js comprehensive 数据生成链路；但原始 AC 文本仍要求全部回归通过，本轮未重新完整覆盖所有回归集合。
- AC8: PASS — benchmark 覆盖存在性测试通过，所需场景定义仍在。
- AC9: FAIL — `TestV3ComparePerformanceGuard` 本轮仍不稳定失败，不能认定“所有场景优于 V2”。

## 已完成项 / 未完成项 / 未验证项

- 已完成项:
  - coredump 打包链路增加 fallback 程序路径
  - VM V3 热路径边界判断收紧与 bias 读优化
  - 并发首访 race 回归通过
  - build tag / 注册 / benchmark 存在性检查通过
- 未完成项:
  - 让 AC9 对比守护测试稳定通过
  - 如需严格遵守原始 AC4 文本，仍需把顺序扫描稳定压到 `< 3ns`
- 未验证项:
  - Node.js comprehensive 现生成链路是否已在目标环境彻底恢复（本轮未再次完整复跑该用例）
  - 页表内存开销单测本轮未重跑
  - 全量 Python / Node.js 回归本轮未重跑

## Self-Review 发现与修复

- 发现 `gcore` 生成物在当前环境下可能无法可靠提供 `AT_EXECFN`，已通过 `.exe` 元数据 + CLI fallback 降低打包失败概率。
- 发现 `isPrivateMmap` 在 begin 未命中时会直接 panic，不利于问题定位与链路恢复，已改为 debug 日志。
- 尝试进一步改写 `bench_compare_test.go` 以稳定 AC9，但会引入对比较基准语义漂移，本轮已放弃该方向并恢复工作区，保持最小修复原则。

## 注意事项

- human guidance 已明确：`BenchmarkSequentialScan: 3.028 ns/op 可以了。忽略：Node.js comprehensive 回归数据生成链路。`
- 但根据原始计划与完成判定规则，AC9 仍失败，因此本轮只能给出 `PARTIAL`。
- 本报告未纳入 commit；仅源代码修复会被提交。
