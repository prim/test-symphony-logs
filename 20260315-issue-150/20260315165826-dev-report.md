## Summary
本轮为 **PARTIAL** 修复：针对 QA 指出的 V3 热路径分支与边界判断开销，收敛了 `common/vm/v3/vm.go` 中 8/4 字节读取的快路径判定，改为基于预计算可读范围与 bias 直接解引用，并为四个入口补充 `//go:nosplit`，以减少热路径额外开销。

## Changes
- `common/vm/v3/vm.go`: 将 `GetMemory/GetMemorySafe/GetMemory4/GetMemory4Safe` 的快路径条件从双重比较改为基于 `hotSpanRange*` / `hotSegRange*` 的单偏移范围判断；直接使用 `hotSpanBias` / `hotSegBias` 做指针读取；为四个高频入口增加 `//go:nosplit`，降低调用开销。

## Testing
- `./maze --build`：通过
- `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree -count=1`：通过
- `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem -benchtime=20ms -count=1`：通过执行，但指标仍未达 AC3/AC4（约 `RandomAccess=15.11ns/op`，`SequentialScan=12.04ns/op`，`ConcurrentAccess=0.5405ns/op`）
- `go test -tags vm_v3 ./common/vm -run TestV3ComparePerformanceGuard -count=1`：失败，当前失败项为 `RandomAccess` 场景下 V3 仍慢于 V2
- `python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`：通过

## 验收核对
- AC1: PASS，未回归，构建通过
- AC2: PASS，race 测试通过
- AC3: FAIL，随机访问仍约 15.11ns/op，未达到 < 4ns
- AC4: FAIL，顺序扫描仍约 12.04ns/op，未达到 < 3ns
- AC5: PASS，并发访问约 0.5405ns/op，满足 < 1.5ns
- AC6: PARTIAL，本轮未重新执行页表开销测试，沿用上轮 QA 结论
- AC7: PARTIAL，仅复测 Python/Node.js 代表性样例
- AC8: PARTIAL，本轮未重新跑完整 benchmark 覆盖矩阵，仅复测关键三项
- AC9: FAIL，`TestV3ComparePerformanceGuard` 仍失败

## 剩余工作
- 未完成项: 继续剖析 V3 单线程随机访问与顺序扫描热路径，解决 AC3/AC4/AC9
- 未验证项: 页表内存开销复测、完整 benchmark 覆盖矩阵、更广 Python/Node.js 兼容矩阵

## Self-Review 发现与修复
- 发现热路径仍存在多次边界比较与辅助函数调用开销，已改为基于预计算 range 的单偏移判断并直接指针读取。
- 尝试过引入额外读缓存与延迟加载 linear span，但前者会恶化随机访问/并发性能，后者在 benchmark 场景触发非法指针访问风险，均已回退，保留保守且可验证的优化。

## 注意事项
- 当前修复仅改善热路径实现形式，未改变 V3 整体数据结构，因此对 AC3/AC4/AC9 只是局部推进，不能宣称全部完成。
- 本轮结论：**PARTIAL**。
