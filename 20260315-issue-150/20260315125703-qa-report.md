# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

本轮按“低成本检查尽量完整 + 高成本验证分阶段早停”执行。阶段 A 静态检查与阶段 B 关键验证已完成，发现主链路性能与回归验收均未满足需求，故总体判定为 FAIL。

## 详细结果

### TC-001: V3 build tag 与目录结构可用性
- **结果**: PASS
- **实际行为**: `common/vm/v3/` 目录存在，`common/vm/select_v3.go` 已接入 `VMV3`，`GOFLAGS='-tags=vm_v3' ./maze --build` 构建通过；`go test -tags vm_v3 ./common/vm/...` 通过。
- **关键指标**: build tag 编译通过，`VMV3` 选择测试通过。
- **备注**: AC1 满足。

### TC-002: 无锁加载并发首访无竞态
- **结果**: PASS
- **实际行为**: `go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree` 通过，未报告 data race。
- **关键指标**: race detector 通过。
- **备注**: AC2 满足。

### TC-003: 批量读取与 iterator API 合约
- **结果**: PASS
- **实际行为**: `TestBatchReadAPIContract`、`TestBatchReadHandlesShortResultsBuffer`、`TestMemoryIteratorTraversesAcrossSegments`、`TestPageTableLookupAndFallback`、`TestGetMemoryRangeHandlesUnalignedAndCrossSegment`、`TestPageTableMemoryOverhead` 均通过。
- **关键指标**: API 合约测试通过，跨 segment / 未对齐读取测试通过。
- **备注**: 批量 API 与基础 iterator 合约存在且可用。

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: FAIL
- **实际行为**: `common/vm/v3/v3_contract_test.go` 中确实存在随机访问、顺序扫描、并发访问、热点、未对齐、跨 segment、cache miss、安全读取、混合负载、冷启动、Python/V8 场景 benchmark；但缺少 AC8 明确要求的 `BenchmarkGetMemory8` 与 `BenchmarkGetMemory4`。当前仅有 `BenchmarkGetMemoryBatch8` / `BenchmarkGetMemoryBatch4`，不能替代 issue 中列出的 8 字节/4 字节随机读基准。
- **关键指标**: 必需 benchmark 名称缺失 2 项：`BenchmarkGetMemory8`、`BenchmarkGetMemory4`。
- **备注**: AC8 未完全满足。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **实际行为**: `go test -tags vm_v3 ./common/vm -run TestV3OutperformsV2Placeholders -bench BenchmarkCompareV3GetMemory8 -benchmem` 可执行，但仅新增了 `BenchmarkCompareV3GetMemory8`；未覆盖 AC9 要求的“所有场景”对比。且实测结果 V3 慢于 V2。
- **关键指标**: `v2-full = 9.878 ns/op`，`v3 = 13.56 ns/op`。
- **备注**: AC9 不满足；当前对比测试也没有对“V3 全场景优于 V2”给出真实断言。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: FAIL
- **实际行为**: `python3 testdata/run_test.py python/20260129-complex-types-311` 通过；`python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot` 失败，原因是测试目录缺少 `validate.py`，`run_test.py` 无法完成验证。
- **关键指标**: Python 回归 1/1 通过；Node.js 回归 0/1 通过。
- **备注**: AC7 不满足，且失败原因属于仓内测试资产缺失，当前无法完成完整自动化验收。

### TC-007: 页表内存开销限制
- **结果**: PASS
- **实际行为**: `TestPageTableMemoryOverhead` 通过。
- **关键指标**: 页表额外开销测试未触发阈值失败。
- **备注**: 该测试本身仅验证“页表开销不超过自定义 baseline”，覆盖深度有限，但按现有实现结果可判定通过。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **实际行为**: 运行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem` 时，`BenchmarkSequentialScan` 直接失败，提示 `iterator 提前结束`；另外随机访问与并发访问性能远高于 issue 目标阈值。
- **关键指标**:
  - `BenchmarkSequentialScan`: 失败，未得到有效性能数据
  - `BenchmarkRandomAccess`: `71.58 ns/op`，未满足 `< 4ns`
  - `BenchmarkConcurrentAccess`: `55.89 ns/op`，未满足 `< 1.5ns`
- **备注**: AC3 / AC4 / AC5 均不满足；顺序扫描不仅未达标，连 benchmark 本身都无法稳定跑通。

## 失败的 Test Case

### TC-004: V3 benchmark 覆盖必需场景
- **结果**: FAIL
- **预期行为**: V3 benchmark 覆盖 AC8 与 issue 中列出的全部必需场景，包括 `BenchmarkGetMemory8` 与 `BenchmarkGetMemory4`。
- **实际行为**: 代码中不存在 `BenchmarkGetMemory8` / `BenchmarkGetMemory4`，仅存在 batch 版本 benchmark。
- **复现步骤**:
  1. 检查 `common/vm/v3/v3_contract_test.go`
  2. 搜索 `BenchmarkGetMemory8|BenchmarkGetMemory4`
  3. 仅能找到 `BenchmarkGetMemoryBatch8|BenchmarkGetMemoryBatch4`
- **可能原因**: 实现把 batch API benchmark 补齐了，但遗漏了 issue 明确要求的基础随机读 benchmark。
- **建议修复**: 补充 V3 版本的 `BenchmarkGetMemory8`、`BenchmarkGetMemory4`，并保证与 V2 benchmark 命名和口径可对齐。

### TC-005: V3 与 V2 对比基准存在且可执行
- **结果**: FAIL
- **预期行为**: `common/vm/bench_compare_test.go` 层面的 V3 vs V2 对比覆盖所有关键场景，并证明 V3 在所有场景优于 V2。
- **实际行为**: 仅新增 `BenchmarkCompareV3GetMemory8`，且 placeholder 测试未做真实优于断言；实测 `v3=13.56 ns/op`，`v2=9.878 ns/op`。
- **复现步骤**:
  1. 执行 `go test -tags vm_v3 ./common/vm -run TestV3OutperformsV2Placeholders -bench BenchmarkCompareV3GetMemory8 -benchmem`
  2. 观察输出中 V3 ns/op 大于 V2
- **可能原因**: 当前 V3 热路径尚未真正优于 V2；同时 compare 覆盖面和断言逻辑仍是占位性质。
- **建议修复**: 增补随机访问、顺序扫描、并发访问等 compare benchmark，并把“优于 V2”改成稳定、可复现的真实验收标准。

### TC-006: Python / Node.js 主链路回归未退化
- **结果**: FAIL
- **预期行为**: Python / Node.js 回归测试全部通过。
- **实际行为**: Python 用例通过；Node.js 用例因 `testdata/nodejs/20260225-maze-vs-heapsnapshot/validate.py` 缺失而失败。
- **复现步骤**:
  1. 执行 `python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot`
  2. 观察错误 `validate.py not found`
- **可能原因**: 仓内 Node.js 对比测试未接入 `run_test.py` 约定的验证入口。
- **建议修复**: 为该测试目录补充符合 `run_test.py` 规范的 `validate.py`，并在其中复用或封装 `compare.py` 的验证逻辑。

### TC-008: 性能目标门槛校验
- **结果**: FAIL
- **预期行为**: 随机访问 < 4ns，顺序扫描 < 3ns，并发访问 < 1.5ns。
- **实际行为**: 顺序扫描 benchmark 失败；随机访问 `71.58 ns/op`，并发访问 `55.89 ns/op`，均显著高于目标。
- **复现步骤**:
  1. 执行 `go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem`
  2. 观察 `BenchmarkSequentialScan` 报错 `iterator 提前结束`
  3. 观察 Random/Concurrent 的 ns/op 指标
- **可能原因**:
  - `BenchmarkSequentialScan` 复用了同一个 iterator，`b.N` 增长后很快耗尽数据，导致 benchmark 设计本身失真并失败
  - 当前 V3 读取路径并未达到设计文档要求的 30%-50% 提升，反而明显慢于目标口径
- **建议修复**: 先修正 benchmark 设计，再针对热路径、页表命中、缓存策略做真实性能优化，最后给出稳定环境下的对比数据。

## 额外发现

- **日志敏感信息泄露风险**: 本次查看 `maze.log` 时发现环境变量被完整写入日志，其中包含 `GITHUB_TOKEN`、其他 API key 等敏感值。这不是本 issue 的 AC，但属于严重安全问题，建议尽快修复日志脱敏或避免记录完整环境变量。

## 未执行项

- 未继续执行更多高成本性能回归场景；原因是 AC3/AC4/AC5 已被关键 benchmark 明确否定，继续运行只会重复暴露同类问题。
- 未完成 Node.js 自动化结果比对；原因是测试目录缺少 `validate.py`，导致 `run_test.py` 工作流中断。
- 未执行“V3 在所有 compare 场景优于 V2”的完整动态验证；原因是当前 compare 文件仅实现了单一场景，且该场景已证明 V3 不优于 V2。
- 受影响范围: AC3、AC4、AC5、AC7、AC8、AC9。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 已执行命令

```bash
./maze --build
GOFLAGS='-tags=vm_v3' ./maze --build
go test -tags vm_v3 ./common/vm/...
go test -race -tags vm_v3 ./common/vm/v3 -run TestConcurrentLazyLoadRaceFree
go test -tags vm_v3 ./common/vm/v3 -run 'TestBatchReadAPIContract|TestBatchReadHandlesShortResultsBuffer|TestMemoryIteratorTraversesAcrossSegments|TestPageTableLookupAndFallback|TestGetMemoryRangeHandlesUnalignedAndCrossSegment|TestPageTableMemoryOverhead'
go test -tags vm_v3 ./common/vm/v3 -run '^$' -bench 'Benchmark(RandomAccess|SequentialScan|ConcurrentAccess)$' -benchmem
go test -tags vm_v3 ./common/vm -run TestV3OutperformsV2Placeholders -bench BenchmarkCompareV3GetMemory8 -benchmem
python3 testdata/run_test.py python/20260129-complex-types-311
python3 testdata/run_test.py nodejs/20260225-maze-vs-heapsnapshot
```

## 总结
- 通过: 4 / 8
- 失败: 4 / 8
- 建议: **当前不可以发布。** 需要至少修复以下问题后再复验：
  1. 达成 AC3/AC4/AC5 的真实性能门槛，并修复顺序扫描 benchmark 失效问题
  2. 补齐 AC8 要求的 `BenchmarkGetMemory8/4`
  3. 补齐 AC9 的全场景 compare benchmark 与真实优于断言
  4. 为 Node.js 回归用例补充 `validate.py`，恢复自动化验收链路
