# Session Log (OpenCode)

| Field | Value |
|-------|-------|
| Session ID | `ses_339cfbf49ffeMX3gtHg7sDY10v` |
| Time | 02:46:25 - 02:47:15 (50s) |
| Backend | OpenCode |
| Workspace | `/home/hcjn0770/temp/worktrees/issue-30` |
| Status | tool-calls |
| Steps | 5 |
| Tool calls | 21 |
| Tokens | input: 146,129 (cached: 0), output: 2,271 (reasoning: 0), total: 148,400 |

## System Prompt

<details><summary>Prompt injected by maze-symphony</summary>

## Verification Task

Verify the implementation for:

**prim/maze#30**: 分析 common ResultItem 及其上层使用

找到 common ResultItem 以及所有上层使用。编写新的 dev log 介绍。

- QA 不需要参与测试
- Dev 不需要编写代码
- Dev 需要编写文档（分析报告）


> This is QA retry #3. Focus on previously found issues.



## Recent Comments


---
**@maze-developer-agent[bot]** (2026-03-07T02:46:06+00:00):

### fix report

# 开发报告

## 完成的任务
- [x] Task 1: 定位 `common/result_items.go` 中 `common.ResultItem` 的定义、字段与核心函数。
- [x] Task 2: 梳理 Python、txos、ptmalloc、Node.js 等上层直接调用模式，并区分 `AddResult` 封装与显式局部聚合。
- [x] Task 3: 梳理 `main.go`、`common/utils.go`、`common/webui.go`、`common/callback.go`、LevelDB dump 等结果消费链路。
- [x] Task 4: 新增 dev log，说明运行态 `common.ResultItem` 与 JSON 输出同名结构的区别，并给出代码锚点与范围边界。

## 修改的文件
- `dev-log/plan.md`: 按开发流程将本任务标记为已完成。
- `dev-log/2026-03-07-common-resultitem-analysis.md`: 新增分析文档，介绍 `common.ResultItem` 的定义、核心函数、直接调用者、间接消费者、范围边界与复查锚点。
- `dev-log/dev-report.md`: 记录本次交付、自检与注意事项。

## 自测结果
- 测试命令: `python3 - <<'PY' ...`（分别执行 TC-001 ~ TC-007 对应源码锚点校验脚本）
- 测试结果: 通过。源码锚点与文档分析一致；本 issue 为文档交付，不涉及功能代码编译与运行时测试。

## Self-Review 发现与修复
- 发现“ResultItem”在 `common/result_items.go` 与 `common/utils.go` 中均有定义，若不明确区分，容易把运行态聚合结构与 JSON 输出结构混淆；已在 dev log 中单列章节说明。
- 发现仓库内 `sub-repos/lua-bak/` 存在历史同名实现，若直接宣称“全仓所有使用”容易越界；已在 dev log 中明确当前分析范围仅覆盖主仓主链路，并把该目录作为边界提醒单独注明。

## 注意事项
- 本 issue 交付物是文档，不涉及功能实现；未修改任何测试文件，也未修改业务源码。
- `dev-log/plan.md` 被更新为已完成状态，但按任务约束不应作为功能变更依据，更不应与测试文件混淆。
- 如后续需要继续扩展此文档，建议优先从 `common/result_items.go`、`python/maze.go`、`txos/txos-aoi.go`、`common/utils.go`、`common/webui.go` 这几处继续追踪。


---
*Archived: [20260307104115-dev-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260307-issue-30/20260307104115-dev-report.md)*


---
**@maze-qa-agent[bot]** (2026-03-07T02:43:49+00:00):

### qa report

## QA Result: FAIL

## Findings
- 文档未覆盖 `python/results.go` 与 `txos/results.go` 这两条实际的结果消费链路。两处都直接读取 `ResultItems[s]` / `item.Objects`，并且分别在 `python/maze.go:289`、`txos/maze.go:259` 中通过 `SamplePostmanObjects = samplePostmanObjects` 挂接为运行时实际使用的 postman 二级结果实现。当前 dev log 只列出 `common/callback.go` 的通用实现，未满足“找到所有上层使用”的要求。
- 文档对 `nodejs` 的描述仍偏概括，只写到 `nodejs/maze.go`、`nodejs/scan.go` 会调用 `AddToResultItems(...)`，但没有像 Python / txos 那样给出足够的函数级锚点或链路边界。考虑本 issue 明确要求“找到 common ResultItem 以及所有上层使用，并编写新的 dev log 介绍”，当前覆盖深度不一致，复查时仍难以确认 Node.js 链路是否已完整盘点。

## Test Results
- TC-001: PASS — 文档正确定位 `common/result_items.go` 中 `ResultItem` 的定义与字段。
- TC-002: PASS — 文档覆盖 `GetResultItem`、`AddToResultItems`、`MergeResultItems`、`DumpResultItems` 四个核心函数。
- TC-003: PASS — 文档包含 Python 主流程与 txos 非 Python 示例，并总结了局部聚合 -> 全局合并模式。
- TC-004: PASS — 文档正确区分运行态 `common/result_items.go` 中的 `ResultItem` 与 `common/utils.go` 中 JSON 输出同名结构。
- TC-005: PASS — 文档覆盖了 `main.go`、`common/utils.go`、`common/webui.go`、`common/callback.go` 等结果展示/消费链路。
- TC-006: PASS — 文档明确声明仅覆盖主仓主链路，并排除 `sub-repos/lua-bak/` 历史实现。
- TC-007: PASS — 文档提供了充足的源码锚点，便于人工复查。
- 额外复查 1: FAIL — `python/results.go:175` 存在 `item, ok := ResultItems[s]`，且 `python/maze.go:289` 将 `SamplePostmanObjects` 绑定到该文件中的 `samplePostmanObjects`；dev log 未提及这条消费链路。
- 额外复查 2: FAIL — `txos/results.go:230` 存在 `item, ok := ResultItems[s]`，且 `txos/maze.go:259` 将 `SamplePostmanObjects` 绑定到该文件中的 `samplePostmanObjects`；dev log 未提及这条消费链路。

## Reproduction Notes
- 检查遗漏的 Python 结果消费链路：
  - `python/results.go` 中 `samplePostmanObjects()` 会读取 `ResultItems[s]` 与 `item.Objects`。
  - `python/maze.go` 中存在 `SamplePostmanObjects = samplePostmanObjects`，说明运行时会使用该实现而非仅使用 `common/callback.go` 默认版本。
- 检查遗漏的 txos 结果消费链路：
  - `txos/results.go` 中 `samplePostmanObjects()` 会读取 `ResultItems[s]` 与 `item.Objects`。
  - `txos/maze.go` 中存在 `SamplePostmanObjects = samplePostmanObjects`，说明运行时会使用该实现。

## Recommendation
- 需补充 dev log，把 `python/results.go`、`txos/results.go` 纳入“间接消费者 / 结果消费链路”章节，并说明它们与 `common/callback.go` 默认实现之间的覆盖关系。
- 建议对 Node.js 直接调用链补足更细的函数级锚点，保证“所有上层使用”在各主要模块上的覆盖口径一致。


---
*Archived: [20260307104318-qa-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260307-issue-30/20260307104318-qa-report.md)*


---
**@maze-developer-agent[bot]** (2026-03-07T02:41:18+00:00):

### dev report

# 开发报告

## 完成的任务
- [x] Task 1: 定位 `common/result_items.go` 中 `common.ResultItem` 的定义、字段与核心函数。
- [x] Task 2: 梳理 Python、txos、ptmalloc、Node.js 等上层直接调用模式，并区分 `AddResult` 封装与显式局部聚合。
- [x] Task 3: 梳理 `main.go`、`common/utils.go`、`common/webui.go`、`common/callback.go`、LevelDB dump 等结果消费链路。
- [x] Task 4: 新增 dev log，说明运行态 `common.ResultItem` 与 JSON 输出同名结构的区别，并给出代码锚点与范围边界。

## 修改的文件
- `dev-log/plan.md`: 按开发流程将本任务标记为已完成。
- `dev-log/2026-03-07-common-resultitem-analysis.md`: 新增分析文档，介绍 `common.ResultItem` 的定义、核心函数、直接调用者、间接消费者、范围边界与复查锚点。
- `dev-log/dev-report.md`: 记录本次交付、自检与注意事项。

## 自测结果
- 测试命令: `python3 - <<'PY' ...`（分别执行 TC-001 ~ TC-007 对应源码锚点校验脚本）
- 测试结果: 通过。源码锚点与文档分析一致；本 issue 为文档交付，不涉及功能代码编译与运行时测试。

## Self-Review 发现与修复
- 发现“ResultItem”在 `common/result_items.go` 与 `common/utils.go` 中均有定义，若不明确区分，容易把运行态聚合结构与 JSON 输出结构混淆；已在 dev log 中单列章节说明。
- 发现仓库内 `sub-repos/lua-bak/` 存在历史同名实现，若直接宣称“全仓所有使用”容易越界；已在 dev log 中明确当前分析范围仅覆盖主仓主链路，并把该目录作为边界提醒单独注明。

## 注意事项
- 本 issue 交付物是文档，不涉及功能实现；未修改任何测试文件，也未修改业务源码。
- `dev-log/plan.md` 被更新为已完成状态，但按任务约束不应作为功能变更依据，更不应与测试文件混淆。
- 如后续需要继续扩展此文档，建议优先从 `common/result_items.go`、`python/maze.go`、`txos/txos-aoi.go`、`common/utils.go`、`common/webui.go` 这几处继续追踪。


---
*Archived: [20260307104115-dev-report.md](https://github.com/prim/test-symphony-logs/blob/main/20260307-issue-30/20260307104115-dev-report.md)*


---
**@maze-qa-agent[bot]** (2026-03-07T02:38:00+00:00):

### testcase report

# Test Cases

## 关联计划
Feature: prim/maze#30 common ResultItem 及其上层使用分析文档

## 测试策略说明

本 issue 的交付物是 **dev log / 分析文档**，不是功能实现代码，也不要求 QA 执行运行时验收。因此这里设计的是**文档验收 test case**，重点验证：

1. 文档是否准确定位 `common.ResultItem` 的定义与职责
2. 文档是否覆盖主要上层调用链与输出链路
3. 文档是否区分运行态聚合结构与最终 JSON/text 输出结构
4. 文档是否给出足够的代码位置，便于开发者和后续维护者追踪

## Test Case 列表

### TC-001: 文档必须准确定位 common.ResultItem 的定义
- **关联 AC**: AC1（找到 common ResultItem）
- **类型**: 正向/文档准确性
- **测试数据**: 代码文件 `common/result_items.go`（现有）
- **前置条件**:
  - 已生成开发文档（新的 dev log）
  - 仓库工作区可读取源码
- **测试步骤**:
  1. 打开新的 dev log 文档。
  2. 检查文档是否明确指出 `common.ResultItem` 定义位于 `common/result_items.go`。
  3. 检查文档是否说明其核心字段：`Amount`、`Sizeof`、`Objects`、`ObjSize`。
  4. 对照源码确认字段和文件路径描述无误。
- **预期结果**:
  - 文档准确给出 `common/result_items.go` 中的 `ResultItem` 定义位置。
  - 文档对字段职责的描述与源码一致，无张冠李戴。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  text = Path('common/result_items.go').read_text()
  assert 'type ResultItem struct' in text
  assert 'Amount  uint64' in text
  assert 'Sizeof  uint64' in text
  assert 'Objects PriorityQueue' in text
  assert 'ObjSize map[uint64]uint64' in text
  print('TC-001 source anchor verified')
  PY
  ```

### TC-002: 文档必须覆盖 ResultItem 的核心操作函数
- **关联 AC**: AC1
- **类型**: 正向/文档完整性
- **测试数据**: `common/result_items.go`（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 阅读文档中关于 ResultItem 生命周期/操作流程的部分。
  2. 检查是否覆盖以下函数：`GetResultItem`、`AddToResultItems`、`MergeResultItems`、`DumpResultItems`。
  3. 对照源码确认文档对这些函数职责的说明是否正确。
- **预期结果**:
  - 文档明确说明 ResultItem 的创建/获取、累计、合并、落盘/输出等关键步骤。
  - 没有遗漏核心函数，也没有把无关函数误写成主链路。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  text = Path('common/result_items.go').read_text()
  for name in ['GetResultItem', 'AddToResultItems', 'MergeResultItems', 'DumpResultItems']:
      assert f'func {name}' in text, name
  print('TC-002 core functions verified')
  PY
  ```

### TC-003: 文档必须说明 ResultItem 的主要上层使用入口
- **关联 AC**: AC2（找到所有上层使用）
- **类型**: 正向/覆盖性
- **测试数据**: `python/maze.go`、`python/mobile_server2_main.go`、`python/mosd.go`、`txos/*.go`（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 检查文档是否至少覆盖以下上层模式：
     - 各模块内建立局部 `resultItems := make(map[uint64]*ResultItem)`
     - 通过闭包 `function := func(s uint64) *ResultItem { return GetResultItem(s, resultItems) }`
     - 扫描阶段调用 `AddToResultItems(...)`
     - 阶段结束调用 `MergeResultItems(resultItems)`
  2. 检查文档是否至少举出 Python 主流程和一个非 Python 模块（如 txos）的实际文件例子。
- **预期结果**:
  - 文档能够总结共性的“局部聚合 -> 全局合并”调用模式。
  - 文档至少给出 `python/maze.go` 和 `txos` 目录中的实际使用示例。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  files = ['python/maze.go', 'python/mobile_server2_main.go', 'python/mosd.go', 'txos/txos-aoi.go', 'txos/leak.go']
  hits = 0
  for f in files:
      text = Path(f).read_text()
      if 'GetResultItem(s, resultItems)' in text and 'MergeResultItems(resultItems)' in text:
          hits += 1
  assert hits >= 2, hits
  print('TC-003 upstream pattern verified, hits=', hits)
  PY
  ```

### TC-004: 文档必须区分运行态 common.ResultItem 与 JSON 输出结构
- **关联 AC**: AC3（编写介绍文档需准确描述上下游结构）
- **类型**: 边界/准确性
- **测试数据**: `common/result_items.go`、`common/utils.go`（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 阅读文档中关于“结果输出”或“最终展示”的章节。
  2. 检查文档是否明确说明：
     - `common/result_items.go` 中的 `ResultItem` 是运行态聚合结构；
     - `common/utils.go` 中存在用于 JSON 输出的局部结构（同名但不同语义）。
  3. 对照源码确认文档没有把两者混为一谈。
- **预期结果**:
  - 文档清晰区分运行时统计结构与序列化输出结构。
  - 文档指出 `common/utils.go` 的 JSON 输出结构字段如 `order/amount/total_size/avg_size/type/type_id`。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  r1 = Path('common/result_items.go').read_text()
  r2 = Path('common/utils.go').read_text()
  assert 'type ResultItem struct' in r1
  assert 'type ResultItem struct {' in r2
  assert 'json:"order"' in r2
  assert 'json:"type_id"' in r2
  print('TC-004 runtime vs json result structures verified')
  PY
  ```

### TC-005: 文档必须覆盖结果展示/消费链路
- **关联 AC**: AC2
- **类型**: 正向/完整性
- **测试数据**: `main.go`、`common/webui.go`、`common/utils.go`、`common/callback.go`（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 检查文档是否说明 `ResultItems` 不仅在采集阶段被写入，还会被多个展示/输出链路消费。
  2. 检查文档是否覆盖至少两种消费方式，例如：
     - text/JSON 输出；
     - web UI；
     - callback / topN 输出；
     - dump 到 LevelDB。
  3. 对照源码确认文档引用的位置真实存在。
- **预期结果**:
  - 文档能从“采集 -> 排序 -> 展示/导出”的角度说明上层使用，而不是只列扫描入口。
  - 文档给出消费链路对应代码文件。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  checks = {
      'main.go': 'SortByValue(ResultItems, SortBySize)',
      'common/webui.go': 'SortByValue(ResultItems, sortType)',
      'common/utils.go': 'SortByValue(ResultItems, SortBySize)',
      'common/callback.go': 'FormatResultType(order, s)',
  }
  for path, needle in checks.items():
      text = Path(path).read_text()
      assert needle in text, path
  print('TC-005 output consumption chain verified')
  PY
  ```

### TC-006: 文档必须识别并声明“所有上层使用”的边界范围
- **关联 AC**: AC2
- **类型**: 边界/范围定义
- **测试数据**: 全仓 `*.go`（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 检查文档是否说明“所有上层使用”的统计边界，例如：
     - 是否只统计当前主仓源码；
     - 是否排除 `sub-repos/lua-bak` 之类历史/备份目录；
     - 是否按“直接使用 ResultItem API”与“仅消费全局 ResultItems”分层描述。
  2. 对照代码搜索结果确认该边界定义合理。
- **预期结果**:
  - 文档不会模糊声称“全部覆盖”却不说明是否包含历史备份目录。
  - 文档能解释直接调用者与间接消费者的范围差异。
- **验证方式**: 手动代码检查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  assert Path('sub-repos/lua-bak/sorter.go').exists()
  assert Path('common/result_items.go').exists()
  print('TC-006 repository scope anchors verified')
  PY
  ```

### TC-007: 文档应提供可复查的代码定位信息
- **关联 AC**: AC3
- **类型**: 正向/可维护性
- **测试数据**: 新 dev log 文档本身 + 相关源码（现有）
- **前置条件**: 新的 dev log 已编写完成
- **测试步骤**:
  1. 检查文档是否按模块列出关键文件路径。
  2. 检查文档是否包含足够的函数名/结构名，便于读者通过 grep 复查。
  3. 检查文档是否避免纯口头总结而缺少源码锚点。
- **预期结果**:
  - 文档至少包含以下源码锚点中的大部分：
    `common/result_items.go`、`common/utils.go`、`python/maze.go`、`common/webui.go`、`main.go`、`txos/*`。
  - 读者可根据文档内容直接定位到实现代码。
- **验证方式**: 文档审查
- **验收命令**:
  ```bash
  python3 - <<'PY'
  from pathlib import Path
  required = [
      'common/result_items.go',
      'common/utils.go',
      'python/maze.go',
      'common/webui.go',
      'main.go',
  ]
  for path in required:
      assert Path(path).exists(), path
  print('TC-007 document anchor files exist')
  PY
  ```

## 备注

- 本 issue 明确说明 **QA 不需要参与测试**，因此以上 test case 主要用于**文档交付验收标准设计**，不是要求新增 `testdata/` 或运行 `run_test.py` 的功能测试。
- 本 issue 也明确说明 **Dev 不需要编写代码**，因此本次不设计“功能失败即预期”的 TDD 测试文件；重点是确保后续提交的分析文档具备准确性、范围完整性和可追溯性。


---
*Archived: [test-cases.md](https://github.com/prim/test-symphony-logs/blob/main/20260307-issue-30/test-cases.md)*


---


## Requirements Document

The requirements document is at `dev-log/plan.md`. Read it to understand the full specification.

## Steps

1. Read the latest dev report: `ls -t dev-log/*-dev-report.md | head -1 | xargs cat`.
2. Read the testcase report: `cat dev-log/test-cases.md`.
3. Read the requirements document: `cat dev-log/plan.md`.
4. Review all changes: `git diff origin/master...HEAD` (use `origin/main` if the default branch is `main`)
5. Run the test suite.
6. Check correctness against the requirements document, completeness, edge cases, and test coverage.
7. Write a QA report to `dev-log/$(date +%Y%m%d%H%M%S)-qa-report.md`:
   ```
   cat > "dev-log/$(date +%Y%m%d%H%M%S)-qa-report.md" << 'REPORT'
   ## QA Result: [PASS / FAIL]

   ## Findings
   - [Finding 1]

   ## Test Results
   [Summary]
   REPORT
   ```
8. **IMPORTANT**: The system reads the QA report to determine pass/fail. Make sure the report contains a clear status line like `## QA Result: PASS` or `## QA Result: FAIL`.

## Constraints

- Do NOT fix issues yourself when signaling failure — the fix agent handles that.
- If issues are minor, fix them, commit with `qa:` prefix, and signal pass.
- Do NOT commit `dev-log/` working files (`plan.md`, `test-cases.md`, `*-qa-report.md`).
---

## Symphony Task Protocol

1. Commit all your changes (`git add . && git commit`).
2. Do **NOT** run `git push` or modify any git remote.
3. Do **NOT** commit files under `dev-log/` (e.g. `plan.md`, `test-cases.md`, `*-report.md`).

</details>

## Timeline

### `02:46:25` Step 1

#### `02:46:25` Tool: `read` — dev-log/plan.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/dev-log/plan.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/dev-log/plan.md</path>
<type>file</type>
<content>1: # prim/maze#30: 分析 common ResultItem 及其上层使用
2: 
3: - [x] 找到 common ResultItem 以及所有上层使用，并编写新的 dev log 介绍。
4: 
5: - QA 不需要参与测试
6: - Dev 不需要编写代码
7: - Dev 需要编写文档（分析报告）

(End of file - total 7 lines)
</content>
```
</details>

#### `02:46:25` Tool: `read` — dev-log/test-cases.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/dev-log/test-cases.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/dev-log/test-cases.md</path>
<type>file</type>
<content>1: # Test Cases
2: 
3: ## 关联计划
4: Feature: prim/maze#30 common ResultItem 及其上层使用分析文档
5: 
6: ## 测试策略说明
7: 
8: 本 issue 的交付物是 **dev log / 分析文档**，不是功能实现代码，也不要求 QA 执行运行时验收。因此这里设计的是**文档验收 test case**，重点验证：
9: 
10: 1. 文档是否准确定位 `common.ResultItem` 的定义与职责
11: 2. 文档是否覆盖主要上层调用链与输出链路
12: 3. 文档是否区分运行态聚合结构与最终 JSON/text 输出结构
13: 4. 文档是否给出足够的代码位置，便于开发者和后续维护者追踪
14: 
15: ## Test Case 列表
16: 
17: ### TC-001: 文档必须准确定位 common.ResultItem 的定义
18: - **关联 AC**: AC1（找到 common ResultItem）
19: - **类型**: 正向/文档准确性
20: - **测试数据**: 代码文件 `common/result_items.go`（现有）
21: - **前置条件**:
22:   - 已生成开发文档（新的 dev log）
23:   - 仓库工作区可读取源码
24: - **测试步骤**:
25:   1. 打开新的 dev log 文档。
26:   2. 检查文档是否明确指出 `common.ResultItem` 定义位于 `common/result_items.go`。
27:   3. 检查文档是否说明其核心字段：`Amount`、`Sizeof`、`Objects`、`ObjSize`。
28:   4. 对照源码确认字段和文件路径描述无误。
29: - **预期结果**:
30:   - 文档准确给出 `common/result_items.go` 中的 `ResultItem` 定义位置。
31:   - 文档对字段职责的描述与源码一致，无张冠李戴。
32: - **验证方式**: 手动代码检查
33: - **验收命令**:
34:   ```bash
35:   python3 - <<'PY'
36:   from pathlib import Path
37:   text = Path('common/result_items.go').read_text()
38:   assert 'type ResultItem struct' in text
39:   assert 'Amount  uint64' in text
40:   assert 'Sizeof  uint64' in text
41:   assert 'Objects PriorityQueue' in text
42:   assert 'ObjSize map[uint64]uint64' in text
43:   print('TC-001 source anchor verified')
44:   PY
45:   ```
46: 
47: ### TC-002: 文档必须覆盖 ResultItem 的核心操作函数
48: - **关联 AC**: AC1
49: - **类型**: 正向/文档完整性
50: - **测试数据**: `common/result_items.go`（现有）
51: - **前置条件**: 新的 dev log 已编写完成
52: - **测试步骤**:
53:   1. 阅读文档中关于 ResultItem 生命周期/操作流程的部分。
54:   2. 检查是否覆盖以下函数：`GetResultItem`、`AddToResultItems`、`MergeResultItems`、`DumpResultItems`。
55:   3. 对照源码确认文档对这些函数职责的说明是否正确。
56: - **预期结果**:
57:   - 文档明确说明 ResultItem 的创建/获取、累计、合并、落盘/输出等关键步骤。
58:   - 没有遗漏核心函数，也没有把无关函数误写成主链路。
59: - **验证方式**: 手动代码检查
60: - **验收命令**:
61:   ```bash
62:   python3 - <<'PY'
63:   from p
... (truncated, 7654 chars total)
```
</details>

#### `02:46:25` Tool: `read` — dev-log/dev-report.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/dev-log/dev-report.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/dev-log/dev-report.md</path>
<type>file</type>
<content>1: # 开发报告
2: 
3: ## 完成的任务
4: - [x] Task 1: 根据 QA 反馈补充 `python/results.go` 与 `txos/results.go` 两条实际的 `ResultItems` 消费链路，并说明它们对 `common/callback.go` 默认实现的覆盖关系。
5: - [x] Task 2: 补充 Node.js 对 `ResultItem` 的函数级写入锚点，明确 `nodejs/maze.go` fallback 扫描与 `nodejs/scan.go` HeapWalk 扫描两条直接调用链。
6: - [x] Task 3: 更新 dev log 的复查锚点与总结，统一“直接调用者 / 间接消费者 / 运行时覆盖实现”的覆盖口径。
7: 
8: ## 修改的文件
9: - `dev-log/2026-03-07-common-resultitem-analysis.md`: 补充 Python/txos 的运行时 `SamplePostmanObjects` 覆盖链、`ResultItems[s]` / `item.Objects` / `ObjSize` 读取路径，并细化 Node.js 的函数级直接调用锚点。
10: - `dev-log/dev-report.md`: 记录本轮 QA 修复范围、修改点与自检结果。
11: 
12: ## 自测结果
13: - 测试命令: `python3` 自检脚本，校验文档已覆盖 `python/results.go`、`python/maze.go`、`txos/results.go`、`txos/maze.go`、`nodejs/maze.go`、`nodejs/scan.go` 等 QA 指定锚点。
14: - 测试结果: 通过。本 issue 为文档修订，不涉及功能代码编译、运行时分析或测试文件修改。
15: 
16: ## Self-Review 发现与修复
17: - 发现上一版文档只把 `common/callback.go` 作为 callback 消费链代表，没有继续跟踪 `SamplePostmanObjects` 在 Python / txos 初始化阶段会被模块私有实现覆盖；本轮已把“默认实现 vs 运行时覆盖实现”的关系单列说明。
18: - 发现上一版对 Node.js 只写到了文件级别，没有把 `nodejs/maze.go:Main() -> AddResult(do) -> main(function) -> V(...) -> AddToResultItems(...)` 与 `nodejs/scan.go` HeapWalk 写入路径拆到函数级；本轮已补齐。
19: 
20: ## 注意事项
21: - 本轮严格只修正文档，未修改业务源码、测试文件或 `dev-log/test-cases.md`。
22: - `common/callback.go` 中的 `SamplePostmanObjects` 不能简单视为唯一实现；阅读 callback/postman 链路时，应优先检查目标模块是否在初始化阶段重绑了该函数指针。

(End of file - total 22 lines)
</content>
```
</details>

#### `02:46:25` Tool: `glob` — dev-log (completed)

Pattern: `*-common-resultitem-analysis.md`

```
/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md
```

*Step finished: reason=tool-calls, tokens=20,057*

### `02:46:30` Step 2

#### `02:46:38` Tool: `read` — dev-log/2026-03-07-common-resultitem-analysis.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md</path>
<type>file</type>
<content>1: # common.ResultItem 及其上层使用分析
2: 
3: **日期**: 2026-03-07  
4: **任务**: 梳理 `common.ResultItem` 的定义、核心操作函数、主要上层调用链，以及结果展示/导出链路。
5: 
6: ## 背景
7: 
8: 本 issue 不要求修改功能代码，而是要把 Maze 当前的结果聚合主链路讲清楚：
9: 
10: 1. `common.ResultItem` 在哪里定义；
11: 2. 它如何在扫描阶段被创建、累计、合并；
12: 3. 哪些上层模块直接调用它的 API；
13: 4. 哪些模块虽然不直接创建 `ResultItem`，但会消费全局 `ResultItems` 做 text / JSON / Web UI / callback / dump 输出。
14: 
15: 这份文档只统计**当前主仓源码**中的 Go 实现，不把 `sub-repos/lua-bak/` 这类历史/备份目录算入“当前上层使用”的正式范围；但会在范围章节明确说明该目录中存在一套旧版同名实现，避免误判“全仓唯一”。
16: 
17: ## 1. 定义位置与结构职责
18: 
19: `common.ResultItem` 定义在 `common/result_items.go`：
20: 
21: ```go
22: type ResultItem struct {
23:     Amount  uint64
24:     Sizeof  uint64
25:     Objects PriorityQueue
26:     ObjSize map[uint64]uint64
27: }
28: ```
29: 
30: 字段职责如下：
31: 
32: - `Amount`：该分类下累计对象数量；
33: - `Sizeof`：该分类累计总内存大小；
34: - `Objects`：保留一组对象地址，用于后续 Web UI / callback / 二级对象展示；
35: - `ObjSize`：对象地址到对象大小的映射，主要用于 `EnablePyMerge` 场景下维护优先队列中的“大对象样本”。
36: 
37: 相关配套定义也都在 `common/result_items.go`：
38: 
39: - 全局聚合表：`var ResultItems = make(map[uint64]*ResultItem)`
40: - 运行态对象池：`ResultItemPool`
41: - 二级对象排序容器：`type PriorityQueue []uint64`
42: - 合并时使用的大小索引：`CurObjSize`
43: 
44: 因此，`common/result_items.go` 里的 `ResultItem` 是**运行态聚合结构**，服务于分析阶段和中间结果保留，不是直接对外输出的 JSON DTO。
45: 
46: ## 2. 核心操作函数
47: 
48: `common/result_items.go` 中围绕 `ResultItem` 的核心函数有 4 个，构成最主要生命周期。
49: 
50: ### 2.1 `GetResultItem`
51: 
52: 代码位置：`common/result_items.go`
53: 
54: 职责：
55: 
56: - 从调用方传入的局部 `map[uint64]*ResultItem` 中按分类 ID 取结果项；
57: - 如果不存在，则从 `ResultItemPool` 取一个空对象并放入 map；
58: - 返回给上层继续累计。
59: 
60: 这意味着大多数调用方不会直接 `&ResultItem{}`，而是统一走：
61: 
62: ```go
63: resultItems := make(map[uint64]*ResultItem)
64: function := func(s uint64) *ResultItem {
65:     return GetResultItem(s, resultItems)
66: }
67: ```
68: 
69: ### 2.2 `AddToResultItems`
70: 
71: 代码位置：`common/result_items.go`
72: 
73:
... (truncated, 12834 chars total)
```
</details>

#### `02:46:38` Tool: `read` — python/results.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/python/results.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/python/results.go</path>
<type>file</type>
<content>1: package python
2: 
3: import (
4: 	. "maze/common"
5: 	"regexp"
6: 
7: 	"fmt"
8: 	"math/rand"
9: 	"path"
10: 	"path/filepath"
11: 	"runtime"
12: 	"sort"
13: 	"strings"
14: 	"sync"
15: 
16: 	// "io/ioutil"
17: 	"time"
18: 
19: 	"github.com/panjf2000/ants/v2"
20: )
21: 
22: func maxStringObjectsAmount(os []uint64, maxAmount int) int {
23: 	if maxAmount < len(os)/2 {
24: 		maxAmount = len(os) / 2
25: 	}
26: 	m := 480000
27: 	if maxAmount < m {
28: 		return maxAmount
29: 	}
30: 	return m
31: }
32: 
33: func printObjectsNormal(order string, l []interface{}, os []uint64, maxAmount int) []interface{} {
34: 	pyt := PyTypeAddress(os[0])
35: 
36: 	do := true
37: 	if do && pyt == pytype_unicode {
38: 		return printObjectsString(l, os, maxAmount, stringOfPyUnicodeObject, false)
39: 	} else if do && pyt == pytype_str {
40: 		return printObjectsString(l, os, maxAmount, bytesOfPyStringObject, false)
41: 	} else {
42: 		return printObjectsOthers(l, os, maxAmount)
43: 	}
44: }
45: 
46: func printObjectsString(l []interface{}, os []uint64, maxAmount int, vf func(uint64) string, isPostman bool) []interface{} {
47: 	maxAmount = maxStringObjectsAmount(os, maxAmount)
48: 	stat := make(map[string]int)
49: 	pyos := make(map[string]uint64)
50: 	count := 0
51: 	for _, pyo := range os[:maxAmount] {
52: 		count += 1
53: 		if count%1000 == 0 {
54: 			// fmt.Println("printObjectsString", count)
55: 		}
56: 		value := vf(pyo)
57: 		stat[value] += 1
58: 		if stat[value] == 1 {
59: 			pyos[value] = pyo
60: 		}
61: 	}
62: 	for i, node := range SortedStringIntMap(stat) {
63: 		if i > 100 {
64: 			break
65: 		}
66: 		value := node.Key
67: 		counter := node.Value
68: 
69: 		one := map[string]interface{}{
70: 			"id":   fmt.Sprintf("0x%x", pyos[value]),
71: 			"repr": value,
72: 		}
73: 		if isPostman {
74: 			one["percent"] = float64(counter) / float64(maxAmount)
75: 		} else {
76: 			one["percent"] = fm
... (truncated, 7539 chars total)
```
</details>

#### `02:46:38` Tool: `read` — txos/results.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/txos/results.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/txos/results.go</path>
<type>file</type>
<content>1: package txos
2: 
3: import (
4: 	. "maze/common"
5: 
6: 	"container/heap"
7: 	"fmt"
8: 	"math/rand"
9: 	"path"
10: 	"path/filepath"
11: 	"sort"
12: 	"strings"
13: 	"time"
14: )
15: 
16: func maxStringObjectsAmount(os []uint64, maxAmount int) int {
17: 	if maxAmount < len(os)/2 {
18: 		maxAmount = len(os) / 2
19: 	}
20: 	m := 48 * 10000
21: 	if maxAmount < m {
22: 		return maxAmount
23: 	}
24: 	return m
25: }
26: 
27: // GetCachedCstr 会调用这个函数
28: func stringOfPyStringObject(ptr_u uint64) string {
29: 	return Cstr(ptr_u)
30: }
31: 
32: func bytesOfPyStringObject(var_ uint64) string {
33: 	s := Cstr(var_)
34: 	// fmt.Println("bytesOfPyStringObject", pyo, s)
35: 	return s
36: }
37: 
38: // 输出 webui 二级表格
39: func printObjectsNormal(order string, l []interface{}, os []uint64, maxAmount int) []interface{} {
40: 	pyt := lpcValueType(os[0])
41: 	fmt.Println("printObjectsNormal", pyt)
42: 
43: 	if EnablePyMerge {
44: 		pq := make(PriorityQueue, 0)
45: 
46: 		CurObjSize = ResultItems[GlobalS].ObjSize
47: 		for _, obj := range os {
48: 			heap.Push(&pq, obj)
49: 		}
50: 
51: 		rl := make([]uint64, 0)
52: 		for len(pq) > 0 {
53: 			rl = append(rl, heap.Pop(&pq).(uint64))
54: 		}
55: 
56: 		for i := len(rl) - 1; i >= 0; i-- {
57: 			pyo := rl[i]
58: 			one := map[string]interface{}{
59: 				"id":   fmt.Sprintf("0x%x", pyo),
60: 				"repr": "repr",
61: 			}
62: 			one["percent"] = fmt.Sprintf("%d", CurObjSize[pyo])
63: 			l = append(l, one)
64: 		}
65: 		return l
66: 	}
67: 
68: 	if pyt == T_STRING || pyt == CUSTOM_POOL_STRING {
69: 		// fmt.Println("printObjectsString", os)
70: 		return printObjectsString(order, l, os, maxAmount, bytesOfPyStringObject, false)
71: 	} else if pyt == TXOS_PROGRAM {
72: 		return printProgram(order, l, os, maxAmount, false)
73: 	} else {
74: 		return printObjectsOthers(order, l, os, maxAmount)
75: 	}
76: }
77: 
78: func printProgram(order string, l []inte
... (truncated, 9502 chars total)
```
</details>

#### `02:46:38` Tool: `read` — nodejs/maze.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/nodejs/maze.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/nodejs/maze.go</path>
<type>file</type>
<content>1: package nodejs
2: 
3: import (
4: 	"bytes"
5: 	"errors"
6: 	"fmt"
7: 	"io/ioutil"
8: 	"sort"
9: 	"strings"
10: 	"sync"
11: 
12: 	. "maze/common"
13: 	// . "maze/mallocer"
14: )
15: 
16: var seenMutex sync.Mutex
17: 
18: var Stats = make(map[string][]uint64)
19: var SizeStats = make(map[string]uint64) // key: type_name, value: total size in bytes
20: 
21: // String 长度分类的 class ID
22: // 使用 CustomClassifyTypeNodejsBegin 范围内的固定值
23: const (
24: 	StringClass_0_64   uint64 = CustomClassifyTypeNodejsBegin + 1
25: 	StringClass_65_256 uint64 = CustomClassifyTypeNodejsBegin + 2
26: 	StringClass_257_1K uint64 = CustomClassifyTypeNodejsBegin + 3
27: 	StringClass_1K_4K  uint64 = CustomClassifyTypeNodejsBegin + 4
28: 	StringClass_Over4K uint64 = CustomClassifyTypeNodejsBegin + 5
29: )
30: 
31: // StringClassNames 映射 String class ID 到显示名称
32: var StringClassNames = map[uint64]string{
33: 	StringClass_0_64:   "(String) [0-64]",
34: 	StringClass_65_256: "(String) [65-256]",
35: 	StringClass_257_1K: "(String) [257-1K]",
36: 	StringClass_1K_4K:  "(String) [1K-4K]",
37: 	StringClass_Over4K: "(String) [>4K]",
38: }
39: 
40: // JSFunction class ID 分配
41: // 每个唯一的 classify key 对应一个唯一的 class ID
42: // 这样同名函数合并统计，不同名函数分开统计
43: // displayName 用于最终展示，从第一个遇到的函数获取
44: var (
45: 	jsFunctionClassNext  uint64 = CustomClassifyTypeNodejsBegin + 100 // 从 100 开始，避免与 StringClass 冲突
46: 	JSFunctionClassMap          = make(map[string]uint64)             // classify_key -> class_id
47: 	JSFunctionClassNames        = make(map[uint64]string)             // class_id -> display_name
48: )
49: 
50: // GetJSFunctionClass 根据 JSFunction 的 classify key 返回对应的 class ID
51: // classifyKey 用于分类（相同 key 归为一类）
52: // displayName 用于展示（优先选择有实际内容的）
53: func GetJSFunctionClass(classifyKey, displayName string) uint64 {
54: 	if classId, ok := JSFunctionClassMap[classifyKey]; ok {
55: 		if isBetterDisplayName(displayName, JSFunctio
... (truncated, 8056 chars total)
```
</details>

#### `02:46:38` Tool: `read` — nodejs/scan.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/nodejs/scan.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/nodejs/scan.go</path>
<type>file</type>
<content>1: package nodejs
2: 
3: // V8 Heap 扫描实现
4: // 支持两种模式：
5: // 1. Heap Walk 模式：使用 Isolate -> Heap -> Space -> Chunk -> Object 结构化遍历
6: // 2. Memory Scan 模式：扫描所有内存段查找 V8 对象（降级方案）
7: 
8: import (
9: 	"bytes"
10: 	"fmt"
11: 	"io/ioutil"
12: 	"sort"
13: 
14: 	. "maze/common"
15: )
16: 
17: type jsFunctionDebugInfo struct {
18: 	Addr         uint64
19: 	FunctionKind uint8
20: 	Name         string
21: 	DisplayName  string
22: }
23: 
24: var jsFunctionDebugList []jsFunctionDebugInfo
25: 
26: // AllHeapObjectAddrs 收集所有扫描到的 HeapObject 地址
27: // 用于对比 HeapWalk 和 MemScan 的结果
28: var AllHeapObjectAddrs []uint64
29: 
30: // HeapObjectInfo 存储 HeapObject 的详细信息，用于与 V8 调试输出对比
31: type HeapObjectInfo struct {
32: 	Addr         uint64 // 对象地址（不带 tag）
33: 	InstanceType uint16 // V8 InstanceType
34: 	Size         uint64 // 对象大小（字节）
35: }
36: 
37: // AllHeapObjects 收集所有扫描到的 HeapObject 详细信息
38: var AllHeapObjects []HeapObjectInfo
39: 
40: // ResetHeapObjectAddrs 重置地址收集
41: func ResetHeapObjectAddrs() {
42: 	AllHeapObjectAddrs = nil
43: 	AllHeapObjects = nil
44: }
45: 
46: // RecordHeapObjectAddr 记录一个 HeapObject 地址
47: func RecordHeapObjectAddr(addr uint64) {
48: 	AllHeapObjectAddrs = append(AllHeapObjectAddrs, addr)
49: }
50: 
51: // RecordHeapObjectInfo 记录一个 HeapObject 的完整信息
52: func RecordHeapObjectInfo(addr uint64, instType uint16, size uint64) {
53: 	AllHeapObjects = append(AllHeapObjects, HeapObjectInfo{
54: 		Addr:         addr,
55: 		InstanceType: instType,
56: 		Size:         size,
57: 	})
58: }
59: 
60: // DumpAllHeapObjectAddrs 导出所有收集到的地址到文件
61: func DumpAllHeapObjectAddrs(suffix string) {
62: 	if len(AllHeapObjectAddrs) == 0 {
63: 		// Result.Printf("[dump] No heap object addresses to dump\n")
64: 		return
65: 	}
66: 
67: 	// 排序
68: 	sorted := make([]uint64, len(AllHeapObjectAddrs))
69: 	copy(sorted, AllHeapObjectAddrs)
70: 	sort.Slice(sorted, func(i, j int) bool { return sorted[i] < sort
... (truncated, 8819 chars total)
```
</details>

#### `02:46:38` Tool: `read` — common/callback.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/common/callback.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/common/callback.go</path>
<type>file</type>
<content>1: package common
2: 
3: import (
4: 	"fmt"
5: 	"math/rand"
6: 	"path"
7: 	"path/filepath"
8: 	"strings"
9: 	"sync"
10: 	"time"
11: 
12: 	"github.com/panjf2000/ants/v2"
13: )
14: 
15: // 将 main.go 的函数透传给 postman package
16: var (
17: 	Maze     func()
18: 	MazeHttp func()
19: )
20: 
21: // vm_map.go dmap 对包暴露的 find 命令功能
22: // TODO 不这么写
23: var (
24: 	SearchMemoryMapG func(uint64, uint64, uint64, bool, bool) string
25: 	DebugMemoryMapG  func()
26: )
27: 
28: // 现在只有 vm_map.go 用来显示
29: var (
30: 	TypeNameG func(uint64) string
31: )
32: 
33: var (
34: 	// 格式化显示单个内存块(对象)
35: 	FormatFunction func(addr uint64) string
36: 
37: 	// TODO sizetree
38: 	SizeOfTreeString func(uint64, bool) (string, []byte)
39: 
40: 	// 输出引用图
41: 	ShowGraphG func(uint64, int, map[string]int, bool) (string, string, []byte)
42: 
43: 	// stringify 单个对象
44: 	StringifyObjectG func(uint64) string
45: 
46: 	// webui 输出 2级 表格的数据
47: 	PrintObjectsNormal func(order string, l []interface{}, os []uint64, maxAmount int) []interface{}
48: 
49: 	// postman 输出 2级 表格的数据
50: 	SamplePostmanObjects func(M, N int) []interface{} = samplePostmanObjects
51: 
52: 	// 格式化一级表格中单个行的名字
53: 	FormatResultType func(order int, t uint64) string = formatResultType
54: )
55: 
56: func formatResultType(order int, t uint64) string {
57: 	if t >= MinPieceType && t < NextPieceType {
58: 		return FormatPieceTypeName(t)
59: 	} else if t >= MinVtableType && t < NextVtableType {
60: 		return FormatVtableTypeName(t)
61: 	} else {
62: 		return "..."
63: 	}
64: }
65: 
66: // 输出给 postman web 显示的所有二级列表的结果
67: // TODO 删除这个函数?
68: func samplePostmanObjects(M, N int) []interface{} {
69: 	// TODO
70: 	PoolSize := 16
71: 	D.Println("samplePostmanObjects size ", PoolSize)
72: 	Debug.Println("samplePostmanObjects size ", PoolSize)
73: 	p, _ := ants.NewPoolWithFunc(PoolSize, func(i interface{}) {
74: 		f, _ := i.(func())
75: 		f()
76: 		// }, ants.WithNon
... (truncated, 5550 chars total)
```
</details>

#### `02:46:38` Tool: `read` — python/maze.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/python/maze.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/python/maze.go</path>
<type>file</type>
<content>240: 		}
241: 	}
242: 	if MazeProfileTimes == 2 {
243: 		AllPyTypes_.Release()
244: 	}
245: }
246: 
247: func writeDebugData() {
248: 	Debug.Printf("writeDebugData")
249: 
250: 	// ri := ResultItems[pytype_unicode]
251: 	// Result.Println("----------", ri)
252: 	// for _, pyo := range ri.Objects {
253: 	// 	Result.Printf("writeDebugData %x\n", pyo)
254: 	// 	ShowGraph(pyo, S.Result.MaxDepth, true)
255: 	// }
256: 
257: 	maxAmount := 30000 // TODO
258: 
259: 	sortType := SortBySize
260: 	PrintSortedResults(sortType, 100, false)
261: 	for order := 1; order < 100+1; order++ {
262: 		// if ordr <= 2 || order >= 5 {
263: 		//     continue
264: 		// }
265: 		if order != 4 {
266: 			continue
267: 		}
268: 		s, _ := SortedResultMap[order-1]
269: 		item, _ := ResultItems[s]
270: 		os := item.Objects
271: 		n := len(os)
272: 		if n < maxAmount {
273: 			maxAmount = n
274: 		}
275: 		for _, v := range os[:maxAmount] {
276: 			TimedThen(fmt.Sprintf("-- %d %x", order, v), func() {
277: 				ShowGraph(v, S.Result.MaxDepth, nil, true)
278: 			}, 1)
279: 		}
280: 	}
281: }
282: 
283: func initMazeCallback() {
284: 	sizeof_double, sizeof_pointer, sizeof_int, sizeof_long, sizeof_char = BasicSizeof()
285: 
286: 	FormatResultType = FormatPythonResultType
287: 	FormatFunction = formatPyObject
288: 	PrintObjectsNormal = printObjectsNormal
289: 	SamplePostmanObjects = samplePostmanObjects
290: 	SizeOfTreeString = SizeofTree
291: 	ShowGraphG = ShowGraph
292: 	TypeNameG = TypeName
293: }

(End of file - total 293 lines)
</content>
```
</details>

#### `02:46:38` Tool: `read` — txos/maze.go (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/txos/maze.go`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/txos/maze.go</path>
<type>file</type>
<content>220: 
221: 	Timed(CheckBatchResultType)
222: 
223: 	Timed(MergeUnknowMemoryPiece)
224: 
225: 	Timed(SaveRefIndex)
226: 	Timed(CheckRefs)
227: }
228: 
229: func Test() {
230: 	do := true
231: 	do = false
232: 	if !do {
233: 		return
234: 	}
235: 	Timed(getAllUDBTable)
236: 	// Timed(getAllObject)
237: 	// printMapping(0x7fc2d77f2820)
238: 	panic("txos.Test() stop")
239: }
240: 
241: func InitTxos() {
242: 	Debug.Println("initTxos")
243: 
244: 	initOffsets()
245: 	initTxosClassify()
246: 	initTxosTypeEmptyNames()
247: 	// initTxosDB()
248: 	initTxosRDB()
249: 
250: 	the_null_array = S.Symbols["the_null_array"]
251: 	null_buf = S.Symbols["null_buf"]
252: 	globalAtomTable = Pointer(S.Symbols["globalAtomTable"] + S.Structs["AtomTable"]["pString"])
253: 
254: 	FormatResultType = FormatTxosResultType
255: 	FormatFunction = FormatTxosObject
256: 	PrintObjectsNormal = printObjectsNormal
257: 	ShowGraphG = ShowGraph
258: 	SizeOfTreeString = SizeOfTree
259: 	SamplePostmanObjects = samplePostmanObjects
260: 	StringOfPyObject = stringOfPyStringObject
261: 	LpcGetResultObjTypeMapG = GetResultObjTypeMap
262: 	TypeNameG = TypeName
263: 
264: 	Timed(addIgnoreSymbols)
265: 	Timed(initTx2Obj)
266: 
267: 	Timed(SetRefFunc)
268: }
269: 
270: func initTx2Obj() {
271: 	if Project == "g18" {
272: 		item_tx2obj_amount = 2
273: 		npc_tx2obj_amount = 3
274: 	} else if Project == "mhxy" || Project == "ma100" || Project == "dh2" {
275: 		item_tx2obj_amount = 1
276: 		npc_tx2obj_amount = 2
277: 	}
278: }
279: 
280: func initTxosClassify() {
281: 	SeenMap.Init()
282: 	SeenTemp.Init()
283: 	SROTM.Init()
284: 	SROTM1.Init()
285: 
286: 	// ClassifyFunctionMap[T_NUMBER] = classifyNumberObject
287: 	ClassifyFunctionMap[T_FUNCTION] = classifyFunctionObject
288: 	ClassifyFunctionMap[T_STRING] = classifyStringObject
289: 	ClassifyFunctionMap[T_ARRAY] = classifyArrayObject
290: 	ClassifyFunctionMap[T_OBJECT] = clas
... (truncated, 8713 chars total)
```
</details>

#### `02:46:38` Tool: `bash` — Shows diff for updated dev logs (completed)

```bash
git diff origin/master...HEAD -- dev-log/2026-03-07-common-resultitem-analysis.md dev-log/dev-report.md
```

#### `02:46:39` Tool: `bash` — Checks git status and base branch (completed)

```bash
git status --short && git branch --show-current && git remote show origin | sed -n 's/.*HEAD branch: //p'
```

<details><summary>Output</summary>

```
?? .symphony_prompt.md
?? dev-log/2026-03-07-common-resultitem-analysis.md
?? dev-log/20260307104115-dev-report.md
?? dev-log/20260307104318-qa-report.md
?? dev-log/dev-report.md
?? dev-log/plan.md
?? dev-log/test-cases.md
symphony/30

```
</details>

*Step finished: reason=tool-calls, tokens=24,714*

### `02:47:02` Step 3

#### `02:47:02` Tool: `read` — dev-log/2026-03-07-common-resultitem-analysis.md (completed)

Read: `/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md`

<details><summary>Output</summary>

```
<path>/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md</path>
<type>file</type>
<content>401: |------|--------|
402: | `common/result_items.go` | `ResultItem` 定义、`GetResultItem`、`AddToResultItems`、`MergeResultItems`、`DumpResultItems`、`AddResult` |
403: | `common/utils.go` | JSON 输出局部 `ResultItem`、`SortByValue(ResultItems, SortBySize)` |
404: | `common/webui.go` | `SortByValue`、`PrintSortedResults`、`PrintObjects` |
405: | `common/callback.go` | 默认 `SamplePostmanObjects`、`FormatResultType(order, s)`、`ResultItems[s]`、`item.Objects` |
406: | `main.go` | `printTextModeResults()` 中的 text 输出链路 |
407: | `python/maze.go` | Python 主扫描中的局部聚合 -> 全局合并 |
408: | `python/results.go` | Python 运行时覆盖版 `samplePostmanObjects()`，直接读取 `ResultItems[s]` / `item.Objects` |
409: | `python/mobile_server2_main.go` | MB2 C++ 分类阶段的局部聚合 |
410: | `python/mosd.go` | 串行/并行版本的两种典型聚合写法 |
411: | `txos/maze.go` | `InitTxos()` 中 `SamplePostmanObjects = samplePostmanObjects` 的覆盖挂接 |
412: | `txos/results.go` | txos 运行时覆盖版 `samplePostmanObjects()`，以及 merge 模式下的 `ResultItems[s].ObjSize` 消费 |
413: | `txos/txos-aoi.go` | 非 Python 场景的标准写法 |
414: | `txos/leak.go` | 页扫描型的标准写法 |
415: | `txos/merge.go` | `tryMergeAddToResultItems` 与 merge 模式边界 |
416: | `mallocer/ptmalloc/ptmalloc.go` | 并发分配器扫描时的局部聚合与汇总 |
417: 
418: ## 9. 总结
419: 
420: 可以把 `common.ResultItem` 理解成 Maze 分析引擎里“按类型聚合内存结果”的核心运行态单元：
421: 
422: 1. 各分析模块先在局部 `map[uint64]*ResultItem` 中累计；
423: 2. 通过 `AddToResultItems` 写入对象数量、总大小和样本对象；
424: 3. 通过 `MergeResultItems` 汇总到全局 `ResultItems`；
425: 4. 最终再由 `main.go`、`common/utils.go`、`common/webui.go`、`common/callback.go` 默认实现，以及 Python / txos 自己覆盖的 `samplePostmanObjects()`、`DumpResultItems` 等链路消费。
426: 
427: 因此，`ResultItem` 的“上层使用”既包括扫描/分类阶段的直接写入者，也包括排序、展示、抽样和落盘阶段的间接消费者。并且在 callback / postman 这条消费链里，还要继续区分“`common/callback.go` 默认版本”和“Python / txos 在初始化阶段覆盖后的运行时版本”；只有把这两层都纳入，才算完整理解它在 Maze 中的角色。

(End of file - total 427 lines)
</content>
```
</details>

#### `02:47:02` Tool: `grep` — AddToResultItems\( (completed)

Pattern: `AddToResultItems\(`

<details><summary>Output</summary>

```
Found 2 matches
/home/hcjn0770/temp/worktrees/issue-30/nodejs/maze.go:
  Line 1080: 		AddToResultItems(class, sizeof, pyo, pyt, addToObjects, function, chunkSize)

/home/hcjn0770/temp/worktrees/issue-30/nodejs/scan.go:
  Line 457: 	AddToResultItems(class, sizeof, pyo, pyt, addToObjects, function, chunkSize)
```
</details>

#### `02:47:02` Tool: `grep` — python/results\.go|txos/results\.go|nodejs/maze\.go|nodejs/scan\.go|SamplePostmanObjects|ObjSize (completed)

Pattern: `python/results\.go|txos/results\.go|nodejs/maze\.go|nodejs/scan\.go|SamplePostmanObjects|ObjSize`

<details><summary>Output</summary>

```
Found 95 matches
/home/hcjn0770/temp/worktrees/issue-30/dev-log/dev-report.md:
  Line 4: - [x] Task 1: 根据 QA 反馈补充 `python/results.go` 与 `txos/results.go` 两条实际的 `ResultItems` 消费链路，并说明它们对 `common/callback.go` 默认实现的覆盖关系。
  Line 5: - [x] Task 2: 补充 Node.js 对 `ResultItem` 的函数级写入锚点，明确 `nodejs/maze.go` fallback 扫描与 `nodejs/scan.go` HeapWalk 扫描两条直接调用链。
  Line 9: - `dev-log/2026-03-07-common-resultitem-analysis.md`: 补充 Python/txos 的运行时 `SamplePostmanObjects` 覆盖链、`ResultItems[s]` / `item.Objects` / `ObjSize` 读取路径，并细化 Node.js 的函数级直接调用锚点。
  Line 13: - 测试命令: `python3` 自检脚本，校验文档已覆盖 `python/results.go`、`python/maze.go`、`txos/results.go`、`txos/maze.go`、`nodejs/maze.go`、`nodejs/scan.go` 等 QA 指定锚点。
  Line 17: - 发现上一版文档只把 `common/callback.go` 作为 callback 消费链代表，没有继续跟踪 `SamplePostmanObjects` 在 Python / txos 初始化阶段会被模块私有实现覆盖；本轮已把“默认实现 vs 运行时覆盖实现”的关系单列说明。
  Line 18: - 发现上一版对 Node.js 只写到了文件级别，没有把 `nodejs/maze.go:Main() -> AddResult(do) -> main(function) -> V(...) -> AddToResultItems(...)` 与 `nodejs/scan.go` HeapWalk 写入路径拆到函数级；本轮已补齐。
  Line 22: - `common/callback.go` 中的 `SamplePostmanObjects` 不能简单视为唯一实现；阅读 callback/postman 链路时，应优先检查目标模块是否在初始化阶段重绑了该函数指针。

/home/hcjn0770/temp/worktrees/issue-30/dev-log/2026-03-07-common-resultitem-analysis.md:
  Line 26:     ObjSize map[uint64]uint64
  Line 35: - `ObjSize`：对象地址到对象大小的映射，主要用于 `EnablePyMerge` 场景下维护优先队列中的“大对象样本”。
  Line 42: - 合并时使用的大小索引：`CurObjSize`
  Line 78: - 在 `EnablePyMerge` 模式下，用 `ObjSize` + `PriorityQueue` 维护保留对象样本；
  Line 81: 该函数是扫描/分类阶段最常见的统计入口。`python/pyobject.go`、`nodejs/maze.go`、`nodejs/scan.go`、`mallocer/ptmalloc/ptmalloc.go` 间接调用链、`txos/*.go` 等模块都会走到它。
  Line 236: - `nodejs/maze.go`：`Main()` 里通过 `AddResult(do)` 建立标准的“局部聚合 -> 全局合并”框架，实际扫描入口是 `main(function)`；在该文件的 `V(location, raw, function)` 中，完成 `class` / `sizeof` 计算后直接执行 `AddToResultItems(class, sizeof, pyo, pyt, addToObjects, function, chunkSize)`；
  Line 237: - `nodejs/scan.go`：HeapWalk 路径中的对象扫描函数也会在完成实例大小与 backing store 大小计算后，调用 `AddToResultItems(class, sizeof, pyo, pyt, addToOb
... (truncated, 10619 chars total)
```
</details>

#### `02:47:02` Tool: `grep` — ObjSize|samplePostmanObjects|ResultItems\[s\] (completed)

Pattern: `ObjSize|samplePostmanObjects|ResultItems\[s\]`

<details><summary>Output</summary>

```
Found 16 matches
/home/hcjn0770/temp/worktrees/issue-30/txos/x-rocksdb.go:
  Line 19: func GetObjSize0(obj uint64) (uint64, bool) {
  Line 24: func GetObjSize(obj uint64) uint64 {
  Line 27: 		Debug.Printf("GetObjSize %x %s\n", obj, Stack())
  Line 28: 		Result.Printf("GetObjSize %x %s\n", obj, Stack())
  Line 29: 		panic(fmt.Sprintf("GetObjSize %x", obj))

/home/hcjn0770/temp/worktrees/issue-30/txos/maze.go:
  Line 259: 	SamplePostmanObjects = samplePostmanObjects

/home/hcjn0770/temp/worktrees/issue-30/txos/size-tree.go:
  Line 51: 	total_sz, ok := GetObjSize0(node)
  Line 74: 			sz := GetObjSize(child_node)

/home/hcjn0770/temp/worktrees/issue-30/txos/results.go:
  Line 46: 		CurObjSize = ResultItems[GlobalS].ObjSize
  Line 62: 			one["percent"] = fmt.Sprintf("%d", CurObjSize[pyo])
  Line 206: func samplePostmanObjects(M, N int) []interface{} {
  Line 207: 	D.Println("samplePostmanObjects ")
  Line 208: 	Debug.Println("samplePostmanObjects")
  Line 230: 		item, ok := ResultItems[s]
  Line 365: 	CurObjSize = ResultItems[s].ObjSize
  Line 380: 			"percent": CurObjSize[pyo],
```
</details>

#### `02:47:02` Tool: `grep` — samplePostmanObjects|ResultItems\[s\] (completed)

Pattern: `samplePostmanObjects|ResultItems\[s\]`

<details><summary>Output</summary>

```
Found 7 matches
/home/hcjn0770/temp/worktrees/issue-30/python/results.go:
  Line 150: func samplePostmanObjects(M, N int) []interface{} {
  Line 153: 	D.Println("samplePostmanObjects size ", PoolSize)
  Line 154: 	Debug.Println("samplePostmanObjects size ", PoolSize)
  Line 175: 		item, ok := ResultItems[s]
  Line 208: 		Debug.Printf("samplePostmanObjects M %d N %d maxAmount %d len(os) %d\n", M, N, maxAmount, n)

/home/hcjn0770/temp/worktrees/issue-30/python/maze.go:
  Line 269: 		item, _ := ResultItems[s]
  Line 289: 	SamplePostmanObjects = samplePostmanObjects
```
</details>

#### `02:47:02` Tool: `bash` — Runs document acceptance verification script (completed)

```bash
python3 - <<'PY'
from pathlib import Path
checks = []
# TC-001
text = Path('common/result_items.go').read_text(); assert 'type ResultItem struct' in text and 'Amount  uint64' in text and 'Sizeof  uint64' in text and 'Objects PriorityQueue' in text and 'ObjSize map[uint64]uint64' in text; checks.append('TC-001 PASS')
# TC-002
for name in ['GetResultItem', 'AddToResultItems', 'MergeResultItems', 'DumpResultItems']: assert f'func {name}' in text, name
checks.append('TC-002 PASS')
# TC-003
files = ['python/maze.go', 'python/mobile_server2_main.go', 'python/mosd.go', 'txos/txos-aoi.go', 'txos/leak.go']
hits = 0
for f in files:
    t = Path(f).read_text()
    if 'GetResultItem(s, resultItems)' in t and 'MergeResultItems(resultItems)' in t:
        hits += 1
assert hits >= 2, hits
checks.append(f'TC-003 PASS hits={hits}')
# TC-004
r2 = Path('common/utils.go').read_text(); assert 'type ResultItem struct {' in r2 and 'json:"order"' in r2 and 'json:"type_id"' in r2
checks.append('TC-004 PASS')
#
... (truncated, 2056 chars total)
```

```
TC-001 PASS
TC-002 PASS
TC-003 PASS hits=5
TC-004 PASS
TC-005 PASS
TC-006 PASS
TC-007 PASS
EXTRA-01 PASS analysis coverage

```

*Step finished: reason=tool-calls, tokens=48,878*

### `02:47:08` Step 4

#### `02:47:10` Tool: `bash` — Generates timestamp for QA report (completed)

```bash
date +%Y%m%d%H%M%S
```

```
20260307104710

```

*Step finished: reason=tool-calls, tokens=54,751*

### `02:47:15` Step 5
