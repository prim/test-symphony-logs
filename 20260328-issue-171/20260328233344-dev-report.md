## Summary

修复了 `cmd/todo_audit.py` 的 Python 2/3 文件读写兼容性问题，避免正式 TODO 审计入口继续依赖 Python 3 专属的 `open(..., encoding=...)` 用法。

## Changes

- `cmd/todo_audit.py`: 将 shebang 改为 `python`，统一改用 `io.open(..., encoding="utf-8")` 读取 UTF-8 文本，并新增 `write_stdout()` 处理 Python 2 下标准输出的 unicode/bytes 兼容。

## Testing

- `python3 cmd/todo_audit.py --check` ✅
- `python3 -m unittest tests/test_issue_171_todo_audit_regression.py` ✅
- `./maze --build` ✅
- `python2 cmd/todo_audit.py --check` ⚠️ 当前环境缺少 `python2` 可执行文件，无法完成实机验证；已按 Python 2/3 兼容写法修复源码。
