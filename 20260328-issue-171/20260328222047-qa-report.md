## QA Result: FAIL

# QA 验收报告

## 总体结果

**[STATUS: FAIL]**

## Findings

- `cmd/todo_audit.py --render` 生成的正式报告内容与仓库内声明的正式报告 `dev-log/2026-03-28-todo-audit-report.md` 不一致。首个差异出现在第 10 行：正式报告包含“校验脚本”行，而渲染输出直接写“校验命令”，说明“结构化数据/脚本可再生成正式报告”的链路尚未闭环，正式报告仍是手工快照而非稳定可复核产物。
- 回归测试仍保留对固定日期的硬编码断言：`tests/test_issue_171_todo_audit_regression.py:50` 直接要求 `data["report_date"] == "2026-03-28"`。虽然报告路径已改为通过 `current_report` 解析，但测试仍绑定单次审计日期，后续更新结构化数据或重新审计时仍可能产生无关失败，不符合“稳定入口 + 可维护总表”的目标。

## 详细结果

### TC-001: 审计产物存在且声明结构化字段
- **结果**: PASS
- **实际行为**: `dev-log/todo-audit-latest.md`、`dev-log/todo-audit-data.json`、`cmd/todo_audit.py` 与正式报告均存在；回归测试验证字段 `id/source/summary/module/status/value/evidence/action`、稳定编号和维护入口成功通过。
- **关键指标**: `python3 -m unittest tests/test_issue_171_todo_audit_regression.py` 运行 5 个测试，全部通过。
- **备注**: 该项验证的是“存在性与基本契约”，不代表正式报告与渲染结果完全一致。

### TC-002: 审计数据与仓库状态保持一致
- **结果**: FAIL
- **实际行为**: `python3 cmd/todo_audit.py --check` 成功，说明数据、引用、扫描命中数和稳定入口自检通过；但 `python3 cmd/todo_audit.py --render` 的输出与正式报告文件内容不一致，表明正式报告并非由当前结构化数据稳定生成。
- **关键指标**: 首个差异位于第 10 行；正式报告为 `- 校验脚本：\`cmd/todo_audit.py\``，渲染输出为 `- 校验命令：\`python3 cmd/todo_audit.py --check\``。
- **备注**: 这会削弱“可维护 TODO 总表”的长期可靠性，因为后续维护者无法确信正式报告就是当前数据的真实渲染结果。

### TC-003: 覆盖全部 TODO 来源并按重点主题归类
- **结果**: PASS
- **实际行为**: 正式报告覆盖根目录 `TODO.md`、`doc/TODO.md`、源码 `TODO/FIXME/XXX` 注释、`dev-log` 未收敛待办，并补充 `cpp/TODO.md`；同时按 7 个重点方向分组，`todo-audit-data.json` 中共有 37 个条目。
- **关键指标**: `required_sources=4`，`supplemental_sources` 含 `cpp/TODO.md`，`entries=37`。
- **备注**: 静态审阅显示报告已把 Python、allocator、GDB/symbol/ctypes、printer、引用图、工具链、文档部署全部纳入。

### TC-004: 关键 smoke test
- **结果**: PASS
- **实际行为**: `./maze --build` 成功，且 `python3 testdata/run_test.py python/20260129-complex-types-311` 完成 Maze 分析与 `validate.py` 校验闭环。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500`、`Config=300`；测试总计 `1 passed, 0 failed`。
- **备注**: 已满足“本次验收至少执行一个 testdata 用例”的要求。

### TC-005: 稳定入口不再绑定单次审计文件/日期
- **结果**: FAIL
- **实际行为**: 测试文件虽已通过 `current_report` 定位正式报告，但仍在 `tests/test_issue_171_todo_audit_regression.py:50` 对 `report_date` 做固定值断言 `2026-03-28`。
- **关键指标**: 1 处固定日期硬编码断言。
- **备注**: 这不是立即影响本次分支通过的运行失败，但会阻碍后续按同样流程生成新审计结果，属于与需求目标直接相关的可维护性缺陷。

## Test Results

- `./maze --build`：PASS
- `python3 cmd/todo_audit.py --check`：PASS
- `python3 -m unittest tests/test_issue_171_todo_audit_regression.py`：PASS（5/5）
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS
- `python3 cmd/todo_audit.py --render` 与正式报告比对：FAIL

## 失败的 Test Case

### TC-002: 审计数据与仓库状态保持一致
- **结果**: FAIL
- **预期行为**: 正式报告应可由 `dev-log/todo-audit-data.json` 经 `cmd/todo_audit.py --render` 稳定生成，至少语义与内容保持一致。
- **实际行为**: 渲染结果与正式报告出现内容差异，首个差异出现在第 10 行。
- **复现步骤**:
  1. 运行 `python3 cmd/todo_audit.py --render`
  2. 打开 `dev-log/2026-03-28-todo-audit-report.md`
  3. 对比两者内容
  4. 观察第 10 行开始出现不一致
- **可能原因**: 正式报告仍经手工编辑，未完全由结构化数据和渲染脚本单源生成。
- **建议修复**: 让正式报告与 `--render` 输出收敛到同一单一来源，避免“结构化数据一份、正式报告另一份”的双轨漂移。

### TC-005: 稳定入口不再绑定单次审计文件/日期
- **结果**: FAIL
- **预期行为**: 回归测试应依赖稳定入口与结构化数据的当前声明，而不应再硬编码具体审计日期。
- **实际行为**: `tests/test_issue_171_todo_audit_regression.py:50` 仍断言 `data["report_date"] == "2026-03-28"`。
- **复现步骤**:
  1. 打开 `tests/test_issue_171_todo_audit_regression.py`
  2. 查看 `test_audit_report_exists_and_declares_structured_columns`
  3. 观察第 50 行固定日期断言
- **可能原因**: 本次修改只移除了对固定报告文件名的依赖，但未完全移除对单次审计元数据的硬编码假设。
- **建议修复**: 将该断言改为验证 `report_date` 字段存在且格式合法，或只验证其与正式报告/稳定入口保持一致，而非固定为某一天。

## 未执行项

- 未执行 `cpp/20260304-stack-multithread` 等额外扩展回归用例。
- **原因**: 阶段 A/B 已发现与本工单核心目标直接相关的可维护性缺陷；继续追加更多动态用例无法消除“正式报告不可稳定再生成”和“测试仍硬编码日期”这两个主阻塞。
- **影响范围**: 主要影响 AC5“输出可维护的 TODO 总表，并给出后续动作”；对构建与基础 smoke 的通过结论影响较小。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 3 / 5
- 失败: 2 / 5
- 建议: 当前分支已补齐稳定入口、结构化数据、自检脚本和至少一个真实 smoke 用例，方向正确；但“正式报告与渲染结果不一致”以及“回归测试仍硬编码审计日期”这两个问题仍直接影响交付物的长期可维护性，当前不建议按 PASS 验收。
