## Summary
补上自动发现命中点与结构化声明之间的逐项一致性校验，避免同一已登记文件内 TODO/FIXME/XXX 命中行新增、删除或迁移后继续静默通过；同时把回归测试从快照数量锁定改为校验发现结果与 refs 精确一致。

## Changes
- `cmd/todo_audit.py`: `validate_discovered_items()` 新增 discovered refs 与声明 refs 的逐项比对，发现缺失/过期命中行时直接失败。
- `tests/test_issue_171_todo_audit_regression.py`: 去掉对固定条目总数和精确配置快照的脆弱断言，改为验证各重点模块都有条目、自动发现结果与 JSON 中的 refs 完全一致，并增加命中漂移失败用例。
- `dev-log/todo-audit-data.json`: 按当前仓库扫描结果补齐 `common/cpp.go`、`common/var.go`、`gdbcommand.py`、`stdprinter.py` 的完整 refs 与说明。
- `dev-log/2026-03-28-todo-audit-report.md`: 重新由 `python3 cmd/todo_audit.py --render` 生成，确保正式报告与结构化数据同步。

## Testing
- `python3 cmd/todo_audit.py --check`
- `python3 -m unittest tests/test_issue_171_todo_audit_regression.py`
- `./maze --build`
- `python3 testdata/run_test.py python/20260129-complex-types-311`
