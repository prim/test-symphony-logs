# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 `prim/maze#138` 的 `common/mpm/v2` 热路径优化执行了阶段 A 静态核对、阶段 B/C 关键动态验证与基准回归。需求文档中的 5 个 AC 均已覆盖；关键单元测试、微基准、workload/trace replay 基准、`./maze --build` 以及至少 1 个 `testdata` 主链路用例均通过，未发现阻断发布的问题。

## 详细结果

### TC-001: Finalize 后禁止结构性修改
- **结果**: PASS
- **实际行为**: `go test ./common/mpm/v2 -count=1` 通过，`TestFinalizeDisallowsCollectAfterFinalize` 验证 `Finalize()` 后再次 `Collect()` 会触发拒绝写入逻辑。
- **关键指标**: 单元测试通过；实现中 `Collect()` / `CollectPage()` 在检测到 frozen 视图后会 `panic`。
- **备注**: 满足 AC2。

### TC-002: Finalize 后查询路径不依赖 manager 锁
- **结果**: PASS
- **实际行为**: `TestLookupDoesNotBlockOnManagerMutexAfterFinalize` 通过；在测试线程持有 `m.mu.Lock()` 期间，`SizeAndIsKnow()` 仍能完成查询。
- **关键指标**: 单元测试通过；`lookup()` 直接从 `atomic.Value` 读取 frozen 视图，不再走 manager `RWMutex`。
- **备注**: 满足 AC1。

### TC-003: 小页集合 IterPage 走串行 fast path
- **结果**: PASS
- **实际行为**: `TestIterPageUsesSerialFastPathForSmallPageSets` 通过；单页场景未 fan-out 出整组 worker。
- **关键指标**: `BenchmarkIterPageSinglePage` = `59.56 ns/op`, `24 B/op`, `2 allocs/op`。
- **备注**: 满足 AC3。

### TC-004: mostly-single-page Collect 分配基线
- **结果**: PASS
- **实际行为**: 基准成功执行，可稳定复核 mostly-single-page 场景分配表现。
- **关键指标**: `BenchmarkCollectMostlySinglePage` = `137831 ns/op`, `139904 B/op`, `524 allocs/op`。
- **备注**: 该 TC 属于性能基线型用例；本轮结果与开发报告一致，可用于后续继续追踪 AC5。

### TC-005: cross-page-heavy Collect 对照基线
- **结果**: PASS
- **实际行为**: 基准成功执行，cross-page-heavy 场景未出现构建失败、功能失败或明显异常。
- **关键指标**: `BenchmarkCollectCrossPageHeavy` = `133479 ns/op`, `103193 B/op`, `1555 allocs/op`。
- **备注**: 该 TC 主要用于对照观察，当前已具备可复测基线。

### TC-006: Finalize 后并行查询微基准
- **结果**: PASS
- **实际行为**: finalize 后并行查询基准成功执行；同时补充复核了 query-heavy 与 trace replay 相关基准。
- **关键指标**:
  - `BenchmarkLookupAfterFinalizeParallel` = `0.6105 ns/op`, `0 B/op`, `0 allocs/op`
  - `BenchmarkMPMTraceReplayQuery/V1` = `224.95 ms/op`
  - `BenchmarkMPMTraceReplayQuery/V2` = `191.09 ms/op`
- **备注**: `V2` 在 trace replay 查询基准上已优于 `V1`，满足 AC1、AC4 的目标方向。

### TC-007: 单页 IterPage 调度基线
- **结果**: PASS
- **实际行为**: 单页 `IterPage()` 基准成功执行，small-page fast path 生效。
- **关键指标**: `BenchmarkIterPageSinglePage` = `59.56 ns/op`, `24 B/op`, `2 allocs/op`。
- **备注**: 满足 AC3、AC4。

## 补充验证

### 构建验证
- `./maze --build`：通过。

### 扩展 workload / query 基准
- `BenchmarkMPMSizeAndKnowHit`
  - Sparse: `V2 50.91 µs/op`，慢于 `V1 20.89 µs/op`
  - Dense: `V2 32.16 µs/op`，优于 `V1 43.52 µs/op`
  - Mixed: `V2 48.61 µs/op`，优于 `V1 57.15 µs/op`
- `BenchmarkMPMSizeAndIsKnowMiss`
  - Sparse: `V2 46.76 µs/op`，慢于 `V1 12.30 µs/op`
  - Dense: `V2 36.85 µs/op`，略慢于 `V1 33.33 µs/op`
  - Mixed: `V2 48.55 µs/op`，优于 `V1 62.52 µs/op`
- `BenchmarkMPMWorkloadMixedRounds`
  - Sparse: `V2 1.36 ms/op`，显著优于 `V1 196.44 ms/op`
  - Dense: `V2 0.28 ms/op`，显著优于 `V1 170.83 ms/op`
  - Mixed: `V2 1.01 ms/op`，显著优于 `V1 169.48 ms/op`

### testdata 主链路 smoke test
- **用例**: `python/20260129-complex-types-311`
- **命令**: `python3 testdata/run_test.py python/20260129-complex-types-311`
- **结果**: PASS
- **实际行为**: Maze 成功分析 tarball，`validate.py` 全部检查通过；`pymempool_objects = 68626`，`PersonClass=1000`，`GameEntity=300` 等关键对象统计均符合预期。

### 日志检查
- `maze.py.log`：本轮未生成异常匹配项。
- `maze.log`：未发现 `panic/fatal` 级别失败；存在包含 `Traceback` 字样的 Python 类型名调试日志，但本次 `testdata` 验证整体通过，未构成失败信号。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）
- 未单独逐条运行 `TC-001` ~ `TC-003` 的 `-run` 精确命令；改为执行 `go test ./common/mpm/v2 -count=1` 一次性覆盖同一测试文件中的全部单元测试。
  - **原因**: 覆盖范围更完整、成本更低。
  - **影响范围**: 不影响 AC1/AC2/AC3 的判定。
- 未额外执行多 Python 版本、glibc 版本、Node.js 版本兼容性矩阵。
  - **原因**: 当前 issue 聚焦 `common/mpm/v2` Go 热路径优化，且本轮未发现阻断主链路问题。
  - **可能影响**: 兼容性结论仅能部分给出，不能外推到完整跨版本矩阵。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] `IterPage` 小页场景保持低调度开销
- [x] trace replay query 基准优于当前 `V1` 基线
- [x] mixed workload 基准继续优于当前 `V1` 基线

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: **可以发布。** 本轮 issue 目标集中在 `common/mpm/v2` 热路径优化，关键 AC 已满足，新增测试与基准均已建立并通过。需要注意的是，Sparse query 微基准下 `V2` 仍慢于 `V1`，但这不构成当前需求的阻塞项；建议后续继续观察是否需要针对极稀疏页访问分布做进一步定向优化。
