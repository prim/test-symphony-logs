## Summary

修复了 `common/mpm/v2` 中不兼容 Go 1.18 的 `atomic.OrUint32` 使用，改为基于 `atomic.LoadUint32 + atomic.CompareAndSwapUint32` 的兼容实现，保持 `Known` 位图热路径语义不变并恢复仓库声明构建基线的一致性。

结论：**COMPLETE**

## Changes

- `common/mpm/v2/mpm.go`: 新增 `orUint32()` CAS 辅助函数，替换 `SetKnown()` / `SwapKnown()` 中的 `atomic.OrUint32`
- `dev-log/2026-03-14-mpm-v2-go118-atomic-compat.md`: 记录本轮 Go 1.18 兼容性修复背景、方案与验证结果

## Testing

- `go test ./common/mpm/v2 -count=1`
  - 结果：PASS
- `go test ./common/mpm/v2 -run ^$ -bench BenchmarkLookupAfterFinalizeParallel -benchmem -count=1`
  - 结果：PASS，`0 B/op, 0 allocs/op`
- `./maze --build`
  - 结果：PASS

## 验收核对

- AC1: PASS。本轮未改动 finalize 后无锁查询结构；`go test ./common/mpm/v2 -count=1` 与并行查询 benchmark 继续通过，结合最新 QA 报告可确认查询路径仍不依赖 manager 读锁。
- AC2: PASS。本轮未改动冻结后拒绝结构性修改逻辑；`go test ./common/mpm/v2 -count=1` 继续覆盖相关单测。
- AC3: PASS。本轮未改动 `IterPage()` fast path；结合最新 QA 报告与本轮回归，功能未回退。
- AC4: PASS。保留已有 query-heavy / trace replay / mixed workload 基准资产；本轮补测了 finalize 后并行查询 benchmark，未见热路径回退。
- AC5: PASS。保留已有 `Collect()` 分配敏感 benchmark 资产；本轮未改动该路径，基准入口仍完整可用。

## 已完成项

- 已修复 code review 报告指出的 Go 1.18 原子 API 兼容性问题
- 已确认仓库内不再使用 `atomic.OrUint32`
- 已完成最小必要回归测试与构建验证

## 未完成项

- 无

## 未验证项

- 未在真实 Go 1.18 二进制环境下单独执行一轮构建；本轮依据 `go.mod` 约束与 API 兼容性静态核对判定实现已恢复 Go 1.18 可编译性

## Self-Review 发现与修复

- 发现 `atomic.OrUint32` 超出仓库声明的最低 Go 版本能力边界，已改为 Go 1.18 可用的 CAS 循环实现

## 注意事项

- 本轮严格只修复 code review 指出的兼容性问题，未修改测试文件
- `dev-flow/` 下报告文件按要求生成，但不应纳入提交
