## Summary

本轮根据 human review 的新增要求，放弃 Go 1.18 兼容声明，将仓库最低 Go 版本基线提升到 1.21，并同步修正文档说明；同时补上 `common/mpm/v2` 中 `IsKnown` 的负索引防御式判断，消化最新 code review 的非阻塞建议。

结论：COMPLETE

# 开发报告

## 完成的任务
- [x] Task 1: 将 `go.mod` 的 Go 版本声明从 `1.18` 提升到 `1.21`，落实 human review“放弃 1.18 兼容支持”的要求
- [x] Task 2: 同步更新 `README.md`、`doc/getting_started.md`、`AGENTS.md` 中的 Go 环境要求，消除仓库声明与 CI 基线不一致
- [x] Task 3: 在 `common/mpm/v2` 中为 `IsKnown` 增加 `idx < 0` 防御式判断，提升接口健壮性
- [x] Task 4: 运行单测、基准、构建与 1 个主链路 `testdata` 用例完成回归验证

## 修改的文件
- `go.mod`: 将仓库最低 Go 版本提升到 `1.21`
- `README.md`: 将公开文档中的 Go 环境要求更新为 `Go 1.21+`
- `doc/getting_started.md`: 同步快速开始中的 Go 版本要求
- `AGENTS.md`: 同步仓库内开发说明中的 Go 版本要求
- `common/mpm/v2/mpm.go`: 为 `IsKnown` 相关路径补充负索引防御判断
- `dev-log/2026-03-14-go-version-baseline-121.md`: 记录本轮版本基线调整的背景、方案与验证

## 自测结果
- 测试命令: `go test ./common/mpm/v2 -count=1`
  - 测试结果: 通过
- 测试命令: `go test ./common/mpm/v2 -run ^$ -bench 'BenchmarkCollectMostlySinglePage|BenchmarkCollectCrossPageHeavy|BenchmarkLookupAfterFinalizeParallel|BenchmarkIterPageSinglePage' -benchmem -count=1`
  - 测试结果: 通过；`BenchmarkLookupAfterFinalizeParallel = 0.7550 ns/op, 0 B/op, 0 allocs/op`
- 测试命令: `go test ./common -run ^$ -bench 'BenchmarkMPMSizeAndKnowHit|BenchmarkMPMSizeAndIsKnowMiss|BenchmarkMPMWorkloadMixedRounds|BenchmarkMPMTraceReplayQuery' -benchmem -count=1`
  - 测试结果: 通过；`BenchmarkMPMTraceReplayQuery/V2 = 202992018 ns/op`，继续优于 `V1 = 228388640 ns/op`
- 测试命令: `./maze --build`
  - 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
  - 测试结果: 通过；`pymempool_objects = 68626`，`PersonClass = 1000`，`GameEntity = 300`

## 验收核对
- AC1: PASS 已实现 finalize 后无锁查询；本轮回归测试和基准均通过
- AC2: PASS 已实现 finalize 后禁止结构性修改；既有测试保持通过
- AC3: PASS 已实现 `IterPage()` 小页串行 fast path；既有测试与基准保持通过
- AC4: PASS query-heavy / mixed workload / trace replay 基准已保留并回归通过
- AC5: PASS `Collect()` 的 mostly-single-page 与 cross-page-heavy 分配敏感基准均已执行并通过
- Human review: PASS 已落实“声明更新版本的 Go，放弃 1.18 兼容支持”

## 剩余工作
- 未完成项: 无
- 未验证项: 无

## Self-Review 发现与修复
- 发现公开文档与 `go.mod` 的版本声明仍停留在 `Go 1.18+`，但 CI 已使用 `Go 1.21`；已统一提升到 `1.21`
- 发现 `IsKnown(pageID, idx)` 对负索引缺少显式防御；已补充 `idx < 0` 返回 `false` 的保护逻辑

## 注意事项
- `dev-log/` 下仍可能保留历史“Go 1.18 兼容性修复”背景记录，这是历史过程文档，不代表当前仓库最低版本声明
- 本轮没有修改任何测试文件，均通过源代码与文档调整完成修复

## 三份清单交叉核对

### 已完成项
- 原始需求中 AC1~AC5 对应功能与基准均已存在且本轮重新验证通过
- 最新 QA 报告中的通过项未回退
- 最新 human review 要求的 Go 版本基线调整已落地

### 未完成项
- 无

### 未验证项
- 无
