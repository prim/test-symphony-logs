## Summary
- 修复了 `common/mpm/v2` 在 trace replay 查询基准上仍慢于 `V1` 的 QA 问题。
- 在不改变 `v2` 架构方向的前提下，引入冻结页只读开放寻址表与 dense page coarse 索引，压低 trace replay 热路径常数开销。

## Changes
- `common/mpm/v2/mpm.go`: 用冻结期构建的 `frozenPageTable` 替代 `map` 回退路径，并为 dense page 增加块级 coarse 索引。
- `dev-log/2026-03-14-mpm-v2-trace-replay-optimization.md`: 记录本轮问题分析、实现方式与验证结果。

## Testing
- `go test ./common/mpm/v2 -count=1`
- `go test ./common/mpm/v2 -run ^$ -bench 'BenchmarkCollectMostlySinglePage|BenchmarkCollectCrossPageHeavy|BenchmarkLookupAfterFinalizeParallel|BenchmarkIterPageSinglePage' -benchmem -count=1`
- `go test ./common -run ^$ -bench 'BenchmarkMPMWorkloadMixedRounds|BenchmarkMPMTraceReplayQuery' -benchmem -count=1`
- `./maze --build`
- `python3 testdata/run_test.py python/20260129-complex-types-311`

结果：
- `BenchmarkMPMTraceReplayQuery/V2` ≈ `192.1 ms/op`，优于 `V1` 的 ≈ `230.8 ms/op`
- 其余 hot path benchmark、构建与 testdata 用例均通过
