# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 `prim/maze#138` 执行了需求核对、代码范围检查、关键单元测试、性能基准、构建验证，以及至少 1 个 `testdata` 主链路用例验证。结合最新修复内容，本次重点复核了此前发现的 Go 1.18 原子 API 兼容性问题，未发现新的阻断缺陷；5 个验收标准均可判定为满足。

## 详细结果

### TC-001: Finalize 后禁止结构性修改
- **结果**: PASS
- **实际行为**: `go test ./common/mpm/v2 -run 'TestFinalizeDisallowsCollectAfterFinalize|TestLookupDoesNotBlockOnManagerMutexAfterFinalize|TestIterPageUsesSerialFastPathForSmallPageSets|TestSizeAndIsKnowMatchesFacadeKnownSemantics' -count=1` 通过，其中 `TestFinalizeDisallowsCollectAfterFinalize` 验证 `Finalize()` 后再次 `Collect()` 会被拒绝。
- **关键指标**: `Collect()` / `CollectPage()` 在 frozen 视图存在时显式 `panic`。
- **备注**: 对应 AC2。

### TC-002: Finalize 后查询路径不依赖 manager 锁
- **结果**: PASS
- **实际行为**: `TestLookupDoesNotBlockOnManagerMutexAfterFinalize` 通过；持有 `m.mu.Lock()` 期间，`SizeAndIsKnow()` 查询仍能在超时前完成。
- **关键指标**: 查询路径通过 `atomic.Value` 读取 frozen 视图；`BenchmarkLookupAfterFinalizeParallel = 0.6810 ns/op, 0 B/op, 0 allocs/op`。
- **备注**: 对应 AC1。

### TC-003: 小页集合 IterPage 走串行 fast path
- **结果**: PASS
- **实际行为**: `TestIterPageUsesSerialFastPathForSmallPageSets` 通过；单页场景不会 fan-out 出整组 worker。
- **关键指标**: `BenchmarkIterPageSinglePage = 60.01 ns/op, 24 B/op, 2 allocs/op`。
- **备注**: 对应 AC3。

### TC-004: mostly-single-page Collect 分配基线
- **结果**: PASS
- **实际行为**: 基准成功执行，可复核 mostly-single-page 输入下的中间分配表现。
- **关键指标**: `BenchmarkCollectMostlySinglePage = 132405 ns/op, 139905 B/op, 524 allocs/op`。
- **备注**: 对应 AC5；属于基线型性能用例。

### TC-005: cross-page-heavy Collect 对照基线
- **结果**: PASS
- **实际行为**: 基准成功执行，跨页场景未出现异常退化或功能性失败。
- **关键指标**: `BenchmarkCollectCrossPageHeavy = 127742 ns/op, 103194 B/op, 1555 allocs/op`。
- **备注**: 对应 AC5；用于对照优化没有牺牲跨页场景。

### TC-006: Finalize 后并行查询微基准
- **结果**: PASS
- **实际行为**: 微基准与扩展 workload/trace replay 基准均成功执行。
- **关键指标**:
  - `BenchmarkLookupAfterFinalizeParallel = 0.6810 ns/op, 0 B/op, 0 allocs/op`
  - `BenchmarkMPMTraceReplayQuery/V1 = 254.33 ms/op`
  - `BenchmarkMPMTraceReplayQuery/V2 = 204.54 ms/op`
- **备注**: 对应 AC1、AC4；`V2` 在 trace replay query 上继续优于 `V1`。

### TC-007: 单页 IterPage 调度基线
- **结果**: PASS
- **实际行为**: 单页 `IterPage()` 基准成功执行，符合 small-page fast path 设计目标。
- **关键指标**: `BenchmarkIterPageSinglePage = 60.01 ns/op, 24 B/op, 2 allocs/op`。
- **备注**: 对应 AC3、AC4。

## 补充验证

### 阶段 A：静态检查
- 已核对需求文档 AC1~AC5 与实现映射关系。
- 已检查代码变更范围，核心实现集中在 `common/mpm/v2/mpm.go`，并补充了对应单测与 benchmark 文件。
- 已检查 `go.mod`，确认仓库声明最低 Go 版本为 `1.18`。
- 已检查仓库中不再残留 `atomic.OrUint32` 调用。

### 阶段 B：关键 smoke test
- `go test ./common/mpm/v2 -count=1`：PASS
- `./maze --build`：PASS

### 阶段 C：扩展回归
- `go test ./common/mpm/v2 -run 'TestFinalizeDisallowsCollectAfterFinalize|TestLookupDoesNotBlockOnManagerMutexAfterFinalize|TestIterPageUsesSerialFastPathForSmallPageSets|TestSizeAndIsKnowMatchesFacadeKnownSemantics' -count=1`：PASS
- `go test ./common/mpm/v2 -run ^$ -bench BenchmarkCollectMostlySinglePage -benchmem -count=1`：PASS
- `go test ./common/mpm/v2 -run ^$ -bench BenchmarkCollectCrossPageHeavy -benchmem -count=1`：PASS
- `go test ./common/mpm/v2 -run ^$ -bench BenchmarkLookupAfterFinalizeParallel -benchmem -count=1`：PASS
- `go test ./common/mpm/v2 -run ^$ -bench BenchmarkIterPageSinglePage -benchmem -count=1`：PASS
- `go test ./common -run ^$ -bench 'BenchmarkMPMSizeAndKnowHit|BenchmarkMPMSizeAndIsKnowMiss|BenchmarkMPMWorkloadMixedRounds' -benchmem -count=1`：PASS
- `go test ./common -run ^$ -bench BenchmarkMPMTraceReplayQuery -benchmem -count=1`：PASS

### testdata 主链路验证
- **用例**: `python/20260129-complex-types-311`
- **命令**: `python3 testdata/run_test.py python/20260129-complex-types-311`
- **结果**: PASS
- **实际行为**: Maze 成功分析 tarball，`validate.py` 全部检查通过；`pymempool_objects = 68626`，`PersonClass = 1000`，`GameEntity = 300` 等关键统计符合预期。

### 日志检查
- `maze.log`: 未匹配到 `panic/fatal/FATAL/PANIC`
- `maze.py.log`: 未匹配到 `Traceback/ERROR/CRITICAL/Fatal/Exception`

## 失败的 Test Case（如有）

无。

## 未执行项（如有）
- 未执行跨 Go 版本真实环境矩阵验证。
  - **原因**: 本轮重点是复核 issue #138 及此前 QA 指出的 Go 1.18 API 兼容性回归；当前通过静态代码核对、`go.mod` 版本约束、单元测试和构建验证确认问题已修复。
  - **影响范围**: 对 AC1~AC5 的功能性判断无阻塞，但“真实 Go 1.18 环境构建”仍属于额外兼容性增强验证。
- 未执行 Python 2.7 / 多 glibc / 多 Node.js 版本矩阵。
  - **原因**: 当前 issue 变更集中在 `common/mpm/v2` 的 Go 热路径实现。
  - **影响范围**: 不影响本次需求验收结论，但不能外推为完整跨环境兼容性结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过
- [x] Go 1.18 API 兼容性静态核对通过

## 性能验证（如适用）
- [x] `IterPage` 小页场景保持低调度开销
- [x] finalize 后查询路径保持 `0 allocs/op`
- [x] trace replay query 基准优于 `V1`
- [x] mixed workload 基准优于 `V1`
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 7 / 7
- 失败: 0 / 7
- 建议: **可以通过本轮验收。** 这次 QA retry #7 的关注点——此前发现的 Go 1.18 原子操作兼容性问题——已修复；关键功能、性能基准、构建与主链路 smoke test 均通过。仍需注意：Sparse query 微基准下 `V2` 依旧慢于 `V1`，但该现象已在此前报告中存在，且不构成本 issue 当前 AC 的阻塞项。
