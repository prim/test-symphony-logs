# QA 验收报告

## QA Result: FAIL

## 总体结果

**[STATUS: FAIL]**

原因：阶段 A/B 的静态检查、构建、单测与至少一个 `testdata` 用例已通过，但验收标准中的 AC4（`pprof` 热点复测）与 AC5（`refs=v0` / `refs=v1` 端到端耗时收敛）本轮未完成，无法给出完整 PASS 结论。

## Findings

- `common/vm/facade.go` 已移除 `implementationSnapshot`、`acquireSnapshot()` / `releaseSnapshot()`、`markSnapshotClosing()` / `waitSnapshotDrained()`，`GetMemory*` / `GetMemoryRange` 热路径改为直接读取当前绑定实现，不再执行 `atomic.Add`。
- `SwitchToLRU()`、`SwitchToFull()`、`SwitchToV3()`、`CloseCurrent()` 已从 facade 删除，`common/vm.go` 中 `SetVMType()` 已收敛为通过 `vm.SelectImplementation()` 选择初始化期实现，不再表达运行时切换。
- 相关契约测试与集成测试已迁移，`go test ./qa/issue160`、`go test ./common/vm/...`、`go test -tags vm_v3 ./common/vm/... ./common` 均通过。
- 上一轮开发报告中声称 `testdata/run_test.py` 缺失的问题，本轮复测已确认是因为 `testdata` 子模块未初始化；初始化后 `python/20260129-complex-types-311` 可正常执行并通过。
- 仍缺少对“同一目标进程”的 `pprof` 热点复测，以及 `refs=v0` / `refs=v1` 端到端耗时对比，因此无法闭合性能验收标准。

## 详细结果

### TC-001: facade 热路径移除 snapshot 与 atomic.Add
- **结果**: PASS
- **实际行为**: 静态审查 `common/vm/facade.go` 与 `qa/issue160` 约束测试均确认热路径不再调用 snapshot 生命周期协议，也未发现 `state.Add(1)` / `state.Add(^uint64(0))`。
- **关键指标**: `go test ./qa/issue160` 通过；`go test ./common/vm/...` 通过。
- **备注**: `dispatchGetMemory*` / `dispatchGetMemoryRange` 已变为 `impl := currentImplementation()` 后直接分发。

### TC-002: facade 不再暴露运行时切换和关闭入口
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已删除 `SwitchToLRU`、`SwitchToFull`、`SwitchToV3`、`CloseCurrent`、`switchImplementation`；相关 QA 静态约束测试通过。
- **关键指标**: `go test ./qa/issue160 -run TestFacadeRemovesRuntimeSwitchAndCloseEntrypoints` 被包含在整套 `go test ./qa/issue160` 中并通过。
- **备注**: 代码库中未发现这些入口的功能性残留引用。

### TC-003: SetVMType 语义收敛为仅初始化阶段生效
- **结果**: PASS
- **实际行为**: `common/vm.go` 中 `SetVMType()` 已改为调用 `vm.SelectImplementation()`，`initVirtualMemory()` 统一执行 `SetVMType(typ)` 后 `vm.Init()`。
- **关键指标**: `go test ./qa/issue160` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: 语义层面已不再通过兼容层触发 runtime switch。

### TC-004: 遗留 lifecycle / dispatch 契约测试完成迁移
- **结果**: PASS
- **实际行为**: `common/vm/facade_lifecycle_test.go`、`common/vm/facade_dispatch_test.go`、`common/vm/v3_select_test.go`、`common/vm/init_v3_integration_test.go`、`common/vm_init_integration_test.go` 已移除对 snapshot/runtime switch 旧语义的依赖。
- **关键指标**: `go test ./common/vm/...` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: `shared.isPrivateMmap()` 的未命中降级逻辑也经 `vm_v3` 相关测试路径覆盖，避免了之前的 panic。

### TC-005: 需求与性能验收目标保持一致
- **结果**: FAIL
- **实际行为**: 需求文档、test case 和 dev-log 中仍保留了 `pprof` 热点消失与 `refs=v0` / `refs=v1` 收敛目标，但本轮仅完成了文档/静态一致性核对，未完成对应动态性能复测。
- **关键指标**: `.symphony_prompt.md`、`dev-flow/plan.md`、`dev-log/2026-03-17-vm-facade-no-runtime-switch-plan.md` 均明确 AC4/AC5；但本轮未产出新的 `pprof` 或耗时对比结果。
- **备注**: 该 TC 设计本身偏文档约束，真正的 AC4/AC5 仍需额外动态验收闭环。

## 失败的 Test Case

### TC-005: 需求与性能验收目标保持一致
- **结果**: FAIL
- **预期行为**: 除了文档与静态约束一致外，还应基于同一目标进程完成 `pprof` 复测，验证 `acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是全局热点，并完成 `refs=v0` / `refs=v1` 的端到端耗时对比，证明耗时进一步收敛。
- **实际行为**: 本轮没有执行新的同目标进程 `pprof` 采样，也没有执行新的 `refs=v0` / `refs=v1` 端到端耗时对比；因此 AC4/AC5 仍处于未验收状态。
- **复现步骤**:
  1. 准备与需求一致的同一目标进程或可复现 PID。
  2. 分别执行带 `--cpu` 的 `./maze --build --refs v0 ...` 与 `./maze --build --refs v1 ...`。
  3. 保存 `go.cpu` 与对应二进制，使用 `pprof -top` / `pprof -diff_base` 对比热点。
  4. 检查是否仍出现 `acquireSnapshot` 或 `sync/atomic.(*Uint64).Add` 热点，以及 `refs=v0` / `refs=v1` 实际耗时是否进一步收敛。
- **可能原因**: 当前分支主要完成了代码结构静态化与单测迁移，但未包含本轮动态性能复测产物；验收环境也未直接提供同一目标进程的复测上下文。
- **建议修复**: 不需要修改功能代码；请补齐针对同一目标进程的性能复测与结果归档，再重新发起 QA。

## Test Results

- `go test ./qa/issue160`：PASS
- `go test ./common/vm/...`：PASS
- `go test -tags vm_v3 ./common/vm/... ./common`：PASS
- `./maze --build`：PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## 未执行项

- AC4 对应动态验收：未对同一目标进程执行新的 `pprof` 复测。
  - **原因**: 当前任务上下文未直接提供可重复使用的同一目标进程/PID，本轮先完成了所有低成本静态检查、构建、单测与代表性 testdata 验证。
  - **影响范围**: 直接影响 AC4，以及与 TC-005 对应的性能验收结论。
- AC5 对应动态验收：未执行新的 `refs=v0` / `refs=v1` 端到端耗时对比。
  - **原因**: 与 AC4 相同，缺少本轮统一目标进程上下文；且在未完成同目标 `pprof` 复测前，继续做高成本性能验证的收益有限。
  - **影响范围**: 直接影响 AC5，以及整体发布判断中的性能结论。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [ ] 同一目标进程 `pprof` 显示 `acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是全局热点
- [ ] `refs=v0` / `refs=v1` 端到端耗时进一步收敛

## 总结
- 通过: 4 / 5
- 失败: 1 / 5
- 建议: 当前实现的结构性改动、构建与回归测试表现正常，但性能验收闭环未完成，**暂不建议按 PASS 结案**。建议补齐同目标进程的 `pprof` 与 `refs=v0` / `refs=v1` 端到端性能复测后，再进行最终验收。
