# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #160 的静态门面收敛目标完成了代码核对、契约测试、`common/vm` 相关测试，以及至少一个真实 `testdata` 用例验收。未发现阻断发布的问题。

## 详细结果

### TC-001: facade 热路径移除 snapshot 与 atomic.Add
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已删除 `implementationSnapshot`、`snapshotClosingBit`、`acquireSnapshot()`、`releaseSnapshot()`、`markSnapshotClosing()`、`waitSnapshotDrained()` 以及 `state.Add(...)` 相关逻辑；`dispatchGetMemory*` / `dispatchGetMemoryRange` 统一改为通过 `currentImplementation()` 直接分发。
- **关键指标**: 代码检索未发现热路径上的 `atomic.Add` / snapshot 生命周期协议残留；`go test ./qa/issue160` 通过。
- **备注**: 满足 AC1。

### TC-002: facade 不再暴露运行时切换和关闭入口
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 已不再包含 `SwitchToLRU`、`SwitchToFull`、`SwitchToV3`、`CloseCurrent`、`switchImplementation` 等运行时切换/关闭入口。
- **关键指标**: 静态检查与 `go test ./qa/issue160` 均通过。
- **备注**: facade API 已收敛为初始化阶段绑定 + 生命周期控制模型。

### TC-003: SetVMType 语义收敛为仅初始化阶段生效
- **结果**: PASS
- **实际行为**: `common/vm.go` 中 `SetVMType()` 已改为调用 `mustApplyVMType()`，通过 `selectVMTypeBinding()` 与 `vm.SelectImplementation()` 在初始化期完成绑定；不再调用 `vm.SwitchTo*`。若请求类型无法绑定或绑定结果不一致，会显式 `panic`。
- **关键指标**: `initVirtualMemory()` 路径固定为 `selectPreferredVMType()` → `SetVMType()` → `vm.Init()`；`go test ./qa/issue160` 通过。
- **备注**: 满足 AC3 中关于兼容层语义收敛的要求。

### TC-004: 遗留 lifecycle / dispatch 契约测试完成迁移
- **结果**: PASS
- **实际行为**: `common/vm/facade_lifecycle_test.go`、`common/vm/facade_dispatch_test.go`、`common/vm/v3_select_test.go`、`common/vm/init_v3_integration_test.go`、`common/vm_init_integration_test.go` 中已移除旧的 runtime switch / snapshot 语义依赖，并替换为初始化期固定实现、重复绑定、关闭后重绑等新契约验证。
- **关键指标**: `go test ./common/vm/...` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: 满足 AC2、AC3。

### TC-005: 需求与性能验收目标保持一致
- **结果**: PASS（按人类指引可忽略动态性能复测，仅核对需求基线）
- **实际行为**: `.symphony_prompt.md` 与 `dev-flow/plan.md` 仍保留“**不需要运行时切换 VM 版本**”“**仅初始化阶段生效**”以及 `acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再成为热点等验收描述。
- **关键指标**: `go test ./qa/issue160` 中相关文档契约测试通过。
- **备注**: 依据 prompt 中人类说明，本轮可忽略 TC-005 的额外动态性能执行，重点保持需求与性能目标一致。

### Testdata Smoke: python/20260129-complex-types-311
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功；Maze 完成分析并通过 validate。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500`、`Config=300` 等关键对象计数均符合预期。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，也间接说明本次改动未破坏主流程。

## 失败的 Test Case（如有）

无。

## 已执行的检查项
- 阅读需求文档 `dev-flow/plan.md`
- 阅读测试用例文档 `dev-flow/test-cases.md`
- 阅读最新开发报告 `dev-flow/20260317175407-dev-report.md`
- 查看 `git diff origin/master...HEAD` 与关键文件改动范围
- 静态核对 `common/vm/facade.go`、`common/vm.go`、`qa/issue160/vm_facade_staticization_test.go`
- 静态核对迁移后的 lifecycle / dispatch / integration 测试文件
- 运行 `go test ./common/vm/...`
- 运行 `go test ./qa/issue160`
- 运行 `go test -tags vm_v3 ./common/vm/... ./common`
- 运行 `./maze --build`
- 运行 `python3 testdata/run_test.py python/20260129-complex-types-311`
- 检查 `maze.log`

## 未执行项
- 未重新执行基于同一目标进程的 `pprof` 性能复测。
- 未重新执行 `refs=v0` / `refs=v1` 端到端耗时对比。

未执行原因：
- 本轮变更主要集中在初始化绑定语义校验与测试/结果复制收敛，热路径结构在静态检查中已确认未回退；同时人类指引已明确“可以忽略 TC-005”。

影响范围：
- 对 AC4、AC5 的“再次实测确认”仍属于未复跑状态；但本轮未发现会导致性能目标回退的代码证据。

## 日志检查
- `maze.log` 中存在 `Cpp error: Traceback ... module 'gdb' has no attribute 'all_variables_symbol'` 调试日志，但对应 Python testdata 用例最终 **validate 全部通过**，且该日志属于仓库现有 C++ 符号采集降级路径表现，本轮未观察到其导致主链路失败。
- 未发现本次 issue #160 直接引入的 fatal / panic / 主流程阻断。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

说明：本轮未执行新的性能基准/pprof 复测；按需求文档与人类指引，仅完成性能目标一致性核对，不将未复测视为失败。

## Test Results
- `go test ./common/vm/...`：PASS
- `go test ./qa/issue160`：PASS
- `go test -tags vm_v3 ./common/vm/... ./common`：PASS
- `./maze --build`：PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## Findings
- 未发现 `common/vm/facade.go` 热路径回退到 snapshot + `atomic.Add` 协议。
- `SetVMType()` 已从运行时切换语义收敛为初始化期绑定语义，并增加绑定失败显式报错，修复了此前“请求类型与实际绑定类型可能静默背离”的风险。
- 相关 QA 契约测试与 `common/vm` 生命周期测试已更新到静态门面模型。
- 主流程 smoke test 通过，说明本轮修改未破坏基础分析链路。

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以继续推进；若需要对 AC4/AC5 做发布前最终背书，建议后续单独补跑一次同样本 `pprof` 与 `refs=v0/v1` 端到端性能复测，但这不构成本轮代码验收阻塞。
