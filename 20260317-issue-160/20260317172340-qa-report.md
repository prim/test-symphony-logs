# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本次围绕 issue #160 执行了静态约束检查、构建与单元测试、至少一个 testdata 动态验收，以及补充的 `--tar ... --cpu` 复测。未发现阻断发布的问题。

## 详细结果

### TC-001: facade 热路径移除 snapshot 与 atomic.Add
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 已删除 `implementationSnapshot`、`snapshotClosingBit`、`acquireSnapshot`、`releaseSnapshot` 及 `state.Add(...)`。`dispatchGetMemory*` / `dispatchGetMemoryRange` 改为通过 `currentImplementation()` 直接分发。
- **关键指标**: `go test ./qa/issue160` 通过；源码中未检出上述禁用符号。
- **备注**: 满足 AC1 的核心热路径收敛要求。

### TC-002: facade 不再暴露运行时切换和关闭入口
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已无 `SwitchToLRU`、`SwitchToFull`、`SwitchToV3`、`CloseCurrent`、`switchImplementation` 入口。
- **关键指标**: `go test ./qa/issue160` 通过。
- **备注**: 对外 runtime switch API 已完成清理。

### TC-003: SetVMType 语义收敛为仅初始化阶段生效
- **结果**: PASS
- **实际行为**: `common/vm.go` 保留 `SetVMType(t VMType)`，但其内部改为基于 `vm.SelectImplementation(...)` 选择已编译实现，不再调用 `vm.SwitchTo*`。初始化路径仍为 `selectPreferredVMType()` → `SetVMType(...)` → `vm.Init()`。
- **关键指标**: `go test ./qa/issue160`、`go test -tags vm_v3 ./common/vm/... ./common` 均通过。
- **备注**: 语义已从“运行时切换”收敛为“初始化阶段固定实现”。

### TC-004: 遗留 lifecycle / dispatch 契约测试完成迁移
- **结果**: PASS
- **实际行为**: `common/vm/facade_lifecycle_test.go`、`common/vm/facade_dispatch_test.go`、`common/vm/v3_select_test.go`、`common/vm/init_v3_integration_test.go`、`common/vm_init_integration_test.go` 已迁移到静态绑定模型，不再依赖 snapshot/runtime switch 旧语义。
- **关键指标**: `go test ./common/vm/...` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: AC2、AC3 的测试更新要求已满足。

### TC-005: 需求与性能验收目标保持一致
- **结果**: PASS
- **实际行为**: `.symphony_prompt.md`、`dev-flow/plan.md` 均保留“仅初始化阶段生效”“`acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是热点”等目标；同时实际运行 `--cpu` 复测后，`pprof top` 未见这两个热点。
- **关键指标**:
  - `refs=v1` profile 总样本 `1450ms`
  - `refs=v0` profile 总样本 `1570ms`
  - 两次 `pprof -top` 均未出现 `acquireSnapshot` 或 `sync/atomic.(*Uint64).Add`
- **备注**: 人工备注虽说明可忽略 TC-005，但本轮仍完成了动态闭环验证。

## Test Results

- `./maze --build` ✅
- `go test ./qa/issue160` ✅
- `go test ./common/vm/...` ✅
- `go test -tags vm_v3 ./common/vm/... ./common` ✅
- `python3 testdata/run_test.py python/20260129-complex-types-311` ✅
- `./maze --build --refs v1 --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --limit 10 --cpu` ✅
- `./maze --build --refs v0 --tar testdata/python/20260129-complex-types-311/coredump-2636550-1769661412.tar.gz --text --limit 10 --cpu` ✅
- `go tool pprof -top -nodecount=20 ./maze tmp/go.issue160.qa.refs-v1.cpu.pprof` ✅
- `go tool pprof -top -nodecount=20 ./maze tmp/go.issue160.qa.refs-v0.cpu.pprof` ✅

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行跨 Python 2.7 / 多 glibc / 多 Node.js 版本的额外兼容矩阵；本需求改动集中在 `common/vm` facade 静态化与热路径消除，当前分支已通过代表性 Python 3.11 testdata 与 vm/vm_v3 测试。
- 未进行严格的端到端耗时多轮统计，仅完成单轮 `refs=v0` / `refs=v1` CPU profile 与主链路运行核验。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [x] 已复测 `pprof`，`acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是全局热点
- [x] 已验证 `refs=v0` / `refs=v1` 主链路均可完成，热点从 facade 原子协议移除
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 观察与备注

- `maze.log` 中存在若干 GDB `No symbol ...` 与 Cpp 分析 `AttributeError` 调试日志，但对应测试用例整体通过，属于当前样本上已有的非阻断性调试输出，未表现为本需求回归问题。
- `main.go` 的 `copyResultFiles()` 已包含 `go.cpu`，并在本轮 `--tar ... --cpu` 复测中确认文件会被复制回仓库根目录，可支持后续 `pprof` 分析。

## 总结
- 通过: 5 / 5
- 失败: 0 / 5
- 建议: 可以进入下一阶段；就 issue #160 的静态门面收敛、测试迁移和性能热点消除目标而言，本轮验收通过。
