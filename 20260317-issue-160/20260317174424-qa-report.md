# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本次围绕 issue #160 的静态门面收敛改动，已完成低成本代码核对、关键单元测试、`vm_v3` 相关回归，以及至少一个 `testdata` 动态用例验收。结果显示本次实现满足计划中的结构性验收标准，且在复测样本上未再观察到 `acquireSnapshot` / `sync/atomic.(*Uint64).Add` 成为 CPU profile 全局热点。

## 详细结果

### TC-001: facade 热路径移除 snapshot 与 atomic.Add
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已删除 `implementationSnapshot`、`snapshotClosingBit`、`acquireSnapshot()`、`releaseSnapshot()` 等 snapshot 生命周期协议；`dispatchGetMemory*` / `dispatchGetMemoryRange` 直接通过 `currentImplementation()` 获取当前绑定实现并分发。
- **关键指标**: 静态检索未发现 `state.Add(1)`、`state.Add(^uint64(0))`、`acquireSnapshot`、`releaseSnapshot`。
- **备注**: `go test ./qa/issue160 -run TestFacadeHotPathDropsSnapshotAtomicProtocol` 已包含在 `go test ./qa/issue160` 中并通过。

### TC-002: facade 不再暴露运行时切换和关闭入口
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 不再包含 `SwitchToLRU`、`SwitchToFull`、`SwitchToV3`、`CloseCurrent`、`switchImplementation` 等运行时切换/关闭入口。
- **关键指标**: 相关 API 与内部调用均已从 facade 中移除。
- **备注**: `SelectImplementation()` 语义已收敛为初始化前绑定实现，初始化后仅允许维持当前实现。

### TC-003: SetVMType 语义收敛为仅初始化阶段生效
- **结果**: PASS
- **实际行为**: `common/vm.go` 保留 `SetVMType()` 兼容层入口，但其内部已改为调用 `vm.SelectImplementation(...)`，未再调用 `vm.SwitchTo*`；`initVirtualMemory()` 仍使用 `selectPreferredVMType()` + `SetVMType()` + `vm.Init()` 固定初始化实现。
- **关键指标**: 静态检查未发现 `vm.SwitchToLRU()`、`vm.SwitchToFull()`、`vm.SwitchToV3()`、`vmcore.CloseCurrent()`。
- **备注**: 与计划文档“仅初始化阶段生效”要求一致。

### TC-004: 遗留 lifecycle / dispatch 契约测试完成迁移
- **结果**: PASS
- **实际行为**: 遗留测试文件已迁移到静态初始化语义，不再依赖 runtime switch / snapshot 协议。`common/vm/facade_lifecycle_test.go` 改为验证初始化前选型、初始化后禁止切换、Close 后可重新选择；`common/vm/facade_dispatch_test.go` 聚焦当前实现分发与 v3 公共入口绑定；`common/vm_init_integration_test.go` 改为验证初始化路径在 `vm_v3` 构建下固定选择并初始化 v3。
- **关键指标**: `go test ./common/vm/...` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: 静态检查未在指定遗留测试文件中发现 `SwitchTo*`、`CloseCurrent`、`boundImplementationSnap`、`acquireSnapshot`。

### TC-005: 需求与性能验收目标保持一致
- **结果**: PASS（按人类指引可忽略，不作为阻塞项；本次仍完成核对）
- **实际行为**: `.symphony_prompt.md` 与 `dev-flow/plan.md` 均保留“**不需要运行时切换 VM 版本**”“**仅初始化阶段生效**”“`acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是全局热点”等关键验收点。
- **关键指标**: 文档基线与测试约束一致，`go test ./qa/issue160` 中 `TestFacadeDocsAndBenchExpectationsDescribeStaticInitialization` 通过。
- **备注**: 虽然上游说明可忽略 TC-005，但当前实现已满足该文档一致性检查。

### 动态验收: testdata Python 主链路回归
- **结果**: PASS
- **实际行为**: 执行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，`pymempool_objects=68626`，多类 Python 对象计数校验全部通过。
- **关键指标**: `PersonClass=1000`、`GameEntity=300`、`TreeNode=600`、`SimpleClass=500`、`Player=500`、`Config=300`、`deque=300`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的 QA 约束。

### 性能复测: refs=v0 / refs=v1 热点与耗时收敛
- **结果**: PASS
- **实际行为**: 对同一 tarball 分别执行 `--refs v1` 与 `--refs v0` 的 `--cpu` 复测，并使用 `go tool pprof -top -nodecount=20 ./maze go.cpu` 检查热点；两次 profile 均未出现 `acquireSnapshot` 或 `sync/atomic.(*Uint64).Add`。`refs=v0` profile 出现的是 `sync/atomic.(*Int32).Add`，对应当前 VM 类型状态字段，不属于本需求要移除的热路径原子加法。
- **关键指标**:
  - `refs=v1`：`real 7.08s`
  - `refs=v0`：`real 7.09s`
  - 两者端到端耗时基本一致，达到“进一步收敛”目标
- **备注**: 当前样本为 Python `20260129-complex-types-311`；这是代表性复测，不等价于完整性能矩阵。

## 失败的 Test Case（如有）

无。

## 未执行项（如有）

- 未执行更多语言/分配器的扩展 testdata 回归（如 Node.js、C++、不同 allocator 组合）。
- 原因: 阶段 A/B 已全部通过，当前关键 AC 已由静态检查、单元测试、`vm_v3` 回归和一个主链路 testdata 用例覆盖；继续扩展动态回归属于增量收益较低的高成本验证。
- 可能影响范围: 更广的兼容性结论仍需后续专项回归补全，但不影响本次 issue #160 的核心结构性验收与主链路通过结论。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%
- [x] `acquireSnapshot` / `sync/atomic.(*Uint64).Add` 不再是当前复测样本的全局热点
- [x] `refs=v0` / `refs=v1` 在当前复测样本上的端到端耗时进一步收敛

## 附加观察

- `maze.log` 中仍可见若干历史性/环境相关调试噪声，例如部分 GDB symbol 查询失败、`gdb` 缺少 `all_variables_symbol` 属性等，但本次 `testdata` 用例整体校验仍通过，且与 issue #160 的 VM facade 热路径改动无直接阻断关系。
- 本轮运行未生成独立 `maze.py.log` 文件，未影响本次验收结论。

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以合入；如需形成更强发布信心，后续可补充 Node.js/C++ 路径与更大样本的性能专项回归，但这不阻塞 issue #160 当前验收。
