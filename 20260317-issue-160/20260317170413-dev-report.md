# 开发报告

## 完成的任务
- [x] Task 1: 将 `common/vm` facade 从 snapshot 动态门面收敛为静态绑定门面，移除热路径 `atomic.Add`
- [x] Task 2: 删除运行时切换入口，收敛 `SetVMType()` 为初始化阶段选择语义
- [x] Task 3: 迁移/重写 facade 相关测试，消除对 runtime switch 与 snapshot 生命周期协议的依赖
- [x] Task 4: 修正 `shared.isPrivateMmap()` 未命中映射时的降级行为，保证 `vm_v3` 集成测试稳定

## 修改的文件
- `common/vm/facade.go`: 删除 snapshot 生命周期协议与 runtime switch API，改为静态绑定分发
- `common/vm.go`: `SetVMType()` 改为初始化阶段选择，`initVirtualMemory()` 统一为先选型后 `vm.Init()`
- `common/vm/shared/helpers.go`: `isPrivateMmap()` 未命中时改为记录日志并返回 `false`
- `common/vm/facade_lifecycle_test.go`: 替换为静态绑定模型下的 `dispatchInit` / `dispatchClose` 测试
- `common/vm/facade_dispatch_test.go`: 改为验证 `SelectImplementation()` 与静态分发行为
- `common/vm/v3_select_test.go`: 改为验证 `SelectImplementation(VMV3)`
- `common/vm/v3_select_tag_test.go`: 改为验证 `SelectImplementation(VMV3)`
- `common/vm/init_v3_integration_test.go`: 去除对 `CloseCurrent`/snapshot 的依赖
- `common/vm_init_integration_test.go`: 改用 `vmcore.Close()`
- `common/vm/bench_compare_test.go`: benchmark 改用静态绑定入口
- `dev-log/2026-03-17-vm-facade-no-runtime-switch-plan.md`: 补充正式实现记录与验收核对

## 自测结果
- 测试命令: `go test ./qa/issue160`
- 测试结果: 通过
- 测试命令: `go test ./common/vm/...`
- 测试结果: 通过
- 测试命令: `go test -tags vm_v3 ./common/vm/... ./common`
- 测试结果: 通过
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 失败，仓库当前缺少 `testdata/run_test.py`，无法执行该回归脚本

## 验收核对
- AC1: PASS `common/vm/facade.go` 热路径已删除 `implementationSnapshot`、`acquireSnapshot`、`releaseSnapshot` 与 `atomic.Add`
- AC2: PASS `go test ./common/vm/...` 已通过
- AC3: PASS `go test ./qa/issue160` 已通过，相关 facade 契约测试已更新
- AC4: PARTIAL 尚未对同一目标进程执行 `pprof` 复测，仅完成代码结构与单测层验证
- AC5: PARTIAL 尚未执行 `refs=v0` / `refs=v1` 端到端耗时对比

## 剩余工作
- 未完成项: 对同一目标进程执行 `pprof` 复测，并做 `refs=v0` / `refs=v1` 端到端耗时对比
- 未验证项: `testdata` 代表性回归脚本未执行；当前工作树未提供对应脚本/子模块内容

## Self-Review 发现与修复
- 发现 `go test -tags vm_v3 ./common/vm/... ./common` 初次执行时，`shared.isPrivateMmap()` 会因测试二进制地址未出现在 `S.Process.Maps` 中而 panic
- 修复方式: 将该路径改为记录调试日志并返回 `false`，避免初始化阶段直接崩溃

## 注意事项
- 本轮结论: PARTIAL
- facade 层已明确不再支持运行时切换；后续若需要切换 VM 版本，应在初始化前重新选择，而不是在分析过程中切换
- `v3` 内部公共入口仍保留自身关闭保护逻辑，本次仅移除 facade 层多余的 snapshot 原子协议
