## QA Result: PASS

# QA 验收报告

## 总体结果

**[STATUS: PASS]**

## Findings
- `common/vm/facade.go` 已移除 `implementationSnapshot`、`acquireSnapshot`/`releaseSnapshot` 与热路径 `atomic.Add` 协议，`dispatchGetMemory*` / `dispatchGetMemoryRange` 已改为直接读取当前绑定实现。
- 运行时切换入口 `SwitchToLRU` / `SwitchToFull` / `SwitchToV3` / `CloseCurrent` 已从 facade 与相关测试中移除；`SetVMType()` 已收敛为初始化阶段绑定语义。
- `go test ./qa/issue160`、`go test ./common/vm/...`、`go test -tags vm_v3 ./common/vm/... ./common` 与至少一个 `testdata` 动态用例均通过。

## 详细结果

### TC-001: facade 热路径移除 snapshot 与 atomic.Add
- **结果**: PASS
- **实际行为**: 静态检查确认 `common/vm/facade.go` 不再包含 `implementationSnapshot`、`snapshotClosingBit`、`acquireSnapshot`、`releaseSnapshot`、`markSnapshotClosing`、`waitSnapshotDrained` 及 `state.Add(...)`；`dispatchGetMemory*` / `dispatchGetMemoryRange` 使用 `currentImplementation()` 直接分发。
- **关键指标**: `go test ./qa/issue160` 通过，其中 `TestFacadeHotPathDropsSnapshotAtomicProtocol` 通过。
- **备注**: 热路径仍保留 `atomic.Pointer` 读取当前 binding，但已移除每次访问的原子加减开销，符合 AC1 目标。

### TC-002: facade 不再暴露运行时切换和关闭入口
- **结果**: PASS
- **实际行为**: `common/vm/facade.go` 中已无 `SwitchToLRU`、`SwitchToFull`、`SwitchToV3`、`CloseCurrent`、`switchImplementation` 定义或调用；替代接口为初始化期 `SelectImplementation` + `Init` / `Close`。
- **关键指标**: `go test ./qa/issue160` 通过，其中 `TestFacadeRemovesRuntimeSwitchAndCloseEntrypoints` 通过。
- **备注**: API 语义已从“运行时切换”收敛为“初始化/收尾期生命周期控制”。

### TC-003: SetVMType 语义收敛为仅初始化阶段生效
- **结果**: PASS
- **实际行为**: `common/vm.go` 中 `SetVMType()` 仅调用 `mustApplyVMType()`，不再调用任何 `vm.SwitchTo*`；`initVirtualMemory()` 统一执行 `SetVMType(typ)` 后调用 `vm.Init()` 固定实现。
- **关键指标**: `go test ./qa/issue160` 通过，其中 `TestCompatLayerSetVMTypeStopsRuntimeSwitching` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过。
- **备注**: 当前实现对“请求类型不可绑定”采用显式 panic，避免静默偏离。

### TC-004: 遗留 lifecycle / dispatch 契约测试完成迁移
- **结果**: PASS
- **实际行为**: `common/vm/facade_lifecycle_test.go`、`common/vm/facade_dispatch_test.go`、`common/vm/v3_select_test.go`、`common/vm/init_v3_integration_test.go`、`common/vm_init_integration_test.go` 已迁移到静态绑定模型，不再依赖 runtime switch / snapshot 协议。
- **关键指标**: `go test ./common/vm/...` 通过；`go test -tags vm_v3 ./common/vm/... ./common` 通过；`go test ./qa/issue160` 中 `TestLegacySwitchLifecycleTestsAreRemovedOrReplaced` 通过。
- **备注**: 集成测试中已补足恢复全局状态和最小 `maps` 输入，避免因旧测试假设导致误报。

### Smoke Test: Python 真实 testdata 回归
- **结果**: PASS
- **实际行为**: 运行 `python3 testdata/run_test.py python/20260129-complex-types-311` 成功，Maze 分析输出与 `validate.py` 验证全部通过。
- **关键指标**: `pymempool_objects = 68626`；`PersonClass = 1000`；`GameEntity = 300`；`TreeNode = 600`；`SimpleClass = 500`；`Player = 500`；`Config = 300`；`Point = 400`；`Rectangle = 300`；集合对象总数 `2310`。
- **备注**: 满足“每次验收至少执行一个 testdata 用例”的要求，主链路未见回归。

## Test Results
- `./maze --build`：PASS
- `go test ./qa/issue160`：PASS
- `go test ./common/vm/...`：PASS
- `go test -tags vm_v3 ./common/vm/... ./common`：PASS
- `python3 testdata/run_test.py python/20260129-complex-types-311`：PASS

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- TC-005：根据 `.symphony_prompt.md` 中的人类指引“可以忽略 TC-005: 需求与性能验收目标保持一致”，本轮未独立复做 `pprof` 与 `refs=v0/v1` 端到端性能复测。
- 影响范围：AC4、AC5 未由本轮 QA 独立重跑验证；当前 PASS 结论基于 AC1-AC3 的代码/测试验收以及开发报告中给出的性能复测记录，不构成本轮独立性能背书。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 5 / 5（含 4 个计划 TC + 1 个实际 smoke test）
- 失败: 0 / 5
- 建议: 可以合并当前实现；若发布前需要完整闭环 AC4/AC5，建议后续单独补做独立 `pprof` 与 `refs=v0/v1` 复测，但这不属于本轮已被允许忽略的 TC-005 范围。
