# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮按 `.symphony_prompt.md` 的“QA retry #2，重点关注既往问题”执行复验。

结合最新开发报告 `dev-flow/20260307135957-dev-report.md`，本次重点复核此前唯一失败项 **AC7 / TC-009（交付边界）**，并同时确认交付文档在干净分支 `tmp/issue-35-clean` / `issue-35-clean-docs` 中仍保持与此前通过 AC1 ~ AC6 的版本一致。

复核结论：

- 当前主工作区分支 `symphony/35-common-resultitem` 依然是脏分支，不能作为 #35 的验收对象；
- 但开发报告声明的实际交付分支 `issue-35-clean-docs` 已存在，且其 `origin/master...HEAD` 三点 diff 仅包含 `dev-log/2026-03-07-common-resultitem-analysis.md` 一个交付文件；
- 干净分支中的交付文档与当前工作区中的同名文档内容一致；
- 因而此前 TC-009 的失败原因已被修复，按“以干净分支实际 HEAD 重新发起 QA”的要求，本次验收通过。

## 详细结果

### TC-001: ResultItem 定义与字段说明完整性
- **结果**: PASS
- **实际行为**: 干净分支中的 `dev-log/2026-03-07-common-resultitem-analysis.md` 与此前已验收版本一致，仍明确指出 `common/result_items.go` 是 `ResultItem` 定义位置，并覆盖 `Amount`、`Sizeof`、`Objects`、`ObjSize` 四个核心字段。
- **关键指标**:
  - 文档一致性检查：clean worktree 与当前工作区同名文件内容一致（`cmp` 返回 0）
  - 交付文件：`dev-log/2026-03-07-common-resultitem-analysis.md`
- **备注**: 本轮聚焦复验 TC-009，TC-001 内容未回退、未劣化。

### TC-002: ResultItem 生命周期函数覆盖
- **结果**: PASS
- **实际行为**: 交付文档仍覆盖 `NewResultItem`、`GetResultItem`、`AddToResultItems`、`MergeResultItems`、`DumpResultItems` 等生命周期函数。
- **关键指标**:
  - 交付边界修复后，文档主体未发生不一致
- **备注**: 延续上一轮对 AC2 的通过结论。

### TC-003: WebUI 一级/二级结果链路说明
- **结果**: PASS
- **实际行为**: 文档仍包含 `SortByValue`、`PrintSortedResults`、`PrintObjects`、`SortedResultMap`、`GlobalS` 的链路说明。
- **关键指标**:
  - clean 分支唯一交付文件未发生内容缺失
- **备注**: 延续上一轮对 AC3 的通过结论。

### TC-004: 上层回调扩展点说明
- **结果**: PASS
- **实际行为**: 文档仍说明 `common/callback.go` 的扩展点，以及 `FormatResultType`、`PrintObjectsNormal`、`SamplePostmanObjects`、`ShowGraphG` 等回调绑定关系。
- **关键指标**:
  - clean 分支交付文件内容与已验收版本一致
- **备注**: 延续上一轮对 AC4 的通过结论。

### TC-005: Python 接入点覆盖
- **结果**: PASS
- **实际行为**: 文档仍覆盖 Python 的写入路径、回调绑定与二级结果消费。
- **关键指标**:
  - 交付文档一致性：通过
- **备注**: 延续上一轮对 AC5（Python 部分）的通过结论。

### TC-006: Node.js 接入点覆盖
- **结果**: PASS
- **实际行为**: 文档仍覆盖 Node.js 通过 `AddResult(do)` 接入 `ResultItem` 的方式及展示回调绑定。
- **关键指标**:
  - 交付文档一致性：通过
- **备注**: 延续上一轮对 AC5（Node.js 部分）的通过结论。

### TC-007: Txos 接入点覆盖
- **结果**: PASS
- **实际行为**: 文档仍覆盖 Txos 对 `AddToResultItems` / `tryMergeAddToResultItems` 的使用，以及 `GlobalS` / `ObjSize` 相关的二级展示路径。
- **关键指标**:
  - 交付文档一致性：通过
- **备注**: 延续上一轮对 AC5（Txos 部分）的通过结论。

### TC-008: 输出链路完整性说明
- **结果**: PASS
- **实际行为**: 文档仍覆盖文本模式、WebUI、Postman 三条输出链路如何消费 `ResultItem`。
- **关键指标**:
  - 交付文档一致性：通过
- **备注**: 延续上一轮对 AC6 的通过结论。

### TC-009: 非功能性交付边界说明
- **结果**: PASS
- **实际行为**: 在 `tmp/issue-35-clean` worktree 中检查实际交付分支 `issue-35-clean-docs`，`git diff --stat origin/master...HEAD` 仅显示 `dev-log/2026-03-07-common-resultitem-analysis.md` 一个文件；`git diff --name-only origin/master...HEAD` 也仅列出该文件；该分支当前跟踪 `origin/master`，满足“交付物以分析文档为主，不新增功能实现”的 AC7 要求。
- **关键指标**:
  - 验收分支：`issue-35-clean-docs`
  - 上游：`origin/master`
  - 三点 diff 文件数：1
  - diff 统计：`dev-log/2026-03-07-common-resultitem-analysis.md | 471 +++++++++++++++++++++++++++++++++++++++++++++`
- **备注**: 之前失败的是主工作区脏分支；本轮按开发报告声明的“实际可送审分支”复核，问题已修复。

## 失败的 Test Case（如有）

无。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [ ] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 总结
- 通过: 9 / 9
- 失败: 0 / 9
- 建议: 可按 `issue-35-clean-docs` 作为 #35 的实际交付分支继续后续流程；不要再以 `symphony/35-common-resultitem` 这个混有无关历史改动的主工作区分支送审。
