# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮围绕 issue #156（统一 refs API、删除 Python 专用通道）完成了阶段 A 静态核对、阶段 B 构建与关键 smoke test，以及阶段 C 关键回归。未发现与需求文档、test case 或此前失败点相关的残留问题。

## Findings
- 未发现 `PythonGetReferrersFast`、`PythonAddReverseIndex`、`PythonResetRefs`、`PythonForEachReverseIndex`、`PythonReverseIndexStats` 在实现层残留。
- 默认 refs 绑定已由混合模式 `v1+py-v0` 收敛为单一全局版本；默认 `v1`，显式可切换 `refs_v0` / `refs_v1`。
- Python 主链路已改为普通 `GetReferrers` / `GAddReverseIndex`，`python/traverse.go` 中旧的 reverse-index LevelDB 导出逻辑已删除。
- `refs_v0` 与 `refs_v1` 下 `python/20260129-complex-types-311` 动态回归均通过，说明统一 API 后 Python 主链路未回归。

## 详细结果

### TC-001: common 兼容层仅暴露统一 refs API
- **结果**: PASS
- **实际行为**: `common/refs.go` 仅保留 `AddIndexes`、`GAddReverseIndex`、`AddIndex`、`GetReferrers`、`GetReferents`、`SaveRefIndex`、`CheckRefs` 等统一入口，不再导出 Python 专用 refs API。
- **关键指标**: `go test ./qa/issue156 -run TestCommonRefsOnlyExposeUnifiedAPI` 通过。
- **备注**: 与 AC1、AC3 一致。

### TC-002: facade/compat 层删除 Python 专用槽位
- **结果**: PASS
- **实际行为**: `common/refs/facade.go`、`compat_bridge.go`、`compatcommon/compat.go` 中 Python 专用函数槽位与转发已删除；`InitCompat()` 仅执行配置、模式同步与 `Init()`，不再调用 `PythonResetRefs()`。
- **关键指标**: `go test ./qa/issue156 -run TestFacadeAndCompatBridgeDropPythonSpecificHooks` 通过。
- **备注**: 代码阅读与契约测试结果一致。

### TC-003: refs 版本选择切换为单一全局版本
- **结果**: PASS
- **实际行为**: `select_default.go` 默认版本为 `v1`；`select_v0.go`/`select_v1.go` 仅绑定单一后端；未见 `v1+py-v0` 混合绑定残留。
- **关键指标**: `go test ./qa/issue156 -run TestRefsVersionSelectionUsesSingleBackendVersion` 通过。
- **备注**: 满足 AC4。

### TC-004: v0/v1 实现仅暴露统一公共合约
- **结果**: PASS
- **实际行为**: `common/refs/v0`、`common/refs/v1` 均保留统一 API；`ForEachReverseIndex`、`Stats`、`ResetRefs` 等旧 reverse-index 导出接口已删除。
- **关键指标**: `go test ./qa/issue156 -run TestRefsImplementationsExposeOnlyUnifiedContract` 通过；`go test ./common/refs/v0 ./common/refs/v1` 通过。
- **备注**: 满足 AC1、AC4。

### TC-005: Python 主链路改用普通 refs API 并移除 LevelDB 导出逻辑
- **结果**: PASS
- **实际行为**: `python/refs_shim.go` 使用 `GetReferrers` 与 `GAddReverseIndex`；`python/python.go` 已删除 `PythonResetRefs()` 初始化；`python/traverse.go` 不再包含 `GenerateLevelDBFile` 与 Python reverse-index 导出逻辑。
- **关键指标**:
  - `go test ./qa/issue156 -run TestPythonPackageUsesGenericRefsEntrypoints` 通过
  - `GOFLAGS='-tags=refs_v0' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311` 通过
  - `GOFLAGS='-tags=refs_v1' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311` 通过
- **备注**: 这是本轮最关键主链路验证项，直接覆盖 AC2、AC5、AC6。

### TC-006: 文档与历史 QA 契约同步更新
- **结果**: PASS
- **实际行为**: `doc/refs.md` 已改写为统一 refs API 文档；`qa/issue144/refs_param_name_test.go` 中旧 Python fast path 契约已移除。
- **关键指标**:
  - `go test ./qa/issue156 -run TestDocumentationAndLegacyQAExpectationsUpdated` 通过
  - `go test ./qa/issue144` 通过
- **备注**: 文档与测试契约已同步。

## Test Results
- `./maze --build`：通过
- `go test ./qa/issue156`：通过
- `go test ./qa/issue144`：通过
- `go test ./common/refs/v0 ./common/refs/v1`：通过
- `GOFLAGS='-tags=refs_v0' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `GOFLAGS='-tags=refs_v1' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）
- 无

## 未执行项（如有）
- Python 2.7 兼容性未执行：本轮需求聚焦 refs API 收敛，且 test case 未提供对应 Python 2.7 用例；对 AC1-AC6 的主结论无阻塞。
- 不同 glibc 版本验证未执行：当前工作树仅完成单环境验收。
- 不同 Node.js 版本验证未执行：本轮需求的关键改动点在 Python refs 主链路，未额外扩展 Node.js 动态回归。
- 大内存性能验证未执行：需求与 test case 未将性能设为本轮阻塞项。

## 兼容性验证
- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）
- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 日志检查
- `maze.log` 已生成，未发现与 issue #156 相关的新 fatal / panic；Python 主链路分析完成且测试通过。
- `maze.py.log` 本轮运行后未生成，未见因此导致测试失败；当前不构成本需求阻塞。

## 总结
- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以通过本轮验收。就 issue #156 而言，Python 专用 refs 通道已完成下线，统一 refs API 与单一版本绑定策略已正确落地。
