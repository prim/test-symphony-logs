# 开发报告

## 完成的任务
- [x] Task 1: 删除 common/refs 与 facade/compat 层中的 Python 专用 refs API
- [x] Task 2: 删除默认混合绑定，refs 版本切换统一为单一全局后端
- [x] Task 3: 清理 v0/v1 中废弃的 reverse-index 专用导出函数
- [x] Task 4: Python 主链路改为普通 GetReferrers / AddReverseIndex
- [x] Task 5: 删除 python/traverse.go 中的 reverse index LevelDB 导出逻辑
- [x] Task 6: 更新 refs 文档与历史 QA 契约测试
- [x] Task 7: 完成编译、静态测试与 refs_v0/refs_v1 动态回归

## 修改的文件
- `common/refs.go`: 删除 Python 专用 facade 转发，仅保留统一 refs API
- `common/refs/facade.go`: 删除 Python 专用槽位与转发，InitCompat 不再执行 Python 重置
- `common/refs/compat_bridge.go`: 删除 Python 专用 compat bridge 转发
- `common/refs/compatcommon/compat.go`: 删除 Python 专用 compat API 字段与辅助函数
- `common/refs/facade_contract_test.go`: 同步门面绑定契约，去掉 Python 专用实现检查
- `common/refs/select_default.go`: 默认版本切换为单一 `v1`
- `common/refs/select_v0.go`: 删除 Python 专用混合绑定
- `common/refs/select_v1.go`: 显式 `v1` 模式改为单一 `v1`
- `common/refs/v0/refs.go`: 删除废弃的 reverse-index 遍历/统计导出
- `common/refs/v0/refs_test.go`: 删除对已废弃 `Stats()` 的测试依赖
- `common/refs/v1/refs.go`: 删除废弃的 reverse-index 遍历/统计导出
- `python/refs_shim.go`: 切换到 `GetReferrers` 与 `GAddReverseIndex`
- `python/python.go`: 删除初始化阶段 `PythonResetRefs()`
- `python/traverse.go`: 删除 Python reverse-index 导出到 LevelDB 的历史逻辑
- `qa/issue144/refs_param_name_test.go`: 删除 Python fast path 的历史旧契约测试
- `doc/refs.md`: 重写为统一 refs API 文档
- `dev-log/2026-03-16-refs-api-unification.md`: 记录本次重构方案、AC 与验证结果

## 自测结果
- 测试命令: `go test ./qa/issue156 -run TestCommonRefsOnlyExposeUnifiedAPI`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue156 -run TestFacadeAndCompatBridgeDropPythonSpecificHooks`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue156 -run TestRefsVersionSelectionUsesSingleBackendVersion`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue156 -run TestRefsImplementationsExposeOnlyUnifiedContract`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue156 -run TestPythonPackageUsesGenericRefsEntrypoints`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue156 -run TestDocumentationAndLegacyQAExpectationsUpdated`
- 测试结果: 通过
- 测试命令: `go test ./qa/issue144`
- 测试结果: 通过
- 测试命令: `go test ./common/refs/v0 ./common/refs/v1`
- 测试结果: 通过
- 测试命令: `./maze --build`
- 测试结果: 通过
- 测试命令: `GOFLAGS='-tags=refs_v0' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，`validate.py` 全部断言通过
- 测试命令: `GOFLAGS='-tags=refs_v1' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`
- 测试结果: 通过，`validate.py` 全部断言通过

## 验收核对
- AC1: PASS 删除 `PythonGetReferrersFast`、`PythonAddReverseIndex`、`PythonResetRefs`、`PythonForEachReverseIndex`、`PythonReverseIndexStats`
- AC2: PASS Python 初始化不再调用 `PythonResetRefs()`，统一依赖 `Init()`
- AC3: PASS facade/compat 层已删除 Python 专用槽位、字段与转发逻辑
- AC4: PASS 默认版本改为 `v1`，显式版本为 `v0` / `v1`，不再存在 `v1+py-v0`
- AC5: PASS 已删除 `python/traverse.go` 中基于 reverse index 的 LevelDB 导出逻辑
- AC6: PASS 已验证 `refs_v0` 与 `refs_v1` 下 `python/20260129-complex-types-311` 均通过动态回归

## 剩余工作
- 未完成项: 无
- 未验证项: 无

## Self-Review 发现与修复
- 发现 `python/traverse.go` 删除历史逻辑后，调试分支原先依赖专用 reverse-index 遍历；已改为通过统一 `GetReferrers` + GC 遍历统计，避免引用已删除 API
- 发现 `common/refs/v0/refs_test.go` 依赖已废弃 `Stats()`；已同步改写测试，保持 v0 合约验证但不再绑定历史实现细节
- 发现文档仍描述 Python 专用 fast path 与混合绑定；已整体重写为统一 refs API 说明

## 注意事项
- 本轮未修改任何 `testdata/` 测试文件，仅通过源代码修复满足既有测试
- `testdata` 子模块原先为空，已先执行 `git submodule update --init --recursive testdata` 后完成动态回归
- 旧的 Python reverse-index 裸数据导出路径已经移除；若仓库外脚本依赖旧 `maze.db.*` 数据格式，需要额外迁移评估

## 结论
- COMPLETE
