# QA 验收报告

## QA Result: PASS

## 总体结果

**[STATUS: PASS]**

本轮针对 issue #156 的重试验收已完成。结合需求文档、test case、最新开发报告、代码 diff、静态契约测试、构建验证以及 Python 主链路动态回归，未发现阻塞 AC1-AC6 的残留问题；此前代码审查中提出的 `refs=v1` 热路径性能风险，本轮已通过新增查询缓存实现进行补强，相关单测与动态回归均通过。

## Findings

- 已确认 Python 专用 refs API 已从 `common/refs.go`、facade、compat 层以及 `v0/v1` 后端导出面删除。
- 已确认默认版本绑定已从 `refs=v1+py-v0` 收敛为单一全局版本；默认 `v1`，显式可切换 `refs_v0` / `refs_v1`。
- 已确认 Python 主链路统一走普通 `GetReferrers` / `GAddReverseIndex`，`python/traverse.go` 中历史 reverse-index LevelDB 导出逻辑已删除。
- 已确认 `common/refs/v1` 新增分片查询缓存，并在 `AddIndex` / `AddReverseIndex` 写路径同步预热缓存，覆盖此前审查指出的交互式反向引用查询热路径性能风险。
- `refs_v0` 与 `refs_v1` 下同一 Python testdata 动态回归均通过，说明统一 API 后主链路结果保持正确。

## 详细结果

### TC-001: common 兼容层仅暴露统一 refs API
- **结果**: PASS
- **实际行为**: `common/refs.go` 仅保留 `AddIndexes`、`GAddReverseIndex`、`AddIndex`、`GetReferrers`、`GetReferents`、`SaveRefIndex`、`CheckRefs` 等统一入口，不再导出 `Python*` refs API。
- **关键指标**: `go test ./qa/issue156 -run TestCommonRefsOnlyExposeUnifiedAPI` 通过。
- **备注**: 满足 AC1、AC3。

### TC-002: facade/compat 层删除 Python 专用槽位
- **结果**: PASS
- **实际行为**: `common/refs/facade.go`、`common/refs/compat_bridge.go`、`common/refs/compatcommon/compat.go` 中 Python 专用槽位与转发逻辑均已移除；`InitCompat()` 不再调用 `PythonResetRefs()`。
- **关键指标**: `go test ./qa/issue156 -run TestFacadeAndCompatBridgeDropPythonSpecificHooks` 通过。
- **备注**: 满足 AC2、AC3。

### TC-003: refs 版本选择切换为单一全局版本
- **结果**: PASS
- **实际行为**: `select_default.go` 默认版本为 `v1`；`select_v0.go` / `select_v1.go` 仅绑定单一后端；未发现 `v1+py-v0` 混合绑定残留。
- **关键指标**: `go test ./qa/issue156 -run TestRefsVersionSelectionUsesSingleBackendVersion` 通过。
- **备注**: 满足 AC4。

### TC-004: v0/v1 实现仅暴露统一公共合约
- **结果**: PASS
- **实际行为**: `common/refs/v0`、`common/refs/v1` 均仅保留统一公共合约；`ForEachReverseIndex`、`Stats`、`ResetRefs` 等旧 reverse-index API 已删除。
- **关键指标**:
  - `go test ./qa/issue156 -run TestRefsImplementationsExposeOnlyUnifiedContract` 通过
  - `go test ./common/refs/v0 ./common/refs/v1` 通过
- **备注**: 满足 AC1、AC4。

### TC-005: Python 主链路改用普通 refs API 并移除 LevelDB 导出逻辑
- **结果**: PASS
- **实际行为**: `python/refs_shim.go` 已改用 `GetReferrers` 与 `GAddReverseIndex`；`python/python.go` 已删除 `PythonResetRefs()` 初始化；`python/traverse.go` 不再包含 `GenerateLevelDBFile` 与 Python reverse-index 导出逻辑。
- **关键指标**:
  - `go test ./qa/issue156 -run TestPythonPackageUsesGenericRefsEntrypoints` 通过
  - `GOFLAGS='-tags=refs_v0' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311` 通过
  - `GOFLAGS='-tags=refs_v1' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311` 通过
- **备注**: 该项直接覆盖 AC2、AC5、AC6，是本轮最关键动态验收项。

### TC-006: 文档与历史 QA 契约同步更新
- **结果**: PASS
- **实际行为**: `doc/refs.md` 已更新为统一 refs API 文档；`qa/issue144/refs_param_name_test.go` 中旧 Python fast path 契约已删除。
- **关键指标**:
  - `go test ./qa/issue156 -run TestDocumentationAndLegacyQAExpectationsUpdated` 通过
  - `go test ./qa/issue144` 通过
- **备注**: 文档与 QA 契约已与设计保持一致。

## Test Results

- `./maze --build`：通过
- `go test ./qa/issue156 ./qa/issue144 ./common/refs/v0 ./common/refs/v1`：通过
- `GOFLAGS='-tags=refs_v0' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`：通过
- `GOFLAGS='-tags=refs_v1' ./maze --build && python3 testdata/run_test.py python/20260129-complex-types-311`：通过

## 失败的 Test Case（如有）

- 无

## 未执行项（如有）

- Python 2.7 兼容性未执行：本轮已完成 Python 3.11 主链路动态回归，但未补充 Python 2.7 专项 testdata；对 AC1-AC6 主结论无阻塞。
- 不同 glibc 版本验证未执行：当前仅在单一 Linux 环境完成验收。
- 不同 Node.js 版本验证未执行：需求核心聚焦 Python 专用 refs API 下线与统一 API 收敛，本轮未额外做 Node.js 动态回归。
- 大对象图/大内存定量性能验证未执行：本轮验证到 `refs=v1` 热路径缓存实现与相关回归测试已通过，但未执行 >1GB 进程性能基准。

## 兼容性验证

- [ ] Python 2.7 测试通过
- [x] Python 3.x 测试通过
- [ ] 不同 glibc 版本测试通过
- [ ] 不同 Node.js 版本测试通过

## 性能验证（如适用）

- [ ] 大内存进程（>1GB）扫描时间 < 30秒
- [ ] 内存占用 < 分析目标内存的 10%

## 日志检查

- `maze.log` 中未发现与 issue #156 直接相关的新 fatal / panic。
- 日志中仍可见若干已有的 GDB / Cpp 调试信息与 `AttributeError: module 'gdb' has no attribute 'all_variables_symbol'`，但本次 Python testdata 验证已完整通过，且未体现为 refs API 统一后的功能回归阻塞。
- 本轮动态回归的 `maze-result.json` 通过 `validate.py` 全部断言。

## 总结

- 通过: 6 / 6
- 失败: 0 / 6
- 建议: 可以通过本轮验收。issue #156 的核心目标——删除 Python 专用 refs 通道、切换为统一公共 refs API、移除混合版本绑定、下沉持久化职责，并保证 `refs_v0` / `refs_v1` 下 Python 主链路正确——均已验证通过。
